import { createRequire as __createRequire } from 'node:module'; const require = __createRequire(import.meta.url);

// node_modules/@cristianormazabal/triton-core/dist/index.js
var CONNECTOR_ANIMATIONS = [
  "march",
  "particle",
  "draw",
  "pulse",
  "glow",
  "comet",
  "stream",
  "flow",
  "colorcycle"
];
function isRenderedConnectorAnimation(value) {
  return typeof value === "string" && CONNECTOR_ANIMATIONS.includes(value);
}
function ok(value) {
  return { ok: true, value };
}
function err(code, message, cause) {
  return {
    ok: false,
    error: { code, message, ...cause !== void 0 ? { cause } : {} }
  };
}
var MERMAID_PATTERNS = [
  [/^(flowchart|graph)\s/i, "flowchart"],
  [/^sequenceDiagram/i, "sequence"],
  [/^timeline/i, "timeline"],
  [/^mindmap/i, "mindmap"],
  [/^stateDiagram/i, "state"],
  [/^classDiagram/i, "class"],
  [/^erDiagram/i, "er"],
  [/^gantt/i, "gantt"],
  [/^journey/i, "journey"],
  [/^C4(Context|Container|Component|Dynamic|Deployment)/i, "c4"],
  [/^sankey/i, "sankey"],
  [/^kanban/i, "kanban"],
  [/^requirementDiagram\b/i, "requirement"],
  [/^gitGraph/i, "gitgraph"],
  [/^packet/i, "packet"],
  [/^pie\b/i, "pie"],
  [/^xychart(-beta)?/i, "xychart"],
  [/^quadrantChart/i, "quadrant"],
  [/^radar(-beta)?/i, "radar"],
  [/^block/i, "block"],
  [/^architecture/i, "architecture"],
  [/^poster/i, "poster"],
  [/^tree\b/i, "tree"],
  [/^plan\b/i, "plan"],
  [/^avl\b/i, "avl"],
  [/^rbtree\b/i, "rbtree"],
  [/^b\+tree\b/i, "bplustree"],
  [/^bplustree\b/i, "bplustree"],
  [/^btree\b/i, "btree"],
  [/^radix\b/i, "radix"],
  [/^segtree\b/i, "segtree"],
  [/^heap\b/i, "heap"],
  [/^array\b/i, "array"],
  [/^linkedlist\b/i, "linkedlist"],
  [/^memory\b/i, "memory"],
  [/^page\b/i, "page"],
  [/^cqueue\b/i, "cqueue"],
  [/^deque\b/i, "deque"],
  [/^pqueue\b/i, "pqueue"],
  [/^queue\b/i, "queue"],
  [/^stack\b/i, "stack"],
  [/^hashmap\b/i, "hashmap"],
  [/^matrix\b/i, "matrix"],
  [/^trie\b/i, "trie"],
  // `graph` is owned by Mermaid flowchart (`graph TD`), so the data-structure
  // graph uses a distinct, unambiguous keyword instead.
  [/^nodegraph\b/i, "nodegraph"],
  [/^dsgraph\b/i, "nodegraph"],
  [/^unionfind\b/i, "unionfind"],
  [/^dsu\b/i, "unionfind"],
  [/^topology\b/i, "topology"],
  [/^list\b/i, "list"],
  [/^fishbone\b/i, "fishbone"],
  [/^(pyramid|funnel)\b/i, "pyramid"],
  [/^(loop|flywheel)\b/i, "loop"],
  [/^merkletree\b/i, "merkletree"],
  [/^lsmtree\b/i, "lsmtree"],
  [/^behaviortree\b/i, "behaviortree"],
  [/^quadtree\b/i, "quadtree"],
  [/^treap\b/i, "treap"],
  [/^(2-3-4tree|234tree)\b/i, "234tree"]
];
function matchMermaid(text) {
  for (const [pattern, type] of MERMAID_PATTERNS) {
    if (pattern.test(text)) return type;
  }
  return void 0;
}
function detect(input) {
  const trimmed = input.trimStart();
  if (trimmed.startsWith("---")) {
    const fm = trimmed.match(/^---\r?\n[\s\S]*?\r?\n---\r?\n?/);
    if (fm) {
      const body = trimmed.slice(fm[0].length).trimStart();
      const bodyType = matchMermaid(body);
      if (bodyType) return { format: "mermaid", diagramType: bodyType };
      const typeInFm = fm[0].match(/^\s*type:\s*(\w+)/m);
      if (typeInFm) return { format: "yaml", diagramType: typeInFm[1] };
    }
    const typeMatch = trimmed.match(/^type:\s*(\w+)/m);
    return { format: "yaml", diagramType: typeMatch?.[1] ?? "flowchart" };
  }
  if (/^type:\s/.test(trimmed)) {
    const typeMatch = trimmed.match(/^type:\s*(\w+)/);
    return { format: "yaml", diagramType: typeMatch?.[1] ?? "flowchart" };
  }
  return { format: "mermaid", diagramType: matchMermaid(trimmed) ?? "flowchart" };
}
function stripComments(input) {
  const trimmed = input.trimStart();
  if (/^type:\s/.test(trimmed)) return input;
  if (trimmed.startsWith("---")) {
    const fmMatch = trimmed.match(/^---\r?\n[\s\S]*?\r?\n---\r?\n?/);
    if (fmMatch) {
      const leadingWs = input.slice(0, input.length - trimmed.length);
      const frontmatter = fmMatch[0];
      const body = trimmed.slice(frontmatter.length);
      return leadingWs + frontmatter + removeCommentLines(body);
    }
    return input;
  }
  return removeCommentLines(input);
}
function removeCommentLines(text) {
  return text.split("\n").filter((line) => !/^\s*%%/.test(line)).join("\n");
}
var registry = /* @__PURE__ */ new Map();
function registerDiagram(kind, module) {
  registry.set(kind, module);
}
function getModule(kind) {
  return registry.get(kind);
}
var registry2 = /* @__PURE__ */ new Map();
function registerRenderer(renderer) {
  registry2.set(renderer.name, renderer);
}
function getRenderer(name) {
  return registry2.get(name);
}
var SANS = "Inter, system-ui, -apple-system, sans-serif";
var SERIF = 'Georgia, "Times New Roman", serif';
var MONO = "JetBrains Mono, Fira Code, monospace";
var baseTypography = {
  fontFamily: SANS,
  monoFamily: MONO,
  baseFontSize: 14,
  titleFontSize: 18,
  smallFontSize: 11,
  lineHeight: 1.4
};
var baseSpacing = {
  unit: 8,
  nodePadding: 12,
  nodeGap: 40,
  diagramMargin: 24
};
var baseEdges = {
  strokeWidth: 1.5,
  arrowSize: 8,
  labelFontSize: 12,
  curveTension: 0.4
};
var basePanel = {
  titleAlign: "left",
  titlePosition: "inside",
  titleChrome: "none"
};
var baseNodes = {
  standard: {
    borderWidth: 1.5,
    cornerRadius: 6,
    padding: 12
  },
  leaf: {
    borderWidth: 1.2,
    cornerRadius: 18,
    padding: 10
  },
  datastore: {
    borderWidth: 1.5,
    padding: 12
  }
};
var defaultTheme = {
  name: "default",
  palette: {
    primary: "#4A90D9",
    secondary: "#7C3AED",
    background: "#FFFFFF",
    surface: "#F8FAFC",
    border: "#CBD5E1",
    text: "#1E293B",
    textMuted: "#64748B",
    success: "#22C55E",
    warning: "#F59E0B",
    error: "#EF4444"
  },
  typography: baseTypography,
  spacing: baseSpacing,
  edges: baseEdges,
  panel: basePanel,
  nodes: baseNodes
};
var executiveTheme = {
  name: "executive",
  palette: {
    primary: "#3A86C8",
    secondary: "#7986CB",
    background: "#0D1B2A",
    surface: "#16263B",
    border: "#2A4060",
    text: "#E8EEF5",
    textMuted: "#8BAAC8",
    success: "#2DD4BF",
    warning: "#F4A742",
    error: "#E05252"
  },
  typography: { ...baseTypography, fontFamily: SERIF },
  spacing: baseSpacing,
  edges: baseEdges,
  panel: { titleAlign: "center", titlePosition: "on-border", titleChrome: "box" },
  nodes: baseNodes
};
var minimalTheme = {
  name: "minimal",
  palette: {
    primary: "#525252",
    secondary: "#A3A3A3",
    background: "#FFFFFF",
    surface: "#F5F5F5",
    border: "#D4D4D4",
    text: "#171717",
    textMuted: "#737373",
    success: "#737373",
    warning: "#A3A3A3",
    error: "#404040"
  },
  typography: baseTypography,
  spacing: baseSpacing,
  edges: baseEdges,
  panel: basePanel,
  nodes: baseNodes
};
var bwLightTheme = {
  name: "bw-light",
  palette: {
    primary: "#525252",
    secondary: "#A3A3A3",
    background: "#FFFFFF",
    surface: "#F5F5F5",
    border: "#D4D4D4",
    text: "#171717",
    textMuted: "#737373",
    success: "#737373",
    warning: "#A3A3A3",
    error: "#404040"
  },
  typography: {
    ...baseTypography,
    baseFontSize: 13
  },
  spacing: baseSpacing,
  edges: baseEdges,
  panel: basePanel,
  nodes: baseNodes
};
var bwDarkTheme = {
  name: "bw-dark",
  palette: {
    primary: "#D4D4D4",
    secondary: "#8B8B8B",
    background: "#171717",
    surface: "#262626",
    border: "#3F3F3F",
    text: "#FAFAFA",
    textMuted: "#A3A3A3",
    success: "#A3A3A3",
    warning: "#8B8B8B",
    error: "#E5E5E5"
  },
  typography: {
    ...baseTypography,
    baseFontSize: 13
  },
  spacing: baseSpacing,
  edges: baseEdges,
  panel: basePanel,
  nodes: baseNodes
};
var consultingTheme = {
  name: "consulting",
  palette: {
    primary: "#1F497D",
    secondary: "#D97706",
    background: "#FFFFFF",
    surface: "#F4F6FA",
    border: "#D6DEE8",
    text: "#111111",
    textMuted: "#555555",
    success: "#1F8A5B",
    warning: "#D97706",
    error: "#DC2626"
  },
  typography: { ...baseTypography, fontFamily: SERIF },
  spacing: baseSpacing,
  edges: baseEdges,
  panel: { titleAlign: "left", titlePosition: "on-border", titleChrome: "none" },
  nodes: baseNodes
};
var productTheme = {
  name: "product",
  palette: {
    primary: "#3B82F6",
    secondary: "#8B5CF6",
    background: "#FFFFFF",
    surface: "#F9FAFB",
    border: "#E5E7EB",
    text: "#111827",
    textMuted: "#6B7280",
    success: "#10B981",
    warning: "#F59E0B",
    error: "#EF4444"
  },
  typography: baseTypography,
  spacing: baseSpacing,
  edges: baseEdges,
  panel: { titleAlign: "center", titlePosition: "inside", titleChrome: "pill" },
  nodes: baseNodes
};
var releaseTheme = {
  name: "release",
  palette: {
    primary: "#2563EB",
    secondary: "#16A34A",
    background: "#FFFFFF",
    surface: "#F8FAFC",
    border: "#E2E8F0",
    text: "#111111",
    textMuted: "#6B7280",
    success: "#16A34A",
    warning: "#D97706",
    error: "#DC2626"
  },
  typography: baseTypography,
  spacing: baseSpacing,
  edges: baseEdges,
  panel: { titleAlign: "left", titlePosition: "inside", titleChrome: "box" },
  nodes: baseNodes
};
var aiTimelineTheme = {
  name: "ai-timeline",
  palette: {
    primary: "#5B4FCF",
    secondary: "#0EA5A8",
    background: "#F8F9FF",
    surface: "#FFFFFF",
    border: "#C0C4DF",
    text: "#1A1A2E",
    textMuted: "#4A4A6A",
    success: "#2D9E67",
    warning: "#D97706",
    error: "#E05B5B"
  },
  typography: baseTypography,
  spacing: baseSpacing,
  edges: baseEdges,
  panel: { titleAlign: "left", titlePosition: "above", titleChrome: "none" },
  nodes: baseNodes
};
var byteByteGoTheme = {
  name: "bytebytego",
  palette: {
    primary: "#2DD4BF",
    secondary: "#4D9AFF",
    background: "#111827",
    surface: "#1F2937",
    border: "#374151",
    text: "#F9FAFB",
    textMuted: "#9CA3AF",
    success: "#2DD4BF",
    warning: "#FFB800",
    error: "#FF4D6D"
  },
  typography: baseTypography,
  spacing: baseSpacing,
  edges: baseEdges,
  panel: { titleAlign: "center", titlePosition: "on-border", titleChrome: "pill" },
  nodes: baseNodes
};
var gitlineTheme = {
  name: "gitline",
  palette: {
    primary: "#4D9AFF",
    secondary: "#2C6EA8",
    background: "#0A1628",
    surface: "#132035",
    border: "#1C3558",
    text: "#E8EDF5",
    textMuted: "#6B8FAF",
    success: "#3FB950",
    warning: "#D29922",
    error: "#F85149"
  },
  typography: baseTypography,
  spacing: baseSpacing,
  edges: baseEdges,
  panel: { titleAlign: "left", titlePosition: "on-border", titleChrome: "box" },
  nodes: baseNodes
};
var ourTimelineTheme = {
  name: "our-timeline",
  palette: {
    primary: "#1F497D",
    secondary: "#D97706",
    background: "#FFFFFF",
    surface: "#F4F6FA",
    border: "#CCCCCC",
    text: "#111111",
    textMuted: "#555555",
    success: "#1F8A5B",
    warning: "#D97706",
    error: "#DC2626"
  },
  typography: baseTypography,
  spacing: baseSpacing,
  edges: baseEdges,
  panel: { titleAlign: "center", titlePosition: "above", titleChrome: "none" },
  nodes: baseNodes
};
var subjectTimelineTheme = {
  name: "subject-timeline",
  palette: {
    primary: "#22D3EE",
    secondary: "#EC4899",
    background: "#1A1A2E",
    surface: "#2A2A48",
    border: "#3A3A5C",
    text: "#E8EEF4",
    textMuted: "#8A9BB0",
    success: "#34D399",
    warning: "#F59E0B",
    error: "#EC4899"
  },
  typography: baseTypography,
  spacing: baseSpacing,
  edges: baseEdges,
  panel: { titleAlign: "center", titlePosition: "on-border", titleChrome: "pill" },
  nodes: baseNodes
};
var showcaseTheme = {
  name: "showcase",
  palette: {
    primary: "#00D4FF",
    secondary: "#9B8FDF",
    background: "#0D1B2A",
    surface: "#132035",
    border: "#1E3A5A",
    text: "#E8EEF5",
    textMuted: "#7AACCC",
    success: "#00CC88",
    warning: "#FFB800",
    error: "#FF4D6D"
  },
  typography: baseTypography,
  spacing: baseSpacing,
  edges: baseEdges,
  panel: { titleAlign: "center", titlePosition: "on-border", titleChrome: "box" },
  nodes: baseNodes
};
var editorialTheme = {
  name: "editorial",
  palette: {
    primary: "#EB6C36",
    secondary: "#2E5AA8",
    background: "#F5F5F5",
    surface: "#ECECEC",
    border: "#BFC0C0",
    text: "#2D3142",
    textMuted: "#4F5D75",
    success: "#7C8F6F",
    warning: "#B8915A",
    error: "#9C6B50"
  },
  typography: {
    ...baseTypography,
    fontFamily: SANS,
    monoFamily: "Geist Mono, JetBrains Mono, Fira Code, monospace",
    titleFontSize: 20
  },
  spacing: baseSpacing,
  edges: baseEdges,
  panel: { titleAlign: "left", titlePosition: "inside", titleChrome: "none" },
  nodes: baseNodes
};
var editorialDarkTheme = {
  name: "editorial-dark",
  palette: {
    primary: "#F08A59",
    secondary: "#6A95D8",
    background: "#2D3142",
    surface: "#393E53",
    border: "#4F5D75",
    text: "#F5F5F5",
    textMuted: "#BFC0C0",
    success: "#9CAF8F",
    warning: "#D3AD7A",
    error: "#B88670"
  },
  typography: {
    ...baseTypography,
    fontFamily: SANS,
    monoFamily: "Geist Mono, JetBrains Mono, Fira Code, monospace",
    titleFontSize: 20
  },
  spacing: baseSpacing,
  edges: baseEdges,
  panel: { titleAlign: "left", titlePosition: "inside", titleChrome: "none" },
  nodes: baseNodes
};
var spectrumTheme = {
  name: "spectrum",
  palette: {
    primary: "#00D4B2",
    secondary: "#F72585",
    background: "#0B1120",
    surface: "#162032",
    border: "#27354A",
    text: "#F8FAFC",
    textMuted: "#94A3B8",
    success: "#10B981",
    warning: "#F59E0B",
    error: "#EF4444"
  },
  typography: baseTypography,
  spacing: baseSpacing,
  edges: baseEdges,
  panel: { titleAlign: "center", titlePosition: "on-border", titleChrome: "pill" },
  nodes: baseNodes
};
var THEMES = {
  default: defaultTheme,
  executive: executiveTheme,
  minimal: minimalTheme,
  editorial: editorialTheme,
  "editorial-dark": editorialDarkTheme,
  "bw-light": bwLightTheme,
  "bw-dark": bwDarkTheme,
  consulting: consultingTheme,
  product: productTheme,
  release: releaseTheme,
  "ai-timeline": aiTimelineTheme,
  bytebytego: byteByteGoTheme,
  gitline: gitlineTheme,
  "our-timeline": ourTimelineTheme,
  "subject-timeline": subjectTimelineTheme,
  showcase: showcaseTheme,
  spectrum: spectrumTheme
};
var themePresetNames = Object.freeze(Object.keys(THEMES));
function getThemePreset(name) {
  return name !== void 0 && THEMES[name] || defaultTheme;
}
function resolveTheme(input, base) {
  const baseNodes2 = base.nodes;
  const inputNodes = input.nodes;
  const nodes = baseNodes2 || inputNodes ? {
    standard: {
      ...baseNodes2?.standard ?? { borderWidth: 1.5, cornerRadius: 6, padding: 12 },
      ...inputNodes?.standard
    },
    leaf: {
      ...baseNodes2?.leaf ?? { borderWidth: 1.2, cornerRadius: 18, padding: 10 },
      ...inputNodes?.leaf
    },
    datastore: {
      ...baseNodes2?.datastore ?? { borderWidth: 1.5, padding: 12 },
      ...inputNodes?.datastore
    }
  } : void 0;
  return {
    name: input.name ?? base.name,
    palette: { ...base.palette, ...input.palette },
    typography: { ...base.typography, ...input.typography },
    spacing: { ...base.spacing, ...input.spacing },
    edges: { ...base.edges, ...input.edges },
    panel: { ...base.panel, ...input.panel },
    ...nodes !== void 0 ? { nodes } : {}
  };
}
function extractFrontmatter(input) {
  const m = input.match(/^\s*---\r?\n([\s\S]*?)\r?\n---\r?\n?/);
  if (!m) return { metadata: {}, body: input };
  const metadata = {};
  for (const line of (m[1] ?? "").split(/\r?\n/)) {
    const idx = line.indexOf(":");
    if (idx === -1) continue;
    const key = line.slice(0, idx).trim();
    const value = line.slice(idx + 1).trim().replace(/^["']|["']$/g, "");
    if (key) metadata[key] = value;
  }
  return { metadata, body: input.slice(m[0].length) };
}
var PREFIX_RE = /^[a-z][a-z0-9-]*$/;
var NAME_RE = /^[a-z0-9][a-z0-9-]*$/;
var DEFAULT_WIDTH = 16;
var DEFAULT_HEIGHT = 16;
var DEFAULT_LEFT = 0;
var DEFAULT_TOP = 0;
var MAX_ALIAS_DEPTH = 4;
function parseIconRef(token) {
  if (typeof token !== "string" || token.length === 0) {
    return err(
      "ICON_NOT_FOUND",
      `Icon token must be a non-empty string, got ${JSON.stringify(token)}`
    );
  }
  const colonIdx = token.indexOf(":");
  if (colonIdx === -1) {
    return err(
      "ICON_NOT_FOUND",
      `Invalid icon token ${JSON.stringify(token)}: must be "prefix:name"`
    );
  }
  if (token.indexOf(":", colonIdx + 1) !== -1) {
    return err(
      "ICON_NOT_FOUND",
      `Invalid icon token ${JSON.stringify(token)}: too many colons \u2014 expected "prefix:name"`
    );
  }
  const prefix = token.slice(0, colonIdx);
  const name = token.slice(colonIdx + 1);
  if (!PREFIX_RE.test(prefix)) {
    return err(
      "ICON_NOT_FOUND",
      `Invalid icon token ${JSON.stringify(token)}: prefix ${JSON.stringify(prefix)} must match ^[a-z][a-z0-9-]*$`
    );
  }
  if (!NAME_RE.test(name)) {
    return err(
      "ICON_NOT_FOUND",
      `Invalid icon token ${JSON.stringify(token)}: name ${JSON.stringify(name)} must match ^[a-z0-9][a-z0-9-]*$`
    );
  }
  return ok({ prefix, name });
}
function detectColorMode(body) {
  if (body.includes("<linearGradient") || body.includes("<radialGradient")) {
    return "brand";
  }
  const ATTR_RE = /(?:fill|stroke)=["']([^"']+)["']/g;
  const NEUTRAL = /* @__PURE__ */ new Set(["none", "currentcolor", "inherit"]);
  let match;
  while ((match = ATTR_RE.exec(body)) !== null) {
    const value = match[1];
    if (value === void 0) continue;
    const lower = value.toLowerCase();
    if (!NEUTRAL.has(lower)) {
      return "brand";
    }
  }
  return "monochrome";
}
function mergeViewBox(aliasLevel, iconLevel, packLevel) {
  return {
    width: aliasLevel.width ?? iconLevel.width ?? packLevel.width ?? DEFAULT_WIDTH,
    height: aliasLevel.height ?? iconLevel.height ?? packLevel.height ?? DEFAULT_HEIGHT,
    left: aliasLevel.left ?? iconLevel.left ?? packLevel.left ?? DEFAULT_LEFT,
    top: aliasLevel.top ?? iconLevel.top ?? packLevel.top ?? DEFAULT_TOP
  };
}
function composeTransforms(icon, alias) {
  const iconRotate = icon.rotate ?? 0;
  const iconHFlip = icon.hFlip ?? false;
  const iconVFlip = icon.vFlip ?? false;
  if (alias === null) {
    return {
      rotate: iconRotate,
      hFlip: iconHFlip,
      vFlip: iconVFlip
    };
  }
  const aliasRotate = alias.rotate ?? 0;
  const aliasHFlip = alias.hFlip ?? false;
  const aliasVFlip = alias.vFlip ?? false;
  return {
    rotate: (iconRotate + aliasRotate) % 4,
    hFlip: iconHFlip !== aliasHFlip,
    // XOR
    vFlip: iconVFlip !== aliasVFlip
    // XOR
  };
}
function resolveIcon(ref, packs) {
  const pack = packs.get(ref.prefix);
  if (pack === void 0) {
    return err(
      "ICON_NOT_FOUND",
      `Icon pack "${ref.prefix}" not loaded (looking up "${ref.prefix}:${ref.name}")`
    );
  }
  const directIcon = pack.icons[ref.name];
  if (directIcon !== void 0) {
    const viewBox = mergeViewBox({}, directIcon, pack);
    const transforms = composeTransforms(directIcon, null);
    return ok({
      body: directIcon.body,
      viewBox,
      transforms,
      colorMode: detectColorMode(directIcon.body)
    });
  }
  const aliases = pack.aliases;
  if (aliases === void 0 || !(ref.name in aliases)) {
    return err(
      "ICON_NOT_FOUND",
      `Icon "${ref.name}" not found in pack "${ref.prefix}" (checked icons and aliases)`
    );
  }
  let currentName = ref.name;
  let currentAlias = aliases[ref.name] ?? null;
  const topAlias = currentAlias;
  for (let depth = 0; depth < MAX_ALIAS_DEPTH; depth++) {
    if (currentAlias === null) break;
    const parentName = currentAlias.parent;
    const parentIcon = pack.icons[parentName];
    if (parentIcon !== void 0) {
      const viewBox = mergeViewBox(topAlias ?? {}, parentIcon, pack);
      const transforms = composeTransforms(parentIcon, topAlias);
      return ok({
        body: parentIcon.body,
        viewBox,
        transforms,
        colorMode: detectColorMode(parentIcon.body)
      });
    }
    const parentAlias = aliases[parentName];
    if (parentAlias === void 0) {
      return err(
        "ICON_NOT_FOUND",
        `Alias "${currentName}" in pack "${ref.prefix}" points to parent "${parentName}" which does not exist`
      );
    }
    currentName = parentName;
    currentAlias = parentAlias;
  }
  return err(
    "ICON_NOT_FOUND",
    `Alias chain for "${ref.name}" in pack "${ref.prefix}" exceeds maximum depth of ${MAX_ALIAS_DEPTH}`
  );
}
var registry3 = /* @__PURE__ */ new Map();
function registerRouter(style, router) {
  registry3.set(style, router);
}
function getRouter(style) {
  return registry3.get(style);
}
var StraightRouter = class {
  route({ from, to, obstacles, padding }) {
    const pad = padding ?? 12;
    if (!obstacles || obstacles.length === 0 || !straightHitsObstacles(from, to, obstacles, pad)) {
      return {
        points: [from, to],
        path: `M ${from.x} ${from.y} L ${to.x} ${to.y}`,
        labelPosition: midpoint(from, to)
      };
    }
    const dx = to.x - from.x, dy = to.y - from.y;
    const len = Math.sqrt(dx * dx + dy * dy);
    if (len < 1) {
      return {
        points: [from, to],
        path: `M ${from.x} ${from.y} L ${to.x} ${to.y}`,
        labelPosition: from
      };
    }
    const perpX = -dy / len, perpY = dx / len;
    const mid = midpoint(from, to);
    for (let offset = len * 0.1; offset <= len * 0.6; offset += len * 0.1) {
      for (const sign of [1, -1]) {
        const wp = { x: mid.x + perpX * offset * sign, y: mid.y + perpY * offset * sign };
        if (!straightHitsObstacles(from, wp, obstacles, pad) && !straightHitsObstacles(wp, to, obstacles, pad)) {
          return {
            points: [from, wp, to],
            path: `M ${from.x} ${from.y} L ${wp.x} ${wp.y} L ${to.x} ${to.y}`,
            labelPosition: wp
          };
        }
      }
    }
    return {
      points: [from, to],
      path: `M ${from.x} ${from.y} L ${to.x} ${to.y}`,
      labelPosition: midpoint(from, to)
    };
  }
};
function straightHitsObstacles(from, to, obstacles, pad) {
  for (const obs of obstacles) {
    if (segCrossesInterior(
      from,
      to,
      {
        x: obs.x - pad,
        y: obs.y - pad,
        width: obs.width + 2 * pad,
        height: obs.height + 2 * pad
      },
      0
    )) {
      return true;
    }
  }
  return false;
}
var OrthogonalRouter = class {
  route({ from, to, fromDir, toDir, obstacles, padding }) {
    const dx = to.x - from.x;
    const dy = to.y - from.y;
    const midX = (from.x + to.x) / 2;
    const midY = (from.y + to.y) / 2;
    const pad = padding ?? 12;
    const STUB = Math.max(pad, 20);
    const straightH = Math.abs(dy) < 1;
    const straightV = Math.abs(dx) < 1;
    if (straightH || straightV) {
      const dirsOk = straightDirsOk(from, to, straightH, straightV, fromDir, toDir);
      const clear = !obstacles || countRouteCollisions([from, to], obstacles) === 0;
      if (dirsOk && clear) {
        return {
          points: [from, to],
          path: `M ${from.x} ${from.y} L ${to.x} ${to.y}`,
          labelPosition: { x: midX, y: midY }
        };
      }
    }
    if (fromDir && toDir) {
      const exitH = fromDir === "E" || fromDir === "W";
      const entryH = toDir === "E" || toDir === "W";
      let points2;
      let path2;
      if (exitH && entryH) {
        const sameWall = fromDir === toDir;
        const baseX = sameWall ? fromDir === "E" ? Math.max(from.x, to.x) + STUB : Math.min(from.x, to.x) - STUB : midX;
        let bendX;
        if (sameWall) {
          bendX = clearBendXOutboard(baseX, from, to, obstacles, pad, fromDir === "E" ? 1 : -1);
        } else {
          bendX = clearVerticalBend(baseX, from.y, to.y, from.x, to.x, obstacles, pad);
          bendX = clearHorizontalSegments(bendX, from, to, obstacles, pad);
        }
        const v1 = { x: bendX, y: from.y };
        const v2 = { x: bendX, y: to.y };
        let hhPoints = [from, v1, v2, to];
        if (sameWall && obstacles) {
          const originalCollisions = countRouteCollisions(hhPoints, obstacles);
          if (originalCollisions > 0) {
            const detour = buildSameHorizontalWallRoute(from, to, fromDir, obstacles, pad, STUB);
            if (countRouteCollisions(detour, obstacles) < originalCollisions || routeHitsEndpointObstacle(hhPoints, from, to, obstacles)) {
              hhPoints = detour;
            }
          }
        }
        if (!sameWall && obstacles && countRouteCollisions(hhPoints, obstacles) > 0) {
          const vvRoute = buildVVRouteWithStubs(from, to, fromDir, toDir, obstacles, pad);
          if (countRouteCollisions(vvRoute, obstacles) < countRouteCollisions(hhPoints, obstacles)) {
            points2 = vvRoute;
          } else {
            points2 = hhPoints;
          }
        } else {
          points2 = hhPoints;
        }
        path2 = points2.map((p, i) => `${i === 0 ? "M" : "L"} ${p.x} ${p.y}`).join(" ");
      } else if (!exitH && !entryH) {
        const sameWall = fromDir === toDir;
        const baseY = sameWall ? fromDir === "S" ? Math.max(from.y, to.y) + STUB : Math.min(from.y, to.y) - STUB : midY;
        let bendY;
        if (sameWall) {
          bendY = clearBendYOutboard(baseY, from, to, obstacles, pad, fromDir === "S" ? 1 : -1);
        } else {
          bendY = clearHorizontalBend(baseY, from.x, to.x, from.y, to.y, obstacles, pad);
          bendY = clearVerticalSegments(bendY, from, to, obstacles, pad);
        }
        const v1 = { x: from.x, y: bendY };
        const v2 = { x: to.x, y: bendY };
        let vvPoints = [from, v1, v2, to];
        if (sameWall && obstacles) {
          const originalCollisions = countRouteCollisions(vvPoints, obstacles);
          if (originalCollisions > 0) {
            const detour = buildSameVerticalWallRoute(from, to, fromDir, obstacles, pad, STUB);
            if (countRouteCollisions(detour, obstacles) < originalCollisions || routeHitsEndpointObstacle(vvPoints, from, to, obstacles)) {
              vvPoints = detour;
            }
          }
        }
        if (!sameWall && obstacles && countRouteCollisions(vvPoints, obstacles) > 0) {
          const hhRoute = buildHHRouteWithStubs(from, to, fromDir, toDir, obstacles, pad);
          if (countRouteCollisions(hhRoute, obstacles) < countRouteCollisions(vvPoints, obstacles)) {
            points2 = hhRoute;
          } else {
            points2 = vvPoints;
          }
        } else {
          points2 = vvPoints;
        }
        path2 = points2.map((p, i) => `${i === 0 ? "M" : "L"} ${p.x} ${p.y}`).join(" ");
      } else if (exitH && !entryH) {
        const corner = { x: to.x, y: from.y };
        const leavesOk = fromDir === "E" ? to.x >= from.x : to.x <= from.x;
        const arrivesOk = toDir === "N" ? from.y <= to.y : from.y >= to.y;
        const directRoute = [from, corner, to];
        const isClear = leavesOk && arrivesOk && (!obstacles || countRouteCollisions(directRoute, obstacles) === 0);
        if (isClear) {
          points2 = directRoute;
        } else {
          const sign = fromDir === "E" ? 1 : -1;
          const obsEdge = obstacles && obstacles.length > 0 ? fromDir === "E" ? Math.max(...obstacles.map((o) => o.x + o.width)) : Math.min(...obstacles.map((o) => o.x)) : from.x;
          const span = Math.abs(to.y - from.y);
          const spanOffset = Math.min(20, Math.max(0, (span - 100) * 0.08));
          const baseX = fromDir === "E" ? Math.max(from.x + STUB, obsEdge + pad) + spanOffset : Math.min(from.x - STUB, obsEdge - pad) - spanOffset;
          const bendX = clearBendXOutboard(baseX, from, to, obstacles, pad, sign);
          const bendY = toDir === "N" ? to.y - STUB : to.y + STUB;
          points2 = [from, { x: bendX, y: from.y }, { x: bendX, y: bendY }, { x: to.x, y: bendY }, to];
        }
        path2 = points2.map((p, i) => `${i === 0 ? "M" : "L"} ${p.x} ${p.y}`).join(" ");
        return { points: points2, path: path2, labelPosition: { x: (points2[0].x + points2[1].x) / 2, y: points2[0].y } };
      } else {
        const corner = { x: from.x, y: to.y };
        const leavesOk = fromDir === "S" ? to.y >= from.y : to.y <= from.y;
        const arrivesOk = toDir === "W" ? from.x <= to.x : from.x >= to.x;
        const directRoute = [from, corner, to];
        const isClear = leavesOk && arrivesOk && (!obstacles || countRouteCollisions(directRoute, obstacles) === 0);
        if (isClear) {
          points2 = directRoute;
        } else {
          const sign = fromDir === "S" ? 1 : -1;
          const obsEdge = obstacles && obstacles.length > 0 ? fromDir === "S" ? Math.max(...obstacles.map((o) => o.y + o.height)) : Math.min(...obstacles.map((o) => o.y)) : from.y;
          const baseY = fromDir === "S" ? Math.max(from.y + STUB, obsEdge + pad) : Math.min(from.y - STUB, obsEdge - pad);
          const bendY = clearBendYOutboard(baseY, from, to, obstacles, pad, sign);
          const bendX = toDir === "W" ? to.x - STUB : to.x + STUB;
          points2 = [from, { x: from.x, y: bendY }, { x: bendX, y: bendY }, { x: bendX, y: to.y }, to];
        }
        path2 = points2.map((p, i) => `${i === 0 ? "M" : "L"} ${p.x} ${p.y}`).join(" ");
        return { points: points2, path: path2, labelPosition: { x: points2[0].x, y: (points2[0].y + points2[1].y) / 2 } };
      }
      return { points: points2, path: path2, labelPosition: { x: midX, y: midY } };
    }
    let points;
    let path;
    if (Math.abs(dy) >= Math.abs(dx)) {
      const bendY = clearHorizontalBend(midY, from.x, to.x, from.y, to.y, obstacles, pad);
      const v1 = { x: from.x, y: bendY };
      const v2 = { x: to.x, y: bendY };
      points = [from, v1, v2, to];
      path = `M ${from.x} ${from.y} L ${v1.x} ${v1.y} L ${v2.x} ${v2.y} L ${to.x} ${to.y}`;
    } else {
      const bendX = clearVerticalBend(midX, from.y, to.y, from.x, to.x, obstacles, pad);
      const v1 = { x: bendX, y: from.y };
      const v2 = { x: bendX, y: to.y };
      points = [from, v1, v2, to];
      path = `M ${from.x} ${from.y} L ${v1.x} ${v1.y} L ${v2.x} ${v2.y} L ${to.x} ${to.y}`;
    }
    return { points, path, labelPosition: { x: midX, y: midY } };
  }
};
function clearBendXOutboard(baseX, from, to, obstacles, pad, dir) {
  if (!obstacles || obstacles.length === 0) return baseX;
  let x = baseX;
  const yMin = Math.min(from.y, to.y);
  const yMax = Math.max(from.y, to.y);
  for (let pass = 0; pass < obstacles.length * 2 + 2; pass++) {
    let moved = false;
    for (const obs of obstacles) {
      const oLeft = obs.x - pad;
      const oRight = obs.x + obs.width + pad;
      const oTop = obs.y;
      const oBottom = obs.y + obs.height;
      const vHits = x > oLeft && x < oRight && yMax > oTop && yMin < oBottom;
      const hHitTo = segCrossesInterior({ x, y: to.y }, { x: to.x, y: to.y }, obs, pad);
      const hHitFrom = segCrossesInterior({ x: from.x, y: from.y }, { x, y: from.y }, obs, pad);
      if (vHits || hHitTo || hHitFrom) {
        const edge = dir < 0 ? oLeft : oRight;
        if (dir < 0 && edge < x || dir > 0 && edge > x) {
          x = edge;
          moved = true;
          break;
        }
      }
    }
    if (!moved) break;
  }
  return x;
}
function clearBendYOutboard(baseY, from, to, obstacles, pad, dir) {
  if (!obstacles || obstacles.length === 0) return baseY;
  let y = baseY;
  const xMin = Math.min(from.x, to.x);
  const xMax = Math.max(from.x, to.x);
  for (let pass = 0; pass < obstacles.length * 2 + 2; pass++) {
    let moved = false;
    for (const obs of obstacles) {
      const oTop = obs.y - pad;
      const oBottom = obs.y + obs.height + pad;
      const oLeft = obs.x;
      const oRight = obs.x + obs.width;
      const hHits = y > oTop && y < oBottom && xMax > oLeft && xMin < oRight;
      const vHitTo = segCrossesInterior({ x: to.x, y }, { x: to.x, y: to.y }, obs, pad);
      const vHitFrom = segCrossesInterior({ x: from.x, y: from.y }, { x: from.x, y }, obs, pad);
      if (hHits || vHitTo || vHitFrom) {
        const edge = dir < 0 ? oTop : oBottom;
        if (dir < 0 && edge < y || dir > 0 && edge > y) {
          y = edge;
          moved = true;
          break;
        }
      }
    }
    if (!moved) break;
  }
  return y;
}
function straightDirsOk(from, to, straightH, straightV, fromDir, toDir) {
  if (!fromDir && !toDir) return true;
  const dx = to.x - from.x;
  const dy = to.y - from.y;
  if (straightH && Math.abs(dx) < 1) return true;
  let requiredFrom;
  let requiredTo;
  if (straightH) {
    requiredFrom = dx > 0 ? "E" : "W";
    requiredTo = dx > 0 ? "W" : "E";
  } else if (straightV) {
    requiredFrom = dy > 0 ? "S" : "N";
    requiredTo = dy > 0 ? "N" : "S";
  } else {
    return false;
  }
  return (!fromDir || fromDir === requiredFrom) && (!toDir || toDir === requiredTo);
}
function buildSameVerticalWallRoute(from, to, wall, obstacles, pad, stub) {
  const dir = wall === "S" ? 1 : -1;
  const fromOut = { x: from.x, y: from.y + dir * stub };
  const toOut = { x: to.x, y: to.y + dir * stub };
  const colliders = collidingObstacles([from, fromOut, { x: to.x, y: fromOut.y }, to], obstacles);
  const relevant = colliders.length > 0 ? colliders : obstacles;
  const leftX = Math.min(...relevant.map((o) => o.x - pad));
  const rightX = Math.max(...relevant.map((o) => o.x + o.width + pad));
  return bestSameVerticalCandidate(from, to, fromOut, toOut, [leftX, rightX], obstacles);
}
function buildSameHorizontalWallRoute(from, to, wall, obstacles, pad, stub) {
  const dir = wall === "E" ? 1 : -1;
  const fromOut = { x: from.x + dir * stub, y: from.y };
  const toOut = { x: to.x + dir * stub, y: to.y };
  const colliders = collidingObstacles([from, fromOut, { x: fromOut.x, y: to.y }, to], obstacles);
  const relevant = colliders.length > 0 ? colliders : obstacles;
  const topY = Math.min(...relevant.map((o) => o.y - pad));
  const bottomY = Math.max(...relevant.map((o) => o.y + o.height + pad));
  return bestSameHorizontalCandidate(from, to, fromOut, toOut, [topY, bottomY], obstacles);
}
function bestSameVerticalCandidate(from, to, fromOut, toOut, escapeXs, obstacles) {
  let best;
  let bestCost = Infinity;
  for (const escapeX of escapeXs) {
    const candidate = [
      from,
      fromOut,
      { x: escapeX, y: fromOut.y },
      { x: escapeX, y: toOut.y },
      toOut,
      to
    ];
    const cost = countRouteCollisions(candidate, obstacles) * 1e6 + polylineLength(candidate);
    if (cost < bestCost) {
      best = candidate;
      bestCost = cost;
    }
  }
  return best;
}
function bestSameHorizontalCandidate(from, to, fromOut, toOut, escapeYs, obstacles) {
  let best;
  let bestCost = Infinity;
  for (const escapeY of escapeYs) {
    const candidate = [
      from,
      fromOut,
      { x: fromOut.x, y: escapeY },
      { x: toOut.x, y: escapeY },
      toOut,
      to
    ];
    const cost = countRouteCollisions(candidate, obstacles) * 1e6 + polylineLength(candidate);
    if (cost < bestCost) {
      best = candidate;
      bestCost = cost;
    }
  }
  return best;
}
function collidingObstacles(points, obstacles) {
  return obstacles.filter((obs) => {
    for (let i = 0; i < points.length - 1; i++) {
      if (segCrossesInterior(points[i], points[i + 1], obs, 0)) return true;
    }
    return false;
  });
}
function routeHitsEndpointObstacle(points, from, to, obstacles) {
  return collidingObstacles(points, obstacles).some(
    (obs) => pointInOrOnRect(from, obs) || pointInOrOnRect(to, obs)
  );
}
function pointInOrOnRect(p, r) {
  const eps = 1e-6;
  return p.x >= r.x - eps && p.x <= r.x + r.width + eps && p.y >= r.y - eps && p.y <= r.y + r.height + eps;
}
function polylineLength(points) {
  let len = 0;
  for (let i = 0; i < points.length - 1; i++) {
    const a = points[i];
    const b = points[i + 1];
    len += Math.abs(b.x - a.x) + Math.abs(b.y - a.y);
  }
  return len;
}
function clearVerticalBend(bendX, y1, y2, fromX, toX, obstacles, pad) {
  if (!obstacles || obstacles.length === 0) return bendX;
  const yMin = Math.min(y1, y2);
  const yMax = Math.max(y1, y2);
  let shifted = bendX;
  for (let pass = 0; pass < obstacles.length; pass++) {
    let hit = false;
    for (const obs of obstacles) {
      const oLeft = obs.x - pad;
      const oRight = obs.x + obs.width + pad;
      const oTop = obs.y;
      const oBottom = obs.y + obs.height;
      if (shifted > oLeft && shifted < oRight && yMax > oTop && yMin < oBottom) {
        const leftCandidate = oLeft;
        const rightCandidate = oRight;
        const minX = Math.min(fromX, toX);
        const maxX = Math.max(fromX, toX);
        const leftValid = leftCandidate >= minX;
        const rightValid = rightCandidate <= maxX;
        if (leftValid && rightValid) {
          shifted = shifted - leftCandidate < rightCandidate - shifted ? leftCandidate : rightCandidate;
        } else if (leftValid) {
          shifted = leftCandidate;
        } else if (rightValid) {
          shifted = rightCandidate;
        } else {
          shifted = shifted - leftCandidate < rightCandidate - shifted ? leftCandidate : rightCandidate;
        }
        hit = true;
        break;
      }
    }
    if (!hit) break;
  }
  return shifted;
}
function clearHorizontalBend(bendY, x1, x2, fromY, toY, obstacles, pad) {
  if (!obstacles || obstacles.length === 0) return bendY;
  const xMin = Math.min(x1, x2);
  const xMax = Math.max(x1, x2);
  let shifted = bendY;
  for (let pass = 0; pass < obstacles.length; pass++) {
    let hit = false;
    for (const obs of obstacles) {
      const oTop = obs.y - pad;
      const oBottom = obs.y + obs.height + pad;
      const oLeft = obs.x;
      const oRight = obs.x + obs.width;
      if (shifted > oTop && shifted < oBottom && xMax > oLeft && xMin < oRight) {
        const topCandidate = oTop;
        const bottomCandidate = oBottom;
        const minY = Math.min(fromY, toY);
        const maxY = Math.max(fromY, toY);
        const topValid = topCandidate >= minY;
        const bottomValid = bottomCandidate <= maxY;
        if (topValid && bottomValid) {
          shifted = shifted - topCandidate < bottomCandidate - shifted ? topCandidate : bottomCandidate;
        } else if (topValid) {
          shifted = topCandidate;
        } else if (bottomValid) {
          shifted = bottomCandidate;
        } else {
          shifted = shifted - topCandidate < bottomCandidate - shifted ? topCandidate : bottomCandidate;
        }
        hit = true;
        break;
      }
    }
    if (!hit) break;
  }
  return shifted;
}
function clearHorizontalSegments(bendX, from, to, obstacles, pad) {
  if (!obstacles || obstacles.length === 0) return bendX;
  let shifted = bendX;
  let prevShifted = NaN;
  for (let pass = 0; pass < obstacles.length * 2; pass++) {
    if (shifted === prevShifted) break;
    prevShifted = shifted;
    let hit = false;
    for (const obs of obstacles) {
      if (segCrossesInterior({ x: shifted, y: to.y }, { x: to.x, y: to.y }, obs, pad)) {
        shifted = to.x < obs.x ? obs.x - pad : obs.x + obs.width + pad;
        hit = true;
        break;
      }
      if (segCrossesInterior({ x: from.x, y: from.y }, { x: shifted, y: from.y }, obs, pad)) {
        shifted = from.x < obs.x ? obs.x - pad : obs.x + obs.width + pad;
        hit = true;
        break;
      }
    }
    if (!hit) break;
  }
  const stillCollides = obstacles.some(
    (obs) => segCrossesInterior({ x: shifted, y: to.y }, { x: to.x, y: to.y }, obs, pad) || segCrossesInterior({ x: from.x, y: from.y }, { x: shifted, y: from.y }, obs, pad)
  );
  if (stillCollides) {
    let globalLeft = Infinity, globalRight = -Infinity;
    for (const obs of obstacles) {
      globalLeft = Math.min(globalLeft, obs.x - pad);
      globalRight = Math.max(globalRight, obs.x + obs.width + pad);
    }
    const leftDist = Math.abs(to.x - globalLeft) + Math.abs(from.x - globalLeft);
    const rightDist = Math.abs(to.x - globalRight) + Math.abs(from.x - globalRight);
    shifted = leftDist < rightDist ? globalLeft : globalRight;
  }
  return shifted;
}
function clearVerticalSegments(bendY, from, to, obstacles, pad) {
  if (!obstacles || obstacles.length === 0) return bendY;
  let shifted = bendY;
  let prevShifted = NaN;
  for (let pass = 0; pass < obstacles.length * 2; pass++) {
    if (shifted === prevShifted) break;
    prevShifted = shifted;
    let hit = false;
    for (const obs of obstacles) {
      if (segCrossesInterior({ x: to.x, y: shifted }, { x: to.x, y: to.y }, obs, pad)) {
        shifted = to.y < obs.y ? obs.y - pad : obs.y + obs.height + pad;
        hit = true;
        break;
      }
      if (segCrossesInterior({ x: from.x, y: from.y }, { x: from.x, y: shifted }, obs, pad)) {
        shifted = from.y < obs.y ? obs.y - pad : obs.y + obs.height + pad;
        hit = true;
        break;
      }
    }
    if (!hit) break;
  }
  const stillCollides = obstacles.some(
    (obs) => segCrossesInterior({ x: to.x, y: shifted }, { x: to.x, y: to.y }, obs, pad) || segCrossesInterior({ x: from.x, y: from.y }, { x: from.x, y: shifted }, obs, pad)
  );
  if (stillCollides) {
    let globalTop = Infinity, globalBottom = -Infinity;
    for (const obs of obstacles) {
      globalTop = Math.min(globalTop, obs.y - pad);
      globalBottom = Math.max(globalBottom, obs.y + obs.height + pad);
    }
    const topDist = Math.abs(to.y - globalTop) + Math.abs(from.y - globalTop);
    const bottomDist = Math.abs(to.y - globalBottom) + Math.abs(from.y - globalBottom);
    shifted = topDist < bottomDist ? globalTop : globalBottom;
  }
  return shifted;
}
function segCrossesInterior(p1, p2, obs, _pad) {
  const dx = p2.x - p1.x, dy = p2.y - p1.y;
  const xmin = obs.x, xmax = obs.x + obs.width;
  const ymin = obs.y, ymax = obs.y + obs.height;
  let tmin = 0, tmax = 1;
  const edges2 = [
    { p: -dx, q: p1.x - xmin },
    { p: dx, q: xmax - p1.x },
    { p: -dy, q: p1.y - ymin },
    { p: dy, q: ymax - p1.y }
  ];
  for (const { p, q } of edges2) {
    if (Math.abs(p) < 1e-10) {
      if (q <= 0) return false;
    } else {
      const t = q / p;
      if (p < 0) tmin = Math.max(tmin, t);
      else tmax = Math.min(tmax, t);
      if (tmin > tmax) return false;
    }
  }
  return tmin < tmax;
}
function countRouteCollisions(points, obstacles) {
  let count = 0;
  for (let i = 0; i < points.length - 1; i++) {
    for (const obs of obstacles) {
      if (segCrossesInterior(points[i], points[i + 1], obs, 0)) count++;
    }
  }
  return count;
}
function buildVVRouteWithStubs(from, to, fromDir, toDir, obstacles, pad) {
  const stubLen = Math.max(pad, 24);
  const stub1 = {
    x: from.x + (fromDir === "W" ? -stubLen : fromDir === "E" ? stubLen : 0),
    y: from.y + (fromDir === "N" ? -stubLen : fromDir === "S" ? stubLen : 0)
  };
  const stub2 = {
    x: to.x + (toDir === "W" ? -stubLen : toDir === "E" ? stubLen : 0),
    y: to.y + (toDir === "N" ? -stubLen : toDir === "S" ? stubLen : 0)
  };
  const midY = (stub1.y + stub2.y) / 2;
  let bendY = clearHorizontalBend(midY, stub1.x, stub2.x, stub1.y, stub2.y, obstacles, pad);
  bendY = clearVerticalSegments(bendY, stub1, stub2, obstacles, pad);
  return [from, stub1, { x: stub1.x, y: bendY }, { x: stub2.x, y: bendY }, stub2, to];
}
function buildHHRouteWithStubs(from, to, fromDir, toDir, obstacles, pad) {
  const stubLen = Math.max(pad, 24);
  const stub1 = {
    x: from.x + (fromDir === "W" ? -stubLen : fromDir === "E" ? stubLen : 0),
    y: from.y + (fromDir === "N" ? -stubLen : fromDir === "S" ? stubLen : 0)
  };
  const stub2 = {
    x: to.x + (toDir === "W" ? -stubLen : toDir === "E" ? stubLen : 0),
    y: to.y + (toDir === "N" ? -stubLen : toDir === "S" ? stubLen : 0)
  };
  const midX = (stub1.x + stub2.x) / 2;
  let bendX = clearVerticalBend(midX, stub1.y, stub2.y, stub1.x, stub2.x, obstacles, pad);
  bendX = clearHorizontalSegments(bendX, stub1, stub2, obstacles, pad);
  return [from, stub1, { x: bendX, y: stub1.y }, { x: bendX, y: stub2.y }, stub2, to];
}
function controlPointForPort(point, dir, pull) {
  switch (dir) {
    case "N":
      return { x: point.x, y: point.y - pull };
    case "S":
      return { x: point.x, y: point.y + pull };
    case "E":
      return { x: point.x + pull, y: point.y };
    case "W":
      return { x: point.x - pull, y: point.y };
  }
}
function pointDistance(a, b) {
  const dx = b.x - a.x;
  const dy = b.y - a.y;
  return Math.sqrt(dx * dx + dy * dy);
}
function avoidObstaclesWithPortTangents(from, to, cp1, cp2, fromDir, toDir, basePull, obstacles, pad) {
  if (!fromDir && !toDir) return { cp1, cp2 };
  if (!bezierSegmentsHitObstacles(from, cp1, cp2, to, obstacles, 96)) return { cp1, cp2 };
  const maxPull = Math.max(basePull, Math.min(pointDistance(from, to) * 0.6, 600));
  const step = Math.max(pad / 2, 6);
  const fromPulls = fromDir ? portPullCandidates(basePull, maxPull, step) : [basePull];
  const toPulls = toDir ? portPullCandidates(basePull, maxPull, step) : [basePull];
  const pairs = fromPulls.flatMap(
    (fromPull) => toPulls.map((toPull) => ({
      fromPull,
      toPull,
      cost: (fromDir ? Math.abs(fromPull - basePull) : 0) + (toDir ? Math.abs(toPull - basePull) : 0)
    }))
  ).sort((a, b) => a.cost - b.cost);
  for (const { fromPull, toPull } of pairs) {
    const candidateCp1 = fromDir ? controlPointForPort(from, fromDir, fromPull) : cp1;
    const candidateCp2 = toDir ? controlPointForPort(to, toDir, toPull) : cp2;
    if (!bezierSegmentsHitObstacles(from, candidateCp1, candidateCp2, to, obstacles, 96)) {
      return { cp1: candidateCp1, cp2: candidateCp2 };
    }
  }
  return { cp1, cp2 };
}
function portPullCandidates(basePull, maxPull, step) {
  const pulls = /* @__PURE__ */ new Set([basePull]);
  const minPull = basePull > 0 ? Math.min(step, basePull) : 0;
  for (let pull = minPull; pull <= maxPull; pull += step) pulls.add(Math.min(pull, maxPull));
  pulls.add(maxPull);
  return [...pulls].sort((a, b) => Math.abs(a - basePull) - Math.abs(b - basePull));
}
function bezierSegmentsHitObstacles(p0, p1, p2, p3, obstacles, samples) {
  let prev = p0;
  for (let i = 1; i <= samples; i++) {
    const curr = sampleBezier(p0, p1, p2, p3, i / samples);
    for (const obs of obstacles) {
      if (segCrossesInterior(prev, curr, obs, 0)) return true;
    }
    prev = curr;
  }
  return false;
}
var BezierRouter = class {
  route({ from, to, tension, obstacles, padding, fromDir, toDir }) {
    const t = tension ?? 0.4;
    const dx = to.x - from.x;
    const dy = to.y - from.y;
    const pad = padding ?? 12;
    const dist = Math.sqrt(dx * dx + dy * dy);
    const MAX_PULL = 150;
    let cp1;
    let cp2;
    if (Math.abs(dy) >= Math.abs(dx)) {
      const pull = Math.min(Math.abs(dy) * t, MAX_PULL);
      cp1 = { x: from.x, y: from.y + (dy >= 0 ? pull : -pull) };
      cp2 = { x: to.x, y: to.y + (dy >= 0 ? -pull : pull) };
    } else {
      const pull = Math.min(Math.abs(dx) * t, MAX_PULL);
      cp1 = { x: from.x + (dx >= 0 ? pull : -pull), y: from.y };
      cp2 = { x: to.x + (dx >= 0 ? -pull : pull), y: to.y };
    }
    const portPull = Math.min(dist * t, MAX_PULL);
    if (fromDir) cp1 = controlPointForPort(from, fromDir, portPull);
    if (toDir) cp2 = controlPointForPort(to, toDir, portPull);
    if (obstacles && obstacles.length > 0) {
      const adjusted = avoidObstaclesBezier(from, to, cp1, cp2, obstacles, pad, t);
      cp1 = adjusted.cp1;
      cp2 = adjusted.cp2;
      if (fromDir) cp1 = controlPointForPort(from, fromDir, portPull);
      if (toDir) cp2 = controlPointForPort(to, toDir, portPull);
      const portAdjusted = avoidObstaclesWithPortTangents(
        from,
        to,
        cp1,
        cp2,
        fromDir,
        toDir,
        portPull,
        obstacles,
        pad
      );
      cp1 = portAdjusted.cp1;
      cp2 = portAdjusted.cp2;
      if (bezierSegmentsHitObstacles(from, cp1, cp2, to, obstacles, 96)) {
        const fallbackRequest = {
          from,
          to,
          style: "orthogonal",
          obstacles,
          padding: pad
        };
        if (fromDir) fallbackRequest.fromDir = fromDir;
        if (toDir) fallbackRequest.toDir = toDir;
        return new OrthogonalRouter().route(fallbackRequest);
      }
    }
    return {
      points: [from, cp1, cp2, to],
      path: `M ${from.x} ${from.y} C ${cp1.x} ${cp1.y} ${cp2.x} ${cp2.y} ${to.x} ${to.y}`,
      labelPosition: midpoint(from, to)
    };
  }
};
function avoidObstaclesBezier(from, to, cp1, cp2, obstacles, pad, tension) {
  const SAMPLES = 64;
  if (!bezierHitsObstacles(from, cp1, cp2, to, obstacles, pad, SAMPLES)) {
    return { cp1, cp2 };
  }
  const dx = to.x - from.x, dy = to.y - from.y;
  const len = Math.sqrt(dx * dx + dy * dy);
  if (len < 1) return { cp1, cp2 };
  const perpX = -dy / len;
  const perpY = dx / len;
  const mid = midpoint(from, to);
  const MAX_OFFSET = len * 0.75;
  const STEP = Math.max(20, len * 0.05);
  for (let offset = STEP; offset <= MAX_OFFSET; offset += STEP) {
    const cp1p2 = { x: cp1.x + perpX * offset, y: cp1.y + perpY * offset };
    const cp2p2 = { x: cp2.x + perpX * offset, y: cp2.y + perpY * offset };
    const clearP = !bezierHitsObstacles(from, cp1p2, cp2p2, to, obstacles, pad, SAMPLES);
    const cp1n2 = { x: cp1.x - perpX * offset, y: cp1.y - perpY * offset };
    const cp2n2 = { x: cp2.x - perpX * offset, y: cp2.y - perpY * offset };
    const clearN = !bezierHitsObstacles(from, cp1n2, cp2n2, to, obstacles, pad, SAMPLES);
    if (clearP && clearN) {
      const midP = sampleBezier(from, cp1p2, cp2p2, to, 0.5);
      const midN = sampleBezier(from, cp1n2, cp2n2, to, 0.5);
      const distP = (midP.x - mid.x) ** 2 + (midP.y - mid.y) ** 2;
      const distN = (midN.x - mid.x) ** 2 + (midN.y - mid.y) ** 2;
      return distP <= distN ? { cp1: cp1p2, cp2: cp2p2 } : { cp1: cp1n2, cp2: cp2n2 };
    }
    if (clearP) return { cp1: cp1p2, cp2: cp2p2 };
    if (clearN) return { cp1: cp1n2, cp2: cp2n2 };
  }
  const maxOff = MAX_OFFSET;
  const cp1p = { x: cp1.x + perpX * maxOff, y: cp1.y + perpY * maxOff };
  const cp2p = { x: cp2.x + perpX * maxOff, y: cp2.y + perpY * maxOff };
  const cp1n = { x: cp1.x - perpX * maxOff, y: cp1.y - perpY * maxOff };
  const cp2n = { x: cp2.x - perpX * maxOff, y: cp2.y - perpY * maxOff };
  const hitsP = countBezierHits(from, cp1p, cp2p, to, obstacles, pad, SAMPLES);
  const hitsN = countBezierHits(from, cp1n, cp2n, to, obstacles, pad, SAMPLES);
  if (hitsP === hitsN) {
    const midP = sampleBezier(from, cp1p, cp2p, to, 0.5);
    const midN = sampleBezier(from, cp1n, cp2n, to, 0.5);
    const distP = (midP.x - mid.x) ** 2 + (midP.y - mid.y) ** 2;
    const distN = (midN.x - mid.x) ** 2 + (midN.y - mid.y) ** 2;
    return distP <= distN ? { cp1: cp1p, cp2: cp2p } : { cp1: cp1n, cp2: cp2n };
  }
  return hitsP < hitsN ? { cp1: cp1p, cp2: cp2p } : { cp1: cp1n, cp2: cp2n };
}
function bezierHitsObstacles(p0, p1, p2, p3, obstacles, pad, samples) {
  for (let i = 1; i < samples; i++) {
    const t = i / samples;
    const pt = sampleBezier(p0, p1, p2, p3, t);
    for (const obs of obstacles) {
      if (pt.x > obs.x - pad && pt.x < obs.x + obs.width + pad && pt.y > obs.y - pad && pt.y < obs.y + obs.height + pad)
        return true;
    }
  }
  return false;
}
function countBezierHits(p0, p1, p2, p3, obstacles, pad, samples) {
  let count = 0;
  for (let i = 1; i < samples; i++) {
    const t = i / samples;
    const pt = sampleBezier(p0, p1, p2, p3, t);
    for (const obs of obstacles) {
      if (pt.x > obs.x - pad && pt.x < obs.x + obs.width + pad && pt.y > obs.y - pad && pt.y < obs.y + obs.height + pad)
        count++;
    }
  }
  return count;
}
function sampleBezier(p0, p1, p2, p3, t) {
  const u = 1 - t;
  return {
    x: u * u * u * p0.x + 3 * u * u * t * p1.x + 3 * u * t * t * p2.x + t * t * t * p3.x,
    y: u * u * u * p0.y + 3 * u * u * t * p1.y + 3 * u * t * t * p2.y + t * t * t * p3.y
  };
}
var PolylineRouter = class {
  route(req) {
    return new StraightRouter().route(req);
  }
};
var straightRouter = new StraightRouter();
var orthogonalRouter = new OrthogonalRouter();
var bezierRouter = new BezierRouter();
var polylineRouter = new PolylineRouter();
function createRouter(style) {
  switch (style) {
    case "straight":
      return straightRouter;
    case "orthogonal":
      return orthogonalRouter;
    case "bezier":
      return bezierRouter;
    case "polyline":
      return polylineRouter;
  }
}
var defaultRouter = orthogonalRouter;
function midpoint(a, b) {
  return { x: (a.x + b.x) / 2, y: (a.y + b.y) / 2 };
}
function compileOverlays(raw) {
  const annotations = [];
  let legend;
  for (const item of raw) {
    switch (item.type) {
      case "callout":
      case "note": {
        const dx = item.offset?.dx ?? 0;
        const dy = item.offset?.dy ?? -60;
        annotations.push({
          id: `${item.type}-${slugify(item.target)}-${annotations.length}`,
          type: item.type,
          text: item.text,
          // position is the offset from the anchor; absolute coords resolved at layout time
          position: { x: dx, y: dy },
          anchor: { elementId: item.target }
        });
        break;
      }
      case "legend": {
        legend = {
          ...item.title !== void 0 ? { title: item.title } : {},
          entries: item.entries,
          corner: parseLegendCorner(item.corner)
        };
        break;
      }
    }
  }
  return { annotations, ...legend !== void 0 ? { legend } : {} };
}
function slugify(text) {
  return text.toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-+|-+$/g, "");
}
function parseLegendCorner(raw) {
  const valid = ["top-left", "top-right", "bottom-left", "bottom-right"];
  return valid.includes(raw) ? raw : "bottom-right";
}
var ANNO_MIN_W = 100;
var ANNO_MAX_W = 200;
var ANNO_PAD = 10;
var ANNO_FOLD = 10;
var LEGEND_MIN_W = 140;
var LEGEND_ROW_H = 20;
var LEGEND_PAD = 10;
var LEGEND_MARGIN = 12;
function layoutOverlays(compiled, scene, theme) {
  const result = [];
  let viewBox = { ...scene.viewBox };
  const boundsMap = /* @__PURE__ */ new Map();
  for (const el of scene.elements) {
    collectGroupBounds(el, 0, 0, boundsMap);
  }
  for (const anno of compiled.annotations) {
    const laid = layoutAnnotation(anno, boundsMap, theme, scene.elements);
    if (laid) {
      result.push(laid.group);
      viewBox = expandViewBox(viewBox, laid.bounds);
    }
  }
  if (compiled.legend) {
    const laid = layoutLegend(compiled.legend, viewBox, theme);
    result.push(laid.group);
    viewBox = expandViewBox(viewBox, laid.bounds);
  }
  return { elements: result, viewBox };
}
function findObstructingHorizontalLine(elements, x, yMin, yMax) {
  for (const el of elements) {
    if (el.type === "path" && el.d) {
      const m = el.d.match(/^M\s*([\d.]+)\s*[, ]\s*([\d.]+)\s*L\s*([\d.]+)\s*[, ]\s*([\d.]+)/i);
      if (m) {
        const x1 = parseFloat(m[1]), y1 = parseFloat(m[2]), x2 = parseFloat(m[3]), y2 = parseFloat(m[4]);
        if (Math.abs(y1 - y2) < 1e-3) {
          const minX = Math.min(x1, x2), maxX = Math.max(x1, x2);
          if (x >= minX - 10 && x <= maxX + 10 && y1 >= yMin && y1 <= yMax) {
            return y1;
          }
        }
      }
    }
  }
  return null;
}
function layoutAnnotation(anno, boundsMap, theme, sceneElements) {
  const { palette, typography } = theme;
  let anchorPt;
  if ("elementId" in anno.anchor) {
    const rawId = anno.anchor.elementId;
    const elBounds = boundsMap.get(rawId) ?? boundsMap.get(slugify2(rawId)) ?? boundsMap.get(rawId.toLowerCase());
    if (!elBounds) return null;
    anchorPt = { x: elBounds.x + elBounds.width / 2, y: elBounds.y };
  } else {
    anchorPt = anno.anchor.point;
  }
  const charW = typography.baseFontSize * 0.52;
  const maxW = anno.width ?? ANNO_MAX_W;
  const lines2 = wrapText(anno.text, maxW, charW);
  const textW = Math.max(
    ANNO_MIN_W,
    Math.min(maxW, Math.max(...lines2.map((l) => l.length * charW)) + ANNO_PAD * 2)
  );
  const lineH = typography.baseFontSize * typography.lineHeight;
  const boxW = textW;
  const boxH = lines2.length * lineH + ANNO_PAD * 2;
  let x;
  let y;
  if ("elementId" in anno.anchor) {
    x = anchorPt.x - boxW / 2 + anno.position.x;
    const yOffset = anno.position.y !== -60 ? anno.position.y : -boxH - 14;
    y = anchorPt.y + yOffset;
    if (anno.position.y === -60) {
      const obstLineY = findObstructingHorizontalLine(sceneElements, anchorPt.x, y, y + boxH);
      if (obstLineY !== null) {
        y = obstLineY - boxH - 14;
      }
    }
  } else {
    x = anno.position.x;
    y = anno.position.y;
  }
  const connStart = closestEdgePoint({ x, y, width: boxW, height: boxH }, anchorPt);
  const isCallout = anno.type === "callout";
  const children = [];
  if (isCallout) {
    children.push({
      type: "path",
      d: `M ${connStart.x} ${connStart.y} L ${anchorPt.x} ${anchorPt.y}`,
      stroke: palette.primary,
      strokeWidth: 1
    });
    children.push({
      type: "circle",
      center: anchorPt,
      radius: 2.5,
      fill: palette.primary,
      stroke: palette.primary,
      strokeWidth: 1
    });
    children.push({
      type: "rect",
      bounds: { x, y, width: boxW, height: boxH },
      rx: 4,
      fill: palette.background,
      stroke: palette.primary,
      strokeWidth: 1
    });
  } else {
    children.push({
      type: "path",
      d: `M ${connStart.x} ${connStart.y} L ${anchorPt.x} ${anchorPt.y}`,
      stroke: palette.border,
      strokeWidth: 1,
      strokeDasharray: "4 3"
    });
    children.push({
      type: "path",
      d: `M ${x} ${y} L ${x + boxW - ANNO_FOLD} ${y} L ${x + boxW} ${y + ANNO_FOLD} L ${x + boxW} ${y + boxH} L ${x} ${y + boxH} Z`,
      fill: palette.surface,
      stroke: palette.border,
      strokeWidth: 1
    });
    children.push({
      type: "path",
      d: `M ${x + boxW - ANNO_FOLD} ${y} L ${x + boxW - ANNO_FOLD} ${y + ANNO_FOLD} L ${x + boxW} ${y + ANNO_FOLD} Z`,
      fill: palette.border,
      stroke: palette.border,
      strokeWidth: 1
    });
  }
  for (let i = 0; i < lines2.length; i++) {
    children.push({
      type: "text",
      content: lines2[i] ?? "",
      position: { x: x + ANNO_PAD, y: y + ANNO_PAD + typography.baseFontSize + i * lineH },
      fontSize: typography.baseFontSize,
      fontFamily: isCallout ? 'Instrument Serif, Georgia, "Times New Roman", serif' : typography.fontFamily,
      fill: palette.text
    });
  }
  const bx = Math.min(x, anchorPt.x);
  const by = Math.min(y, anchorPt.y);
  const bounds = {
    x: bx,
    y: by,
    width: Math.max(boxW, Math.abs(anchorPt.x - x)) + Math.max(0, x - bx),
    height: Math.max(boxH, Math.abs(anchorPt.y - y)) + Math.max(0, y - by)
  };
  return { group: { type: "group", id: anno.id, children }, bounds };
}
function layoutLegend(legend, viewBox, theme) {
  const { palette, typography } = theme;
  const titleH = legend.title ? LEGEND_ROW_H + 4 : 0;
  const boxH = LEGEND_PAD * 2 + titleH + legend.entries.length * LEGEND_ROW_H;
  const boxW = Math.max(
    legend.width ?? LEGEND_MIN_W,
    ...legend.title ? [legend.title.length * typography.baseFontSize * 0.55 + LEGEND_PAD * 2] : [],
    ...legend.entries.map(
      (e) => (e.key.length + e.value.length + 3) * typography.smallFontSize * 0.52 + LEGEND_PAD * 2
    )
  );
  const c = legend.corner;
  const x = c === "top-left" || c === "bottom-left" ? viewBox.x + LEGEND_MARGIN : viewBox.x + viewBox.width - boxW - LEGEND_MARGIN;
  const y = c === "top-left" || c === "top-right" ? viewBox.y + LEGEND_MARGIN : viewBox.y + viewBox.height - boxH - LEGEND_MARGIN;
  const children = [
    {
      type: "rect",
      bounds: { x, y, width: boxW, height: boxH },
      fill: palette.background,
      stroke: palette.border,
      strokeWidth: 1,
      rx: 4
    }
  ];
  let rowY = y + LEGEND_PAD;
  if (legend.title) {
    children.push({
      type: "text",
      content: legend.title,
      position: { x: x + LEGEND_PAD, y: rowY + typography.baseFontSize },
      fontSize: typography.baseFontSize,
      fontFamily: typography.fontFamily,
      fontWeight: "bold",
      fill: palette.text
    });
    rowY += LEGEND_ROW_H + 4;
    children.push({
      type: "path",
      d: `M ${x + 4} ${rowY - 2} L ${x + boxW - 4} ${rowY - 2}`,
      stroke: palette.border,
      strokeWidth: 1
    });
  }
  for (const entry of legend.entries) {
    children.push({
      type: "text",
      content: entry.key,
      position: { x: x + LEGEND_PAD, y: rowY + typography.smallFontSize },
      fontSize: typography.smallFontSize,
      fontFamily: typography.fontFamily,
      fontWeight: "bold",
      fill: palette.text
    });
    children.push({
      type: "text",
      content: entry.value,
      position: { x: x + boxW / 2 + 4, y: rowY + typography.smallFontSize },
      fontSize: typography.smallFontSize,
      fontFamily: typography.fontFamily,
      fill: palette.textMuted
    });
    rowY += LEGEND_ROW_H;
  }
  return {
    group: { type: "group", children },
    bounds: { x, y, width: boxW, height: boxH }
  };
}
function slugify2(text) {
  return text.toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-+|-+$/g, "");
}
function collectGroupBounds(el, ox, oy, map) {
  if (el.type === "text") {
    const b = elementBoundsAt(el, ox, oy);
    if (b) {
      const trimmed = el.content.trim();
      if (!map.has(trimmed)) map.set(trimmed, b);
      const slug = slugify2(trimmed);
      if (slug && !map.has(slug)) map.set(slug, b);
    }
    return;
  }
  if (el.type !== "group") return;
  const [tx, ty] = parseTranslate(el.transform);
  const nx = ox + tx;
  const ny = oy + ty;
  if (el.id) {
    const childBounds = el.children.map((c) => elementBoundsAt(c, nx, ny)).filter((b) => b !== null);
    if (childBounds.length > 0) map.set(el.id, unionRects(childBounds));
  }
  for (const child of el.children) {
    collectGroupBounds(child, nx, ny, map);
  }
}
function elementBoundsAt(el, ox, oy) {
  switch (el.type) {
    case "rect":
      return {
        x: el.bounds.x + ox,
        y: el.bounds.y + oy,
        width: el.bounds.width,
        height: el.bounds.height
      };
    case "circle":
      return {
        x: el.center.x - el.radius + ox,
        y: el.center.y - el.radius + oy,
        width: el.radius * 2,
        height: el.radius * 2
      };
    case "text": {
      const w = el.content.length * el.fontSize * 0.52;
      const anchor = el.anchor ?? "start";
      const startX = anchor === "middle" ? el.position.x - w / 2 : anchor === "end" ? el.position.x - w : el.position.x;
      return {
        x: startX + ox,
        y: el.position.y - el.fontSize + oy,
        width: w,
        height: el.fontSize * 1.4
      };
    }
    case "group": {
      const [tx, ty] = parseTranslate(el.transform);
      const kids = el.children.map((c) => elementBoundsAt(c, ox + tx, oy + ty)).filter((b) => b !== null);
      return kids.length > 0 ? unionRects(kids) : null;
    }
    case "path":
      return null;
    case "icon":
      return { x: el.x + ox, y: el.y + oy, width: el.size, height: el.size };
  }
}
function parseTranslate(transform) {
  if (!transform) return [0, 0];
  const m = transform.match(/translate\(\s*(-?[\d.]+)\s*[, ]\s*(-?[\d.]+)\s*\)/);
  return m ? [parseFloat(m[1]), parseFloat(m[2])] : [0, 0];
}
function closestEdgePoint(box, target) {
  const cx = box.x + box.width / 2;
  const cy = box.y + box.height / 2;
  const dx = target.x - cx;
  const dy = target.y - cy;
  if (Math.abs(dx) * box.height > Math.abs(dy) * box.width) {
    const sx = dx > 0 ? 1 : -1;
    const ex = cx + sx * box.width / 2;
    const ey = cy + (Math.abs(dx) > 0 ? dy / Math.abs(dx) * (box.width / 2) : 0);
    return { x: ex, y: clamp(ey, box.y, box.y + box.height) };
  } else {
    const sy = dy > 0 ? 1 : -1;
    const ey = cy + sy * box.height / 2;
    const ex = cx + (Math.abs(dy) > 0 ? dx / Math.abs(dy) * (box.height / 2) : 0);
    return { x: clamp(ex, box.x, box.x + box.width), y: ey };
  }
}
function wrapText(text, maxWidth, charWidth) {
  const words = text.split(" ");
  const lines2 = [];
  let current = "";
  for (const word of words) {
    const candidate = current ? `${current} ${word}` : word;
    if (candidate.length * charWidth > maxWidth && current) {
      lines2.push(current);
      current = word;
    } else {
      current = candidate;
    }
  }
  if (current) lines2.push(current);
  return lines2.length > 0 ? lines2 : [""];
}
function expandViewBox(vb, bounds) {
  const x = Math.min(vb.x, bounds.x);
  const y = Math.min(vb.y, bounds.y);
  const right = Math.max(vb.x + vb.width, bounds.x + bounds.width);
  const bottom = Math.max(vb.y + vb.height, bounds.y + bounds.height);
  return { x, y, width: right - x, height: bottom - y };
}
function unionRects(rects) {
  const x = Math.min(...rects.map((r) => r.x));
  const y = Math.min(...rects.map((r) => r.y));
  const right = Math.max(...rects.map((r) => r.x + r.width));
  const bottom = Math.max(...rects.map((r) => r.y + r.height));
  return { x, y, width: right - x, height: bottom - y };
}
function clamp(v, lo, hi) {
  return Math.max(lo, Math.min(hi, v));
}
function applyOverlays(scene, overlays3, theme) {
  if (!overlays3 || overlays3.length === 0) return scene;
  const compiled = compileOverlays(overlays3);
  const { elements, viewBox } = layoutOverlays(compiled, scene, theme);
  return { ...scene, elements: [...scene.elements, ...elements], viewBox };
}
function pen(theme) {
  const fontFamily = theme.typography.fontFamily;
  return {
    text(content, x, y, size, fill, opts2 = {}) {
      return {
        type: "text",
        content,
        position: { x, y },
        fontSize: size,
        fontFamily,
        fill,
        ...opts2.weight !== void 0 ? { fontWeight: opts2.weight } : {},
        ...opts2.anchor !== void 0 ? { anchor: opts2.anchor } : {},
        ...opts2.opacity !== void 0 ? { opacity: opts2.opacity } : {}
      };
    },
    rect(bounds, fill, stroke, strokeWidth, opts2 = {}) {
      return {
        type: "rect",
        bounds,
        fill,
        stroke,
        strokeWidth,
        ...opts2.rx !== void 0 ? { rx: opts2.rx } : {},
        ...opts2.fillOpacity !== void 0 ? { fillOpacity: opts2.fillOpacity } : {},
        ...opts2.opacity !== void 0 ? { opacity: opts2.opacity } : {}
      };
    },
    circle(center, radius, fill, stroke, strokeWidth, opts2 = {}) {
      return {
        type: "circle",
        center,
        radius,
        fill,
        stroke,
        strokeWidth,
        ...opts2.opacity !== void 0 ? { opacity: opts2.opacity } : {}
      };
    },
    path(d, stroke, strokeWidth, opts2 = {}) {
      return {
        type: "path",
        d,
        stroke,
        strokeWidth,
        ...opts2.fill !== void 0 ? { fill: opts2.fill } : {},
        ...opts2.dash !== void 0 ? { strokeDasharray: opts2.dash } : {},
        ...opts2.markerEnd !== void 0 ? { markerEnd: opts2.markerEnd } : {},
        ...opts2.markerStart !== void 0 ? { markerStart: opts2.markerStart } : {},
        ...opts2.animated !== void 0 ? { animated: opts2.animated } : {},
        ...opts2.opacity !== void 0 ? { opacity: opts2.opacity } : {}
      };
    },
    group(children, opts2 = {}) {
      return {
        type: "group",
        children,
        ...opts2.id !== void 0 ? { id: opts2.id } : {},
        ...opts2.transform !== void 0 ? { transform: opts2.transform } : {},
        ...opts2.opacity !== void 0 ? { opacity: opts2.opacity } : {}
      };
    },
    icon(resolvedIcon, x, y, size, opts2 = {}) {
      return {
        type: "icon",
        icon: resolvedIcon,
        x,
        y,
        size,
        ...opts2.color !== void 0 ? { color: opts2.color } : {},
        ...opts2.opacity !== void 0 ? { opacity: opts2.opacity } : {}
      };
    }
  };
}
var ADVANCE = {
  32: 0.2793,
  33: 0.2793,
  34: 0.3574,
  35: 0.5576,
  36: 0.5576,
  37: 0.8906,
  38: 0.6699,
  39: 0.1914,
  40: 0.334,
  41: 0.334,
  42: 0.3906,
  43: 0.5859,
  44: 0.2793,
  45: 0.334,
  46: 0.2793,
  47: 0.3125,
  48: 0.5576,
  49: 0.5576,
  50: 0.5576,
  51: 0.5576,
  52: 0.5576,
  53: 0.5576,
  54: 0.5576,
  55: 0.5576,
  56: 0.5576,
  57: 0.5576,
  58: 0.2793,
  59: 0.2793,
  60: 0.5859,
  61: 0.5859,
  62: 0.5859,
  63: 0.5576,
  64: 1.0186,
  65: 0.6699,
  66: 0.6699,
  67: 0.7227,
  68: 0.7227,
  69: 0.6699,
  70: 0.6133,
  71: 0.7793,
  72: 0.7227,
  73: 0.2783,
  74: 0.5,
  75: 0.6699,
  76: 0.5576,
  77: 0.8359,
  78: 0.7227,
  79: 0.7793,
  80: 0.6133,
  81: 0.7793,
  82: 0.7227,
  83: 0.6133,
  84: 0.6133,
  85: 0.7227,
  86: 0.6699,
  87: 0.9453,
  88: 0.6699,
  89: 0.6699,
  90: 0.6133,
  91: 0.2793,
  92: 0.3125,
  93: 0.2793,
  94: 0.5859,
  95: 0.5576,
  96: 0.334,
  97: 0.5576,
  98: 0.5576,
  99: 0.5,
  100: 0.5576,
  101: 0.5576,
  102: 0.2793,
  103: 0.5576,
  104: 0.5576,
  105: 0.2217,
  106: 0.2217,
  107: 0.5,
  108: 0.2217,
  109: 0.8359,
  110: 0.5576,
  111: 0.5576,
  112: 0.5576,
  113: 0.5576,
  114: 0.334,
  115: 0.5,
  116: 0.334,
  117: 0.5576,
  118: 0.5,
  119: 0.7227,
  120: 0.5,
  121: 0.5,
  122: 0.5,
  123: 0.334,
  124: 0.2598,
  125: 0.334,
  126: 0.5859
};
var DEFAULT_ADVANCE = 0.5576;
var LINE_HEIGHT_FACTOR = 1.2;
function measureText(text, fontSizePx) {
  let totalEm = 0;
  for (let i = 0; i < text.length; i++) {
    const cp = text.codePointAt(i) ?? 32;
    totalEm += ADVANCE[cp] ?? DEFAULT_ADVANCE;
  }
  return {
    width: totalEm * fontSizePx,
    height: fontSizePx * LINE_HEIGHT_FACTOR
  };
}
var ELLIPSIS = "\u2026";
function wrapText2(text, fontSizePx, maxWidth, maxLines) {
  if (!text || maxLines <= 0 || maxWidth <= 0) return { lines: [] };
  const words = text.split(/\s+/).filter(Boolean);
  if (words.length === 0) return { lines: [] };
  const lines2 = [];
  let current = "";
  for (let wi = 0; wi < words.length; wi++) {
    const word = words[wi];
    const candidate = current ? `${current} ${word}` : word;
    if (measureText(candidate, fontSizePx).width <= maxWidth) {
      current = candidate;
    } else {
      if (current) {
        if (lines2.length === maxLines - 1) {
          const remaining = words.slice(wi).join(" ");
          const full = current ? `${current} ${remaining}` : remaining;
          lines2.push(truncateText(full, fontSizePx, maxWidth));
          return { lines: lines2 };
        }
        lines2.push(current);
        current = word;
      } else {
        if (lines2.length === maxLines - 1) {
          lines2.push(truncateText(word, fontSizePx, maxWidth));
          return { lines: lines2 };
        }
        lines2.push(truncateText(word, fontSizePx, maxWidth));
        current = "";
      }
    }
  }
  if (current) {
    if (lines2.length < maxLines) {
      lines2.push(current);
    } else {
      const last = lines2[lines2.length - 1] ?? "";
      lines2[lines2.length - 1] = truncateText(`${last} ${current}`, fontSizePx, maxWidth);
    }
  }
  return { lines: lines2 };
}
function truncateText(text, fontSizePx, maxWidth) {
  if (measureText(text, fontSizePx).width <= maxWidth) return text;
  const ellipsisWidth = measureText(ELLIPSIS, fontSizePx).width;
  const budget = maxWidth - ellipsisWidth;
  if (budget <= 0) return ELLIPSIS;
  let lo = 0;
  let hi = text.length;
  while (lo < hi) {
    const mid = Math.floor((lo + hi + 1) / 2);
    if (measureText(text.slice(0, mid), fontSizePx).width <= budget) {
      lo = mid;
    } else {
      hi = mid - 1;
    }
  }
  return text.slice(0, lo) + ELLIPSIS;
}
function rhuInt(v) {
  return Math.floor(v + 0.5);
}
function rhu(v, decimals = 2) {
  const f2 = 10 ** decimals;
  return Math.floor(v * f2 + 0.5) / f2;
}
function measureFormattedText(rawText, font, smallFont, maxAutoWrapWidth = 260, maxWrapLines = 6) {
  const sepIdx = rawText.indexOf("::");
  if (sepIdx !== -1) {
    const rawTitle = rawText.slice(0, sepIdx).trim();
    const rawSubtitle = rawText.slice(sepIdx + 2).trim();
    const titleNorm = rawTitle.replace(/<br\s*\/?>/gi, "\n").replace(/\\n/g, "\n");
    const subNorm = rawSubtitle.replace(/<br\s*\/?>/gi, "\n").replace(/\\n/g, "\n");
    const titleRawLines = titleNorm.split(/\r?\n/).map((l) => l.trim()).filter(Boolean);
    const subtitleRawLines = subNorm.split(/\r?\n/).map((l) => l.trim()).filter(Boolean);
    const titleLines = [];
    for (const r of titleRawLines) {
      if (measureText(r, font).width > maxAutoWrapWidth) {
        const wrapped = wrapText2(r, font, maxAutoWrapWidth, maxWrapLines);
        titleLines.push(...wrapped.lines);
      } else {
        titleLines.push(r);
      }
    }
    const subtitleLines = [];
    for (const r of subtitleRawLines) {
      if (measureText(r, smallFont).width > maxAutoWrapWidth) {
        const wrapped = wrapText2(r, smallFont, maxAutoWrapWidth, maxWrapLines);
        subtitleLines.push(...wrapped.lines);
      } else {
        subtitleLines.push(r);
      }
    }
    let titleMaxW = 0;
    for (const t of titleLines) {
      titleMaxW = Math.max(titleMaxW, measureText(t, font).width * 1.05);
    }
    let subMaxW = 0;
    for (const s of subtitleLines) {
      subMaxW = Math.max(subMaxW, measureText(s, smallFont).width);
    }
    const firstTitle = titleLines[0];
    const firstSub = subtitleLines.length > 0 ? subtitleLines.join(" ") : void 0;
    return {
      title: firstTitle,
      subtitle: firstSub,
      titleLines: titleLines.length > 0 ? titleLines : void 0,
      subtitleLines: subtitleLines.length > 0 ? subtitleLines : void 0,
      lines: [...titleLines, ...subtitleLines],
      maxLineWidth: Math.max(titleMaxW, subMaxW),
      lineCount: titleLines.length + subtitleLines.length
    };
  }
  const normalized = rawText.replace(/<br\s*\/?>/gi, "\n").replace(/\\n/g, "\n");
  const rawLines = normalized.split(/\r?\n/).map((l) => l.trim()).filter(Boolean);
  const lines2 = [];
  for (const r of rawLines) {
    if (measureText(r, font).width > maxAutoWrapWidth) {
      const wrapped = wrapText2(r, font, maxAutoWrapWidth, maxWrapLines);
      lines2.push(...wrapped.lines);
    } else {
      lines2.push(r);
    }
  }
  const finalLines = lines2.length > 0 ? lines2 : [rawText.replace(/<br\s*\/?>/gi, " ").trim()];
  const maxLineWidth = finalLines.reduce((m, l) => Math.max(m, measureText(l, font).width), 0);
  return {
    lines: finalLines,
    maxLineWidth,
    lineCount: finalLines.length
  };
}
function renderFormattedText(p, itemInfo, x, y, w, h2, font, smallFont, textColor, mutedColor, opts2 = {}) {
  const elements = [];
  const align = opts2.align ?? "start";
  const paddingLeft = opts2.paddingLeft ?? 0;
  const paddingRight = opts2.paddingRight ?? 0;
  const defaultBold = opts2.defaultBold ?? false;
  const opacity = opts2.opacity;
  const targetX = align === "middle" ? rhu(x + w / 2) : align === "end" ? rhu(x + w - paddingRight) : rhu(x + paddingLeft);
  if (itemInfo.titleLines !== void 0) {
    const titleLines = itemInfo.titleLines;
    const subLines = itemInfo.subtitleLines ?? [];
    const titleLH = font * 1.25;
    const subLH = smallFont * 1.25;
    const totalH = titleLines.length * titleLH + subLines.length * subLH;
    const startY2 = y + (h2 - totalH) / 2 + font * 0.85;
    let curY = startY2;
    for (let ti = 0; ti < titleLines.length; ti++) {
      elements.push(
        p.text(titleLines[ti], targetX, rhu(curY), font, textColor, {
          weight: "bold",
          anchor: align,
          ...opacity !== void 0 ? { opacity } : {}
        })
      );
      curY += titleLH;
    }
    for (let si = 0; si < subLines.length; si++) {
      elements.push(
        p.text(subLines[si], targetX, rhu(curY), smallFont, mutedColor, {
          anchor: align,
          ...opacity !== void 0 ? { opacity } : {}
        })
      );
      curY += subLH;
    }
    return elements;
  }
  const lines2 = itemInfo.lines;
  const count = lines2.length;
  const lineH = font * 1.25;
  if (count === 1) {
    const textY = rhu(y + h2 / 2 + font * 0.34);
    elements.push(
      p.text(lines2[0], targetX, textY, font, textColor, {
        ...defaultBold ? { weight: "bold" } : {},
        anchor: align,
        ...opacity !== void 0 ? { opacity } : {}
      })
    );
    return elements;
  }
  const startY = y + (h2 - (count - 1) * lineH) / 2 + font * 0.34;
  for (let li = 0; li < count; li++) {
    const lineY = rhu(startY + li * lineH);
    elements.push(
      p.text(lines2[li], targetX, lineY, font, textColor, {
        ...defaultBold ? { weight: "bold" } : {},
        anchor: align,
        ...opacity !== void 0 ? { opacity } : {}
      })
    );
  }
  return elements;
}
var NODE_W = 120;
var NODE_H = 40;
var ARROW_MARKER_ID = "triton-arrow";
var CARD_PAD = 8;
var CARD_ICON_BOX = 40;
var CARD_ICON_GAP = 12;
var CARD_MIN_W = 192;
var CARD_MAX_W = 400;
var CARD_MAX_BODY_LINES = 3;
var MAX_AUTO_WRAP_WIDTH = 260;
function splitAndWrapLabel(label, fontSize) {
  if (!label) return [""];
  const normalized = label.replace(/<br\s*\/?>/gi, "\n").replace(/\\n/g, "\n");
  const rawLines = normalized.split(/\r?\n/).map((l) => l.trim());
  const finalLines = [];
  for (const rawLine of rawLines) {
    if (!rawLine) continue;
    const measured = measureText(rawLine, fontSize).width;
    if (measured > MAX_AUTO_WRAP_WIDTH) {
      const wrapped = wrapText2(rawLine, fontSize, MAX_AUTO_WRAP_WIDTH, 10);
      finalLines.push(...wrapped.lines);
    } else {
      finalLines.push(rawLine);
    }
  }
  return finalLines.length > 0 ? finalLines : [""];
}
function splitCardLabel(label) {
  const sepIdx = label.indexOf("::");
  if (sepIdx !== -1) {
    return { title: label.slice(0, sepIdx).trim(), body: label.slice(sepIdx + 2).trim() };
  }
  const idx = label.search(/\\n|\n/);
  if (idx === -1) return { title: label.trim(), body: "" };
  const sep = label[idx] === "\n" ? 1 : 2;
  return { title: label.slice(0, idx).trim(), body: label.slice(idx + sep).trim() };
}
function measureCardNode(node, typography) {
  const { title, body } = splitCardLabel(node.label);
  const titleW = measureText(title, typography.baseFontSize).width * 1.1;
  const titleLH = typography.baseFontSize * 1.2;
  const maxRightW = CARD_MAX_W - CARD_ICON_BOX - CARD_ICON_GAP - 2 * CARD_PAD;
  let bodyH = 0;
  let bodyW = 0;
  if (body) {
    const wrapped = wrapText2(body, typography.smallFontSize, maxRightW, CARD_MAX_BODY_LINES);
    bodyH = wrapped.lines.length * typography.smallFontSize * 1.2;
    bodyW = wrapped.lines.reduce(
      (mx, l) => Math.max(mx, measureText(l, typography.smallFontSize).width),
      0
    );
  }
  const textW = Math.max(titleW, bodyW);
  const width = Math.min(
    CARD_MAX_W,
    Math.max(CARD_MIN_W, CARD_ICON_BOX + CARD_ICON_GAP + textW + 2 * CARD_PAD)
  );
  const textH = body ? titleLH + bodyH : titleLH;
  const height = Math.max(CARD_ICON_BOX, textH) + 2 * CARD_PAD;
  return { width, height };
}
function measureNode(node, typography) {
  if (node.shape === "card") {
    return measureCardNode(node, typography);
  }
  const fontSize = typography.baseFontSize;
  const smallFont = typography.smallFontSize;
  let totalTextHeight;
  let maxLineWidth;
  if (node.label.includes("::")) {
    const info = measureFormattedText(node.label, fontSize, smallFont);
    const titleLinesCount = info.titleLines?.length ?? 1;
    const subLinesCount = info.subtitleLines?.length ?? 0;
    totalTextHeight = titleLinesCount * fontSize * 1.25 + subLinesCount * smallFont * 1.25;
    maxLineWidth = info.maxLineWidth;
  } else {
    const lines2 = splitAndWrapLabel(node.label, fontSize);
    const lineH = fontSize * 1.2;
    totalTextHeight = lines2.length * lineH;
    maxLineWidth = lines2.reduce((max, l) => Math.max(max, measureText(l, fontSize).width), 0);
  }
  const iconPad = node.icon ? 36 : 0;
  const padX = 24;
  const padY = 16;
  switch (node.shape) {
    case "diamond": {
      const textW = Math.max(80, maxLineWidth + padX + iconPad);
      const textH = Math.max(24, totalTextHeight + padY);
      const width = Math.max(NODE_W, Math.ceil((textW + textH) * 1.05 + 16));
      const height = Math.max(NODE_H, Math.ceil((textW + textH) * 0.7 + 10));
      return { width, height };
    }
    case "circle": {
      const diag = Math.sqrt(Math.pow(maxLineWidth + iconPad, 2) + Math.pow(totalTextHeight, 2));
      const diam = Math.max(NODE_H, Math.ceil(diag + 24));
      return { width: diam, height: diam };
    }
    case "stadium": {
      const height = Math.max(NODE_H, Math.ceil(totalTextHeight + padY));
      const width = Math.max(NODE_W, Math.ceil(maxLineWidth + height * 0.8 + 16 + iconPad));
      return { width, height };
    }
    case "subroutine": {
      const height = Math.max(NODE_H, Math.ceil(totalTextHeight + padY));
      const width = Math.max(NODE_W, Math.ceil(maxLineWidth + 36 + iconPad));
      return { width, height };
    }
    case "cylinder": {
      const height = Math.max(48, Math.ceil(totalTextHeight + 24));
      const width = Math.max(NODE_W, Math.ceil(maxLineWidth + padX + iconPad));
      return { width, height };
    }
    case "hexagon": {
      const height = Math.max(NODE_H, Math.ceil(totalTextHeight + padY));
      const width = Math.max(NODE_W, Math.ceil(maxLineWidth + height * 0.6 + 16 + iconPad));
      return { width, height };
    }
    case "parallelogram":
    case "parallelogram-alt":
    case "asymmetric": {
      const height = Math.max(NODE_H, Math.ceil(totalTextHeight + padY));
      const width = Math.max(NODE_W, Math.ceil(maxLineWidth + 32 + iconPad));
      return { width, height };
    }
    case "rect":
    case "rounded-rect":
    default: {
      const height = Math.max(NODE_H, Math.ceil(totalTextHeight + padY));
      const width = Math.max(NODE_W, Math.ceil(maxLineWidth + padX + iconPad));
      return { width, height };
    }
  }
}
function layoutFlowchart(ir, theme, options) {
  const { spacing, palette, typography, edges: edgeTheme } = theme;
  const p = pen(theme);
  const margin = spacing.diagramMargin;
  const isLR = ir.direction === "LR" || ir.direction === "RL";
  const isReverse = ir.direction === "RL" || ir.direction === "BT";
  const layers = assignLayers(ir.nodes, ir.edges);
  const byLayer = groupByLayer(ir.nodes, layers);
  const backEdges = findBackEdges(ir.nodes, ir.edges);
  const orderedByLayer = minimizeCrossings(byLayer, ir.edges, backEdges);
  const colGap = spacing.nodeGap;
  const rowGap = spacing.nodeGap;
  let maxNodesInLayer = 0;
  for (const [, nodes] of orderedByLayer) maxNodesInLayer = Math.max(maxNodesInLayer, nodes.length);
  const nodeSizeMap = /* @__PURE__ */ new Map();
  for (const node of ir.nodes) {
    nodeSizeMap.set(node.id, measureNode(node, typography));
  }
  const nodePos = assignCoordinatesBK(
    orderedByLayer,
    ir.edges,
    backEdges,
    isLR,
    isReverse,
    maxNodesInLayer,
    NODE_W,
    NODE_H,
    colGap,
    rowGap,
    margin,
    nodeSizeMap
  );
  const elements = [];
  if (ir.metadata.title) {
    elements.push(
      p.text(ir.metadata.title, margin, margin - 8, typography.titleFontSize, palette.text, {
        weight: "bold"
      })
    );
  }
  for (const sg of ir.subgraphs) {
    const sgRects = sg.nodeIds.map((id) => nodePos.get(id)).filter((r) => r !== void 0);
    if (sgRects.length === 0) continue;
    const pad = 12;
    const minX = Math.min(...sgRects.map((r) => r.x)) - pad;
    const minY = Math.min(...sgRects.map((r) => r.y)) - pad - 20;
    const maxX = Math.max(...sgRects.map((r) => r.x + r.width)) + pad;
    const maxY = Math.max(...sgRects.map((r) => r.y + r.height)) + pad;
    elements.push(
      p.rect(
        { x: minX, y: minY, width: maxX - minX, height: maxY - minY },
        palette.surface,
        palette.border,
        1,
        { rx: 6, opacity: 0.6 }
      )
    );
    elements.push(
      p.text(sg.label, minX + 8, minY + 14, typography.smallFontSize, palette.textMuted)
    );
  }
  let bowMaxX = -Infinity;
  let bowMaxY = -Infinity;
  const nodeLayerMap = /* @__PURE__ */ new Map();
  for (const [l, nodes] of orderedByLayer) {
    for (const n of nodes) nodeLayerMap.set(n.id, l);
  }
  const edgeWalls = /* @__PURE__ */ new Map();
  for (let ei = 0; ei < ir.edges.length; ei++) {
    if (backEdges.has(ei)) continue;
    const edge = ir.edges[ei];
    if (edge.from === edge.to) continue;
    const fromRect = nodePos.get(edge.from);
    const toRect2 = nodePos.get(edge.to);
    if (!fromRect || !toRect2) continue;
    const fromNode = ir.nodes.find((n) => n.id === edge.from);
    const toNode = ir.nodes.find((n) => n.id === edge.to);
    const fromL = nodeLayerMap.get(edge.from) ?? 0;
    const toL = nodeLayerMap.get(edge.to) ?? 0;
    const isBypassSkip = toL >= fromL + 2 && fromNode?.shape !== "diamond";
    const defaultExitWall = isBypassSkip ? isLR ? "N" : "W" : void 0;
    const fA = edge.exitWall ? wallAnchor(fromRect, edge.exitWall) : defaultExitWall ? wallAnchor(fromRect, defaultExitWall) : edgeAnchor(fromRect, ir.direction, "exit", toRect2, fromNode?.shape);
    const tA = edge.entryWall ? wallAnchor(toRect2, edge.entryWall) : edgeAnchor(toRect2, ir.direction, "enter", fromRect, toNode?.shape);
    const fromWall = fA.portDir;
    const toWall = tA.portDir;
    const toPeerCoord = fromWall === "N" || fromWall === "S" ? toRect2.x + toRect2.width / 2 : toRect2.y + toRect2.height / 2;
    const fromPeerCoord = toWall === "N" || toWall === "S" ? fromRect.x + fromRect.width / 2 : fromRect.y + fromRect.height / 2;
    edgeWalls.set(ei, { fromWall, toWall, fromPeerCoord, toPeerCoord });
  }
  const nodeWallOut = /* @__PURE__ */ new Map();
  const nodeWallIn = /* @__PURE__ */ new Map();
  for (const [ei, ew] of edgeWalls) {
    const edge = ir.edges[ei];
    const outKey = `${edge.from}:${ew.fromWall}`;
    const inKey = `${edge.to}:${ew.toWall}`;
    if (!nodeWallOut.has(outKey)) nodeWallOut.set(outKey, []);
    if (!nodeWallIn.has(inKey)) nodeWallIn.set(inKey, []);
    nodeWallOut.get(outKey).push(ei);
    nodeWallIn.get(inKey).push(ei);
  }
  for (const [, list2] of nodeWallOut) {
    list2.sort((a, b) => edgeWalls.get(a).toPeerCoord - edgeWalls.get(b).toPeerCoord);
  }
  for (const [, list2] of nodeWallIn) {
    list2.sort((a, b) => edgeWalls.get(a).fromPeerCoord - edgeWalls.get(b).fromPeerCoord);
  }
  for (let ei = 0; ei < ir.edges.length; ei++) {
    const edge = ir.edges[ei];
    const fromRect = nodePos.get(edge.from);
    const toRect2 = nodePos.get(edge.to);
    if (!fromRect || !toRect2) continue;
    const dash = edge.style === "dotted" ? "6 3" : edge.style === "dashed" ? "8 4" : void 0;
    const sw = edge.style === "thick" ? edgeTheme.strokeWidth * 2 : edgeTheme.strokeWidth;
    const color = edge.style === "dotted" ? palette.textMuted : palette.primary;
    if (edge.from === edge.to) {
      const loop2 = selfLoopRoute(fromRect, isLR);
      elements.push(
        p.path(loop2.path, color, sw, {
          ...edge.animation && edge.animation !== "none" ? { animated: edge.animation } : {},
          ...dash !== void 0 ? { dash } : {},
          markerEnd: ARROW_MARKER_ID
        })
      );
      bowMaxX = Math.max(bowMaxX, loop2.maxX);
      bowMaxY = Math.max(bowMaxY, loop2.maxY);
      if (edge.label) {
        elements.push(
          p.text(
            edge.label,
            loop2.labelPos.x,
            loop2.labelPos.y - 4,
            edgeTheme.labelFontSize,
            palette.textMuted,
            { anchor: "middle" }
          )
        );
      }
      continue;
    }
    if (backEdges.has(ei)) {
      const bow = backEdgeRoute(fromRect, toRect2, isLR);
      elements.push(
        p.path(bow.path, color, sw, {
          ...edge.animation && edge.animation !== "none" ? { animated: edge.animation } : {},
          ...dash !== void 0 ? { dash } : {},
          markerEnd: ARROW_MARKER_ID
        })
      );
      bowMaxX = Math.max(bowMaxX, bow.maxX);
      bowMaxY = Math.max(bowMaxY, bow.maxY);
      if (edge.label) {
        elements.push(
          p.text(
            edge.label,
            bow.labelPos.x,
            bow.labelPos.y - 4,
            edgeTheme.labelFontSize,
            palette.textMuted,
            { anchor: "middle" }
          )
        );
      }
      continue;
    }
    const fromNode = ir.nodes.find((n) => n.id === edge.from);
    const toNode = ir.nodes.find((n) => n.id === edge.to);
    const ew = edgeWalls.get(ei);
    const outList = ew ? nodeWallOut.get(`${edge.from}:${ew.fromWall}`) : void 0;
    const inList = ew ? nodeWallIn.get(`${edge.to}:${ew.toWall}`) : void 0;
    const outIdx = outList && outList.length > 1 ? outList.indexOf(ei) : 0;
    const outTot = outList ? outList.length : 1;
    const inIdx = inList && inList.length > 1 ? inList.indexOf(ei) : 0;
    const inTot = inList ? inList.length : 1;
    const fromAnchor = edge.exitWall ? wallAnchor(fromRect, edge.exitWall, outIdx, outTot) : ew ? wallAnchor(fromRect, ew.fromWall, outIdx, outTot) : edgeAnchor(fromRect, ir.direction, "exit", toRect2, fromNode?.shape, outIdx, outTot);
    const toAnchor = edge.entryWall ? wallAnchor(toRect2, edge.entryWall, inIdx, inTot) : ew ? wallAnchor(toRect2, ew.toWall, inIdx, inTot) : edgeAnchor(toRect2, ir.direction, "enter", fromRect, toNode?.shape, inIdx, inTot);
    const obstacles = [];
    for (const [id, r] of nodePos) {
      if (id !== edge.from && id !== edge.to) {
        obstacles.push(r);
      }
    }
    const style = edge.routing ?? "orthogonal";
    const router = getRouter(style) ?? defaultRouter;
    const route = router.route({
      from: fromAnchor.point,
      to: toAnchor.point,
      style,
      curveStyle: "linear",
      obstacles,
      padding: 8,
      fromDir: fromAnchor.portDir,
      toDir: toAnchor.portDir
    });
    elements.push(
      p.path(route.path, color, sw, {
        ...edge.animation && edge.animation !== "none" ? { animated: edge.animation } : {},
        ...dash !== void 0 ? { dash } : {},
        markerEnd: ARROW_MARKER_ID
      })
    );
    if (edge.label) {
      const lp = route.labelPosition;
      elements.push(
        p.text(edge.label, lp.x, lp.y - 4, edgeTheme.labelFontSize, palette.textMuted, {
          anchor: "middle"
        })
      );
    }
  }
  const icons = options?.icons;
  for (const node of ir.nodes) {
    const r = nodePos.get(node.id);
    if (!r) continue;
    const nodeElements = [];
    const isFocal = node.classes?.includes("focal") || node.status === "active";
    const fill = isFocal ? palette.primary + "22" : nodeStatusFill(node, palette);
    const stroke = isFocal ? palette.primary : palette.border;
    const sw = isFocal ? Math.max(edgeTheme.strokeWidth * 1.5, 2) : edgeTheme.strokeWidth;
    if (node.shape === "card") {
      nodeElements.push(p.rect(r, fill, stroke, sw, { rx: 6, fillOpacity: 0.85 }));
      const { title, body } = splitCardLabel(node.label);
      const iconX = r.x + CARD_PAD;
      const iconY = r.y + (r.height - CARD_ICON_BOX) / 2;
      const textX = r.x + CARD_PAD + CARD_ICON_BOX + CARD_ICON_GAP;
      const rightW = r.width - 2 * CARD_PAD - CARD_ICON_BOX - CARD_ICON_GAP;
      if (node.icon !== void 0 && icons !== void 0) {
        const resolved = resolveIcon(node.icon, icons);
        if (resolved.ok) {
          const glyphSize = CARD_ICON_BOX - 8;
          const gx = iconX + (CARD_ICON_BOX - glyphSize) / 2;
          const gy = iconY + (CARD_ICON_BOX - glyphSize) / 2;
          nodeElements.push(p.icon(resolved.value, gx, gy, glyphSize, { color: palette.primary }));
        }
      }
      const bodyText = body ? body.replace(/\\n|\n/g, " ").trim() : "";
      const wrapped = bodyText ? wrapText2(bodyText, typography.smallFontSize, rightW, CARD_MAX_BODY_LINES) : { lines: [] };
      const hasBody = wrapped.lines.length > 0;
      const titleBaseY = hasBody ? r.y + CARD_PAD + typography.baseFontSize * 0.85 : r.y + r.height / 2 + typography.baseFontSize * 0.35;
      nodeElements.push(
        p.text(title, textX, titleBaseY, typography.baseFontSize, palette.text, { weight: "bold" })
      );
      if (hasBody) {
        const titleLH = typography.baseFontSize * 1.2;
        let bodyY = r.y + CARD_PAD + titleLH + typography.smallFontSize * 0.85;
        for (const line of wrapped.lines) {
          nodeElements.push(
            p.text(line, textX, bodyY, typography.smallFontSize, palette.textMuted)
          );
          bodyY += typography.smallFontSize * 1.2;
        }
      }
    } else {
      nodeElements.push(...renderNodeShape(node, r, fill, stroke, sw, theme));
      let textRegionX = r.x;
      let textRegionW = r.width;
      if (node.icon !== void 0 && icons !== void 0) {
        const resolved = resolveIcon(node.icon, icons);
        if (resolved.ok) {
          const iconSize = Math.min(28, r.height - 8);
          const ix = r.x + 8;
          const iy = r.y + (r.height - iconSize) / 2;
          nodeElements.push(p.icon(resolved.value, ix, iy, iconSize, { color: palette.text }));
          textRegionX = ix + iconSize;
          textRegionW = r.width - (iconSize + 8);
        }
      }
      if (node.label.includes("::")) {
        const info = measureFormattedText(
          node.label,
          typography.baseFontSize,
          typography.smallFontSize,
          textRegionW - 16
        );
        nodeElements.push(
          ...renderFormattedText(
            p,
            info,
            textRegionX,
            r.y,
            textRegionW,
            r.height,
            typography.baseFontSize,
            typography.smallFontSize,
            palette.text,
            palette.textMuted,
            { align: "middle", defaultBold: true }
          )
        );
      } else {
        const lines2 = splitAndWrapLabel(node.label, typography.baseFontSize);
        const totalLines = lines2.length;
        const lineH = typography.baseFontSize * 1.2;
        const textCenterX = textRegionX + textRegionW / 2;
        for (let i = 0; i < totalLines; i++) {
          const lineY = r.y + r.height / 2 - (totalLines - 1) * lineH / 2 + i * lineH + typography.baseFontSize * 0.35;
          nodeElements.push(
            p.text(lines2[i], textCenterX, lineY, typography.baseFontSize, palette.text, {
              anchor: "middle"
            })
          );
        }
      }
    }
    elements.push(p.group(nodeElements, { id: node.id }));
  }
  const allRects = [...nodePos.values()];
  const nodeRight = Math.max(...allRects.map((r) => r.x + r.width));
  const nodeBottom = Math.max(...allRects.map((r) => r.y + r.height));
  const right = (Number.isFinite(bowMaxX) ? Math.max(nodeRight, bowMaxX) : nodeRight) + margin;
  const bottom = (Number.isFinite(bowMaxY) ? Math.max(nodeBottom, bowMaxY) : nodeBottom) + margin;
  const titleOffset = ir.metadata.title ? typography.titleFontSize + 12 : 0;
  let scene = {
    viewBox: { x: 0, y: 0, width: right, height: bottom + titleOffset },
    background: palette.background,
    elements,
    defs: [arrowMarkerDef(palette.primary, edgeTheme.arrowSize)]
  };
  scene = applyOverlays(scene, ir.overlays, theme);
  const anchors = {};
  for (const [id, rect] of nodePos) {
    anchors[id] = {
      bounds: rect,
      ports: {
        N: { x: rect.x + rect.width / 2, y: rect.y },
        S: { x: rect.x + rect.width / 2, y: rect.y + rect.height },
        E: { x: rect.x + rect.width, y: rect.y + rect.height / 2 },
        W: { x: rect.x, y: rect.y + rect.height / 2 }
      }
    };
  }
  const occupiedPorts = [];
  for (const edge of ir.edges) {
    const fromRect = nodePos.get(edge.from);
    const toRect2 = nodePos.get(edge.to);
    if (!fromRect || !toRect2) continue;
    const fromNode = ir.nodes.find((n) => n.id === edge.from);
    const toNode = ir.nodes.find((n) => n.id === edge.to);
    const fromAnch = edgeAnchor(fromRect, ir.direction, "exit", toRect2, fromNode?.shape);
    const toAnch = edgeAnchor(toRect2, ir.direction, "enter", fromRect, toNode?.shape);
    occupiedPorts.push({
      nodeKey: edge.from,
      wall: fromAnch.portDir,
      t: wallT(fromRect, fromAnch.portDir, fromAnch.point),
      source: "intra"
    });
    occupiedPorts.push({
      nodeKey: edge.to,
      wall: toAnch.portDir,
      t: wallT(toRect2, toAnch.portDir, toAnch.point),
      source: "intra"
    });
  }
  return { scene, anchors, occupiedPorts };
}
function findBackEdges(nodes, edges2) {
  const adj = /* @__PURE__ */ new Map();
  for (const n of nodes) adj.set(n.id, []);
  edges2.forEach((e, idx) => {
    adj.get(e.from)?.push({ to: e.to, idx });
  });
  const WHITE = 0, GRAY = 1, BLACK2 = 2;
  const color = /* @__PURE__ */ new Map();
  for (const n of nodes) color.set(n.id, WHITE);
  const backEdges = /* @__PURE__ */ new Set();
  for (const start of nodes) {
    if (color.get(start.id) !== WHITE) continue;
    const stack2 = [{ id: start.id, i: 0 }];
    color.set(start.id, GRAY);
    while (stack2.length > 0) {
      const frame = stack2[stack2.length - 1];
      const neighbors = adj.get(frame.id);
      if (frame.i < neighbors.length) {
        const { to, idx } = neighbors[frame.i];
        frame.i++;
        const c = color.get(to);
        if (c === GRAY) {
          backEdges.add(idx);
        } else if (c === WHITE) {
          color.set(to, GRAY);
          stack2.push({ id: to, i: 0 });
        }
      } else {
        color.set(frame.id, BLACK2);
        stack2.pop();
      }
    }
  }
  return backEdges;
}
function assignLayers(nodes, edges2) {
  const backEdges = findBackEdges(nodes, edges2);
  const forwardEdges = edges2.filter((_, i) => !backEdges.has(i));
  const solidEdges = forwardEdges.filter((e) => e.style !== "dotted" && e.style !== "dashed");
  const dottedEdges = forwardEdges.filter((e) => e.style === "dotted" || e.style === "dashed");
  const solidPreds = /* @__PURE__ */ new Map();
  for (const n of nodes) solidPreds.set(n.id, /* @__PURE__ */ new Set());
  for (const e of solidEdges) solidPreds.get(e.to)?.add(e.from);
  const layers = /* @__PURE__ */ new Map();
  const queue2 = [];
  for (const n of nodes) {
    if ((solidPreds.get(n.id)?.size ?? 0) === 0) {
      queue2.push({ id: n.id, layer: 0 });
    }
  }
  while (queue2.length > 0) {
    const item = queue2.shift();
    const current = layers.get(item.id) ?? -1;
    if (item.layer <= current) continue;
    layers.set(item.id, item.layer);
    for (const e of solidEdges) {
      if (e.from === item.id) queue2.push({ id: e.to, layer: item.layer + 1 });
    }
  }
  for (const e of dottedEdges) {
    if ((solidPreds.get(e.to)?.size ?? 0) === 0) {
      const srcLayer = layers.get(e.from) ?? 0;
      const cur = layers.get(e.to) ?? 0;
      if (cur < srcLayer + 1) {
        layers.set(e.to, srcLayer + 1);
        for (const f2 of solidEdges) {
          if (f2.from === e.to) queue2.push({ id: f2.to, layer: srcLayer + 2 });
        }
      }
    }
  }
  for (const n of nodes) {
    if (!layers.has(n.id)) layers.set(n.id, 0);
  }
  return layers;
}
function groupByLayer(nodes, layers) {
  const groups = /* @__PURE__ */ new Map();
  for (const node of nodes) {
    const l = layers.get(node.id) ?? 0;
    if (!groups.has(l)) groups.set(l, []);
    groups.get(l).push(node);
  }
  return new Map([...groups.entries()].sort(([a], [b]) => a - b));
}
function minimizeCrossings(byLayer, edges2, backEdgeSet) {
  const pred = /* @__PURE__ */ new Map();
  const succ = /* @__PURE__ */ new Map();
  for (const [, nodes] of byLayer) {
    for (const n of nodes) {
      pred.set(n.id, []);
      succ.set(n.id, []);
    }
  }
  edges2.forEach((e, i) => {
    if (backEdgeSet.has(i) || e.from === e.to) return;
    pred.get(e.to)?.push(e.from);
    succ.get(e.from)?.push(e.to);
  });
  const layerKeys = [...byLayer.keys()].sort((a, b) => a - b);
  const origOrder = /* @__PURE__ */ new Map();
  for (const [, nodes] of byLayer) nodes.forEach((n, i) => origOrder.set(n.id, i));
  const order = /* @__PURE__ */ new Map();
  for (const k of layerKeys) order.set(k, [...byLayer.get(k)]);
  const posInLayer = /* @__PURE__ */ new Map();
  function rebuildPos() {
    for (const [, nodes] of order) nodes.forEach((n, i) => posInLayer.set(n.id, i));
  }
  rebuildPos();
  function reorderLayer(layerIdx, neighborMap) {
    const curr = order.get(layerIdx);
    const bary = curr.map((node, i) => {
      const nbrs = neighborMap.get(node.id) ?? [];
      if (nbrs.length === 0) {
        return { node, b: i, orig: origOrder.get(node.id) ?? i };
      }
      const sum = nbrs.reduce((s, nid) => s + (posInLayer.get(nid) ?? 0), 0);
      return { node, b: sum / nbrs.length, orig: origOrder.get(node.id) ?? i };
    });
    bary.sort((a, b) => a.b !== b.b ? a.b - b.b : a.orig - b.orig);
    order.set(
      layerIdx,
      bary.map((e) => e.node)
    );
    rebuildPos();
  }
  const MAX_PASSES = 4;
  for (let pass = 0; pass < MAX_PASSES; pass++) {
    if (pass % 2 === 0) {
      for (let li = 1; li < layerKeys.length; li++) reorderLayer(layerKeys[li], pred);
    } else {
      for (let li = layerKeys.length - 2; li >= 0; li--) reorderLayer(layerKeys[li], succ);
    }
  }
  return order;
}
function assignCoordinatesBK(byLayer, edges2, backEdgeSet, isLR, isReverse, maxNodesInLayer, nodeW, nodeH, colGap, rowGap, margin, nodeSizes) {
  const getW = (id) => nodeSizes?.get(id)?.width ?? nodeW;
  const getH = (id) => nodeSizes?.get(id)?.height ?? nodeH;
  const nodeLayer = /* @__PURE__ */ new Map();
  for (const [l, nodes] of byLayer) {
    for (const n of nodes) nodeLayer.set(n.id, l);
  }
  const predMap = /* @__PURE__ */ new Map();
  const succMap = /* @__PURE__ */ new Map();
  for (const [, nodes] of byLayer) {
    for (const n of nodes) {
      predMap.set(n.id, []);
      succMap.set(n.id, []);
    }
  }
  edges2.forEach((e, i) => {
    if (backEdgeSet.has(i) || e.from === e.to) return;
    const fromL = nodeLayer.get(e.from);
    const toL = nodeLayer.get(e.to);
    if (fromL !== void 0 && toL !== void 0 && fromL < toL) {
      predMap.get(e.to)?.push(e.from);
      succMap.get(e.from)?.push(e.to);
    }
  });
  const layerKeys = [...byLayer.keys()].sort((a, b) => a - b);
  const numLayers = layerKeys.length;
  const crossGap = isLR ? rowGap : colGap;
  const mainGap = isLR ? colGap : rowGap;
  let globalCrossSize = isLR ? nodeH : nodeW;
  for (const [, nodes] of byLayer) {
    for (const n of nodes) {
      const cs = isLR ? getH(n.id) : getW(n.id);
      if (cs > globalCrossSize) globalCrossSize = cs;
    }
  }
  const crossStep = globalCrossSize + crossGap;
  const layerMainSizes = layerKeys.map((lk) => {
    const nodes = byLayer.get(lk);
    let mx = isLR ? nodeW : nodeH;
    for (const n of nodes) {
      const ms = isLR ? getW(n.id) : getH(n.id);
      if (ms > mx) mx = ms;
    }
    return mx;
  });
  const fwdMainPos = [];
  let cumMain = margin;
  for (let li = 0; li < numLayers; li++) {
    fwdMainPos.push(cumMain);
    cumMain += layerMainSizes[li] + mainGap;
  }
  function onePass(topDown, fallbackPositions) {
    const crossPos = /* @__PURE__ */ new Map();
    const neighborMap = topDown ? predMap : succMap;
    const indices = topDown ? Array.from({ length: numLayers }, (_, i) => i) : Array.from({ length: numLayers }, (_, i) => numLayers - 1 - i);
    for (const li of indices) {
      const layerIdx = layerKeys[li];
      const nodes = byLayer.get(layerIdx);
      const count = nodes.length;
      if (count === 0) continue;
      const centeredStart = margin + (maxNodesInLayer - count) * crossStep / 2;
      const pref = nodes.map((node, i) => {
        const nbrs = neighborMap.get(node.id) ?? [];
        if (nbrs.length === 0) {
          if (fallbackPositions && fallbackPositions.has(node.id)) {
            return fallbackPositions.get(node.id);
          }
          return centeredStart + i * crossStep;
        }
        if (topDown && nbrs.length === 1) {
          const p = nbrs[0];
          const siblings = (succMap.get(p) ?? []).filter((sid) => nodes.some((n) => n.id === sid));
          if (siblings.length > 1) {
            const sIdx = siblings.indexOf(node.id);
            const pPos = crossPos.get(p) ?? centeredStart;
            return pPos + (sIdx - (siblings.length - 1) / 2) * crossStep;
          }
        }
        const nbrPos = nbrs.map((nid) => crossPos.get(nid)).filter((p) => p !== void 0);
        return nbrPos.reduce((s, p) => s + p, 0) / nbrPos.length;
      });
      const pos = [...pref];
      pos[0] = Math.max(margin, pos[0]);
      for (let i = 1; i < count; i++) {
        pos[i] = Math.max(pos[i], pos[i - 1] + crossStep);
      }
      for (let i = count - 2; i >= 0; i--) {
        const maxAllowed = pos[i + 1] - crossStep;
        if (pos[i] > maxAllowed) pos[i] = maxAllowed;
      }
      const minX = Math.min(...pos);
      const shift = minX < margin ? margin - minX : 0;
      for (let i = 0; i < count; i++) {
        crossPos.set(nodes[i].id, pos[i] + shift);
      }
    }
    return crossPos;
  }
  const pass2 = onePass(false);
  const pass1 = onePass(true, pass2);
  const nodePos = /* @__PURE__ */ new Map();
  for (let li = 0; li < numLayers; li++) {
    const layerIdx = layerKeys[li];
    const nodes = byLayer.get(layerIdx);
    const layerMainSz = layerMainSizes[li];
    const mainPos = isReverse ? fwdMainPos[numLayers - 1 - li] : fwdMainPos[li];
    for (const node of nodes) {
      const c1 = pass1.get(node.id) ?? margin;
      const c2 = pass2.get(node.id) ?? margin;
      const preds = predMap.get(node.id) ?? [];
      const slotLeft = preds.length === 0 ? c2 : c1;
      const nw = getW(node.id);
      const nh = getH(node.id);
      const crossOffset = (globalCrossSize - (isLR ? nh : nw)) / 2;
      const mainOffset = (layerMainSz - (isLR ? nw : nh)) / 2;
      nodePos.set(node.id, {
        x: isLR ? mainPos + mainOffset : slotLeft + crossOffset,
        y: isLR ? slotLeft + crossOffset : mainPos + mainOffset,
        width: nw,
        height: nh
      });
    }
  }
  return nodePos;
}
function renderNodeShape(node, r, fill, stroke, sw, theme) {
  const { x, y, width: w, height: h2 } = r;
  const stdRadius = theme?.nodes?.standard.cornerRadius ?? 6;
  const stdSw = theme?.nodes?.standard.borderWidth ?? sw;
  const leafRadius = theme?.nodes?.leaf.cornerRadius ?? h2 / 2;
  const leafSw = theme?.nodes?.leaf.borderWidth ?? sw;
  const dsSw = theme?.nodes?.datastore.borderWidth ?? sw;
  switch (node.shape) {
    case "diamond": {
      const cx = x + w / 2, cy = y + h2 / 2;
      return [
        {
          type: "path",
          d: `M ${cx} ${y} L ${x + w} ${cy} L ${cx} ${y + h2} L ${x} ${cy} Z`,
          fill,
          stroke,
          strokeWidth: stdSw
        }
      ];
    }
    case "circle":
      return [
        {
          type: "circle",
          center: { x: x + w / 2, y: y + h2 / 2 },
          radius: Math.min(w, h2) / 2,
          fill,
          stroke,
          strokeWidth: stdSw
        }
      ];
    case "rounded-rect":
      return [{ type: "rect", bounds: r, fill, stroke, strokeWidth: stdSw, rx: stdRadius }];
    case "stadium":
      return [
        {
          type: "rect",
          bounds: r,
          fill,
          stroke,
          strokeWidth: leafSw,
          rx: Math.min(h2 / 2, leafRadius)
        }
      ];
    case "subroutine":
      return [
        { type: "rect", bounds: r, fill, stroke, strokeWidth: stdSw, rx: 2 },
        {
          type: "path",
          d: `M ${x + 8} ${y} L ${x + 8} ${y + h2} M ${x + w - 8} ${y} L ${x + w - 8} ${y + h2}`,
          fill: "none",
          stroke,
          strokeWidth: stdSw * 0.7
        }
      ];
    case "cylinder": {
      const ry = Math.min(10, Math.max(4, h2 * 0.15));
      const dBody = `M ${x} ${y + ry} A ${w / 2} ${ry} 0 0 1 ${x + w} ${y + ry} L ${x + w} ${y + h2 - ry} A ${w / 2} ${ry} 0 0 1 ${x} ${y + h2 - ry} Z`;
      const dRim = `M ${x} ${y + ry} A ${w / 2} ${ry} 0 0 0 ${x + w} ${y + ry}`;
      return [
        { type: "path", d: dBody, fill, stroke, strokeWidth: dsSw },
        { type: "path", d: dRim, fill: "none", stroke, strokeWidth: dsSw }
      ];
    }
    case "hexagon": {
      const chamfer = Math.min(w * 0.15, h2 * 0.4);
      const d = `M ${x + chamfer} ${y} L ${x + w - chamfer} ${y} L ${x + w} ${y + h2 / 2} L ${x + w - chamfer} ${y + h2} L ${x + chamfer} ${y + h2} L ${x} ${y + h2 / 2} Z`;
      return [{ type: "path", d, fill, stroke, strokeWidth: stdSw }];
    }
    case "parallelogram": {
      const slant = Math.min(w * 0.2, 16);
      const d = `M ${x + slant} ${y} L ${x + w} ${y} L ${x + w - slant} ${y + h2} L ${x} ${y + h2} Z`;
      return [{ type: "path", d, fill, stroke, strokeWidth: stdSw }];
    }
    case "parallelogram-alt": {
      const slant = Math.min(w * 0.2, 16);
      const d = `M ${x} ${y} L ${x + w - slant} ${y} L ${x + w} ${y + h2} L ${x + slant} ${y + h2} Z`;
      return [{ type: "path", d, fill, stroke, strokeWidth: stdSw }];
    }
    case "asymmetric": {
      const point = Math.min(w * 0.15, 14);
      const d = `M ${x} ${y} L ${x + w - point} ${y} L ${x + w} ${y + h2 / 2} L ${x + w - point} ${y + h2} L ${x} ${y + h2} Z`;
      return [{ type: "path", d, fill, stroke, strokeWidth: stdSw }];
    }
    case "card":
      return [{ type: "rect", bounds: r, fill, stroke, strokeWidth: stdSw, rx: stdRadius }];
    default:
      return [{ type: "rect", bounds: r, fill, stroke, strokeWidth: stdSw, rx: stdRadius }];
  }
}
function wallAnchor(r, wall, portIndex = 0, portTotal = 1) {
  const t = portTotal <= 1 ? 0.5 : (portIndex + 1) / (portTotal + 1);
  switch (wall) {
    case "N":
      return { point: { x: r.x + t * r.width, y: r.y }, portDir: "N" };
    case "S":
      return { point: { x: r.x + t * r.width, y: r.y + r.height }, portDir: "S" };
    case "E":
      return { point: { x: r.x + r.width, y: r.y + t * r.height }, portDir: "E" };
    case "W":
      return { point: { x: r.x, y: r.y + t * r.height }, portDir: "W" };
  }
}
function edgeAnchor(r, dir, role, peer, nodeShape, portIndex = 0, portTotal = 1) {
  const cx = r.x + r.width / 2;
  const cy = r.y + r.height / 2;
  const pcx = peer.x + peer.width / 2;
  const pcy = peer.y + peer.height / 2;
  const dx = pcx - cx;
  const dy = pcy - cy;
  const isLR = dir === "LR" || dir === "RL";
  const t = portTotal <= 1 ? 0.5 : (portIndex + 1) / (portTotal + 1);
  const getPort = (wall) => {
    switch (wall) {
      case "N":
        return { point: { x: r.x + t * r.width, y: r.y }, portDir: "N" };
      case "S":
        return { point: { x: r.x + t * r.width, y: r.y + r.height }, portDir: "S" };
      case "E":
        return { point: { x: r.x + r.width, y: r.y + t * r.height }, portDir: "E" };
      case "W":
        return { point: { x: r.x, y: r.y + t * r.height }, portDir: "W" };
    }
  };
  if (isLR) {
    if (nodeShape === "diamond") {
      if (role === "exit") {
        if (dy < -1) return getPort("N");
        if (dy > 1) return getPort("S");
        return getPort("E");
      } else {
        if (dx < -r.width) {
          if (peer.y + peer.height <= r.y) return getPort("N");
          if (peer.y >= r.y + r.height) return getPort("S");
          return getPort("W");
        }
        if (dy < -1) return getPort("N");
        if (dy > 1) return getPort("S");
        return getPort("W");
      }
    }
    const offAxis = Math.abs(dy);
    const onAxis = Math.abs(dx);
    if (offAxis > onAxis && offAxis > r.height / 2) {
      return dy > 0 ? getPort("S") : getPort("N");
    }
    return role === "exit" ? getPort("E") : getPort("W");
  } else {
    if (nodeShape === "diamond") {
      if (role === "exit") {
        if (dx < -1) return getPort("W");
        if (dx > 1) return getPort("E");
        return getPort("S");
      } else {
        if (dy < -r.height) {
          if (peer.x + peer.width <= r.x) return getPort("W");
          if (peer.x >= r.x + r.width) return getPort("E");
          return getPort("N");
        }
        if (dx < -1) return getPort("W");
        if (dx > 1) return getPort("E");
        return getPort("N");
      }
    }
    const offAxis = Math.abs(dx);
    const onAxis = Math.abs(dy);
    if (offAxis > onAxis && offAxis > r.width / 2) {
      return dx > 0 ? getPort("E") : getPort("W");
    }
    return role === "exit" ? getPort("S") : getPort("N");
  }
}
function backEdgeRoute(from, to, isLR) {
  if (isLR) {
    const start2 = { x: from.x + from.width / 2, y: from.y + from.height };
    const end2 = { x: to.x + to.width / 2, y: to.y + to.height };
    const span2 = Math.abs(end2.x - start2.x);
    const bow2 = Math.max(NODE_H * 0.9, span2 * 0.35);
    const c12 = { x: start2.x, y: start2.y + bow2 };
    const c22 = { x: end2.x, y: end2.y + bow2 };
    return {
      path: `M ${start2.x} ${start2.y} C ${c12.x} ${c12.y}, ${c22.x} ${c22.y}, ${end2.x} ${end2.y}`,
      labelPos: { x: (start2.x + end2.x) / 2, y: Math.max(start2.y, end2.y) + bow2 * 0.75 },
      maxX: Math.max(start2.x, end2.x),
      maxY: Math.max(c12.y, c22.y)
    };
  }
  const start = { x: from.x + from.width, y: from.y + from.height / 2 };
  const end = { x: to.x + to.width, y: to.y + to.height / 2 };
  const span = Math.abs(end.y - start.y);
  const bow = Math.max(NODE_W * 0.75, span * 0.35);
  const c1 = { x: start.x + bow, y: start.y };
  const c2 = { x: end.x + bow, y: end.y };
  return {
    path: `M ${start.x} ${start.y} C ${c1.x} ${c1.y}, ${c2.x} ${c2.y}, ${end.x} ${end.y}`,
    labelPos: { x: Math.max(start.x, end.x) + bow * 0.75, y: (start.y + end.y) / 2 },
    maxX: Math.max(c1.x, c2.x),
    maxY: Math.max(start.y, end.y)
  };
}
function selfLoopRoute(r, isLR) {
  const loop2 = 28;
  if (isLR) {
    const x1 = r.x + r.width * 0.35;
    const x2 = r.x + r.width * 0.65;
    const sy = r.y + r.height;
    return {
      path: `M ${x1} ${sy} C ${x1 - loop2} ${sy + loop2}, ${x2 + loop2} ${sy + loop2}, ${x2} ${sy}`,
      labelPos: { x: r.x + r.width / 2, y: sy + loop2 + 4 },
      maxX: x2 + loop2,
      maxY: sy + loop2
    };
  }
  const y1 = r.y + r.height * 0.3;
  const y2 = r.y + r.height * 0.7;
  const ex = r.x + r.width;
  return {
    path: `M ${ex} ${y1} C ${ex + loop2} ${y1 - loop2}, ${ex + loop2} ${y2 + loop2}, ${ex} ${y2}`,
    labelPos: { x: ex + loop2, y: r.y + r.height / 2 },
    maxX: ex + loop2,
    maxY: y2 + loop2
  };
}
function nodeStatusFill(node, palette) {
  switch (node.status) {
    case "active":
      return palette.primary + "22";
    case "success":
      return palette.success + "22";
    case "warning":
      return palette.warning + "22";
    case "error":
      return palette.error + "22";
    case "muted":
      return palette.surface;
    default:
      return palette.surface;
  }
}
function arrowMarkerDef(color, size) {
  const s = size;
  return `<marker id="${ARROW_MARKER_ID}" markerWidth="${s}" markerHeight="${s * 0.7}" refX="${s - 1}" refY="${s * 0.35}" orient="auto"><polygon points="0 0, ${s} ${s * 0.35}, 0 ${s * 0.7}" fill="${color}" /></marker>`;
}
function wallT(bounds, wall, pt) {
  switch (wall) {
    case "N":
    case "S":
      return bounds.width > 0 ? Math.max(0, Math.min(1, (pt.x - bounds.x) / bounds.width)) : 0.5;
    case "E":
    case "W":
      return bounds.height > 0 ? Math.max(0, Math.min(1, (pt.y - bounds.y) / bounds.height)) : 0.5;
  }
}
function sanitizeId(raw) {
  let id = raw.replace(/([A-Z]+)([A-Z][a-z])/g, "$1-$2").replace(/([a-z0-9])([A-Z])/g, "$1-$2").toLowerCase().replace(/[_\s]+/g, "-").replace(/[^a-z0-9-]/g, "").replace(/-+/g, "-").replace(/^-|-$/g, "");
  if (!id || /^[0-9]/.test(id)) id = "n" + id;
  return id;
}
function stripQuotes(text) {
  if (text.length >= 2 && text[0] === '"' && text[text.length - 1] === '"') {
    return text.slice(1, -1);
  }
  return text;
}
var nodeMap = /* @__PURE__ */ new Map();
var edges = [];
var overlays = [];
var idCounts = /* @__PURE__ */ new Map();
function registerNode(rawId, label, shape) {
  const id = sanitizeId(rawId);
  if (!nodeMap.has(id)) {
    nodeMap.set(id, { id, label: label || rawId, shape: shape || "rect", status: "default" });
  } else if (label !== null && label !== void 0) {
    const n = nodeMap.get(id);
    n.label = label;
    if (shape) n.shape = shape;
  }
  return id;
}
function addEdge(from, to, label, style, ann, edgeProps) {
  const e = { from, to, label: label || void 0, style: style || "solid" };
  if (edgeProps) {
    if (edgeProps.bidirectional) e.bidirectional = true;
    if (edgeProps.undirected) e.undirected = true;
    if (edgeProps.endMarker) e.endMarker = edgeProps.endMarker;
  }
  if (ann) {
    if (ann.animation) e.animation = ann.animation;
    if (ann.routing) e.routing = ann.routing;
    if (ann.exitWall) e.exitWall = ann.exitWall;
    if (ann.entryWall) e.entryWall = ann.entryWall;
  }
  edges.push(e);
}
function reset() {
  nodeMap.clear();
  edges.length = 0;
  overlays.length = 0;
  idCounts.clear();
}
function peg$subclass(child, parent) {
  function C() {
    this.constructor = child;
  }
  C.prototype = parent.prototype;
  child.prototype = new C();
}
function peg$SyntaxError(message, expected, found, location) {
  var self = Error.call(this, message);
  if (Object.setPrototypeOf) {
    Object.setPrototypeOf(self, peg$SyntaxError.prototype);
  }
  self.expected = expected;
  self.found = found;
  self.location = location;
  self.name = "SyntaxError";
  return self;
}
peg$subclass(peg$SyntaxError, Error);
function peg$padEnd(str, targetLength, padString) {
  padString = padString || " ";
  if (str.length > targetLength) {
    return str;
  }
  targetLength -= str.length;
  padString += padString.repeat(targetLength);
  return str + padString.slice(0, targetLength);
}
peg$SyntaxError.prototype.format = function(sources) {
  var str = "Error: " + this.message;
  if (this.location) {
    var src = null;
    var k;
    for (k = 0; k < sources.length; k++) {
      if (sources[k].source === this.location.source) {
        src = sources[k].text.split(/\r\n|\n|\r/g);
        break;
      }
    }
    var s = this.location.start;
    var offset_s = this.location.source && typeof this.location.source.offset === "function" ? this.location.source.offset(s) : s;
    var loc = this.location.source + ":" + offset_s.line + ":" + offset_s.column;
    if (src) {
      var e = this.location.end;
      var filler = peg$padEnd("", offset_s.line.toString().length, " ");
      var line = src[s.line - 1];
      var last = s.line === e.line ? e.column : line.length + 1;
      var hatLen = last - s.column || 1;
      str += "\n --> " + loc + "\n" + filler + " |\n" + offset_s.line + " | " + line + "\n" + filler + " | " + peg$padEnd("", s.column - 1, " ") + peg$padEnd("", hatLen, "^");
    } else {
      str += "\n at " + loc;
    }
  }
  return str;
};
peg$SyntaxError.buildMessage = function(expected, found) {
  var DESCRIBE_EXPECTATION_FNS = {
    literal: function(expectation) {
      return '"' + literalEscape(expectation.text) + '"';
    },
    class: function(expectation) {
      var escapedParts = expectation.parts.map(function(part) {
        return Array.isArray(part) ? classEscape(part[0]) + "-" + classEscape(part[1]) : classEscape(part);
      });
      return "[" + (expectation.inverted ? "^" : "") + escapedParts.join("") + "]";
    },
    any: function() {
      return "any character";
    },
    end: function() {
      return "end of input";
    },
    other: function(expectation) {
      return expectation.description;
    }
  };
  function hex(ch) {
    return ch.charCodeAt(0).toString(16).toUpperCase();
  }
  function literalEscape(s) {
    return s.replace(/\\/g, "\\\\").replace(/"/g, '\\"').replace(/\0/g, "\\0").replace(/\t/g, "\\t").replace(/\n/g, "\\n").replace(/\r/g, "\\r").replace(/[\x00-\x0F]/g, function(ch) {
      return "\\x0" + hex(ch);
    }).replace(/[\x10-\x1F\x7F-\x9F]/g, function(ch) {
      return "\\x" + hex(ch);
    });
  }
  function classEscape(s) {
    return s.replace(/\\/g, "\\\\").replace(/\]/g, "\\]").replace(/\^/g, "\\^").replace(/-/g, "\\-").replace(/\0/g, "\\0").replace(/\t/g, "\\t").replace(/\n/g, "\\n").replace(/\r/g, "\\r").replace(/[\x00-\x0F]/g, function(ch) {
      return "\\x0" + hex(ch);
    }).replace(/[\x10-\x1F\x7F-\x9F]/g, function(ch) {
      return "\\x" + hex(ch);
    });
  }
  function describeExpectation(expectation) {
    return DESCRIBE_EXPECTATION_FNS[expectation.type](expectation);
  }
  function describeExpected(expected2) {
    var descriptions = expected2.map(describeExpectation);
    var i, j;
    descriptions.sort();
    if (descriptions.length > 0) {
      for (i = 1, j = 1; i < descriptions.length; i++) {
        if (descriptions[i - 1] !== descriptions[i]) {
          descriptions[j] = descriptions[i];
          j++;
        }
      }
      descriptions.length = j;
    }
    switch (descriptions.length) {
      case 1:
        return descriptions[0];
      case 2:
        return descriptions[0] + " or " + descriptions[1];
      default:
        return descriptions.slice(0, -1).join(", ") + ", or " + descriptions[descriptions.length - 1];
    }
  }
  function describeFound(found2) {
    return found2 ? '"' + literalEscape(found2) + '"' : "end of input";
  }
  return "Expected " + describeExpected(expected) + " but " + describeFound(found) + " found.";
};
function peg$parse(input, options) {
  options = options !== void 0 ? options : {};
  var peg$FAILED = {};
  var peg$source = options.grammarSource;
  var peg$startRuleFunctions = { Document: peg$parseDocument };
  var peg$startRuleFunction = peg$parseDocument;
  var peg$c0 = "---";
  var peg$c1 = "flowchart";
  var peg$c2 = "graph";
  var peg$c3 = "TD";
  var peg$c4 = "TB";
  var peg$c5 = "BT";
  var peg$c6 = "LR";
  var peg$c7 = "RL";
  var peg$c8 = ";";
  var peg$c9 = "end";
  var peg$c10 = "%%";
  var peg$c11 = "subgraph";
  var peg$c12 = "[";
  var peg$c13 = "]";
  var peg$c14 = "@";
  var peg$c15 = "|";
  var peg$c16 = "<-~->";
  var peg$c17 = "<-_->";
  var peg$c18 = "<-.->";
  var peg$c19 = "<==>";
  var peg$c20 = "<-->";
  var peg$c21 = "-~->";
  var peg$c22 = "-_->";
  var peg$c23 = "-..->";
  var peg$c24 = "-.->";
  var peg$c25 = "===>";
  var peg$c26 = "==>";
  var peg$c27 = "--->";
  var peg$c28 = "-->";
  var peg$c29 = "--x";
  var peg$c30 = "--o";
  var peg$c31 = "-~-";
  var peg$c32 = "-_-";
  var peg$c33 = "-.-";
  var peg$c34 = "===";
  var peg$c35 = "anim:";
  var peg$c36 = "march";
  var peg$c37 = "particle";
  var peg$c38 = "draw";
  var peg$c39 = "pulse";
  var peg$c40 = "glow";
  var peg$c41 = "comet";
  var peg$c42 = "stream";
  var peg$c43 = "flow";
  var peg$c44 = "colorcycle";
  var peg$c45 = "none";
  var peg$c46 = "{";
  var peg$c47 = "anim";
  var peg$c48 = ":";
  var peg$c49 = "}";
  var peg$c50 = "straight";
  var peg$c51 = "orthogonal";
  var peg$c52 = "bezier";
  var peg$c53 = "polyline";
  var peg$c54 = ":::";
  var peg$c55 = '"';
  var peg$c56 = "((";
  var peg$c57 = "))";
  var peg$c58 = "([";
  var peg$c59 = "])";
  var peg$c60 = "[[";
  var peg$c61 = "]]";
  var peg$c62 = "[(";
  var peg$c63 = ")]";
  var peg$c64 = "{{";
  var peg$c65 = "}}";
  var peg$c66 = "[/";
  var peg$c67 = "/]";
  var peg$c68 = "[\\";
  var peg$c69 = "\\]";
  var peg$c70 = ">";
  var peg$c71 = "(";
  var peg$c72 = ")";
  var peg$c73 = "style";
  var peg$c74 = "classDef";
  var peg$c75 = "class";
  var peg$c76 = "click";
  var peg$c77 = "note";
  var peg$c78 = "callout";
  var peg$c79 = "at";
  var peg$c80 = "offset";
  var peg$c81 = ",";
  var peg$c82 = "-";
  var peg$c83 = "legend";
  var peg$c84 = "bottom-right";
  var peg$c85 = "bottom-left";
  var peg$c86 = "top-right";
  var peg$c87 = "top-left";
  var peg$c88 = "\r\n";
  var peg$r0 = /^[^\n]/;
  var peg$r1 = /^[a-zA-Z_]/;
  var peg$r2 = /^[a-zA-Z0-9_]/;
  var peg$r3 = /^[^\]]/;
  var peg$r4 = /^[^\n%]/;
  var peg$r5 = /^[^|]/;
  var peg$r6 = /^[a-zA-Z0-9_\-]/;
  var peg$r7 = /^[ENSW]/;
  var peg$r8 = /^[a-z]/;
  var peg$r9 = /^[a-z0-9\-]/;
  var peg$r10 = /^[^"]/;
  var peg$r11 = /^[^ \t\r\n@[\](){};]/;
  var peg$r12 = /^[^)]/;
  var peg$r13 = /^[^}]/;
  var peg$r14 = /^[^\/]/;
  var peg$r15 = /^[^\\]/;
  var peg$r16 = /^[^\n;]/;
  var peg$r17 = /^[0-9]/;
  var peg$r18 = /^[^:\n]/;
  var peg$r19 = /^[ \t]/;
  var peg$r20 = /^[\n\r]/;
  var peg$e0 = peg$literalExpectation("---", false);
  var peg$e1 = peg$classExpectation(["\n"], true, false);
  var peg$e2 = peg$literalExpectation("flowchart", false);
  var peg$e3 = peg$literalExpectation("graph", false);
  var peg$e4 = peg$literalExpectation("TD", false);
  var peg$e5 = peg$literalExpectation("TB", false);
  var peg$e6 = peg$literalExpectation("BT", false);
  var peg$e7 = peg$literalExpectation("LR", false);
  var peg$e8 = peg$literalExpectation("RL", false);
  var peg$e9 = peg$literalExpectation(";", false);
  var peg$e10 = peg$literalExpectation("end", false);
  var peg$e11 = peg$anyExpectation();
  var peg$e12 = peg$literalExpectation("%%", false);
  var peg$e13 = peg$literalExpectation("subgraph", false);
  var peg$e14 = peg$classExpectation([["a", "z"], ["A", "Z"], "_"], false, false);
  var peg$e15 = peg$classExpectation([["a", "z"], ["A", "Z"], ["0", "9"], "_"], false, false);
  var peg$e16 = peg$literalExpectation("[", false);
  var peg$e17 = peg$classExpectation(["]"], true, false);
  var peg$e18 = peg$literalExpectation("]", false);
  var peg$e19 = peg$classExpectation(["\n", "%"], true, false);
  var peg$e20 = peg$literalExpectation("@", false);
  var peg$e21 = peg$literalExpectation("|", false);
  var peg$e22 = peg$classExpectation(["|"], true, false);
  var peg$e23 = peg$literalExpectation("<-~->", false);
  var peg$e24 = peg$literalExpectation("<-_->", false);
  var peg$e25 = peg$literalExpectation("<-.->", false);
  var peg$e26 = peg$literalExpectation("<==>", false);
  var peg$e27 = peg$literalExpectation("<-->", false);
  var peg$e28 = peg$literalExpectation("-~->", false);
  var peg$e29 = peg$literalExpectation("-_->", false);
  var peg$e30 = peg$literalExpectation("-..->", false);
  var peg$e31 = peg$literalExpectation("-.->", false);
  var peg$e32 = peg$literalExpectation("===>", false);
  var peg$e33 = peg$literalExpectation("==>", false);
  var peg$e34 = peg$literalExpectation("--->", false);
  var peg$e35 = peg$literalExpectation("-->", false);
  var peg$e36 = peg$literalExpectation("--x", false);
  var peg$e37 = peg$literalExpectation("--o", false);
  var peg$e38 = peg$literalExpectation("-~-", false);
  var peg$e39 = peg$literalExpectation("-_-", false);
  var peg$e40 = peg$literalExpectation("-.-", false);
  var peg$e41 = peg$literalExpectation("===", false);
  var peg$e42 = peg$literalExpectation("anim:", false);
  var peg$e43 = peg$literalExpectation("march", false);
  var peg$e44 = peg$literalExpectation("particle", false);
  var peg$e45 = peg$literalExpectation("draw", false);
  var peg$e46 = peg$literalExpectation("pulse", false);
  var peg$e47 = peg$literalExpectation("glow", false);
  var peg$e48 = peg$literalExpectation("comet", false);
  var peg$e49 = peg$literalExpectation("stream", false);
  var peg$e50 = peg$literalExpectation("flow", false);
  var peg$e51 = peg$literalExpectation("colorcycle", false);
  var peg$e52 = peg$literalExpectation("none", false);
  var peg$e53 = peg$classExpectation([["a", "z"], ["A", "Z"], ["0", "9"], "_", "-"], false, false);
  var peg$e54 = peg$literalExpectation("{", false);
  var peg$e55 = peg$literalExpectation("anim", false);
  var peg$e56 = peg$literalExpectation(":", false);
  var peg$e57 = peg$literalExpectation("}", false);
  var peg$e58 = peg$literalExpectation("straight", false);
  var peg$e59 = peg$literalExpectation("orthogonal", false);
  var peg$e60 = peg$literalExpectation("bezier", false);
  var peg$e61 = peg$literalExpectation("polyline", false);
  var peg$e62 = peg$classExpectation(["E", "N", "S", "W"], false, false);
  var peg$e63 = peg$literalExpectation(":::", false);
  var peg$e64 = peg$classExpectation([["a", "z"]], false, false);
  var peg$e65 = peg$classExpectation([["a", "z"], ["0", "9"], "-"], false, false);
  var peg$e66 = peg$literalExpectation('"', false);
  var peg$e67 = peg$classExpectation(['"'], true, false);
  var peg$e68 = peg$classExpectation([" ", "	", "\r", "\n", "@", "[", "]", "(", ")", "{", "}", ";"], true, false);
  var peg$e69 = peg$literalExpectation("((", false);
  var peg$e70 = peg$classExpectation([")"], true, false);
  var peg$e71 = peg$literalExpectation("))", false);
  var peg$e72 = peg$literalExpectation("([", false);
  var peg$e73 = peg$literalExpectation("])", false);
  var peg$e74 = peg$literalExpectation("[[", false);
  var peg$e75 = peg$literalExpectation("]]", false);
  var peg$e76 = peg$literalExpectation("[(", false);
  var peg$e77 = peg$literalExpectation(")]", false);
  var peg$e78 = peg$literalExpectation("{{", false);
  var peg$e79 = peg$classExpectation(["}"], true, false);
  var peg$e80 = peg$literalExpectation("}}", false);
  var peg$e81 = peg$literalExpectation("[/", false);
  var peg$e82 = peg$classExpectation(["/"], true, false);
  var peg$e83 = peg$literalExpectation("/]", false);
  var peg$e84 = peg$literalExpectation("[\\", false);
  var peg$e85 = peg$classExpectation(["\\"], true, false);
  var peg$e86 = peg$literalExpectation("\\]", false);
  var peg$e87 = peg$literalExpectation(">", false);
  var peg$e88 = peg$literalExpectation("(", false);
  var peg$e89 = peg$literalExpectation(")", false);
  var peg$e90 = peg$literalExpectation("style", false);
  var peg$e91 = peg$classExpectation(["\n", ";"], true, false);
  var peg$e92 = peg$literalExpectation("classDef", false);
  var peg$e93 = peg$literalExpectation("class", false);
  var peg$e94 = peg$literalExpectation("click", false);
  var peg$e95 = peg$literalExpectation("note", false);
  var peg$e96 = peg$literalExpectation("callout", false);
  var peg$e97 = peg$literalExpectation("at", false);
  var peg$e98 = peg$literalExpectation("offset", false);
  var peg$e99 = peg$literalExpectation(",", false);
  var peg$e100 = peg$literalExpectation("-", false);
  var peg$e101 = peg$classExpectation([["0", "9"]], false, false);
  var peg$e102 = peg$literalExpectation("legend", false);
  var peg$e103 = peg$literalExpectation("bottom-right", false);
  var peg$e104 = peg$literalExpectation("bottom-left", false);
  var peg$e105 = peg$literalExpectation("top-right", false);
  var peg$e106 = peg$literalExpectation("top-left", false);
  var peg$e107 = peg$classExpectation([":", "\n"], true, false);
  var peg$e108 = peg$otherExpectation("optional whitespace");
  var peg$e109 = peg$classExpectation([" ", "	"], false, false);
  var peg$e110 = peg$otherExpectation("required whitespace");
  var peg$e111 = peg$otherExpectation("newline");
  var peg$e112 = peg$literalExpectation("\r\n", false);
  var peg$e113 = peg$classExpectation(["\n", "\r"], false, false);
  var peg$f0 = function(frontmatter, header, statements) {
    return {
      version: "1.0",
      metadata: frontmatter || {},
      direction: header.direction,
      flow: { nodes: [...nodeMap.values()], edges },
      overlays
    };
  };
  var peg$f1 = function(line) {
    return line;
  };
  var peg$f2 = function(lines2) {
    const meta = {};
    for (const line of lines2) {
      const m = line.match(/^\s*(\w+)\s*:\s*(.+?)\s*$/);
      if (m) meta[m[1]] = m[2];
    }
    return meta;
  };
  var peg$f3 = function(dir) {
    return { direction: dir };
  };
  var peg$f4 = function(stmts) {
    return stmts.filter(Boolean);
  };
  var peg$f5 = function(stmt) {
    return stmt;
  };
  var peg$f6 = function() {
    return null;
  };
  var peg$f7 = function(id, label, body) {
    return { type: "subgraph", id, label: label || id, body };
  };
  var peg$f8 = function(text2) {
    return stripQuotes(text2.trim());
  };
  var peg$f9 = function(label) {
    return stripQuotes(label.trim());
  };
  var peg$f10 = function(head, edge, target, anns, props) {
    const ann = props ? { ...props[1] } : {};
    for (const annotation of anns) Object.assign(ann, annotation[2]);
    return { edge, target, ann };
  };
  var peg$f11 = function(head, rest) {
    let prev = head;
    for (const { edge, target, ann } of rest) {
      addEdge(prev.id, target.id, edge.label, edge.style, ann, edge);
      prev = target;
    }
    return { type: "chain" };
  };
  var peg$f12 = function(op, label) {
    return { ...op, label: label || void 0 };
  };
  var peg$f13 = function(text2) {
    return text2.trim();
  };
  var peg$f14 = function() {
    return { style: "wavy", bidirectional: true };
  };
  var peg$f15 = function() {
    return { style: "dashed", bidirectional: true };
  };
  var peg$f16 = function() {
    return { style: "dotted", bidirectional: true };
  };
  var peg$f17 = function() {
    return { style: "thick", bidirectional: true };
  };
  var peg$f18 = function() {
    return { style: "solid", bidirectional: true };
  };
  var peg$f19 = function() {
    return { style: "wavy" };
  };
  var peg$f20 = function() {
    return { style: "dashed" };
  };
  var peg$f21 = function() {
    return { style: "dotted" };
  };
  var peg$f22 = function() {
    return { style: "dotted" };
  };
  var peg$f23 = function() {
    return { style: "thick" };
  };
  var peg$f24 = function() {
    return { style: "thick" };
  };
  var peg$f25 = function() {
    return { style: "solid" };
  };
  var peg$f26 = function() {
    return { style: "solid" };
  };
  var peg$f27 = function() {
    return { style: "solid", endMarker: "cross" };
  };
  var peg$f28 = function() {
    return { style: "solid", endMarker: "circle" };
  };
  var peg$f29 = function() {
    return { style: "wavy", undirected: true };
  };
  var peg$f30 = function() {
    return { style: "dashed", undirected: true };
  };
  var peg$f31 = function() {
    return { style: "dotted", undirected: true };
  };
  var peg$f32 = function() {
    return { style: "thick", undirected: true };
  };
  var peg$f33 = function() {
    return { style: "solid", undirected: true };
  };
  var peg$f34 = function(value) {
    return { animation: value };
  };
  var peg$f35 = function(value) {
    return value;
  };
  var peg$f36 = function(value) {
    return { animation: value };
  };
  var peg$f37 = function(route, walls) {
    return { routing: route, ...walls ? walls[1] : {} };
  };
  var peg$f38 = function(walls) {
    return walls;
  };
  var peg$f39 = function() {
    return "straight";
  };
  var peg$f40 = function() {
    return "orthogonal";
  };
  var peg$f41 = function() {
    return "bezier";
  };
  var peg$f42 = function() {
    return "polyline";
  };
  var peg$f43 = function(exit, entry) {
    return { exitWall: exit, entryWall: entry };
  };
  var peg$f44 = function(exit) {
    return { exitWall: exit };
  };
  var peg$f45 = function(w) {
    return w;
  };
  var peg$f46 = function(id, shape, c) {
    return c;
  };
  var peg$f47 = function(id, shape, cls) {
    const registeredId = registerNode(id, shape ? shape.label : null, shape ? shape.shape : null);
    if (cls) {
      const node = nodeMap.get(registeredId);
      if (node) {
        node.classes = node.classes || [];
        node.classes.push(cls);
        if (cls === "focal") node.status = "active";
      }
    }
    return { id: registeredId };
  };
  var peg$f48 = function(ref, key, value) {
    return { key, value };
  };
  var peg$f49 = function(ref, anns) {
    if (anns.length > 0) {
      const node = nodeMap.get(ref.id);
      if (node) {
        for (const ann of anns) {
          if (ann.key === "shape") node.shape = ann.value;
          if (ann.key === "icon") node.iconToken = ann.value;
        }
      }
    }
    return { type: "node", ...ref };
  };
  var peg$f50 = function(val) {
    return val;
  };
  var peg$f51 = function(id) {
    return id;
  };
  var peg$f52 = function(text2) {
    return { shape: "circle", label: stripQuotes(text2.trim()) };
  };
  var peg$f53 = function(text2) {
    return { shape: "stadium", label: stripQuotes(text2.trim()) };
  };
  var peg$f54 = function(text2) {
    return { shape: "subroutine", label: stripQuotes(text2.trim()) };
  };
  var peg$f55 = function(text2) {
    return { shape: "cylinder", label: stripQuotes(text2.trim()) };
  };
  var peg$f56 = function(text2) {
    return { shape: "hexagon", label: stripQuotes(text2.trim()) };
  };
  var peg$f57 = function(text2) {
    return { shape: "parallelogram", label: stripQuotes(text2.trim()) };
  };
  var peg$f58 = function(text2) {
    return { shape: "parallelogram-alt", label: stripQuotes(text2.trim()) };
  };
  var peg$f59 = function(text2) {
    return { shape: "asymmetric", label: stripQuotes(text2.trim()) };
  };
  var peg$f60 = function(text2) {
    return { shape: "rect", label: stripQuotes(text2.trim()) };
  };
  var peg$f61 = function(text2) {
    return { shape: "diamond", label: stripQuotes(text2.trim()) };
  };
  var peg$f62 = function(text2) {
    return { shape: "rounded-rect", label: stripQuotes(text2.trim()) };
  };
  var peg$f63 = function(content) {
    return { type: "style", raw: content };
  };
  var peg$f64 = function(content) {
    return { type: "class", raw: content };
  };
  var peg$f65 = function(content) {
    return { type: "click", raw: content };
  };
  var peg$f66 = function(kind, text2, t) {
    return t;
  };
  var peg$f67 = function(kind, text2, target, offset2) {
    const note = { type: kind, text: text2, target, offset: offset2 || void 0 };
    overlays.push(note);
    return note;
  };
  var peg$f68 = function(dx, dy) {
    return { dx, dy };
  };
  var peg$f69 = function(sign, digits) {
    return parseInt((sign || "") + digits, 10);
  };
  var peg$f70 = function(corner, t) {
    return t;
  };
  var peg$f71 = function(corner, title, entries) {
    const legend = { type: "legend", corner, title: title || void 0, entries };
    overlays.push(legend);
    return legend;
  };
  var peg$f72 = function(key, value) {
    return { key: key.trim(), value: value.trim() };
  };
  var peg$currPos = options.peg$currPos | 0;
  var peg$savedPos = peg$currPos;
  var peg$posDetailsCache = [{ line: 1, column: 1 }];
  var peg$maxFailPos = peg$currPos;
  var peg$maxFailExpected = options.peg$maxFailExpected || [];
  var peg$silentFails = options.peg$silentFails | 0;
  var peg$result;
  if (options.startRule) {
    if (!(options.startRule in peg$startRuleFunctions)) {
      throw new Error(`Can't start parsing from rule "` + options.startRule + '".');
    }
    peg$startRuleFunction = peg$startRuleFunctions[options.startRule];
  }
  function text() {
    return input.substring(peg$savedPos, peg$currPos);
  }
  function offset() {
    return peg$savedPos;
  }
  function range() {
    return {
      source: peg$source,
      start: peg$savedPos,
      end: peg$currPos
    };
  }
  function location() {
    return peg$computeLocation(peg$savedPos, peg$currPos);
  }
  function expected(description, location2) {
    location2 = location2 !== void 0 ? location2 : peg$computeLocation(peg$savedPos, peg$currPos);
    throw peg$buildStructuredError(
      [peg$otherExpectation(description)],
      input.substring(peg$savedPos, peg$currPos),
      location2
    );
  }
  function error(message, location2) {
    location2 = location2 !== void 0 ? location2 : peg$computeLocation(peg$savedPos, peg$currPos);
    throw peg$buildSimpleError(message, location2);
  }
  function peg$literalExpectation(text2, ignoreCase) {
    return { type: "literal", text: text2, ignoreCase };
  }
  function peg$classExpectation(parts, inverted, ignoreCase) {
    return { type: "class", parts, inverted, ignoreCase };
  }
  function peg$anyExpectation() {
    return { type: "any" };
  }
  function peg$endExpectation() {
    return { type: "end" };
  }
  function peg$otherExpectation(description) {
    return { type: "other", description };
  }
  function peg$computePosDetails(pos) {
    var details = peg$posDetailsCache[pos];
    var p;
    if (details) {
      return details;
    } else {
      if (pos >= peg$posDetailsCache.length) {
        p = peg$posDetailsCache.length - 1;
      } else {
        p = pos;
        while (!peg$posDetailsCache[--p]) {
        }
      }
      details = peg$posDetailsCache[p];
      details = {
        line: details.line,
        column: details.column
      };
      while (p < pos) {
        if (input.charCodeAt(p) === 10) {
          details.line++;
          details.column = 1;
        } else {
          details.column++;
        }
        p++;
      }
      peg$posDetailsCache[pos] = details;
      return details;
    }
  }
  function peg$computeLocation(startPos, endPos, offset2) {
    var startPosDetails = peg$computePosDetails(startPos);
    var endPosDetails = peg$computePosDetails(endPos);
    var res = {
      source: peg$source,
      start: {
        offset: startPos,
        line: startPosDetails.line,
        column: startPosDetails.column
      },
      end: {
        offset: endPos,
        line: endPosDetails.line,
        column: endPosDetails.column
      }
    };
    if (offset2 && peg$source && typeof peg$source.offset === "function") {
      res.start = peg$source.offset(res.start);
      res.end = peg$source.offset(res.end);
    }
    return res;
  }
  function peg$fail(expected2) {
    if (peg$currPos < peg$maxFailPos) {
      return;
    }
    if (peg$currPos > peg$maxFailPos) {
      peg$maxFailPos = peg$currPos;
      peg$maxFailExpected = [];
    }
    peg$maxFailExpected.push(expected2);
  }
  function peg$buildSimpleError(message, location2) {
    return new peg$SyntaxError(message, null, null, location2);
  }
  function peg$buildStructuredError(expected2, found, location2) {
    return new peg$SyntaxError(
      peg$SyntaxError.buildMessage(expected2, found),
      expected2,
      found,
      location2
    );
  }
  function peg$parseDocument() {
    var s0, s1, s2, s3, s4, s5, s6;
    s0 = peg$currPos;
    s1 = peg$parseFrontmatter();
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    s2 = peg$parse_();
    s3 = peg$parseHeader();
    if (s3 !== peg$FAILED) {
      s4 = peg$parse_();
      s5 = peg$parseStatements();
      s6 = peg$parse_();
      peg$savedPos = s0;
      s0 = peg$f0(s1, s3, s5);
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseFrontmatter() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 3) === peg$c0) {
      s1 = peg$c0;
      peg$currPos += 3;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e0);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$parseNL();
      if (s2 !== peg$FAILED) {
        s3 = [];
        s4 = peg$currPos;
        s5 = peg$currPos;
        peg$silentFails++;
        s6 = peg$currPos;
        if (input.substr(peg$currPos, 3) === peg$c0) {
          s7 = peg$c0;
          peg$currPos += 3;
        } else {
          s7 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e0);
          }
        }
        if (s7 !== peg$FAILED) {
          s8 = peg$parseNL();
          if (s8 !== peg$FAILED) {
            s7 = [s7, s8];
            s6 = s7;
          } else {
            peg$currPos = s6;
            s6 = peg$FAILED;
          }
        } else {
          peg$currPos = s6;
          s6 = peg$FAILED;
        }
        peg$silentFails--;
        if (s6 === peg$FAILED) {
          s5 = void 0;
        } else {
          peg$currPos = s5;
          s5 = peg$FAILED;
        }
        if (s5 !== peg$FAILED) {
          s6 = peg$currPos;
          s7 = [];
          s8 = input.charAt(peg$currPos);
          if (peg$r0.test(s8)) {
            peg$currPos++;
          } else {
            s8 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e1);
            }
          }
          while (s8 !== peg$FAILED) {
            s7.push(s8);
            s8 = input.charAt(peg$currPos);
            if (peg$r0.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e1);
              }
            }
          }
          s6 = input.substring(s6, peg$currPos);
          s7 = peg$parseNL();
          if (s7 !== peg$FAILED) {
            peg$savedPos = s4;
            s4 = peg$f1(s6);
          } else {
            peg$currPos = s4;
            s4 = peg$FAILED;
          }
        } else {
          peg$currPos = s4;
          s4 = peg$FAILED;
        }
        while (s4 !== peg$FAILED) {
          s3.push(s4);
          s4 = peg$currPos;
          s5 = peg$currPos;
          peg$silentFails++;
          s6 = peg$currPos;
          if (input.substr(peg$currPos, 3) === peg$c0) {
            s7 = peg$c0;
            peg$currPos += 3;
          } else {
            s7 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e0);
            }
          }
          if (s7 !== peg$FAILED) {
            s8 = peg$parseNL();
            if (s8 !== peg$FAILED) {
              s7 = [s7, s8];
              s6 = s7;
            } else {
              peg$currPos = s6;
              s6 = peg$FAILED;
            }
          } else {
            peg$currPos = s6;
            s6 = peg$FAILED;
          }
          peg$silentFails--;
          if (s6 === peg$FAILED) {
            s5 = void 0;
          } else {
            peg$currPos = s5;
            s5 = peg$FAILED;
          }
          if (s5 !== peg$FAILED) {
            s6 = peg$currPos;
            s7 = [];
            s8 = input.charAt(peg$currPos);
            if (peg$r0.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e1);
              }
            }
            while (s8 !== peg$FAILED) {
              s7.push(s8);
              s8 = input.charAt(peg$currPos);
              if (peg$r0.test(s8)) {
                peg$currPos++;
              } else {
                s8 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e1);
                }
              }
            }
            s6 = input.substring(s6, peg$currPos);
            s7 = peg$parseNL();
            if (s7 !== peg$FAILED) {
              peg$savedPos = s4;
              s4 = peg$f1(s6);
            } else {
              peg$currPos = s4;
              s4 = peg$FAILED;
            }
          } else {
            peg$currPos = s4;
            s4 = peg$FAILED;
          }
        }
        if (input.substr(peg$currPos, 3) === peg$c0) {
          s4 = peg$c0;
          peg$currPos += 3;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e0);
          }
        }
        if (s4 !== peg$FAILED) {
          s5 = peg$parseNL();
          if (s5 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f2(s3);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseHeader() {
    var s0, s1, s2, s3;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 9) === peg$c1) {
      s1 = peg$c1;
      peg$currPos += 9;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e2);
      }
    }
    if (s1 === peg$FAILED) {
      if (input.substr(peg$currPos, 5) === peg$c2) {
        s1 = peg$c2;
        peg$currPos += 5;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e3);
        }
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$parse__();
      if (s2 !== peg$FAILED) {
        s3 = peg$parseDirection();
        if (s3 !== peg$FAILED) {
          peg$savedPos = s0;
          s0 = peg$f3(s3);
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseDirection() {
    var s0;
    if (input.substr(peg$currPos, 2) === peg$c3) {
      s0 = peg$c3;
      peg$currPos += 2;
    } else {
      s0 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e4);
      }
    }
    if (s0 === peg$FAILED) {
      if (input.substr(peg$currPos, 2) === peg$c4) {
        s0 = peg$c4;
        peg$currPos += 2;
      } else {
        s0 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e5);
        }
      }
      if (s0 === peg$FAILED) {
        if (input.substr(peg$currPos, 2) === peg$c5) {
          s0 = peg$c5;
          peg$currPos += 2;
        } else {
          s0 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e6);
          }
        }
        if (s0 === peg$FAILED) {
          if (input.substr(peg$currPos, 2) === peg$c6) {
            s0 = peg$c6;
            peg$currPos += 2;
          } else {
            s0 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e7);
            }
          }
          if (s0 === peg$FAILED) {
            if (input.substr(peg$currPos, 2) === peg$c7) {
              s0 = peg$c7;
              peg$currPos += 2;
            } else {
              s0 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e8);
              }
            }
          }
        }
      }
    }
    return s0;
  }
  function peg$parseStatements() {
    var s0, s1, s2;
    s0 = peg$currPos;
    s1 = [];
    s2 = peg$parseStatement();
    if (s2 === peg$FAILED) {
      s2 = peg$parseBlankLine();
    }
    while (s2 !== peg$FAILED) {
      s1.push(s2);
      s2 = peg$parseStatement();
      if (s2 === peg$FAILED) {
        s2 = peg$parseBlankLine();
      }
    }
    peg$savedPos = s0;
    s1 = peg$f4(s1);
    s0 = s1;
    return s0;
  }
  function peg$parseStatement() {
    var s0, s1, s2, s3, s4, s5, s6;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = peg$parseNoteDirective();
    if (s2 === peg$FAILED) {
      s2 = peg$parseLegendBlock();
      if (s2 === peg$FAILED) {
        s2 = peg$parseSubgraphBlock();
        if (s2 === peg$FAILED) {
          s2 = peg$parseEdgeChain();
          if (s2 === peg$FAILED) {
            s2 = peg$parseNodeDecl();
            if (s2 === peg$FAILED) {
              s2 = peg$parseStyleDirective();
              if (s2 === peg$FAILED) {
                s2 = peg$parseClassDirective();
                if (s2 === peg$FAILED) {
                  s2 = peg$parseClickDirective();
                }
              }
            }
          }
        }
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$parse_();
      s4 = peg$parseComment();
      if (s4 === peg$FAILED) {
        s4 = null;
      }
      s5 = peg$parseNL();
      if (s5 === peg$FAILED) {
        if (input.charCodeAt(peg$currPos) === 59) {
          s5 = peg$c8;
          peg$currPos++;
        } else {
          s5 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e9);
          }
        }
        if (s5 === peg$FAILED) {
          s5 = peg$currPos;
          peg$silentFails++;
          if (input.substr(peg$currPos, 3) === peg$c9) {
            s6 = peg$c9;
            peg$currPos += 3;
          } else {
            s6 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e10);
            }
          }
          peg$silentFails--;
          if (s6 !== peg$FAILED) {
            peg$currPos = s5;
            s5 = void 0;
          } else {
            s5 = peg$FAILED;
          }
          if (s5 === peg$FAILED) {
            s5 = peg$currPos;
            peg$silentFails++;
            if (input.length > peg$currPos) {
              s6 = input.charAt(peg$currPos);
              peg$currPos++;
            } else {
              s6 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e11);
              }
            }
            peg$silentFails--;
            if (s6 === peg$FAILED) {
              s5 = void 0;
            } else {
              peg$currPos = s5;
              s5 = peg$FAILED;
            }
          }
        }
      }
      if (s5 !== peg$FAILED) {
        peg$savedPos = s0;
        s0 = peg$f5(s2);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseBlankLine() {
    var s0, s1, s2, s3;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = peg$parseComment();
    if (s2 === peg$FAILED) {
      s2 = null;
    }
    s3 = peg$parseNL();
    if (s3 !== peg$FAILED) {
      peg$savedPos = s0;
      s0 = peg$f6();
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseComment() {
    var s0, s1, s2, s3;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 2) === peg$c10) {
      s1 = peg$c10;
      peg$currPos += 2;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e12);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = [];
      s3 = input.charAt(peg$currPos);
      if (peg$r0.test(s3)) {
        peg$currPos++;
      } else {
        s3 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e1);
        }
      }
      while (s3 !== peg$FAILED) {
        s2.push(s3);
        s3 = input.charAt(peg$currPos);
        if (peg$r0.test(s3)) {
          peg$currPos++;
        } else {
          s3 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e1);
          }
        }
      }
      s1 = [s1, s2];
      s0 = s1;
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseSubgraphBlock() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9, s10;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 8) === peg$c11) {
      s1 = peg$c11;
      peg$currPos += 8;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e13);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$parse__();
      if (s2 !== peg$FAILED) {
        s3 = peg$parseSubgraphId();
        if (s3 !== peg$FAILED) {
          s4 = peg$parse_();
          s5 = peg$parseSubgraphLabel();
          if (s5 === peg$FAILED) {
            s5 = null;
          }
          s6 = peg$parse_();
          s7 = peg$parseNL();
          if (s7 !== peg$FAILED) {
            s8 = peg$parseStatements();
            if (s8 !== peg$FAILED) {
              s9 = peg$parse_();
              if (input.substr(peg$currPos, 3) === peg$c9) {
                s10 = peg$c9;
                peg$currPos += 3;
              } else {
                s10 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e10);
                }
              }
              if (s10 !== peg$FAILED) {
                peg$savedPos = s0;
                s0 = peg$f7(s3, s5, s8);
              } else {
                peg$currPos = s0;
                s0 = peg$FAILED;
              }
            } else {
              peg$currPos = s0;
              s0 = peg$FAILED;
            }
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseSubgraphId() {
    var s0, s1, s2, s3, s4;
    s0 = peg$currPos;
    s1 = peg$currPos;
    s2 = input.charAt(peg$currPos);
    if (peg$r1.test(s2)) {
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e14);
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = [];
      s4 = input.charAt(peg$currPos);
      if (peg$r2.test(s4)) {
        peg$currPos++;
      } else {
        s4 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e15);
        }
      }
      while (s4 !== peg$FAILED) {
        s3.push(s4);
        s4 = input.charAt(peg$currPos);
        if (peg$r2.test(s4)) {
          peg$currPos++;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e15);
          }
        }
      }
      s2 = [s2, s3];
      s1 = s2;
    } else {
      peg$currPos = s1;
      s1 = peg$FAILED;
    }
    if (s1 !== peg$FAILED) {
      s0 = input.substring(s0, peg$currPos);
    } else {
      s0 = s1;
    }
    return s0;
  }
  function peg$parseSubgraphLabel() {
    var s0, s1, s2, s3, s4;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 91) {
      s1 = peg$c12;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e16);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$currPos;
      s3 = [];
      s4 = input.charAt(peg$currPos);
      if (peg$r3.test(s4)) {
        peg$currPos++;
      } else {
        s4 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e17);
        }
      }
      while (s4 !== peg$FAILED) {
        s3.push(s4);
        s4 = input.charAt(peg$currPos);
        if (peg$r3.test(s4)) {
          peg$currPos++;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e17);
          }
        }
      }
      s2 = input.substring(s2, peg$currPos);
      if (input.charCodeAt(peg$currPos) === 93) {
        s3 = peg$c13;
        peg$currPos++;
      } else {
        s3 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e18);
        }
      }
      if (s3 !== peg$FAILED) {
        peg$savedPos = s0;
        s0 = peg$f8(s2);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      s1 = peg$currPos;
      s2 = [];
      s3 = input.charAt(peg$currPos);
      if (peg$r4.test(s3)) {
        peg$currPos++;
      } else {
        s3 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e19);
        }
      }
      if (s3 !== peg$FAILED) {
        while (s3 !== peg$FAILED) {
          s2.push(s3);
          s3 = input.charAt(peg$currPos);
          if (peg$r4.test(s3)) {
            peg$currPos++;
          } else {
            s3 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e19);
            }
          }
        }
      } else {
        s2 = peg$FAILED;
      }
      if (s2 !== peg$FAILED) {
        s1 = input.substring(s1, peg$currPos);
      } else {
        s1 = s2;
      }
      if (s1 !== peg$FAILED) {
        peg$savedPos = s0;
        s1 = peg$f9(s1);
      }
      s0 = s1;
    }
    return s0;
  }
  function peg$parseEdgeChain() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9, s10, s11, s12;
    s0 = peg$currPos;
    s1 = peg$parseNodeRef();
    if (s1 !== peg$FAILED) {
      s2 = [];
      s3 = peg$currPos;
      s4 = peg$parse_();
      s5 = peg$parseEdgeOp();
      if (s5 !== peg$FAILED) {
        s6 = peg$parse_();
        s7 = peg$parseNodeRef();
        if (s7 !== peg$FAILED) {
          s8 = [];
          s9 = peg$currPos;
          s10 = peg$parse_();
          if (input.charCodeAt(peg$currPos) === 64) {
            s11 = peg$c14;
            peg$currPos++;
          } else {
            s11 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e20);
            }
          }
          if (s11 !== peg$FAILED) {
            s12 = peg$parseEdgeAnnotation();
            if (s12 !== peg$FAILED) {
              s10 = [s10, s11, s12];
              s9 = s10;
            } else {
              peg$currPos = s9;
              s9 = peg$FAILED;
            }
          } else {
            peg$currPos = s9;
            s9 = peg$FAILED;
          }
          while (s9 !== peg$FAILED) {
            s8.push(s9);
            s9 = peg$currPos;
            s10 = peg$parse_();
            if (input.charCodeAt(peg$currPos) === 64) {
              s11 = peg$c14;
              peg$currPos++;
            } else {
              s11 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e20);
              }
            }
            if (s11 !== peg$FAILED) {
              s12 = peg$parseEdgeAnnotation();
              if (s12 !== peg$FAILED) {
                s10 = [s10, s11, s12];
                s9 = s10;
              } else {
                peg$currPos = s9;
                s9 = peg$FAILED;
              }
            } else {
              peg$currPos = s9;
              s9 = peg$FAILED;
            }
          }
          s9 = peg$currPos;
          s10 = peg$parse_();
          s11 = peg$parseAnimationProperties();
          if (s11 !== peg$FAILED) {
            s10 = [s10, s11];
            s9 = s10;
          } else {
            peg$currPos = s9;
            s9 = peg$FAILED;
          }
          if (s9 === peg$FAILED) {
            s9 = null;
          }
          peg$savedPos = s3;
          s3 = peg$f10(s1, s5, s7, s8, s9);
        } else {
          peg$currPos = s3;
          s3 = peg$FAILED;
        }
      } else {
        peg$currPos = s3;
        s3 = peg$FAILED;
      }
      if (s3 !== peg$FAILED) {
        while (s3 !== peg$FAILED) {
          s2.push(s3);
          s3 = peg$currPos;
          s4 = peg$parse_();
          s5 = peg$parseEdgeOp();
          if (s5 !== peg$FAILED) {
            s6 = peg$parse_();
            s7 = peg$parseNodeRef();
            if (s7 !== peg$FAILED) {
              s8 = [];
              s9 = peg$currPos;
              s10 = peg$parse_();
              if (input.charCodeAt(peg$currPos) === 64) {
                s11 = peg$c14;
                peg$currPos++;
              } else {
                s11 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e20);
                }
              }
              if (s11 !== peg$FAILED) {
                s12 = peg$parseEdgeAnnotation();
                if (s12 !== peg$FAILED) {
                  s10 = [s10, s11, s12];
                  s9 = s10;
                } else {
                  peg$currPos = s9;
                  s9 = peg$FAILED;
                }
              } else {
                peg$currPos = s9;
                s9 = peg$FAILED;
              }
              while (s9 !== peg$FAILED) {
                s8.push(s9);
                s9 = peg$currPos;
                s10 = peg$parse_();
                if (input.charCodeAt(peg$currPos) === 64) {
                  s11 = peg$c14;
                  peg$currPos++;
                } else {
                  s11 = peg$FAILED;
                  if (peg$silentFails === 0) {
                    peg$fail(peg$e20);
                  }
                }
                if (s11 !== peg$FAILED) {
                  s12 = peg$parseEdgeAnnotation();
                  if (s12 !== peg$FAILED) {
                    s10 = [s10, s11, s12];
                    s9 = s10;
                  } else {
                    peg$currPos = s9;
                    s9 = peg$FAILED;
                  }
                } else {
                  peg$currPos = s9;
                  s9 = peg$FAILED;
                }
              }
              s9 = peg$currPos;
              s10 = peg$parse_();
              s11 = peg$parseAnimationProperties();
              if (s11 !== peg$FAILED) {
                s10 = [s10, s11];
                s9 = s10;
              } else {
                peg$currPos = s9;
                s9 = peg$FAILED;
              }
              if (s9 === peg$FAILED) {
                s9 = null;
              }
              peg$savedPos = s3;
              s3 = peg$f10(s1, s5, s7, s8, s9);
            } else {
              peg$currPos = s3;
              s3 = peg$FAILED;
            }
          } else {
            peg$currPos = s3;
            s3 = peg$FAILED;
          }
        }
      } else {
        s2 = peg$FAILED;
      }
      if (s2 !== peg$FAILED) {
        peg$savedPos = s0;
        s0 = peg$f11(s1, s2);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseEdgeOp() {
    var s0, s1, s2;
    s0 = peg$currPos;
    s1 = peg$parseEdgeArrow();
    if (s1 !== peg$FAILED) {
      s2 = peg$parseEdgeLabel();
      if (s2 === peg$FAILED) {
        s2 = null;
      }
      peg$savedPos = s0;
      s0 = peg$f12(s1, s2);
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseEdgeLabel() {
    var s0, s1, s2, s3, s4;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 124) {
      s1 = peg$c15;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e21);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$currPos;
      s3 = [];
      s4 = input.charAt(peg$currPos);
      if (peg$r5.test(s4)) {
        peg$currPos++;
      } else {
        s4 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e22);
        }
      }
      if (s4 !== peg$FAILED) {
        while (s4 !== peg$FAILED) {
          s3.push(s4);
          s4 = input.charAt(peg$currPos);
          if (peg$r5.test(s4)) {
            peg$currPos++;
          } else {
            s4 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e22);
            }
          }
        }
      } else {
        s3 = peg$FAILED;
      }
      if (s3 !== peg$FAILED) {
        s2 = input.substring(s2, peg$currPos);
      } else {
        s2 = s3;
      }
      if (s2 !== peg$FAILED) {
        if (input.charCodeAt(peg$currPos) === 124) {
          s3 = peg$c15;
          peg$currPos++;
        } else {
          s3 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e21);
          }
        }
        if (s3 !== peg$FAILED) {
          peg$savedPos = s0;
          s0 = peg$f13(s2);
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseEdgeArrow() {
    var s0, s1;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 5) === peg$c16) {
      s1 = peg$c16;
      peg$currPos += 5;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e23);
      }
    }
    if (s1 !== peg$FAILED) {
      peg$savedPos = s0;
      s1 = peg$f14();
    }
    s0 = s1;
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      if (input.substr(peg$currPos, 5) === peg$c17) {
        s1 = peg$c17;
        peg$currPos += 5;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e24);
        }
      }
      if (s1 !== peg$FAILED) {
        peg$savedPos = s0;
        s1 = peg$f15();
      }
      s0 = s1;
      if (s0 === peg$FAILED) {
        s0 = peg$currPos;
        if (input.substr(peg$currPos, 5) === peg$c18) {
          s1 = peg$c18;
          peg$currPos += 5;
        } else {
          s1 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e25);
          }
        }
        if (s1 !== peg$FAILED) {
          peg$savedPos = s0;
          s1 = peg$f16();
        }
        s0 = s1;
        if (s0 === peg$FAILED) {
          s0 = peg$currPos;
          if (input.substr(peg$currPos, 4) === peg$c19) {
            s1 = peg$c19;
            peg$currPos += 4;
          } else {
            s1 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e26);
            }
          }
          if (s1 !== peg$FAILED) {
            peg$savedPos = s0;
            s1 = peg$f17();
          }
          s0 = s1;
          if (s0 === peg$FAILED) {
            s0 = peg$currPos;
            if (input.substr(peg$currPos, 4) === peg$c20) {
              s1 = peg$c20;
              peg$currPos += 4;
            } else {
              s1 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e27);
              }
            }
            if (s1 !== peg$FAILED) {
              peg$savedPos = s0;
              s1 = peg$f18();
            }
            s0 = s1;
            if (s0 === peg$FAILED) {
              s0 = peg$currPos;
              if (input.substr(peg$currPos, 4) === peg$c21) {
                s1 = peg$c21;
                peg$currPos += 4;
              } else {
                s1 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e28);
                }
              }
              if (s1 !== peg$FAILED) {
                peg$savedPos = s0;
                s1 = peg$f19();
              }
              s0 = s1;
              if (s0 === peg$FAILED) {
                s0 = peg$currPos;
                if (input.substr(peg$currPos, 4) === peg$c22) {
                  s1 = peg$c22;
                  peg$currPos += 4;
                } else {
                  s1 = peg$FAILED;
                  if (peg$silentFails === 0) {
                    peg$fail(peg$e29);
                  }
                }
                if (s1 !== peg$FAILED) {
                  peg$savedPos = s0;
                  s1 = peg$f20();
                }
                s0 = s1;
                if (s0 === peg$FAILED) {
                  s0 = peg$currPos;
                  if (input.substr(peg$currPos, 5) === peg$c23) {
                    s1 = peg$c23;
                    peg$currPos += 5;
                  } else {
                    s1 = peg$FAILED;
                    if (peg$silentFails === 0) {
                      peg$fail(peg$e30);
                    }
                  }
                  if (s1 !== peg$FAILED) {
                    peg$savedPos = s0;
                    s1 = peg$f21();
                  }
                  s0 = s1;
                  if (s0 === peg$FAILED) {
                    s0 = peg$currPos;
                    if (input.substr(peg$currPos, 4) === peg$c24) {
                      s1 = peg$c24;
                      peg$currPos += 4;
                    } else {
                      s1 = peg$FAILED;
                      if (peg$silentFails === 0) {
                        peg$fail(peg$e31);
                      }
                    }
                    if (s1 !== peg$FAILED) {
                      peg$savedPos = s0;
                      s1 = peg$f22();
                    }
                    s0 = s1;
                    if (s0 === peg$FAILED) {
                      s0 = peg$currPos;
                      if (input.substr(peg$currPos, 4) === peg$c25) {
                        s1 = peg$c25;
                        peg$currPos += 4;
                      } else {
                        s1 = peg$FAILED;
                        if (peg$silentFails === 0) {
                          peg$fail(peg$e32);
                        }
                      }
                      if (s1 !== peg$FAILED) {
                        peg$savedPos = s0;
                        s1 = peg$f23();
                      }
                      s0 = s1;
                      if (s0 === peg$FAILED) {
                        s0 = peg$currPos;
                        if (input.substr(peg$currPos, 3) === peg$c26) {
                          s1 = peg$c26;
                          peg$currPos += 3;
                        } else {
                          s1 = peg$FAILED;
                          if (peg$silentFails === 0) {
                            peg$fail(peg$e33);
                          }
                        }
                        if (s1 !== peg$FAILED) {
                          peg$savedPos = s0;
                          s1 = peg$f24();
                        }
                        s0 = s1;
                        if (s0 === peg$FAILED) {
                          s0 = peg$currPos;
                          if (input.substr(peg$currPos, 4) === peg$c27) {
                            s1 = peg$c27;
                            peg$currPos += 4;
                          } else {
                            s1 = peg$FAILED;
                            if (peg$silentFails === 0) {
                              peg$fail(peg$e34);
                            }
                          }
                          if (s1 !== peg$FAILED) {
                            peg$savedPos = s0;
                            s1 = peg$f25();
                          }
                          s0 = s1;
                          if (s0 === peg$FAILED) {
                            s0 = peg$currPos;
                            if (input.substr(peg$currPos, 3) === peg$c28) {
                              s1 = peg$c28;
                              peg$currPos += 3;
                            } else {
                              s1 = peg$FAILED;
                              if (peg$silentFails === 0) {
                                peg$fail(peg$e35);
                              }
                            }
                            if (s1 !== peg$FAILED) {
                              peg$savedPos = s0;
                              s1 = peg$f26();
                            }
                            s0 = s1;
                            if (s0 === peg$FAILED) {
                              s0 = peg$currPos;
                              if (input.substr(peg$currPos, 3) === peg$c29) {
                                s1 = peg$c29;
                                peg$currPos += 3;
                              } else {
                                s1 = peg$FAILED;
                                if (peg$silentFails === 0) {
                                  peg$fail(peg$e36);
                                }
                              }
                              if (s1 !== peg$FAILED) {
                                peg$savedPos = s0;
                                s1 = peg$f27();
                              }
                              s0 = s1;
                              if (s0 === peg$FAILED) {
                                s0 = peg$currPos;
                                if (input.substr(peg$currPos, 3) === peg$c30) {
                                  s1 = peg$c30;
                                  peg$currPos += 3;
                                } else {
                                  s1 = peg$FAILED;
                                  if (peg$silentFails === 0) {
                                    peg$fail(peg$e37);
                                  }
                                }
                                if (s1 !== peg$FAILED) {
                                  peg$savedPos = s0;
                                  s1 = peg$f28();
                                }
                                s0 = s1;
                                if (s0 === peg$FAILED) {
                                  s0 = peg$currPos;
                                  if (input.substr(peg$currPos, 3) === peg$c31) {
                                    s1 = peg$c31;
                                    peg$currPos += 3;
                                  } else {
                                    s1 = peg$FAILED;
                                    if (peg$silentFails === 0) {
                                      peg$fail(peg$e38);
                                    }
                                  }
                                  if (s1 !== peg$FAILED) {
                                    peg$savedPos = s0;
                                    s1 = peg$f29();
                                  }
                                  s0 = s1;
                                  if (s0 === peg$FAILED) {
                                    s0 = peg$currPos;
                                    if (input.substr(peg$currPos, 3) === peg$c32) {
                                      s1 = peg$c32;
                                      peg$currPos += 3;
                                    } else {
                                      s1 = peg$FAILED;
                                      if (peg$silentFails === 0) {
                                        peg$fail(peg$e39);
                                      }
                                    }
                                    if (s1 !== peg$FAILED) {
                                      peg$savedPos = s0;
                                      s1 = peg$f30();
                                    }
                                    s0 = s1;
                                    if (s0 === peg$FAILED) {
                                      s0 = peg$currPos;
                                      if (input.substr(peg$currPos, 3) === peg$c33) {
                                        s1 = peg$c33;
                                        peg$currPos += 3;
                                      } else {
                                        s1 = peg$FAILED;
                                        if (peg$silentFails === 0) {
                                          peg$fail(peg$e40);
                                        }
                                      }
                                      if (s1 !== peg$FAILED) {
                                        peg$savedPos = s0;
                                        s1 = peg$f31();
                                      }
                                      s0 = s1;
                                      if (s0 === peg$FAILED) {
                                        s0 = peg$currPos;
                                        if (input.substr(peg$currPos, 3) === peg$c34) {
                                          s1 = peg$c34;
                                          peg$currPos += 3;
                                        } else {
                                          s1 = peg$FAILED;
                                          if (peg$silentFails === 0) {
                                            peg$fail(peg$e41);
                                          }
                                        }
                                        if (s1 !== peg$FAILED) {
                                          peg$savedPos = s0;
                                          s1 = peg$f32();
                                        }
                                        s0 = s1;
                                        if (s0 === peg$FAILED) {
                                          s0 = peg$currPos;
                                          if (input.substr(peg$currPos, 3) === peg$c0) {
                                            s1 = peg$c0;
                                            peg$currPos += 3;
                                          } else {
                                            s1 = peg$FAILED;
                                            if (peg$silentFails === 0) {
                                              peg$fail(peg$e0);
                                            }
                                          }
                                          if (s1 !== peg$FAILED) {
                                            peg$savedPos = s0;
                                            s1 = peg$f33();
                                          }
                                          s0 = s1;
                                        }
                                      }
                                    }
                                  }
                                }
                              }
                            }
                          }
                        }
                      }
                    }
                  }
                }
              }
            }
          }
        }
      }
    }
    return s0;
  }
  function peg$parseEdgeAnnotation() {
    var s0, s1, s2;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 5) === peg$c35) {
      s1 = peg$c35;
      peg$currPos += 5;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e42);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$parseAnimationValue();
      if (s2 !== peg$FAILED) {
        peg$savedPos = s0;
        s0 = peg$f34(s2);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    if (s0 === peg$FAILED) {
      s0 = peg$parseRouteAnnotation();
    }
    return s0;
  }
  function peg$parseAnimationValue() {
    var s0, s1, s2, s3;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 5) === peg$c36) {
      s1 = peg$c36;
      peg$currPos += 5;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e43);
      }
    }
    if (s1 === peg$FAILED) {
      if (input.substr(peg$currPos, 8) === peg$c37) {
        s1 = peg$c37;
        peg$currPos += 8;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e44);
        }
      }
      if (s1 === peg$FAILED) {
        if (input.substr(peg$currPos, 4) === peg$c38) {
          s1 = peg$c38;
          peg$currPos += 4;
        } else {
          s1 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e45);
          }
        }
        if (s1 === peg$FAILED) {
          if (input.substr(peg$currPos, 5) === peg$c39) {
            s1 = peg$c39;
            peg$currPos += 5;
          } else {
            s1 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e46);
            }
          }
          if (s1 === peg$FAILED) {
            if (input.substr(peg$currPos, 4) === peg$c40) {
              s1 = peg$c40;
              peg$currPos += 4;
            } else {
              s1 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e47);
              }
            }
            if (s1 === peg$FAILED) {
              if (input.substr(peg$currPos, 5) === peg$c41) {
                s1 = peg$c41;
                peg$currPos += 5;
              } else {
                s1 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e48);
                }
              }
              if (s1 === peg$FAILED) {
                if (input.substr(peg$currPos, 6) === peg$c42) {
                  s1 = peg$c42;
                  peg$currPos += 6;
                } else {
                  s1 = peg$FAILED;
                  if (peg$silentFails === 0) {
                    peg$fail(peg$e49);
                  }
                }
                if (s1 === peg$FAILED) {
                  if (input.substr(peg$currPos, 4) === peg$c43) {
                    s1 = peg$c43;
                    peg$currPos += 4;
                  } else {
                    s1 = peg$FAILED;
                    if (peg$silentFails === 0) {
                      peg$fail(peg$e50);
                    }
                  }
                  if (s1 === peg$FAILED) {
                    if (input.substr(peg$currPos, 10) === peg$c44) {
                      s1 = peg$c44;
                      peg$currPos += 10;
                    } else {
                      s1 = peg$FAILED;
                      if (peg$silentFails === 0) {
                        peg$fail(peg$e51);
                      }
                    }
                    if (s1 === peg$FAILED) {
                      if (input.substr(peg$currPos, 4) === peg$c45) {
                        s1 = peg$c45;
                        peg$currPos += 4;
                      } else {
                        s1 = peg$FAILED;
                        if (peg$silentFails === 0) {
                          peg$fail(peg$e52);
                        }
                      }
                    }
                  }
                }
              }
            }
          }
        }
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$currPos;
      peg$silentFails++;
      s3 = input.charAt(peg$currPos);
      if (peg$r6.test(s3)) {
        peg$currPos++;
      } else {
        s3 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e53);
        }
      }
      peg$silentFails--;
      if (s3 === peg$FAILED) {
        s2 = void 0;
      } else {
        peg$currPos = s2;
        s2 = peg$FAILED;
      }
      if (s2 !== peg$FAILED) {
        peg$savedPos = s0;
        s0 = peg$f35(s1);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseAnimationProperties() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 123) {
      s1 = peg$c46;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e54);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$parse_();
      if (input.substr(peg$currPos, 4) === peg$c47) {
        s3 = peg$c47;
        peg$currPos += 4;
      } else {
        s3 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e55);
        }
      }
      if (s3 !== peg$FAILED) {
        s4 = peg$parse_();
        if (input.charCodeAt(peg$currPos) === 58) {
          s5 = peg$c48;
          peg$currPos++;
        } else {
          s5 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e56);
          }
        }
        if (s5 !== peg$FAILED) {
          s6 = peg$parse_();
          s7 = peg$parseAnimationValue();
          if (s7 !== peg$FAILED) {
            s8 = peg$parse_();
            if (input.charCodeAt(peg$currPos) === 125) {
              s9 = peg$c49;
              peg$currPos++;
            } else {
              s9 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e57);
              }
            }
            if (s9 !== peg$FAILED) {
              peg$savedPos = s0;
              s0 = peg$f36(s7);
            } else {
              peg$currPos = s0;
              s0 = peg$FAILED;
            }
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseRouteAnnotation() {
    var s0, s1, s2, s3, s4;
    s0 = peg$currPos;
    s1 = peg$parseRouteWord();
    if (s1 !== peg$FAILED) {
      s2 = peg$currPos;
      if (input.charCodeAt(peg$currPos) === 58) {
        s3 = peg$c48;
        peg$currPos++;
      } else {
        s3 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e56);
        }
      }
      if (s3 !== peg$FAILED) {
        s4 = peg$parseWallPair();
        if (s4 !== peg$FAILED) {
          s3 = [s3, s4];
          s2 = s3;
        } else {
          peg$currPos = s2;
          s2 = peg$FAILED;
        }
      } else {
        peg$currPos = s2;
        s2 = peg$FAILED;
      }
      if (s2 === peg$FAILED) {
        s2 = null;
      }
      peg$savedPos = s0;
      s0 = peg$f37(s1, s2);
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      s1 = peg$parseWallPair();
      if (s1 !== peg$FAILED) {
        peg$savedPos = s0;
        s1 = peg$f38(s1);
      }
      s0 = s1;
    }
    return s0;
  }
  function peg$parseRouteWord() {
    var s0, s1;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 8) === peg$c50) {
      s1 = peg$c50;
      peg$currPos += 8;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e58);
      }
    }
    if (s1 !== peg$FAILED) {
      peg$savedPos = s0;
      s1 = peg$f39();
    }
    s0 = s1;
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      if (input.substr(peg$currPos, 10) === peg$c51) {
        s1 = peg$c51;
        peg$currPos += 10;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e59);
        }
      }
      if (s1 !== peg$FAILED) {
        peg$savedPos = s0;
        s1 = peg$f40();
      }
      s0 = s1;
      if (s0 === peg$FAILED) {
        s0 = peg$currPos;
        if (input.substr(peg$currPos, 6) === peg$c52) {
          s1 = peg$c52;
          peg$currPos += 6;
        } else {
          s1 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e60);
          }
        }
        if (s1 !== peg$FAILED) {
          peg$savedPos = s0;
          s1 = peg$f41();
        }
        s0 = s1;
        if (s0 === peg$FAILED) {
          s0 = peg$currPos;
          if (input.substr(peg$currPos, 8) === peg$c53) {
            s1 = peg$c53;
            peg$currPos += 8;
          } else {
            s1 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e61);
            }
          }
          if (s1 !== peg$FAILED) {
            peg$savedPos = s0;
            s1 = peg$f42();
          }
          s0 = s1;
        }
      }
    }
    return s0;
  }
  function peg$parseWallPair() {
    var s0, s1, s2;
    s0 = peg$currPos;
    s1 = peg$parseWall();
    if (s1 !== peg$FAILED) {
      s2 = peg$parseWall();
      if (s2 !== peg$FAILED) {
        peg$savedPos = s0;
        s0 = peg$f43(s1, s2);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      s1 = peg$parseWall();
      if (s1 !== peg$FAILED) {
        peg$savedPos = s0;
        s1 = peg$f44(s1);
      }
      s0 = s1;
    }
    return s0;
  }
  function peg$parseWall() {
    var s0, s1;
    s0 = peg$currPos;
    s1 = input.charAt(peg$currPos);
    if (peg$r7.test(s1)) {
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e62);
      }
    }
    if (s1 !== peg$FAILED) {
      peg$savedPos = s0;
      s1 = peg$f45(s1);
    }
    s0 = s1;
    return s0;
  }
  function peg$parseNodeRef() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8;
    s0 = peg$currPos;
    s1 = peg$parseNodeId();
    if (s1 !== peg$FAILED) {
      s2 = peg$parse_();
      s3 = peg$parseShape();
      if (s3 === peg$FAILED) {
        s3 = null;
      }
      s4 = peg$currPos;
      if (input.substr(peg$currPos, 3) === peg$c54) {
        s5 = peg$c54;
        peg$currPos += 3;
      } else {
        s5 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e63);
        }
      }
      if (s5 !== peg$FAILED) {
        s6 = peg$currPos;
        s7 = [];
        s8 = input.charAt(peg$currPos);
        if (peg$r6.test(s8)) {
          peg$currPos++;
        } else {
          s8 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e53);
          }
        }
        if (s8 !== peg$FAILED) {
          while (s8 !== peg$FAILED) {
            s7.push(s8);
            s8 = input.charAt(peg$currPos);
            if (peg$r6.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e53);
              }
            }
          }
        } else {
          s7 = peg$FAILED;
        }
        if (s7 !== peg$FAILED) {
          s6 = input.substring(s6, peg$currPos);
        } else {
          s6 = s7;
        }
        if (s6 !== peg$FAILED) {
          peg$savedPos = s4;
          s4 = peg$f46(s1, s3, s6);
        } else {
          peg$currPos = s4;
          s4 = peg$FAILED;
        }
      } else {
        peg$currPos = s4;
        s4 = peg$FAILED;
      }
      if (s4 === peg$FAILED) {
        s4 = null;
      }
      peg$savedPos = s0;
      s0 = peg$f47(s1, s3, s4);
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseNodeDecl() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9, s10;
    s0 = peg$currPos;
    s1 = peg$parseNodeRef();
    if (s1 !== peg$FAILED) {
      s2 = [];
      s3 = peg$currPos;
      s4 = peg$parse_();
      if (input.charCodeAt(peg$currPos) === 64) {
        s5 = peg$c14;
        peg$currPos++;
      } else {
        s5 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e20);
        }
      }
      if (s5 !== peg$FAILED) {
        s6 = peg$currPos;
        s7 = peg$currPos;
        s8 = input.charAt(peg$currPos);
        if (peg$r8.test(s8)) {
          peg$currPos++;
        } else {
          s8 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e64);
          }
        }
        if (s8 !== peg$FAILED) {
          s9 = [];
          s10 = input.charAt(peg$currPos);
          if (peg$r9.test(s10)) {
            peg$currPos++;
          } else {
            s10 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e65);
            }
          }
          while (s10 !== peg$FAILED) {
            s9.push(s10);
            s10 = input.charAt(peg$currPos);
            if (peg$r9.test(s10)) {
              peg$currPos++;
            } else {
              s10 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e65);
              }
            }
          }
          s8 = [s8, s9];
          s7 = s8;
        } else {
          peg$currPos = s7;
          s7 = peg$FAILED;
        }
        if (s7 !== peg$FAILED) {
          s6 = input.substring(s6, peg$currPos);
        } else {
          s6 = s7;
        }
        if (s6 !== peg$FAILED) {
          if (input.charCodeAt(peg$currPos) === 58) {
            s7 = peg$c48;
            peg$currPos++;
          } else {
            s7 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e56);
            }
          }
          if (s7 !== peg$FAILED) {
            s8 = peg$parseAnnotationValue();
            if (s8 !== peg$FAILED) {
              peg$savedPos = s3;
              s3 = peg$f48(s1, s6, s8);
            } else {
              peg$currPos = s3;
              s3 = peg$FAILED;
            }
          } else {
            peg$currPos = s3;
            s3 = peg$FAILED;
          }
        } else {
          peg$currPos = s3;
          s3 = peg$FAILED;
        }
      } else {
        peg$currPos = s3;
        s3 = peg$FAILED;
      }
      while (s3 !== peg$FAILED) {
        s2.push(s3);
        s3 = peg$currPos;
        s4 = peg$parse_();
        if (input.charCodeAt(peg$currPos) === 64) {
          s5 = peg$c14;
          peg$currPos++;
        } else {
          s5 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e20);
          }
        }
        if (s5 !== peg$FAILED) {
          s6 = peg$currPos;
          s7 = peg$currPos;
          s8 = input.charAt(peg$currPos);
          if (peg$r8.test(s8)) {
            peg$currPos++;
          } else {
            s8 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e64);
            }
          }
          if (s8 !== peg$FAILED) {
            s9 = [];
            s10 = input.charAt(peg$currPos);
            if (peg$r9.test(s10)) {
              peg$currPos++;
            } else {
              s10 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e65);
              }
            }
            while (s10 !== peg$FAILED) {
              s9.push(s10);
              s10 = input.charAt(peg$currPos);
              if (peg$r9.test(s10)) {
                peg$currPos++;
              } else {
                s10 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e65);
                }
              }
            }
            s8 = [s8, s9];
            s7 = s8;
          } else {
            peg$currPos = s7;
            s7 = peg$FAILED;
          }
          if (s7 !== peg$FAILED) {
            s6 = input.substring(s6, peg$currPos);
          } else {
            s6 = s7;
          }
          if (s6 !== peg$FAILED) {
            if (input.charCodeAt(peg$currPos) === 58) {
              s7 = peg$c48;
              peg$currPos++;
            } else {
              s7 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e56);
              }
            }
            if (s7 !== peg$FAILED) {
              s8 = peg$parseAnnotationValue();
              if (s8 !== peg$FAILED) {
                peg$savedPos = s3;
                s3 = peg$f48(s1, s6, s8);
              } else {
                peg$currPos = s3;
                s3 = peg$FAILED;
              }
            } else {
              peg$currPos = s3;
              s3 = peg$FAILED;
            }
          } else {
            peg$currPos = s3;
            s3 = peg$FAILED;
          }
        } else {
          peg$currPos = s3;
          s3 = peg$FAILED;
        }
      }
      s3 = peg$currPos;
      peg$silentFails++;
      s4 = peg$currPos;
      s5 = peg$parse_();
      s6 = peg$parseEdgeArrow();
      if (s6 !== peg$FAILED) {
        s5 = [s5, s6];
        s4 = s5;
      } else {
        peg$currPos = s4;
        s4 = peg$FAILED;
      }
      peg$silentFails--;
      if (s4 === peg$FAILED) {
        s3 = void 0;
      } else {
        peg$currPos = s3;
        s3 = peg$FAILED;
      }
      if (s3 !== peg$FAILED) {
        peg$savedPos = s0;
        s0 = peg$f49(s1, s2);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseAnnotationValue() {
    var s0, s1, s2, s3, s4;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 34) {
      s1 = peg$c55;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e66);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$currPos;
      s3 = [];
      s4 = input.charAt(peg$currPos);
      if (peg$r10.test(s4)) {
        peg$currPos++;
      } else {
        s4 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e67);
        }
      }
      while (s4 !== peg$FAILED) {
        s3.push(s4);
        s4 = input.charAt(peg$currPos);
        if (peg$r10.test(s4)) {
          peg$currPos++;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e67);
          }
        }
      }
      s2 = input.substring(s2, peg$currPos);
      if (input.charCodeAt(peg$currPos) === 34) {
        s3 = peg$c55;
        peg$currPos++;
      } else {
        s3 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e66);
        }
      }
      if (s3 !== peg$FAILED) {
        peg$savedPos = s0;
        s0 = peg$f50(s2);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      s1 = [];
      s2 = input.charAt(peg$currPos);
      if (peg$r11.test(s2)) {
        peg$currPos++;
      } else {
        s2 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e68);
        }
      }
      if (s2 !== peg$FAILED) {
        while (s2 !== peg$FAILED) {
          s1.push(s2);
          s2 = input.charAt(peg$currPos);
          if (peg$r11.test(s2)) {
            peg$currPos++;
          } else {
            s2 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e68);
            }
          }
        }
      } else {
        s1 = peg$FAILED;
      }
      if (s1 !== peg$FAILED) {
        s0 = input.substring(s0, peg$currPos);
      } else {
        s0 = s1;
      }
    }
    return s0;
  }
  function peg$parseNodeId() {
    var s0, s1, s2, s3, s4, s5, s6, s7;
    s0 = peg$currPos;
    s1 = peg$currPos;
    peg$silentFails++;
    s2 = peg$currPos;
    if (input.substr(peg$currPos, 3) === peg$c9) {
      s3 = peg$c9;
      peg$currPos += 3;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e10);
      }
    }
    if (s3 !== peg$FAILED) {
      s4 = peg$currPos;
      peg$silentFails++;
      s5 = input.charAt(peg$currPos);
      if (peg$r2.test(s5)) {
        peg$currPos++;
      } else {
        s5 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e15);
        }
      }
      peg$silentFails--;
      if (s5 === peg$FAILED) {
        s4 = void 0;
      } else {
        peg$currPos = s4;
        s4 = peg$FAILED;
      }
      if (s4 !== peg$FAILED) {
        s3 = [s3, s4];
        s2 = s3;
      } else {
        peg$currPos = s2;
        s2 = peg$FAILED;
      }
    } else {
      peg$currPos = s2;
      s2 = peg$FAILED;
    }
    peg$silentFails--;
    if (s2 === peg$FAILED) {
      s1 = void 0;
    } else {
      peg$currPos = s1;
      s1 = peg$FAILED;
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$currPos;
      peg$silentFails++;
      s3 = peg$currPos;
      if (input.substr(peg$currPos, 8) === peg$c11) {
        s4 = peg$c11;
        peg$currPos += 8;
      } else {
        s4 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e13);
        }
      }
      if (s4 !== peg$FAILED) {
        s5 = peg$currPos;
        peg$silentFails++;
        s6 = input.charAt(peg$currPos);
        if (peg$r2.test(s6)) {
          peg$currPos++;
        } else {
          s6 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e15);
          }
        }
        peg$silentFails--;
        if (s6 === peg$FAILED) {
          s5 = void 0;
        } else {
          peg$currPos = s5;
          s5 = peg$FAILED;
        }
        if (s5 !== peg$FAILED) {
          s4 = [s4, s5];
          s3 = s4;
        } else {
          peg$currPos = s3;
          s3 = peg$FAILED;
        }
      } else {
        peg$currPos = s3;
        s3 = peg$FAILED;
      }
      peg$silentFails--;
      if (s3 === peg$FAILED) {
        s2 = void 0;
      } else {
        peg$currPos = s2;
        s2 = peg$FAILED;
      }
      if (s2 !== peg$FAILED) {
        s3 = peg$currPos;
        s4 = peg$currPos;
        s5 = input.charAt(peg$currPos);
        if (peg$r1.test(s5)) {
          peg$currPos++;
        } else {
          s5 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e14);
          }
        }
        if (s5 !== peg$FAILED) {
          s6 = [];
          s7 = input.charAt(peg$currPos);
          if (peg$r2.test(s7)) {
            peg$currPos++;
          } else {
            s7 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e15);
            }
          }
          while (s7 !== peg$FAILED) {
            s6.push(s7);
            s7 = input.charAt(peg$currPos);
            if (peg$r2.test(s7)) {
              peg$currPos++;
            } else {
              s7 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e15);
              }
            }
          }
          s5 = [s5, s6];
          s4 = s5;
        } else {
          peg$currPos = s4;
          s4 = peg$FAILED;
        }
        if (s4 !== peg$FAILED) {
          s3 = input.substring(s3, peg$currPos);
        } else {
          s3 = s4;
        }
        if (s3 !== peg$FAILED) {
          peg$savedPos = s0;
          s0 = peg$f51(s3);
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseShape() {
    var s0, s1, s2, s3, s4;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 2) === peg$c56) {
      s1 = peg$c56;
      peg$currPos += 2;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e69);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$currPos;
      s3 = [];
      s4 = input.charAt(peg$currPos);
      if (peg$r12.test(s4)) {
        peg$currPos++;
      } else {
        s4 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e70);
        }
      }
      if (s4 !== peg$FAILED) {
        while (s4 !== peg$FAILED) {
          s3.push(s4);
          s4 = input.charAt(peg$currPos);
          if (peg$r12.test(s4)) {
            peg$currPos++;
          } else {
            s4 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e70);
            }
          }
        }
      } else {
        s3 = peg$FAILED;
      }
      if (s3 !== peg$FAILED) {
        s2 = input.substring(s2, peg$currPos);
      } else {
        s2 = s3;
      }
      if (s2 !== peg$FAILED) {
        if (input.substr(peg$currPos, 2) === peg$c57) {
          s3 = peg$c57;
          peg$currPos += 2;
        } else {
          s3 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e71);
          }
        }
        if (s3 !== peg$FAILED) {
          peg$savedPos = s0;
          s0 = peg$f52(s2);
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      if (input.substr(peg$currPos, 2) === peg$c58) {
        s1 = peg$c58;
        peg$currPos += 2;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e72);
        }
      }
      if (s1 !== peg$FAILED) {
        s2 = peg$currPos;
        s3 = [];
        s4 = input.charAt(peg$currPos);
        if (peg$r3.test(s4)) {
          peg$currPos++;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e17);
          }
        }
        while (s4 !== peg$FAILED) {
          s3.push(s4);
          s4 = input.charAt(peg$currPos);
          if (peg$r3.test(s4)) {
            peg$currPos++;
          } else {
            s4 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e17);
            }
          }
        }
        s2 = input.substring(s2, peg$currPos);
        if (input.substr(peg$currPos, 2) === peg$c59) {
          s3 = peg$c59;
          peg$currPos += 2;
        } else {
          s3 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e73);
          }
        }
        if (s3 !== peg$FAILED) {
          peg$savedPos = s0;
          s0 = peg$f53(s2);
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
      if (s0 === peg$FAILED) {
        s0 = peg$currPos;
        if (input.substr(peg$currPos, 2) === peg$c60) {
          s1 = peg$c60;
          peg$currPos += 2;
        } else {
          s1 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e74);
          }
        }
        if (s1 !== peg$FAILED) {
          s2 = peg$currPos;
          s3 = [];
          s4 = input.charAt(peg$currPos);
          if (peg$r3.test(s4)) {
            peg$currPos++;
          } else {
            s4 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e17);
            }
          }
          while (s4 !== peg$FAILED) {
            s3.push(s4);
            s4 = input.charAt(peg$currPos);
            if (peg$r3.test(s4)) {
              peg$currPos++;
            } else {
              s4 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e17);
              }
            }
          }
          s2 = input.substring(s2, peg$currPos);
          if (input.substr(peg$currPos, 2) === peg$c61) {
            s3 = peg$c61;
            peg$currPos += 2;
          } else {
            s3 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e75);
            }
          }
          if (s3 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f54(s2);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
        if (s0 === peg$FAILED) {
          s0 = peg$currPos;
          if (input.substr(peg$currPos, 2) === peg$c62) {
            s1 = peg$c62;
            peg$currPos += 2;
          } else {
            s1 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e76);
            }
          }
          if (s1 !== peg$FAILED) {
            s2 = peg$currPos;
            s3 = [];
            s4 = input.charAt(peg$currPos);
            if (peg$r12.test(s4)) {
              peg$currPos++;
            } else {
              s4 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e70);
              }
            }
            while (s4 !== peg$FAILED) {
              s3.push(s4);
              s4 = input.charAt(peg$currPos);
              if (peg$r12.test(s4)) {
                peg$currPos++;
              } else {
                s4 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e70);
                }
              }
            }
            s2 = input.substring(s2, peg$currPos);
            if (input.substr(peg$currPos, 2) === peg$c63) {
              s3 = peg$c63;
              peg$currPos += 2;
            } else {
              s3 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e77);
              }
            }
            if (s3 !== peg$FAILED) {
              peg$savedPos = s0;
              s0 = peg$f55(s2);
            } else {
              peg$currPos = s0;
              s0 = peg$FAILED;
            }
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
          if (s0 === peg$FAILED) {
            s0 = peg$currPos;
            if (input.substr(peg$currPos, 2) === peg$c64) {
              s1 = peg$c64;
              peg$currPos += 2;
            } else {
              s1 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e78);
              }
            }
            if (s1 !== peg$FAILED) {
              s2 = peg$currPos;
              s3 = [];
              s4 = input.charAt(peg$currPos);
              if (peg$r13.test(s4)) {
                peg$currPos++;
              } else {
                s4 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e79);
                }
              }
              while (s4 !== peg$FAILED) {
                s3.push(s4);
                s4 = input.charAt(peg$currPos);
                if (peg$r13.test(s4)) {
                  peg$currPos++;
                } else {
                  s4 = peg$FAILED;
                  if (peg$silentFails === 0) {
                    peg$fail(peg$e79);
                  }
                }
              }
              s2 = input.substring(s2, peg$currPos);
              if (input.substr(peg$currPos, 2) === peg$c65) {
                s3 = peg$c65;
                peg$currPos += 2;
              } else {
                s3 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e80);
                }
              }
              if (s3 !== peg$FAILED) {
                peg$savedPos = s0;
                s0 = peg$f56(s2);
              } else {
                peg$currPos = s0;
                s0 = peg$FAILED;
              }
            } else {
              peg$currPos = s0;
              s0 = peg$FAILED;
            }
            if (s0 === peg$FAILED) {
              s0 = peg$currPos;
              if (input.substr(peg$currPos, 2) === peg$c66) {
                s1 = peg$c66;
                peg$currPos += 2;
              } else {
                s1 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e81);
                }
              }
              if (s1 !== peg$FAILED) {
                s2 = peg$currPos;
                s3 = [];
                s4 = input.charAt(peg$currPos);
                if (peg$r14.test(s4)) {
                  peg$currPos++;
                } else {
                  s4 = peg$FAILED;
                  if (peg$silentFails === 0) {
                    peg$fail(peg$e82);
                  }
                }
                while (s4 !== peg$FAILED) {
                  s3.push(s4);
                  s4 = input.charAt(peg$currPos);
                  if (peg$r14.test(s4)) {
                    peg$currPos++;
                  } else {
                    s4 = peg$FAILED;
                    if (peg$silentFails === 0) {
                      peg$fail(peg$e82);
                    }
                  }
                }
                s2 = input.substring(s2, peg$currPos);
                if (input.substr(peg$currPos, 2) === peg$c67) {
                  s3 = peg$c67;
                  peg$currPos += 2;
                } else {
                  s3 = peg$FAILED;
                  if (peg$silentFails === 0) {
                    peg$fail(peg$e83);
                  }
                }
                if (s3 !== peg$FAILED) {
                  peg$savedPos = s0;
                  s0 = peg$f57(s2);
                } else {
                  peg$currPos = s0;
                  s0 = peg$FAILED;
                }
              } else {
                peg$currPos = s0;
                s0 = peg$FAILED;
              }
              if (s0 === peg$FAILED) {
                s0 = peg$currPos;
                if (input.substr(peg$currPos, 2) === peg$c68) {
                  s1 = peg$c68;
                  peg$currPos += 2;
                } else {
                  s1 = peg$FAILED;
                  if (peg$silentFails === 0) {
                    peg$fail(peg$e84);
                  }
                }
                if (s1 !== peg$FAILED) {
                  s2 = peg$currPos;
                  s3 = [];
                  s4 = input.charAt(peg$currPos);
                  if (peg$r15.test(s4)) {
                    peg$currPos++;
                  } else {
                    s4 = peg$FAILED;
                    if (peg$silentFails === 0) {
                      peg$fail(peg$e85);
                    }
                  }
                  while (s4 !== peg$FAILED) {
                    s3.push(s4);
                    s4 = input.charAt(peg$currPos);
                    if (peg$r15.test(s4)) {
                      peg$currPos++;
                    } else {
                      s4 = peg$FAILED;
                      if (peg$silentFails === 0) {
                        peg$fail(peg$e85);
                      }
                    }
                  }
                  s2 = input.substring(s2, peg$currPos);
                  if (input.substr(peg$currPos, 2) === peg$c69) {
                    s3 = peg$c69;
                    peg$currPos += 2;
                  } else {
                    s3 = peg$FAILED;
                    if (peg$silentFails === 0) {
                      peg$fail(peg$e86);
                    }
                  }
                  if (s3 !== peg$FAILED) {
                    peg$savedPos = s0;
                    s0 = peg$f58(s2);
                  } else {
                    peg$currPos = s0;
                    s0 = peg$FAILED;
                  }
                } else {
                  peg$currPos = s0;
                  s0 = peg$FAILED;
                }
                if (s0 === peg$FAILED) {
                  s0 = peg$currPos;
                  if (input.charCodeAt(peg$currPos) === 62) {
                    s1 = peg$c70;
                    peg$currPos++;
                  } else {
                    s1 = peg$FAILED;
                    if (peg$silentFails === 0) {
                      peg$fail(peg$e87);
                    }
                  }
                  if (s1 !== peg$FAILED) {
                    s2 = peg$currPos;
                    s3 = [];
                    s4 = input.charAt(peg$currPos);
                    if (peg$r3.test(s4)) {
                      peg$currPos++;
                    } else {
                      s4 = peg$FAILED;
                      if (peg$silentFails === 0) {
                        peg$fail(peg$e17);
                      }
                    }
                    while (s4 !== peg$FAILED) {
                      s3.push(s4);
                      s4 = input.charAt(peg$currPos);
                      if (peg$r3.test(s4)) {
                        peg$currPos++;
                      } else {
                        s4 = peg$FAILED;
                        if (peg$silentFails === 0) {
                          peg$fail(peg$e17);
                        }
                      }
                    }
                    s2 = input.substring(s2, peg$currPos);
                    if (input.charCodeAt(peg$currPos) === 93) {
                      s3 = peg$c13;
                      peg$currPos++;
                    } else {
                      s3 = peg$FAILED;
                      if (peg$silentFails === 0) {
                        peg$fail(peg$e18);
                      }
                    }
                    if (s3 !== peg$FAILED) {
                      peg$savedPos = s0;
                      s0 = peg$f59(s2);
                    } else {
                      peg$currPos = s0;
                      s0 = peg$FAILED;
                    }
                  } else {
                    peg$currPos = s0;
                    s0 = peg$FAILED;
                  }
                  if (s0 === peg$FAILED) {
                    s0 = peg$currPos;
                    if (input.charCodeAt(peg$currPos) === 91) {
                      s1 = peg$c12;
                      peg$currPos++;
                    } else {
                      s1 = peg$FAILED;
                      if (peg$silentFails === 0) {
                        peg$fail(peg$e16);
                      }
                    }
                    if (s1 !== peg$FAILED) {
                      s2 = peg$currPos;
                      s3 = [];
                      s4 = input.charAt(peg$currPos);
                      if (peg$r3.test(s4)) {
                        peg$currPos++;
                      } else {
                        s4 = peg$FAILED;
                        if (peg$silentFails === 0) {
                          peg$fail(peg$e17);
                        }
                      }
                      while (s4 !== peg$FAILED) {
                        s3.push(s4);
                        s4 = input.charAt(peg$currPos);
                        if (peg$r3.test(s4)) {
                          peg$currPos++;
                        } else {
                          s4 = peg$FAILED;
                          if (peg$silentFails === 0) {
                            peg$fail(peg$e17);
                          }
                        }
                      }
                      s2 = input.substring(s2, peg$currPos);
                      if (input.charCodeAt(peg$currPos) === 93) {
                        s3 = peg$c13;
                        peg$currPos++;
                      } else {
                        s3 = peg$FAILED;
                        if (peg$silentFails === 0) {
                          peg$fail(peg$e18);
                        }
                      }
                      if (s3 !== peg$FAILED) {
                        peg$savedPos = s0;
                        s0 = peg$f60(s2);
                      } else {
                        peg$currPos = s0;
                        s0 = peg$FAILED;
                      }
                    } else {
                      peg$currPos = s0;
                      s0 = peg$FAILED;
                    }
                    if (s0 === peg$FAILED) {
                      s0 = peg$currPos;
                      if (input.charCodeAt(peg$currPos) === 123) {
                        s1 = peg$c46;
                        peg$currPos++;
                      } else {
                        s1 = peg$FAILED;
                        if (peg$silentFails === 0) {
                          peg$fail(peg$e54);
                        }
                      }
                      if (s1 !== peg$FAILED) {
                        s2 = peg$currPos;
                        s3 = [];
                        s4 = input.charAt(peg$currPos);
                        if (peg$r13.test(s4)) {
                          peg$currPos++;
                        } else {
                          s4 = peg$FAILED;
                          if (peg$silentFails === 0) {
                            peg$fail(peg$e79);
                          }
                        }
                        while (s4 !== peg$FAILED) {
                          s3.push(s4);
                          s4 = input.charAt(peg$currPos);
                          if (peg$r13.test(s4)) {
                            peg$currPos++;
                          } else {
                            s4 = peg$FAILED;
                            if (peg$silentFails === 0) {
                              peg$fail(peg$e79);
                            }
                          }
                        }
                        s2 = input.substring(s2, peg$currPos);
                        if (input.charCodeAt(peg$currPos) === 125) {
                          s3 = peg$c49;
                          peg$currPos++;
                        } else {
                          s3 = peg$FAILED;
                          if (peg$silentFails === 0) {
                            peg$fail(peg$e57);
                          }
                        }
                        if (s3 !== peg$FAILED) {
                          peg$savedPos = s0;
                          s0 = peg$f61(s2);
                        } else {
                          peg$currPos = s0;
                          s0 = peg$FAILED;
                        }
                      } else {
                        peg$currPos = s0;
                        s0 = peg$FAILED;
                      }
                      if (s0 === peg$FAILED) {
                        s0 = peg$currPos;
                        if (input.charCodeAt(peg$currPos) === 40) {
                          s1 = peg$c71;
                          peg$currPos++;
                        } else {
                          s1 = peg$FAILED;
                          if (peg$silentFails === 0) {
                            peg$fail(peg$e88);
                          }
                        }
                        if (s1 !== peg$FAILED) {
                          s2 = peg$currPos;
                          s3 = [];
                          s4 = input.charAt(peg$currPos);
                          if (peg$r12.test(s4)) {
                            peg$currPos++;
                          } else {
                            s4 = peg$FAILED;
                            if (peg$silentFails === 0) {
                              peg$fail(peg$e70);
                            }
                          }
                          while (s4 !== peg$FAILED) {
                            s3.push(s4);
                            s4 = input.charAt(peg$currPos);
                            if (peg$r12.test(s4)) {
                              peg$currPos++;
                            } else {
                              s4 = peg$FAILED;
                              if (peg$silentFails === 0) {
                                peg$fail(peg$e70);
                              }
                            }
                          }
                          s2 = input.substring(s2, peg$currPos);
                          if (input.charCodeAt(peg$currPos) === 41) {
                            s3 = peg$c72;
                            peg$currPos++;
                          } else {
                            s3 = peg$FAILED;
                            if (peg$silentFails === 0) {
                              peg$fail(peg$e89);
                            }
                          }
                          if (s3 !== peg$FAILED) {
                            peg$savedPos = s0;
                            s0 = peg$f62(s2);
                          } else {
                            peg$currPos = s0;
                            s0 = peg$FAILED;
                          }
                        } else {
                          peg$currPos = s0;
                          s0 = peg$FAILED;
                        }
                      }
                    }
                  }
                }
              }
            }
          }
        }
      }
    }
    return s0;
  }
  function peg$parseStyleDirective() {
    var s0, s1, s2, s3, s4, s5;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 5) === peg$c73) {
      s1 = peg$c73;
      peg$currPos += 5;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e90);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$parse__();
      if (s2 !== peg$FAILED) {
        s3 = peg$currPos;
        s4 = [];
        s5 = input.charAt(peg$currPos);
        if (peg$r16.test(s5)) {
          peg$currPos++;
        } else {
          s5 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e91);
          }
        }
        if (s5 !== peg$FAILED) {
          while (s5 !== peg$FAILED) {
            s4.push(s5);
            s5 = input.charAt(peg$currPos);
            if (peg$r16.test(s5)) {
              peg$currPos++;
            } else {
              s5 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e91);
              }
            }
          }
        } else {
          s4 = peg$FAILED;
        }
        if (s4 !== peg$FAILED) {
          s3 = input.substring(s3, peg$currPos);
        } else {
          s3 = s4;
        }
        if (s3 !== peg$FAILED) {
          peg$savedPos = s0;
          s0 = peg$f63(s3);
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseClassDirective() {
    var s0, s1, s2, s3, s4, s5;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 8) === peg$c74) {
      s1 = peg$c74;
      peg$currPos += 8;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e92);
      }
    }
    if (s1 === peg$FAILED) {
      if (input.substr(peg$currPos, 5) === peg$c75) {
        s1 = peg$c75;
        peg$currPos += 5;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e93);
        }
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$parse__();
      if (s2 !== peg$FAILED) {
        s3 = peg$currPos;
        s4 = [];
        s5 = input.charAt(peg$currPos);
        if (peg$r16.test(s5)) {
          peg$currPos++;
        } else {
          s5 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e91);
          }
        }
        if (s5 !== peg$FAILED) {
          while (s5 !== peg$FAILED) {
            s4.push(s5);
            s5 = input.charAt(peg$currPos);
            if (peg$r16.test(s5)) {
              peg$currPos++;
            } else {
              s5 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e91);
              }
            }
          }
        } else {
          s4 = peg$FAILED;
        }
        if (s4 !== peg$FAILED) {
          s3 = input.substring(s3, peg$currPos);
        } else {
          s3 = s4;
        }
        if (s3 !== peg$FAILED) {
          peg$savedPos = s0;
          s0 = peg$f64(s3);
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseClickDirective() {
    var s0, s1, s2, s3, s4, s5;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 5) === peg$c76) {
      s1 = peg$c76;
      peg$currPos += 5;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e94);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$parse__();
      if (s2 !== peg$FAILED) {
        s3 = peg$currPos;
        s4 = [];
        s5 = input.charAt(peg$currPos);
        if (peg$r16.test(s5)) {
          peg$currPos++;
        } else {
          s5 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e91);
          }
        }
        if (s5 !== peg$FAILED) {
          while (s5 !== peg$FAILED) {
            s4.push(s5);
            s5 = input.charAt(peg$currPos);
            if (peg$r16.test(s5)) {
              peg$currPos++;
            } else {
              s5 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e91);
              }
            }
          }
        } else {
          s4 = peg$FAILED;
        }
        if (s4 !== peg$FAILED) {
          s3 = input.substring(s3, peg$currPos);
        } else {
          s3 = s4;
        }
        if (s3 !== peg$FAILED) {
          peg$savedPos = s0;
          s0 = peg$f65(s3);
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseNoteDirective() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9, s10, s11, s12, s13;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 4) === peg$c77) {
      s1 = peg$c77;
      peg$currPos += 4;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e95);
      }
    }
    if (s1 === peg$FAILED) {
      if (input.substr(peg$currPos, 7) === peg$c78) {
        s1 = peg$c78;
        peg$currPos += 7;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e96);
        }
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$parse__();
      if (s2 !== peg$FAILED) {
        if (input.charCodeAt(peg$currPos) === 34) {
          s3 = peg$c55;
          peg$currPos++;
        } else {
          s3 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e66);
          }
        }
        if (s3 !== peg$FAILED) {
          s4 = peg$currPos;
          s5 = [];
          s6 = input.charAt(peg$currPos);
          if (peg$r10.test(s6)) {
            peg$currPos++;
          } else {
            s6 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e67);
            }
          }
          while (s6 !== peg$FAILED) {
            s5.push(s6);
            s6 = input.charAt(peg$currPos);
            if (peg$r10.test(s6)) {
              peg$currPos++;
            } else {
              s6 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e67);
              }
            }
          }
          s4 = input.substring(s4, peg$currPos);
          if (input.charCodeAt(peg$currPos) === 34) {
            s5 = peg$c55;
            peg$currPos++;
          } else {
            s5 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e66);
            }
          }
          if (s5 !== peg$FAILED) {
            s6 = peg$parse__();
            if (s6 !== peg$FAILED) {
              if (input.substr(peg$currPos, 2) === peg$c79) {
                s7 = peg$c79;
                peg$currPos += 2;
              } else {
                s7 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e97);
                }
              }
              if (s7 !== peg$FAILED) {
                s8 = peg$parse__();
                if (s8 !== peg$FAILED) {
                  s9 = peg$currPos;
                  if (input.charCodeAt(peg$currPos) === 34) {
                    s10 = peg$c55;
                    peg$currPos++;
                  } else {
                    s10 = peg$FAILED;
                    if (peg$silentFails === 0) {
                      peg$fail(peg$e66);
                    }
                  }
                  if (s10 !== peg$FAILED) {
                    s11 = peg$currPos;
                    s12 = [];
                    s13 = input.charAt(peg$currPos);
                    if (peg$r10.test(s13)) {
                      peg$currPos++;
                    } else {
                      s13 = peg$FAILED;
                      if (peg$silentFails === 0) {
                        peg$fail(peg$e67);
                      }
                    }
                    while (s13 !== peg$FAILED) {
                      s12.push(s13);
                      s13 = input.charAt(peg$currPos);
                      if (peg$r10.test(s13)) {
                        peg$currPos++;
                      } else {
                        s13 = peg$FAILED;
                        if (peg$silentFails === 0) {
                          peg$fail(peg$e67);
                        }
                      }
                    }
                    s11 = input.substring(s11, peg$currPos);
                    if (input.charCodeAt(peg$currPos) === 34) {
                      s12 = peg$c55;
                      peg$currPos++;
                    } else {
                      s12 = peg$FAILED;
                      if (peg$silentFails === 0) {
                        peg$fail(peg$e66);
                      }
                    }
                    if (s12 !== peg$FAILED) {
                      peg$savedPos = s9;
                      s9 = peg$f66(s1, s4, s11);
                    } else {
                      peg$currPos = s9;
                      s9 = peg$FAILED;
                    }
                  } else {
                    peg$currPos = s9;
                    s9 = peg$FAILED;
                  }
                  if (s9 === peg$FAILED) {
                    s9 = peg$currPos;
                    s10 = peg$currPos;
                    s11 = input.charAt(peg$currPos);
                    if (peg$r1.test(s11)) {
                      peg$currPos++;
                    } else {
                      s11 = peg$FAILED;
                      if (peg$silentFails === 0) {
                        peg$fail(peg$e14);
                      }
                    }
                    if (s11 !== peg$FAILED) {
                      s12 = [];
                      s13 = input.charAt(peg$currPos);
                      if (peg$r2.test(s13)) {
                        peg$currPos++;
                      } else {
                        s13 = peg$FAILED;
                        if (peg$silentFails === 0) {
                          peg$fail(peg$e15);
                        }
                      }
                      while (s13 !== peg$FAILED) {
                        s12.push(s13);
                        s13 = input.charAt(peg$currPos);
                        if (peg$r2.test(s13)) {
                          peg$currPos++;
                        } else {
                          s13 = peg$FAILED;
                          if (peg$silentFails === 0) {
                            peg$fail(peg$e15);
                          }
                        }
                      }
                      s11 = [s11, s12];
                      s10 = s11;
                    } else {
                      peg$currPos = s10;
                      s10 = peg$FAILED;
                    }
                    if (s10 !== peg$FAILED) {
                      s9 = input.substring(s9, peg$currPos);
                    } else {
                      s9 = s10;
                    }
                  }
                  if (s9 !== peg$FAILED) {
                    s10 = peg$parseNoteOffset();
                    if (s10 === peg$FAILED) {
                      s10 = null;
                    }
                    peg$savedPos = s0;
                    s0 = peg$f67(s1, s4, s9, s10);
                  } else {
                    peg$currPos = s0;
                    s0 = peg$FAILED;
                  }
                } else {
                  peg$currPos = s0;
                  s0 = peg$FAILED;
                }
              } else {
                peg$currPos = s0;
                s0 = peg$FAILED;
              }
            } else {
              peg$currPos = s0;
              s0 = peg$FAILED;
            }
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseNoteOffset() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8;
    s0 = peg$currPos;
    s1 = peg$parse__();
    if (s1 !== peg$FAILED) {
      if (input.substr(peg$currPos, 6) === peg$c80) {
        s2 = peg$c80;
        peg$currPos += 6;
      } else {
        s2 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e98);
        }
      }
      if (s2 !== peg$FAILED) {
        s3 = peg$parse__();
        if (s3 !== peg$FAILED) {
          s4 = peg$parseSignedInt();
          if (s4 !== peg$FAILED) {
            s5 = peg$parse_();
            if (input.charCodeAt(peg$currPos) === 44) {
              s6 = peg$c81;
              peg$currPos++;
            } else {
              s6 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e99);
              }
            }
            if (s6 !== peg$FAILED) {
              s7 = peg$parse_();
              s8 = peg$parseSignedInt();
              if (s8 !== peg$FAILED) {
                peg$savedPos = s0;
                s0 = peg$f68(s4, s8);
              } else {
                peg$currPos = s0;
                s0 = peg$FAILED;
              }
            } else {
              peg$currPos = s0;
              s0 = peg$FAILED;
            }
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseSignedInt() {
    var s0, s1, s2, s3, s4;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 45) {
      s1 = peg$c82;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e100);
      }
    }
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    s2 = peg$currPos;
    s3 = [];
    s4 = input.charAt(peg$currPos);
    if (peg$r17.test(s4)) {
      peg$currPos++;
    } else {
      s4 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e101);
      }
    }
    if (s4 !== peg$FAILED) {
      while (s4 !== peg$FAILED) {
        s3.push(s4);
        s4 = input.charAt(peg$currPos);
        if (peg$r17.test(s4)) {
          peg$currPos++;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e101);
          }
        }
      }
    } else {
      s3 = peg$FAILED;
    }
    if (s3 !== peg$FAILED) {
      s2 = input.substring(s2, peg$currPos);
    } else {
      s2 = s3;
    }
    if (s2 !== peg$FAILED) {
      peg$savedPos = s0;
      s0 = peg$f69(s1, s2);
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseLegendBlock() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 6) === peg$c83) {
      s1 = peg$c83;
      peg$currPos += 6;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e102);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$parse__();
      if (s2 !== peg$FAILED) {
        s3 = peg$parseLegendCorner();
        if (s3 !== peg$FAILED) {
          s4 = peg$currPos;
          s5 = peg$parse_();
          if (input.charCodeAt(peg$currPos) === 34) {
            s6 = peg$c55;
            peg$currPos++;
          } else {
            s6 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e66);
            }
          }
          if (s6 !== peg$FAILED) {
            s7 = peg$currPos;
            s8 = [];
            s9 = input.charAt(peg$currPos);
            if (peg$r10.test(s9)) {
              peg$currPos++;
            } else {
              s9 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e67);
              }
            }
            while (s9 !== peg$FAILED) {
              s8.push(s9);
              s9 = input.charAt(peg$currPos);
              if (peg$r10.test(s9)) {
                peg$currPos++;
              } else {
                s9 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e67);
                }
              }
            }
            s7 = input.substring(s7, peg$currPos);
            if (input.charCodeAt(peg$currPos) === 34) {
              s8 = peg$c55;
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e66);
              }
            }
            if (s8 !== peg$FAILED) {
              peg$savedPos = s4;
              s4 = peg$f70(s3, s7);
            } else {
              peg$currPos = s4;
              s4 = peg$FAILED;
            }
          } else {
            peg$currPos = s4;
            s4 = peg$FAILED;
          }
          if (s4 === peg$FAILED) {
            s4 = null;
          }
          s5 = peg$parse_();
          s6 = peg$parseNL();
          if (s6 !== peg$FAILED) {
            s7 = [];
            s8 = peg$parseLegendEntryLine();
            while (s8 !== peg$FAILED) {
              s7.push(s8);
              s8 = peg$parseLegendEntryLine();
            }
            s8 = peg$parse_();
            if (input.substr(peg$currPos, 3) === peg$c9) {
              s9 = peg$c9;
              peg$currPos += 3;
            } else {
              s9 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e10);
              }
            }
            if (s9 !== peg$FAILED) {
              peg$savedPos = s0;
              s0 = peg$f71(s3, s4, s7);
            } else {
              peg$currPos = s0;
              s0 = peg$FAILED;
            }
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseLegendCorner() {
    var s0;
    if (input.substr(peg$currPos, 12) === peg$c84) {
      s0 = peg$c84;
      peg$currPos += 12;
    } else {
      s0 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e103);
      }
    }
    if (s0 === peg$FAILED) {
      if (input.substr(peg$currPos, 11) === peg$c85) {
        s0 = peg$c85;
        peg$currPos += 11;
      } else {
        s0 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e104);
        }
      }
      if (s0 === peg$FAILED) {
        if (input.substr(peg$currPos, 9) === peg$c86) {
          s0 = peg$c86;
          peg$currPos += 9;
        } else {
          s0 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e105);
          }
        }
        if (s0 === peg$FAILED) {
          if (input.substr(peg$currPos, 8) === peg$c87) {
            s0 = peg$c87;
            peg$currPos += 8;
          } else {
            s0 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e106);
            }
          }
        }
      }
    }
    return s0;
  }
  function peg$parseLegendEntryLine() {
    var s0, s1, s2, s3, s4, s5, s6, s7;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = peg$currPos;
    peg$silentFails++;
    s3 = peg$currPos;
    if (input.substr(peg$currPos, 3) === peg$c9) {
      s4 = peg$c9;
      peg$currPos += 3;
    } else {
      s4 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e10);
      }
    }
    if (s4 !== peg$FAILED) {
      s5 = peg$currPos;
      peg$silentFails++;
      s6 = input.charAt(peg$currPos);
      if (peg$r2.test(s6)) {
        peg$currPos++;
      } else {
        s6 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e15);
        }
      }
      peg$silentFails--;
      if (s6 === peg$FAILED) {
        s5 = void 0;
      } else {
        peg$currPos = s5;
        s5 = peg$FAILED;
      }
      if (s5 !== peg$FAILED) {
        s4 = [s4, s5];
        s3 = s4;
      } else {
        peg$currPos = s3;
        s3 = peg$FAILED;
      }
    } else {
      peg$currPos = s3;
      s3 = peg$FAILED;
    }
    peg$silentFails--;
    if (s3 === peg$FAILED) {
      s2 = void 0;
    } else {
      peg$currPos = s2;
      s2 = peg$FAILED;
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$currPos;
      s4 = [];
      s5 = input.charAt(peg$currPos);
      if (peg$r18.test(s5)) {
        peg$currPos++;
      } else {
        s5 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e107);
        }
      }
      if (s5 !== peg$FAILED) {
        while (s5 !== peg$FAILED) {
          s4.push(s5);
          s5 = input.charAt(peg$currPos);
          if (peg$r18.test(s5)) {
            peg$currPos++;
          } else {
            s5 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e107);
            }
          }
        }
      } else {
        s4 = peg$FAILED;
      }
      if (s4 !== peg$FAILED) {
        s3 = input.substring(s3, peg$currPos);
      } else {
        s3 = s4;
      }
      if (s3 !== peg$FAILED) {
        if (input.charCodeAt(peg$currPos) === 58) {
          s4 = peg$c48;
          peg$currPos++;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e56);
          }
        }
        if (s4 !== peg$FAILED) {
          s5 = peg$currPos;
          s6 = [];
          s7 = input.charAt(peg$currPos);
          if (peg$r0.test(s7)) {
            peg$currPos++;
          } else {
            s7 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e1);
            }
          }
          if (s7 !== peg$FAILED) {
            while (s7 !== peg$FAILED) {
              s6.push(s7);
              s7 = input.charAt(peg$currPos);
              if (peg$r0.test(s7)) {
                peg$currPos++;
              } else {
                s7 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e1);
                }
              }
            }
          } else {
            s6 = peg$FAILED;
          }
          if (s6 !== peg$FAILED) {
            s5 = input.substring(s5, peg$currPos);
          } else {
            s5 = s6;
          }
          if (s5 !== peg$FAILED) {
            s6 = peg$parseNL();
            if (s6 !== peg$FAILED) {
              peg$savedPos = s0;
              s0 = peg$f72(s3, s5);
            } else {
              peg$currPos = s0;
              s0 = peg$FAILED;
            }
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parse_() {
    var s0, s1;
    peg$silentFails++;
    s0 = [];
    s1 = input.charAt(peg$currPos);
    if (peg$r19.test(s1)) {
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e109);
      }
    }
    while (s1 !== peg$FAILED) {
      s0.push(s1);
      s1 = input.charAt(peg$currPos);
      if (peg$r19.test(s1)) {
        peg$currPos++;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e109);
        }
      }
    }
    peg$silentFails--;
    s1 = peg$FAILED;
    if (peg$silentFails === 0) {
      peg$fail(peg$e108);
    }
    return s0;
  }
  function peg$parse__() {
    var s0, s1;
    peg$silentFails++;
    s0 = [];
    s1 = input.charAt(peg$currPos);
    if (peg$r19.test(s1)) {
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e109);
      }
    }
    if (s1 !== peg$FAILED) {
      while (s1 !== peg$FAILED) {
        s0.push(s1);
        s1 = input.charAt(peg$currPos);
        if (peg$r19.test(s1)) {
          peg$currPos++;
        } else {
          s1 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e109);
          }
        }
      }
    } else {
      s0 = peg$FAILED;
    }
    peg$silentFails--;
    if (s0 === peg$FAILED) {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e110);
      }
    }
    return s0;
  }
  function peg$parseNL() {
    var s0, s1;
    peg$silentFails++;
    if (input.substr(peg$currPos, 2) === peg$c88) {
      s0 = peg$c88;
      peg$currPos += 2;
    } else {
      s0 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e112);
      }
    }
    if (s0 === peg$FAILED) {
      s0 = input.charAt(peg$currPos);
      if (peg$r20.test(s0)) {
        peg$currPos++;
      } else {
        s0 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e113);
        }
      }
    }
    peg$silentFails--;
    if (s0 === peg$FAILED) {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e111);
      }
    }
    return s0;
  }
  reset();
  peg$result = peg$startRuleFunction();
  if (options.peg$library) {
    return (
      /** @type {any} */
      {
        peg$result,
        peg$currPos,
        peg$FAILED,
        peg$maxFailExpected,
        peg$maxFailPos
      }
    );
  }
  if (peg$result !== peg$FAILED && peg$currPos === input.length) {
    return peg$result;
  } else {
    if (peg$result !== peg$FAILED && peg$currPos < input.length) {
      peg$fail(peg$endExpectation());
    }
    throw peg$buildStructuredError(
      peg$maxFailExpected,
      peg$maxFailPos < input.length ? input.charAt(peg$maxFailPos) : null,
      peg$maxFailPos < input.length ? peg$computeLocation(peg$maxFailPos, peg$maxFailPos + 1) : peg$computeLocation(peg$maxFailPos, peg$maxFailPos)
    );
  }
}
var flowchart = {
  parseMermaid(input) {
    const raw = peg$parse(input);
    const nodes = raw.flow.nodes.map((n) => {
      const base = {
        id: n.id,
        label: n.label,
        shape: n.shape || "rect",
        status: n.status || "default",
        ...n.subgraph !== void 0 ? { subgraph: n.subgraph } : {}
      };
      if (n.iconToken !== void 0) {
        const result = parseIconRef(String(n.iconToken));
        if (!result.ok) {
          throw new Error(
            `Flowchart parse error: invalid @icon value "${n.iconToken}": ${result.error.message}`
          );
        }
        return { ...base, icon: result.value };
      }
      return base;
    });
    return {
      version: raw.version,
      metadata: raw.metadata ?? {},
      direction: raw.direction,
      nodes,
      edges: raw.flow.edges,
      subgraphs: raw.subgraphs ?? [],
      overlays: raw.overlays?.length > 0 ? raw.overlays : void 0
    };
  },
  parseYaml(input) {
    return JSON.parse(input);
  },
  layout(ir, theme, options) {
    return layoutFlowchart(ir, theme, options);
  }
};
function isLeapYear(year) {
  return year % 4 === 0 && year % 100 !== 0 || year % 400 === 0;
}
function daysInMonth(year, month) {
  const table = [31, isLeapYear(year) ? 29 : 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31];
  return table[month - 1] ?? 30;
}
function daysInYear(year) {
  return isLeapYear(year) ? 366 : 365;
}
function dateToOrdinal(year, month, day) {
  let ord = 0;
  if (year >= 2e3) {
    for (let y = 2e3; y < year; y++) ord += daysInYear(y);
  } else {
    for (let y = year; y < 2e3; y++) ord -= daysInYear(y);
  }
  for (let m = 1; m < month; m++) ord += daysInMonth(year, m);
  ord += day - 1;
  return ord;
}
function ordinalToDate(ord) {
  let year = 2e3;
  let remaining = ord;
  if (remaining >= 0) {
    while (remaining >= daysInYear(year)) {
      remaining -= daysInYear(year);
      year += 1;
    }
  } else {
    while (remaining < 0) {
      year -= 1;
      remaining += daysInYear(year);
    }
  }
  let month = 1;
  while (month <= 12 && remaining >= daysInMonth(year, month)) {
    remaining -= daysInMonth(year, month);
    month += 1;
  }
  return [year, month, remaining + 1];
}
function parseIRDate(s) {
  const dayM = /^(\d{4})-(\d{2})-(\d{2})$/.exec(s);
  if (dayM) return { precision: "day", year: +dayM[1], month: +dayM[2], day: +dayM[3] };
  const monthM = /^(\d{4})-(\d{2})$/.exec(s);
  if (monthM) return { precision: "month", year: +monthM[1], month: +monthM[2] };
  const quarterM = /^(\d{4})-Q([1-4])$/.exec(s);
  if (quarterM) return { precision: "quarter", year: +quarterM[1], quarter: +quarterM[2] };
  const halfM = /^(\d{4})-H([12])$/.exec(s);
  if (halfM) return { precision: "half", year: +halfM[1], half: +halfM[2] };
  const yearM = /^(\d{4})$/.exec(s);
  if (yearM) return { precision: "year", year: +yearM[1] };
  throw new Error(`Unsupported IRDate format: "${s}"`);
}
function coerceLeft(parsed) {
  switch (parsed.precision) {
    case "day":
      return [parsed.year, parsed.month, parsed.day];
    case "month":
      return [parsed.year, parsed.month, 1];
    case "year":
      return [parsed.year, 1, 1];
    case "quarter":
      return [parsed.year, (parsed.quarter - 1) * 3 + 1, 1];
    case "half":
      return [parsed.year, parsed.half === 1 ? 1 : 7, 1];
  }
}
function coerceRight(parsed) {
  switch (parsed.precision) {
    case "day":
      return [parsed.year, parsed.month, parsed.day];
    case "month":
      return [parsed.year, parsed.month, daysInMonth(parsed.year, parsed.month)];
    case "year":
      return [parsed.year, 12, 31];
    case "quarter": {
      const lastMonth = parsed.quarter * 3;
      return [parsed.year, lastMonth, daysInMonth(parsed.year, lastMonth)];
    }
    case "half": {
      const lastMonth = parsed.half === 1 ? 6 : 12;
      return [parsed.year, lastMonth, daysInMonth(parsed.year, lastMonth)];
    }
  }
}
function inferAxisUnit(spanDays) {
  if (spanDays <= 14) return "day";
  if (spanDays <= 91) return "week";
  if (spanDays <= 548) return "month";
  if (spanDays <= 1461) return "quarter";
  if (spanDays <= 2922) return "half";
  return "year";
}
function advancePeriod(year, month, day, unit) {
  switch (unit) {
    case "day":
      return ordinalToDate(dateToOrdinal(year, month, day) + 1);
    case "week":
      return ordinalToDate(dateToOrdinal(year, month, day) + 7);
    case "month": {
      let m = month + 1, y = year;
      if (m > 12) {
        m = 1;
        y += 1;
      }
      return [y, m, 1];
    }
    case "quarter": {
      let m = month + 3, y = year;
      if (m > 12) {
        m = m - 12;
        y += 1;
      }
      return [y, m, 1];
    }
    case "half": {
      let m = month + 6, y = year;
      if (m > 12) {
        m = m - 12;
        y += 1;
      }
      return [y, m, 1];
    }
    case "year":
      return [year + 1, 1, 1];
  }
}
function snapToPeriodStart(year, month, _day, unit) {
  switch (unit) {
    case "day":
      return [year, month, _day];
    case "week": {
      const o = dateToOrdinal(year, month, _day);
      const jsEpoch = 9466848e5;
      const ms = jsEpoch + o * 864e5;
      const jsDay = new Date(ms).getUTCDay();
      const daysToMonday = jsDay === 0 ? 6 : jsDay - 1;
      return ordinalToDate(o - daysToMonday);
    }
    case "month":
      return [year, month, 1];
    case "quarter": {
      const q = Math.floor((month - 1) / 3);
      return [year, q * 3 + 1, 1];
    }
    case "half":
      return [year, month <= 6 ? 1 : 7, 1];
    case "year":
      return [year, 1, 1];
  }
}
function enumTicks(tsOrd, teOrd, unit) {
  const ticks = [];
  const [sy, sm, sd] = ordinalToDate(tsOrd);
  let [cy, cm, cd] = snapToPeriodStart(sy, sm, sd, unit);
  let curOrd = dateToOrdinal(cy, cm, cd);
  if (curOrd < tsOrd) {
    [cy, cm, cd] = advancePeriod(cy, cm, cd, unit);
    curOrd = dateToOrdinal(cy, cm, cd);
  }
  let safety = 0;
  while (curOrd <= teOrd && safety < 2e3) {
    ticks.push({ year: cy, month: cm, day: cd, ordinal: curOrd });
    [cy, cm, cd] = advancePeriod(cy, cm, cd, unit);
    curOrd = dateToOrdinal(cy, cm, cd);
    safety += 1;
  }
  return ticks;
}
var MONTH_ABBR = [
  "Jan",
  "Feb",
  "Mar",
  "Apr",
  "May",
  "Jun",
  "Jul",
  "Aug",
  "Sep",
  "Oct",
  "Nov",
  "Dec"
];
var MONTH_FULL = [
  "January",
  "February",
  "March",
  "April",
  "May",
  "June",
  "July",
  "August",
  "September",
  "October",
  "November",
  "December"
];
function ordinalSuffix(n) {
  const abs = Math.abs(n);
  const mod100 = abs % 100;
  if (mod100 >= 11 && mod100 <= 13) return `${n}th`;
  switch (abs % 10) {
    case 1:
      return `${n}st`;
    case 2:
      return `${n}nd`;
    case 3:
      return `${n}rd`;
    default:
      return `${n}th`;
  }
}
function formatTickLabel(tick, unit, tickIndex) {
  switch (unit) {
    case "day":
      if (tickIndex % 7 === 0) return MONTH_ABBR[tick.month - 1] ?? "";
      return String(tick.day);
    case "week":
      if (tickIndex % 4 === 0) return MONTH_ABBR[tick.month - 1] ?? "";
      return String(tick.day);
    case "month":
      if (tick.month === 1 || tickIndex === 0) return String(tick.year);
      return MONTH_ABBR[tick.month - 1] ?? "";
    case "quarter": {
      const q = Math.ceil(tick.month / 3);
      if (q === 1 || tickIndex === 0) return `Q${q} ${tick.year}`;
      return `Q${q}`;
    }
    case "half": {
      const h2 = tick.month <= 6 ? 1 : 2;
      if (h2 === 1 || tickIndex === 0) return `H${h2} ${tick.year}`;
      return `H${h2}`;
    }
    case "year":
      return String(tick.year);
  }
}
function formatMilestoneDate(year, month, day) {
  return `${ordinalSuffix(day)} ${MONTH_FULL[month - 1] ?? ""} ${year}`;
}
function startOrdinal(date) {
  try {
    const [y, m, d] = coerceLeft(parseIRDate(date));
    return dateToOrdinal(y, m, d);
  } catch {
    return 0;
  }
}
function endOrdinal(date) {
  try {
    const [y, m, d] = coerceRight(parseIRDate(date));
    return dateToOrdinal(y, m, d);
  } catch {
    return 0;
  }
}
function formatDate(date, style = "axis") {
  try {
    const p = parseIRDate(date);
    switch (p.precision) {
      case "day":
        return style === "full" ? formatMilestoneDate(p.year, p.month ?? 1, p.day ?? 1) : `${MONTH_ABBR[(p.month ?? 1) - 1] ?? ""} ${p.day ?? 1}`;
      case "month":
        return `${MONTH_ABBR[(p.month ?? 1) - 1] ?? ""} ${p.year}`;
      case "quarter":
        return `Q${p.quarter ?? ""} ${p.year}`;
      case "half":
        return `H${p.half ?? ""} ${p.year}`;
      case "year":
        return String(p.year);
    }
  } catch {
  }
  return date;
}
var CATEGORICAL_HUES = [
  "#7C3AED",
  "#0EA5A8",
  "#D97706",
  "#5B4FCF",
  "#DB2777",
  "#2563EB",
  "#16A34A",
  "#CA8A04"
];
var MONOCHROME_LIGHT_HUES = [
  "#525252",
  "#737373",
  "#404040",
  "#8A8A8A",
  "#262626",
  "#5E5E5E"
];
var MONOCHROME_DARK_HUES = [
  "#D4D4D4",
  "#A3A3A3",
  "#E5E5E5",
  "#8B8B8B",
  "#FAFAFA",
  "#B8B8B8"
];
function categoricalHue(index, theme) {
  if (theme) {
    if (theme.name === "bw-dark") {
      const n2 = MONOCHROME_DARK_HUES.length;
      return MONOCHROME_DARK_HUES[(index % n2 + n2) % n2];
    }
    if (theme.name === "bw-light" || theme.name === "minimal") {
      const n2 = MONOCHROME_LIGHT_HUES.length;
      return MONOCHROME_LIGHT_HUES[(index % n2 + n2) % n2];
    }
  }
  const n = CATEGORICAL_HUES.length;
  return CATEGORICAL_HUES[(index % n + n) % n];
}
function statusColor(palette, status) {
  switch (status) {
    case "done":
      return palette.success;
    case "active":
      return palette.primary;
    case "blocked":
      return palette.error;
    default:
      return palette.secondary;
  }
}
function collectEntries(ir) {
  const entries = [];
  let counter = 0;
  for (const m of ir.milestones) {
    entries.push({
      id: m.id,
      label: m.label,
      kind: "milestone",
      date: m.date,
      ord: startOrdinal(m.date),
      ...m.icon ? { icon: m.icon } : {},
      ...m.description ? { description: m.description } : {},
      _idx: counter++
    });
  }
  for (const a of ir.activities) {
    entries.push({
      id: a.id,
      label: a.label,
      kind: "activity",
      date: a.start,
      ord: startOrdinal(a.start),
      ...a.end ? { end: a.end } : {},
      ...a.status ? { status: a.status } : {},
      ...a.description ? { description: a.description } : {},
      _idx: counter++
    });
  }
  entries.sort((p, q) => p.ord - q.ord || p._idx - q._idx);
  return entries;
}
var formatEntryDate = (date) => formatDate(date, "full");
function layoutVerticalSpine(ir, theme) {
  const { palette, typography, spacing } = theme;
  const margin = spacing.diagramMargin;
  const entries = collectEntries(ir).map((e) => ({
    id: e.id,
    label: e.label,
    dateStr: formatEntryDate(e.date),
    ord: e.ord,
    ...e.description ? { description: e.description } : {},
    type: e.kind,
    ...e.status ? { statusHint: e.status } : {},
    dotColor: palette.primary
  }));
  entries.forEach((e, i) => {
    e.dotColor = e.statusHint === "active" ? palette.primary : e.statusHint === "blocked" ? palette.error : categoricalHue(i, theme);
  });
  const cardW = 260;
  const cardPad = 12;
  const spineGap = 48;
  const rowGap = 22;
  const dotR = 7;
  const innerW = cardW - 2 * cardPad;
  const titleSize = typography.baseFontSize;
  const dateSize = typography.smallFontSize;
  const descSize = typography.smallFontSize;
  const titleLH = rhuInt(titleSize * 1.3);
  const lineLH = rhuInt(descSize * 1.35);
  const spineX = rhuInt(margin + cardW + spineGap);
  const leftCardR = spineX - spineGap;
  const rightCardL = spineX + spineGap;
  const totalW = rhuInt(rightCardL + cardW + margin);
  const subtitle = typeof ir.metadata.subtitle === "string" ? ir.metadata.subtitle : void 0;
  const headerH = ir.metadata.title ? typography.titleFontSize + (subtitle ? typography.baseFontSize + 10 : 0) + 24 : 0;
  let y = margin + headerH + 8;
  const startY = y;
  const placed = entries.map((e) => {
    const labelInfo = measureFormattedText(e.label, titleSize, dateSize, innerW, 4);
    const descInfo = e.description ? measureFormattedText(e.description, descSize, descSize, innerW, 4) : void 0;
    const labelH = labelInfo.lineCount * titleLH;
    const descH = descInfo ? 4 + descInfo.lineCount * lineLH : 0;
    const cardH = rhuInt(cardPad + dateSize * 1.3 + 4 + labelH + descH + cardPad);
    return { ...e, labelInfo, descInfo, cardH };
  });
  const elements = [];
  const p = pen(theme);
  if (ir.metadata.title) {
    elements.push(
      p.text(
        ir.metadata.title,
        rhuInt(totalW / 2),
        margin + typography.titleFontSize,
        typography.titleFontSize,
        palette.text,
        { weight: "bold", anchor: "middle" }
      )
    );
    if (subtitle) {
      elements.push(
        p.text(
          subtitle,
          rhuInt(totalW / 2),
          margin + typography.titleFontSize + typography.baseFontSize + 8,
          typography.baseFontSize,
          palette.textMuted,
          { anchor: "middle" }
        )
      );
    }
  }
  let lastNodeY = startY;
  placed.forEach((e, i) => {
    const onRight = i % 2 === 0;
    const nodeY = rhuInt(y + e.cardH / 2);
    const cardX = onRight ? rightCardL : leftCardR - cardW;
    const cardY = rhuInt(y);
    const connX2 = onRight ? rightCardL : leftCardR;
    elements.push(p.path(`M ${spineX} ${nodeY} L ${connX2} ${nodeY}`, palette.border, 1.5));
    elements.push(
      p.rect(
        { x: cardX, y: cardY, width: cardW, height: e.cardH },
        palette.surface,
        palette.border,
        1,
        { rx: 8 }
      )
    );
    const stripeX = onRight ? cardX : cardX + cardW - 3;
    elements.push(
      p.rect({ x: stripeX, y: cardY, width: 3, height: e.cardH }, e.dotColor, e.dotColor, 0)
    );
    const textX = cardX + cardPad;
    let ty = cardY + cardPad + dateSize;
    elements.push(
      p.text(e.dateStr, textX, ty, dateSize, e.dotColor, { weight: "bold", anchor: "start" })
    );
    ty += 4;
    const labelH = e.labelInfo.lineCount * titleLH;
    elements.push(
      ...renderFormattedText(
        p,
        e.labelInfo,
        textX,
        ty - 4,
        innerW,
        labelH + 4,
        titleSize,
        dateSize,
        palette.text,
        palette.textMuted,
        { align: "start", defaultBold: true }
      )
    );
    ty += labelH;
    if (e.descInfo) {
      ty += 2;
      const descH = e.descInfo.lineCount * lineLH;
      elements.push(
        ...renderFormattedText(
          p,
          e.descInfo,
          textX,
          ty - 4,
          innerW,
          descH + 4,
          descSize,
          descSize,
          palette.textMuted,
          palette.textMuted,
          { align: "start" }
        )
      );
    }
    if (e.type === "milestone") {
      const r = dotR + 1;
      elements.push(
        p.path(
          `M ${spineX} ${nodeY - r} L ${spineX + r} ${nodeY} L ${spineX} ${nodeY + r} L ${spineX - r} ${nodeY} Z`,
          palette.background,
          2,
          { fill: e.dotColor }
        )
      );
    } else {
      elements.push(p.circle({ x: spineX, y: nodeY }, dotR, e.dotColor, palette.background, 2));
    }
    lastNodeY = nodeY;
    y += e.cardH + rowGap;
  });
  elements.unshift(p.path(`M ${spineX} ${startY} L ${spineX} ${lastNodeY}`, palette.border, 2.5));
  const totalH = rhuInt(y - rowGap + margin);
  const scene = applyOverlays(
    {
      viewBox: { x: 0, y: 0, width: totalW, height: totalH },
      background: palette.background,
      elements
    },
    ir.overlays,
    theme
  );
  return { scene, anchors: {} };
}
function pathPointAtS(s, geo) {
  const { xl, xr, pathStartY, nRows, rowSpacing, turnRadius: r, rowWidth } = geo;
  const L_turn = Math.PI * r;
  let remaining = Math.max(0, Math.min(s, geo.totalLength));
  for (let row = 0; row < nRows; row++) {
    const y_row = pathStartY + row * rowSpacing;
    if (remaining <= rowWidth + 1e-9) {
      const t = Math.min(remaining, rowWidth);
      return row % 2 === 0 ? { x: rhu(xl + t), y: rhu(y_row) } : { x: rhu(xr - t), y: rhu(y_row) };
    }
    remaining -= rowWidth;
    if (row < nRows - 1) {
      if (remaining <= L_turn + 1e-9) {
        const alpha = Math.min(remaining, L_turn) / r;
        return row % 2 === 0 ? { x: rhu(xr + r * Math.sin(alpha)), y: rhu(y_row + r * (1 - Math.cos(alpha))) } : { x: rhu(xl - r * Math.sin(alpha)), y: rhu(y_row + r * (1 - Math.cos(alpha))) };
      }
      remaining -= L_turn;
    }
  }
  const lastRow = nRows - 1;
  const y_last = rhu(pathStartY + lastRow * rowSpacing);
  return lastRow % 2 === 0 ? { x: rhu(xr), y: y_last } : { x: rhu(xl), y: y_last };
}
function buildPathD(geo) {
  const { xl, xr, pathStartY, nRows, rowSpacing, turnRadius: r } = geo;
  const parts = [];
  for (let row = 0; row < nRows; row++) {
    const y = rhu(pathStartY + row * rowSpacing);
    const y_next = rhu(pathStartY + (row + 1) * rowSpacing);
    if (row === 0) parts.push(`M ${xl} ${y}`);
    if (row % 2 === 0) {
      parts.push(`L ${xr} ${y}`);
      if (row < nRows - 1) parts.push(`A ${r} ${r} 0 1 1 ${xr} ${y_next}`);
    } else {
      parts.push(`L ${xl} ${y}`);
      if (row < nRows - 1) parts.push(`A ${r} ${r} 0 1 0 ${xl} ${y_next}`);
    }
  }
  return parts.join(" ");
}
function layoutSerpentine(ir, theme) {
  const { palette, typography } = theme;
  const p = pen(theme);
  const entries = collectEntries(ir).map((e) => ({
    id: e.id,
    label: e.label,
    ord: e.ord,
    color: e.kind === "milestone" ? palette.primary : statusColor(palette, e.status)
  }));
  const n = entries.length;
  const W = 1040;
  const turnRadius = 80;
  const rowSpacing = 160;
  const trackWidth = 14;
  const nodeR = 10;
  const badgeR = 22;
  const PATH_XL = rhu(turnRadius + 10);
  const PATH_XR = rhu(W - turnRadius - 10);
  const ROW_W = rhu(PATH_XR - PATH_XL);
  const nRows = Math.max(2, Math.ceil(n / 3));
  const headerH = ir.metadata.title ? typography.titleFontSize + 28 : 0;
  const TOP_PAD = 64;
  const BOTTOM_PAD = 64;
  const pathStartY = rhu(headerH + TOP_PAD);
  const L_turn = rhu(Math.PI * turnRadius);
  const totalLength = rhu(nRows * ROW_W + (nRows - 1) * L_turn);
  const pathEndY = rhu(pathStartY + (nRows - 1) * rowSpacing);
  const canvasH = rhuInt(pathEndY + BOTTOM_PAD);
  const geo = {
    xl: PATH_XL,
    xr: PATH_XR,
    pathStartY,
    nRows,
    rowSpacing,
    turnRadius,
    rowWidth: ROW_W,
    totalLength
  };
  const elements = [];
  if (ir.metadata.title) {
    elements.push(
      p.text(
        ir.metadata.title,
        rhu(W / 2),
        rhuInt(typography.titleFontSize + 8),
        typography.titleFontSize,
        palette.text,
        { weight: "bold", anchor: "middle" }
      )
    );
  }
  const pathD = buildPathD(geo);
  const pathStart = pathPointAtS(0, geo);
  const pathEnd = pathPointAtS(totalLength, geo);
  elements.push(p.path(pathD, palette.primary, trackWidth, { fill: "none" }));
  function rowForS(s) {
    let remaining = Math.max(0, s);
    for (let row = 0; row < nRows; row++) {
      if (remaining <= ROW_W + 1e-9) return row;
      remaining -= ROW_W;
      if (row < nRows - 1) {
        if (remaining <= L_turn + 1e-9) return row;
        remaining -= L_turn;
      }
    }
    return nRows - 1;
  }
  const labelFontSize = typography.smallFontSize;
  for (let i = 0; i < n; i++) {
    const s = rhu((i + 0.5) / n * totalLength);
    const pt = pathPointAtS(s, geo);
    const e = entries[i];
    elements.push(p.circle({ x: pt.x, y: pt.y }, nodeR, palette.background, e.color, 3));
    const isEvenRow = rowForS(s) % 2 === 0;
    const labelY = isEvenRow ? rhu(pt.y + nodeR + 6 + labelFontSize * 0.9) : rhu(pt.y - nodeR - 8);
    const labelText = e.label.length > 18 ? `${e.label.slice(0, 16)}\u2026` : e.label;
    elements.push(
      p.text(labelText, pt.x, labelY, labelFontSize, palette.text, { anchor: "middle" })
    );
  }
  elements.push(
    p.circle({ x: pathStart.x, y: pathStart.y }, badgeR, palette.primary, palette.primary, 2)
  );
  {
    const cx = pathStart.x, cy = pathStart.y, t = badgeR * 0.5;
    elements.push(
      p.path(
        `M ${rhu(cx - t * 0.5)} ${rhu(cy - t)} L ${rhu(cx + t)} ${rhu(cy)} L ${rhu(cx - t * 0.5)} ${rhu(cy + t)} Z`,
        palette.background,
        0,
        { fill: palette.background }
      )
    );
  }
  elements.push(
    p.circle({ x: pathEnd.x, y: pathEnd.y }, badgeR, palette.primary, palette.primary, 2)
  );
  elements.push(
    p.circle({ x: pathEnd.x, y: pathEnd.y }, rhu(badgeR * 0.62), "none", palette.background, 2.5)
  );
  elements.push(
    p.circle(
      { x: pathEnd.x, y: pathEnd.y },
      rhu(badgeR * 0.28),
      palette.background,
      palette.background,
      0
    )
  );
  const scene = applyOverlays(
    {
      viewBox: { x: 0, y: 0, width: W, height: canvasH },
      background: palette.background,
      elements
    },
    ir.overlays,
    theme
  );
  return { scene, anchors: {} };
}
var leftOrd = startOrdinal;
var rightOrd = endOrdinal;
var fmtDate = (date) => formatDate(date, "axis");
function layoutRoadmap(ir, theme) {
  const { palette, typography, spacing } = theme;
  const draw = pen(theme);
  const margin = spacing.diagramMargin;
  const acts = ir.activities;
  const miles = ir.milestones;
  const allOrds = [];
  for (const a of acts) {
    allOrds.push(leftOrd(a.start), rightOrd(a.end ?? a.start));
  }
  for (const m of miles) allOrds.push(leftOrd(m.date));
  if (allOrds.length === 0) allOrds.push(0, 1);
  const minOrd = Math.min(...allOrds);
  const maxOrd = Math.max(...allOrds);
  const span = Math.max(1, maxOrd - minOrd);
  const W = 1040;
  const axisLeft = margin + 10;
  const axisRight = W - margin - 10;
  const axisW = axisRight - axisLeft;
  const dateX = (ord) => rhuInt(axisLeft + (ord - minOrd) / span * axisW);
  const sorted = [...acts].sort((a, b) => leftOrd(a.start) - leftOrd(b.start));
  const laneEnds = [];
  const pills = sorted.map((a) => {
    const s = leftOrd(a.start);
    const e = Math.max(rightOrd(a.end ?? a.start), s + Math.round(span / 30));
    let lane = laneEnds.findIndex((end) => s >= end);
    if (lane === -1) {
      lane = laneEnds.length;
      laneEnds.push(e);
    } else laneEnds[lane] = e;
    return { label: a.label, startOrd: s, endOrd: e, color: statusColor(palette, a.status), lane };
  });
  const numLanes = Math.max(1, laneEnds.length);
  const headerH = ir.metadata.title ? typography.titleFontSize + 20 : 8;
  const calloutH = miles.length ? 56 : 12;
  const laneH = 34;
  const lanePad = 6;
  const bandTop = margin + headerH + calloutH;
  const bandH = numLanes * laneH + (numLanes - 1) * lanePad;
  const bandBot = bandTop + bandH;
  const axisY = bandBot + 26;
  const totalH = rhuInt(axisY + typography.smallFontSize + margin);
  const elements = [];
  if (ir.metadata.title) {
    elements.push(
      draw.text(
        ir.metadata.title,
        margin,
        margin + typography.titleFontSize,
        typography.titleFontSize,
        palette.text,
        { weight: "bold", anchor: "start" }
      )
    );
  }
  elements.push(
    draw.rect(
      { x: axisLeft, y: bandTop, width: axisW, height: bandH },
      palette.surface,
      palette.border,
      1,
      { rx: 6 }
    )
  );
  const pillFont = typography.smallFontSize;
  for (const p of pills) {
    const x1 = dateX(p.startOrd), x2 = dateX(p.endOrd);
    const px = x1, pw = Math.max(14, x2 - x1);
    const py = bandTop + p.lane * (laneH + lanePad);
    elements.push(
      draw.rect({ x: px, y: py, width: pw, height: laneH }, p.color, p.color, 0, { rx: 6 })
    );
    const label = truncateText(p.label, pillFont, pw - 12);
    if (label && measureText(label, pillFont).width <= pw - 8) {
      elements.push(
        draw.text(
          label,
          px + pw / 2,
          py + laneH / 2 + pillFont * 0.35,
          pillFont,
          palette.background,
          { weight: "bold", anchor: "middle" }
        )
      );
    }
  }
  for (const m of miles) {
    const mx = dateX(leftOrd(m.date));
    const labelY = margin + headerH + typography.baseFontSize;
    elements.push(
      draw.text(m.label, mx, labelY, typography.baseFontSize, palette.text, {
        weight: "bold",
        anchor: "middle"
      })
    );
    elements.push(
      draw.text(
        `(${fmtDate(m.date)})`,
        mx,
        labelY + typography.smallFontSize + 2,
        typography.smallFontSize,
        palette.textMuted,
        { anchor: "middle" }
      )
    );
    elements.push(
      draw.path(
        `M ${mx} ${labelY + typography.smallFontSize + 6} L ${mx} ${bandTop}`,
        palette.border,
        1
      )
    );
    elements.push(draw.circle({ x: mx, y: bandTop }, 4, palette.primary, palette.background, 1.5));
  }
  elements.push(draw.path(`M ${axisLeft} ${axisY} L ${axisRight} ${axisY}`, palette.border, 1.5));
  const axisDates = /* @__PURE__ */ new Set();
  for (const m of miles) axisDates.add(m.date);
  for (const a of acts) {
    axisDates.add(a.start);
    if (a.end) axisDates.add(a.end);
  }
  for (const d of axisDates) {
    const x = dateX(leftOrd(d));
    elements.push(draw.path(`M ${x} ${axisY - 3} L ${x} ${axisY + 3}`, palette.border, 1));
    elements.push(
      draw.text(
        fmtDate(d),
        x,
        axisY + typography.smallFontSize + 2,
        typography.smallFontSize,
        palette.textMuted,
        { anchor: "middle" }
      )
    );
  }
  const scene = applyOverlays(
    {
      viewBox: { x: 0, y: 0, width: W, height: totalH },
      background: palette.background,
      elements
    },
    ir.overlays,
    theme
  );
  return { scene, anchors: {} };
}
function parseHex(c) {
  const s = c.trim();
  const m3 = s.match(/^#([0-9a-f])([0-9a-f])([0-9a-f])$/i);
  if (m3)
    return [
      parseInt(m3[1] + m3[1], 16),
      parseInt(m3[2] + m3[2], 16),
      parseInt(m3[3] + m3[3], 16)
    ];
  const m6 = s.match(/^#([0-9a-f]{2})([0-9a-f]{2})([0-9a-f]{2})$/i);
  if (m6) return [parseInt(m6[1], 16), parseInt(m6[2], 16), parseInt(m6[3], 16)];
  return null;
}
function relativeLuminance(color) {
  const rgb = parseHex(color);
  if (!rgb) return void 0;
  const linear = (v) => {
    const s = v / 255;
    return s <= 0.03928 ? s / 12.92 : ((s + 0.055) / 1.055) ** 2.4;
  };
  return 0.2126 * linear(rgb[0]) + 0.7152 * linear(rgb[1]) + 0.0722 * linear(rgb[2]);
}
function contrastRatio(a, b) {
  const la = relativeLuminance(a), lb = relativeLuminance(b);
  if (la === void 0 || lb === void 0) return 1;
  const hi = Math.max(la, lb), lo = Math.min(la, lb);
  return (hi + 0.05) / (lo + 0.05);
}
function bestContrast(fill, candidates, fallback) {
  let best = fallback, score = contrastRatio(fallback, fill);
  for (const c of candidates) {
    if (relativeLuminance(c) === void 0) continue;
    const next = contrastRatio(c, fill);
    if (next > score) {
      best = c;
      score = next;
    }
  }
  return best;
}
function readableText(fill, theme) {
  const p = theme.palette;
  return bestContrast(fill, [p.text, p.background, p.surface, p.textMuted, p.border], p.text);
}
function isDarkTheme(theme) {
  const lum = relativeLuminance(theme.palette.background);
  return lum !== void 0 ? lum < 0.2 : false;
}
var SECTION_PALETTE = [
  {
    header: "#5368D4",
    period: "#3F52B5",
    event: "#EEF0FC",
    headerText: "#FFFFFF",
    eventText: "#1E2D8A",
    eventBorder: "#BBBFEA"
  },
  {
    header: "#E07825",
    period: "#C46010",
    event: "#FEF1E4",
    headerText: "#FFFFFF",
    eventText: "#7A3A00",
    eventBorder: "#E8C09A"
  },
  {
    header: "#27A565",
    period: "#1A8A50",
    event: "#E4F7EF",
    headerText: "#FFFFFF",
    eventText: "#0B4D2C",
    eventBorder: "#A8DFC5"
  },
  {
    header: "#9B3A9A",
    period: "#7E2480",
    event: "#F6E8F6",
    headerText: "#FFFFFF",
    eventText: "#520050",
    eventBorder: "#D8A8D8"
  },
  {
    header: "#1FA8C0",
    period: "#1288A0",
    event: "#E0F4F8",
    headerText: "#FFFFFF",
    eventText: "#084E60",
    eventBorder: "#A0D8E4"
  },
  {
    header: "#B03068",
    period: "#901848",
    event: "#F8E0EA",
    headerText: "#FFFFFF",
    eventText: "#580028",
    eventBorder: "#E0A8C0"
  },
  {
    header: "#7A8000",
    period: "#606400",
    event: "#F0F0D0",
    headerText: "#FFFFFF",
    eventText: "#3A3C00",
    eventBorder: "#C8C880"
  },
  {
    header: "#2E6098",
    period: "#1E4878",
    event: "#D8E8F5",
    headerText: "#FFFFFF",
    eventText: "#102840",
    eventBorder: "#90B8D8"
  }
];
var SECTION_HDR_H = 38;
var PERIOD_HDR_H = 30;
var EVENT_BOX_H = 38;
var EVENT_BOX_GAP = 4;
var EVENT_AREA_PAD = 8;
var COL_PAD_H = 5;
var MIN_COL_W = 120;
var MARGIN = 32;
var leftOrd2 = startOrdinal;
var fmtPeriod = (date) => formatDate(date, "axis");
function layoutTimelineColumns(ir, theme) {
  const { palette, typography } = theme;
  const dark = isDarkTheme(theme);
  const getSectionColors = (idx) => {
    const raw = SECTION_PALETTE[idx % SECTION_PALETTE.length];
    if (dark) {
      return {
        header: raw.header,
        period: raw.period,
        event: palette.surface,
        headerText: "#FFFFFF",
        eventText: palette.text,
        eventBorder: palette.border
      };
    }
    return raw;
  };
  const irSections = ir.sections ?? [];
  const baseSecs = (irSections.length ? irSections.map((s) => ({
    label: s.label,
    startOrd: leftOrd2(s.start),
    periods: []
  })) : [{ label: ir.metadata.title ?? "Timeline", startOrd: -Infinity, periods: [] }]).sort((a, b) => a.startOrd - b.startOrd);
  const assignSection = (ord) => {
    let chosen = baseSecs[0];
    for (const s of baseSecs) if (s.startOrd <= ord) chosen = s;
    return chosen;
  };
  const entries = [];
  for (const m of ir.milestones)
    entries.push({ date: m.date, label: m.label });
  for (const a of ir.activities)
    entries.push({ date: a.start, label: a.label });
  for (const e of entries) {
    const ord = leftOrd2(e.date);
    const sec = assignSection(ord);
    let period = sec.periods.find((p2) => p2.date === e.date);
    if (!period) {
      period = { date: e.date, ord, events: [] };
      sec.periods.push(period);
    }
    period.events.push(e.label);
  }
  const sections2 = baseSecs.filter((s) => s.periods.length > 0);
  for (const s of sections2) s.periods.sort((a, b) => a.ord - b.ord);
  const totalPeriods = sections2.reduce((n, s) => n + s.periods.length, 0);
  const maxEvents = sections2.reduce(
    (m, s) => Math.max(
      m,
      s.periods.reduce((p2, per) => Math.max(p2, per.events.length), 0)
    ),
    0
  );
  const colW = MIN_COL_W;
  const canvasW = rhuInt(Math.max(totalPeriods, 1) * colW + MARGIN * 2);
  const titleH = ir.metadata.title ? typography.titleFontSize + 18 : 8;
  const eventAreaH = rhuInt(
    EVENT_AREA_PAD + maxEvents * EVENT_BOX_H + Math.max(0, maxEvents - 1) * EVENT_BOX_GAP + EVENT_AREA_PAD
  );
  const sectionBandTopY = MARGIN + titleH;
  const periodHdrTopY = sectionBandTopY + SECTION_HDR_H;
  const periodHdrBotY = periodHdrTopY + PERIOD_HDR_H;
  const AXIS_GAP = 16;
  const axisLineY = periodHdrBotY + AXIS_GAP;
  const eventsTopY = axisLineY + EVENT_AREA_PAD;
  const canvasH = rhuInt(axisLineY + eventAreaH + MARGIN);
  const elements = [];
  const p = pen(theme);
  if (ir.metadata.title) {
    elements.push(
      p.text(
        ir.metadata.title,
        rhuInt(canvasW / 2),
        MARGIN + typography.titleFontSize,
        typography.titleFontSize,
        palette.text,
        { weight: "bold", anchor: "middle" }
      )
    );
  }
  const eventFont = typography.smallFontSize;
  let cursorX = MARGIN;
  sections2.forEach((section, si) => {
    const colors = getSectionColors(si);
    const sectionW = rhuInt(colW * section.periods.length);
    elements.push(
      p.rect(
        { x: cursorX, y: sectionBandTopY, width: sectionW, height: SECTION_HDR_H },
        colors.header,
        colors.header,
        0
      )
    );
    elements.push(
      p.text(
        section.label,
        cursorX + sectionW / 2,
        sectionBandTopY + SECTION_HDR_H / 2 + eventFont * 0.35,
        typography.baseFontSize,
        colors.headerText,
        { weight: "bold", anchor: "middle" }
      )
    );
    section.periods.forEach((period, pi) => {
      const px = cursorX + pi * colW;
      elements.push(
        p.rect(
          { x: px, y: periodHdrTopY, width: colW, height: PERIOD_HDR_H },
          colors.period,
          colors.period,
          0
        )
      );
      if (pi > 0) {
        elements.push(
          p.path(
            `M ${px} ${periodHdrTopY} L ${px} ${periodHdrTopY + PERIOD_HDR_H}`,
            colors.header,
            1
          )
        );
      }
      elements.push(
        p.text(
          fmtPeriod(period.date),
          px + colW / 2,
          periodHdrTopY + PERIOD_HDR_H / 2 + eventFont * 0.35,
          eventFont,
          colors.headerText,
          { weight: "bold", anchor: "middle" }
        )
      );
      period.events.forEach((label, ei) => {
        const ey = eventsTopY + ei * (EVENT_BOX_H + EVENT_BOX_GAP);
        elements.push(
          p.rect(
            { x: px + COL_PAD_H, y: ey, width: colW - 2 * COL_PAD_H, height: EVENT_BOX_H },
            colors.event,
            colors.eventBorder,
            1,
            { rx: 5 }
          )
        );
        const lines2 = wrapText2(label, eventFont, colW - 2 * COL_PAD_H - 8, 2).lines;
        const lineH = rhuInt(eventFont * 1.25);
        const startY = ey + EVENT_BOX_H / 2 - (lines2.length - 1) * lineH / 2 + eventFont * 0.35;
        lines2.forEach((ln, li) => {
          elements.push(
            p.text(ln, px + colW / 2, startY + li * lineH, eventFont, colors.eventText, {
              anchor: "middle"
            })
          );
        });
      });
    });
    cursorX += sectionW;
  });
  const axisEndX = cursorX;
  elements.push(
    p.path(`M ${MARGIN} ${axisLineY} L ${axisEndX} ${axisLineY}`, palette.textMuted, 2)
  );
  elements.push(
    p.path(
      `M ${axisEndX} ${axisLineY} L ${axisEndX - 8} ${axisLineY - 4} L ${axisEndX - 8} ${axisLineY + 4} Z`,
      palette.textMuted,
      0,
      { fill: palette.textMuted }
    )
  );
  const scene = applyOverlays(
    {
      viewBox: { x: 0, y: 0, width: canvasW, height: canvasH },
      background: palette.background,
      elements
    },
    ir.overlays,
    theme
  );
  return { scene, anchors: {} };
}
function layoutNumbered(ir, theme) {
  const { palette, typography, spacing } = theme;
  const p = pen(theme);
  const margin = spacing.diagramMargin;
  const entries = collectEntries(ir);
  const n = Math.max(entries.length, 1);
  const circleR = 34;
  const numFont = rhuInt(typography.titleFontSize + 2);
  const dateFont = typography.smallFontSize;
  const titleFont = typography.baseFontSize;
  const innerW = 150;
  const titleLH = rhuInt(titleFont * 1.3);
  const dateGap = 6;
  const calloutGap = circleR + 14;
  const placed = entries.map((e) => ({
    ...e,
    dateStr: formatDate(e.date, "full"),
    titleLines: wrapText2(e.label, titleFont, innerW, 2).lines
  }));
  const maxTitleLines = Math.max(1, ...placed.map((e) => e.titleLines.length));
  const calloutH = calloutGap + dateFont + dateGap + maxTitleLines * titleLH + 10;
  const subtitle = typeof ir.metadata.subtitle === "string" ? ir.metadata.subtitle : void 0;
  const headerH = ir.metadata.title ? typography.titleFontSize + (subtitle ? typography.baseFontSize + 10 : 0) + 22 : 0;
  const axisY = margin + headerH + calloutH + circleR;
  const pitch = Math.max(innerW + 30, circleR * 2 + 60);
  const leftX = margin + 24;
  const nodeX = (i) => leftX + i * pitch + circleR;
  const totalW = rhuInt(nodeX(n - 1) + circleR + 24 + margin);
  const totalH = rhuInt(axisY + circleR + calloutH + margin);
  const elements = [];
  if (ir.metadata.title) {
    elements.push(
      p.text(
        ir.metadata.title,
        rhuInt(totalW / 2),
        margin + typography.titleFontSize,
        typography.titleFontSize,
        palette.text,
        { weight: "bold", anchor: "middle" }
      )
    );
    if (subtitle) {
      elements.push(
        p.text(
          subtitle,
          rhuInt(totalW / 2),
          margin + typography.titleFontSize + typography.baseFontSize + 8,
          typography.baseFontSize,
          palette.textMuted,
          { anchor: "middle" }
        )
      );
    }
  }
  const arcR = circleR + 5;
  let spine = `M ${rhu(margin)} ${rhu(axisY)}`;
  const apexes = [];
  placed.forEach((_e, i) => {
    const x = nodeX(i);
    const lineAbove = i % 2 === 0;
    const sweep = lineAbove ? 1 : 0;
    spine += ` L ${rhu(x - arcR)} ${rhu(axisY)} A ${arcR} ${arcR} 0 0 ${sweep} ${rhu(x + arcR)} ${rhu(axisY)}`;
    apexes.push({ x, y: lineAbove ? axisY - arcR : axisY + arcR });
  });
  spine += ` L ${rhu(totalW - margin)} ${rhu(axisY)}`;
  elements.push(p.path(spine, palette.border, 2));
  for (const a of apexes)
    elements.push(
      p.circle({ x: rhu(a.x), y: rhu(a.y) }, 4, palette.primary, palette.background, 1.5)
    );
  placed.forEach((e, i) => {
    const x = nodeX(i);
    const above = i % 2 === 1;
    const filled = e.status === "active";
    const numColor = filled ? "#FFFFFF" : palette.primary;
    elements.push(
      p.circle(
        { x: rhu(x), y: rhu(axisY) },
        circleR,
        filled ? palette.primary : palette.background,
        palette.primary,
        3
      )
    );
    elements.push(
      p.text(
        String(i + 1).padStart(2, "0"),
        rhu(x),
        rhu(axisY + numFont * 0.35),
        numFont,
        numColor,
        { weight: "bold", anchor: "middle" }
      )
    );
    if (above) {
      const dateY = axisY - calloutGap;
      elements.push(
        p.text(e.dateStr, rhu(x), rhu(dateY), dateFont, palette.textMuted, { anchor: "middle" })
      );
      let ty = dateY - dateFont - dateGap;
      for (let li = e.titleLines.length - 1; li >= 0; li--) {
        elements.push(
          p.text(e.titleLines[li], rhu(x), rhu(ty), titleFont, palette.primary, {
            weight: "bold",
            anchor: "middle"
          })
        );
        ty -= titleLH;
      }
    } else {
      const dateY = axisY + calloutGap;
      elements.push(
        p.text(e.dateStr, rhu(x), rhu(dateY), dateFont, palette.textMuted, { anchor: "middle" })
      );
      let ty = dateY + titleLH;
      for (const ln of e.titleLines) {
        elements.push(
          p.text(ln, rhu(x), rhu(ty), titleFont, palette.primary, {
            weight: "bold",
            anchor: "middle"
          })
        );
        ty += titleLH;
      }
    }
  });
  const scene = applyOverlays(
    {
      viewBox: { x: 0, y: 0, width: totalW, height: totalH },
      background: palette.background,
      elements
    },
    ir.overlays,
    theme
  );
  return { scene, anchors: {} };
}
var SPECTRAL_PALETTE = [
  "#00C49F",
  // Teal
  "#82CA9D",
  // Lime
  "#FFBB28",
  // Yellow / Warm Gold
  "#FF8042",
  // Orange
  "#FF4877",
  // Magenta / Pink
  "#A855F7",
  // Purple
  "#3B82F6",
  // Blue
  "#06B6D4"
  // Cyan
];
var LIGHT_PALETTE = [
  "#0284C7",
  // Sky Blue
  "#6366F1",
  // Indigo
  "#8B5CF6",
  // Violet
  "#EC4899",
  // Pink
  "#F59E0B",
  // Amber
  "#10B981",
  // Emerald
  "#14B8A6"
  // Teal
];
function resolveNodeColor(i, n, theme, entry) {
  if (entry.status && entry.status !== "default") {
    return statusColor(theme.palette, entry.status);
  }
  const isMono = theme.name === "minimal" || theme.name.startsWith("bw-") || theme.name === "executive";
  if (isMono) {
    return i % 2 === 0 ? theme.palette.primary : theme.palette.secondary;
  }
  const lum = relativeLuminance(theme.palette.background);
  const isDark = lum !== void 0 ? lum < 0.5 : false;
  const base = isDark ? SPECTRAL_PALETTE : LIGHT_PALETTE;
  return base[i % base.length];
}
function valleyPinPath(cx, cy, r, cusp) {
  const tipY = cy + r + cusp;
  return `M ${rhu(cx - r)} ${rhu(cy)} A ${r} ${r} 0 1 1 ${rhu(cx + r)} ${rhu(cy)} Q ${rhu(cx + r * 0.85)} ${rhu(cy + r * 0.65)} ${rhu(cx)} ${rhu(tipY)} Q ${rhu(cx - r * 0.85)} ${rhu(cy + r * 0.65)} ${rhu(cx - r)} ${rhu(cy)} Z`;
}
function peakPinPath(cx, cy, r) {
  return `M ${rhu(cx - r)} ${rhu(cy)} A ${r} ${r} 0 1 1 ${rhu(cx + r)} ${rhu(cy)} A ${r} ${r} 0 1 1 ${rhu(cx - r)} ${rhu(cy)} Z`;
}
function layoutWave(ir, theme, options) {
  const { palette, typography, spacing } = theme;
  const p = pen(theme);
  const margin = spacing.diagramMargin + 20;
  const elements = [];
  const entries = collectEntries(ir);
  const n = Math.max(entries.length, 1);
  const circleR = 26;
  const innerR = 17;
  const pinCusp = 14;
  const ribbonWidth = 26;
  const amplitude = 38;
  const pitch = Math.max(160, rhuInt(typography.baseFontSize * 12));
  const innerW = pitch - 20;
  const titleFont = typography.baseFontSize;
  const titleLH = rhuInt(titleFont * 1.3);
  const dateFont = typography.smallFontSize;
  const dateLH = rhuInt(dateFont * 1.3);
  const colors = entries.map((e, i) => resolveNodeColor(i, n, theme, e));
  const placed = entries.map((e) => {
    const isSyntheticDate = /^(?:date-|step-|activity-|milestone-|\d+$)/i.test(e.date) || e.ord === 0;
    const dateStr = !isSyntheticDate ? formatDate(e.date, "axis") : void 0;
    const titleLines = wrapText2(e.label, titleFont, innerW, 2).lines;
    const descLines = e.description ? wrapText2(e.description, dateFont, innerW, 2).lines : [];
    return {
      ...e,
      dateStr,
      titleLines,
      descLines
    };
  });
  const maxTitleLines = Math.max(1, ...placed.map((e) => e.titleLines.length));
  const maxDescLines = Math.max(0, ...placed.map((e) => e.descLines.length));
  const hasDates = placed.some((e) => e.dateStr !== void 0);
  const labelStackH = (hasDates ? dateLH + 4 : 0) + maxTitleLines * titleLH + (maxDescLines > 0 ? maxDescLines * dateLH + 4 : 0);
  const subtitle = typeof ir.metadata.subtitle === "string" ? ir.metadata.subtitle : void 0;
  const title = ir.metadata.title;
  const headerH = title ? typography.titleFontSize + 8 + (subtitle ? typography.baseFontSize + 8 : 0) + 28 : 0;
  const y_center = margin + headerH + circleR + amplitude;
  const y_peak = y_center - amplitude;
  const y_valley = y_center + amplitude;
  const leftX = margin + circleR + 10;
  const nodeX = (i) => leftX + i * pitch;
  const nodeY = (i) => i % 2 === 0 ? y_valley : y_peak;
  const totalW = rhuInt(nodeX(n - 1) + circleR + margin + 16);
  const labelTopY = y_valley + circleR + pinCusp + 22;
  const totalH = rhuInt(labelTopY + labelStackH + margin);
  if (title) {
    const titleY = margin + typography.titleFontSize;
    elements.push(
      p.text(title, totalW / 2, titleY, typography.titleFontSize + 6, palette.text, {
        anchor: "middle",
        weight: "bold"
      })
    );
    if (subtitle) {
      elements.push(
        p.text(subtitle, totalW / 2, titleY + typography.baseFontSize + 10, typography.baseFontSize, palette.textMuted, {
          anchor: "middle"
        })
      );
    }
  }
  const pts = Array.from({ length: n }, (_, i) => ({
    x: nodeX(i),
    y: nodeY(i)
  }));
  const defs = [];
  if (n > 1) {
    const gradId = "triton-wave-ribbon-grad";
    const stops = colors.map((c, idx) => {
      const pct = rhu(idx / (n - 1) * 100);
      return `<stop offset="${pct}%" stop-color="${c}" />`;
    }).join("");
    defs.push(
      `<linearGradient id="${gradId}" gradientUnits="userSpaceOnUse" x1="${rhu(pts[0].x)}" y1="0" x2="${rhu(pts[n - 1].x)}" y2="0">${stops}</linearGradient>`
    );
    let d = `M ${rhu(pts[0].x)} ${rhu(pts[0].y)}`;
    for (let i = 0; i < n - 1; i++) {
      const p0 = pts[i];
      const p1 = pts[i + 1];
      const dx = p1.x - p0.x;
      const cp1x = rhu(p0.x + dx * 0.5);
      const cp1y = rhu(p0.y);
      const cp2x = rhu(p1.x - dx * 0.5);
      const cp2y = rhu(p1.y);
      d += ` C ${cp1x} ${cp1y}, ${cp2x} ${cp2y}, ${rhu(p1.x)} ${rhu(p1.y)}`;
    }
    elements.push(
      p.path(d, `url(#${gradId})`, ribbonWidth)
    );
  }
  const anchors = {};
  for (let i = 0; i < n; i++) {
    const pt = pts[i];
    const color = colors[i];
    const entry = placed[i];
    const isValley = i % 2 === 0;
    const pinD = isValley ? valleyPinPath(pt.x, pt.y, circleR, pinCusp) : peakPinPath(pt.x, pt.y, circleR);
    elements.push(p.path(pinD, color, 1, { fill: color }));
    elements.push(p.circle({ x: pt.x, y: pt.y }, innerR, "#FFFFFF", "#FFFFFF", 0));
    let renderedIcon = false;
    if (entry.icon && options?.icons) {
      const ref = parseIconRef(entry.icon);
      if (ref.ok) {
        const resolved = resolveIcon(ref.value, options.icons);
        if (resolved.ok) {
          const iconSize = 18;
          elements.push(
            p.icon(resolved.value, rhu(pt.x - iconSize / 2), rhu(pt.y - iconSize / 2), iconSize)
          );
          renderedIcon = true;
        }
      }
    }
    if (!renderedIcon) {
      const stepNumber = String(i + 1).padStart(2, "0");
      elements.push(
        p.text(stepNumber, pt.x, pt.y + 4.5, 12, "#1E293B", {
          anchor: "middle",
          weight: "bold"
        })
      );
    }
    let curY = labelTopY;
    if (entry.dateStr) {
      elements.push(
        p.text(entry.dateStr, pt.x, curY, dateFont, palette.textMuted, {
          anchor: "middle"
        })
      );
      curY += dateLH + 2;
    }
    for (let li = 0; li < entry.titleLines.length; li++) {
      const lineText = entry.titleLines[li] ?? "";
      elements.push(
        p.text(lineText, pt.x, curY + li * titleLH, titleFont, palette.text, {
          anchor: "middle",
          weight: "bold"
        })
      );
    }
    curY += entry.titleLines.length * titleLH;
    for (let li = 0; li < entry.descLines.length; li++) {
      const lineText = entry.descLines[li] ?? "";
      elements.push(
        p.text(lineText, pt.x, curY + li * dateLH, dateFont, palette.textMuted, {
          anchor: "middle"
        })
      );
    }
    anchors[entry.id] = {
      bounds: {
        x: pt.x - circleR,
        y: pt.y - circleR,
        width: circleR * 2,
        height: isValley ? circleR * 2 + pinCusp : circleR * 2
      }
    };
  }
  const baseScene = {
    viewBox: { x: 0, y: 0, width: totalW, height: totalH },
    background: palette.background,
    elements,
    ...defs.length > 0 ? { defs } : {}
  };
  const finalScene = applyOverlays(baseScene, ir.overlays, theme);
  return { scene: finalScene, anchors };
}
function layoutTimeline(ir, theme, options) {
  switch (ir.layout) {
    case "vertical-spine":
      return layoutVerticalSpine(ir, theme);
    case "serpentine":
      return layoutSerpentine(ir, theme);
    case "roadmap":
      return layoutRoadmap(ir, theme);
    case "timeline-columns":
      return layoutTimelineColumns(ir, theme);
    case "numbered":
      return layoutNumbered(ir, theme);
    case "wave":
    case "undulating":
      return layoutWave(ir, theme, options);
    case "horizontal":
    default:
      return layoutHorizontal(ir, theme);
  }
}
function layoutHorizontal(ir, theme) {
  const { palette, typography, spacing } = theme;
  const p = pen(theme);
  const margin = spacing.diagramMargin;
  const elements = [];
  const charWidth = typography.smallFontSize * 0.6;
  const minBarW = 60;
  const barPadding = 16;
  const slotGap = 24;
  const staggerGap = 8;
  const trackHeight = 36;
  const trackPad = 8;
  const milestoneR = 8;
  const trackOrder = ir.tracks.map((t) => t.id);
  const trackIndex = new Map(trackOrder.map((id, i) => [id, i]));
  const dateMap = buildDateMap([
    ...ir.milestones.map((m) => m.date),
    ...ir.activities.flatMap((a) => [a.start, ...a.end ? [a.end] : []]),
    ...ir.sections?.flatMap((s) => [s.start, s.end]) ?? []
  ]);
  const sortedDates = [
    .../* @__PURE__ */ new Set([
      ...ir.milestones.map((m) => m.date),
      ...ir.activities.map((a) => a.start),
      ...ir.activities.filter((a) => a.end).map((a) => a.end),
      ...ir.sections?.flatMap((s) => [s.start, s.end]) ?? []
    ])
  ].sort((a, b) => dateMap.toNum(a) - dateMap.toNum(b));
  function actBarWidth(act) {
    const textW = act.label.length * charWidth + barPadding;
    return Math.max(textW, minBarW);
  }
  const activitiesAtDate = /* @__PURE__ */ new Map();
  for (const act of ir.activities) {
    if (!activitiesAtDate.has(act.start)) activitiesAtDate.set(act.start, []);
    activitiesAtDate.get(act.start).push(act);
  }
  const slotWidth = /* @__PURE__ */ new Map();
  for (const date of sortedDates) {
    const acts = activitiesAtDate.get(date);
    if (!acts || acts.length === 0) {
      slotWidth.set(date, 0);
      continue;
    }
    const byTrack = /* @__PURE__ */ new Map();
    for (const a of acts) {
      if (!byTrack.has(a.track)) byTrack.set(a.track, []);
      byTrack.get(a.track).push(a);
    }
    let widest = 0;
    for (const [, trackActs] of byTrack) {
      let w = 0;
      for (let i = 0; i < trackActs.length; i++) {
        if (i > 0) w += staggerGap;
        w += actBarWidth(trackActs[i]);
      }
      widest = Math.max(widest, w);
    }
    slotWidth.set(date, widest);
  }
  const axisLeft = margin + 20;
  const dateX = /* @__PURE__ */ new Map();
  const totalProportionalW = Math.max(400, dateMap.range / 365 * 80);
  let cursor = axisLeft;
  for (let i = 0; i < sortedDates.length; i++) {
    const date = sortedDates[i];
    const proportionalX = axisLeft + (dateMap.toNum(date) - dateMap.min) / (dateMap.range || 1) * totalProportionalW;
    const x = Math.max(proportionalX, cursor);
    dateX.set(date, x);
    cursor = x + (slotWidth.get(date) ?? 0) + slotGap;
  }
  const axisRight = cursor - slotGap + margin;
  const axisY = margin + (ir.metadata.title ? typography.titleFontSize + 16 : 0) + 60;
  if (ir.metadata.title) {
    elements.push(
      p.text(
        ir.metadata.title,
        margin,
        margin + typography.titleFontSize,
        typography.titleFontSize,
        palette.text,
        { weight: "bold" }
      )
    );
  }
  if (ir.sections) {
    const bgHeight = 50 + trackOrder.length * (trackHeight + trackPad) + 40;
    for (const sec of ir.sections) {
      const sx = dateX.get(sec.start) ?? axisLeft;
      const endDateX = dateX.get(sec.end) ?? sx;
      const endSlotW = slotWidth.get(sec.end) ?? 0;
      const ex = endDateX + Math.max(endSlotW, 20);
      elements.push(
        p.rect(
          { x: sx - 8, y: axisY - 50, width: ex - sx + 16, height: bgHeight },
          palette.primary + "0A",
          palette.primary + "33",
          1,
          { rx: 4 }
        )
      );
      elements.push(
        p.text(sec.label, sx - 2, axisY - 36, typography.smallFontSize, palette.primary, {
          weight: "bold"
        })
      );
    }
  }
  elements.push(p.path(`M ${axisLeft} ${axisY} L ${axisRight} ${axisY}`, palette.border, 2));
  const showTrackLabels = !(trackOrder.length === 1 && trackOrder[0] === "default");
  if (showTrackLabels) {
    for (let i = 0; i < trackOrder.length; i++) {
      const track = ir.tracks.find((t) => t.id === trackOrder[i]);
      if (!track) continue;
      const laneY = axisY + milestoneR + trackPad + i * (trackHeight + trackPad);
      elements.push(
        p.text(
          track.label,
          margin,
          laneY + trackHeight / 2 + typography.smallFontSize * 0.4,
          typography.smallFontSize,
          palette.textMuted
        )
      );
    }
  }
  const slotCursor = /* @__PURE__ */ new Map();
  for (const act of ir.activities) {
    const tIdx = trackIndex.get(act.track) ?? 0;
    const laneY = axisY + milestoneR + trackPad + tIdx * (trackHeight + trackPad);
    const sx = dateX.get(act.start) ?? axisLeft;
    const barW = act.end ? Math.max((dateX.get(act.end) ?? sx) - sx, actBarWidth(act)) : actBarWidth(act);
    const groupKey = `${act.track}:${act.start}`;
    const offset = slotCursor.get(groupKey) ?? 0;
    slotCursor.set(groupKey, offset + barW + staggerGap);
    const barX = sx + offset;
    const barH = trackHeight - 4;
    const barY = laneY + 2;
    const fill = activityFill(act, palette);
    elements.push(
      p.rect({ x: barX, y: barY, width: barW, height: barH }, fill, palette.border, 1, { rx: 4 })
    );
    elements.push(
      p.text(
        act.label,
        barX + barW / 2,
        barY + barH / 2 + typography.smallFontSize * 0.4,
        typography.smallFontSize,
        palette.text,
        { anchor: "middle" }
      )
    );
  }
  for (const ms of ir.milestones) {
    const mx = dateX.get(ms.date) ?? axisLeft;
    const my = axisY;
    const r = milestoneR;
    elements.push(
      p.path(
        `M ${mx} ${my - r} L ${mx + r} ${my} L ${mx} ${my + r} L ${mx - r} ${my} Z`,
        palette.background,
        1,
        { fill: palette.primary }
      )
    );
    elements.push(
      p.text(ms.date, mx, my - r - 6, typography.smallFontSize, palette.textMuted, {
        anchor: "middle"
      })
    );
    elements.push(
      p.text(
        ms.label,
        mx,
        my + r + typography.baseFontSize + 2,
        typography.baseFontSize,
        palette.text,
        { anchor: "middle" }
      )
    );
  }
  const numTracks = Math.max(trackOrder.length, 1);
  const totalH = axisY + milestoneR + trackPad + numTracks * (trackHeight + trackPad) + typography.baseFontSize + 20 + margin;
  const scene = applyOverlays(
    {
      viewBox: { x: 0, y: 0, width: axisRight + margin, height: totalH },
      background: palette.background,
      elements
    },
    ir.overlays,
    theme
  );
  return { scene, anchors: {} };
}
function tryOrdinal(date) {
  try {
    const [y, m, d] = coerceLeft(parseIRDate(date));
    return dateToOrdinal(y, m, d);
  } catch {
    return NaN;
  }
}
function buildDateMap(dates) {
  const unique = [...new Set(dates)];
  const ords = unique.map(tryOrdinal);
  const valid = ords.filter((n) => !isNaN(n));
  if (valid.length > 0) {
    const min = Math.min(...valid);
    const max = Math.max(...valid);
    const range = max - min || 1;
    return {
      toNum: (d) => {
        const n = tryOrdinal(d);
        return isNaN(n) ? min : n;
      },
      min,
      max,
      range
    };
  }
  const order = new Map(unique.map((d, i) => [d, i]));
  return {
    toNum: (d) => order.get(d) ?? 0,
    min: 0,
    max: unique.length - 1,
    range: unique.length
  };
}
function activityFill(act, palette) {
  switch (act.status) {
    case "active":
      return palette.primary + "33";
    case "done":
      return palette.success + "33";
    case "blocked":
      return palette.error + "33";
    default:
      return palette.surface;
  }
}
function slugify3(text) {
  return text.toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-|-$/g, "") || "";
}
var tracks = /* @__PURE__ */ new Map();
var activities = [];
var milestones = [];
var sections = [];
var overlays2 = [];
var actCounter = 0;
var msCounter = 0;
var secCounter = 0;
var currentSec = null;
function ensureTrack(id) {
  if (!id) id = "default";
  if (!tracks.has(id)) tracks.set(id, { id, label: id.charAt(0).toUpperCase() + id.slice(1) });
  return id;
}
function addActivity(start, end, label, status, trackId, description) {
  actCounter++;
  activities.push({
    id: slugify3(label) || `activity-${actCounter}`,
    label,
    track: ensureTrack(trackId),
    start,
    end: end || void 0,
    status: status || "default",
    ...description ? { description } : {}
  });
}
function addMilestone(date, label, trackId, description) {
  msCounter++;
  milestones.push({
    id: slugify3(label) || `milestone-${msCounter}`,
    label,
    date,
    track: trackId ? ensureTrack(trackId) : void 0,
    ...description ? { description } : {}
  });
}
function openSection(label) {
  secCounter++;
  currentSec = { id: slugify3(label) || `section-${secCounter}`, label, start: null, end: null };
}
function updateSectionDate(date) {
  if (currentSec) {
    if (!currentSec.start) currentSec.start = date;
    currentSec.end = date;
  }
}
function closeSection() {
  if (currentSec && currentSec.start) {
    sections.push(currentSec);
  }
  currentSec = null;
}
function reset2() {
  tracks.clear();
  activities.length = 0;
  milestones.length = 0;
  sections.length = 0;
  overlays2.length = 0;
  actCounter = 0;
  msCounter = 0;
  secCounter = 0;
  currentSec = null;
}
function peg$subclass2(child, parent) {
  function C() {
    this.constructor = child;
  }
  C.prototype = parent.prototype;
  child.prototype = new C();
}
function peg$SyntaxError2(message, expected, found, location) {
  var self = Error.call(this, message);
  if (Object.setPrototypeOf) {
    Object.setPrototypeOf(self, peg$SyntaxError2.prototype);
  }
  self.expected = expected;
  self.found = found;
  self.location = location;
  self.name = "SyntaxError";
  return self;
}
peg$subclass2(peg$SyntaxError2, Error);
function peg$padEnd2(str, targetLength, padString) {
  padString = padString || " ";
  if (str.length > targetLength) {
    return str;
  }
  targetLength -= str.length;
  padString += padString.repeat(targetLength);
  return str + padString.slice(0, targetLength);
}
peg$SyntaxError2.prototype.format = function(sources) {
  var str = "Error: " + this.message;
  if (this.location) {
    var src = null;
    var k;
    for (k = 0; k < sources.length; k++) {
      if (sources[k].source === this.location.source) {
        src = sources[k].text.split(/\r\n|\n|\r/g);
        break;
      }
    }
    var s = this.location.start;
    var offset_s = this.location.source && typeof this.location.source.offset === "function" ? this.location.source.offset(s) : s;
    var loc = this.location.source + ":" + offset_s.line + ":" + offset_s.column;
    if (src) {
      var e = this.location.end;
      var filler = peg$padEnd2("", offset_s.line.toString().length, " ");
      var line = src[s.line - 1];
      var last = s.line === e.line ? e.column : line.length + 1;
      var hatLen = last - s.column || 1;
      str += "\n --> " + loc + "\n" + filler + " |\n" + offset_s.line + " | " + line + "\n" + filler + " | " + peg$padEnd2("", s.column - 1, " ") + peg$padEnd2("", hatLen, "^");
    } else {
      str += "\n at " + loc;
    }
  }
  return str;
};
peg$SyntaxError2.buildMessage = function(expected, found) {
  var DESCRIBE_EXPECTATION_FNS = {
    literal: function(expectation) {
      return '"' + literalEscape(expectation.text) + '"';
    },
    class: function(expectation) {
      var escapedParts = expectation.parts.map(function(part) {
        return Array.isArray(part) ? classEscape(part[0]) + "-" + classEscape(part[1]) : classEscape(part);
      });
      return "[" + (expectation.inverted ? "^" : "") + escapedParts.join("") + "]";
    },
    any: function() {
      return "any character";
    },
    end: function() {
      return "end of input";
    },
    other: function(expectation) {
      return expectation.description;
    }
  };
  function hex(ch) {
    return ch.charCodeAt(0).toString(16).toUpperCase();
  }
  function literalEscape(s) {
    return s.replace(/\\/g, "\\\\").replace(/"/g, '\\"').replace(/\0/g, "\\0").replace(/\t/g, "\\t").replace(/\n/g, "\\n").replace(/\r/g, "\\r").replace(/[\x00-\x0F]/g, function(ch) {
      return "\\x0" + hex(ch);
    }).replace(/[\x10-\x1F\x7F-\x9F]/g, function(ch) {
      return "\\x" + hex(ch);
    });
  }
  function classEscape(s) {
    return s.replace(/\\/g, "\\\\").replace(/\]/g, "\\]").replace(/\^/g, "\\^").replace(/-/g, "\\-").replace(/\0/g, "\\0").replace(/\t/g, "\\t").replace(/\n/g, "\\n").replace(/\r/g, "\\r").replace(/[\x00-\x0F]/g, function(ch) {
      return "\\x0" + hex(ch);
    }).replace(/[\x10-\x1F\x7F-\x9F]/g, function(ch) {
      return "\\x" + hex(ch);
    });
  }
  function describeExpectation(expectation) {
    return DESCRIBE_EXPECTATION_FNS[expectation.type](expectation);
  }
  function describeExpected(expected2) {
    var descriptions = expected2.map(describeExpectation);
    var i, j;
    descriptions.sort();
    if (descriptions.length > 0) {
      for (i = 1, j = 1; i < descriptions.length; i++) {
        if (descriptions[i - 1] !== descriptions[i]) {
          descriptions[j] = descriptions[i];
          j++;
        }
      }
      descriptions.length = j;
    }
    switch (descriptions.length) {
      case 1:
        return descriptions[0];
      case 2:
        return descriptions[0] + " or " + descriptions[1];
      default:
        return descriptions.slice(0, -1).join(", ") + ", or " + descriptions[descriptions.length - 1];
    }
  }
  function describeFound(found2) {
    return found2 ? '"' + literalEscape(found2) + '"' : "end of input";
  }
  return "Expected " + describeExpected(expected) + " but " + describeFound(found) + " found.";
};
function peg$parse2(input, options) {
  options = options !== void 0 ? options : {};
  var peg$FAILED = {};
  var peg$source = options.grammarSource;
  var peg$startRuleFunctions = { Document: peg$parseDocument };
  var peg$startRuleFunction = peg$parseDocument;
  var peg$c0 = "timeline";
  var peg$c1 = "title";
  var peg$c2 = "subtitle";
  var peg$c3 = "theme";
  var peg$c4 = "layout";
  var peg$c5 = "axisUnit";
  var peg$c6 = "---";
  var peg$c7 = "section";
  var peg$c8 = ":";
  var peg$c9 = "--";
  var peg$c10 = "@";
  var peg$c11 = "|";
  var peg$c12 = "milestone";
  var peg$c13 = "active";
  var peg$c14 = "done";
  var peg$c15 = "blocked";
  var peg$c16 = "default";
  var peg$c17 = "note";
  var peg$c18 = "callout";
  var peg$c19 = '"';
  var peg$c20 = "at";
  var peg$c21 = "offset";
  var peg$c22 = ",";
  var peg$c23 = "-";
  var peg$c24 = "legend";
  var peg$c25 = "end";
  var peg$c26 = "bottom-right";
  var peg$c27 = "bottom-left";
  var peg$c28 = "top-right";
  var peg$c29 = "top-left";
  var peg$c30 = "%%";
  var peg$c31 = "\r\n";
  var peg$r0 = /^[^\n]/;
  var peg$r1 = /^[^:\n@|]/;
  var peg$r2 = /^[a-zA-Z0-9_\-\/.]/;
  var peg$r3 = /^[a-zA-Z_]/;
  var peg$r4 = /^[a-zA-Z0-9_\-]/;
  var peg$r5 = /^[^"]/;
  var peg$r6 = /^[0-9]/;
  var peg$r7 = /^[a-zA-Z0-9_]/;
  var peg$r8 = /^[^:\n]/;
  var peg$r9 = /^[ \t]/;
  var peg$r10 = /^[\n\r]/;
  var peg$e0 = peg$literalExpectation("timeline", false);
  var peg$e1 = peg$literalExpectation("title", false);
  var peg$e2 = peg$classExpectation(["\n"], true, false);
  var peg$e3 = peg$literalExpectation("subtitle", false);
  var peg$e4 = peg$literalExpectation("theme", false);
  var peg$e5 = peg$literalExpectation("layout", false);
  var peg$e6 = peg$literalExpectation("axisUnit", false);
  var peg$e7 = peg$literalExpectation("---", false);
  var peg$e8 = peg$literalExpectation("section", false);
  var peg$e9 = peg$literalExpectation(":", false);
  var peg$e10 = peg$literalExpectation("--", false);
  var peg$e11 = peg$literalExpectation("@", false);
  var peg$e12 = peg$literalExpectation("|", false);
  var peg$e13 = peg$classExpectation([":", "\n", "@", "|"], true, false);
  var peg$e14 = peg$literalExpectation("milestone", false);
  var peg$e15 = peg$literalExpectation("active", false);
  var peg$e16 = peg$literalExpectation("done", false);
  var peg$e17 = peg$literalExpectation("blocked", false);
  var peg$e18 = peg$literalExpectation("default", false);
  var peg$e19 = peg$classExpectation([["a", "z"], ["A", "Z"], ["0", "9"], "_", "-", "/", "."], false, false);
  var peg$e20 = peg$classExpectation([["a", "z"], ["A", "Z"], "_"], false, false);
  var peg$e21 = peg$classExpectation([["a", "z"], ["A", "Z"], ["0", "9"], "_", "-"], false, false);
  var peg$e22 = peg$literalExpectation("note", false);
  var peg$e23 = peg$literalExpectation("callout", false);
  var peg$e24 = peg$literalExpectation('"', false);
  var peg$e25 = peg$classExpectation(['"'], true, false);
  var peg$e26 = peg$literalExpectation("at", false);
  var peg$e27 = peg$literalExpectation("offset", false);
  var peg$e28 = peg$literalExpectation(",", false);
  var peg$e29 = peg$literalExpectation("-", false);
  var peg$e30 = peg$classExpectation([["0", "9"]], false, false);
  var peg$e31 = peg$literalExpectation("legend", false);
  var peg$e32 = peg$literalExpectation("end", false);
  var peg$e33 = peg$literalExpectation("bottom-right", false);
  var peg$e34 = peg$literalExpectation("bottom-left", false);
  var peg$e35 = peg$literalExpectation("top-right", false);
  var peg$e36 = peg$literalExpectation("top-left", false);
  var peg$e37 = peg$classExpectation([["a", "z"], ["A", "Z"], ["0", "9"], "_"], false, false);
  var peg$e38 = peg$classExpectation([":", "\n"], true, false);
  var peg$e39 = peg$literalExpectation("%%", false);
  var peg$e40 = peg$anyExpectation();
  var peg$e41 = peg$classExpectation([" ", "	"], false, false);
  var peg$e42 = peg$literalExpectation("\r\n", false);
  var peg$e43 = peg$classExpectation(["\n", "\r"], false, false);
  var peg$f0 = function(fm, header, directives, body) {
    closeSection();
    const titleDir = directives.find((d) => d && d.key === "title");
    const subtitleDir = directives.find((d) => d && d.key === "subtitle");
    const themeDir = directives.find((d) => d && d.key === "theme");
    const layoutDir = directives.find((d) => d && d.key === "layout");
    return {
      version: "1.0",
      metadata: {
        ...fm || {},
        ...titleDir ? { title: titleDir.value } : {},
        ...subtitleDir ? { subtitle: subtitleDir.value } : {},
        ...themeDir ? { theme: themeDir.value } : {}
      },
      layout: layoutDir ? layoutDir.value : "horizontal",
      tracks: tracks.size > 0 ? [...tracks.values()] : [{ id: "default", label: "Default" }],
      activities,
      milestones,
      sections: sections.length > 0 ? sections : void 0,
      overlays: overlays2
    };
  };
  var peg$f1 = function() {
    return "timeline";
  };
  var peg$f2 = function(value) {
    return { key: "title", value: value.trim().replace(/^["']|["']$/g, "") };
  };
  var peg$f3 = function(value) {
    return { key: "subtitle", value: value.trim().replace(/^["']|["']$/g, "") };
  };
  var peg$f4 = function(value) {
    return { key: "theme", value: value.trim().replace(/^["']|["']$/g, "") };
  };
  var peg$f5 = function() {
    return ext;
  };
  var peg$f6 = function(key, value) {
    return { key, value: value.trim() };
  };
  var peg$f7 = function() {
    return ext;
  };
  var peg$f8 = function(line) {
    return line;
  };
  var peg$f9 = function(lines2) {
    const meta = {};
    for (const line of lines2) {
      const m = line.match(/^\s*(\w+)\s*:\s*(.+?)\s*$/);
      if (m) meta[m[1]] = m[2];
    }
    return meta;
  };
  var peg$f10 = function(items) {
    return items.filter(Boolean);
  };
  var peg$f11 = function(label) {
    closeSection();
    openSection(label.trim());
    return null;
  };
  var peg$f12 = function(entry) {
    return entry;
  };
  var peg$f13 = function(date, label) {
    addActivity(date, null, label, "default", null);
    updateSectionDate(date);
    return { type: "activity", label };
  };
  var peg$f14 = function(text2) {
    return text2.trim();
  };
  var peg$f15 = function() {
    return ext;
  };
  var peg$f16 = function(entry) {
    return entry;
  };
  var peg$f17 = function(start, end, label, s) {
    return s;
  };
  var peg$f18 = function(start, end, label, status, id) {
    return id;
  };
  var peg$f19 = function(start, end, label, status, track, d) {
    return d;
  };
  var peg$f20 = function(start, end, label, status, track, desc) {
    addActivity(start, end, label, status, track, desc);
    updateSectionDate(start);
    updateSectionDate(end);
    return { type: "activity", label };
  };
  var peg$f21 = function(date, label, kind, id) {
    return id;
  };
  var peg$f22 = function(date, label, kind, track, d) {
    return d;
  };
  var peg$f23 = function(date, label, kind, track, desc) {
    if (kind === "milestone") addMilestone(date, label, track, desc);
    else addActivity(date, null, label, kind, track, desc);
    updateSectionDate(date);
    return { type: kind === "milestone" ? "milestone" : "activity", label };
  };
  var peg$f24 = function(text2) {
    return text2.trim();
  };
  var peg$f25 = function(text2) {
    return text2.trim();
  };
  var peg$f26 = function(kind, text2, t) {
    return t;
  };
  var peg$f27 = function(kind, text2, target, offset2) {
    const note = { type: kind, text: text2, target, offset: offset2 || void 0 };
    overlays2.push(note);
    return note;
  };
  var peg$f28 = function(dx, dy) {
    return { dx, dy };
  };
  var peg$f29 = function(sign, digits) {
    return parseInt((sign || "") + digits, 10);
  };
  var peg$f30 = function(corner, t) {
    return t;
  };
  var peg$f31 = function(corner, title, entries) {
    const legend = { type: "legend", corner, title: title || void 0, entries };
    overlays2.push(legend);
    return legend;
  };
  var peg$f32 = function(key, value) {
    return { key: key.trim(), value: value.trim() };
  };
  var peg$f33 = function() {
    return null;
  };
  var peg$currPos = options.peg$currPos | 0;
  var peg$savedPos = peg$currPos;
  var peg$posDetailsCache = [{ line: 1, column: 1 }];
  var peg$maxFailPos = peg$currPos;
  var peg$maxFailExpected = options.peg$maxFailExpected || [];
  var peg$silentFails = options.peg$silentFails | 0;
  var peg$result;
  if (options.startRule) {
    if (!(options.startRule in peg$startRuleFunctions)) {
      throw new Error(`Can't start parsing from rule "` + options.startRule + '".');
    }
    peg$startRuleFunction = peg$startRuleFunctions[options.startRule];
  }
  function text() {
    return input.substring(peg$savedPos, peg$currPos);
  }
  function offset() {
    return peg$savedPos;
  }
  function range() {
    return {
      source: peg$source,
      start: peg$savedPos,
      end: peg$currPos
    };
  }
  function location() {
    return peg$computeLocation(peg$savedPos, peg$currPos);
  }
  function expected(description, location2) {
    location2 = location2 !== void 0 ? location2 : peg$computeLocation(peg$savedPos, peg$currPos);
    throw peg$buildStructuredError(
      [peg$otherExpectation(description)],
      input.substring(peg$savedPos, peg$currPos),
      location2
    );
  }
  function error(message, location2) {
    location2 = location2 !== void 0 ? location2 : peg$computeLocation(peg$savedPos, peg$currPos);
    throw peg$buildSimpleError(message, location2);
  }
  function peg$literalExpectation(text2, ignoreCase) {
    return { type: "literal", text: text2, ignoreCase };
  }
  function peg$classExpectation(parts, inverted, ignoreCase) {
    return { type: "class", parts, inverted, ignoreCase };
  }
  function peg$anyExpectation() {
    return { type: "any" };
  }
  function peg$endExpectation() {
    return { type: "end" };
  }
  function peg$otherExpectation(description) {
    return { type: "other", description };
  }
  function peg$computePosDetails(pos) {
    var details = peg$posDetailsCache[pos];
    var p;
    if (details) {
      return details;
    } else {
      if (pos >= peg$posDetailsCache.length) {
        p = peg$posDetailsCache.length - 1;
      } else {
        p = pos;
        while (!peg$posDetailsCache[--p]) {
        }
      }
      details = peg$posDetailsCache[p];
      details = {
        line: details.line,
        column: details.column
      };
      while (p < pos) {
        if (input.charCodeAt(p) === 10) {
          details.line++;
          details.column = 1;
        } else {
          details.column++;
        }
        p++;
      }
      peg$posDetailsCache[pos] = details;
      return details;
    }
  }
  function peg$computeLocation(startPos, endPos, offset2) {
    var startPosDetails = peg$computePosDetails(startPos);
    var endPosDetails = peg$computePosDetails(endPos);
    var res = {
      source: peg$source,
      start: {
        offset: startPos,
        line: startPosDetails.line,
        column: startPosDetails.column
      },
      end: {
        offset: endPos,
        line: endPosDetails.line,
        column: endPosDetails.column
      }
    };
    if (offset2 && peg$source && typeof peg$source.offset === "function") {
      res.start = peg$source.offset(res.start);
      res.end = peg$source.offset(res.end);
    }
    return res;
  }
  function peg$fail(expected2) {
    if (peg$currPos < peg$maxFailPos) {
      return;
    }
    if (peg$currPos > peg$maxFailPos) {
      peg$maxFailPos = peg$currPos;
      peg$maxFailExpected = [];
    }
    peg$maxFailExpected.push(expected2);
  }
  function peg$buildSimpleError(message, location2) {
    return new peg$SyntaxError2(message, null, null, location2);
  }
  function peg$buildStructuredError(expected2, found, location2) {
    return new peg$SyntaxError2(
      peg$SyntaxError2.buildMessage(expected2, found),
      expected2,
      found,
      location2
    );
  }
  function peg$parseDocument() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8;
    s0 = peg$currPos;
    s1 = peg$parseExtFrontmatter();
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    s2 = peg$parse_();
    s3 = peg$parseHeader();
    if (s3 !== peg$FAILED) {
      s4 = peg$parse_();
      s5 = [];
      s6 = peg$parseDirective();
      while (s6 !== peg$FAILED) {
        s5.push(s6);
        s6 = peg$parseDirective();
      }
      s6 = peg$parse_();
      s7 = peg$parseBody();
      s8 = peg$parse_();
      peg$savedPos = s0;
      s0 = peg$f0(s1, s3, s5, s7);
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseHeader() {
    var s0, s1, s2;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 8) === peg$c0) {
      s1 = peg$c0;
      peg$currPos += 8;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e0);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$parseNL();
      if (s2 !== peg$FAILED) {
        peg$savedPos = s0;
        s0 = peg$f1();
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseDirective() {
    var s0;
    s0 = peg$parseExtDirective();
    if (s0 === peg$FAILED) {
      s0 = peg$parseMermaidDirective();
    }
    return s0;
  }
  function peg$parseMermaidDirective() {
    var s0, s1, s2, s3, s4, s5, s6;
    s0 = peg$currPos;
    s1 = peg$parse_();
    if (input.substr(peg$currPos, 5) === peg$c1) {
      s2 = peg$c1;
      peg$currPos += 5;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e1);
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$parse__();
      if (s3 !== peg$FAILED) {
        s4 = peg$currPos;
        s5 = [];
        s6 = input.charAt(peg$currPos);
        if (peg$r0.test(s6)) {
          peg$currPos++;
        } else {
          s6 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e2);
          }
        }
        if (s6 !== peg$FAILED) {
          while (s6 !== peg$FAILED) {
            s5.push(s6);
            s6 = input.charAt(peg$currPos);
            if (peg$r0.test(s6)) {
              peg$currPos++;
            } else {
              s6 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e2);
              }
            }
          }
        } else {
          s5 = peg$FAILED;
        }
        if (s5 !== peg$FAILED) {
          s4 = input.substring(s4, peg$currPos);
        } else {
          s4 = s5;
        }
        if (s4 !== peg$FAILED) {
          s5 = peg$parse_();
          s6 = peg$parseLineEnd();
          if (s6 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f2(s4);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      s1 = peg$parse_();
      if (input.substr(peg$currPos, 8) === peg$c2) {
        s2 = peg$c2;
        peg$currPos += 8;
      } else {
        s2 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e3);
        }
      }
      if (s2 !== peg$FAILED) {
        s3 = peg$parse__();
        if (s3 !== peg$FAILED) {
          s4 = peg$currPos;
          s5 = [];
          s6 = input.charAt(peg$currPos);
          if (peg$r0.test(s6)) {
            peg$currPos++;
          } else {
            s6 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e2);
            }
          }
          if (s6 !== peg$FAILED) {
            while (s6 !== peg$FAILED) {
              s5.push(s6);
              s6 = input.charAt(peg$currPos);
              if (peg$r0.test(s6)) {
                peg$currPos++;
              } else {
                s6 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e2);
                }
              }
            }
          } else {
            s5 = peg$FAILED;
          }
          if (s5 !== peg$FAILED) {
            s4 = input.substring(s4, peg$currPos);
          } else {
            s4 = s5;
          }
          if (s4 !== peg$FAILED) {
            s5 = peg$parse_();
            s6 = peg$parseLineEnd();
            if (s6 !== peg$FAILED) {
              peg$savedPos = s0;
              s0 = peg$f3(s4);
            } else {
              peg$currPos = s0;
              s0 = peg$FAILED;
            }
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
      if (s0 === peg$FAILED) {
        s0 = peg$currPos;
        s1 = peg$parse_();
        if (input.substr(peg$currPos, 5) === peg$c3) {
          s2 = peg$c3;
          peg$currPos += 5;
        } else {
          s2 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e4);
          }
        }
        if (s2 !== peg$FAILED) {
          s3 = peg$parse__();
          if (s3 !== peg$FAILED) {
            s4 = peg$currPos;
            s5 = [];
            s6 = input.charAt(peg$currPos);
            if (peg$r0.test(s6)) {
              peg$currPos++;
            } else {
              s6 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e2);
              }
            }
            if (s6 !== peg$FAILED) {
              while (s6 !== peg$FAILED) {
                s5.push(s6);
                s6 = input.charAt(peg$currPos);
                if (peg$r0.test(s6)) {
                  peg$currPos++;
                } else {
                  s6 = peg$FAILED;
                  if (peg$silentFails === 0) {
                    peg$fail(peg$e2);
                  }
                }
              }
            } else {
              s5 = peg$FAILED;
            }
            if (s5 !== peg$FAILED) {
              s4 = input.substring(s4, peg$currPos);
            } else {
              s4 = s5;
            }
            if (s4 !== peg$FAILED) {
              s5 = peg$parse_();
              s6 = peg$parseLineEnd();
              if (s6 !== peg$FAILED) {
                peg$savedPos = s0;
                s0 = peg$f4(s4);
              } else {
                peg$currPos = s0;
                s0 = peg$FAILED;
              }
            } else {
              peg$currPos = s0;
              s0 = peg$FAILED;
            }
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      }
    }
    return s0;
  }
  function peg$parseExtDirective() {
    var s0, s1, s2, s3, s4, s5, s6, s7;
    s0 = peg$currPos;
    peg$savedPos = peg$currPos;
    s1 = peg$f5();
    if (s1) {
      s1 = void 0;
    } else {
      s1 = peg$FAILED;
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$parse_();
      if (input.substr(peg$currPos, 6) === peg$c4) {
        s3 = peg$c4;
        peg$currPos += 6;
      } else {
        s3 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e5);
        }
      }
      if (s3 === peg$FAILED) {
        if (input.substr(peg$currPos, 8) === peg$c5) {
          s3 = peg$c5;
          peg$currPos += 8;
        } else {
          s3 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e6);
          }
        }
      }
      if (s3 !== peg$FAILED) {
        s4 = peg$parse__();
        if (s4 !== peg$FAILED) {
          s5 = peg$currPos;
          s6 = [];
          s7 = input.charAt(peg$currPos);
          if (peg$r0.test(s7)) {
            peg$currPos++;
          } else {
            s7 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e2);
            }
          }
          if (s7 !== peg$FAILED) {
            while (s7 !== peg$FAILED) {
              s6.push(s7);
              s7 = input.charAt(peg$currPos);
              if (peg$r0.test(s7)) {
                peg$currPos++;
              } else {
                s7 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e2);
                }
              }
            }
          } else {
            s6 = peg$FAILED;
          }
          if (s6 !== peg$FAILED) {
            s5 = input.substring(s5, peg$currPos);
          } else {
            s5 = s6;
          }
          if (s5 !== peg$FAILED) {
            s6 = peg$parse_();
            s7 = peg$parseLineEnd();
            if (s7 !== peg$FAILED) {
              peg$savedPos = s0;
              s0 = peg$f6(s3, s5);
            } else {
              peg$currPos = s0;
              s0 = peg$FAILED;
            }
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseExtFrontmatter() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9;
    s0 = peg$currPos;
    peg$savedPos = peg$currPos;
    s1 = peg$f7();
    if (s1) {
      s1 = void 0;
    } else {
      s1 = peg$FAILED;
    }
    if (s1 !== peg$FAILED) {
      if (input.substr(peg$currPos, 3) === peg$c6) {
        s2 = peg$c6;
        peg$currPos += 3;
      } else {
        s2 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e7);
        }
      }
      if (s2 !== peg$FAILED) {
        s3 = peg$parseNL();
        if (s3 !== peg$FAILED) {
          s4 = [];
          s5 = peg$currPos;
          s6 = peg$currPos;
          peg$silentFails++;
          s7 = peg$currPos;
          if (input.substr(peg$currPos, 3) === peg$c6) {
            s8 = peg$c6;
            peg$currPos += 3;
          } else {
            s8 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e7);
            }
          }
          if (s8 !== peg$FAILED) {
            s9 = peg$parseNL();
            if (s9 !== peg$FAILED) {
              s8 = [s8, s9];
              s7 = s8;
            } else {
              peg$currPos = s7;
              s7 = peg$FAILED;
            }
          } else {
            peg$currPos = s7;
            s7 = peg$FAILED;
          }
          peg$silentFails--;
          if (s7 === peg$FAILED) {
            s6 = void 0;
          } else {
            peg$currPos = s6;
            s6 = peg$FAILED;
          }
          if (s6 !== peg$FAILED) {
            s7 = peg$currPos;
            s8 = [];
            s9 = input.charAt(peg$currPos);
            if (peg$r0.test(s9)) {
              peg$currPos++;
            } else {
              s9 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e2);
              }
            }
            while (s9 !== peg$FAILED) {
              s8.push(s9);
              s9 = input.charAt(peg$currPos);
              if (peg$r0.test(s9)) {
                peg$currPos++;
              } else {
                s9 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e2);
                }
              }
            }
            s7 = input.substring(s7, peg$currPos);
            s8 = peg$parseNL();
            if (s8 !== peg$FAILED) {
              peg$savedPos = s5;
              s5 = peg$f8(s7);
            } else {
              peg$currPos = s5;
              s5 = peg$FAILED;
            }
          } else {
            peg$currPos = s5;
            s5 = peg$FAILED;
          }
          while (s5 !== peg$FAILED) {
            s4.push(s5);
            s5 = peg$currPos;
            s6 = peg$currPos;
            peg$silentFails++;
            s7 = peg$currPos;
            if (input.substr(peg$currPos, 3) === peg$c6) {
              s8 = peg$c6;
              peg$currPos += 3;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e7);
              }
            }
            if (s8 !== peg$FAILED) {
              s9 = peg$parseNL();
              if (s9 !== peg$FAILED) {
                s8 = [s8, s9];
                s7 = s8;
              } else {
                peg$currPos = s7;
                s7 = peg$FAILED;
              }
            } else {
              peg$currPos = s7;
              s7 = peg$FAILED;
            }
            peg$silentFails--;
            if (s7 === peg$FAILED) {
              s6 = void 0;
            } else {
              peg$currPos = s6;
              s6 = peg$FAILED;
            }
            if (s6 !== peg$FAILED) {
              s7 = peg$currPos;
              s8 = [];
              s9 = input.charAt(peg$currPos);
              if (peg$r0.test(s9)) {
                peg$currPos++;
              } else {
                s9 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e2);
                }
              }
              while (s9 !== peg$FAILED) {
                s8.push(s9);
                s9 = input.charAt(peg$currPos);
                if (peg$r0.test(s9)) {
                  peg$currPos++;
                } else {
                  s9 = peg$FAILED;
                  if (peg$silentFails === 0) {
                    peg$fail(peg$e2);
                  }
                }
              }
              s7 = input.substring(s7, peg$currPos);
              s8 = peg$parseNL();
              if (s8 !== peg$FAILED) {
                peg$savedPos = s5;
                s5 = peg$f8(s7);
              } else {
                peg$currPos = s5;
                s5 = peg$FAILED;
              }
            } else {
              peg$currPos = s5;
              s5 = peg$FAILED;
            }
          }
          if (input.substr(peg$currPos, 3) === peg$c6) {
            s5 = peg$c6;
            peg$currPos += 3;
          } else {
            s5 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e7);
            }
          }
          if (s5 !== peg$FAILED) {
            s6 = peg$parseNL();
            if (s6 !== peg$FAILED) {
              peg$savedPos = s0;
              s0 = peg$f9(s4);
            } else {
              peg$currPos = s0;
              s0 = peg$FAILED;
            }
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseBody() {
    var s0, s1, s2;
    s0 = peg$currPos;
    s1 = [];
    s2 = peg$parseNoteDirective();
    if (s2 === peg$FAILED) {
      s2 = peg$parseLegendBlock();
      if (s2 === peg$FAILED) {
        s2 = peg$parseSectionHeader();
        if (s2 === peg$FAILED) {
          s2 = peg$parseEntry();
          if (s2 === peg$FAILED) {
            s2 = peg$parseBlankLine();
          }
        }
      }
    }
    while (s2 !== peg$FAILED) {
      s1.push(s2);
      s2 = peg$parseNoteDirective();
      if (s2 === peg$FAILED) {
        s2 = peg$parseLegendBlock();
        if (s2 === peg$FAILED) {
          s2 = peg$parseSectionHeader();
          if (s2 === peg$FAILED) {
            s2 = peg$parseEntry();
            if (s2 === peg$FAILED) {
              s2 = peg$parseBlankLine();
            }
          }
        }
      }
    }
    peg$savedPos = s0;
    s1 = peg$f10(s1);
    s0 = s1;
    return s0;
  }
  function peg$parseSectionHeader() {
    var s0, s1, s2, s3, s4, s5, s6;
    s0 = peg$currPos;
    s1 = peg$parse_();
    if (input.substr(peg$currPos, 7) === peg$c7) {
      s2 = peg$c7;
      peg$currPos += 7;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e8);
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$parse__();
      if (s3 !== peg$FAILED) {
        s4 = peg$currPos;
        s5 = [];
        s6 = input.charAt(peg$currPos);
        if (peg$r0.test(s6)) {
          peg$currPos++;
        } else {
          s6 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e2);
          }
        }
        if (s6 !== peg$FAILED) {
          while (s6 !== peg$FAILED) {
            s5.push(s6);
            s6 = input.charAt(peg$currPos);
            if (peg$r0.test(s6)) {
              peg$currPos++;
            } else {
              s6 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e2);
              }
            }
          }
        } else {
          s5 = peg$FAILED;
        }
        if (s5 !== peg$FAILED) {
          s4 = input.substring(s4, peg$currPos);
        } else {
          s4 = s5;
        }
        if (s4 !== peg$FAILED) {
          s5 = peg$parseLineEnd();
          if (s5 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f11(s4);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseEntry() {
    var s0, s1, s2, s3, s4, s5;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = peg$parseExtEntry();
    if (s2 === peg$FAILED) {
      s2 = peg$parseMermaidEntry();
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$parse_();
      s4 = peg$parseComment();
      if (s4 === peg$FAILED) {
        s4 = null;
      }
      s5 = peg$parseLineEnd();
      if (s5 !== peg$FAILED) {
        peg$savedPos = s0;
        s0 = peg$f12(s2);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseMermaidEntry() {
    var s0, s1, s2, s3, s4, s5;
    s0 = peg$currPos;
    s1 = peg$parseDateToken();
    if (s1 !== peg$FAILED) {
      s2 = peg$parse_();
      if (input.charCodeAt(peg$currPos) === 58) {
        s3 = peg$c8;
        peg$currPos++;
      } else {
        s3 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e9);
        }
      }
      if (s3 !== peg$FAILED) {
        s4 = peg$parse_();
        s5 = peg$parseMermaidLabel();
        if (s5 !== peg$FAILED) {
          peg$savedPos = s0;
          s0 = peg$f13(s1, s5);
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseMermaidLabel() {
    var s0, s1, s2, s3;
    s0 = peg$currPos;
    s1 = peg$currPos;
    s2 = [];
    s3 = input.charAt(peg$currPos);
    if (peg$r0.test(s3)) {
      peg$currPos++;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e2);
      }
    }
    if (s3 !== peg$FAILED) {
      while (s3 !== peg$FAILED) {
        s2.push(s3);
        s3 = input.charAt(peg$currPos);
        if (peg$r0.test(s3)) {
          peg$currPos++;
        } else {
          s3 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e2);
          }
        }
      }
    } else {
      s2 = peg$FAILED;
    }
    if (s2 !== peg$FAILED) {
      s1 = input.substring(s1, peg$currPos);
    } else {
      s1 = s2;
    }
    if (s1 !== peg$FAILED) {
      peg$savedPos = s0;
      s1 = peg$f14(s1);
    }
    s0 = s1;
    return s0;
  }
  function peg$parseExtEntry() {
    var s0, s1, s2;
    s0 = peg$currPos;
    peg$savedPos = peg$currPos;
    s1 = peg$f15();
    if (s1) {
      s1 = void 0;
    } else {
      s1 = peg$FAILED;
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$parseExtRangeActivity();
      if (s2 === peg$FAILED) {
        s2 = peg$parseExtPointEntry();
      }
      if (s2 !== peg$FAILED) {
        peg$savedPos = s0;
        s0 = peg$f16(s2);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseExtRangeActivity() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9, s10, s11, s12, s13, s14, s15, s16;
    s0 = peg$currPos;
    s1 = peg$parseDateToken();
    if (s1 !== peg$FAILED) {
      s2 = peg$parse_();
      if (input.substr(peg$currPos, 2) === peg$c9) {
        s3 = peg$c9;
        peg$currPos += 2;
      } else {
        s3 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e10);
        }
      }
      if (s3 !== peg$FAILED) {
        s4 = peg$parse_();
        s5 = peg$parseDateToken();
        if (s5 !== peg$FAILED) {
          s6 = peg$parse_();
          if (input.charCodeAt(peg$currPos) === 58) {
            s7 = peg$c8;
            peg$currPos++;
          } else {
            s7 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e9);
            }
          }
          if (s7 !== peg$FAILED) {
            s8 = peg$parse_();
            s9 = peg$parseExtLabel();
            if (s9 !== peg$FAILED) {
              s10 = peg$currPos;
              s11 = peg$parse_();
              if (input.charCodeAt(peg$currPos) === 58) {
                s12 = peg$c8;
                peg$currPos++;
              } else {
                s12 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e9);
                }
              }
              if (s12 !== peg$FAILED) {
                s13 = peg$parse_();
                s14 = peg$parseExtStatus();
                if (s14 !== peg$FAILED) {
                  peg$savedPos = s10;
                  s10 = peg$f17(s1, s5, s9, s14);
                } else {
                  peg$currPos = s10;
                  s10 = peg$FAILED;
                }
              } else {
                peg$currPos = s10;
                s10 = peg$FAILED;
              }
              if (s10 === peg$FAILED) {
                s10 = null;
              }
              s11 = peg$currPos;
              s12 = peg$parse_();
              if (input.charCodeAt(peg$currPos) === 64) {
                s13 = peg$c10;
                peg$currPos++;
              } else {
                s13 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e11);
                }
              }
              if (s13 !== peg$FAILED) {
                s14 = peg$parseIdentifier();
                if (s14 !== peg$FAILED) {
                  peg$savedPos = s11;
                  s11 = peg$f18(s1, s5, s9, s10, s14);
                } else {
                  peg$currPos = s11;
                  s11 = peg$FAILED;
                }
              } else {
                peg$currPos = s11;
                s11 = peg$FAILED;
              }
              if (s11 === peg$FAILED) {
                s11 = null;
              }
              s12 = peg$currPos;
              s13 = peg$parse_();
              if (input.charCodeAt(peg$currPos) === 124) {
                s14 = peg$c11;
                peg$currPos++;
              } else {
                s14 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e12);
                }
              }
              if (s14 !== peg$FAILED) {
                s15 = peg$parse_();
                s16 = peg$parseDescText();
                if (s16 !== peg$FAILED) {
                  peg$savedPos = s12;
                  s12 = peg$f19(s1, s5, s9, s10, s11, s16);
                } else {
                  peg$currPos = s12;
                  s12 = peg$FAILED;
                }
              } else {
                peg$currPos = s12;
                s12 = peg$FAILED;
              }
              if (s12 === peg$FAILED) {
                s12 = null;
              }
              peg$savedPos = s0;
              s0 = peg$f20(s1, s5, s9, s10, s11, s12);
            } else {
              peg$currPos = s0;
              s0 = peg$FAILED;
            }
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseExtPointEntry() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9, s10, s11, s12, s13, s14;
    s0 = peg$currPos;
    s1 = peg$parseDateToken();
    if (s1 !== peg$FAILED) {
      s2 = peg$parse_();
      if (input.charCodeAt(peg$currPos) === 58) {
        s3 = peg$c8;
        peg$currPos++;
      } else {
        s3 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e9);
        }
      }
      if (s3 !== peg$FAILED) {
        s4 = peg$parse_();
        s5 = peg$parseExtLabel();
        if (s5 !== peg$FAILED) {
          if (input.charCodeAt(peg$currPos) === 58) {
            s6 = peg$c8;
            peg$currPos++;
          } else {
            s6 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e9);
            }
          }
          if (s6 !== peg$FAILED) {
            s7 = peg$parse_();
            s8 = peg$parseExtEntryKind();
            if (s8 !== peg$FAILED) {
              s9 = peg$currPos;
              s10 = peg$parse_();
              if (input.charCodeAt(peg$currPos) === 64) {
                s11 = peg$c10;
                peg$currPos++;
              } else {
                s11 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e11);
                }
              }
              if (s11 !== peg$FAILED) {
                s12 = peg$parseIdentifier();
                if (s12 !== peg$FAILED) {
                  peg$savedPos = s9;
                  s9 = peg$f21(s1, s5, s8, s12);
                } else {
                  peg$currPos = s9;
                  s9 = peg$FAILED;
                }
              } else {
                peg$currPos = s9;
                s9 = peg$FAILED;
              }
              if (s9 === peg$FAILED) {
                s9 = null;
              }
              s10 = peg$currPos;
              s11 = peg$parse_();
              if (input.charCodeAt(peg$currPos) === 124) {
                s12 = peg$c11;
                peg$currPos++;
              } else {
                s12 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e12);
                }
              }
              if (s12 !== peg$FAILED) {
                s13 = peg$parse_();
                s14 = peg$parseDescText();
                if (s14 !== peg$FAILED) {
                  peg$savedPos = s10;
                  s10 = peg$f22(s1, s5, s8, s9, s14);
                } else {
                  peg$currPos = s10;
                  s10 = peg$FAILED;
                }
              } else {
                peg$currPos = s10;
                s10 = peg$FAILED;
              }
              if (s10 === peg$FAILED) {
                s10 = null;
              }
              peg$savedPos = s0;
              s0 = peg$f23(s1, s5, s8, s9, s10);
            } else {
              peg$currPos = s0;
              s0 = peg$FAILED;
            }
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseExtLabel() {
    var s0, s1, s2, s3;
    s0 = peg$currPos;
    s1 = peg$currPos;
    s2 = [];
    s3 = input.charAt(peg$currPos);
    if (peg$r1.test(s3)) {
      peg$currPos++;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e13);
      }
    }
    if (s3 !== peg$FAILED) {
      while (s3 !== peg$FAILED) {
        s2.push(s3);
        s3 = input.charAt(peg$currPos);
        if (peg$r1.test(s3)) {
          peg$currPos++;
        } else {
          s3 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e13);
          }
        }
      }
    } else {
      s2 = peg$FAILED;
    }
    if (s2 !== peg$FAILED) {
      s1 = input.substring(s1, peg$currPos);
    } else {
      s1 = s2;
    }
    if (s1 !== peg$FAILED) {
      peg$savedPos = s0;
      s1 = peg$f24(s1);
    }
    s0 = s1;
    return s0;
  }
  function peg$parseDescText() {
    var s0, s1, s2, s3;
    s0 = peg$currPos;
    s1 = peg$currPos;
    s2 = [];
    s3 = input.charAt(peg$currPos);
    if (peg$r0.test(s3)) {
      peg$currPos++;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e2);
      }
    }
    if (s3 !== peg$FAILED) {
      while (s3 !== peg$FAILED) {
        s2.push(s3);
        s3 = input.charAt(peg$currPos);
        if (peg$r0.test(s3)) {
          peg$currPos++;
        } else {
          s3 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e2);
          }
        }
      }
    } else {
      s2 = peg$FAILED;
    }
    if (s2 !== peg$FAILED) {
      s1 = input.substring(s1, peg$currPos);
    } else {
      s1 = s2;
    }
    if (s1 !== peg$FAILED) {
      peg$savedPos = s0;
      s1 = peg$f25(s1);
    }
    s0 = s1;
    return s0;
  }
  function peg$parseExtEntryKind() {
    var s0;
    if (input.substr(peg$currPos, 9) === peg$c12) {
      s0 = peg$c12;
      peg$currPos += 9;
    } else {
      s0 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e14);
      }
    }
    if (s0 === peg$FAILED) {
      s0 = peg$parseExtStatus();
    }
    return s0;
  }
  function peg$parseExtStatus() {
    var s0;
    if (input.substr(peg$currPos, 6) === peg$c13) {
      s0 = peg$c13;
      peg$currPos += 6;
    } else {
      s0 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e15);
      }
    }
    if (s0 === peg$FAILED) {
      if (input.substr(peg$currPos, 4) === peg$c14) {
        s0 = peg$c14;
        peg$currPos += 4;
      } else {
        s0 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e16);
        }
      }
      if (s0 === peg$FAILED) {
        if (input.substr(peg$currPos, 7) === peg$c15) {
          s0 = peg$c15;
          peg$currPos += 7;
        } else {
          s0 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e17);
          }
        }
        if (s0 === peg$FAILED) {
          if (input.substr(peg$currPos, 7) === peg$c16) {
            s0 = peg$c16;
            peg$currPos += 7;
          } else {
            s0 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e18);
            }
          }
        }
      }
    }
    return s0;
  }
  function peg$parseDateToken() {
    var s0, s1, s2;
    s0 = peg$currPos;
    s1 = [];
    s2 = input.charAt(peg$currPos);
    if (peg$r2.test(s2)) {
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e19);
      }
    }
    if (s2 !== peg$FAILED) {
      while (s2 !== peg$FAILED) {
        s1.push(s2);
        s2 = input.charAt(peg$currPos);
        if (peg$r2.test(s2)) {
          peg$currPos++;
        } else {
          s2 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e19);
          }
        }
      }
    } else {
      s1 = peg$FAILED;
    }
    if (s1 !== peg$FAILED) {
      s0 = input.substring(s0, peg$currPos);
    } else {
      s0 = s1;
    }
    return s0;
  }
  function peg$parseIdentifier() {
    var s0, s1, s2, s3, s4;
    s0 = peg$currPos;
    s1 = peg$currPos;
    s2 = input.charAt(peg$currPos);
    if (peg$r3.test(s2)) {
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e20);
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = [];
      s4 = input.charAt(peg$currPos);
      if (peg$r4.test(s4)) {
        peg$currPos++;
      } else {
        s4 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e21);
        }
      }
      while (s4 !== peg$FAILED) {
        s3.push(s4);
        s4 = input.charAt(peg$currPos);
        if (peg$r4.test(s4)) {
          peg$currPos++;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e21);
          }
        }
      }
      s2 = [s2, s3];
      s1 = s2;
    } else {
      peg$currPos = s1;
      s1 = peg$FAILED;
    }
    if (s1 !== peg$FAILED) {
      s0 = input.substring(s0, peg$currPos);
    } else {
      s0 = s1;
    }
    return s0;
  }
  function peg$parseNoteDirective() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9, s10, s11, s12, s13, s14;
    s0 = peg$currPos;
    s1 = peg$parse_();
    if (input.substr(peg$currPos, 4) === peg$c17) {
      s2 = peg$c17;
      peg$currPos += 4;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e22);
      }
    }
    if (s2 === peg$FAILED) {
      if (input.substr(peg$currPos, 7) === peg$c18) {
        s2 = peg$c18;
        peg$currPos += 7;
      } else {
        s2 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e23);
        }
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$parse__();
      if (s3 !== peg$FAILED) {
        if (input.charCodeAt(peg$currPos) === 34) {
          s4 = peg$c19;
          peg$currPos++;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e24);
          }
        }
        if (s4 !== peg$FAILED) {
          s5 = peg$currPos;
          s6 = [];
          s7 = input.charAt(peg$currPos);
          if (peg$r5.test(s7)) {
            peg$currPos++;
          } else {
            s7 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e25);
            }
          }
          while (s7 !== peg$FAILED) {
            s6.push(s7);
            s7 = input.charAt(peg$currPos);
            if (peg$r5.test(s7)) {
              peg$currPos++;
            } else {
              s7 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e25);
              }
            }
          }
          s5 = input.substring(s5, peg$currPos);
          if (input.charCodeAt(peg$currPos) === 34) {
            s6 = peg$c19;
            peg$currPos++;
          } else {
            s6 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e24);
            }
          }
          if (s6 !== peg$FAILED) {
            s7 = peg$parse__();
            if (s7 !== peg$FAILED) {
              if (input.substr(peg$currPos, 2) === peg$c20) {
                s8 = peg$c20;
                peg$currPos += 2;
              } else {
                s8 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e26);
                }
              }
              if (s8 !== peg$FAILED) {
                s9 = peg$parse__();
                if (s9 !== peg$FAILED) {
                  s10 = peg$currPos;
                  if (input.charCodeAt(peg$currPos) === 34) {
                    s11 = peg$c19;
                    peg$currPos++;
                  } else {
                    s11 = peg$FAILED;
                    if (peg$silentFails === 0) {
                      peg$fail(peg$e24);
                    }
                  }
                  if (s11 !== peg$FAILED) {
                    s12 = peg$currPos;
                    s13 = [];
                    s14 = input.charAt(peg$currPos);
                    if (peg$r5.test(s14)) {
                      peg$currPos++;
                    } else {
                      s14 = peg$FAILED;
                      if (peg$silentFails === 0) {
                        peg$fail(peg$e25);
                      }
                    }
                    while (s14 !== peg$FAILED) {
                      s13.push(s14);
                      s14 = input.charAt(peg$currPos);
                      if (peg$r5.test(s14)) {
                        peg$currPos++;
                      } else {
                        s14 = peg$FAILED;
                        if (peg$silentFails === 0) {
                          peg$fail(peg$e25);
                        }
                      }
                    }
                    s12 = input.substring(s12, peg$currPos);
                    if (input.charCodeAt(peg$currPos) === 34) {
                      s13 = peg$c19;
                      peg$currPos++;
                    } else {
                      s13 = peg$FAILED;
                      if (peg$silentFails === 0) {
                        peg$fail(peg$e24);
                      }
                    }
                    if (s13 !== peg$FAILED) {
                      peg$savedPos = s10;
                      s10 = peg$f26(s2, s5, s12);
                    } else {
                      peg$currPos = s10;
                      s10 = peg$FAILED;
                    }
                  } else {
                    peg$currPos = s10;
                    s10 = peg$FAILED;
                  }
                  if (s10 === peg$FAILED) {
                    s10 = peg$currPos;
                    s11 = [];
                    s12 = input.charAt(peg$currPos);
                    if (peg$r2.test(s12)) {
                      peg$currPos++;
                    } else {
                      s12 = peg$FAILED;
                      if (peg$silentFails === 0) {
                        peg$fail(peg$e19);
                      }
                    }
                    if (s12 !== peg$FAILED) {
                      while (s12 !== peg$FAILED) {
                        s11.push(s12);
                        s12 = input.charAt(peg$currPos);
                        if (peg$r2.test(s12)) {
                          peg$currPos++;
                        } else {
                          s12 = peg$FAILED;
                          if (peg$silentFails === 0) {
                            peg$fail(peg$e19);
                          }
                        }
                      }
                    } else {
                      s11 = peg$FAILED;
                    }
                    if (s11 !== peg$FAILED) {
                      s10 = input.substring(s10, peg$currPos);
                    } else {
                      s10 = s11;
                    }
                  }
                  if (s10 !== peg$FAILED) {
                    s11 = peg$parseNoteOffset();
                    if (s11 === peg$FAILED) {
                      s11 = null;
                    }
                    s12 = peg$parse_();
                    s13 = peg$parseComment();
                    if (s13 === peg$FAILED) {
                      s13 = null;
                    }
                    s14 = peg$parseLineEnd();
                    if (s14 !== peg$FAILED) {
                      peg$savedPos = s0;
                      s0 = peg$f27(s2, s5, s10, s11);
                    } else {
                      peg$currPos = s0;
                      s0 = peg$FAILED;
                    }
                  } else {
                    peg$currPos = s0;
                    s0 = peg$FAILED;
                  }
                } else {
                  peg$currPos = s0;
                  s0 = peg$FAILED;
                }
              } else {
                peg$currPos = s0;
                s0 = peg$FAILED;
              }
            } else {
              peg$currPos = s0;
              s0 = peg$FAILED;
            }
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseNoteOffset() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8;
    s0 = peg$currPos;
    s1 = peg$parse__();
    if (s1 !== peg$FAILED) {
      if (input.substr(peg$currPos, 6) === peg$c21) {
        s2 = peg$c21;
        peg$currPos += 6;
      } else {
        s2 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e27);
        }
      }
      if (s2 !== peg$FAILED) {
        s3 = peg$parse__();
        if (s3 !== peg$FAILED) {
          s4 = peg$parseSignedInt();
          if (s4 !== peg$FAILED) {
            s5 = peg$parse_();
            if (input.charCodeAt(peg$currPos) === 44) {
              s6 = peg$c22;
              peg$currPos++;
            } else {
              s6 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e28);
              }
            }
            if (s6 !== peg$FAILED) {
              s7 = peg$parse_();
              s8 = peg$parseSignedInt();
              if (s8 !== peg$FAILED) {
                peg$savedPos = s0;
                s0 = peg$f28(s4, s8);
              } else {
                peg$currPos = s0;
                s0 = peg$FAILED;
              }
            } else {
              peg$currPos = s0;
              s0 = peg$FAILED;
            }
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseSignedInt() {
    var s0, s1, s2, s3, s4;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 45) {
      s1 = peg$c23;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e29);
      }
    }
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    s2 = peg$currPos;
    s3 = [];
    s4 = input.charAt(peg$currPos);
    if (peg$r6.test(s4)) {
      peg$currPos++;
    } else {
      s4 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e30);
      }
    }
    if (s4 !== peg$FAILED) {
      while (s4 !== peg$FAILED) {
        s3.push(s4);
        s4 = input.charAt(peg$currPos);
        if (peg$r6.test(s4)) {
          peg$currPos++;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e30);
          }
        }
      }
    } else {
      s3 = peg$FAILED;
    }
    if (s3 !== peg$FAILED) {
      s2 = input.substring(s2, peg$currPos);
    } else {
      s2 = s3;
    }
    if (s2 !== peg$FAILED) {
      peg$savedPos = s0;
      s0 = peg$f29(s1, s2);
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseLegendBlock() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9, s10, s11, s12;
    s0 = peg$currPos;
    s1 = peg$parse_();
    if (input.substr(peg$currPos, 6) === peg$c24) {
      s2 = peg$c24;
      peg$currPos += 6;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e31);
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$parse__();
      if (s3 !== peg$FAILED) {
        s4 = peg$parseLegendCornerToken();
        if (s4 !== peg$FAILED) {
          s5 = peg$currPos;
          s6 = peg$parse_();
          if (input.charCodeAt(peg$currPos) === 34) {
            s7 = peg$c19;
            peg$currPos++;
          } else {
            s7 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e24);
            }
          }
          if (s7 !== peg$FAILED) {
            s8 = peg$currPos;
            s9 = [];
            s10 = input.charAt(peg$currPos);
            if (peg$r5.test(s10)) {
              peg$currPos++;
            } else {
              s10 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e25);
              }
            }
            while (s10 !== peg$FAILED) {
              s9.push(s10);
              s10 = input.charAt(peg$currPos);
              if (peg$r5.test(s10)) {
                peg$currPos++;
              } else {
                s10 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e25);
                }
              }
            }
            s8 = input.substring(s8, peg$currPos);
            if (input.charCodeAt(peg$currPos) === 34) {
              s9 = peg$c19;
              peg$currPos++;
            } else {
              s9 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e24);
              }
            }
            if (s9 !== peg$FAILED) {
              peg$savedPos = s5;
              s5 = peg$f30(s4, s8);
            } else {
              peg$currPos = s5;
              s5 = peg$FAILED;
            }
          } else {
            peg$currPos = s5;
            s5 = peg$FAILED;
          }
          if (s5 === peg$FAILED) {
            s5 = null;
          }
          s6 = peg$parse_();
          s7 = peg$parseLineEnd();
          if (s7 !== peg$FAILED) {
            s8 = [];
            s9 = peg$parseLegendEntryLine();
            while (s9 !== peg$FAILED) {
              s8.push(s9);
              s9 = peg$parseLegendEntryLine();
            }
            s9 = peg$parse_();
            if (input.substr(peg$currPos, 3) === peg$c25) {
              s10 = peg$c25;
              peg$currPos += 3;
            } else {
              s10 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e32);
              }
            }
            if (s10 !== peg$FAILED) {
              s11 = peg$parse_();
              s12 = peg$parseLineEnd();
              if (s12 === peg$FAILED) {
                s12 = null;
              }
              peg$savedPos = s0;
              s0 = peg$f31(s4, s5, s8);
            } else {
              peg$currPos = s0;
              s0 = peg$FAILED;
            }
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseLegendCornerToken() {
    var s0;
    if (input.substr(peg$currPos, 12) === peg$c26) {
      s0 = peg$c26;
      peg$currPos += 12;
    } else {
      s0 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e33);
      }
    }
    if (s0 === peg$FAILED) {
      if (input.substr(peg$currPos, 11) === peg$c27) {
        s0 = peg$c27;
        peg$currPos += 11;
      } else {
        s0 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e34);
        }
      }
      if (s0 === peg$FAILED) {
        if (input.substr(peg$currPos, 9) === peg$c28) {
          s0 = peg$c28;
          peg$currPos += 9;
        } else {
          s0 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e35);
          }
        }
        if (s0 === peg$FAILED) {
          if (input.substr(peg$currPos, 8) === peg$c29) {
            s0 = peg$c29;
            peg$currPos += 8;
          } else {
            s0 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e36);
            }
          }
        }
      }
    }
    return s0;
  }
  function peg$parseLegendEntryLine() {
    var s0, s1, s2, s3, s4, s5, s6, s7;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = peg$currPos;
    peg$silentFails++;
    s3 = peg$currPos;
    if (input.substr(peg$currPos, 3) === peg$c25) {
      s4 = peg$c25;
      peg$currPos += 3;
    } else {
      s4 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e32);
      }
    }
    if (s4 !== peg$FAILED) {
      s5 = peg$currPos;
      peg$silentFails++;
      s6 = input.charAt(peg$currPos);
      if (peg$r7.test(s6)) {
        peg$currPos++;
      } else {
        s6 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e37);
        }
      }
      peg$silentFails--;
      if (s6 === peg$FAILED) {
        s5 = void 0;
      } else {
        peg$currPos = s5;
        s5 = peg$FAILED;
      }
      if (s5 !== peg$FAILED) {
        s4 = [s4, s5];
        s3 = s4;
      } else {
        peg$currPos = s3;
        s3 = peg$FAILED;
      }
    } else {
      peg$currPos = s3;
      s3 = peg$FAILED;
    }
    peg$silentFails--;
    if (s3 === peg$FAILED) {
      s2 = void 0;
    } else {
      peg$currPos = s2;
      s2 = peg$FAILED;
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$currPos;
      s4 = [];
      s5 = input.charAt(peg$currPos);
      if (peg$r8.test(s5)) {
        peg$currPos++;
      } else {
        s5 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e38);
        }
      }
      if (s5 !== peg$FAILED) {
        while (s5 !== peg$FAILED) {
          s4.push(s5);
          s5 = input.charAt(peg$currPos);
          if (peg$r8.test(s5)) {
            peg$currPos++;
          } else {
            s5 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e38);
            }
          }
        }
      } else {
        s4 = peg$FAILED;
      }
      if (s4 !== peg$FAILED) {
        s3 = input.substring(s3, peg$currPos);
      } else {
        s3 = s4;
      }
      if (s3 !== peg$FAILED) {
        if (input.charCodeAt(peg$currPos) === 58) {
          s4 = peg$c8;
          peg$currPos++;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e9);
          }
        }
        if (s4 !== peg$FAILED) {
          s5 = peg$currPos;
          s6 = [];
          s7 = input.charAt(peg$currPos);
          if (peg$r0.test(s7)) {
            peg$currPos++;
          } else {
            s7 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e2);
            }
          }
          if (s7 !== peg$FAILED) {
            while (s7 !== peg$FAILED) {
              s6.push(s7);
              s7 = input.charAt(peg$currPos);
              if (peg$r0.test(s7)) {
                peg$currPos++;
              } else {
                s7 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e2);
                }
              }
            }
          } else {
            s6 = peg$FAILED;
          }
          if (s6 !== peg$FAILED) {
            s5 = input.substring(s5, peg$currPos);
          } else {
            s5 = s6;
          }
          if (s5 !== peg$FAILED) {
            s6 = peg$parseLineEnd();
            if (s6 !== peg$FAILED) {
              peg$savedPos = s0;
              s0 = peg$f32(s3, s5);
            } else {
              peg$currPos = s0;
              s0 = peg$FAILED;
            }
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseBlankLine() {
    var s0, s1, s2, s3;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = peg$parseComment();
    if (s2 === peg$FAILED) {
      s2 = null;
    }
    s3 = peg$parseNL();
    if (s3 !== peg$FAILED) {
      peg$savedPos = s0;
      s0 = peg$f33();
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseComment() {
    var s0, s1, s2, s3;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 2) === peg$c30) {
      s1 = peg$c30;
      peg$currPos += 2;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e39);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = [];
      s3 = input.charAt(peg$currPos);
      if (peg$r0.test(s3)) {
        peg$currPos++;
      } else {
        s3 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e2);
        }
      }
      while (s3 !== peg$FAILED) {
        s2.push(s3);
        s3 = input.charAt(peg$currPos);
        if (peg$r0.test(s3)) {
          peg$currPos++;
        } else {
          s3 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e2);
          }
        }
      }
      s1 = [s1, s2];
      s0 = s1;
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseLineEnd() {
    var s0, s1;
    s0 = peg$parseNL();
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      peg$silentFails++;
      if (input.length > peg$currPos) {
        s1 = input.charAt(peg$currPos);
        peg$currPos++;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e40);
        }
      }
      peg$silentFails--;
      if (s1 === peg$FAILED) {
        s0 = void 0;
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    }
    return s0;
  }
  function peg$parse_() {
    var s0, s1;
    s0 = [];
    s1 = input.charAt(peg$currPos);
    if (peg$r9.test(s1)) {
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e41);
      }
    }
    while (s1 !== peg$FAILED) {
      s0.push(s1);
      s1 = input.charAt(peg$currPos);
      if (peg$r9.test(s1)) {
        peg$currPos++;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e41);
        }
      }
    }
    return s0;
  }
  function peg$parse__() {
    var s0, s1;
    s0 = [];
    s1 = input.charAt(peg$currPos);
    if (peg$r9.test(s1)) {
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e41);
      }
    }
    if (s1 !== peg$FAILED) {
      while (s1 !== peg$FAILED) {
        s0.push(s1);
        s1 = input.charAt(peg$currPos);
        if (peg$r9.test(s1)) {
          peg$currPos++;
        } else {
          s1 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e41);
          }
        }
      }
    } else {
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseNL() {
    var s0;
    if (input.substr(peg$currPos, 2) === peg$c31) {
      s0 = peg$c31;
      peg$currPos += 2;
    } else {
      s0 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e42);
      }
    }
    if (s0 === peg$FAILED) {
      s0 = input.charAt(peg$currPos);
      if (peg$r10.test(s0)) {
        peg$currPos++;
      } else {
        s0 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e43);
        }
      }
    }
    return s0;
  }
  reset2();
  const ext = options.extensions !== false;
  peg$result = peg$startRuleFunction();
  if (options.peg$library) {
    return (
      /** @type {any} */
      {
        peg$result,
        peg$currPos,
        peg$FAILED,
        peg$maxFailExpected,
        peg$maxFailPos
      }
    );
  }
  if (peg$result !== peg$FAILED && peg$currPos === input.length) {
    return peg$result;
  } else {
    if (peg$result !== peg$FAILED && peg$currPos < input.length) {
      peg$fail(peg$endExpectation());
    }
    throw peg$buildStructuredError(
      peg$maxFailExpected,
      peg$maxFailPos < input.length ? input.charAt(peg$maxFailPos) : null,
      peg$maxFailPos < input.length ? peg$computeLocation(peg$maxFailPos, peg$maxFailPos + 1) : peg$computeLocation(peg$maxFailPos, peg$maxFailPos)
    );
  }
}
var timeline = {
  parseMermaid(input) {
    const raw = peg$parse2(input, { extensions: true });
    return {
      ...raw,
      overlays: raw.overlays?.length > 0 ? raw.overlays : void 0
    };
  },
  parseYaml(input) {
    return JSON.parse(input);
  },
  layout(ir, theme, options) {
    return layoutTimeline(ir, theme, options);
  }
};
var HASH_OFFSET = 2166136261;
var HASH_PRIME = 16777619;
function crossLinkMarkerId(baseId, color) {
  return `${baseId}-${colorToken(color)}`;
}
function colorToken(color) {
  const normalized = color.trim().toLowerCase();
  const hex = normalized.match(/^#([0-9a-f]+)$/);
  if (hex) return hex[1];
  let hash = HASH_OFFSET;
  for (let i = 0; i < normalized.length; i++) {
    hash ^= normalized.charCodeAt(i);
    hash = Math.imul(hash, HASH_PRIME) >>> 0;
  }
  return `c${hash.toString(36)}`;
}
function wavifyPath(points, amplitude, wavelength) {
  if (points.length < 2) {
    return points.map((p, i) => `${i === 0 ? "M" : "L"} ${p.x} ${p.y}`).join(" ");
  }
  const sampleInterval = wavelength / 4;
  const arcLen = [0];
  for (let i = 1; i < points.length; i++) {
    const a = points[i - 1], b = points[i];
    const dx = b.x - a.x, dy = b.y - a.y;
    arcLen.push(arcLen[i - 1] + Math.sqrt(dx * dx + dy * dy));
  }
  const totalLen = arcLen[arcLen.length - 1];
  if (totalLen < 1) {
    return `M ${points[0].x} ${points[0].y}`;
  }
  const sampleCount = Math.max(2, Math.ceil(totalLen / sampleInterval) + 1);
  const samples = [];
  const sampleArcLens = [];
  function polylineAt(s) {
    s = Math.max(0, Math.min(s, totalLen));
    for (let i = 1; i < points.length; i++) {
      if (arcLen[i] >= s - 1e-9) {
        const segStart = arcLen[i - 1];
        const segEnd = arcLen[i];
        const segLen = segEnd - segStart;
        const t = segLen < 1e-9 ? 0 : (s - segStart) / segLen;
        const a = points[i - 1], b = points[i];
        const pt = { x: a.x + t * (b.x - a.x), y: a.y + t * (b.y - a.y) };
        const dx2 = b.x - a.x, dy2 = b.y - a.y;
        const len2 = Math.sqrt(dx2 * dx2 + dy2 * dy2);
        const tx = len2 > 1e-9 ? dx2 / len2 : 1;
        const ty = len2 > 1e-9 ? dy2 / len2 : 0;
        return { pt, tangentX: tx, tangentY: ty };
      }
    }
    const last = points[points.length - 1], prev = points[points.length - 2];
    const dx = last.x - prev.x, dy = last.y - prev.y;
    const len = Math.sqrt(dx * dx + dy * dy);
    return { pt: last, tangentX: len > 1e-9 ? dx / len : 1, tangentY: len > 1e-9 ? dy / len : 0 };
  }
  const cornerArcLens = [];
  for (let i = 1; i < points.length - 1; i++) {
    const a = points[i - 1], b = points[i], c = points[i + 1];
    const d1x = b.x - a.x, d1y = b.y - a.y;
    const d2x = c.x - b.x, d2y = c.y - b.y;
    const len1 = Math.sqrt(d1x * d1x + d1y * d1y);
    const len2 = Math.sqrt(d2x * d2x + d2y * d2y);
    if (len1 < 1e-9 || len2 < 1e-9) continue;
    const dot = d1x / len1 * (d2x / len2) + d1y / len1 * (d2y / len2);
    if (dot < 0.7) {
      cornerArcLens.push(arcLen[i]);
    }
  }
  function cornerAmplitudeFactor(s) {
    let minDist = Infinity;
    for (const cs of cornerArcLens) {
      minDist = Math.min(minDist, Math.abs(s - cs));
    }
    if (minDist >= wavelength) return 1;
    return minDist / wavelength;
  }
  function endpointAmplitudeFactor(s) {
    const endpointDist = Math.min(s, totalLen - s);
    return Math.max(
      0,
      Math.min(1, (endpointDist - sampleInterval) / (wavelength - sampleInterval))
    );
  }
  for (let k = 0; k < sampleCount; k++) {
    const s = k / (sampleCount - 1) * totalLen;
    const { pt, tangentX, tangentY } = polylineAt(s);
    const nx = -tangentY;
    const ny = tangentX;
    const phase = 2 * Math.PI * s / wavelength;
    const cornerFactor = cornerAmplitudeFactor(s);
    const factor = Math.min(cornerFactor, endpointAmplitudeFactor(s));
    const endpointStub = k <= 1 || k >= sampleCount - 2;
    const displacement = endpointStub ? 0 : amplitude * factor * Math.sin(phase);
    samples.push({ x: pt.x + nx * displacement, y: pt.y + ny * displacement });
    sampleArcLens.push(s);
  }
  const tension = 0.5;
  let d = `M ${f(samples[0].x)} ${f(samples[0].y)}`;
  for (let i = 1; i < samples.length; i++) {
    const p0 = samples[Math.max(0, i - 2)];
    const p1 = samples[i - 1];
    const p2 = samples[i];
    const p3 = samples[Math.min(samples.length - 1, i + 1)];
    const cp1x = p1.x + (p2.x - p0.x) * tension / 3;
    const cp1y = p1.y + (p2.y - p0.y) * tension / 3;
    const cp2x = p2.x - (p3.x - p1.x) * tension / 3;
    const cp2y = p2.y - (p3.y - p1.y) * tension / 3;
    d += ` C ${f(cp1x)} ${f(cp1y)} ${f(cp2x)} ${f(cp2y)} ${f(p2.x)} ${f(p2.y)}`;
  }
  return d;
}
function f(n) {
  return Math.round(n * 100) / 100 + "";
}
var W_CROSS = 1e4;
var W_INTER = 3e3;
var W_SIDE = 800;
var W_INTRA = 500;
var W_BEND = 50;
var W_LEN = 1;
var W_ALIGN = 400;
var SNAP_THRESHOLD = 14;
var CONFLICT_RADIUS = 28;
var CELL_SHRINK = 12;
var CHANNEL_GAP = 12;
var ROUTE_PADDING = 12;
var WALL_TS = [0.1, 0.25, 0.5, 0.75, 0.9];
var ARROW_ID = "triton-crosslink-arrow";
var BI_ARROW_ID = "triton-crosslink-arrow-both";
function routeAndRenderCrossLinks3(links2, theme, anchors, intraPorts, occupiedRects, routingObstacles, cellRects) {
  const { palette, typography, edges: edgeTheme } = theme;
  const elements = [];
  const defs = [];
  const PALETTE = [
    "#E11D48",
    "#16A34A",
    "#9333EA",
    "#0891B2",
    "#CA8A04",
    "#DC2626",
    "#2563EB",
    "#7C3AED"
  ];
  let explicitColorIdx = 0;
  const allNodeBounds = Object.values(anchors).map((a) => a.bounds);
  const addrKey = (addr) => [...addr.cellPath, addr.nodeId].join(".");
  function centerOf(key) {
    const a = anchors[key];
    return a ? { x: a.bounds.x + a.bounds.width / 2, y: a.bounds.y + a.bounds.height / 2 } : { x: 0, y: 0 };
  }
  const resolvable = links2.filter((l) => anchors[addrKey(l.from)] && anchors[addrKey(l.to)]);
  const portHints = preAssignSharedPorts(resolvable, anchors);
  const sortedLinks = [...resolvable].sort((a, b) => {
    const ca = centerOf(addrKey(a.from)), cb = centerOf(addrKey(a.to));
    const cc = centerOf(addrKey(b.from)), cd = centerOf(addrKey(b.to));
    const axA = Math.abs(cb.x - ca.x), ayA = Math.abs(cb.y - ca.y);
    const axB = Math.abs(cd.x - cc.x), ayB = Math.abs(cd.y - cc.y);
    const sA = Math.min(axA, ayA) / (Math.max(axA, ayA) + 1);
    const sB = Math.min(axB, ayB) / (Math.max(axB, ayB) + 1);
    if (Math.abs(sA - sB) > 0.05) return sA - sB;
    return Math.hypot(cb.x - ca.x, cb.y - ca.y) - Math.hypot(cd.x - cc.x, cd.y - cc.y);
  }).reverse();
  const committed = [];
  const workingByOriginalIdx = /* @__PURE__ */ new Map();
  for (let si = 0; si < sortedLinks.length; si++) {
    const link = sortedLinks[si];
    const rIdx = resolvable.indexOf(link);
    const hint = rIdx >= 0 ? portHints.get(rIdx) : void 0;
    const best = findBestRoute(
      si,
      link,
      anchors,
      allNodeBounds,
      cellRects,
      committed,
      intraPorts ?? [],
      hint
    );
    if (!best) continue;
    committed.push(best.committed);
    let color;
    const explicitColor = typeof link.props?.color === "string" ? link.props.color : void 0;
    if (explicitColor) {
      color = explicitColor;
    } else {
      color = PALETTE[explicitColorIdx % PALETTE.length];
      explicitColorIdx++;
    }
    const dash = edgeStyleToDash(link.style);
    const animation = link.animation === "none" ? void 0 : isRenderedConnectorAnimation(link.animation) ? link.animation : void 0;
    let markerEnd;
    let markerStart;
    if (link.direction === "directed") {
      markerEnd = crossLinkMarkerId(ARROW_ID, color);
    } else if (link.direction === "bidirectional") {
      markerEnd = crossLinkMarkerId(ARROW_ID, color);
      markerStart = crossLinkMarkerId(BI_ARROW_ID, color);
    }
    const strokeWidth = link.style === "thick" ? (edgeTheme.strokeWidth + 0.5) * 2 : void 0;
    const isWavy = link.style === "wavy";
    const wavyAmp = link.props?.amplitude ?? 3;
    const wavyLambda = link.props?.wavelength ?? 12;
    const origIdx = links2.indexOf(link);
    workingByOriginalIdx.set(origIdx >= 0 ? origIdx : workingByOriginalIdx.size, {
      points: [...best.rawPts],
      ...best.committed.isBezier ? { isBezier: true } : {},
      color,
      ...dash ? { dash } : {},
      ...strokeWidth ? { strokeWidth } : {},
      ...isWavy ? { isWavy: true, wavyAmplitude: wavyAmp, wavyWavelength: wavyLambda } : {},
      ...animation ? { animation } : {},
      ...markerEnd ? { markerEnd } : {},
      ...markerStart ? { markerStart } : {},
      ...link.label ? { label: link.label } : {},
      ...link.props?.labelPlacement === "path-midpoint" ? { labelPlacement: "path-midpoint" } : {}
    });
  }
  repairCrossings(
    committed,
    sortedLinks,
    anchors,
    allNodeBounds,
    cellRects,
    intraPorts ?? [],
    workingByOriginalIdx,
    links2,
    portHints,
    resolvable
  );
  const workingRoutes = [];
  for (let i = 0; i < links2.length; i++) {
    const wr = workingByOriginalIdx.get(i);
    if (wr) workingRoutes.push(wr);
  }
  if (routingObstacles && routingObstacles.length > 0) {
    nudgeOffBorders(workingRoutes, routingObstacles);
  }
  separateChannels(workingRoutes);
  const CORRIDOR_TOL = 20;
  const labelFractions = new Array(workingRoutes.length).fill(0.5);
  const labeledIdxs = workingRoutes.map((r, i) => r.label ? i : -1).filter((i) => i >= 0);
  const staggered = /* @__PURE__ */ new Set();
  for (const i of labeledIdxs) {
    if (staggered.has(i)) continue;
    const wrI = workingRoutes[i];
    const ptsI = sampledPts(wrI);
    const infoI = dominantSegment(ptsI);
    const group = [i];
    for (const j of labeledIdxs) {
      if (j === i || staggered.has(j)) continue;
      const wrJ = workingRoutes[j];
      const ptsJ = sampledPts(wrJ);
      const infoJ = dominantSegment(ptsJ);
      if (infoI.axis === infoJ.axis && Math.abs(infoI.coord - infoJ.coord) < CORRIDOR_TOL) {
        group.push(j);
      }
    }
    if (group.length > 1) {
      group.sort((a, b) => a - b);
      group.forEach((idx, k) => {
        labelFractions[idx] = (k + 1) / (group.length + 1);
      });
      group.forEach((idx) => staggered.add(idx));
    }
  }
  const arrowMarkerColors = /* @__PURE__ */ new Map();
  const biArrowMarkerColors = /* @__PURE__ */ new Map();
  const pendingLabels = [];
  for (let i = 0; i < workingRoutes.length; i++) {
    const wr = workingRoutes[i];
    let path = wr.isBezier ? `M ${wr.points[0].x} ${wr.points[0].y} C ${wr.points[1].x} ${wr.points[1].y} ${wr.points[2].x} ${wr.points[2].y} ${wr.points[3].x} ${wr.points[3].y}` : wr.points.map((p, pi) => `${pi === 0 ? "M" : "L"} ${p.x} ${p.y}`).join(" ");
    if (wr.isWavy) {
      path = wavifyPath([...wr.points], wr.wavyAmplitude ?? 3, wr.wavyWavelength ?? 12);
    }
    if (wr.markerEnd) arrowMarkerColors.set(wr.markerEnd, wr.color);
    if (wr.markerStart) biArrowMarkerColors.set(wr.markerStart, wr.color);
    elements.push({
      type: "path",
      d: path,
      stroke: wr.color,
      strokeWidth: wr.strokeWidth ?? edgeTheme.strokeWidth + 0.5,
      ...wr.dash ? { strokeDasharray: wr.dash } : {},
      ...wr.animation ? { animated: wr.animation } : {},
      ...wr.markerEnd ? { markerEnd: wr.markerEnd } : {},
      ...wr.markerStart ? { markerStart: wr.markerStart } : {}
    });
    if (wr.label) {
      const pts = sampledPts(wr);
      const labelPos = staggered.has(i) || wr.isBezier || wr.labelPlacement === "path-midpoint" ? pointAtFrac(pts, labelFractions[i] ?? 0.5) : labelAnchor(pts);
      pendingLabels.push({
        content: wr.label,
        x: labelPos.x,
        y: labelPos.y - 6,
        fontSize: edgeTheme.labelFontSize,
        fontFamily: typography.fontFamily,
        fill: wr.color
      });
    }
  }
  const CHAR_W = 0.65;
  const LABEL_PAD = 4;
  const fixedRects = [...allNodeBounds, ...occupiedRects ?? []];
  const labelRects = pendingLabels.map((l) => ({
    x: l.x - l.content.length * l.fontSize * CHAR_W / 2 - LABEL_PAD,
    y: l.y - l.fontSize,
    width: l.content.length * l.fontSize * CHAR_W + LABEL_PAD * 2,
    height: l.fontSize + LABEL_PAD * 2
  }));
  deCollideLabels(labelRects, fixedRects);
  for (let i = 0; i < pendingLabels.length; i++) {
    const l = pendingLabels[i];
    const r = labelRects[i];
    elements.push({
      type: "text",
      content: l.content,
      position: { x: r.x + r.width / 2, y: r.y + r.height - LABEL_PAD },
      fontSize: l.fontSize,
      fontFamily: typography.fontFamily,
      fill: l.fill,
      anchor: "middle",
      fontWeight: "bold"
    });
  }
  const s = edgeTheme.arrowSize;
  const sH = rhu(s * 0.7);
  const sMidY = rhu(s * 0.35);
  const sRefX = rhu(s - 1);
  for (const [id, color] of arrowMarkerColors) {
    defs.push(
      `<marker id="${id}" markerWidth="${s}" markerHeight="${sH}" refX="${sRefX}" refY="${sMidY}" orient="auto" markerUnits="userSpaceOnUse"><polygon points="0 0, ${s} ${sMidY}, 0 ${sH}" fill="${color}" /></marker>`
    );
  }
  for (const [id, color] of biArrowMarkerColors) {
    defs.push(
      `<marker id="${id}" markerWidth="${s}" markerHeight="${sH}" refX="1" refY="${sMidY}" orient="auto" markerUnits="userSpaceOnUse"><polygon points="${s} 0, 0 ${sMidY}, ${s} ${sH}" fill="${color}" /></marker>`
    );
  }
  return { defs, elements };
}
function findBestRoute(linkIdx, link, anchors, allNodeBounds, cellRects, committed, intraPorts, portHint) {
  const addrKey = (addr) => [...addr.cellPath, addr.nodeId].join(".");
  const srcKey = addrKey(link.from);
  const dstKey = addrKey(link.to);
  const srcAnchor = anchors[srcKey];
  const dstAnchor = anchors[dstKey];
  if (!srcAnchor || !dstAnchor) return null;
  const srcCenter = {
    x: srcAnchor.bounds.x + srcAnchor.bounds.width / 2,
    y: srcAnchor.bounds.y + srcAnchor.bounds.height / 2
  };
  const dstCenter = {
    x: dstAnchor.bounds.x + dstAnchor.bounds.width / 2,
    y: dstAnchor.bounds.y + dstAnchor.bounds.height / 2
  };
  const srcCellId = link.from.cellPath.join(".");
  const dstCellId = link.to.cellPath.join(".");
  const sameSourceCell = (key) => srcCellId.length > 0 && key.startsWith(srcCellId + ".");
  const sameTargetCell = (key) => dstCellId.length > 0 && key.startsWith(dstCellId + ".");
  const linkObstacles = Object.entries(anchors).filter(
    ([key]) => !sameSourceCell(key) && !sameTargetCell(key) && key !== srcKey && key !== dstKey
  ).map(([, a]) => a.bounds);
  if (cellRects) {
    for (const [cellId, r] of cellRects) {
      if (cellId === srcCellId || cellId === dstCellId) continue;
      const sw = r.width - 2 * CELL_SHRINK;
      const sh = r.height - 2 * CELL_SHRINK;
      if (sw > 0 && sh > 0) {
        linkObstacles.push({ x: r.x + CELL_SHRINK, y: r.y + CELL_SHRINK, width: sw, height: sh });
      }
    }
  }
  const routeStyle = link.routing ?? "orthogonal";
  const sides = ["N", "S", "E", "W"];
  const srcWalls = link.exitWall ? [link.exitWall] : portHint?.srcWall ? [portHint.srcWall] : sides;
  const dstWalls = link.entryWall ? [link.entryWall] : portHint?.dstWall ? [portHint.dstWall] : sides;
  const srcTs = portHint?.srcT !== void 0 ? [portHint.srcT] : WALL_TS;
  const dstTs = portHint?.dstT !== void 0 ? [portHint.dstT] : WALL_TS;
  let bestScore = Infinity;
  let bestCommitted;
  let bestRawPts;
  for (const ss of srcWalls) {
    for (const ds of dstWalls) {
      for (const srcT of srcTs) {
        for (const dstT of dstTs) {
          let sp = wallPoint(srcAnchor.bounds, ss, srcT);
          let dp = wallPoint(dstAnchor.bounds, ds, dstT);
          const snapped = snapAlignment(ss, sp, ds, dp);
          sp = snapped.srcPort;
          dp = snapped.dstPort;
          const router = createRouter(routeStyle);
          const routePadding = typeof link.props?.routePadding === "number" ? link.props.routePadding : ROUTE_PADDING;
          const routeObstacles = [
            ...linkObstacles,
            ...forcedAwayEndpointObstacles(
              link,
              ss,
              ds,
              srcCenter,
              dstCenter,
              anchors,
              cellRects,
              srcKey,
              dstKey,
              srcCellId,
              dstCellId,
              sameSourceCell,
              sameTargetCell
            )
          ];
          const route = router.route({
            from: sp,
            to: dp,
            style: routeStyle,
            obstacles: routeObstacles,
            padding: routePadding,
            fromDir: ss,
            toDir: ds
          });
          const rawPts = route.points;
          const isBez = routeStyle === "bezier" && rawPts.length === 4;
          const pts = isBez ? sampleCubicBezier(rawPts[0], rawPts[1], rawPts[2], rawPts[3], 12) : rawPts;
          const { hSegs, vSegs } = decomposeHV(pts, routeStyle);
          const score = sidePenalty(ss, ds, srcCenter, dstCenter) + crossingScore(pts, hSegs, vSegs, isBez, committed) + portConflictScore(
            srcKey,
            ss,
            sp,
            srcT,
            dstKey,
            ds,
            dp,
            dstT,
            committed,
            intraPorts,
            anchors
          ) + (isBez ? 0 : Math.max(0, rawPts.length - 2) * W_BEND) + pathLen(pts) * W_LEN + (isBez ? 0 : alignPenalty(rawPts));
          if (score < bestScore) {
            bestScore = score;
            bestRawPts = rawPts;
            bestCommitted = {
              linkIdx,
              srcKey,
              dstKey,
              srcWall: ss,
              srcT,
              srcPort: sp,
              dstWall: ds,
              dstT,
              dstPort: dp,
              points: pts,
              rawPts,
              isBezier: isBez,
              hSegs,
              vSegs
            };
          }
        }
      }
    }
  }
  return bestScore < Infinity ? { committed: bestCommitted, rawPts: bestRawPts } : null;
}
function forcedAwayEndpointObstacles(link, srcWall, dstWall, srcCenter, dstCenter, anchors, cellRects, srcKey, dstKey, srcCellId, dstCellId, sameSourceCell, sameTargetCell) {
  const exitAway = link.exitWall !== void 0 && wallFacesAway(srcWall, srcCenter, dstCenter);
  const entryAway = link.entryWall !== void 0 && wallFacesAway(dstWall, dstCenter, srcCenter);
  const rects = [anchors[srcKey]?.bounds, anchors[dstKey]?.bounds].filter(
    (r) => r !== void 0
  );
  if (exitAway || entryAway) {
    for (const [key, anchor] of Object.entries(anchors)) {
      if (sameSourceCell(key) || sameTargetCell(key)) rects.push(anchor.bounds);
    }
    const srcCell = cellRects?.get(srcCellId);
    const dstCell = cellRects?.get(dstCellId);
    if (srcCell) rects.push(srcCell);
    if (dstCell) rects.push(dstCell);
  }
  return rects;
}
function wallFacesAway(wall, fromCenter, toCenter) {
  const nx = wall === "E" ? 1 : wall === "W" ? -1 : 0;
  const ny = wall === "S" ? 1 : wall === "N" ? -1 : 0;
  const vx = toCenter.x - fromCenter.x;
  const vy = toCenter.y - fromCenter.y;
  return vx * nx + vy * ny < 0;
}
function repairCrossings(committed, sortedLinks, anchors, allNodeBounds, cellRects, intraPorts, workingByOrigIdx, links2, portHints, resolvable) {
  for (let j = 1; j < committed.length; j++) {
    const crJ = committed[j];
    let jCrossings = 0;
    for (let i = 0; i < j; i++) {
      jCrossings += countCrossingsBetween(committed[i], crJ);
    }
    if (jCrossings === 0) continue;
    const committedWithoutJ = committed.filter((_, k) => k !== j);
    const link = sortedLinks[crJ.linkIdx];
    if (!link) continue;
    const rIdx = resolvable.indexOf(link);
    const hint = rIdx >= 0 ? portHints.get(rIdx) : void 0;
    const result = findBestRoute(
      crJ.linkIdx,
      link,
      anchors,
      allNodeBounds,
      cellRects,
      committedWithoutJ,
      intraPorts,
      hint
    );
    if (!result) continue;
    let newCrossings = 0;
    for (let i = 0; i < j; i++) {
      newCrossings += countCrossingsBetween(committed[i], result.committed);
    }
    if (newCrossings >= jCrossings) continue;
    committed[j] = result.committed;
    const origIdx = links2.indexOf(link);
    const wr = workingByOrigIdx.get(origIdx >= 0 ? origIdx : -1);
    if (wr) {
      wr.points = [...result.rawPts];
      if (result.committed.isBezier) {
        wr.isBezier = true;
      } else {
        delete wr.isBezier;
      }
    }
  }
}
function decomposeHV(pts, style) {
  const hSegs = [];
  const vSegs = [];
  if (style !== "orthogonal") return { hSegs, vSegs };
  for (let i = 0; i < pts.length - 1; i++) {
    const a = pts[i], b = pts[i + 1];
    if (Math.abs(b.y - a.y) < 0.5 && Math.abs(b.x - a.x) > 0.5) {
      hSegs.push({ y: (a.y + b.y) / 2, x1: Math.min(a.x, b.x), x2: Math.max(a.x, b.x) });
    } else if (Math.abs(b.x - a.x) < 0.5 && Math.abs(b.y - a.y) > 0.5) {
      vSegs.push({ x: (a.x + b.x) / 2, y1: Math.min(a.y, b.y), y2: Math.max(a.y, b.y) });
    }
  }
  return { hSegs, vSegs };
}
function countHVCrossings(aH, aV, bH, bV) {
  let n = 0;
  for (const h2 of aH) for (const v of bV) if (hvCross(h2, v)) n++;
  for (const v of aV) for (const h2 of bH) if (hvCross(h2, v)) n++;
  return n;
}
function hvCross(h2, v) {
  return v.x > h2.x1 && v.x < h2.x2 && h2.y > v.y1 && h2.y < v.y2;
}
function countPolylineCrossings(a, b) {
  let n = 0;
  for (let i = 0; i < a.length - 1; i++) {
    const a1 = a[i], a2 = a[i + 1];
    const axMin = Math.min(a1.x, a2.x), axMax = Math.max(a1.x, a2.x);
    const ayMin = Math.min(a1.y, a2.y), ayMax = Math.max(a1.y, a2.y);
    for (let j = 0; j < b.length - 1; j++) {
      const b1 = b[j], b2 = b[j + 1];
      if (axMax < Math.min(b1.x, b2.x) || axMin > Math.max(b1.x, b2.x)) continue;
      if (ayMax < Math.min(b1.y, b2.y) || ayMin > Math.max(b1.y, b2.y)) continue;
      if (properlyCross(a1, a2, b1, b2)) n++;
    }
  }
  return n;
}
function countCrossingsBetween(a, b) {
  if (!a.isBezier && !b.isBezier) {
    return countHVCrossings(a.hSegs, a.vSegs, b.hSegs, b.vSegs);
  }
  return countPolylineCrossings(a.points, b.points);
}
function crossingScore(pts, hSegs, vSegs, isBezier, committed) {
  let crossings = 0;
  for (const cr of committed) {
    if (!isBezier && !cr.isBezier) {
      crossings += countHVCrossings(hSegs, vSegs, cr.hSegs, cr.vSegs);
    } else {
      crossings += countPolylineCrossings(pts, cr.points);
    }
  }
  return crossings * W_CROSS;
}
function sidePenalty(srcWall, dstWall, srcCenter, dstCenter) {
  const dx = dstCenter.x - srcCenter.x;
  const dy = dstCenter.y - srcCenter.y;
  const absX = Math.abs(dx), absY = Math.abs(dy);
  const idealSrc = absX >= absY ? dx >= 0 ? "E" : "W" : dy >= 0 ? "S" : "N";
  const idealDst = absX >= absY ? dx >= 0 ? "W" : "E" : dy >= 0 ? "N" : "S";
  return sideSteps(srcWall, idealSrc) * W_SIDE + sideSteps(dstWall, idealDst) * W_SIDE;
}
function sideSteps(wall, ideal) {
  if (wall === ideal) return 0;
  const opp = { N: "S", S: "N", E: "W", W: "E" };
  return opp[wall] === ideal ? 2 : 1;
}
function portConflictScore(srcKey, srcWall, srcPt, _srcT, dstKey, dstWall, dstPt, _dstT, committed, intraPorts, anchors) {
  let penalty = 0;
  for (const cr of committed) {
    for (const [key, wall, pt] of [
      [cr.srcKey, cr.srcWall, cr.srcPort],
      [cr.dstKey, cr.dstWall, cr.dstPort]
    ]) {
      if (key === srcKey && wall === srcWall) {
        const d = Math.hypot(srcPt.x - pt.x, srcPt.y - pt.y);
        if (d < CONFLICT_RADIUS) penalty += W_INTER * (1 - d / CONFLICT_RADIUS);
      }
      if (key === dstKey && wall === dstWall) {
        const d = Math.hypot(dstPt.x - pt.x, dstPt.y - pt.y);
        if (d < CONFLICT_RADIUS) penalty += W_INTER * (1 - d / CONFLICT_RADIUS);
      }
    }
  }
  for (const ip of intraPorts) {
    const anchor = anchors[ip.nodeKey];
    if (!anchor) continue;
    const ipPt = wallPoint(anchor.bounds, ip.wall, ip.t);
    if (ip.nodeKey === srcKey && ip.wall === srcWall) {
      const d = Math.hypot(srcPt.x - ipPt.x, srcPt.y - ipPt.y);
      if (d < CONFLICT_RADIUS) penalty += W_INTRA * (1 - d / CONFLICT_RADIUS);
    }
    if (ip.nodeKey === dstKey && ip.wall === dstWall) {
      const d = Math.hypot(dstPt.x - ipPt.x, dstPt.y - ipPt.y);
      if (d < CONFLICT_RADIUS) penalty += W_INTRA * (1 - d / CONFLICT_RADIUS);
    }
  }
  return penalty;
}
function alignPenalty(pts) {
  let p = 0;
  for (let i = 0; i < pts.length - 1; i++) {
    const a = pts[i], b = pts[i + 1];
    if (Math.hypot(b.x - a.x, b.y - a.y) < SNAP_THRESHOLD) p += W_ALIGN;
  }
  return p;
}
function snapAlignment(ss, sp, ds, dp) {
  const sH = ss === "E" || ss === "W", dH = ds === "E" || ds === "W";
  if (sH && dH && Math.abs(sp.y - dp.y) < SNAP_THRESHOLD) {
    const avgY = (sp.y + dp.y) / 2;
    return { srcPort: { x: sp.x, y: avgY }, dstPort: { x: dp.x, y: avgY } };
  }
  const sV = ss === "N" || ss === "S", dV = ds === "N" || ds === "S";
  if (sV && dV && Math.abs(sp.x - dp.x) < SNAP_THRESHOLD) {
    const avgX = (sp.x + dp.x) / 2;
    return { srcPort: { x: avgX, y: sp.y }, dstPort: { x: avgX, y: dp.y } };
  }
  return { srcPort: sp, dstPort: dp };
}
function preAssignSharedPorts(links2, anchors) {
  const key = (addr) => [...addr.cellPath, addr.nodeId].join(".");
  const walls = links2.map((link) => {
    const sa = anchors[key(link.from)];
    const da = anchors[key(link.to)];
    if (!sa || !da) return { srcWall: "E", dstWall: "W" };
    const sc = rectCenter(sa.bounds), dc = rectCenter(da.bounds);
    return {
      srcWall: link.exitWall ?? idealSrcWall(sc, dc),
      dstWall: link.entryWall ?? idealDstWall(sc, dc)
    };
  });
  const groups = /* @__PURE__ */ new Map();
  for (let i = 0; i < links2.length; i++) {
    const link = links2[i];
    const srcKey = key(link.from);
    const dstKey = key(link.to);
    const sa = anchors[srcKey];
    const da = anchors[dstKey];
    if (!sa || !da) continue;
    const { srcWall, dstWall } = walls[i];
    const sc = rectCenter(sa.bounds), dc = rectCenter(da.bounds);
    const srcTK = srcWall === "N" || srcWall === "S" ? dc.x : dc.y;
    const dstTK = dstWall === "N" || dstWall === "S" ? sc.x : sc.y;
    const sg = `${srcKey}:${srcWall}`;
    const dg = `${dstKey}:${dstWall}`;
    if (!groups.has(sg)) groups.set(sg, []);
    if (!groups.has(dg)) groups.set(dg, []);
    groups.get(sg).push({ linkIdx: i, isSource: true, tangentKey: srcTK });
    groups.get(dg).push({ linkIdx: i, isSource: false, tangentKey: dstTK });
  }
  const result = /* @__PURE__ */ new Map();
  for (const [groupKey, members] of groups) {
    const wall = groupKey.split(":").at(-1);
    const pinnedHere = members.filter((m) => {
      const l = links2[m.linkIdx];
      return m.isSource ? l.exitWall === wall : l.entryWall === wall;
    });
    const fanned = pinnedHere.length > 0 ? pinnedHere : members;
    if (fanned.length < 2 && pinnedHere.length === 0) continue;
    fanned.sort((a, b) => a.tangentKey - b.tangentKey);
    const n = fanned.length;
    for (let k = 0; k < n; k++) {
      const { linkIdx, isSource } = fanned[k];
      const t = (k + 1) / (n + 1);
      const ex = result.get(linkIdx) ?? {};
      result.set(
        linkIdx,
        isSource ? { ...ex, srcWall: wall, srcT: t } : { ...ex, dstWall: wall, dstT: t }
      );
    }
  }
  return result;
}
function rectCenter(r) {
  return { x: r.x + r.width / 2, y: r.y + r.height / 2 };
}
function idealSrcWall(sc, dc) {
  const dx = dc.x - sc.x, dy = dc.y - sc.y;
  return Math.abs(dx) >= Math.abs(dy) ? dx >= 0 ? "E" : "W" : dy >= 0 ? "S" : "N";
}
function idealDstWall(sc, dc) {
  const opp = { N: "S", S: "N", E: "W", W: "E" };
  return opp[idealSrcWall(sc, dc)];
}
function wallPoint(bounds, wall, t) {
  const { x, y, width: w, height: h2 } = bounds;
  switch (wall) {
    case "N":
      return { x: x + w * t, y };
    case "S":
      return { x: x + w * t, y: y + h2 };
    case "W":
      return { x, y: y + h2 * t };
    case "E":
      return { x: x + w, y: y + h2 * t };
  }
}
function sampleCubicBezier(p0, p1, p2, p3, n) {
  const pts = [];
  for (let i = 0; i <= n; i++) {
    const t = i / n, t2 = t * t, t3 = t2 * t;
    const mt = 1 - t, mt2 = mt * mt, mt3 = mt2 * mt;
    pts.push({
      x: mt3 * p0.x + 3 * mt2 * t * p1.x + 3 * mt * t2 * p2.x + t3 * p3.x,
      y: mt3 * p0.y + 3 * mt2 * t * p1.y + 3 * mt * t2 * p2.y + t3 * p3.y
    });
  }
  return pts;
}
function sampledPts(wr) {
  if (wr.isBezier && wr.points.length === 4) {
    return sampleCubicBezier(wr.points[0], wr.points[1], wr.points[2], wr.points[3], 12);
  }
  return wr.points;
}
function properlyCross(p1, p2, p3, p4) {
  const d1 = cross2(p3, p4, p1), d2 = cross2(p3, p4, p2);
  const d3 = cross2(p1, p2, p3), d4 = cross2(p1, p2, p4);
  return (d1 > 0 && d2 < 0 || d1 < 0 && d2 > 0) && (d3 > 0 && d4 < 0 || d3 < 0 && d4 > 0);
}
function cross2(a, b, c) {
  return (b.x - a.x) * (c.y - a.y) - (b.y - a.y) * (c.x - a.x);
}
function pathLen(pts) {
  let len = 0;
  for (let i = 0; i < pts.length - 1; i++) {
    const a = pts[i], b = pts[i + 1];
    len += Math.hypot(b.x - a.x, b.y - a.y);
  }
  return len;
}
function pointAtFrac(pts, t) {
  if (pts.length === 0) return { x: 0, y: 0 };
  if (pts.length === 1) return pts[0];
  const lens = [];
  let total = 0;
  for (let i = 0; i < pts.length - 1; i++) {
    const l = Math.hypot(pts[i + 1].x - pts[i].x, pts[i + 1].y - pts[i].y);
    lens.push(l);
    total += l;
  }
  if (total === 0) return pts[0];
  let acc = 0;
  const target = total * Math.min(Math.max(t, 0), 1);
  for (let i = 0; i < lens.length; i++) {
    const l = lens[i];
    if (acc + l >= target) {
      const frac = l > 0 ? (target - acc) / l : 0;
      const a = pts[i], b = pts[i + 1];
      return { x: a.x + (b.x - a.x) * frac, y: a.y + (b.y - a.y) * frac };
    }
    acc += l;
  }
  return pts[pts.length - 1];
}
function dominantSegment(pts) {
  let bestLen = 0, axis = "h", coord = 0;
  for (let i = 0; i < pts.length - 1; i++) {
    const a = pts[i], b = pts[i + 1];
    const dx = Math.abs(b.x - a.x), dy = Math.abs(b.y - a.y);
    const len = dx + dy;
    if (len > bestLen) {
      bestLen = len;
      if (dy > dx) {
        axis = "v";
        coord = (a.x + b.x) / 2;
      } else {
        axis = "h";
        coord = (a.y + b.y) / 2;
      }
    }
  }
  return { axis, coord };
}
function labelAnchor(pts) {
  if (pts.length === 0) return { x: 0, y: 0 };
  if (pts.length === 1) return pts[0];
  let bestH = -1;
  let hMid = null;
  let bestAny = -1;
  let anyMid = pts[0];
  for (let i = 0; i < pts.length - 1; i++) {
    const a = pts[i], b = pts[i + 1];
    const dx = Math.abs(b.x - a.x), dy = Math.abs(b.y - a.y);
    const len = Math.hypot(dx, dy);
    if (len > bestAny) {
      bestAny = len;
      anyMid = { x: (a.x + b.x) / 2, y: (a.y + b.y) / 2 };
    }
    if (dx > dy && dx > bestH) {
      bestH = dx;
      hMid = { x: (a.x + b.x) / 2, y: (a.y + b.y) / 2 };
    }
  }
  return hMid ?? anyMid;
}
function edgeStyleToDash(style) {
  if (style === "dotted") return "4 3";
  if (style === "dashed") return "8 4";
  return void 0;
}
function nudgeOffBorders(routes, borders) {
  const TOLERANCE = 3, NUDGE = 8;
  const hBorderYs = borders.filter((b) => b.width > b.height).map((b) => b.y + b.height / 2);
  const vBorderXs = borders.filter((b) => b.height >= b.width).map((b) => b.x + b.width / 2);
  for (const route of routes) {
    if (route.isBezier) continue;
    const pts = route.points;
    for (let i = 0; i < pts.length - 1; i++) {
      const a = pts[i], b = pts[i + 1];
      if (Math.abs(b.y - a.y) < 1 && Math.abs(b.x - a.x) > 1) {
        for (const borderY of hBorderYs) {
          if (Math.abs(a.y - borderY) < TOLERANCE) {
            const midY = (Math.min(...pts.map((p) => p.y)) + Math.max(...pts.map((p) => p.y))) / 2;
            const nd = a.y < midY ? -NUDGE : NUDGE;
            pts[i] = { x: a.x, y: a.y + nd };
            pts[i + 1] = { x: b.x, y: b.y + nd };
            break;
          }
        }
      } else if (Math.abs(b.x - a.x) < 1 && Math.abs(b.y - a.y) > 1) {
        for (const borderX of vBorderXs) {
          if (Math.abs(a.x - borderX) < TOLERANCE) {
            const midX = (Math.min(...pts.map((p) => p.x)) + Math.max(...pts.map((p) => p.x))) / 2;
            const nd = a.x < midX ? -NUDGE : NUDGE;
            pts[i] = { x: a.x + nd, y: a.y };
            pts[i + 1] = { x: b.x + nd, y: b.y };
            break;
          }
        }
      }
    }
  }
}
function separateChannels(routes) {
  const segs = [];
  for (let ri = 0; ri < routes.length; ri++) {
    if (routes[ri].isBezier) continue;
    const pts = routes[ri].points;
    for (let si = 0; si < pts.length - 1; si++) {
      const a = pts[si], b = pts[si + 1];
      const dx = Math.abs(b.x - a.x), dy = Math.abs(b.y - a.y);
      if (dx < 1 && dy > 1) {
        segs.push({
          ri,
          si,
          isV: true,
          coord: (a.x + b.x) / 2,
          lo: Math.min(a.y, b.y),
          hi: Math.max(a.y, b.y)
        });
      } else if (dy < 1 && dx > 1) {
        segs.push({
          ri,
          si,
          isV: false,
          coord: (a.y + b.y) / 2,
          lo: Math.min(a.x, b.x),
          hi: Math.max(a.x, b.x)
        });
      }
    }
  }
  const COORD_TOL = 10;
  const used = /* @__PURE__ */ new Set();
  for (let i = 0; i < segs.length; i++) {
    if (used.has(i)) continue;
    const si = segs[i];
    const group = [i];
    for (let j = i + 1; j < segs.length; j++) {
      if (used.has(j)) continue;
      const sj = segs[j];
      if (sj.isV !== si.isV) continue;
      if (Math.abs(sj.coord - si.coord) > COORD_TOL) continue;
      if (sj.hi - si.lo > 8 && si.hi - sj.lo > 8) {
        group.push(j);
        used.add(j);
      }
    }
    used.add(i);
    if (group.length < 2) continue;
    const n = group.length;
    for (let k = 0; k < n; k++) {
      const seg = segs[group[k]];
      const offset = (k - (n - 1) / 2) * CHANNEL_GAP;
      const pts = routes[seg.ri].points;
      if (seg.isV) {
        pts[seg.si] = { x: pts[seg.si].x + offset, y: pts[seg.si].y };
        pts[seg.si + 1] = { x: pts[seg.si + 1].x + offset, y: pts[seg.si + 1].y };
      } else {
        pts[seg.si] = { x: pts[seg.si].x, y: pts[seg.si].y + offset };
        pts[seg.si + 1] = { x: pts[seg.si + 1].x, y: pts[seg.si + 1].y + offset };
      }
    }
  }
}
function deCollideLabels(labelRects, fixedRects) {
  const MAX_PASSES = 20, NUDGE = 2;
  for (let pass = 0; pass < MAX_PASSES; pass++) {
    let moved = false;
    for (const lr of labelRects) {
      for (const fr of fixedRects) {
        if (!rectsOverlap(lr, fr)) continue;
        const ox = Math.min(lr.x + lr.width, fr.x + fr.width) - Math.max(lr.x, fr.x);
        const oy = Math.min(lr.y + lr.height, fr.y + fr.height) - Math.max(lr.y, fr.y);
        if (ox <= oy) {
          lr.x = lr.x + lr.width / 2 <= fr.x + fr.width / 2 ? fr.x - lr.width - NUDGE : fr.x + fr.width + NUDGE;
        } else {
          lr.y = lr.y + lr.height / 2 <= fr.y + fr.height / 2 ? fr.y - lr.height - NUDGE : fr.y + fr.height + NUDGE;
        }
        moved = true;
      }
    }
    for (let i = 0; i < labelRects.length; i++) {
      for (let j = i + 1; j < labelRects.length; j++) {
        const a = labelRects[i], b = labelRects[j];
        if (!rectsOverlap(a, b)) continue;
        const oy = Math.min(a.y + a.height, b.y + b.height) - Math.max(a.y, b.y);
        if (a.y + a.height / 2 <= b.y + b.height / 2) {
          a.y -= oy / 2 + NUDGE;
          b.y += oy / 2 + NUDGE;
        } else {
          a.y += oy / 2 + NUDGE;
          b.y -= oy / 2 + NUDGE;
        }
        moved = true;
      }
    }
    if (!moved) break;
  }
}
function rectsOverlap(a, b) {
  return a.x < b.x + b.width && a.x + a.width > b.x && a.y < b.y + b.height && a.y + a.height > b.y;
}
function routeConnectors(input) {
  const diagnostics = connectorDiagnostics(input.connectors, input.anchors);
  const resolvable = input.connectors.filter(
    (spec) => input.anchors[spec.fromKey] !== void 0 && input.anchors[spec.toKey] !== void 0
  );
  const links2 = resolvable.map(specToCrossLink);
  const rendered = links2.length > 0 ? routeAndRenderCrossLinks3(
    links2,
    input.theme,
    input.anchors,
    input.occupiedPorts,
    input.occupiedTextRects,
    input.routingObstacles,
    input.containerObstacles
  ) : { defs: [], elements: [] };
  const pathElements = rendered.elements.filter((e) => e.type !== "text");
  const labelElements = rendered.elements.filter((e) => e.type === "text");
  return {
    pathElements,
    labelElements,
    elements: rendered.elements,
    defs: rendered.defs,
    diagnostics,
    extents: connectorExtents(rendered.elements)
  };
}
function crossLinksToConnectorSpecs(links2, defaults) {
  return links2.map((link) => {
    const fromKey = addressToKey(link.from);
    const toKey = addressToKey(link.to);
    const curveStyle = link.curveStyle ?? defaults?.curveStyle;
    return {
      fromKey,
      toKey,
      direction: link.direction,
      style: link.style,
      ...link.label !== void 0 ? { label: link.label } : {},
      ...link.routing !== void 0 ? { routing: link.routing } : {},
      ...curveStyle !== void 0 ? { curveStyle } : {},
      ...link.exitWall !== void 0 ? { exitWall: link.exitWall } : {},
      ...link.entryWall !== void 0 ? { entryWall: link.entryWall } : {},
      ...link.animation !== void 0 ? { animation: link.animation } : {},
      ...link.props !== void 0 ? { props: link.props } : {},
      ...link.from.cellPath.length > 0 ? { fromContainerKey: link.from.cellPath.join(".") } : {},
      ...link.to.cellPath.length > 0 ? { toContainerKey: link.to.cellPath.join(".") } : {}
    };
  });
}
function connectorDiagnostics(connectors, anchors) {
  const diagnostics = [];
  for (let i = 0; i < connectors.length; i++) {
    const spec = connectors[i];
    if (!anchors[spec.fromKey]) {
      diagnostics.push({
        connectorIndex: i,
        message: `Cannot resolve source: "${spec.fromKey}" not found in anchor registry`
      });
    }
    if (!anchors[spec.toKey]) {
      diagnostics.push({
        connectorIndex: i,
        message: `Cannot resolve target: "${spec.toKey}" not found in anchor registry`
      });
    }
  }
  return diagnostics;
}
function specToCrossLink(spec) {
  return {
    from: keyToAddress(spec.fromKey, spec.fromContainerKey),
    to: keyToAddress(spec.toKey, spec.toContainerKey),
    direction: spec.direction,
    style: spec.style,
    ...spec.label !== void 0 ? { label: spec.label } : {},
    ...spec.routing !== void 0 ? { routing: spec.routing } : {},
    ...spec.curveStyle !== void 0 ? { curveStyle: spec.curveStyle } : {},
    ...spec.exitWall !== void 0 ? { exitWall: spec.exitWall } : {},
    ...spec.entryWall !== void 0 ? { entryWall: spec.entryWall } : {},
    ...spec.animation !== void 0 ? { animation: spec.animation } : {},
    ...spec.props !== void 0 ? { props: spec.props } : {}
  };
}
function keyToAddress(key, containerKey) {
  if (containerKey && key.startsWith(`${containerKey}.`)) {
    return {
      cellPath: containerKey.split("."),
      nodeId: key.slice(containerKey.length + 1)
    };
  }
  return { cellPath: [], nodeId: key };
}
function addressToKey(addr) {
  return [...addr.cellPath, addr.nodeId].join(".");
}
function connectorExtents(elements) {
  if (elements.length === 0) {
    return { minX: 0, minY: 0, maxX: 0, maxY: 0 };
  }
  let minX = Infinity;
  let minY = Infinity;
  let maxX = -Infinity;
  let maxY = -Infinity;
  for (const el of elements) {
    if (el.type === "path") {
      const nums = [];
      for (const m of el.d.matchAll(/-?\d+(?:\.\d+)?/g)) {
        const n = parseFloat(m[0]);
        if (!Number.isNaN(n)) nums.push(n);
      }
      for (let i = 0; i + 1 < nums.length; i += 2) {
        minX = Math.min(minX, nums[i]);
        minY = Math.min(minY, nums[i + 1]);
        maxX = Math.max(maxX, nums[i]);
        maxY = Math.max(maxY, nums[i + 1]);
      }
    } else if (el.type === "text") {
      const approxW = el.content.length * (el.fontSize ?? 12) * 0.65;
      const left = el.anchor === "middle" ? el.position.x - approxW / 2 : el.anchor === "end" ? el.position.x - approxW : el.position.x;
      const top = el.position.y - (el.fontSize ?? 12);
      minX = Math.min(minX, left);
      minY = Math.min(minY, top);
      maxX = Math.max(maxX, left + approxW);
      maxY = Math.max(maxY, el.position.y + (el.fontSize ?? 12));
    }
  }
  if (!Number.isFinite(minX)) return { minX: 0, minY: 0, maxX: 0, maxY: 0 };
  return { minX, minY, maxX, maxY };
}
var ARROW_ID2 = "struct-arrow";
function arrowDef(color, size) {
  const s = size != null ? size * 1.5 : 12;
  const sMidY = s / 2;
  const sRefX = s * (10 / 12);
  return `<marker id="${ARROW_ID2}" markerWidth="${s}" markerHeight="${s}" refX="${sRefX}" refY="${sMidY}" orient="auto" markerUnits="userSpaceOnUse"><path d="M0 0 L${s} ${sMidY} L0 ${s} z" fill="${color}" /></marker>`;
}
function lines(input) {
  return input.split(/\r?\n/).map((l) => l.trim()).filter(Boolean);
}
function tokenizeDirective(line) {
  const tokens = [];
  let i = 0;
  while (i < line.length) {
    while (i < line.length && /\s/.test(line[i])) i++;
    if (i >= line.length) break;
    if (line[i] === '"') {
      i++;
      let value = "";
      while (i < line.length) {
        const ch = line[i];
        if (ch === '"') {
          i++;
          break;
        }
        if (ch === "\\") {
          const next = line[i + 1];
          if (next === '"' || next === "\\") {
            value += next;
            i += 2;
            continue;
          }
        }
        value += ch;
        i++;
      }
      if (line[i - 1] !== '"') throw new Error(`Unterminated quoted string: ${line}`);
      tokens.push(value);
      continue;
    }
    const start = i;
    while (i < line.length && !/\s/.test(line[i])) i++;
    tokens.push(line.slice(start, i));
  }
  return tokens;
}
function formatIndex(n, pad) {
  if (pad === void 0 || pad <= 0) return String(n);
  if (n < 0) {
    return "-" + String(Math.abs(n)).padStart(pad, "0");
  }
  return String(n).padStart(pad, "0");
}
function parse(input) {
  let title;
  let cells2 = [];
  let axis = "horizontal";
  let indexShow = false;
  let indexSide = "before";
  let indexOrder = "normal";
  let indexRange;
  let indexPad;
  const ptrs = [];
  let highlights;
  let win;
  for (const line of lines(input)) {
    const t = tokenizeDirective(line);
    if (t.length === 0) continue;
    if (t[0] === "array") {
      if (t.length > 1) cells2 = parseCells(t.slice(1));
      continue;
    }
    if (t[0] === "title") {
      title = t.slice(1).join(" ");
      continue;
    }
    if (t[0] === "cells") {
      cells2 = parseCells(t.slice(1));
      continue;
    }
    if (t[0] === "axis") {
      if (t[1] === "vertical" || t[1] === "horizontal") axis = t[1];
      continue;
    }
    if (t[0] === "index") {
      indexShow = true;
      for (let i = 1; i < t.length; i++) {
        const mod = t[i];
        if (mod === "bottom" || mod === "after") {
          indexSide = "after";
        } else if (mod === "top" || mod === "before") {
          indexSide = "before";
        } else if (mod === "reverse") {
          indexOrder = "reverse";
        } else if (mod === "normal") {
          indexOrder = "normal";
        } else if (mod === "range") {
          const next = t[i + 1];
          if (next) {
            const m = next.match(/^(-?\d+)\.\.(-?\d+)$/);
            if (m) {
              indexRange = { start: Number(m[1]), end: Number(m[2]) };
              i++;
            }
          }
        } else if (mod === "pad") {
          const next = t[i + 1];
          if (next && /^\d+$/.test(next)) {
            indexPad = Number(next);
            i++;
          }
        } else {
          const mRange = mod.match(/^(-?\d+)\.\.(-?\d+)$/);
          if (mRange) {
            indexRange = { start: Number(mRange[1]), end: Number(mRange[2]) };
            continue;
          }
          const mPad = mod.match(/^pad(\d+)$/);
          if (mPad) {
            indexPad = Number(mPad[1]);
            continue;
          }
        }
      }
      continue;
    }
    if (t[0] === "range") {
      indexShow = true;
      const token = t[1] === "range" ? t[2] : t[1];
      if (token) {
        const m = token.match(/^(-?\d+)\.\.(-?\d+)$/);
        if (m) indexRange = { start: Number(m[1]), end: Number(m[2]) };
      } else if (t.length >= 3 && /^-?\d+$/.test(t[1]) && /^-?\d+$/.test(t[2])) {
        indexRange = { start: Number(t[1]), end: Number(t[2]) };
      }
      continue;
    }
    if (t[0] === "pad" && t[1] && /^\d+$/.test(t[1])) {
      indexPad = Number(t[1]);
      continue;
    }
    if (t[0] === "highlight") {
      highlights = t.slice(1).map(Number).filter((n) => !isNaN(n) && Number.isInteger(n));
      continue;
    }
    if (t[0] === "window" && t[1]) {
      const m = t[1].match(/^(-?\d+)(?:-|\.\.)(-?\d+)$/);
      if (m) win = { start: Number(m[1]), end: Number(m[2]) };
      continue;
    }
    if (t[0] === "ptr" && t.length >= 4 && t[2] === "->") {
      ptrs.push({
        id: t[1],
        target: parsePointerTarget(t[3]),
        ...t[4] !== void 0 ? { label: t[4] } : {}
      });
    }
  }
  return {
    ...title !== void 0 ? { title } : {},
    axis,
    index: {
      show: indexShow,
      side: indexSide,
      order: indexOrder,
      ...indexRange !== void 0 ? { range: indexRange } : {},
      ...indexPad !== void 0 ? { pad: indexPad } : {}
    },
    cells: cells2,
    ptrs,
    ...highlights !== void 0 && highlights.length > 0 ? { highlights } : {},
    ...win !== void 0 ? { window: win } : {}
  };
}
function parseCells(tokens) {
  const cells2 = tokens.map(
    (token) => token === "..." ? { kind: "gap" } : { kind: "value", value: token }
  );
  const gapPositions = cells2.map((cell, i) => cell.kind === "gap" ? i : -1).filter((i) => i >= 0);
  if (gapPositions.length > 1) throw new Error("Array cells may contain at most one ... gap");
  if (gapPositions.length === 1) {
    const gap = gapPositions[0];
    if (gap === 0 || gap === cells2.length - 1)
      throw new Error("Array ... gap must be between concrete cells");
  }
  return cells2;
}
function parsePointerTarget(raw) {
  if (/^-?\d+$/.test(raw)) return { raw, value: Number(raw) };
  if (/^c-?\d+$/.test(raw) || raw === "cfirst" || raw === "clast" || raw === "cgap")
    return { raw, anchor: raw };
  return { raw, anchor: raw };
}
function metadataFor(doc) {
  const physicalToLogical = new Array(doc.cells.length).fill(null);
  const physicalLabels = new Array(doc.cells.length).fill(null);
  const indexToPhysical = /* @__PURE__ */ new Map();
  const nonGapPhysicals = [];
  let gapPhysical;
  doc.cells.forEach((cell, physical) => {
    if (cell.kind === "gap") {
      gapPhysical = physical;
    } else {
      nonGapPhysicals.push(physical);
    }
  });
  const logicalCount = nonGapPhysicals.length;
  const range = doc.index.range;
  const pad = doc.index.pad;
  if (range) {
    const { start, end } = range;
    if (start > end) {
      throw new Error(`Array index range start (${start}) cannot be greater than end (${end})`);
    }
    if (gapPhysical !== void 0) {
      const beforeGapCount = gapPhysical;
      const afterGapCount = doc.cells.length - 1 - gapPhysical;
      const concreteCount = beforeGapCount + afterGapCount;
      const rangeSpan = end - start + 1;
      if (rangeSpan < concreteCount + 1) {
        throw new Error(
          `Array index range ${start}..${end} is too small for ${concreteCount} concrete cells and gap`
        );
      }
      if (doc.index.order === "reverse") {
        for (let p = 0; p < beforeGapCount; p++) {
          const logical = end - p;
          physicalToLogical[p] = logical;
          indexToPhysical.set(logical, p);
          physicalLabels[p] = formatIndex(logical, pad);
        }
        const gapHigh = end - beforeGapCount;
        const gapLow = start + afterGapCount;
        const gapLabel = gapHigh === gapLow ? formatIndex(gapHigh, pad) : `${formatIndex(gapHigh, pad)}-${formatIndex(gapLow, pad)}`;
        physicalLabels[gapPhysical] = gapLabel;
        for (let p = gapPhysical + 1; p < doc.cells.length; p++) {
          const k = doc.cells.length - 1 - p;
          const logical = start + k;
          physicalToLogical[p] = logical;
          indexToPhysical.set(logical, p);
          physicalLabels[p] = formatIndex(logical, pad);
        }
      } else {
        for (let p = 0; p < beforeGapCount; p++) {
          const logical = start + p;
          physicalToLogical[p] = logical;
          indexToPhysical.set(logical, p);
          physicalLabels[p] = formatIndex(logical, pad);
        }
        const gapStart = start + beforeGapCount;
        const gapEnd = end - afterGapCount;
        const gapLabel = gapStart === gapEnd ? formatIndex(gapStart, pad) : `${formatIndex(gapStart, pad)}-${formatIndex(gapEnd, pad)}`;
        physicalLabels[gapPhysical] = gapLabel;
        for (let p = gapPhysical + 1; p < doc.cells.length; p++) {
          const k = doc.cells.length - 1 - p;
          const logical = end - k;
          physicalToLogical[p] = logical;
          indexToPhysical.set(logical, p);
          physicalLabels[p] = formatIndex(logical, pad);
        }
      }
    } else {
      const rangeSpan = end - start + 1;
      if (rangeSpan < doc.cells.length) {
        throw new Error(
          `Array index range ${start}..${end} is too small for ${doc.cells.length} cells`
        );
      }
      if (doc.index.order === "reverse") {
        doc.cells.forEach((_, physical) => {
          const logical = end - physical;
          physicalToLogical[physical] = logical;
          indexToPhysical.set(logical, physical);
          physicalLabels[physical] = formatIndex(logical, pad);
        });
      } else {
        doc.cells.forEach((_, physical) => {
          const logical = start + physical;
          physicalToLogical[physical] = logical;
          indexToPhysical.set(logical, physical);
          physicalLabels[physical] = formatIndex(logical, pad);
        });
      }
    }
  } else {
    nonGapPhysicals.forEach((physical, rank) => {
      const logical = doc.index.order === "reverse" ? logicalCount - 1 - rank : rank;
      physicalToLogical[physical] = logical;
      indexToPhysical.set(logical, physical);
      physicalLabels[physical] = formatIndex(logical, pad);
    });
  }
  return {
    physicalToLogical,
    physicalLabels,
    indexToPhysical,
    nonGapPhysicals,
    ...gapPhysical !== void 0 ? { gapPhysical } : {}
  };
}
function resolveArrayElementAnchorId(doc, elementIndex) {
  const meta = metadataFor(normalizeArrayDoc(doc));
  const logicalCount = meta.nonGapPhysicals.length;
  if (elementIndex < 0) {
    const fromEnd = Math.abs(elementIndex);
    if (fromEnd < 1 || fromEnd > logicalCount) return void 0;
    if (fromEnd === 1) return meta.nonGapPhysicals.length > 0 ? "clast" : void 0;
    const targetPhysical = meta.nonGapPhysicals[meta.nonGapPhysicals.length - fromEnd];
    const logical = meta.physicalToLogical[targetPhysical];
    return logical !== null && logical !== void 0 ? `c${logical}` : `c${logicalCount - fromEnd}`;
  }
  if (doc.cells[elementIndex]?.kind === "gap") return "cgap";
  return meta.indexToPhysical.has(elementIndex) ? `c${elementIndex}` : void 0;
}
function layoutArray(inputDoc, theme) {
  const doc = normalizeArrayDoc(inputDoc);
  const { palette, typography, spacing } = theme;
  const p = pen(theme);
  const margin = spacing.diagramMargin;
  const font = typography.baseFontSize;
  const smallFont = typography.smallFontSize;
  const cellH = 40;
  const meta = metadataFor(doc);
  const cellW = Math.max(
    40,
    ...doc.cells.map((c) => measureText(c.kind === "gap" ? "\u2026" : c.value, font).width + 24),
    ...doc.index.show ? meta.physicalLabels.filter((l) => Boolean(l)).map((l) => measureText(l, smallFont).width + 16) : []
  );
  const titleH = doc.title ? typography.titleFontSize + 14 : 0;
  const indexBand = doc.index.show ? smallFont + 12 : 0;
  const arrowLen = 30;
  const labelGap = 6;
  const indexPad = 6;
  const horizontal = doc.axis === "horizontal";
  const resolvedPtrs = doc.ptrs.map((ptr) => {
    const physical = resolvePointerPhysical(ptr.target, doc, meta);
    return physical === void 0 ? null : { ptr, physical };
  }).filter((ptr) => ptr !== null);
  const labelFont = font;
  const labelLaneGap = labelFont + 8;
  const pointerSide = doc.index.side === "before" ? "after" : "before";
  const horizontalLanes = horizontal ? assignHorizontalPointerLanes(resolvedPtrs, doc, meta, cellW, labelFont) : [];
  const pointerLaneCount = Math.max(0, ...horizontalLanes.map((l) => l + 1));
  const pointerBand = resolvedPtrs.length === 0 ? 0 : horizontal ? arrowLen + labelGap + labelFont + Math.max(0, pointerLaneCount - 1) * labelLaneGap + 4 : arrowLen + labelGap + maxPointerLabelWidth(resolvedPtrs, labelFont) + 8;
  const topBand = horizontal ? (doc.index.show && doc.index.side === "before" ? indexBand : 0) + (pointerSide === "before" ? pointerBand : 0) : 0;
  const maxIndexLabelW = doc.index.show && !horizontal ? Math.max(
    smallFont * 2,
    ...meta.physicalLabels.filter((l) => Boolean(l)).map((l) => measureText(l, smallFont).width)
  ) : smallFont * 2;
  const leftBand = !horizontal ? (doc.index.show && doc.index.side === "before" ? maxIndexLabelW + indexPad : 0) + (pointerSide === "before" ? pointerBand : 0) : 0;
  const origin = {
    x: leftBand,
    y: titleH + topBand
  };
  const slots = buildSlots(doc.cells.length, origin, cellW, cellH, doc.axis);
  const elements = [];
  if (doc.title) {
    elements.push(
      p.text(
        doc.title,
        origin.x,
        typography.titleFontSize,
        typography.titleFontSize,
        palette.text,
        { weight: "bold" }
      )
    );
  }
  doc.cells.forEach((cell, physical) => {
    const slot = slots[physical];
    if (cell.kind === "gap") {
      elements.push(p.rect(slot, palette.surface, palette.border, 1.5, { rx: 3, opacity: 0.45 }));
      elements.push(
        p.text("\u2026", slot.x + cellW / 2, slot.y + cellH / 2 + 5, font, palette.textMuted, {
          anchor: "middle",
          weight: "bold"
        })
      );
    } else {
      const logicalMaybe = meta.physicalToLogical[physical];
      const logical = logicalMaybe ?? null;
      const isHighlit = logical !== null && ((doc.highlights?.includes(logical) ?? false) || doc.window !== void 0 && logical >= doc.window.start && logical <= doc.window.end);
      if (isHighlit) {
        elements.push(
          p.rect(slot, palette.primary, palette.primary, 2, { rx: 3, fillOpacity: 0.22 })
        );
      } else {
        elements.push(p.rect(slot, palette.surface, palette.border, 1.5, { rx: 3 }));
      }
      elements.push(
        p.text(
          cell.value,
          slot.x + cellW / 2,
          slot.y + cellH / 2 + 5,
          font,
          isHighlit ? palette.primary : palette.text,
          { anchor: "middle", weight: "bold" }
        )
      );
    }
    if (doc.index.show) {
      const label = meta.physicalLabels[physical];
      if (label !== null && label !== void 0) {
        elements.push(
          renderIndexText(
            p,
            theme,
            label,
            slot,
            cellW,
            cellH,
            doc.axis,
            doc.index.side,
            indexPad
          )
        );
      }
    }
  });
  if (horizontal) {
    renderHorizontalPointers(
      elements,
      p,
      theme,
      resolvedPtrs,
      slots,
      pointerSide,
      horizontalLanes,
      arrowLen,
      labelGap,
      labelLaneGap
    );
  } else {
    renderVerticalPointers(
      elements,
      p,
      theme,
      resolvedPtrs,
      slots,
      doc.index.side,
      arrowLen,
      labelGap
    );
  }
  const anchors = {};
  meta.indexToPhysical.forEach((physical, logical) => {
    anchors[`c${logical}`] = { bounds: slots[physical] };
  });
  if (meta.nonGapPhysicals.length > 0) {
    anchors.cfirst = { bounds: slots[meta.nonGapPhysicals[0]] };
    anchors.clast = { bounds: slots[meta.nonGapPhysicals[meta.nonGapPhysicals.length - 1]] };
  }
  if (meta.gapPhysical !== void 0) {
    anchors.cgap = { bounds: slots[meta.gapPhysical] };
  }
  const contentBounds = boundsForElements(elements);
  const dx = margin - contentBounds.x;
  const dy = margin - contentBounds.y;
  const shiftedElements = translateElements(elements, dx, dy);
  const shiftedAnchors = translateAnchors(anchors, dx, dy);
  const scene = {
    viewBox: {
      x: 0,
      y: 0,
      width: rhu(contentBounds.width + margin * 2),
      height: rhu(contentBounds.height + margin * 2)
    },
    background: palette.background,
    elements: shiftedElements,
    defs: [arrowDef(palette.primary, theme.edges?.arrowSize)]
  };
  return { scene, anchors: shiftedAnchors };
}
function normalizeArrayDoc(doc) {
  const cells2 = (doc.cells ?? []).map(
    (cell) => typeof cell === "string" ? cell === "..." ? { kind: "gap" } : { kind: "value", value: cell } : cell
  );
  const index = typeof doc.index === "boolean" ? { show: doc.index, side: "before", order: "normal" } : {
    show: Boolean(doc.index?.show),
    side: doc.index?.side === "after" ? "after" : "before",
    order: doc.index?.order === "reverse" ? "reverse" : "normal",
    ...doc.index?.range !== void 0 ? { range: doc.index.range } : {},
    ...doc.index?.pad !== void 0 ? { pad: doc.index.pad } : {}
  };
  return {
    ...doc.title !== void 0 ? { title: doc.title } : {},
    axis: doc.axis === "vertical" ? "vertical" : "horizontal",
    index,
    cells: cells2,
    ptrs: (doc.ptrs ?? []).map((ptr) => {
      if ("idx" in ptr)
        return { id: ptr.name ?? ptr.id, target: { raw: String(ptr.idx), value: Number(ptr.idx) } };
      return ptr;
    }),
    ...Array.isArray(doc.highlights) && doc.highlights.length > 0 ? { highlights: doc.highlights } : {},
    ...doc.window !== void 0 ? { window: doc.window } : {}
  };
}
function buildSlots(count, origin, cellW, cellH, axis) {
  return Array.from({ length: count }, (_, i) => ({
    x: origin.x + (axis === "horizontal" ? i * cellW : 0),
    y: origin.y + (axis === "horizontal" ? 0 : i * cellH),
    width: cellW,
    height: cellH
  }));
}
function resolvePointerPhysical(target, doc, meta) {
  if (target.value !== void 0) {
    const physical = meta.indexToPhysical.get(target.value);
    if (physical !== void 0) return physical;
    if (doc.cells[target.value]?.kind === "gap") return meta.gapPhysical;
    return void 0;
  }
  if (!target.anchor) return void 0;
  if (target.anchor === "cfirst") return meta.nonGapPhysicals[0];
  if (target.anchor === "clast") return meta.nonGapPhysicals[meta.nonGapPhysicals.length - 1];
  if (target.anchor === "cgap") return meta.gapPhysical;
  const cMatch = target.anchor.match(/^c(\d+)$/);
  if (cMatch) return meta.indexToPhysical.get(Number(cMatch[1]));
  return void 0;
}
function renderIndexText(p, theme, value, slot, cellW, cellH, axis, side, indexPad) {
  const { palette, typography } = theme;
  if (axis === "horizontal") {
    const x2 = slot.x + cellW / 2;
    const y = side === "before" ? slot.y - 6 : slot.y + cellH + typography.smallFontSize + 4;
    return p.text(value, x2, y, typography.smallFontSize, palette.textMuted, { anchor: "middle" });
  }
  const centerY = slot.y + cellH / 2;
  const x = side === "before" ? slot.x - indexPad : slot.x + cellW + indexPad;
  const anchor = side === "before" ? "end" : "start";
  return p.text(value, x, centerY + 4, typography.smallFontSize, palette.textMuted, { anchor });
}
function assignHorizontalPointerLanes(ptrs, doc, meta, cellW, font) {
  const intervals = ptrs.map((ptr, ptrIndex) => {
    const cx = slotCenterX(ptr.physical, cellW);
    const text = pointerLabel(ptr.ptr);
    const width = measureText(text, font).width + 8;
    return { ptrIndex, start: cx - width / 2, end: cx + width / 2 };
  });
  void doc;
  void meta;
  return colorIntervals(intervals);
}
function colorIntervals(intervals) {
  const lanes = new Array(intervals.length).fill(0);
  const laneEnds = [];
  const sorted = [...intervals].sort((a, b) => a.start - b.start || a.end - b.end);
  for (const interval of sorted) {
    let lane = 0;
    while (laneEnds[lane] !== void 0 && interval.start < laneEnds[lane]) lane++;
    lanes[interval.ptrIndex] = lane;
    laneEnds[lane] = interval.end;
  }
  return lanes;
}
function slotCenterX(physical, cellW) {
  return physical * cellW + cellW / 2;
}
function renderHorizontalPointers(elements, p, theme, ptrs, slots, side, lanes, arrowLen, labelGap, laneGap) {
  const s = side === "after" ? 1 : -1;
  const { palette, typography } = theme;
  const targetCounts = countByPointerPhysical(ptrs);
  const seen = /* @__PURE__ */ new Map();
  ptrs.forEach((ptr, i) => {
    const slot = slots[ptr.physical];
    const count = targetCounts.get(ptr.physical) ?? 1;
    const ordinal = seen.get(ptr.physical) ?? 0;
    seen.set(ptr.physical, ordinal + 1);
    const attachX = slot.x + slot.width / 2 + centeredLaneOffset(ordinal, count, slot.width);
    const edgeY = s > 0 ? slot.y + slot.height : slot.y;
    const lane = lanes[i] ?? 0;
    const tipY = edgeY;
    const tailY = edgeY + s * (arrowLen + lane * laneGap);
    const labelY = tailY + s * (labelGap + (s > 0 ? typography.baseFontSize : 0));
    elements.push(
      p.path(
        `M ${rhu(attachX)} ${rhu(tailY)} L ${rhu(attachX)} ${rhu(tipY)}`,
        palette.primary,
        1.5,
        { markerEnd: ARROW_ID2 }
      )
    );
    elements.push(
      p.text(pointerLabel(ptr.ptr), attachX, labelY, typography.baseFontSize, palette.primary, {
        anchor: "middle",
        weight: "bold"
      })
    );
  });
}
function renderVerticalPointers(elements, p, theme, ptrs, slots, indexSide, arrowLen, labelGap) {
  const s = indexSide === "before" ? 1 : -1;
  const { palette, typography } = theme;
  const columnGap = labelGap + 6;
  const groups = /* @__PURE__ */ new Map();
  for (const ptr of ptrs) {
    const key = `${ptr.physical}:${s}`;
    const widths = groups.get(key) ?? [];
    widths.push(measureText(pointerLabel(ptr.ptr), typography.baseFontSize).width);
    groups.set(key, widths);
  }
  const seen = /* @__PURE__ */ new Map();
  ptrs.forEach((ptr) => {
    const slot = slots[ptr.physical];
    const key = `${ptr.physical}:${s}`;
    const widths = groups.get(key) ?? [];
    const lane = seen.get(key) ?? 0;
    seen.set(key, lane + 1);
    const attachY = slot.y + slot.height / 2 + centeredLaneOffset(lane, widths.length, slot.height);
    const edgeX = s > 0 ? slot.x + slot.width : slot.x;
    const outward = precedingLaneWidth(widths, lane, columnGap);
    const tailX = edgeX + s * (arrowLen + outward);
    const labelX = tailX + s * labelGap;
    const anchor = s > 0 ? "start" : "end";
    elements.push(
      p.path(
        `M ${rhu(tailX)} ${rhu(attachY)} L ${rhu(edgeX)} ${rhu(attachY)}`,
        palette.primary,
        1.5,
        { markerEnd: ARROW_ID2 }
      )
    );
    elements.push(
      p.text(pointerLabel(ptr.ptr), labelX, attachY + 4, typography.baseFontSize, palette.primary, {
        anchor,
        weight: "bold"
      })
    );
  });
}
function maxPointerLabelWidth(ptrs, font) {
  return Math.max(0, ...ptrs.map((ptr) => measureText(pointerLabel(ptr.ptr), font).width));
}
function countByPointerPhysical(ptrs) {
  const counts = /* @__PURE__ */ new Map();
  for (const ptr of ptrs) counts.set(ptr.physical, (counts.get(ptr.physical) ?? 0) + 1);
  return counts;
}
function centeredLaneOffset(lane, count, span) {
  if (count <= 1) return 0;
  const gap = Math.min(12, span / Math.max(2, count + 1));
  return (lane - (count - 1) / 2) * gap;
}
function precedingLaneWidth(widths, lane, gap) {
  let offset = 0;
  for (let i = 0; i < lane; i++) offset += (widths[i] ?? 0) + gap;
  return offset;
}
function boundsForElements(elements) {
  const bounds = elements.map(boundsForElement).filter((b) => b !== void 0);
  if (bounds.length === 0) return { x: 0, y: 0, width: 0, height: 0 };
  const minX = Math.min(...bounds.map((b) => b.x));
  const minY = Math.min(...bounds.map((b) => b.y));
  const maxX = Math.max(...bounds.map((b) => b.x + b.width));
  const maxY = Math.max(...bounds.map((b) => b.y + b.height));
  return { x: minX, y: minY, width: maxX - minX, height: maxY - minY };
}
function boundsForElement(element) {
  switch (element.type) {
    case "rect":
      return element.bounds;
    case "circle":
      return {
        x: element.center.x - element.radius,
        y: element.center.y - element.radius,
        width: element.radius * 2,
        height: element.radius * 2
      };
    case "path":
      return boundsForPath(element.d, element.strokeWidth);
    case "text":
      return boundsForText(element);
    case "group":
      return boundsForElements(element.children);
  }
}
function boundsForText(text) {
  const measured = measureText(text.content, text.fontSize);
  const x = text.anchor === "middle" ? text.position.x - measured.width / 2 : text.anchor === "end" ? text.position.x - measured.width : text.position.x;
  return {
    x,
    y: text.position.y - text.fontSize,
    width: measured.width,
    height: text.fontSize * 1.25
  };
}
function boundsForPath(d, strokeWidth) {
  const nums = [...d.matchAll(/-?\d+(?:\.\d+)?/g)].map((match) => Number(match[0]));
  if (nums.length < 2) return { x: 0, y: 0, width: 0, height: 0 };
  const xs = nums.filter((_, i) => i % 2 === 0);
  const ys = nums.filter((_, i) => i % 2 === 1);
  const pad = strokeWidth / 2;
  const minX = Math.min(...xs) - pad;
  const minY = Math.min(...ys) - pad;
  const maxX = Math.max(...xs) + pad;
  const maxY = Math.max(...ys) + pad;
  return { x: minX, y: minY, width: maxX - minX, height: maxY - minY };
}
function translateElements(elements, dx, dy) {
  return elements.map((element) => translateElement(element, dx, dy));
}
function translateElement(element, dx, dy) {
  switch (element.type) {
    case "rect":
      return { ...element, bounds: translateRect(element.bounds, dx, dy) };
    case "circle":
      return {
        ...element,
        center: { x: rhu(element.center.x + dx), y: rhu(element.center.y + dy) }
      };
    case "path":
      return { ...element, d: translatePathD(element.d, dx, dy) };
    case "text":
      return {
        ...element,
        position: { x: rhu(element.position.x + dx), y: rhu(element.position.y + dy) }
      };
    case "group":
      return { ...element, children: translateElements(element.children, dx, dy) };
    case "icon":
      return { ...element, x: rhu(element.x + dx), y: rhu(element.y + dy) };
  }
}
function translateAnchors(anchors, dx, dy) {
  return Object.fromEntries(
    Object.entries(anchors).map(([key, anchor]) => [
      key,
      { bounds: translateRect(anchor.bounds, dx, dy) }
    ])
  );
}
function translateRect(rect, dx, dy) {
  return { x: rhu(rect.x + dx), y: rhu(rect.y + dy), width: rect.width, height: rect.height };
}
function translatePathD(d, dx, dy) {
  let i = 0;
  return d.replace(/-?\d+(?:\.\d+)?/g, (value) => {
    const delta = i++ % 2 === 0 ? dx : dy;
    return String(rhu(Number(value) + delta));
  });
}
function pointerLabel(ptr) {
  return ptr.label ?? ptr.id;
}
var array = {
  parseMermaid(input) {
    return { version: "1.0", metadata: {}, ...parse(input) };
  },
  parseYaml(input) {
    const parsed = JSON.parse(input);
    return {
      version: parsed.version ?? "1.0",
      metadata: parsed.metadata ?? {},
      ...normalizeArrayDoc(parsed)
    };
  },
  layout(ir, theme) {
    return layoutArray(ir, theme);
  }
};
function layoutPoster(ir, theme) {
  const { spacing, palette, typography } = theme;
  const { grid, cells: cells2 } = ir;
  const unit = spacing.unit;
  const gap = grid.gap !== void 0 ? grid.gap : spacing.nodeGap / 2;
  const padding = spacing.diagramMargin;
  const headerH = ir.metadata.title ? typography.titleFontSize + unit * 2 : 0;
  const MIN_CELL_W = unit * 20;
  const MIN_CELL_H = unit * 15;
  const MAX_CELL_W = unit * 42;
  const MIN_EMBED_SCALE = 0.65;
  const scaleConfig = resolveScaleConfig(
    grid.scale,
    ir.metadata.scale,
    ir.metadata.uniform_scale ?? ir.metadata["uniform-scale"]
  );
  const targetScale = scaleConfig.mode === "natural" ? 1 : scaleConfig.mode === "fixed" ? scaleConfig.fixedScale ?? 1 : MIN_EMBED_SCALE;
  const positioned = assignPositions(cells2, grid.columns);
  const cellResults = positioned.map((cell) => {
    const cellTheme = cell.theme ? getThemePreset(cell.theme) : theme;
    return { cell, cellTheme, result: layoutCellContent(cell.content, cellTheme) };
  });
  const cellResultById = new Map(
    cellResults.map(({ cell, result }) => [cell.id ?? "", { cell, result }])
  );
  const numRows = grid.rows ?? Math.max(...positioned.map((c) => (c.row ?? 0) + (c.rowSpan ?? 1)));
  const colWidths = new Array(grid.columns).fill(MIN_CELL_W);
  for (const { cell, result } of cellResults) {
    if ((cell.colSpan ?? 1) === 1) {
      const col = cell.col ?? 0;
      const inset = unit / 2;
      const scaleForCol = cell.content.kind === "diagram" ? targetScale : 1;
      const needed = result.scene.viewBox.width * scaleForCol + inset * 2;
      colWidths[col] = Math.max(colWidths[col], needed);
    }
  }
  const rowHeights = new Array(numRows).fill(MIN_CELL_H);
  for (const { cell, cellTheme, result } of cellResults) {
    if ((cell.rowSpan ?? 1) === 1) {
      const row = cell.row ?? 0;
      const col = cell.col ?? 0;
      const cellTitleH = cell.title ? reservedTitleHeight(cellTheme, cell.title) : 0;
      const inset = unit / 2;
      const colW = colWidths[col] ?? MIN_CELL_W;
      const contentW = colW - inset * 2;
      const scale = Math.min(contentW / Math.max(result.scene.viewBox.width, 1), 1);
      const effectiveScale = scaleConfig.mode === "natural" ? 1 : scaleConfig.mode === "fixed" ? scaleConfig.fixedScale ?? 1 : Math.max(scale, MIN_EMBED_SCALE);
      const captionH = cell.caption ? reservedCaptionHeight(cellTheme) : 0;
      const neededH = result.scene.viewBox.height * effectiveScale + cellTitleH + captionH + inset * 2;
      rowHeights[row] = Math.max(rowHeights[row], neededH);
    }
  }
  let globalScale;
  if (scaleConfig.mode === "fixed") {
    globalScale = scaleConfig.fixedScale;
  } else if (scaleConfig.mode === "natural") {
    const diagramScales = [];
    for (const { cell, cellTheme, result } of cellResults) {
      if (cell.content.kind !== "diagram") continue;
      const col = cell.col ?? 0;
      const row = cell.row ?? 0;
      const colSpan = cell.colSpan ?? 1;
      const rowSpan = cell.rowSpan ?? 1;
      const cellW = sumWithGaps(colWidths, col, col + colSpan, gap) - gap;
      const cellH = sumWithGaps(rowHeights, row, row + rowSpan, gap) - gap;
      const reservedTop = cell.title ? reservedTitleHeight(cellTheme, cell.title) : 0;
      const captionH = cell.caption ? reservedCaptionHeight(cellTheme) : 0;
      const inset = unit / 2;
      const contentW = cellW - inset * 2;
      const contentH = cellH - reservedTop - captionH - inset * 2;
      const sx = contentW / Math.max(result.scene.viewBox.width, 1);
      const sy = contentH / Math.max(result.scene.viewBox.height, 1);
      diagramScales.push(Math.min(sx, sy, 1));
    }
    globalScale = diagramScales.length > 0 ? Math.min(1, ...diagramScales) : 1;
  } else if (scaleConfig.mode === "uniform") {
    const diagramScales = [];
    for (const { cell, cellTheme, result } of cellResults) {
      if (cell.content.kind !== "diagram") continue;
      const col = cell.col ?? 0;
      const row = cell.row ?? 0;
      const colSpan = cell.colSpan ?? 1;
      const rowSpan = cell.rowSpan ?? 1;
      const cellW = sumWithGaps(colWidths, col, col + colSpan, gap) - gap;
      const cellH = sumWithGaps(rowHeights, row, row + rowSpan, gap) - gap;
      const reservedTop = cell.title ? reservedTitleHeight(cellTheme, cell.title) : 0;
      const captionH = cell.caption ? reservedCaptionHeight(cellTheme) : 0;
      const inset = unit / 2;
      const contentW = cellW - inset * 2;
      const contentH = cellH - reservedTop - captionH - inset * 2;
      const sx = contentW / Math.max(result.scene.viewBox.width, 1);
      const sy = contentH / Math.max(result.scene.viewBox.height, 1);
      diagramScales.push(Math.min(sx, sy, 1));
    }
    globalScale = diagramScales.length > 0 ? Math.min(...diagramScales) : void 0;
  }
  const headerElements = [];
  const cellBg = [];
  const cellContent = [];
  const revealSteps = [];
  const textOccupied = [];
  const cellBorders = [];
  const cellRects = /* @__PURE__ */ new Map();
  const allOccupiedPorts = [];
  if (ir.metadata.title) {
    headerElements.push({
      type: "text",
      content: ir.metadata.title,
      position: { x: padding, y: padding + typography.titleFontSize },
      fontSize: typography.titleFontSize + 2,
      fontFamily: typography.fontFamily,
      fontWeight: "bold",
      fill: palette.text
    });
    const titleW = ir.metadata.title.length * (typography.titleFontSize + 2) * 0.6;
    const titleH = typography.titleFontSize + 2;
    textOccupied.push({ x: padding, y: padding, width: titleW, height: titleH + 4 });
  }
  const mergedAnchors = {};
  for (const { cell, cellTheme, result } of cellResults) {
    const cellPalette = cellTheme.palette;
    const col = cell.col ?? 0;
    const row = cell.row ?? 0;
    const colSpan = cell.colSpan ?? 1;
    const rowSpan = cell.rowSpan ?? 1;
    const cellId = cell.id ?? cellAddressFromPosition(row, col);
    const cellX = padding + sumWithGaps(colWidths, 0, col, gap);
    const cellY = padding + headerH + sumWithGaps(rowHeights, 0, row, gap);
    const cellW = sumWithGaps(colWidths, col, col + colSpan, gap) - gap;
    const cellH = sumWithGaps(rowHeights, row, row + rowSpan, gap) - gap;
    cellBg.push({
      type: "rect",
      bounds: { x: cellX, y: cellY, width: cellW, height: cellH },
      fill: cellPalette.background,
      stroke: cellPalette.border,
      strokeWidth: 1,
      rx: 6
    });
    const borderThick = 4;
    cellBorders.push(
      { x: cellX, y: cellY - borderThick / 2, width: cellW, height: borderThick },
      // top edge
      { x: cellX, y: cellY + cellH - borderThick / 2, width: cellW, height: borderThick },
      // bottom edge
      { x: cellX - borderThick / 2, y: cellY, width: borderThick, height: cellH },
      // left edge
      { x: cellX + cellW - borderThick / 2, y: cellY, width: borderThick, height: cellH }
      // right edge
    );
    cellRects.set(cellId, { x: cellX, y: cellY, width: cellW, height: cellH });
    const reservedTop = cell.title ? reservedTitleHeight(cellTheme, cell.title) : 0;
    if (cell.title) {
      const t = buildCellTitle(cell.title, cellX, cellY, cellW, cellTheme);
      cellContent.push(...t.elements);
      textOccupied.push(t.occupied);
    }
    const inset = unit / 2;
    const captionH = cell.caption ? reservedCaptionHeight(cellTheme) : 0;
    const contentRect = {
      x: cellX + inset,
      y: cellY + reservedTop + inset,
      width: cellW - inset * 2,
      height: cellH - reservedTop - captionH - inset * 2
    };
    const scaleX = contentRect.width / Math.max(result.scene.viewBox.width, 1);
    const scaleY = contentRect.height / Math.max(result.scene.viewBox.height, 1);
    const localScale = Math.min(scaleX, scaleY, 1);
    const scale = cell.content.kind === "diagram" && globalScale !== void 0 ? globalScale : localScale;
    const offsetX = contentRect.x + (contentRect.width - result.scene.viewBox.width * scale) / 2;
    const offsetY = contentRect.y + (contentRect.height - result.scene.viewBox.height * scale) / 2;
    let childScene = result.scene;
    if (result.reveal?.steps.length) {
      const prefixIds = (ids) => ids.map((id) => `${cellId}.${id}`);
      const prefixGroups = (element) => element.type === "group" ? { ...element, ...element.id ? { id: `${cellId}.${element.id}` } : {}, children: element.children.map(prefixGroups) } : element;
      childScene = { ...result.scene, elements: result.scene.elements.map(prefixGroups) };
      for (const step of result.reveal.steps) {
        revealSteps.push({
          ...step,
          index: revealSteps.length + 1,
          enter: prefixIds(step.enter),
          ...step.emphasize ? { emphasize: prefixIds(step.emphasize) } : {},
          ...step.exit ? { exit: prefixIds(step.exit) } : {}
        });
      }
    }
    cellContent.push(embedScene(childScene, contentRect, scale));
    if (cell.caption) {
      const captionY = cellY + cellH - inset / 2;
      cellContent.push({
        type: "text",
        content: cell.caption,
        position: { x: cellX + cellW / 2, y: captionY },
        fontSize: cellTheme.typography.smallFontSize,
        fontFamily: cellTheme.typography.fontFamily,
        fill: cellTheme.palette.textMuted,
        anchor: "middle"
      });
    }
    if (cell.notes && cell.notes.length > 0) {
      for (const note of cell.notes) {
        cellContent.push(...buildNoteOverlay(note, contentRect, cellTheme));
      }
    }
    for (const [nodeId, anchor] of Object.entries(result.anchors)) {
      const prefixedId = `${cellId}.${nodeId}`;
      const transformed = {
        bounds: {
          x: anchor.bounds.x * scale + offsetX,
          y: anchor.bounds.y * scale + offsetY,
          width: anchor.bounds.width * scale,
          height: anchor.bounds.height * scale
        },
        ...anchor.ports ? {
          ports: Object.fromEntries(
            Object.entries(anchor.ports).map(([side, pt]) => [
              side,
              { x: pt.x * scale + offsetX, y: pt.y * scale + offsetY }
            ])
          )
        } : {}
      };
      mergedAnchors[prefixedId] = transformed;
    }
    for (const op of result.occupiedPorts ?? []) {
      allOccupiedPorts.push({ ...op, nodeKey: `${cellId}.${op.nodeKey}` });
    }
    for (const cr of result.chromeRects ?? []) {
      textOccupied.push({
        x: cr.x * scale + offsetX,
        y: cr.y * scale + offsetY,
        width: cr.width * scale,
        height: cr.height * scale
      });
    }
  }
  const totalW = padding * 2 + sumWithGaps(colWidths, 0, grid.columns, gap) - gap;
  const totalH = padding * 2 + headerH + sumWithGaps(rowHeights, 0, numRows, gap) - gap;
  const allDefs = [];
  const seenDefs = /* @__PURE__ */ new Set();
  for (const { result } of cellResults) {
    if (result.scene.defs) {
      for (const def of result.scene.defs) {
        if (!seenDefs.has(def)) {
          seenDefs.add(def);
          allDefs.push(def);
        }
      }
    }
  }
  const links2 = normalizeElementIndexLinks(ir.links ?? [], cellResultById);
  let finalW = totalW;
  let finalH = totalH;
  if (links2.length > 0) {
    const connectorSpecs = crossLinksToConnectorSpecs(
      links2.map((link) => {
        const explicitCurve = link.curveStyle ?? (typeof link.props?.curveStyle === "string" ? link.props.curveStyle : void 0);
        return explicitCurve ? link : { ...link, curveStyle: "cardinal" };
      })
    );
    const connectorResult = routeConnectors({
      anchors: mergedAnchors,
      connectors: connectorSpecs,
      theme,
      occupiedPorts: allOccupiedPorts,
      occupiedTextRects: textOccupied,
      routingObstacles: cellBorders,
      containerObstacles: cellRects
    });
    const linkDefs = connectorResult.defs;
    const linkElements = connectorResult.elements;
    for (const def of linkDefs) {
      if (!seenDefs.has(def)) {
        seenDefs.add(def);
        allDefs.push(def);
      }
    }
    const linkPaths = linkElements.filter((e) => e.type !== "text");
    const linkLabels = linkElements.filter((e) => e.type === "text");
    const ext = connectorResult.extents;
    if (ext.maxX > finalW) finalW = ext.maxX + padding;
    if (ext.maxY > finalH) finalH = ext.maxY + padding;
    const elements2 = [
      ...headerElements,
      ...cellBg,
      ...linkPaths,
      ...cellContent,
      ...linkLabels
    ];
    return {
      scene: {
        viewBox: { x: 0, y: 0, width: finalW, height: finalH },
        background: palette.background,
        elements: elements2,
        ...allDefs.length > 0 ? { defs: allDefs } : {}
      },
      anchors: mergedAnchors,
      ...revealSteps.length > 0 ? { reveal: { steps: revealSteps } } : {}
    };
  }
  const elements = [...headerElements, ...cellBg, ...cellContent];
  return {
    scene: {
      viewBox: { x: 0, y: 0, width: finalW, height: finalH },
      background: palette.background,
      elements,
      ...allDefs.length > 0 ? { defs: allDefs } : {}
    },
    anchors: mergedAnchors,
    ...revealSteps.length > 0 ? { reveal: { steps: revealSteps } } : {}
  };
}
function normalizeElementIndexLinks(links2, cellResultById) {
  return links2.map((link) => ({
    ...link,
    from: normalizeElementIndexAddress(link.from, cellResultById),
    to: normalizeElementIndexAddress(link.to, cellResultById)
  }));
}
function normalizeElementIndexAddress(address, cellResultById) {
  if (address.elementIndex === void 0) return address;
  const cellId = address.cellPath.join(".");
  const cellResult = cellResultById.get(cellId);
  const content = cellResult?.cell.content;
  const arrayNodeId = content?.kind === "diagram" && content.diagramKind === "array" ? resolveArrayElementAnchorId(content.doc, address.elementIndex) : fallbackElementAnchorId(cellResult?.result.anchors ?? {}, address.elementIndex);
  return {
    ...address,
    nodeId: arrayNodeId ?? `__missing_element_${address.elementIndex}`
  };
}
function fallbackElementAnchorId(anchors, elementIndex) {
  if (elementIndex < 0) {
    if (elementIndex === -1 && anchors.clast) return "clast";
    return void 0;
  }
  return anchors[`c${elementIndex}`] ? `c${elementIndex}` : void 0;
}
function layoutCellContent(content, theme) {
  const { palette, typography, spacing } = theme;
  const unit = spacing.unit;
  const pad = spacing.nodePadding;
  switch (content.kind) {
    case "diagram": {
      const module = getModule(content.diagramKind);
      if (!module) {
        const w = unit * 20;
        const h2 = typography.baseFontSize + pad * 2;
        return {
          scene: {
            viewBox: { x: 0, y: 0, width: w, height: h2 },
            elements: [
              {
                type: "text",
                content: `[${content.diagramKind}]`,
                position: { x: pad, y: pad + typography.baseFontSize * 0.8 },
                fontSize: typography.baseFontSize,
                fontFamily: typography.fontFamily,
                fill: palette.textMuted
              }
            ]
          },
          anchors: {}
        };
      }
      return module.layout(content.doc, theme);
    }
    case "text": {
      const estCharW = typography.baseFontSize * 0.6;
      const textW = Math.max(
        unit * 10,
        Math.min(unit * 30, content.text.length * estCharW + pad * 2)
      );
      const textH = typography.baseFontSize * typography.lineHeight + pad * 2;
      return {
        scene: {
          viewBox: { x: 0, y: 0, width: textW, height: textH },
          elements: [
            {
              type: "text",
              content: content.text,
              position: { x: pad, y: pad + typography.baseFontSize * 0.8 },
              fontSize: typography.baseFontSize,
              fontFamily: typography.fontFamily,
              fill: palette.text
            }
          ]
        },
        anchors: {}
      };
    }
    case "stat": {
      const valueFontSize = typography.titleFontSize * 1.5;
      const cellW = Math.max(unit * 10, valueFontSize * 3);
      const centerX = cellW / 2;
      const valueY = pad + valueFontSize * 0.8;
      const labelGap = unit;
      const labelY = valueY + labelGap + typography.smallFontSize * 0.8;
      const cellH = content.label ? valueY + labelGap + typography.smallFontSize + pad : valueY + pad;
      const els = [
        {
          type: "text",
          content: content.value,
          position: { x: centerX, y: valueY },
          fontSize: valueFontSize,
          fontFamily: typography.fontFamily,
          fontWeight: "bold",
          fill: palette.primary,
          anchor: "middle"
        }
      ];
      if (content.label) {
        els.push({
          type: "text",
          content: content.label,
          position: { x: centerX, y: labelY },
          fontSize: typography.smallFontSize,
          fontFamily: typography.fontFamily,
          fill: palette.textMuted,
          anchor: "middle"
        });
      }
      return {
        scene: { viewBox: { x: 0, y: 0, width: cellW, height: cellH }, elements: els },
        anchors: {}
      };
    }
  }
}
function resolveScaleConfig(gridScale, metaScale, metaUniformScale) {
  const raw = gridScale ?? metaScale ?? (metaUniformScale ? "uniform" : void 0);
  if (raw === void 0 || raw === null || raw === "" || raw === "contain" || raw === "none" || raw === "false") {
    return { mode: "contain" };
  }
  if (typeof raw === "number") {
    return raw === 1 ? { mode: "natural" } : { mode: "fixed", fixedScale: raw };
  }
  if (typeof raw === "boolean") {
    return raw ? { mode: "uniform" } : { mode: "contain" };
  }
  const str = String(raw).trim().toLowerCase();
  if (str === "natural" || str === "1" || str === "1.0" || str === "100%") {
    return { mode: "natural" };
  }
  if (str === "uniform" || str === "homologate" || str === "true" || str === "same") {
    return { mode: "uniform" };
  }
  if (str === "contain" || str === "independent" || str === "fit") {
    return { mode: "contain" };
  }
  const parsedNum = parseFloat(str.endsWith("%") ? str.slice(0, -1) : str);
  if (!isNaN(parsedNum) && parsedNum > 0) {
    const val = str.endsWith("%") ? parsedNum / 100 : parsedNum;
    return val === 1 ? { mode: "natural" } : { mode: "fixed", fixedScale: val };
  }
  return { mode: "contain" };
}
function embedScene(scene, into, overrideScale) {
  const scaleX = into.width / Math.max(scene.viewBox.width, 1);
  const scaleY = into.height / Math.max(scene.viewBox.height, 1);
  const scale = overrideScale !== void 0 ? overrideScale : Math.min(scaleX, scaleY, 1);
  const offsetX = into.x + (into.width - scene.viewBox.width * scale) / 2;
  const offsetY = into.y + (into.height - scene.viewBox.height * scale) / 2;
  return {
    type: "group",
    transform: `translate(${offsetX}, ${offsetY}) scale(${scale})`,
    children: scene.elements
  };
}
function parseCellTitle(raw) {
  const sepIdx = raw.indexOf("::");
  if (sepIdx !== -1) {
    return [
      { text: raw.slice(0, sepIdx).trim(), isSubtitle: false },
      { text: raw.slice(sepIdx + 2).trim(), isSubtitle: true }
    ];
  }
  const parts = raw.split(/<br\s*\/?>|\n|\\n/g).map((s) => s.trim()).filter(Boolean);
  if (parts.length > 1) {
    return parts.map((p, i) => ({ text: p, isSubtitle: i > 0 }));
  }
  return [{ text: raw.trim(), isSubtitle: false }];
}
function reservedTitleHeight(theme, title) {
  const { spacing, panel } = theme;
  const boxH = titleBoxHeight(theme, title);
  switch (panel.titlePosition) {
    case "above":
      return 0;
    case "on-border":
      return boxH / 2 + spacing.unit * 0.5;
    case "inside":
    default:
      return boxH + spacing.unit * 0.5;
  }
}
function titleBoxHeight(theme, title) {
  const fs = theme.typography.baseFontSize;
  const padY = theme.panel.titleChrome === "none" ? 0 : theme.spacing.unit * 0.4;
  if (!title) return fs + padY * 2;
  const lines2 = parseCellTitle(title);
  if (lines2.length <= 1) return fs + padY * 2;
  const subtitleFs = theme.typography.smallFontSize;
  return fs + (lines2.length - 1) * (subtitleFs * 1.35) + padY * 2;
}
function reservedCaptionHeight(theme) {
  return theme.typography.smallFontSize + theme.spacing.unit;
}
function buildNoteOverlay(note, rect, theme) {
  const { palette, typography, spacing } = theme;
  const fs = typography.smallFontSize;
  const pad = spacing.unit * 0.5;
  const textW = note.text.length * fs * 0.62 + pad * 2;
  const boxH = fs + pad * 2;
  const pos = note.position ?? "top-right";
  let bx, by;
  switch (pos) {
    case "top-left":
      bx = rect.x + spacing.unit * 0.5;
      by = rect.y + spacing.unit * 0.5;
      break;
    case "top-right":
      bx = rect.x + rect.width - textW - spacing.unit * 0.5;
      by = rect.y + spacing.unit * 0.5;
      break;
    case "bottom-left":
      bx = rect.x + spacing.unit * 0.5;
      by = rect.y + rect.height - boxH - spacing.unit * 0.5;
      break;
    case "bottom-right":
      bx = rect.x + rect.width - textW - spacing.unit * 0.5;
      by = rect.y + rect.height - boxH - spacing.unit * 0.5;
      break;
    case "center":
      bx = rect.x + (rect.width - textW) / 2;
      by = rect.y + (rect.height - boxH) / 2;
      break;
    default:
      bx = rect.x + rect.width - textW - spacing.unit * 0.5;
      by = rect.y + spacing.unit * 0.5;
  }
  return [
    {
      type: "rect",
      bounds: { x: bx, y: by, width: textW, height: boxH },
      fill: palette.surface,
      stroke: palette.primary,
      strokeWidth: 1,
      rx: boxH / 2,
      fillOpacity: 0.88
    },
    {
      type: "text",
      content: note.text,
      position: { x: bx + textW / 2, y: by + pad + fs * 0.8 },
      fontSize: fs,
      fontFamily: typography.fontFamily,
      fontWeight: "bold",
      fill: palette.primary,
      anchor: "middle"
    }
  ];
}
function buildCellTitle(title, cellX, cellY, cellW, theme) {
  const { palette, typography, panel, spacing } = theme;
  const unit = spacing.unit;
  const fs = typography.baseFontSize;
  const smallFs = typography.smallFontSize;
  const padX = panel.titleChrome === "none" ? 0 : unit * 0.75;
  const padY = panel.titleChrome === "none" ? 0 : unit * 0.4;
  const lines2 = parseCellTitle(title);
  const maxTw = Math.max(
    ...lines2.map((l) => measureText(l.text, l.isSubtitle ? smallFs : fs).width)
  );
  const boxW = maxTw + padX * 2;
  const lineSpacing = smallFs * 1.35;
  const boxH = lines2.length <= 1 ? fs + padY * 2 : fs + (lines2.length - 1) * lineSpacing + padY * 2;
  const wall = unit;
  let boxX;
  let anchorX;
  let anchor;
  if (panel.titleAlign === "center") {
    boxX = cellX + cellW / 2 - boxW / 2;
    anchorX = cellX + cellW / 2;
    anchor = "middle";
  } else if (panel.titleAlign === "right") {
    boxX = cellX + cellW - wall - boxW;
    anchorX = boxX + boxW - padX;
    anchor = "end";
  } else {
    boxX = cellX + wall;
    anchorX = boxX + padX;
    anchor = "start";
  }
  let boxTop;
  if (panel.titlePosition === "on-border") {
    boxTop = cellY - boxH / 2;
  } else if (panel.titlePosition === "above") {
    boxTop = cellY - boxH - unit * 0.25;
  } else {
    boxTop = cellY + unit * 0.5;
  }
  const elements = [];
  if (panel.titleChrome !== "none") {
    const rx = panel.titleChrome === "pill" ? boxH / 2 : Math.min(6, boxH / 3);
    elements.push({
      type: "rect",
      bounds: { x: boxX, y: boxTop, width: boxW, height: boxH },
      fill: palette.surface,
      stroke: palette.border,
      strokeWidth: 1,
      rx
    });
  }
  for (let i = 0; i < lines2.length; i++) {
    const line = lines2[i];
    const lineFs = line.isSubtitle ? smallFs : fs;
    const lineBaselineY = boxTop + padY + fs * 0.8 + i * lineSpacing;
    elements.push({
      type: "text",
      content: line.text,
      position: { x: anchorX, y: lineBaselineY },
      fontSize: lineFs,
      fontFamily: typography.fontFamily,
      fontWeight: line.isSubtitle ? "normal" : "bold",
      fill: line.isSubtitle ? palette.textMuted : palette.text,
      anchor
    });
  }
  return { elements, occupied: { x: boxX, y: boxTop, width: boxW, height: boxH } };
}
function assignPositions(cells2, columns) {
  const occupied = /* @__PURE__ */ new Set();
  const key = (r, c) => `${r},${c}`;
  const mark = (r, c, rs, cs) => {
    for (let rr = r; rr < r + rs; rr++)
      for (let cc = c; cc < c + cs; cc++) occupied.add(key(rr, cc));
  };
  const fits = (r, c, rs, cs) => {
    if (c + cs > columns) return false;
    for (let rr = r; rr < r + rs; rr++)
      for (let cc = c; cc < c + cs; cc++) if (occupied.has(key(rr, cc))) return false;
    return true;
  };
  let row = 0, col = 0;
  return cells2.map((cell) => {
    const cs = Math.min(cell.colSpan ?? 1, columns);
    const rs = cell.rowSpan ?? 1;
    if (cell.row !== void 0 && cell.col !== void 0) {
      mark(cell.row, cell.col, rs, cs);
      return cell;
    }
    while (!fits(row, col, rs, cs)) {
      col++;
      if (col >= columns) {
        col = 0;
        row++;
      }
    }
    const assigned = { ...cell, row, col };
    mark(row, col, rs, cs);
    col += cs;
    if (col >= columns) {
      col = 0;
      row++;
    }
    return assigned;
  });
}
function sumWithGaps(sizes, start, end, gap) {
  let total = 0;
  for (let i = start; i < end; i++) total += (sizes[i] ?? 0) + gap;
  return total;
}
function cellAddressFromPosition(row, col) {
  let addr = "";
  let c = col;
  do {
    addr = String.fromCharCode(65 + c % 26) + addr;
    c = Math.floor(c / 26) - 1;
  } while (c >= 0);
  return `${addr}${row + 1}`;
}
var cells = [];
var links = [];
var cellCounter = 0;
function slugify4(text) {
  if (!text) return "";
  return text.toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-|-$/g, "") || "";
}
function addCell(explicitId, title, kind, rawContent, span, theme) {
  cellCounter++;
  const id = explicitId || slugify4(title) || `cell-${cellCounter}`;
  cells.push({ id, explicitId: explicitId || void 0, title: title || void 0, kind, rawContent: rawContent.trim(), span, theme: theme || void 0 });
}
function addLink(from, arrow, to, label, props) {
  links.push({ from, ...arrow, to, label: label || void 0, ...props ? props : {} });
}
function reset3() {
  cells.length = 0;
  links.length = 0;
  cellCounter = 0;
}
function peg$subclass3(child, parent) {
  function C() {
    this.constructor = child;
  }
  C.prototype = parent.prototype;
  child.prototype = new C();
}
function peg$SyntaxError3(message, expected, found, location) {
  var self = Error.call(this, message);
  if (Object.setPrototypeOf) {
    Object.setPrototypeOf(self, peg$SyntaxError3.prototype);
  }
  self.expected = expected;
  self.found = found;
  self.location = location;
  self.name = "SyntaxError";
  return self;
}
peg$subclass3(peg$SyntaxError3, Error);
function peg$padEnd3(str, targetLength, padString) {
  padString = padString || " ";
  if (str.length > targetLength) {
    return str;
  }
  targetLength -= str.length;
  padString += padString.repeat(targetLength);
  return str + padString.slice(0, targetLength);
}
peg$SyntaxError3.prototype.format = function(sources) {
  var str = "Error: " + this.message;
  if (this.location) {
    var src = null;
    var k;
    for (k = 0; k < sources.length; k++) {
      if (sources[k].source === this.location.source) {
        src = sources[k].text.split(/\r\n|\n|\r/g);
        break;
      }
    }
    var s = this.location.start;
    var offset_s = this.location.source && typeof this.location.source.offset === "function" ? this.location.source.offset(s) : s;
    var loc = this.location.source + ":" + offset_s.line + ":" + offset_s.column;
    if (src) {
      var e = this.location.end;
      var filler = peg$padEnd3("", offset_s.line.toString().length, " ");
      var line = src[s.line - 1];
      var last = s.line === e.line ? e.column : line.length + 1;
      var hatLen = last - s.column || 1;
      str += "\n --> " + loc + "\n" + filler + " |\n" + offset_s.line + " | " + line + "\n" + filler + " | " + peg$padEnd3("", s.column - 1, " ") + peg$padEnd3("", hatLen, "^");
    } else {
      str += "\n at " + loc;
    }
  }
  return str;
};
peg$SyntaxError3.buildMessage = function(expected, found) {
  var DESCRIBE_EXPECTATION_FNS = {
    literal: function(expectation) {
      return '"' + literalEscape(expectation.text) + '"';
    },
    class: function(expectation) {
      var escapedParts = expectation.parts.map(function(part) {
        return Array.isArray(part) ? classEscape(part[0]) + "-" + classEscape(part[1]) : classEscape(part);
      });
      return "[" + (expectation.inverted ? "^" : "") + escapedParts.join("") + "]";
    },
    any: function() {
      return "any character";
    },
    end: function() {
      return "end of input";
    },
    other: function(expectation) {
      return expectation.description;
    }
  };
  function hex(ch) {
    return ch.charCodeAt(0).toString(16).toUpperCase();
  }
  function literalEscape(s) {
    return s.replace(/\\/g, "\\\\").replace(/"/g, '\\"').replace(/\0/g, "\\0").replace(/\t/g, "\\t").replace(/\n/g, "\\n").replace(/\r/g, "\\r").replace(/[\x00-\x0F]/g, function(ch) {
      return "\\x0" + hex(ch);
    }).replace(/[\x10-\x1F\x7F-\x9F]/g, function(ch) {
      return "\\x" + hex(ch);
    });
  }
  function classEscape(s) {
    return s.replace(/\\/g, "\\\\").replace(/\]/g, "\\]").replace(/\^/g, "\\^").replace(/-/g, "\\-").replace(/\0/g, "\\0").replace(/\t/g, "\\t").replace(/\n/g, "\\n").replace(/\r/g, "\\r").replace(/[\x00-\x0F]/g, function(ch) {
      return "\\x0" + hex(ch);
    }).replace(/[\x10-\x1F\x7F-\x9F]/g, function(ch) {
      return "\\x" + hex(ch);
    });
  }
  function describeExpectation(expectation) {
    return DESCRIBE_EXPECTATION_FNS[expectation.type](expectation);
  }
  function describeExpected(expected2) {
    var descriptions = expected2.map(describeExpectation);
    var i, j;
    descriptions.sort();
    if (descriptions.length > 0) {
      for (i = 1, j = 1; i < descriptions.length; i++) {
        if (descriptions[i - 1] !== descriptions[i]) {
          descriptions[j] = descriptions[i];
          j++;
        }
      }
      descriptions.length = j;
    }
    switch (descriptions.length) {
      case 1:
        return descriptions[0];
      case 2:
        return descriptions[0] + " or " + descriptions[1];
      default:
        return descriptions.slice(0, -1).join(", ") + ", or " + descriptions[descriptions.length - 1];
    }
  }
  function describeFound(found2) {
    return found2 ? '"' + literalEscape(found2) + '"' : "end of input";
  }
  return "Expected " + describeExpected(expected) + " but " + describeFound(found) + " found.";
};
function peg$parse3(input, options) {
  options = options !== void 0 ? options : {};
  var peg$FAILED = {};
  var peg$source = options.grammarSource;
  var peg$startRuleFunctions = { Document: peg$parseDocument };
  var peg$startRuleFunction = peg$parseDocument;
  var peg$c0 = "---";
  var peg$c1 = "poster";
  var peg$c2 = "columns";
  var peg$c3 = "rows";
  var peg$c4 = "gap";
  var peg$c5 = "scale";
  var peg$c6 = "uniform-scale";
  var peg$c7 = "uniform_scale";
  var peg$c8 = "cell";
  var peg$c9 = "::";
  var peg$c10 = "@theme";
  var peg$c11 = "end";
  var peg$c12 = "[";
  var peg$c13 = "x";
  var peg$c14 = "]";
  var peg$c15 = "link";
  var peg$c16 = "@";
  var peg$c17 = "anim:";
  var peg$c18 = ":";
  var peg$c19 = "march";
  var peg$c20 = "particle";
  var peg$c21 = "draw";
  var peg$c22 = "pulse";
  var peg$c23 = "glow";
  var peg$c24 = "comet";
  var peg$c25 = "stream";
  var peg$c26 = "flow";
  var peg$c27 = "colorcycle";
  var peg$c28 = "none";
  var peg$c29 = "straight";
  var peg$c30 = "orthogonal";
  var peg$c31 = "bezier";
  var peg$c32 = "polyline";
  var peg$c33 = "{";
  var peg$c34 = ",";
  var peg$c35 = "}";
  var peg$c36 = ".";
  var peg$c37 = "<-~->";
  var peg$c38 = "<-_->";
  var peg$c39 = "<-.->";
  var peg$c40 = "<==>";
  var peg$c41 = "<-->";
  var peg$c42 = "-~->";
  var peg$c43 = "-_->";
  var peg$c44 = "-..->";
  var peg$c45 = "-.->";
  var peg$c46 = "===>";
  var peg$c47 = "==>";
  var peg$c48 = "--->";
  var peg$c49 = "-->";
  var peg$c50 = "-~-";
  var peg$c51 = "-_-";
  var peg$c52 = "-.-";
  var peg$c53 = "===";
  var peg$c54 = '"';
  var peg$c55 = "%%";
  var peg$c56 = "\r\n";
  var peg$r0 = /^[^\n]/;
  var peg$r1 = /^[ENSW]/;
  var peg$r2 = /^[0-9]/;
  var peg$r3 = /^[a-zA-Z_]/;
  var peg$r4 = /^[a-zA-Z0-9_\-]/;
  var peg$r5 = /^[^"]/;
  var peg$r6 = /^[+\-]/;
  var peg$r7 = /^[ \t]/;
  var peg$r8 = /^[\n\r]/;
  var peg$e0 = peg$literalExpectation("---", false);
  var peg$e1 = peg$classExpectation(["\n"], true, false);
  var peg$e2 = peg$literalExpectation("poster", false);
  var peg$e3 = peg$literalExpectation("columns", false);
  var peg$e4 = peg$literalExpectation("rows", false);
  var peg$e5 = peg$literalExpectation("gap", false);
  var peg$e6 = peg$literalExpectation("scale", false);
  var peg$e7 = peg$literalExpectation("uniform-scale", false);
  var peg$e8 = peg$literalExpectation("uniform_scale", false);
  var peg$e9 = peg$literalExpectation("cell", false);
  var peg$e10 = peg$literalExpectation("::", false);
  var peg$e11 = peg$literalExpectation("@theme", false);
  var peg$e12 = peg$literalExpectation("end", false);
  var peg$e13 = peg$literalExpectation("[", false);
  var peg$e14 = peg$literalExpectation("x", false);
  var peg$e15 = peg$literalExpectation("]", false);
  var peg$e16 = peg$literalExpectation("link", false);
  var peg$e17 = peg$literalExpectation("@", false);
  var peg$e18 = peg$anyExpectation();
  var peg$e19 = peg$literalExpectation("anim:", false);
  var peg$e20 = peg$literalExpectation(":", false);
  var peg$e21 = peg$literalExpectation("march", false);
  var peg$e22 = peg$literalExpectation("particle", false);
  var peg$e23 = peg$literalExpectation("draw", false);
  var peg$e24 = peg$literalExpectation("pulse", false);
  var peg$e25 = peg$literalExpectation("glow", false);
  var peg$e26 = peg$literalExpectation("comet", false);
  var peg$e27 = peg$literalExpectation("stream", false);
  var peg$e28 = peg$literalExpectation("flow", false);
  var peg$e29 = peg$literalExpectation("colorcycle", false);
  var peg$e30 = peg$literalExpectation("none", false);
  var peg$e31 = peg$literalExpectation("straight", false);
  var peg$e32 = peg$literalExpectation("orthogonal", false);
  var peg$e33 = peg$literalExpectation("bezier", false);
  var peg$e34 = peg$literalExpectation("polyline", false);
  var peg$e35 = peg$classExpectation(["E", "N", "S", "W"], false, false);
  var peg$e36 = peg$literalExpectation("{", false);
  var peg$e37 = peg$literalExpectation(",", false);
  var peg$e38 = peg$literalExpectation("}", false);
  var peg$e39 = peg$classExpectation([["0", "9"]], false, false);
  var peg$e40 = peg$literalExpectation(".", false);
  var peg$e41 = peg$literalExpectation("<-~->", false);
  var peg$e42 = peg$literalExpectation("<-_->", false);
  var peg$e43 = peg$literalExpectation("<-.->", false);
  var peg$e44 = peg$literalExpectation("<==>", false);
  var peg$e45 = peg$literalExpectation("<-->", false);
  var peg$e46 = peg$literalExpectation("-~->", false);
  var peg$e47 = peg$literalExpectation("-_->", false);
  var peg$e48 = peg$literalExpectation("-..->", false);
  var peg$e49 = peg$literalExpectation("-.->", false);
  var peg$e50 = peg$literalExpectation("===>", false);
  var peg$e51 = peg$literalExpectation("==>", false);
  var peg$e52 = peg$literalExpectation("--->", false);
  var peg$e53 = peg$literalExpectation("-->", false);
  var peg$e54 = peg$literalExpectation("-~-", false);
  var peg$e55 = peg$literalExpectation("-_-", false);
  var peg$e56 = peg$literalExpectation("-.-", false);
  var peg$e57 = peg$literalExpectation("===", false);
  var peg$e58 = peg$classExpectation([["a", "z"], ["A", "Z"], "_"], false, false);
  var peg$e59 = peg$classExpectation([["a", "z"], ["A", "Z"], ["0", "9"], "_", "-"], false, false);
  var peg$e60 = peg$literalExpectation('"', false);
  var peg$e61 = peg$classExpectation(['"'], true, false);
  var peg$e62 = peg$classExpectation(["+", "-"], false, false);
  var peg$e63 = peg$literalExpectation("%%", false);
  var peg$e64 = peg$classExpectation([" ", "	"], false, false);
  var peg$e65 = peg$literalExpectation("\r\n", false);
  var peg$e66 = peg$classExpectation(["\n", "\r"], false, false);
  var peg$f0 = function(frontmatter, header, directives, body) {
    const columns = (directives.find((d) => d.key === "columns") || {}).value || 2;
    const rowsDirective = directives.find((d) => d.key === "rows");
    const gapDirective = directives.find((d) => d.key === "gap");
    const scaleDirective = directives.find((d) => d.key === "scale" || d.key === "uniform-scale" || d.key === "uniform_scale");
    return {
      version: "1.0",
      metadata: { ...frontmatter || {}, title: header.title || void 0 },
      grid: {
        columns: parseInt(columns, 10),
        ...rowsDirective ? { rows: parseInt(rowsDirective.value, 10) } : {},
        ...gapDirective ? { gap: parseFloat(gapDirective.value) } : {},
        ...scaleDirective ? { scale: scaleDirective.value } : {}
      },
      cells,
      ...links.length > 0 ? { links } : {}
    };
  };
  var peg$f1 = function(line) {
    return line;
  };
  var peg$f2 = function(lines2) {
    const meta = {};
    for (const line of lines2) {
      const m = line.match(/^\s*(\w+)\s*:\s*(.+?)\s*$/);
      if (m) meta[m[1]] = m[2];
    }
    return meta;
  };
  var peg$f3 = function(title) {
    return { title: title ? title[1] : null };
  };
  var peg$f4 = function(key, value) {
    return { key, value: value.trim() };
  };
  var peg$f5 = function(items) {
    return items.filter(Boolean);
  };
  var peg$f6 = function(ids, span, kind) {
    return kind;
  };
  var peg$f7 = function(ids, span, explicitKind, t) {
    return t;
  };
  var peg$f8 = function(ids, span, explicitKind, theme, content) {
    addCell(ids.id, ids.title, explicitKind || null, content, span || void 0, theme || void 0);
    return { type: "cell" };
  };
  var peg$f9 = function(id, title) {
    return { id, title };
  };
  var peg$f10 = function(id) {
    return { id, title: null };
  };
  var peg$f11 = function(title) {
    return { id: null, title };
  };
  var peg$f12 = function() {
    return { id: null, title: null };
  };
  var peg$f13 = function(cols, rows) {
    return { colSpan: cols, rowSpan: rows };
  };
  var peg$f14 = function(cols) {
    return { colSpan: cols, rowSpan: 1 };
  };
  var peg$f15 = function(from, arrow, to, preAnns, label, postAnns, block2) {
    const result = {};
    if (block2) {
      const b = block2[1];
      if (b.route) {
        result.routing = b.route;
        delete b.route;
      }
      if (b.anim) {
        result.animation = b.anim;
        delete b.anim;
      }
      if (b.curveStyle) {
        result.curveStyle = b.curveStyle;
        delete b.curveStyle;
      }
      if (b.curve) {
        result.curveStyle = b.curve;
        delete b.curve;
      }
      if (Object.keys(b).length > 0) result.props = b;
    }
    const allAnns = [...preAnns, ...postAnns];
    for (const ann of allAnns) {
      Object.assign(result, ann[2]);
    }
    addLink(from, arrow, to, label ? label[1] : null, Object.keys(result).length > 0 ? result : null);
    return { type: "link" };
  };
  var peg$f16 = function(value) {
    return { animation: value };
  };
  var peg$f17 = function(route, walls) {
    return { routing: route, ...walls ? walls[1] : {} };
  };
  var peg$f18 = function(walls) {
    return walls;
  };
  var peg$f19 = function(route, walls) {
    return { routing: route, ...walls ? walls[1] : {} };
  };
  var peg$f20 = function(walls) {
    return walls;
  };
  var peg$f21 = function() {
    return "straight";
  };
  var peg$f22 = function() {
    return "orthogonal";
  };
  var peg$f23 = function() {
    return "bezier";
  };
  var peg$f24 = function() {
    return "polyline";
  };
  var peg$f25 = function(exit, entry) {
    return { exitWall: exit, entryWall: entry };
  };
  var peg$f26 = function(exit) {
    return { exitWall: exit };
  };
  var peg$f27 = function(w) {
    return w;
  };
  var peg$f28 = function(first, rest) {
    const result = {};
    result[first.key] = first.value;
    for (const r of rest) {
      result[r[3].key] = r[3].value;
    }
    return result;
  };
  var peg$f29 = function(key, value) {
    return { key, value };
  };
  var peg$f30 = function(num) {
    return parseFloat(num);
  };
  var peg$f31 = function() {
    return { direction: "bidirectional", style: "wavy" };
  };
  var peg$f32 = function() {
    return { direction: "bidirectional", style: "dashed" };
  };
  var peg$f33 = function() {
    return { direction: "bidirectional", style: "dotted" };
  };
  var peg$f34 = function() {
    return { direction: "bidirectional", style: "thick" };
  };
  var peg$f35 = function() {
    return { direction: "bidirectional", style: "solid" };
  };
  var peg$f36 = function() {
    return { direction: "directed", style: "wavy" };
  };
  var peg$f37 = function() {
    return { direction: "directed", style: "dashed" };
  };
  var peg$f38 = function() {
    return { direction: "directed", style: "dotted" };
  };
  var peg$f39 = function() {
    return { direction: "directed", style: "dotted" };
  };
  var peg$f40 = function() {
    return { direction: "directed", style: "thick" };
  };
  var peg$f41 = function() {
    return { direction: "directed", style: "thick" };
  };
  var peg$f42 = function() {
    return { direction: "directed", style: "solid" };
  };
  var peg$f43 = function() {
    return { direction: "directed", style: "solid" };
  };
  var peg$f44 = function() {
    return { direction: "undirected", style: "wavy" };
  };
  var peg$f45 = function() {
    return { direction: "undirected", style: "dashed" };
  };
  var peg$f46 = function() {
    return { direction: "undirected", style: "dotted" };
  };
  var peg$f47 = function() {
    return { direction: "undirected", style: "thick" };
  };
  var peg$f48 = function() {
    return { direction: "undirected", style: "solid" };
  };
  var peg$f49 = function(cell, index) {
    return { cellPath: [cell], nodeId: "", elementIndex: index };
  };
  var peg$f50 = function(cell, node) {
    return { cellPath: [cell], nodeId: node };
  };
  var peg$f51 = function(line) {
    return line;
  };
  var peg$f52 = function(lines2) {
    return lines2.join("\n");
  };
  var peg$f53 = function(text2) {
    return text2;
  };
  var peg$f54 = function(digits) {
    return parseInt(digits, 10);
  };
  var peg$f55 = function(sign, digits) {
    return parseInt((sign || "") + digits, 10);
  };
  var peg$f56 = function() {
    return null;
  };
  var peg$currPos = options.peg$currPos | 0;
  var peg$savedPos = peg$currPos;
  var peg$posDetailsCache = [{ line: 1, column: 1 }];
  var peg$maxFailPos = peg$currPos;
  var peg$maxFailExpected = options.peg$maxFailExpected || [];
  var peg$silentFails = options.peg$silentFails | 0;
  var peg$result;
  if (options.startRule) {
    if (!(options.startRule in peg$startRuleFunctions)) {
      throw new Error(`Can't start parsing from rule "` + options.startRule + '".');
    }
    peg$startRuleFunction = peg$startRuleFunctions[options.startRule];
  }
  function text() {
    return input.substring(peg$savedPos, peg$currPos);
  }
  function offset() {
    return peg$savedPos;
  }
  function range() {
    return {
      source: peg$source,
      start: peg$savedPos,
      end: peg$currPos
    };
  }
  function location() {
    return peg$computeLocation(peg$savedPos, peg$currPos);
  }
  function expected(description, location2) {
    location2 = location2 !== void 0 ? location2 : peg$computeLocation(peg$savedPos, peg$currPos);
    throw peg$buildStructuredError(
      [peg$otherExpectation(description)],
      input.substring(peg$savedPos, peg$currPos),
      location2
    );
  }
  function error(message, location2) {
    location2 = location2 !== void 0 ? location2 : peg$computeLocation(peg$savedPos, peg$currPos);
    throw peg$buildSimpleError(message, location2);
  }
  function peg$literalExpectation(text2, ignoreCase) {
    return { type: "literal", text: text2, ignoreCase };
  }
  function peg$classExpectation(parts, inverted, ignoreCase) {
    return { type: "class", parts, inverted, ignoreCase };
  }
  function peg$anyExpectation() {
    return { type: "any" };
  }
  function peg$endExpectation() {
    return { type: "end" };
  }
  function peg$otherExpectation(description) {
    return { type: "other", description };
  }
  function peg$computePosDetails(pos) {
    var details = peg$posDetailsCache[pos];
    var p;
    if (details) {
      return details;
    } else {
      if (pos >= peg$posDetailsCache.length) {
        p = peg$posDetailsCache.length - 1;
      } else {
        p = pos;
        while (!peg$posDetailsCache[--p]) {
        }
      }
      details = peg$posDetailsCache[p];
      details = {
        line: details.line,
        column: details.column
      };
      while (p < pos) {
        if (input.charCodeAt(p) === 10) {
          details.line++;
          details.column = 1;
        } else {
          details.column++;
        }
        p++;
      }
      peg$posDetailsCache[pos] = details;
      return details;
    }
  }
  function peg$computeLocation(startPos, endPos, offset2) {
    var startPosDetails = peg$computePosDetails(startPos);
    var endPosDetails = peg$computePosDetails(endPos);
    var res = {
      source: peg$source,
      start: {
        offset: startPos,
        line: startPosDetails.line,
        column: startPosDetails.column
      },
      end: {
        offset: endPos,
        line: endPosDetails.line,
        column: endPosDetails.column
      }
    };
    if (offset2 && peg$source && typeof peg$source.offset === "function") {
      res.start = peg$source.offset(res.start);
      res.end = peg$source.offset(res.end);
    }
    return res;
  }
  function peg$fail(expected2) {
    if (peg$currPos < peg$maxFailPos) {
      return;
    }
    if (peg$currPos > peg$maxFailPos) {
      peg$maxFailPos = peg$currPos;
      peg$maxFailExpected = [];
    }
    peg$maxFailExpected.push(expected2);
  }
  function peg$buildSimpleError(message, location2) {
    return new peg$SyntaxError3(message, null, null, location2);
  }
  function peg$buildStructuredError(expected2, found, location2) {
    return new peg$SyntaxError3(
      peg$SyntaxError3.buildMessage(expected2, found),
      expected2,
      found,
      location2
    );
  }
  function peg$parseDocument() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8;
    s0 = peg$currPos;
    s1 = peg$parseFrontmatter();
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    s2 = peg$parse_();
    s3 = peg$parsePosterHeader();
    if (s3 !== peg$FAILED) {
      s4 = peg$parse_();
      s5 = [];
      s6 = peg$parseGridDirective();
      while (s6 !== peg$FAILED) {
        s5.push(s6);
        s6 = peg$parseGridDirective();
      }
      s6 = peg$parse_();
      s7 = peg$parseBodyItems();
      s8 = peg$parse_();
      peg$savedPos = s0;
      s0 = peg$f0(s1, s3, s5, s7);
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseFrontmatter() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 3) === peg$c0) {
      s1 = peg$c0;
      peg$currPos += 3;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e0);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$parseNL();
      if (s2 !== peg$FAILED) {
        s3 = [];
        s4 = peg$currPos;
        s5 = peg$currPos;
        peg$silentFails++;
        s6 = peg$currPos;
        if (input.substr(peg$currPos, 3) === peg$c0) {
          s7 = peg$c0;
          peg$currPos += 3;
        } else {
          s7 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e0);
          }
        }
        if (s7 !== peg$FAILED) {
          s8 = peg$parseNL();
          if (s8 !== peg$FAILED) {
            s7 = [s7, s8];
            s6 = s7;
          } else {
            peg$currPos = s6;
            s6 = peg$FAILED;
          }
        } else {
          peg$currPos = s6;
          s6 = peg$FAILED;
        }
        peg$silentFails--;
        if (s6 === peg$FAILED) {
          s5 = void 0;
        } else {
          peg$currPos = s5;
          s5 = peg$FAILED;
        }
        if (s5 !== peg$FAILED) {
          s6 = peg$currPos;
          s7 = [];
          s8 = input.charAt(peg$currPos);
          if (peg$r0.test(s8)) {
            peg$currPos++;
          } else {
            s8 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e1);
            }
          }
          while (s8 !== peg$FAILED) {
            s7.push(s8);
            s8 = input.charAt(peg$currPos);
            if (peg$r0.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e1);
              }
            }
          }
          s6 = input.substring(s6, peg$currPos);
          s7 = peg$parseNL();
          if (s7 !== peg$FAILED) {
            peg$savedPos = s4;
            s4 = peg$f1(s6);
          } else {
            peg$currPos = s4;
            s4 = peg$FAILED;
          }
        } else {
          peg$currPos = s4;
          s4 = peg$FAILED;
        }
        while (s4 !== peg$FAILED) {
          s3.push(s4);
          s4 = peg$currPos;
          s5 = peg$currPos;
          peg$silentFails++;
          s6 = peg$currPos;
          if (input.substr(peg$currPos, 3) === peg$c0) {
            s7 = peg$c0;
            peg$currPos += 3;
          } else {
            s7 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e0);
            }
          }
          if (s7 !== peg$FAILED) {
            s8 = peg$parseNL();
            if (s8 !== peg$FAILED) {
              s7 = [s7, s8];
              s6 = s7;
            } else {
              peg$currPos = s6;
              s6 = peg$FAILED;
            }
          } else {
            peg$currPos = s6;
            s6 = peg$FAILED;
          }
          peg$silentFails--;
          if (s6 === peg$FAILED) {
            s5 = void 0;
          } else {
            peg$currPos = s5;
            s5 = peg$FAILED;
          }
          if (s5 !== peg$FAILED) {
            s6 = peg$currPos;
            s7 = [];
            s8 = input.charAt(peg$currPos);
            if (peg$r0.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e1);
              }
            }
            while (s8 !== peg$FAILED) {
              s7.push(s8);
              s8 = input.charAt(peg$currPos);
              if (peg$r0.test(s8)) {
                peg$currPos++;
              } else {
                s8 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e1);
                }
              }
            }
            s6 = input.substring(s6, peg$currPos);
            s7 = peg$parseNL();
            if (s7 !== peg$FAILED) {
              peg$savedPos = s4;
              s4 = peg$f1(s6);
            } else {
              peg$currPos = s4;
              s4 = peg$FAILED;
            }
          } else {
            peg$currPos = s4;
            s4 = peg$FAILED;
          }
        }
        if (input.substr(peg$currPos, 3) === peg$c0) {
          s4 = peg$c0;
          peg$currPos += 3;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e0);
          }
        }
        if (s4 !== peg$FAILED) {
          s5 = peg$parseNL();
          if (s5 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f2(s3);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parsePosterHeader() {
    var s0, s1, s2, s3, s4;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 6) === peg$c1) {
      s1 = peg$c1;
      peg$currPos += 6;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e2);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$currPos;
      s3 = peg$parse_();
      s4 = peg$parseQuotedString();
      if (s4 !== peg$FAILED) {
        s3 = [s3, s4];
        s2 = s3;
      } else {
        peg$currPos = s2;
        s2 = peg$FAILED;
      }
      if (s2 === peg$FAILED) {
        s2 = null;
      }
      s3 = peg$parse_();
      s4 = peg$parseNL();
      if (s4 !== peg$FAILED) {
        peg$savedPos = s0;
        s0 = peg$f3(s2);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseGridDirective() {
    var s0, s1, s2, s3, s4, s5, s6;
    s0 = peg$currPos;
    s1 = peg$parse_();
    if (input.substr(peg$currPos, 7) === peg$c2) {
      s2 = peg$c2;
      peg$currPos += 7;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e3);
      }
    }
    if (s2 === peg$FAILED) {
      if (input.substr(peg$currPos, 4) === peg$c3) {
        s2 = peg$c3;
        peg$currPos += 4;
      } else {
        s2 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e4);
        }
      }
      if (s2 === peg$FAILED) {
        if (input.substr(peg$currPos, 3) === peg$c4) {
          s2 = peg$c4;
          peg$currPos += 3;
        } else {
          s2 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e5);
          }
        }
        if (s2 === peg$FAILED) {
          if (input.substr(peg$currPos, 5) === peg$c5) {
            s2 = peg$c5;
            peg$currPos += 5;
          } else {
            s2 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e6);
            }
          }
          if (s2 === peg$FAILED) {
            if (input.substr(peg$currPos, 13) === peg$c6) {
              s2 = peg$c6;
              peg$currPos += 13;
            } else {
              s2 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e7);
              }
            }
            if (s2 === peg$FAILED) {
              if (input.substr(peg$currPos, 13) === peg$c7) {
                s2 = peg$c7;
                peg$currPos += 13;
              } else {
                s2 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e8);
                }
              }
            }
          }
        }
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$parse__();
      if (s3 !== peg$FAILED) {
        s4 = peg$currPos;
        s5 = [];
        s6 = input.charAt(peg$currPos);
        if (peg$r0.test(s6)) {
          peg$currPos++;
        } else {
          s6 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e1);
          }
        }
        if (s6 !== peg$FAILED) {
          while (s6 !== peg$FAILED) {
            s5.push(s6);
            s6 = input.charAt(peg$currPos);
            if (peg$r0.test(s6)) {
              peg$currPos++;
            } else {
              s6 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e1);
              }
            }
          }
        } else {
          s5 = peg$FAILED;
        }
        if (s5 !== peg$FAILED) {
          s4 = input.substring(s4, peg$currPos);
        } else {
          s4 = s5;
        }
        if (s4 !== peg$FAILED) {
          s5 = peg$parseNL();
          if (s5 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f4(s2, s4);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseBodyItems() {
    var s0, s1, s2;
    s0 = peg$currPos;
    s1 = [];
    s2 = peg$parseCellBlock();
    if (s2 === peg$FAILED) {
      s2 = peg$parseLinkDecl();
      if (s2 === peg$FAILED) {
        s2 = peg$parseBlankLine();
      }
    }
    while (s2 !== peg$FAILED) {
      s1.push(s2);
      s2 = peg$parseCellBlock();
      if (s2 === peg$FAILED) {
        s2 = peg$parseLinkDecl();
        if (s2 === peg$FAILED) {
          s2 = peg$parseBlankLine();
        }
      }
    }
    peg$savedPos = s0;
    s1 = peg$f5(s1);
    s0 = s1;
    return s0;
  }
  function peg$parseCellBlock() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9, s10, s11, s12, s13;
    s0 = peg$currPos;
    s1 = peg$parse_();
    if (input.substr(peg$currPos, 4) === peg$c8) {
      s2 = peg$c8;
      peg$currPos += 4;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e9);
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$parseCellIdTitle();
      if (s3 !== peg$FAILED) {
        s4 = peg$parseCellSpan();
        if (s4 === peg$FAILED) {
          s4 = null;
        }
        s5 = peg$currPos;
        s6 = peg$parse_();
        if (input.substr(peg$currPos, 2) === peg$c9) {
          s7 = peg$c9;
          peg$currPos += 2;
        } else {
          s7 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e10);
          }
        }
        if (s7 !== peg$FAILED) {
          s8 = peg$parse_();
          s9 = peg$parseIdentifier();
          if (s9 !== peg$FAILED) {
            peg$savedPos = s5;
            s5 = peg$f6(s3, s4, s9);
          } else {
            peg$currPos = s5;
            s5 = peg$FAILED;
          }
        } else {
          peg$currPos = s5;
          s5 = peg$FAILED;
        }
        if (s5 === peg$FAILED) {
          s5 = null;
        }
        s6 = peg$currPos;
        s7 = peg$parse_();
        if (input.substr(peg$currPos, 6) === peg$c10) {
          s8 = peg$c10;
          peg$currPos += 6;
        } else {
          s8 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e11);
          }
        }
        if (s8 !== peg$FAILED) {
          s9 = peg$parse__();
          if (s9 !== peg$FAILED) {
            s10 = peg$parseIdentifier();
            if (s10 !== peg$FAILED) {
              peg$savedPos = s6;
              s6 = peg$f7(s3, s4, s5, s10);
            } else {
              peg$currPos = s6;
              s6 = peg$FAILED;
            }
          } else {
            peg$currPos = s6;
            s6 = peg$FAILED;
          }
        } else {
          peg$currPos = s6;
          s6 = peg$FAILED;
        }
        if (s6 === peg$FAILED) {
          s6 = null;
        }
        s7 = peg$parse_();
        s8 = peg$parseNL();
        if (s8 !== peg$FAILED) {
          s9 = peg$parseCellContent();
          s10 = peg$parse_();
          if (input.substr(peg$currPos, 3) === peg$c11) {
            s11 = peg$c11;
            peg$currPos += 3;
          } else {
            s11 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e12);
            }
          }
          if (s11 !== peg$FAILED) {
            s12 = peg$parse_();
            s13 = peg$parseNL();
            if (s13 === peg$FAILED) {
              s13 = null;
            }
            peg$savedPos = s0;
            s0 = peg$f8(s3, s4, s5, s6, s9);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseCellIdTitle() {
    var s0, s1, s2, s3, s4;
    s0 = peg$currPos;
    s1 = peg$parse__();
    if (s1 !== peg$FAILED) {
      s2 = peg$parseIdentifier();
      if (s2 !== peg$FAILED) {
        s3 = peg$parse__();
        if (s3 !== peg$FAILED) {
          s4 = peg$parseQuotedString();
          if (s4 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f9(s2, s4);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      s1 = peg$parse__();
      if (s1 !== peg$FAILED) {
        s2 = peg$parseIdentifier();
        if (s2 !== peg$FAILED) {
          peg$savedPos = s0;
          s0 = peg$f10(s2);
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
      if (s0 === peg$FAILED) {
        s0 = peg$currPos;
        s1 = peg$parse__();
        if (s1 !== peg$FAILED) {
          s2 = peg$parseQuotedString();
          if (s2 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f11(s2);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
        if (s0 === peg$FAILED) {
          s0 = peg$currPos;
          s1 = "";
          peg$savedPos = s0;
          s1 = peg$f12();
          s0 = s1;
        }
      }
    }
    return s0;
  }
  function peg$parseCellSpan() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9, s10;
    s0 = peg$currPos;
    s1 = peg$parse_();
    if (input.charCodeAt(peg$currPos) === 91) {
      s2 = peg$c12;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e13);
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$parse_();
      s4 = peg$parseInteger();
      if (s4 !== peg$FAILED) {
        s5 = peg$parse_();
        if (input.charCodeAt(peg$currPos) === 120) {
          s6 = peg$c13;
          peg$currPos++;
        } else {
          s6 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e14);
          }
        }
        if (s6 !== peg$FAILED) {
          s7 = peg$parse_();
          s8 = peg$parseInteger();
          if (s8 !== peg$FAILED) {
            s9 = peg$parse_();
            if (input.charCodeAt(peg$currPos) === 93) {
              s10 = peg$c14;
              peg$currPos++;
            } else {
              s10 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e15);
              }
            }
            if (s10 !== peg$FAILED) {
              peg$savedPos = s0;
              s0 = peg$f13(s4, s8);
            } else {
              peg$currPos = s0;
              s0 = peg$FAILED;
            }
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      s1 = peg$parse_();
      if (input.charCodeAt(peg$currPos) === 91) {
        s2 = peg$c12;
        peg$currPos++;
      } else {
        s2 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e13);
        }
      }
      if (s2 !== peg$FAILED) {
        s3 = peg$parse_();
        s4 = peg$parseInteger();
        if (s4 !== peg$FAILED) {
          s5 = peg$parse_();
          if (input.charCodeAt(peg$currPos) === 93) {
            s6 = peg$c14;
            peg$currPos++;
          } else {
            s6 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e15);
            }
          }
          if (s6 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f14(s4);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    }
    return s0;
  }
  function peg$parseLinkDecl() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9, s10, s11, s12, s13, s14, s15;
    s0 = peg$currPos;
    s1 = peg$parse_();
    if (input.substr(peg$currPos, 4) === peg$c15) {
      s2 = peg$c15;
      peg$currPos += 4;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e16);
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$parse__();
      if (s3 !== peg$FAILED) {
        s4 = peg$parseNodeRef();
        if (s4 !== peg$FAILED) {
          s5 = peg$parse__();
          if (s5 !== peg$FAILED) {
            s6 = peg$parseArrow();
            if (s6 !== peg$FAILED) {
              s7 = peg$parse__();
              if (s7 !== peg$FAILED) {
                s8 = peg$parseNodeRef();
                if (s8 !== peg$FAILED) {
                  s9 = [];
                  s10 = peg$currPos;
                  s11 = peg$parse_();
                  if (input.charCodeAt(peg$currPos) === 64) {
                    s12 = peg$c16;
                    peg$currPos++;
                  } else {
                    s12 = peg$FAILED;
                    if (peg$silentFails === 0) {
                      peg$fail(peg$e17);
                    }
                  }
                  if (s12 !== peg$FAILED) {
                    s13 = peg$parseAnnotation();
                    if (s13 !== peg$FAILED) {
                      s11 = [s11, s12, s13];
                      s10 = s11;
                    } else {
                      peg$currPos = s10;
                      s10 = peg$FAILED;
                    }
                  } else {
                    peg$currPos = s10;
                    s10 = peg$FAILED;
                  }
                  while (s10 !== peg$FAILED) {
                    s9.push(s10);
                    s10 = peg$currPos;
                    s11 = peg$parse_();
                    if (input.charCodeAt(peg$currPos) === 64) {
                      s12 = peg$c16;
                      peg$currPos++;
                    } else {
                      s12 = peg$FAILED;
                      if (peg$silentFails === 0) {
                        peg$fail(peg$e17);
                      }
                    }
                    if (s12 !== peg$FAILED) {
                      s13 = peg$parseAnnotation();
                      if (s13 !== peg$FAILED) {
                        s11 = [s11, s12, s13];
                        s10 = s11;
                      } else {
                        peg$currPos = s10;
                        s10 = peg$FAILED;
                      }
                    } else {
                      peg$currPos = s10;
                      s10 = peg$FAILED;
                    }
                  }
                  s10 = peg$currPos;
                  s11 = peg$parse_();
                  s12 = peg$parseQuotedString();
                  if (s12 !== peg$FAILED) {
                    s11 = [s11, s12];
                    s10 = s11;
                  } else {
                    peg$currPos = s10;
                    s10 = peg$FAILED;
                  }
                  if (s10 === peg$FAILED) {
                    s10 = null;
                  }
                  s11 = [];
                  s12 = peg$currPos;
                  s13 = peg$parse_();
                  if (input.charCodeAt(peg$currPos) === 64) {
                    s14 = peg$c16;
                    peg$currPos++;
                  } else {
                    s14 = peg$FAILED;
                    if (peg$silentFails === 0) {
                      peg$fail(peg$e17);
                    }
                  }
                  if (s14 !== peg$FAILED) {
                    s15 = peg$parseAnnotation();
                    if (s15 !== peg$FAILED) {
                      s13 = [s13, s14, s15];
                      s12 = s13;
                    } else {
                      peg$currPos = s12;
                      s12 = peg$FAILED;
                    }
                  } else {
                    peg$currPos = s12;
                    s12 = peg$FAILED;
                  }
                  while (s12 !== peg$FAILED) {
                    s11.push(s12);
                    s12 = peg$currPos;
                    s13 = peg$parse_();
                    if (input.charCodeAt(peg$currPos) === 64) {
                      s14 = peg$c16;
                      peg$currPos++;
                    } else {
                      s14 = peg$FAILED;
                      if (peg$silentFails === 0) {
                        peg$fail(peg$e17);
                      }
                    }
                    if (s14 !== peg$FAILED) {
                      s15 = peg$parseAnnotation();
                      if (s15 !== peg$FAILED) {
                        s13 = [s13, s14, s15];
                        s12 = s13;
                      } else {
                        peg$currPos = s12;
                        s12 = peg$FAILED;
                      }
                    } else {
                      peg$currPos = s12;
                      s12 = peg$FAILED;
                    }
                  }
                  s12 = peg$currPos;
                  s13 = peg$parse_();
                  s14 = peg$parsePropBlock();
                  if (s14 !== peg$FAILED) {
                    s13 = [s13, s14];
                    s12 = s13;
                  } else {
                    peg$currPos = s12;
                    s12 = peg$FAILED;
                  }
                  if (s12 === peg$FAILED) {
                    s12 = null;
                  }
                  s13 = peg$parse_();
                  s14 = peg$parseNL();
                  if (s14 === peg$FAILED) {
                    s14 = peg$currPos;
                    peg$silentFails++;
                    if (input.length > peg$currPos) {
                      s15 = input.charAt(peg$currPos);
                      peg$currPos++;
                    } else {
                      s15 = peg$FAILED;
                      if (peg$silentFails === 0) {
                        peg$fail(peg$e18);
                      }
                    }
                    peg$silentFails--;
                    if (s15 === peg$FAILED) {
                      s14 = void 0;
                    } else {
                      peg$currPos = s14;
                      s14 = peg$FAILED;
                    }
                  }
                  if (s14 !== peg$FAILED) {
                    peg$savedPos = s0;
                    s0 = peg$f15(s4, s6, s8, s9, s10, s11, s12);
                  } else {
                    peg$currPos = s0;
                    s0 = peg$FAILED;
                  }
                } else {
                  peg$currPos = s0;
                  s0 = peg$FAILED;
                }
              } else {
                peg$currPos = s0;
                s0 = peg$FAILED;
              }
            } else {
              peg$currPos = s0;
              s0 = peg$FAILED;
            }
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseAnnotation() {
    var s0, s1, s2, s3, s4;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 5) === peg$c17) {
      s1 = peg$c17;
      peg$currPos += 5;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e19);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$parseAnimValue();
      if (s2 !== peg$FAILED) {
        peg$savedPos = s0;
        s0 = peg$f16(s2);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      s1 = peg$parseRouteWord();
      if (s1 !== peg$FAILED) {
        s2 = peg$currPos;
        if (input.charCodeAt(peg$currPos) === 58) {
          s3 = peg$c18;
          peg$currPos++;
        } else {
          s3 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e20);
          }
        }
        if (s3 !== peg$FAILED) {
          s4 = peg$parseWallPair();
          if (s4 !== peg$FAILED) {
            s3 = [s3, s4];
            s2 = s3;
          } else {
            peg$currPos = s2;
            s2 = peg$FAILED;
          }
        } else {
          peg$currPos = s2;
          s2 = peg$FAILED;
        }
        if (s2 === peg$FAILED) {
          s2 = null;
        }
        peg$savedPos = s0;
        s0 = peg$f17(s1, s2);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
      if (s0 === peg$FAILED) {
        s0 = peg$currPos;
        s1 = peg$parseWallPair();
        if (s1 !== peg$FAILED) {
          peg$savedPos = s0;
          s1 = peg$f18(s1);
        }
        s0 = s1;
      }
    }
    return s0;
  }
  function peg$parseAnimValue() {
    var s0;
    if (input.substr(peg$currPos, 5) === peg$c19) {
      s0 = peg$c19;
      peg$currPos += 5;
    } else {
      s0 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e21);
      }
    }
    if (s0 === peg$FAILED) {
      if (input.substr(peg$currPos, 8) === peg$c20) {
        s0 = peg$c20;
        peg$currPos += 8;
      } else {
        s0 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e22);
        }
      }
      if (s0 === peg$FAILED) {
        if (input.substr(peg$currPos, 4) === peg$c21) {
          s0 = peg$c21;
          peg$currPos += 4;
        } else {
          s0 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e23);
          }
        }
        if (s0 === peg$FAILED) {
          if (input.substr(peg$currPos, 5) === peg$c22) {
            s0 = peg$c22;
            peg$currPos += 5;
          } else {
            s0 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e24);
            }
          }
          if (s0 === peg$FAILED) {
            if (input.substr(peg$currPos, 4) === peg$c23) {
              s0 = peg$c23;
              peg$currPos += 4;
            } else {
              s0 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e25);
              }
            }
            if (s0 === peg$FAILED) {
              if (input.substr(peg$currPos, 5) === peg$c24) {
                s0 = peg$c24;
                peg$currPos += 5;
              } else {
                s0 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e26);
                }
              }
              if (s0 === peg$FAILED) {
                if (input.substr(peg$currPos, 6) === peg$c25) {
                  s0 = peg$c25;
                  peg$currPos += 6;
                } else {
                  s0 = peg$FAILED;
                  if (peg$silentFails === 0) {
                    peg$fail(peg$e27);
                  }
                }
                if (s0 === peg$FAILED) {
                  if (input.substr(peg$currPos, 4) === peg$c26) {
                    s0 = peg$c26;
                    peg$currPos += 4;
                  } else {
                    s0 = peg$FAILED;
                    if (peg$silentFails === 0) {
                      peg$fail(peg$e28);
                    }
                  }
                  if (s0 === peg$FAILED) {
                    if (input.substr(peg$currPos, 10) === peg$c27) {
                      s0 = peg$c27;
                      peg$currPos += 10;
                    } else {
                      s0 = peg$FAILED;
                      if (peg$silentFails === 0) {
                        peg$fail(peg$e29);
                      }
                    }
                    if (s0 === peg$FAILED) {
                      if (input.substr(peg$currPos, 4) === peg$c28) {
                        s0 = peg$c28;
                        peg$currPos += 4;
                      } else {
                        s0 = peg$FAILED;
                        if (peg$silentFails === 0) {
                          peg$fail(peg$e30);
                        }
                      }
                    }
                  }
                }
              }
            }
          }
        }
      }
    }
    return s0;
  }
  function peg$parseRouteAnnotation() {
    var s0, s1, s2, s3, s4;
    s0 = peg$currPos;
    s1 = peg$parseRouteWord();
    if (s1 !== peg$FAILED) {
      s2 = peg$currPos;
      if (input.charCodeAt(peg$currPos) === 58) {
        s3 = peg$c18;
        peg$currPos++;
      } else {
        s3 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e20);
        }
      }
      if (s3 !== peg$FAILED) {
        s4 = peg$parseWallPair();
        if (s4 !== peg$FAILED) {
          s3 = [s3, s4];
          s2 = s3;
        } else {
          peg$currPos = s2;
          s2 = peg$FAILED;
        }
      } else {
        peg$currPos = s2;
        s2 = peg$FAILED;
      }
      if (s2 === peg$FAILED) {
        s2 = null;
      }
      peg$savedPos = s0;
      s0 = peg$f19(s1, s2);
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      s1 = peg$parseWallPair();
      if (s1 !== peg$FAILED) {
        peg$savedPos = s0;
        s1 = peg$f20(s1);
      }
      s0 = s1;
    }
    return s0;
  }
  function peg$parseRouteWord() {
    var s0, s1;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 8) === peg$c29) {
      s1 = peg$c29;
      peg$currPos += 8;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e31);
      }
    }
    if (s1 !== peg$FAILED) {
      peg$savedPos = s0;
      s1 = peg$f21();
    }
    s0 = s1;
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      if (input.substr(peg$currPos, 10) === peg$c30) {
        s1 = peg$c30;
        peg$currPos += 10;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e32);
        }
      }
      if (s1 !== peg$FAILED) {
        peg$savedPos = s0;
        s1 = peg$f22();
      }
      s0 = s1;
      if (s0 === peg$FAILED) {
        s0 = peg$currPos;
        if (input.substr(peg$currPos, 6) === peg$c31) {
          s1 = peg$c31;
          peg$currPos += 6;
        } else {
          s1 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e33);
          }
        }
        if (s1 !== peg$FAILED) {
          peg$savedPos = s0;
          s1 = peg$f23();
        }
        s0 = s1;
        if (s0 === peg$FAILED) {
          s0 = peg$currPos;
          if (input.substr(peg$currPos, 8) === peg$c32) {
            s1 = peg$c32;
            peg$currPos += 8;
          } else {
            s1 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e34);
            }
          }
          if (s1 !== peg$FAILED) {
            peg$savedPos = s0;
            s1 = peg$f24();
          }
          s0 = s1;
        }
      }
    }
    return s0;
  }
  function peg$parseWallPair() {
    var s0, s1, s2;
    s0 = peg$currPos;
    s1 = peg$parseWall();
    if (s1 !== peg$FAILED) {
      s2 = peg$parseWall();
      if (s2 !== peg$FAILED) {
        peg$savedPos = s0;
        s0 = peg$f25(s1, s2);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      s1 = peg$parseWall();
      if (s1 !== peg$FAILED) {
        peg$savedPos = s0;
        s1 = peg$f26(s1);
      }
      s0 = s1;
    }
    return s0;
  }
  function peg$parseWall() {
    var s0, s1;
    s0 = peg$currPos;
    s1 = input.charAt(peg$currPos);
    if (peg$r1.test(s1)) {
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e35);
      }
    }
    if (s1 !== peg$FAILED) {
      peg$savedPos = s0;
      s1 = peg$f27(s1);
    }
    s0 = s1;
    return s0;
  }
  function peg$parsePropBlock() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 123) {
      s1 = peg$c33;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e36);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$parse_();
      s3 = peg$parsePropEntry();
      if (s3 !== peg$FAILED) {
        s4 = [];
        s5 = peg$currPos;
        s6 = peg$parse_();
        if (input.charCodeAt(peg$currPos) === 44) {
          s7 = peg$c34;
          peg$currPos++;
        } else {
          s7 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e37);
          }
        }
        if (s7 !== peg$FAILED) {
          s8 = peg$parse_();
          s9 = peg$parsePropEntry();
          if (s9 !== peg$FAILED) {
            s6 = [s6, s7, s8, s9];
            s5 = s6;
          } else {
            peg$currPos = s5;
            s5 = peg$FAILED;
          }
        } else {
          peg$currPos = s5;
          s5 = peg$FAILED;
        }
        while (s5 !== peg$FAILED) {
          s4.push(s5);
          s5 = peg$currPos;
          s6 = peg$parse_();
          if (input.charCodeAt(peg$currPos) === 44) {
            s7 = peg$c34;
            peg$currPos++;
          } else {
            s7 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e37);
            }
          }
          if (s7 !== peg$FAILED) {
            s8 = peg$parse_();
            s9 = peg$parsePropEntry();
            if (s9 !== peg$FAILED) {
              s6 = [s6, s7, s8, s9];
              s5 = s6;
            } else {
              peg$currPos = s5;
              s5 = peg$FAILED;
            }
          } else {
            peg$currPos = s5;
            s5 = peg$FAILED;
          }
        }
        s5 = peg$parse_();
        if (input.charCodeAt(peg$currPos) === 125) {
          s6 = peg$c35;
          peg$currPos++;
        } else {
          s6 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e38);
          }
        }
        if (s6 !== peg$FAILED) {
          peg$savedPos = s0;
          s0 = peg$f28(s3, s4);
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parsePropEntry() {
    var s0, s1, s2, s3, s4, s5;
    s0 = peg$currPos;
    s1 = peg$parseIdentifier();
    if (s1 !== peg$FAILED) {
      s2 = peg$parse_();
      if (input.charCodeAt(peg$currPos) === 58) {
        s3 = peg$c18;
        peg$currPos++;
      } else {
        s3 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e20);
        }
      }
      if (s3 !== peg$FAILED) {
        s4 = peg$parse_();
        s5 = peg$parsePropValue();
        if (s5 !== peg$FAILED) {
          peg$savedPos = s0;
          s0 = peg$f29(s1, s5);
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parsePropValue() {
    var s0, s1, s2, s3, s4, s5, s6, s7;
    s0 = peg$currPos;
    s1 = peg$currPos;
    s2 = peg$currPos;
    s3 = [];
    s4 = input.charAt(peg$currPos);
    if (peg$r2.test(s4)) {
      peg$currPos++;
    } else {
      s4 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e39);
      }
    }
    if (s4 !== peg$FAILED) {
      while (s4 !== peg$FAILED) {
        s3.push(s4);
        s4 = input.charAt(peg$currPos);
        if (peg$r2.test(s4)) {
          peg$currPos++;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e39);
          }
        }
      }
    } else {
      s3 = peg$FAILED;
    }
    if (s3 !== peg$FAILED) {
      s4 = peg$currPos;
      if (input.charCodeAt(peg$currPos) === 46) {
        s5 = peg$c36;
        peg$currPos++;
      } else {
        s5 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e40);
        }
      }
      if (s5 !== peg$FAILED) {
        s6 = [];
        s7 = input.charAt(peg$currPos);
        if (peg$r2.test(s7)) {
          peg$currPos++;
        } else {
          s7 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e39);
          }
        }
        if (s7 !== peg$FAILED) {
          while (s7 !== peg$FAILED) {
            s6.push(s7);
            s7 = input.charAt(peg$currPos);
            if (peg$r2.test(s7)) {
              peg$currPos++;
            } else {
              s7 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e39);
              }
            }
          }
        } else {
          s6 = peg$FAILED;
        }
        if (s6 !== peg$FAILED) {
          s5 = [s5, s6];
          s4 = s5;
        } else {
          peg$currPos = s4;
          s4 = peg$FAILED;
        }
      } else {
        peg$currPos = s4;
        s4 = peg$FAILED;
      }
      if (s4 === peg$FAILED) {
        s4 = null;
      }
      s3 = [s3, s4];
      s2 = s3;
    } else {
      peg$currPos = s2;
      s2 = peg$FAILED;
    }
    if (s2 !== peg$FAILED) {
      s1 = input.substring(s1, peg$currPos);
    } else {
      s1 = s2;
    }
    if (s1 !== peg$FAILED) {
      peg$savedPos = s0;
      s1 = peg$f30(s1);
    }
    s0 = s1;
    if (s0 === peg$FAILED) {
      s0 = peg$parseQuotedString();
      if (s0 === peg$FAILED) {
        s0 = peg$parseIdentifier();
      }
    }
    return s0;
  }
  function peg$parseArrow() {
    var s0, s1;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 5) === peg$c37) {
      s1 = peg$c37;
      peg$currPos += 5;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e41);
      }
    }
    if (s1 !== peg$FAILED) {
      peg$savedPos = s0;
      s1 = peg$f31();
    }
    s0 = s1;
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      if (input.substr(peg$currPos, 5) === peg$c38) {
        s1 = peg$c38;
        peg$currPos += 5;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e42);
        }
      }
      if (s1 !== peg$FAILED) {
        peg$savedPos = s0;
        s1 = peg$f32();
      }
      s0 = s1;
      if (s0 === peg$FAILED) {
        s0 = peg$currPos;
        if (input.substr(peg$currPos, 5) === peg$c39) {
          s1 = peg$c39;
          peg$currPos += 5;
        } else {
          s1 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e43);
          }
        }
        if (s1 !== peg$FAILED) {
          peg$savedPos = s0;
          s1 = peg$f33();
        }
        s0 = s1;
        if (s0 === peg$FAILED) {
          s0 = peg$currPos;
          if (input.substr(peg$currPos, 4) === peg$c40) {
            s1 = peg$c40;
            peg$currPos += 4;
          } else {
            s1 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e44);
            }
          }
          if (s1 !== peg$FAILED) {
            peg$savedPos = s0;
            s1 = peg$f34();
          }
          s0 = s1;
          if (s0 === peg$FAILED) {
            s0 = peg$currPos;
            if (input.substr(peg$currPos, 4) === peg$c41) {
              s1 = peg$c41;
              peg$currPos += 4;
            } else {
              s1 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e45);
              }
            }
            if (s1 !== peg$FAILED) {
              peg$savedPos = s0;
              s1 = peg$f35();
            }
            s0 = s1;
            if (s0 === peg$FAILED) {
              s0 = peg$currPos;
              if (input.substr(peg$currPos, 4) === peg$c42) {
                s1 = peg$c42;
                peg$currPos += 4;
              } else {
                s1 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e46);
                }
              }
              if (s1 !== peg$FAILED) {
                peg$savedPos = s0;
                s1 = peg$f36();
              }
              s0 = s1;
              if (s0 === peg$FAILED) {
                s0 = peg$currPos;
                if (input.substr(peg$currPos, 4) === peg$c43) {
                  s1 = peg$c43;
                  peg$currPos += 4;
                } else {
                  s1 = peg$FAILED;
                  if (peg$silentFails === 0) {
                    peg$fail(peg$e47);
                  }
                }
                if (s1 !== peg$FAILED) {
                  peg$savedPos = s0;
                  s1 = peg$f37();
                }
                s0 = s1;
                if (s0 === peg$FAILED) {
                  s0 = peg$currPos;
                  if (input.substr(peg$currPos, 5) === peg$c44) {
                    s1 = peg$c44;
                    peg$currPos += 5;
                  } else {
                    s1 = peg$FAILED;
                    if (peg$silentFails === 0) {
                      peg$fail(peg$e48);
                    }
                  }
                  if (s1 !== peg$FAILED) {
                    peg$savedPos = s0;
                    s1 = peg$f38();
                  }
                  s0 = s1;
                  if (s0 === peg$FAILED) {
                    s0 = peg$currPos;
                    if (input.substr(peg$currPos, 4) === peg$c45) {
                      s1 = peg$c45;
                      peg$currPos += 4;
                    } else {
                      s1 = peg$FAILED;
                      if (peg$silentFails === 0) {
                        peg$fail(peg$e49);
                      }
                    }
                    if (s1 !== peg$FAILED) {
                      peg$savedPos = s0;
                      s1 = peg$f39();
                    }
                    s0 = s1;
                    if (s0 === peg$FAILED) {
                      s0 = peg$currPos;
                      if (input.substr(peg$currPos, 4) === peg$c46) {
                        s1 = peg$c46;
                        peg$currPos += 4;
                      } else {
                        s1 = peg$FAILED;
                        if (peg$silentFails === 0) {
                          peg$fail(peg$e50);
                        }
                      }
                      if (s1 !== peg$FAILED) {
                        peg$savedPos = s0;
                        s1 = peg$f40();
                      }
                      s0 = s1;
                      if (s0 === peg$FAILED) {
                        s0 = peg$currPos;
                        if (input.substr(peg$currPos, 3) === peg$c47) {
                          s1 = peg$c47;
                          peg$currPos += 3;
                        } else {
                          s1 = peg$FAILED;
                          if (peg$silentFails === 0) {
                            peg$fail(peg$e51);
                          }
                        }
                        if (s1 !== peg$FAILED) {
                          peg$savedPos = s0;
                          s1 = peg$f41();
                        }
                        s0 = s1;
                        if (s0 === peg$FAILED) {
                          s0 = peg$currPos;
                          if (input.substr(peg$currPos, 4) === peg$c48) {
                            s1 = peg$c48;
                            peg$currPos += 4;
                          } else {
                            s1 = peg$FAILED;
                            if (peg$silentFails === 0) {
                              peg$fail(peg$e52);
                            }
                          }
                          if (s1 !== peg$FAILED) {
                            peg$savedPos = s0;
                            s1 = peg$f42();
                          }
                          s0 = s1;
                          if (s0 === peg$FAILED) {
                            s0 = peg$currPos;
                            if (input.substr(peg$currPos, 3) === peg$c49) {
                              s1 = peg$c49;
                              peg$currPos += 3;
                            } else {
                              s1 = peg$FAILED;
                              if (peg$silentFails === 0) {
                                peg$fail(peg$e53);
                              }
                            }
                            if (s1 !== peg$FAILED) {
                              peg$savedPos = s0;
                              s1 = peg$f43();
                            }
                            s0 = s1;
                            if (s0 === peg$FAILED) {
                              s0 = peg$currPos;
                              if (input.substr(peg$currPos, 3) === peg$c50) {
                                s1 = peg$c50;
                                peg$currPos += 3;
                              } else {
                                s1 = peg$FAILED;
                                if (peg$silentFails === 0) {
                                  peg$fail(peg$e54);
                                }
                              }
                              if (s1 !== peg$FAILED) {
                                peg$savedPos = s0;
                                s1 = peg$f44();
                              }
                              s0 = s1;
                              if (s0 === peg$FAILED) {
                                s0 = peg$currPos;
                                if (input.substr(peg$currPos, 3) === peg$c51) {
                                  s1 = peg$c51;
                                  peg$currPos += 3;
                                } else {
                                  s1 = peg$FAILED;
                                  if (peg$silentFails === 0) {
                                    peg$fail(peg$e55);
                                  }
                                }
                                if (s1 !== peg$FAILED) {
                                  peg$savedPos = s0;
                                  s1 = peg$f45();
                                }
                                s0 = s1;
                                if (s0 === peg$FAILED) {
                                  s0 = peg$currPos;
                                  if (input.substr(peg$currPos, 3) === peg$c52) {
                                    s1 = peg$c52;
                                    peg$currPos += 3;
                                  } else {
                                    s1 = peg$FAILED;
                                    if (peg$silentFails === 0) {
                                      peg$fail(peg$e56);
                                    }
                                  }
                                  if (s1 !== peg$FAILED) {
                                    peg$savedPos = s0;
                                    s1 = peg$f46();
                                  }
                                  s0 = s1;
                                  if (s0 === peg$FAILED) {
                                    s0 = peg$currPos;
                                    if (input.substr(peg$currPos, 3) === peg$c53) {
                                      s1 = peg$c53;
                                      peg$currPos += 3;
                                    } else {
                                      s1 = peg$FAILED;
                                      if (peg$silentFails === 0) {
                                        peg$fail(peg$e57);
                                      }
                                    }
                                    if (s1 !== peg$FAILED) {
                                      peg$savedPos = s0;
                                      s1 = peg$f47();
                                    }
                                    s0 = s1;
                                    if (s0 === peg$FAILED) {
                                      s0 = peg$currPos;
                                      if (input.substr(peg$currPos, 3) === peg$c0) {
                                        s1 = peg$c0;
                                        peg$currPos += 3;
                                      } else {
                                        s1 = peg$FAILED;
                                        if (peg$silentFails === 0) {
                                          peg$fail(peg$e0);
                                        }
                                      }
                                      if (s1 !== peg$FAILED) {
                                        peg$savedPos = s0;
                                        s1 = peg$f48();
                                      }
                                      s0 = s1;
                                    }
                                  }
                                }
                              }
                            }
                          }
                        }
                      }
                    }
                  }
                }
              }
            }
          }
        }
      }
    }
    return s0;
  }
  function peg$parseNodeRef() {
    var s0, s1, s2, s3, s4;
    s0 = peg$currPos;
    s1 = peg$parseIdentifier();
    if (s1 !== peg$FAILED) {
      if (input.charCodeAt(peg$currPos) === 91) {
        s2 = peg$c12;
        peg$currPos++;
      } else {
        s2 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e13);
        }
      }
      if (s2 !== peg$FAILED) {
        s3 = peg$parseSignedInteger();
        if (s3 !== peg$FAILED) {
          if (input.charCodeAt(peg$currPos) === 93) {
            s4 = peg$c14;
            peg$currPos++;
          } else {
            s4 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e15);
            }
          }
          if (s4 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f49(s1, s3);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      s1 = peg$parseIdentifier();
      if (s1 !== peg$FAILED) {
        if (input.charCodeAt(peg$currPos) === 46) {
          s2 = peg$c36;
          peg$currPos++;
        } else {
          s2 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e40);
          }
        }
        if (s2 !== peg$FAILED) {
          s3 = peg$parseIdentifier();
          if (s3 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f50(s1, s3);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    }
    return s0;
  }
  function peg$parseIdentifier() {
    var s0, s1, s2, s3, s4;
    s0 = peg$currPos;
    s1 = peg$currPos;
    s2 = input.charAt(peg$currPos);
    if (peg$r3.test(s2)) {
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e58);
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = [];
      s4 = input.charAt(peg$currPos);
      if (peg$r4.test(s4)) {
        peg$currPos++;
      } else {
        s4 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e59);
        }
      }
      while (s4 !== peg$FAILED) {
        s3.push(s4);
        s4 = input.charAt(peg$currPos);
        if (peg$r4.test(s4)) {
          peg$currPos++;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e59);
          }
        }
      }
      s2 = [s2, s3];
      s1 = s2;
    } else {
      peg$currPos = s1;
      s1 = peg$FAILED;
    }
    if (s1 !== peg$FAILED) {
      s0 = input.substring(s0, peg$currPos);
    } else {
      s0 = s1;
    }
    return s0;
  }
  function peg$parseCellContent() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9;
    s0 = peg$currPos;
    s1 = [];
    s2 = peg$currPos;
    s3 = peg$currPos;
    peg$silentFails++;
    s4 = peg$currPos;
    s5 = peg$parse_();
    if (input.substr(peg$currPos, 3) === peg$c11) {
      s6 = peg$c11;
      peg$currPos += 3;
    } else {
      s6 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e12);
      }
    }
    if (s6 !== peg$FAILED) {
      s7 = peg$parse_();
      s8 = peg$parseNL();
      if (s8 === peg$FAILED) {
        s8 = peg$currPos;
        peg$silentFails++;
        if (input.length > peg$currPos) {
          s9 = input.charAt(peg$currPos);
          peg$currPos++;
        } else {
          s9 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e18);
          }
        }
        peg$silentFails--;
        if (s9 === peg$FAILED) {
          s8 = void 0;
        } else {
          peg$currPos = s8;
          s8 = peg$FAILED;
        }
      }
      if (s8 !== peg$FAILED) {
        s5 = [s5, s6, s7, s8];
        s4 = s5;
      } else {
        peg$currPos = s4;
        s4 = peg$FAILED;
      }
    } else {
      peg$currPos = s4;
      s4 = peg$FAILED;
    }
    peg$silentFails--;
    if (s4 === peg$FAILED) {
      s3 = void 0;
    } else {
      peg$currPos = s3;
      s3 = peg$FAILED;
    }
    if (s3 !== peg$FAILED) {
      s4 = peg$currPos;
      s5 = [];
      s6 = input.charAt(peg$currPos);
      if (peg$r0.test(s6)) {
        peg$currPos++;
      } else {
        s6 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e1);
        }
      }
      while (s6 !== peg$FAILED) {
        s5.push(s6);
        s6 = input.charAt(peg$currPos);
        if (peg$r0.test(s6)) {
          peg$currPos++;
        } else {
          s6 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e1);
          }
        }
      }
      s4 = input.substring(s4, peg$currPos);
      s5 = peg$parseNL();
      if (s5 !== peg$FAILED) {
        peg$savedPos = s2;
        s2 = peg$f51(s4);
      } else {
        peg$currPos = s2;
        s2 = peg$FAILED;
      }
    } else {
      peg$currPos = s2;
      s2 = peg$FAILED;
    }
    while (s2 !== peg$FAILED) {
      s1.push(s2);
      s2 = peg$currPos;
      s3 = peg$currPos;
      peg$silentFails++;
      s4 = peg$currPos;
      s5 = peg$parse_();
      if (input.substr(peg$currPos, 3) === peg$c11) {
        s6 = peg$c11;
        peg$currPos += 3;
      } else {
        s6 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e12);
        }
      }
      if (s6 !== peg$FAILED) {
        s7 = peg$parse_();
        s8 = peg$parseNL();
        if (s8 === peg$FAILED) {
          s8 = peg$currPos;
          peg$silentFails++;
          if (input.length > peg$currPos) {
            s9 = input.charAt(peg$currPos);
            peg$currPos++;
          } else {
            s9 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e18);
            }
          }
          peg$silentFails--;
          if (s9 === peg$FAILED) {
            s8 = void 0;
          } else {
            peg$currPos = s8;
            s8 = peg$FAILED;
          }
        }
        if (s8 !== peg$FAILED) {
          s5 = [s5, s6, s7, s8];
          s4 = s5;
        } else {
          peg$currPos = s4;
          s4 = peg$FAILED;
        }
      } else {
        peg$currPos = s4;
        s4 = peg$FAILED;
      }
      peg$silentFails--;
      if (s4 === peg$FAILED) {
        s3 = void 0;
      } else {
        peg$currPos = s3;
        s3 = peg$FAILED;
      }
      if (s3 !== peg$FAILED) {
        s4 = peg$currPos;
        s5 = [];
        s6 = input.charAt(peg$currPos);
        if (peg$r0.test(s6)) {
          peg$currPos++;
        } else {
          s6 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e1);
          }
        }
        while (s6 !== peg$FAILED) {
          s5.push(s6);
          s6 = input.charAt(peg$currPos);
          if (peg$r0.test(s6)) {
            peg$currPos++;
          } else {
            s6 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e1);
            }
          }
        }
        s4 = input.substring(s4, peg$currPos);
        s5 = peg$parseNL();
        if (s5 !== peg$FAILED) {
          peg$savedPos = s2;
          s2 = peg$f51(s4);
        } else {
          peg$currPos = s2;
          s2 = peg$FAILED;
        }
      } else {
        peg$currPos = s2;
        s2 = peg$FAILED;
      }
    }
    peg$savedPos = s0;
    s1 = peg$f52(s1);
    s0 = s1;
    return s0;
  }
  function peg$parseQuotedString() {
    var s0, s1, s2, s3, s4;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 34) {
      s1 = peg$c54;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e60);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$currPos;
      s3 = [];
      s4 = input.charAt(peg$currPos);
      if (peg$r5.test(s4)) {
        peg$currPos++;
      } else {
        s4 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e61);
        }
      }
      while (s4 !== peg$FAILED) {
        s3.push(s4);
        s4 = input.charAt(peg$currPos);
        if (peg$r5.test(s4)) {
          peg$currPos++;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e61);
          }
        }
      }
      s2 = input.substring(s2, peg$currPos);
      if (input.charCodeAt(peg$currPos) === 34) {
        s3 = peg$c54;
        peg$currPos++;
      } else {
        s3 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e60);
        }
      }
      if (s3 !== peg$FAILED) {
        peg$savedPos = s0;
        s0 = peg$f53(s2);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseInteger() {
    var s0, s1, s2, s3;
    s0 = peg$currPos;
    s1 = peg$currPos;
    s2 = [];
    s3 = input.charAt(peg$currPos);
    if (peg$r2.test(s3)) {
      peg$currPos++;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e39);
      }
    }
    if (s3 !== peg$FAILED) {
      while (s3 !== peg$FAILED) {
        s2.push(s3);
        s3 = input.charAt(peg$currPos);
        if (peg$r2.test(s3)) {
          peg$currPos++;
        } else {
          s3 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e39);
          }
        }
      }
    } else {
      s2 = peg$FAILED;
    }
    if (s2 !== peg$FAILED) {
      s1 = input.substring(s1, peg$currPos);
    } else {
      s1 = s2;
    }
    if (s1 !== peg$FAILED) {
      peg$savedPos = s0;
      s1 = peg$f54(s1);
    }
    s0 = s1;
    return s0;
  }
  function peg$parseSignedInteger() {
    var s0, s1, s2, s3, s4;
    s0 = peg$currPos;
    s1 = input.charAt(peg$currPos);
    if (peg$r6.test(s1)) {
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e62);
      }
    }
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    s2 = peg$currPos;
    s3 = [];
    s4 = input.charAt(peg$currPos);
    if (peg$r2.test(s4)) {
      peg$currPos++;
    } else {
      s4 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e39);
      }
    }
    if (s4 !== peg$FAILED) {
      while (s4 !== peg$FAILED) {
        s3.push(s4);
        s4 = input.charAt(peg$currPos);
        if (peg$r2.test(s4)) {
          peg$currPos++;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e39);
          }
        }
      }
    } else {
      s3 = peg$FAILED;
    }
    if (s3 !== peg$FAILED) {
      s2 = input.substring(s2, peg$currPos);
    } else {
      s2 = s3;
    }
    if (s2 !== peg$FAILED) {
      peg$savedPos = s0;
      s0 = peg$f55(s1, s2);
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseBlankLine() {
    var s0, s1, s2, s3;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = peg$parseComment();
    if (s2 === peg$FAILED) {
      s2 = null;
    }
    s3 = peg$parseNL();
    if (s3 !== peg$FAILED) {
      peg$savedPos = s0;
      s0 = peg$f56();
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseComment() {
    var s0, s1, s2, s3;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 2) === peg$c55) {
      s1 = peg$c55;
      peg$currPos += 2;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e63);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = [];
      s3 = input.charAt(peg$currPos);
      if (peg$r0.test(s3)) {
        peg$currPos++;
      } else {
        s3 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e1);
        }
      }
      while (s3 !== peg$FAILED) {
        s2.push(s3);
        s3 = input.charAt(peg$currPos);
        if (peg$r0.test(s3)) {
          peg$currPos++;
        } else {
          s3 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e1);
          }
        }
      }
      s1 = [s1, s2];
      s0 = s1;
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parse_() {
    var s0, s1;
    s0 = [];
    s1 = input.charAt(peg$currPos);
    if (peg$r7.test(s1)) {
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e64);
      }
    }
    while (s1 !== peg$FAILED) {
      s0.push(s1);
      s1 = input.charAt(peg$currPos);
      if (peg$r7.test(s1)) {
        peg$currPos++;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e64);
        }
      }
    }
    return s0;
  }
  function peg$parse__() {
    var s0, s1;
    s0 = [];
    s1 = input.charAt(peg$currPos);
    if (peg$r7.test(s1)) {
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e64);
      }
    }
    if (s1 !== peg$FAILED) {
      while (s1 !== peg$FAILED) {
        s0.push(s1);
        s1 = input.charAt(peg$currPos);
        if (peg$r7.test(s1)) {
          peg$currPos++;
        } else {
          s1 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e64);
          }
        }
      }
    } else {
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseNL() {
    var s0;
    if (input.substr(peg$currPos, 2) === peg$c56) {
      s0 = peg$c56;
      peg$currPos += 2;
    } else {
      s0 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e65);
      }
    }
    if (s0 === peg$FAILED) {
      s0 = input.charAt(peg$currPos);
      if (peg$r8.test(s0)) {
        peg$currPos++;
      } else {
        s0 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e66);
        }
      }
    }
    return s0;
  }
  reset3();
  peg$result = peg$startRuleFunction();
  if (options.peg$library) {
    return (
      /** @type {any} */
      {
        peg$result,
        peg$currPos,
        peg$FAILED,
        peg$maxFailExpected,
        peg$maxFailPos
      }
    );
  }
  if (peg$result !== peg$FAILED && peg$currPos === input.length) {
    return peg$result;
  } else {
    if (peg$result !== peg$FAILED && peg$currPos < input.length) {
      peg$fail(peg$endExpectation());
    }
    throw peg$buildStructuredError(
      peg$maxFailExpected,
      peg$maxFailPos < input.length ? input.charAt(peg$maxFailPos) : null,
      peg$maxFailPos < input.length ? peg$computeLocation(peg$maxFailPos, peg$maxFailPos + 1) : peg$computeLocation(peg$maxFailPos, peg$maxFailPos)
    );
  }
}
var poster = {
  parseMermaid(input) {
    const raw = peg$parse3(input);
    const cells2 = raw.cells.map((c) => {
      const { rawContent, caption, notes } = extractCellAnnotations(c.rawContent);
      const content = parseCell({ ...c, rawContent });
      return {
        id: c.id,
        title: c.title,
        content,
        colSpan: c.span?.colSpan,
        rowSpan: c.span?.rowSpan,
        theme: c.theme,
        ...caption ? { caption } : {},
        ...notes.length > 0 ? { notes } : {}
      };
    });
    const explicitLinks = raw.links ?? [];
    const allLinks = [...explicitLinks];
    return {
      version: raw.version ?? "1.0",
      metadata: { ...raw.metadata },
      grid: raw.grid,
      cells: cells2,
      ...allLinks.length > 0 ? { links: allLinks } : {}
    };
  },
  parseYaml(input) {
    return JSON.parse(input);
  },
  layout(ir, theme, options) {
    return layoutPoster(ir, theme);
  }
};
var DEFAULT_HEADERS = {
  block: "block-beta\n",
  packet: "packet-beta\n",
  architecture: "architecture-beta\n",
  flowchart: "flowchart TD\n",
  sequence: "sequenceDiagram\n",
  state: "stateDiagram-v2\n",
  class: "classDiagram\n",
  er: "erDiagram\n",
  gitgraph: "gitGraph\n",
  gantt: "gantt\n",
  journey: "journey\n",
  pie: "pie\n",
  quadrant: "quadrantChart\n",
  requirement: "requirementDiagram\n",
  c4: "C4Context\n",
  sankey: "sankey-beta\n",
  timeline: "timeline\n",
  mindmap: "mindmap\n",
  kanban: "kanban\n",
  xychart: "xychart-beta\n",
  radar: "radar-beta\n",
  tree: "tree\n",
  plan: "plan\n",
  list: "list\n",
  fishbone: "fishbone\n",
  pyramid: "pyramid\n",
  loop: "loop\n",
  topology: "topology\n",
  bplustree: "bplustree\n",
  "b+tree": "bplustree\n",
  btree: "btree\n",
  merkletree: "merkletree\n",
  lsmtree: "lsmtree\n",
  behaviortree: "behaviortree\n",
  quadtree: "quadtree\n",
  treap: "treap\n",
  "234tree": "234tree\n",
  "2-3-4tree": "234tree\n"
};
function canonicalDiagramKind(rawKind) {
  if (!rawKind) return void 0;
  const k = rawKind.trim();
  const lower = k.toLowerCase();
  if (getModule(lower)) return lower;
  const matched = matchMermaid(k) ?? matchMermaid(lower);
  if (matched && getModule(matched)) return matched;
  if (lower === "flow") return "flowchart";
  if (lower.startsWith("block")) return "block";
  if (lower.startsWith("packet")) return "packet";
  if (lower.startsWith("arch")) return "architecture";
  if (lower.startsWith("seq")) return "sequence";
  if (lower.startsWith("state")) return "state";
  if (lower.startsWith("class")) return "class";
  if (lower.startsWith("er")) return "er";
  if (lower.startsWith("git")) return "gitgraph";
  if (lower.startsWith("quadtree")) return "quadtree";
  if (lower.startsWith("quad")) return "quadrant";
  if (lower.startsWith("req")) return "requirement";
  if (lower.startsWith("sankey")) return "sankey";
  if (lower.startsWith("radar")) return "radar";
  if (lower.startsWith("xychart")) return "xychart";
  if (lower.startsWith("mind")) return "mindmap";
  if (lower.startsWith("time")) return "timeline";
  if (lower.startsWith("bplus") || lower === "b+tree") return "bplustree";
  if (lower.startsWith("btree")) return "btree";
  if (lower.startsWith("merkle")) return "merkletree";
  if (lower.startsWith("lsm")) return "lsmtree";
  if (lower.startsWith("behavior")) return "behaviortree";
  if (lower.startsWith("treap")) return "treap";
  if (lower.startsWith("234") || lower.startsWith("2-3-4")) return "234tree";
  if (lower.startsWith("kan")) return "kanban";
  return void 0;
}
function ensureDiagramHeader(kind, rawContent) {
  const trimmed = rawContent.trimStart();
  if (!trimmed) return rawContent;
  const detected = matchMermaid(trimmed);
  if (detected === kind) return rawContent;
  const defaultHeader = DEFAULT_HEADERS[kind];
  if (defaultHeader) {
    return defaultHeader + rawContent;
  }
  if (!trimmed.startsWith(kind)) {
    return `${kind} ${rawContent}`;
  }
  return rawContent;
}
function parseCell(raw) {
  const kind = raw.kind;
  const inferredKind = inferCellKind(raw.rawContent);
  let rawKind = (kind || inferredKind || "").trim();
  if ((!rawKind || rawKind === "text") && !kind && raw.explicitId) {
    const fromId = canonicalDiagramKind(raw.explicitId);
    if (fromId) {
      rawKind = fromId;
    }
  }
  if (rawKind === "stat") {
    const [value, label] = raw.rawContent.split("|").map((s) => s.trim());
    return { kind: "stat", value: value ?? "", label };
  }
  if (rawKind === "text") {
    return { kind: "text", text: raw.rawContent.trim() };
  }
  const diagramKind = canonicalDiagramKind(rawKind);
  const module = diagramKind ? getModule(diagramKind) : void 0;
  if (!module || !diagramKind) {
    return { kind: "text", text: raw.rawContent.trim() };
  }
  const preparedContent = ensureDiagramHeader(diagramKind, raw.rawContent);
  const content = preparedContent.endsWith("\n") ? preparedContent : preparedContent + "\n";
  return { kind: "diagram", diagramKind, doc: module.parseMermaid(content) };
}
function inferCellKind(rawContent) {
  const trimmed = rawContent.trimStart();
  if (!trimmed.trim()) return "text";
  const diagramKind = matchMermaid(trimmed);
  if (diagramKind) return diagramKind;
  if (trimmed.includes("|")) return "stat";
  return "text";
}
var CAPTION_RE = /^[ \t]*caption[ \t]+"([^"]*)"[ \t]*$/;
var NOTE_RE = /^[ \t]*note[ \t]+"([^"]*)"(?:[ \t]+at[ \t]+(top-left|top-right|bottom-left|bottom-right|center))?[ \t]*$/;
function extractCellAnnotations(rawContent) {
  const inputLines = rawContent.split("\n");
  const outLines = [];
  let caption;
  const notes = [];
  for (const line of inputLines) {
    const cm = line.match(CAPTION_RE);
    if (cm) {
      caption = cm[1];
      continue;
    }
    const nm = line.match(NOTE_RE);
    if (nm) {
      notes.push({ text: nm[1], ...nm[2] ? { position: nm[2] } : {} });
      continue;
    }
    outLines.push(line);
  }
  return { rawContent: outLines.join("\n"), caption, notes };
}
function layoutPie(ir, theme) {
  const { palette, typography, spacing } = theme;
  const p = pen(theme);
  const margin = spacing.diagramMargin;
  const titleH = ir.metadata.title ? typography.titleFontSize + 20 : 0;
  const slices = ir.slices;
  const total = slices.reduce((s, x) => s + x.value, 0) || 1;
  const R = 110;
  const cx = margin + R;
  const cy = margin + titleH + R;
  const legendFont = typography.smallFontSize;
  const legendLabels = slices.map((s) => ir.showData ? `${s.label} [${s.value}]` : s.label);
  const legendTextW = legendLabels.reduce(
    (m, l) => Math.max(m, measureText(l, legendFont).width),
    0
  );
  const swatch = 10;
  const legendGap = 8;
  const legendX = cx + R + 36;
  const rowH = 20;
  const legendTop = cy - slices.length * rowH / 2;
  const elements = [];
  if (ir.metadata.title) {
    const totalWEst = legendX + legendTextW + swatch + legendGap + margin;
    elements.push(
      p.text(
        ir.metadata.title,
        rhuInt(totalWEst / 2),
        margin + typography.titleFontSize,
        typography.titleFontSize,
        palette.text,
        { weight: "bold", anchor: "middle" }
      )
    );
  }
  let a0 = -Math.PI / 2;
  slices.forEach((s, i) => {
    const frac = s.value / total;
    const a1 = a0 + frac * Math.PI * 2;
    const color = categoricalHue(i, theme);
    if (slices.length === 1) {
      elements.push(p.circle({ x: cx, y: cy }, R, color, palette.background, 1));
    } else {
      const x0 = rhu(cx + R * Math.cos(a0)), y0 = rhu(cy + R * Math.sin(a0));
      const x1 = rhu(cx + R * Math.cos(a1)), y1 = rhu(cy + R * Math.sin(a1));
      const large = frac > 0.5 ? 1 : 0;
      const d = `M ${rhu(cx)} ${rhu(cy)} L ${x0} ${y0} A ${R} ${R} 0 ${large} 1 ${x1} ${y1} Z`;
      elements.push(p.path(d, palette.background, 1, { fill: color }));
    }
    const pct = frac * 100;
    if (pct >= 4) {
      const am = (a0 + a1) / 2;
      const lr = R * 0.62;
      elements.push(
        p.text(
          `${pct.toFixed(0)}%`,
          rhu(cx + lr * Math.cos(am)),
          rhu(cy + lr * Math.sin(am) + typography.smallFontSize * 0.35),
          typography.smallFontSize,
          readableText(color, theme),
          { weight: "bold", anchor: "middle" }
        )
      );
    }
    a0 = a1;
  });
  slices.forEach((s, i) => {
    const ly = legendTop + i * rowH;
    const swatchColor = categoricalHue(i, theme);
    elements.push(
      p.rect(
        { x: legendX, y: ly, width: swatch, height: swatch },
        swatchColor,
        swatchColor,
        0,
        { rx: 2 }
      )
    );
    elements.push(
      p.text(
        legendLabels[i],
        legendX + swatch + legendGap,
        ly + swatch - 2,
        legendFont,
        palette.text
      )
    );
  });
  const totalW = rhuInt(legendX + legendTextW + swatch + legendGap + margin);
  const totalH = rhuInt(Math.max(cy + R, legendTop + slices.length * rowH) + margin);
  const scene = applyOverlays(
    {
      viewBox: { x: 0, y: 0, width: totalW, height: totalH },
      background: palette.background,
      elements
    },
    ir.overlays,
    theme
  );
  return { scene, anchors: {} };
}
function peg$subclass4(child, parent) {
  function C() {
    this.constructor = child;
  }
  C.prototype = parent.prototype;
  child.prototype = new C();
}
function peg$SyntaxError4(message, expected, found, location) {
  var self = Error.call(this, message);
  if (Object.setPrototypeOf) {
    Object.setPrototypeOf(self, peg$SyntaxError4.prototype);
  }
  self.expected = expected;
  self.found = found;
  self.location = location;
  self.name = "SyntaxError";
  return self;
}
peg$subclass4(peg$SyntaxError4, Error);
function peg$padEnd4(str, targetLength, padString) {
  padString = padString || " ";
  if (str.length > targetLength) {
    return str;
  }
  targetLength -= str.length;
  padString += padString.repeat(targetLength);
  return str + padString.slice(0, targetLength);
}
peg$SyntaxError4.prototype.format = function(sources) {
  var str = "Error: " + this.message;
  if (this.location) {
    var src = null;
    var k;
    for (k = 0; k < sources.length; k++) {
      if (sources[k].source === this.location.source) {
        src = sources[k].text.split(/\r\n|\n|\r/g);
        break;
      }
    }
    var s = this.location.start;
    var offset_s = this.location.source && typeof this.location.source.offset === "function" ? this.location.source.offset(s) : s;
    var loc = this.location.source + ":" + offset_s.line + ":" + offset_s.column;
    if (src) {
      var e = this.location.end;
      var filler = peg$padEnd4("", offset_s.line.toString().length, " ");
      var line = src[s.line - 1];
      var last = s.line === e.line ? e.column : line.length + 1;
      var hatLen = last - s.column || 1;
      str += "\n --> " + loc + "\n" + filler + " |\n" + offset_s.line + " | " + line + "\n" + filler + " | " + peg$padEnd4("", s.column - 1, " ") + peg$padEnd4("", hatLen, "^");
    } else {
      str += "\n at " + loc;
    }
  }
  return str;
};
peg$SyntaxError4.buildMessage = function(expected, found) {
  var DESCRIBE_EXPECTATION_FNS = {
    literal: function(expectation) {
      return '"' + literalEscape(expectation.text) + '"';
    },
    class: function(expectation) {
      var escapedParts = expectation.parts.map(function(part) {
        return Array.isArray(part) ? classEscape(part[0]) + "-" + classEscape(part[1]) : classEscape(part);
      });
      return "[" + (expectation.inverted ? "^" : "") + escapedParts.join("") + "]";
    },
    any: function() {
      return "any character";
    },
    end: function() {
      return "end of input";
    },
    other: function(expectation) {
      return expectation.description;
    }
  };
  function hex(ch) {
    return ch.charCodeAt(0).toString(16).toUpperCase();
  }
  function literalEscape(s) {
    return s.replace(/\\/g, "\\\\").replace(/"/g, '\\"').replace(/\0/g, "\\0").replace(/\t/g, "\\t").replace(/\n/g, "\\n").replace(/\r/g, "\\r").replace(/[\x00-\x0F]/g, function(ch) {
      return "\\x0" + hex(ch);
    }).replace(/[\x10-\x1F\x7F-\x9F]/g, function(ch) {
      return "\\x" + hex(ch);
    });
  }
  function classEscape(s) {
    return s.replace(/\\/g, "\\\\").replace(/\]/g, "\\]").replace(/\^/g, "\\^").replace(/-/g, "\\-").replace(/\0/g, "\\0").replace(/\t/g, "\\t").replace(/\n/g, "\\n").replace(/\r/g, "\\r").replace(/[\x00-\x0F]/g, function(ch) {
      return "\\x0" + hex(ch);
    }).replace(/[\x10-\x1F\x7F-\x9F]/g, function(ch) {
      return "\\x" + hex(ch);
    });
  }
  function describeExpectation(expectation) {
    return DESCRIBE_EXPECTATION_FNS[expectation.type](expectation);
  }
  function describeExpected(expected2) {
    var descriptions = expected2.map(describeExpectation);
    var i, j;
    descriptions.sort();
    if (descriptions.length > 0) {
      for (i = 1, j = 1; i < descriptions.length; i++) {
        if (descriptions[i - 1] !== descriptions[i]) {
          descriptions[j] = descriptions[i];
          j++;
        }
      }
      descriptions.length = j;
    }
    switch (descriptions.length) {
      case 1:
        return descriptions[0];
      case 2:
        return descriptions[0] + " or " + descriptions[1];
      default:
        return descriptions.slice(0, -1).join(", ") + ", or " + descriptions[descriptions.length - 1];
    }
  }
  function describeFound(found2) {
    return found2 ? '"' + literalEscape(found2) + '"' : "end of input";
  }
  return "Expected " + describeExpected(expected) + " but " + describeFound(found) + " found.";
};
function peg$parse4(input, options) {
  options = options !== void 0 ? options : {};
  var peg$FAILED = {};
  var peg$source = options.grammarSource;
  var peg$startRuleFunctions = { Document: peg$parseDocument };
  var peg$startRuleFunction = peg$parseDocument;
  var peg$c0 = "---";
  var peg$c1 = "pie";
  var peg$c2 = "showdata";
  var peg$c3 = "title";
  var peg$c4 = ":";
  var peg$c5 = "\r";
  var peg$c6 = "\n";
  var peg$c7 = '"';
  var peg$c8 = "-";
  var peg$c9 = ".";
  var peg$r0 = /^[^\n]/;
  var peg$r1 = /^[^"]/;
  var peg$r2 = /^[0-9]/;
  var peg$r3 = /^[ \t]/;
  var peg$e0 = peg$literalExpectation("---", false);
  var peg$e1 = peg$classExpectation(["\n"], true, false);
  var peg$e2 = peg$literalExpectation("pie", true);
  var peg$e3 = peg$literalExpectation("showData", true);
  var peg$e4 = peg$literalExpectation("title", true);
  var peg$e5 = peg$literalExpectation(":", false);
  var peg$e6 = peg$literalExpectation("\r", false);
  var peg$e7 = peg$literalExpectation("\n", false);
  var peg$e8 = peg$literalExpectation('"', false);
  var peg$e9 = peg$classExpectation(['"'], true, false);
  var peg$e10 = peg$literalExpectation("-", false);
  var peg$e11 = peg$classExpectation([["0", "9"]], false, false);
  var peg$e12 = peg$literalExpectation(".", false);
  var peg$e13 = peg$anyExpectation();
  var peg$e14 = peg$classExpectation([" ", "	"], false, false);
  var peg$f0 = function(fm, head, rows) {
    const slices = rows.filter(Boolean);
    return {
      version: "1.0",
      metadata: { ...fm || {}, ...head.title ? { title: head.title } : {} },
      slices,
      ...head.showData ? { showData: true } : {}
    };
  };
  var peg$f1 = function(line) {
    return line;
  };
  var peg$f2 = function(lines2) {
    const meta = {};
    for (const line of lines2) {
      const m = line.match(/^\s*(\w+)\s*:\s*(.+?)\s*$/);
      if (m) meta[m[1]] = m[2];
    }
    return meta;
  };
  var peg$f3 = function(sd, r) {
    return r;
  };
  var peg$f4 = function(sd, t) {
    return { showData: !!sd, title: t ? t.trim() : null };
  };
  var peg$f5 = function(label, value) {
    return { label, value };
  };
  var peg$f6 = function() {
    return null;
  };
  var peg$f7 = function(s) {
    return s;
  };
  var peg$f8 = function(n) {
    return parseFloat(n);
  };
  var peg$currPos = options.peg$currPos | 0;
  var peg$savedPos = peg$currPos;
  var peg$posDetailsCache = [{ line: 1, column: 1 }];
  var peg$maxFailPos = peg$currPos;
  var peg$maxFailExpected = options.peg$maxFailExpected || [];
  var peg$silentFails = options.peg$silentFails | 0;
  var peg$result;
  if (options.startRule) {
    if (!(options.startRule in peg$startRuleFunctions)) {
      throw new Error(`Can't start parsing from rule "` + options.startRule + '".');
    }
    peg$startRuleFunction = peg$startRuleFunctions[options.startRule];
  }
  function text() {
    return input.substring(peg$savedPos, peg$currPos);
  }
  function offset() {
    return peg$savedPos;
  }
  function range() {
    return {
      source: peg$source,
      start: peg$savedPos,
      end: peg$currPos
    };
  }
  function location() {
    return peg$computeLocation(peg$savedPos, peg$currPos);
  }
  function expected(description, location2) {
    location2 = location2 !== void 0 ? location2 : peg$computeLocation(peg$savedPos, peg$currPos);
    throw peg$buildStructuredError(
      [peg$otherExpectation(description)],
      input.substring(peg$savedPos, peg$currPos),
      location2
    );
  }
  function error(message, location2) {
    location2 = location2 !== void 0 ? location2 : peg$computeLocation(peg$savedPos, peg$currPos);
    throw peg$buildSimpleError(message, location2);
  }
  function peg$literalExpectation(text2, ignoreCase) {
    return { type: "literal", text: text2, ignoreCase };
  }
  function peg$classExpectation(parts, inverted, ignoreCase) {
    return { type: "class", parts, inverted, ignoreCase };
  }
  function peg$anyExpectation() {
    return { type: "any" };
  }
  function peg$endExpectation() {
    return { type: "end" };
  }
  function peg$otherExpectation(description) {
    return { type: "other", description };
  }
  function peg$computePosDetails(pos) {
    var details = peg$posDetailsCache[pos];
    var p;
    if (details) {
      return details;
    } else {
      if (pos >= peg$posDetailsCache.length) {
        p = peg$posDetailsCache.length - 1;
      } else {
        p = pos;
        while (!peg$posDetailsCache[--p]) {
        }
      }
      details = peg$posDetailsCache[p];
      details = {
        line: details.line,
        column: details.column
      };
      while (p < pos) {
        if (input.charCodeAt(p) === 10) {
          details.line++;
          details.column = 1;
        } else {
          details.column++;
        }
        p++;
      }
      peg$posDetailsCache[pos] = details;
      return details;
    }
  }
  function peg$computeLocation(startPos, endPos, offset2) {
    var startPosDetails = peg$computePosDetails(startPos);
    var endPosDetails = peg$computePosDetails(endPos);
    var res = {
      source: peg$source,
      start: {
        offset: startPos,
        line: startPosDetails.line,
        column: startPosDetails.column
      },
      end: {
        offset: endPos,
        line: endPosDetails.line,
        column: endPosDetails.column
      }
    };
    if (offset2 && peg$source && typeof peg$source.offset === "function") {
      res.start = peg$source.offset(res.start);
      res.end = peg$source.offset(res.end);
    }
    return res;
  }
  function peg$fail(expected2) {
    if (peg$currPos < peg$maxFailPos) {
      return;
    }
    if (peg$currPos > peg$maxFailPos) {
      peg$maxFailPos = peg$currPos;
      peg$maxFailExpected = [];
    }
    peg$maxFailExpected.push(expected2);
  }
  function peg$buildSimpleError(message, location2) {
    return new peg$SyntaxError4(message, null, null, location2);
  }
  function peg$buildStructuredError(expected2, found, location2) {
    return new peg$SyntaxError4(
      peg$SyntaxError4.buildMessage(expected2, found),
      expected2,
      found,
      location2
    );
  }
  function peg$parseDocument() {
    var s0, s1, s2, s3, s4, s5;
    s0 = peg$currPos;
    s1 = peg$parseFrontmatter();
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    s2 = peg$parse_();
    s3 = peg$parseHeader();
    if (s3 !== peg$FAILED) {
      s4 = [];
      s5 = peg$parseRow();
      while (s5 !== peg$FAILED) {
        s4.push(s5);
        s5 = peg$parseRow();
      }
      s5 = peg$parse_();
      peg$savedPos = s0;
      s0 = peg$f0(s1, s3, s4);
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseFrontmatter() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 3) === peg$c0) {
      s1 = peg$c0;
      peg$currPos += 3;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e0);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$parseNL();
      if (s2 !== peg$FAILED) {
        s3 = [];
        s4 = peg$currPos;
        s5 = peg$currPos;
        peg$silentFails++;
        s6 = peg$currPos;
        if (input.substr(peg$currPos, 3) === peg$c0) {
          s7 = peg$c0;
          peg$currPos += 3;
        } else {
          s7 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e0);
          }
        }
        if (s7 !== peg$FAILED) {
          s8 = peg$parseNL();
          if (s8 !== peg$FAILED) {
            s7 = [s7, s8];
            s6 = s7;
          } else {
            peg$currPos = s6;
            s6 = peg$FAILED;
          }
        } else {
          peg$currPos = s6;
          s6 = peg$FAILED;
        }
        peg$silentFails--;
        if (s6 === peg$FAILED) {
          s5 = void 0;
        } else {
          peg$currPos = s5;
          s5 = peg$FAILED;
        }
        if (s5 !== peg$FAILED) {
          s6 = peg$currPos;
          s7 = [];
          s8 = input.charAt(peg$currPos);
          if (peg$r0.test(s8)) {
            peg$currPos++;
          } else {
            s8 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e1);
            }
          }
          while (s8 !== peg$FAILED) {
            s7.push(s8);
            s8 = input.charAt(peg$currPos);
            if (peg$r0.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e1);
              }
            }
          }
          s6 = input.substring(s6, peg$currPos);
          s7 = peg$parseNL();
          if (s7 !== peg$FAILED) {
            peg$savedPos = s4;
            s4 = peg$f1(s6);
          } else {
            peg$currPos = s4;
            s4 = peg$FAILED;
          }
        } else {
          peg$currPos = s4;
          s4 = peg$FAILED;
        }
        while (s4 !== peg$FAILED) {
          s3.push(s4);
          s4 = peg$currPos;
          s5 = peg$currPos;
          peg$silentFails++;
          s6 = peg$currPos;
          if (input.substr(peg$currPos, 3) === peg$c0) {
            s7 = peg$c0;
            peg$currPos += 3;
          } else {
            s7 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e0);
            }
          }
          if (s7 !== peg$FAILED) {
            s8 = peg$parseNL();
            if (s8 !== peg$FAILED) {
              s7 = [s7, s8];
              s6 = s7;
            } else {
              peg$currPos = s6;
              s6 = peg$FAILED;
            }
          } else {
            peg$currPos = s6;
            s6 = peg$FAILED;
          }
          peg$silentFails--;
          if (s6 === peg$FAILED) {
            s5 = void 0;
          } else {
            peg$currPos = s5;
            s5 = peg$FAILED;
          }
          if (s5 !== peg$FAILED) {
            s6 = peg$currPos;
            s7 = [];
            s8 = input.charAt(peg$currPos);
            if (peg$r0.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e1);
              }
            }
            while (s8 !== peg$FAILED) {
              s7.push(s8);
              s8 = input.charAt(peg$currPos);
              if (peg$r0.test(s8)) {
                peg$currPos++;
              } else {
                s8 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e1);
                }
              }
            }
            s6 = input.substring(s6, peg$currPos);
            s7 = peg$parseNL();
            if (s7 !== peg$FAILED) {
              peg$savedPos = s4;
              s4 = peg$f1(s6);
            } else {
              peg$currPos = s4;
              s4 = peg$FAILED;
            }
          } else {
            peg$currPos = s4;
            s4 = peg$FAILED;
          }
        }
        if (input.substr(peg$currPos, 3) === peg$c0) {
          s4 = peg$c0;
          peg$currPos += 3;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e0);
          }
        }
        if (s4 !== peg$FAILED) {
          s5 = peg$parseNL();
          if (s5 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f2(s3);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseHeader() {
    var s0, s1, s2, s3, s4, s5, s6, s7;
    s0 = peg$currPos;
    s1 = input.substr(peg$currPos, 3);
    if (s1.toLowerCase() === peg$c1) {
      peg$currPos += 3;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e2);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$currPos;
      s3 = peg$parse_();
      s4 = input.substr(peg$currPos, 8);
      if (s4.toLowerCase() === peg$c2) {
        peg$currPos += 8;
      } else {
        s4 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e3);
        }
      }
      if (s4 !== peg$FAILED) {
        s3 = [s3, s4];
        s2 = s3;
      } else {
        peg$currPos = s2;
        s2 = peg$FAILED;
      }
      if (s2 === peg$FAILED) {
        s2 = null;
      }
      s3 = peg$currPos;
      s4 = peg$parse_();
      s5 = input.substr(peg$currPos, 5);
      if (s5.toLowerCase() === peg$c3) {
        peg$currPos += 5;
      } else {
        s5 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e4);
        }
      }
      if (s5 !== peg$FAILED) {
        s6 = peg$parse_();
        s7 = peg$parseRestOfLine();
        if (s7 !== peg$FAILED) {
          peg$savedPos = s3;
          s3 = peg$f3(s2, s7);
        } else {
          peg$currPos = s3;
          s3 = peg$FAILED;
        }
      } else {
        peg$currPos = s3;
        s3 = peg$FAILED;
      }
      if (s3 === peg$FAILED) {
        s3 = null;
      }
      s4 = peg$parseLineEnd();
      if (s4 !== peg$FAILED) {
        peg$savedPos = s0;
        s0 = peg$f4(s2, s3);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseRow() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = peg$parseQuoted();
    if (s2 !== peg$FAILED) {
      s3 = peg$parse_();
      if (input.charCodeAt(peg$currPos) === 58) {
        s4 = peg$c4;
        peg$currPos++;
      } else {
        s4 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e5);
        }
      }
      if (s4 !== peg$FAILED) {
        s5 = peg$parse_();
        s6 = peg$parseNum();
        if (s6 !== peg$FAILED) {
          s7 = peg$parse_();
          s8 = peg$parseLineEnd();
          if (s8 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f5(s2, s6);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      s1 = peg$parse_();
      if (input.charCodeAt(peg$currPos) === 13) {
        s2 = peg$c5;
        peg$currPos++;
      } else {
        s2 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e6);
        }
      }
      if (s2 === peg$FAILED) {
        s2 = null;
      }
      if (input.charCodeAt(peg$currPos) === 10) {
        s3 = peg$c6;
        peg$currPos++;
      } else {
        s3 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e7);
        }
      }
      if (s3 !== peg$FAILED) {
        peg$savedPos = s0;
        s0 = peg$f6();
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    }
    return s0;
  }
  function peg$parseQuoted() {
    var s0, s1, s2, s3, s4;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 34) {
      s1 = peg$c7;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e8);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$currPos;
      s3 = [];
      s4 = input.charAt(peg$currPos);
      if (peg$r1.test(s4)) {
        peg$currPos++;
      } else {
        s4 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e9);
        }
      }
      while (s4 !== peg$FAILED) {
        s3.push(s4);
        s4 = input.charAt(peg$currPos);
        if (peg$r1.test(s4)) {
          peg$currPos++;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e9);
          }
        }
      }
      s2 = input.substring(s2, peg$currPos);
      if (input.charCodeAt(peg$currPos) === 34) {
        s3 = peg$c7;
        peg$currPos++;
      } else {
        s3 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e8);
        }
      }
      if (s3 !== peg$FAILED) {
        peg$savedPos = s0;
        s0 = peg$f7(s2);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseNum() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8;
    s0 = peg$currPos;
    s1 = peg$currPos;
    s2 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 45) {
      s3 = peg$c8;
      peg$currPos++;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e10);
      }
    }
    if (s3 === peg$FAILED) {
      s3 = null;
    }
    s4 = [];
    s5 = input.charAt(peg$currPos);
    if (peg$r2.test(s5)) {
      peg$currPos++;
    } else {
      s5 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e11);
      }
    }
    if (s5 !== peg$FAILED) {
      while (s5 !== peg$FAILED) {
        s4.push(s5);
        s5 = input.charAt(peg$currPos);
        if (peg$r2.test(s5)) {
          peg$currPos++;
        } else {
          s5 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e11);
          }
        }
      }
    } else {
      s4 = peg$FAILED;
    }
    if (s4 !== peg$FAILED) {
      s5 = peg$currPos;
      if (input.charCodeAt(peg$currPos) === 46) {
        s6 = peg$c9;
        peg$currPos++;
      } else {
        s6 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e12);
        }
      }
      if (s6 !== peg$FAILED) {
        s7 = [];
        s8 = input.charAt(peg$currPos);
        if (peg$r2.test(s8)) {
          peg$currPos++;
        } else {
          s8 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e11);
          }
        }
        if (s8 !== peg$FAILED) {
          while (s8 !== peg$FAILED) {
            s7.push(s8);
            s8 = input.charAt(peg$currPos);
            if (peg$r2.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e11);
              }
            }
          }
        } else {
          s7 = peg$FAILED;
        }
        if (s7 !== peg$FAILED) {
          s6 = [s6, s7];
          s5 = s6;
        } else {
          peg$currPos = s5;
          s5 = peg$FAILED;
        }
      } else {
        peg$currPos = s5;
        s5 = peg$FAILED;
      }
      if (s5 === peg$FAILED) {
        s5 = null;
      }
      s3 = [s3, s4, s5];
      s2 = s3;
    } else {
      peg$currPos = s2;
      s2 = peg$FAILED;
    }
    if (s2 !== peg$FAILED) {
      s1 = input.substring(s1, peg$currPos);
    } else {
      s1 = s2;
    }
    if (s1 !== peg$FAILED) {
      peg$savedPos = s0;
      s1 = peg$f8(s1);
    }
    s0 = s1;
    return s0;
  }
  function peg$parseRestOfLine() {
    var s0, s1, s2;
    s0 = peg$currPos;
    s1 = [];
    s2 = input.charAt(peg$currPos);
    if (peg$r0.test(s2)) {
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e1);
      }
    }
    if (s2 !== peg$FAILED) {
      while (s2 !== peg$FAILED) {
        s1.push(s2);
        s2 = input.charAt(peg$currPos);
        if (peg$r0.test(s2)) {
          peg$currPos++;
        } else {
          s2 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e1);
          }
        }
      }
    } else {
      s1 = peg$FAILED;
    }
    if (s1 !== peg$FAILED) {
      s0 = input.substring(s0, peg$currPos);
    } else {
      s0 = s1;
    }
    return s0;
  }
  function peg$parseNL() {
    var s0, s1, s2;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 13) {
      s1 = peg$c5;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e6);
      }
    }
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s2 = peg$c6;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e7);
      }
    }
    if (s2 !== peg$FAILED) {
      s1 = [s1, s2];
      s0 = s1;
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseLineEnd() {
    var s0, s1, s2;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 13) {
      s1 = peg$c5;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e6);
      }
    }
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s2 = peg$c6;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e7);
      }
    }
    if (s2 !== peg$FAILED) {
      s1 = [s1, s2];
      s0 = s1;
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      peg$silentFails++;
      if (input.length > peg$currPos) {
        s1 = input.charAt(peg$currPos);
        peg$currPos++;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e13);
        }
      }
      peg$silentFails--;
      if (s1 === peg$FAILED) {
        s0 = void 0;
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    }
    return s0;
  }
  function peg$parse_() {
    var s0, s1;
    s0 = [];
    s1 = input.charAt(peg$currPos);
    if (peg$r3.test(s1)) {
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e14);
      }
    }
    while (s1 !== peg$FAILED) {
      s0.push(s1);
      s1 = input.charAt(peg$currPos);
      if (peg$r3.test(s1)) {
        peg$currPos++;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e14);
        }
      }
    }
    return s0;
  }
  peg$result = peg$startRuleFunction();
  if (options.peg$library) {
    return (
      /** @type {any} */
      {
        peg$result,
        peg$currPos,
        peg$FAILED,
        peg$maxFailExpected,
        peg$maxFailPos
      }
    );
  }
  if (peg$result !== peg$FAILED && peg$currPos === input.length) {
    return peg$result;
  } else {
    if (peg$result !== peg$FAILED && peg$currPos < input.length) {
      peg$fail(peg$endExpectation());
    }
    throw peg$buildStructuredError(
      peg$maxFailExpected,
      peg$maxFailPos < input.length ? input.charAt(peg$maxFailPos) : null,
      peg$maxFailPos < input.length ? peg$computeLocation(peg$maxFailPos, peg$maxFailPos + 1) : peg$computeLocation(peg$maxFailPos, peg$maxFailPos)
    );
  }
}
var pie = {
  parseMermaid(input) {
    return peg$parse4(input);
  },
  parseYaml(input) {
    return JSON.parse(input);
  },
  layout(ir, theme) {
    return layoutPie(ir, theme);
  }
};
function niceTicks(min, max) {
  const span = max - min || 1;
  const rawStep = span / 5;
  const mag = Math.pow(10, Math.floor(Math.log10(rawStep)));
  const norm = rawStep / mag;
  const step = (norm >= 5 ? 5 : norm >= 2 ? 2 : 1) * mag;
  const ticks = [];
  for (let v = Math.ceil(min / step) * step; v <= max + 1e-9; v += step)
    ticks.push(Math.round(v * 1e6) / 1e6);
  return ticks;
}
function fmtTick(n) {
  if (Math.abs(n) >= 1e3) return `${n / 1e3}k`;
  return String(n);
}
function layoutXYChart(ir, theme) {
  const { palette, typography, spacing } = theme;
  const p = pen(theme);
  const margin = spacing.diagramMargin;
  const cats = ir.categories;
  const nCats = Math.max(cats.length, 1);
  const series = ir.series;
  const barSeries = series.filter((s) => s.kind === "bar");
  const lineSeries = series.filter((s) => s.kind === "line");
  const allVals = series.flatMap((s) => s.values);
  const dataMin = allVals.length ? Math.min(...allVals) : 0;
  const dataMax = allVals.length ? Math.max(...allVals) : 1;
  const yMin = ir.yMin ?? Math.min(0, dataMin);
  const yMax = ir.yMax ?? (dataMax > 0 ? dataMax * 1.1 : 1);
  const ticks = niceTicks(yMin, yMax);
  const title = ir.metadata.title;
  const titleH = title ? typography.titleFontSize + 18 : 0;
  const tickFont = typography.smallFontSize;
  const tickW = Math.max(...ticks.map((t) => measureText(fmtTick(t), tickFont).width), 0);
  const yLabelW = ir.yLabel ? typography.baseFontSize + 6 : 0;
  const plotLeft = margin + yLabelW + tickW + 10;
  const plotTop = margin + titleH;
  const plotH = 360;
  const plotBottom = plotTop + plotH;
  const bandW = 78;
  const plotW = bandW * nCats;
  const plotRight = plotLeft + plotW;
  const yToPx = (v) => rhu(plotBottom - (v - yMin) / (yMax - yMin || 1) * plotH);
  const catX = (i) => plotLeft + (i + 0.5) * bandW;
  const elements = [];
  if (title) {
    elements.push(
      p.text(
        title,
        margin,
        margin + typography.titleFontSize,
        typography.titleFontSize,
        palette.text,
        { weight: "bold" }
      )
    );
  }
  for (const t of ticks) {
    const y = yToPx(t);
    elements.push(
      p.path(`M ${plotLeft} ${y} L ${plotRight} ${y}`, palette.border, 1, { opacity: 0.5 })
    );
    elements.push(
      p.text(fmtTick(t), plotLeft - 8, y + tickFont * 0.35, tickFont, palette.textMuted, {
        anchor: "end"
      })
    );
  }
  elements.push(p.path(`M ${plotLeft} ${plotTop} L ${plotLeft} ${plotBottom}`, palette.text, 1.5));
  elements.push(
    p.path(`M ${plotLeft} ${plotBottom} L ${plotRight} ${plotBottom}`, palette.text, 1.5)
  );
  if (ir.yLabel) {
    const ly = rhuInt(plotTop + plotH / 2);
    const lx = margin + typography.baseFontSize;
    elements.push(
      p.text(ir.yLabel, lx, ly, typography.baseFontSize, palette.textMuted, {
        anchor: "middle",
        weight: "bold"
      })
    );
    const last = elements.pop();
    elements.push(p.group([last], { transform: `rotate(-90 ${lx} ${ly})` }));
  }
  const nBars = Math.max(barSeries.length, 1);
  const groupW = bandW * 0.62;
  const barW = groupW / nBars;
  barSeries.forEach((s, si) => {
    const color = categoricalHue(si, theme);
    s.values.forEach((v, i) => {
      const x = catX(i) - groupW / 2 + si * barW;
      const y = yToPx(v);
      const h2 = yToPx(yMin) - y;
      if (h2 > 0)
        elements.push(
          p.rect({ x: rhu(x), y, width: rhu(barW - 2), height: rhu(h2) }, color, color, 0, {
            rx: 2
          })
        );
    });
  });
  lineSeries.forEach((s, si) => {
    const color = categoricalHue(barSeries.length + si, theme);
    const pts = s.values.map((v, i) => `${rhu(catX(i))} ${yToPx(v)}`);
    if (pts.length) {
      elements.push(p.path(`M ${pts.join(" L ")}`, color, 2.5));
      s.values.forEach((v, i) => {
        elements.push(
          p.circle({ x: rhu(catX(i)), y: yToPx(v) }, 4, color, palette.background, 1.5)
        );
      });
    }
  });
  cats.forEach((c, i) => {
    elements.push(
      p.text(c, rhu(catX(i)), plotBottom + tickFont + 8, tickFont, palette.textMuted, {
        anchor: "middle"
      })
    );
  });
  const totalW = rhuInt(plotRight + margin);
  const totalH = rhuInt(plotBottom + tickFont + 16 + margin);
  const scene = applyOverlays(
    {
      viewBox: { x: 0, y: 0, width: totalW, height: totalH },
      background: palette.background,
      elements
    },
    ir.overlays,
    theme
  );
  return { scene, anchors: {} };
}
function peg$subclass5(child, parent) {
  function C() {
    this.constructor = child;
  }
  C.prototype = parent.prototype;
  child.prototype = new C();
}
function peg$SyntaxError5(message, expected, found, location) {
  var self = Error.call(this, message);
  if (Object.setPrototypeOf) {
    Object.setPrototypeOf(self, peg$SyntaxError5.prototype);
  }
  self.expected = expected;
  self.found = found;
  self.location = location;
  self.name = "SyntaxError";
  return self;
}
peg$subclass5(peg$SyntaxError5, Error);
function peg$padEnd5(str, targetLength, padString) {
  padString = padString || " ";
  if (str.length > targetLength) {
    return str;
  }
  targetLength -= str.length;
  padString += padString.repeat(targetLength);
  return str + padString.slice(0, targetLength);
}
peg$SyntaxError5.prototype.format = function(sources) {
  var str = "Error: " + this.message;
  if (this.location) {
    var src = null;
    var k;
    for (k = 0; k < sources.length; k++) {
      if (sources[k].source === this.location.source) {
        src = sources[k].text.split(/\r\n|\n|\r/g);
        break;
      }
    }
    var s = this.location.start;
    var offset_s = this.location.source && typeof this.location.source.offset === "function" ? this.location.source.offset(s) : s;
    var loc = this.location.source + ":" + offset_s.line + ":" + offset_s.column;
    if (src) {
      var e = this.location.end;
      var filler = peg$padEnd5("", offset_s.line.toString().length, " ");
      var line = src[s.line - 1];
      var last = s.line === e.line ? e.column : line.length + 1;
      var hatLen = last - s.column || 1;
      str += "\n --> " + loc + "\n" + filler + " |\n" + offset_s.line + " | " + line + "\n" + filler + " | " + peg$padEnd5("", s.column - 1, " ") + peg$padEnd5("", hatLen, "^");
    } else {
      str += "\n at " + loc;
    }
  }
  return str;
};
peg$SyntaxError5.buildMessage = function(expected, found) {
  var DESCRIBE_EXPECTATION_FNS = {
    literal: function(expectation) {
      return '"' + literalEscape(expectation.text) + '"';
    },
    class: function(expectation) {
      var escapedParts = expectation.parts.map(function(part) {
        return Array.isArray(part) ? classEscape(part[0]) + "-" + classEscape(part[1]) : classEscape(part);
      });
      return "[" + (expectation.inverted ? "^" : "") + escapedParts.join("") + "]";
    },
    any: function() {
      return "any character";
    },
    end: function() {
      return "end of input";
    },
    other: function(expectation) {
      return expectation.description;
    }
  };
  function hex(ch) {
    return ch.charCodeAt(0).toString(16).toUpperCase();
  }
  function literalEscape(s) {
    return s.replace(/\\/g, "\\\\").replace(/"/g, '\\"').replace(/\0/g, "\\0").replace(/\t/g, "\\t").replace(/\n/g, "\\n").replace(/\r/g, "\\r").replace(/[\x00-\x0F]/g, function(ch) {
      return "\\x0" + hex(ch);
    }).replace(/[\x10-\x1F\x7F-\x9F]/g, function(ch) {
      return "\\x" + hex(ch);
    });
  }
  function classEscape(s) {
    return s.replace(/\\/g, "\\\\").replace(/\]/g, "\\]").replace(/\^/g, "\\^").replace(/-/g, "\\-").replace(/\0/g, "\\0").replace(/\t/g, "\\t").replace(/\n/g, "\\n").replace(/\r/g, "\\r").replace(/[\x00-\x0F]/g, function(ch) {
      return "\\x0" + hex(ch);
    }).replace(/[\x10-\x1F\x7F-\x9F]/g, function(ch) {
      return "\\x" + hex(ch);
    });
  }
  function describeExpectation(expectation) {
    return DESCRIBE_EXPECTATION_FNS[expectation.type](expectation);
  }
  function describeExpected(expected2) {
    var descriptions = expected2.map(describeExpectation);
    var i, j;
    descriptions.sort();
    if (descriptions.length > 0) {
      for (i = 1, j = 1; i < descriptions.length; i++) {
        if (descriptions[i - 1] !== descriptions[i]) {
          descriptions[j] = descriptions[i];
          j++;
        }
      }
      descriptions.length = j;
    }
    switch (descriptions.length) {
      case 1:
        return descriptions[0];
      case 2:
        return descriptions[0] + " or " + descriptions[1];
      default:
        return descriptions.slice(0, -1).join(", ") + ", or " + descriptions[descriptions.length - 1];
    }
  }
  function describeFound(found2) {
    return found2 ? '"' + literalEscape(found2) + '"' : "end of input";
  }
  return "Expected " + describeExpected(expected) + " but " + describeFound(found) + " found.";
};
function peg$parse5(input, options) {
  options = options !== void 0 ? options : {};
  var peg$FAILED = {};
  var peg$source = options.grammarSource;
  var peg$startRuleFunctions = { Document: peg$parseDocument };
  var peg$startRuleFunction = peg$parseDocument;
  var peg$c0 = "xychart-beta";
  var peg$c1 = "horizontal";
  var peg$c2 = "vertical";
  var peg$c3 = "---";
  var peg$c4 = "title";
  var peg$c5 = "x-axis";
  var peg$c6 = "[";
  var peg$c7 = ",";
  var peg$c8 = "]";
  var peg$c9 = "y-axis";
  var peg$c10 = "-->";
  var peg$c11 = "bar";
  var peg$c12 = "line";
  var peg$c13 = '"';
  var peg$c14 = "-";
  var peg$c15 = ".";
  var peg$c16 = "\r";
  var peg$c17 = "\n";
  var peg$r0 = /^[^\n]/;
  var peg$r1 = /^[^,\]\n]/;
  var peg$r2 = /^[^"]/;
  var peg$r3 = /^[0-9]/;
  var peg$r4 = /^[ \t]/;
  var peg$e0 = peg$literalExpectation("xychart-beta", true);
  var peg$e1 = peg$literalExpectation("horizontal", true);
  var peg$e2 = peg$literalExpectation("vertical", true);
  var peg$e3 = peg$literalExpectation("---", false);
  var peg$e4 = peg$classExpectation(["\n"], true, false);
  var peg$e5 = peg$literalExpectation("title", true);
  var peg$e6 = peg$literalExpectation("x-axis", true);
  var peg$e7 = peg$literalExpectation("[", false);
  var peg$e8 = peg$literalExpectation(",", false);
  var peg$e9 = peg$literalExpectation("]", false);
  var peg$e10 = peg$literalExpectation("y-axis", true);
  var peg$e11 = peg$literalExpectation("-->", false);
  var peg$e12 = peg$literalExpectation("bar", true);
  var peg$e13 = peg$literalExpectation("line", true);
  var peg$e14 = peg$classExpectation([",", "]", "\n"], true, false);
  var peg$e15 = peg$literalExpectation('"', false);
  var peg$e16 = peg$classExpectation(['"'], true, false);
  var peg$e17 = peg$literalExpectation("-", false);
  var peg$e18 = peg$classExpectation([["0", "9"]], false, false);
  var peg$e19 = peg$literalExpectation(".", false);
  var peg$e20 = peg$literalExpectation("\r", false);
  var peg$e21 = peg$literalExpectation("\n", false);
  var peg$e22 = peg$anyExpectation();
  var peg$e23 = peg$classExpectation([" ", "	"], false, false);
  var peg$f0 = function(fm, stmts) {
    let title;
    let categories = [];
    let yLabel, yMin, yMax;
    const series = [];
    for (const s of stmts.filter(Boolean)) {
      if (s.t === "title") title = s.v;
      else if (s.t === "x") categories = s.cats;
      else if (s.t === "y") {
        yLabel = s.label;
        yMin = s.min;
        yMax = s.max;
      } else if (s.t === "series") series.push({ kind: s.kind, values: s.values });
    }
    return {
      version: "1.0",
      metadata: { ...fm || {}, ...title ? { title } : {} },
      categories,
      ...yLabel !== void 0 ? { yLabel } : {},
      ...yMin !== void 0 ? { yMin } : {},
      ...yMax !== void 0 ? { yMax } : {},
      series
    };
  };
  var peg$f1 = function(line) {
    return line;
  };
  var peg$f2 = function(lines2) {
    const meta = {};
    for (const line of lines2) {
      const m = line.match(/^\s*(\w+)\s*:\s*(.+?)\s*$/);
      if (m) meta[m[1]] = m[2];
    }
    return meta;
  };
  var peg$f3 = function(v) {
    return { t: "title", v: typeof v === "string" ? v.trim() : v };
  };
  var peg$f4 = function(first, i) {
    return i;
  };
  var peg$f5 = function(first, rest) {
    return { t: "x", cats: [first, ...rest] };
  };
  var peg$f6 = function(q) {
    return q;
  };
  var peg$f7 = function(label, a, b) {
    return { min: a, max: b };
  };
  var peg$f8 = function(label, range2) {
    return { t: "y", label: label || void 0, min: range2 ? range2.min : void 0, max: range2 ? range2.max : void 0 };
  };
  var peg$f9 = function(kind, first, n) {
    return n;
  };
  var peg$f10 = function(kind, first, rest) {
    return { t: "series", kind: kind.toLowerCase(), values: [first, ...rest] };
  };
  var peg$f11 = function(s) {
    return s.trim();
  };
  var peg$f12 = function(n) {
    return parseFloat(n);
  };
  var peg$f13 = function() {
    return null;
  };
  var peg$currPos = options.peg$currPos | 0;
  var peg$savedPos = peg$currPos;
  var peg$posDetailsCache = [{ line: 1, column: 1 }];
  var peg$maxFailPos = peg$currPos;
  var peg$maxFailExpected = options.peg$maxFailExpected || [];
  var peg$silentFails = options.peg$silentFails | 0;
  var peg$result;
  if (options.startRule) {
    if (!(options.startRule in peg$startRuleFunctions)) {
      throw new Error(`Can't start parsing from rule "` + options.startRule + '".');
    }
    peg$startRuleFunction = peg$startRuleFunctions[options.startRule];
  }
  function text() {
    return input.substring(peg$savedPos, peg$currPos);
  }
  function offset() {
    return peg$savedPos;
  }
  function range() {
    return {
      source: peg$source,
      start: peg$savedPos,
      end: peg$currPos
    };
  }
  function location() {
    return peg$computeLocation(peg$savedPos, peg$currPos);
  }
  function expected(description, location2) {
    location2 = location2 !== void 0 ? location2 : peg$computeLocation(peg$savedPos, peg$currPos);
    throw peg$buildStructuredError(
      [peg$otherExpectation(description)],
      input.substring(peg$savedPos, peg$currPos),
      location2
    );
  }
  function error(message, location2) {
    location2 = location2 !== void 0 ? location2 : peg$computeLocation(peg$savedPos, peg$currPos);
    throw peg$buildSimpleError(message, location2);
  }
  function peg$literalExpectation(text2, ignoreCase) {
    return { type: "literal", text: text2, ignoreCase };
  }
  function peg$classExpectation(parts, inverted, ignoreCase) {
    return { type: "class", parts, inverted, ignoreCase };
  }
  function peg$anyExpectation() {
    return { type: "any" };
  }
  function peg$endExpectation() {
    return { type: "end" };
  }
  function peg$otherExpectation(description) {
    return { type: "other", description };
  }
  function peg$computePosDetails(pos) {
    var details = peg$posDetailsCache[pos];
    var p;
    if (details) {
      return details;
    } else {
      if (pos >= peg$posDetailsCache.length) {
        p = peg$posDetailsCache.length - 1;
      } else {
        p = pos;
        while (!peg$posDetailsCache[--p]) {
        }
      }
      details = peg$posDetailsCache[p];
      details = {
        line: details.line,
        column: details.column
      };
      while (p < pos) {
        if (input.charCodeAt(p) === 10) {
          details.line++;
          details.column = 1;
        } else {
          details.column++;
        }
        p++;
      }
      peg$posDetailsCache[pos] = details;
      return details;
    }
  }
  function peg$computeLocation(startPos, endPos, offset2) {
    var startPosDetails = peg$computePosDetails(startPos);
    var endPosDetails = peg$computePosDetails(endPos);
    var res = {
      source: peg$source,
      start: {
        offset: startPos,
        line: startPosDetails.line,
        column: startPosDetails.column
      },
      end: {
        offset: endPos,
        line: endPosDetails.line,
        column: endPosDetails.column
      }
    };
    if (offset2 && peg$source && typeof peg$source.offset === "function") {
      res.start = peg$source.offset(res.start);
      res.end = peg$source.offset(res.end);
    }
    return res;
  }
  function peg$fail(expected2) {
    if (peg$currPos < peg$maxFailPos) {
      return;
    }
    if (peg$currPos > peg$maxFailPos) {
      peg$maxFailPos = peg$currPos;
      peg$maxFailExpected = [];
    }
    peg$maxFailExpected.push(expected2);
  }
  function peg$buildSimpleError(message, location2) {
    return new peg$SyntaxError5(message, null, null, location2);
  }
  function peg$buildStructuredError(expected2, found, location2) {
    return new peg$SyntaxError5(
      peg$SyntaxError5.buildMessage(expected2, found),
      expected2,
      found,
      location2
    );
  }
  function peg$parseDocument() {
    var s0, s1, s2, s3, s4, s5, s6, s7;
    s0 = peg$currPos;
    s1 = peg$parseFrontmatter();
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    s2 = peg$parse_();
    s3 = input.substr(peg$currPos, 12);
    if (s3.toLowerCase() === peg$c0) {
      peg$currPos += 12;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e0);
      }
    }
    if (s3 !== peg$FAILED) {
      s4 = peg$currPos;
      s5 = peg$parse_();
      s6 = input.substr(peg$currPos, 10);
      if (s6.toLowerCase() === peg$c1) {
        peg$currPos += 10;
      } else {
        s6 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e1);
        }
      }
      if (s6 === peg$FAILED) {
        s6 = input.substr(peg$currPos, 8);
        if (s6.toLowerCase() === peg$c2) {
          peg$currPos += 8;
        } else {
          s6 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e2);
          }
        }
      }
      if (s6 !== peg$FAILED) {
        s5 = [s5, s6];
        s4 = s5;
      } else {
        peg$currPos = s4;
        s4 = peg$FAILED;
      }
      if (s4 === peg$FAILED) {
        s4 = null;
      }
      s5 = peg$parseLineEnd();
      if (s5 !== peg$FAILED) {
        s6 = [];
        s7 = peg$parseStatement();
        while (s7 !== peg$FAILED) {
          s6.push(s7);
          s7 = peg$parseStatement();
        }
        s7 = peg$parse_();
        peg$savedPos = s0;
        s0 = peg$f0(s1, s6);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseFrontmatter() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 3) === peg$c3) {
      s1 = peg$c3;
      peg$currPos += 3;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e3);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$parseNL();
      if (s2 !== peg$FAILED) {
        s3 = [];
        s4 = peg$currPos;
        s5 = peg$currPos;
        peg$silentFails++;
        s6 = peg$currPos;
        if (input.substr(peg$currPos, 3) === peg$c3) {
          s7 = peg$c3;
          peg$currPos += 3;
        } else {
          s7 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e3);
          }
        }
        if (s7 !== peg$FAILED) {
          s8 = peg$parseNL();
          if (s8 !== peg$FAILED) {
            s7 = [s7, s8];
            s6 = s7;
          } else {
            peg$currPos = s6;
            s6 = peg$FAILED;
          }
        } else {
          peg$currPos = s6;
          s6 = peg$FAILED;
        }
        peg$silentFails--;
        if (s6 === peg$FAILED) {
          s5 = void 0;
        } else {
          peg$currPos = s5;
          s5 = peg$FAILED;
        }
        if (s5 !== peg$FAILED) {
          s6 = peg$currPos;
          s7 = [];
          s8 = input.charAt(peg$currPos);
          if (peg$r0.test(s8)) {
            peg$currPos++;
          } else {
            s8 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e4);
            }
          }
          while (s8 !== peg$FAILED) {
            s7.push(s8);
            s8 = input.charAt(peg$currPos);
            if (peg$r0.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e4);
              }
            }
          }
          s6 = input.substring(s6, peg$currPos);
          s7 = peg$parseNL();
          if (s7 !== peg$FAILED) {
            peg$savedPos = s4;
            s4 = peg$f1(s6);
          } else {
            peg$currPos = s4;
            s4 = peg$FAILED;
          }
        } else {
          peg$currPos = s4;
          s4 = peg$FAILED;
        }
        while (s4 !== peg$FAILED) {
          s3.push(s4);
          s4 = peg$currPos;
          s5 = peg$currPos;
          peg$silentFails++;
          s6 = peg$currPos;
          if (input.substr(peg$currPos, 3) === peg$c3) {
            s7 = peg$c3;
            peg$currPos += 3;
          } else {
            s7 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e3);
            }
          }
          if (s7 !== peg$FAILED) {
            s8 = peg$parseNL();
            if (s8 !== peg$FAILED) {
              s7 = [s7, s8];
              s6 = s7;
            } else {
              peg$currPos = s6;
              s6 = peg$FAILED;
            }
          } else {
            peg$currPos = s6;
            s6 = peg$FAILED;
          }
          peg$silentFails--;
          if (s6 === peg$FAILED) {
            s5 = void 0;
          } else {
            peg$currPos = s5;
            s5 = peg$FAILED;
          }
          if (s5 !== peg$FAILED) {
            s6 = peg$currPos;
            s7 = [];
            s8 = input.charAt(peg$currPos);
            if (peg$r0.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e4);
              }
            }
            while (s8 !== peg$FAILED) {
              s7.push(s8);
              s8 = input.charAt(peg$currPos);
              if (peg$r0.test(s8)) {
                peg$currPos++;
              } else {
                s8 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e4);
                }
              }
            }
            s6 = input.substring(s6, peg$currPos);
            s7 = peg$parseNL();
            if (s7 !== peg$FAILED) {
              peg$savedPos = s4;
              s4 = peg$f1(s6);
            } else {
              peg$currPos = s4;
              s4 = peg$FAILED;
            }
          } else {
            peg$currPos = s4;
            s4 = peg$FAILED;
          }
        }
        if (input.substr(peg$currPos, 3) === peg$c3) {
          s4 = peg$c3;
          peg$currPos += 3;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e3);
          }
        }
        if (s4 !== peg$FAILED) {
          s5 = peg$parseNL();
          if (s5 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f2(s3);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseStatement() {
    var s0;
    s0 = peg$parseTitle();
    if (s0 === peg$FAILED) {
      s0 = peg$parseXAxis();
      if (s0 === peg$FAILED) {
        s0 = peg$parseYAxis();
        if (s0 === peg$FAILED) {
          s0 = peg$parseSeries();
          if (s0 === peg$FAILED) {
            s0 = peg$parseBlankLine();
          }
        }
      }
    }
    return s0;
  }
  function peg$parseTitle() {
    var s0, s1, s2, s3, s4, s5;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = input.substr(peg$currPos, 5);
    if (s2.toLowerCase() === peg$c4) {
      peg$currPos += 5;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e5);
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$parse_();
      s4 = peg$parseQuoted();
      if (s4 === peg$FAILED) {
        s4 = peg$parseRestOfLine();
      }
      if (s4 !== peg$FAILED) {
        s5 = peg$parseLineEnd();
        if (s5 !== peg$FAILED) {
          peg$savedPos = s0;
          s0 = peg$f3(s4);
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseXAxis() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9, s10, s11, s12;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = input.substr(peg$currPos, 6);
    if (s2.toLowerCase() === peg$c5) {
      peg$currPos += 6;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e6);
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$parse_();
      if (input.charCodeAt(peg$currPos) === 91) {
        s4 = peg$c6;
        peg$currPos++;
      } else {
        s4 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e7);
        }
      }
      if (s4 !== peg$FAILED) {
        s5 = peg$parse_();
        s6 = peg$parseAxisItem();
        if (s6 !== peg$FAILED) {
          s7 = [];
          s8 = peg$currPos;
          s9 = peg$parse_();
          if (input.charCodeAt(peg$currPos) === 44) {
            s10 = peg$c7;
            peg$currPos++;
          } else {
            s10 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e8);
            }
          }
          if (s10 !== peg$FAILED) {
            s11 = peg$parse_();
            s12 = peg$parseAxisItem();
            if (s12 !== peg$FAILED) {
              peg$savedPos = s8;
              s8 = peg$f4(s6, s12);
            } else {
              peg$currPos = s8;
              s8 = peg$FAILED;
            }
          } else {
            peg$currPos = s8;
            s8 = peg$FAILED;
          }
          while (s8 !== peg$FAILED) {
            s7.push(s8);
            s8 = peg$currPos;
            s9 = peg$parse_();
            if (input.charCodeAt(peg$currPos) === 44) {
              s10 = peg$c7;
              peg$currPos++;
            } else {
              s10 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e8);
              }
            }
            if (s10 !== peg$FAILED) {
              s11 = peg$parse_();
              s12 = peg$parseAxisItem();
              if (s12 !== peg$FAILED) {
                peg$savedPos = s8;
                s8 = peg$f4(s6, s12);
              } else {
                peg$currPos = s8;
                s8 = peg$FAILED;
              }
            } else {
              peg$currPos = s8;
              s8 = peg$FAILED;
            }
          }
          s8 = peg$parse_();
          if (input.charCodeAt(peg$currPos) === 93) {
            s9 = peg$c8;
            peg$currPos++;
          } else {
            s9 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e9);
            }
          }
          if (s9 !== peg$FAILED) {
            s10 = peg$parse_();
            s11 = peg$parseLineEnd();
            if (s11 !== peg$FAILED) {
              peg$savedPos = s0;
              s0 = peg$f5(s6, s7);
            } else {
              peg$currPos = s0;
              s0 = peg$FAILED;
            }
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseYAxis() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9, s10;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = input.substr(peg$currPos, 6);
    if (s2.toLowerCase() === peg$c9) {
      peg$currPos += 6;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e10);
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$currPos;
      s4 = peg$parse_();
      s5 = peg$parseQuoted();
      if (s5 !== peg$FAILED) {
        peg$savedPos = s3;
        s3 = peg$f6(s5);
      } else {
        peg$currPos = s3;
        s3 = peg$FAILED;
      }
      if (s3 === peg$FAILED) {
        s3 = null;
      }
      s4 = peg$currPos;
      s5 = peg$parse_();
      s6 = peg$parseNum();
      if (s6 !== peg$FAILED) {
        s7 = peg$parse_();
        if (input.substr(peg$currPos, 3) === peg$c10) {
          s8 = peg$c10;
          peg$currPos += 3;
        } else {
          s8 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e11);
          }
        }
        if (s8 !== peg$FAILED) {
          s9 = peg$parse_();
          s10 = peg$parseNum();
          if (s10 !== peg$FAILED) {
            peg$savedPos = s4;
            s4 = peg$f7(s3, s6, s10);
          } else {
            peg$currPos = s4;
            s4 = peg$FAILED;
          }
        } else {
          peg$currPos = s4;
          s4 = peg$FAILED;
        }
      } else {
        peg$currPos = s4;
        s4 = peg$FAILED;
      }
      if (s4 === peg$FAILED) {
        s4 = null;
      }
      s5 = peg$parse_();
      s6 = peg$parseLineEnd();
      if (s6 !== peg$FAILED) {
        peg$savedPos = s0;
        s0 = peg$f8(s3, s4);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseSeries() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9, s10, s11, s12;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = input.substr(peg$currPos, 3);
    if (s2.toLowerCase() === peg$c11) {
      peg$currPos += 3;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e12);
      }
    }
    if (s2 === peg$FAILED) {
      s2 = input.substr(peg$currPos, 4);
      if (s2.toLowerCase() === peg$c12) {
        peg$currPos += 4;
      } else {
        s2 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e13);
        }
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$parse_();
      if (input.charCodeAt(peg$currPos) === 91) {
        s4 = peg$c6;
        peg$currPos++;
      } else {
        s4 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e7);
        }
      }
      if (s4 !== peg$FAILED) {
        s5 = peg$parse_();
        s6 = peg$parseNum();
        if (s6 !== peg$FAILED) {
          s7 = [];
          s8 = peg$currPos;
          s9 = peg$parse_();
          if (input.charCodeAt(peg$currPos) === 44) {
            s10 = peg$c7;
            peg$currPos++;
          } else {
            s10 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e8);
            }
          }
          if (s10 !== peg$FAILED) {
            s11 = peg$parse_();
            s12 = peg$parseNum();
            if (s12 !== peg$FAILED) {
              peg$savedPos = s8;
              s8 = peg$f9(s2, s6, s12);
            } else {
              peg$currPos = s8;
              s8 = peg$FAILED;
            }
          } else {
            peg$currPos = s8;
            s8 = peg$FAILED;
          }
          while (s8 !== peg$FAILED) {
            s7.push(s8);
            s8 = peg$currPos;
            s9 = peg$parse_();
            if (input.charCodeAt(peg$currPos) === 44) {
              s10 = peg$c7;
              peg$currPos++;
            } else {
              s10 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e8);
              }
            }
            if (s10 !== peg$FAILED) {
              s11 = peg$parse_();
              s12 = peg$parseNum();
              if (s12 !== peg$FAILED) {
                peg$savedPos = s8;
                s8 = peg$f9(s2, s6, s12);
              } else {
                peg$currPos = s8;
                s8 = peg$FAILED;
              }
            } else {
              peg$currPos = s8;
              s8 = peg$FAILED;
            }
          }
          s8 = peg$parse_();
          if (input.charCodeAt(peg$currPos) === 93) {
            s9 = peg$c8;
            peg$currPos++;
          } else {
            s9 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e9);
            }
          }
          if (s9 !== peg$FAILED) {
            s10 = peg$parse_();
            s11 = peg$parseLineEnd();
            if (s11 !== peg$FAILED) {
              peg$savedPos = s0;
              s0 = peg$f10(s2, s6, s7);
            } else {
              peg$currPos = s0;
              s0 = peg$FAILED;
            }
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseAxisItem() {
    var s0, s1, s2;
    s0 = peg$parseQuoted();
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      s1 = [];
      s2 = input.charAt(peg$currPos);
      if (peg$r1.test(s2)) {
        peg$currPos++;
      } else {
        s2 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e14);
        }
      }
      if (s2 !== peg$FAILED) {
        while (s2 !== peg$FAILED) {
          s1.push(s2);
          s2 = input.charAt(peg$currPos);
          if (peg$r1.test(s2)) {
            peg$currPos++;
          } else {
            s2 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e14);
            }
          }
        }
      } else {
        s1 = peg$FAILED;
      }
      if (s1 !== peg$FAILED) {
        s0 = input.substring(s0, peg$currPos);
      } else {
        s0 = s1;
      }
    }
    return s0;
  }
  function peg$parseQuoted() {
    var s0, s1, s2, s3, s4;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 34) {
      s1 = peg$c13;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e15);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$currPos;
      s3 = [];
      s4 = input.charAt(peg$currPos);
      if (peg$r2.test(s4)) {
        peg$currPos++;
      } else {
        s4 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e16);
        }
      }
      while (s4 !== peg$FAILED) {
        s3.push(s4);
        s4 = input.charAt(peg$currPos);
        if (peg$r2.test(s4)) {
          peg$currPos++;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e16);
          }
        }
      }
      s2 = input.substring(s2, peg$currPos);
      if (input.charCodeAt(peg$currPos) === 34) {
        s3 = peg$c13;
        peg$currPos++;
      } else {
        s3 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e15);
        }
      }
      if (s3 !== peg$FAILED) {
        peg$savedPos = s0;
        s0 = peg$f11(s2);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseNum() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8;
    s0 = peg$currPos;
    s1 = peg$currPos;
    s2 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 45) {
      s3 = peg$c14;
      peg$currPos++;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e17);
      }
    }
    if (s3 === peg$FAILED) {
      s3 = null;
    }
    s4 = [];
    s5 = input.charAt(peg$currPos);
    if (peg$r3.test(s5)) {
      peg$currPos++;
    } else {
      s5 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e18);
      }
    }
    if (s5 !== peg$FAILED) {
      while (s5 !== peg$FAILED) {
        s4.push(s5);
        s5 = input.charAt(peg$currPos);
        if (peg$r3.test(s5)) {
          peg$currPos++;
        } else {
          s5 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e18);
          }
        }
      }
    } else {
      s4 = peg$FAILED;
    }
    if (s4 !== peg$FAILED) {
      s5 = peg$currPos;
      if (input.charCodeAt(peg$currPos) === 46) {
        s6 = peg$c15;
        peg$currPos++;
      } else {
        s6 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e19);
        }
      }
      if (s6 !== peg$FAILED) {
        s7 = [];
        s8 = input.charAt(peg$currPos);
        if (peg$r3.test(s8)) {
          peg$currPos++;
        } else {
          s8 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e18);
          }
        }
        if (s8 !== peg$FAILED) {
          while (s8 !== peg$FAILED) {
            s7.push(s8);
            s8 = input.charAt(peg$currPos);
            if (peg$r3.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e18);
              }
            }
          }
        } else {
          s7 = peg$FAILED;
        }
        if (s7 !== peg$FAILED) {
          s6 = [s6, s7];
          s5 = s6;
        } else {
          peg$currPos = s5;
          s5 = peg$FAILED;
        }
      } else {
        peg$currPos = s5;
        s5 = peg$FAILED;
      }
      if (s5 === peg$FAILED) {
        s5 = null;
      }
      s3 = [s3, s4, s5];
      s2 = s3;
    } else {
      peg$currPos = s2;
      s2 = peg$FAILED;
    }
    if (s2 !== peg$FAILED) {
      s1 = input.substring(s1, peg$currPos);
    } else {
      s1 = s2;
    }
    if (s1 !== peg$FAILED) {
      peg$savedPos = s0;
      s1 = peg$f12(s1);
    }
    s0 = s1;
    return s0;
  }
  function peg$parseRestOfLine() {
    var s0, s1, s2;
    s0 = peg$currPos;
    s1 = [];
    s2 = input.charAt(peg$currPos);
    if (peg$r0.test(s2)) {
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e4);
      }
    }
    if (s2 !== peg$FAILED) {
      while (s2 !== peg$FAILED) {
        s1.push(s2);
        s2 = input.charAt(peg$currPos);
        if (peg$r0.test(s2)) {
          peg$currPos++;
        } else {
          s2 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e4);
          }
        }
      }
    } else {
      s1 = peg$FAILED;
    }
    if (s1 !== peg$FAILED) {
      s0 = input.substring(s0, peg$currPos);
    } else {
      s0 = s1;
    }
    return s0;
  }
  function peg$parseNL() {
    var s0, s1, s2;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 13) {
      s1 = peg$c16;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e20);
      }
    }
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s2 = peg$c17;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e21);
      }
    }
    if (s2 !== peg$FAILED) {
      s1 = [s1, s2];
      s0 = s1;
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseBlankLine() {
    var s0, s1, s2, s3;
    s0 = peg$currPos;
    s1 = peg$parse_();
    if (input.charCodeAt(peg$currPos) === 13) {
      s2 = peg$c16;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e20);
      }
    }
    if (s2 === peg$FAILED) {
      s2 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s3 = peg$c17;
      peg$currPos++;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e21);
      }
    }
    if (s3 !== peg$FAILED) {
      peg$savedPos = s0;
      s0 = peg$f13();
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseLineEnd() {
    var s0, s1, s2;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 13) {
      s1 = peg$c16;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e20);
      }
    }
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s2 = peg$c17;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e21);
      }
    }
    if (s2 !== peg$FAILED) {
      s1 = [s1, s2];
      s0 = s1;
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      peg$silentFails++;
      if (input.length > peg$currPos) {
        s1 = input.charAt(peg$currPos);
        peg$currPos++;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e22);
        }
      }
      peg$silentFails--;
      if (s1 === peg$FAILED) {
        s0 = void 0;
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    }
    return s0;
  }
  function peg$parse_() {
    var s0, s1;
    s0 = [];
    s1 = input.charAt(peg$currPos);
    if (peg$r4.test(s1)) {
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e23);
      }
    }
    while (s1 !== peg$FAILED) {
      s0.push(s1);
      s1 = input.charAt(peg$currPos);
      if (peg$r4.test(s1)) {
        peg$currPos++;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e23);
        }
      }
    }
    return s0;
  }
  peg$result = peg$startRuleFunction();
  if (options.peg$library) {
    return (
      /** @type {any} */
      {
        peg$result,
        peg$currPos,
        peg$FAILED,
        peg$maxFailExpected,
        peg$maxFailPos
      }
    );
  }
  if (peg$result !== peg$FAILED && peg$currPos === input.length) {
    return peg$result;
  } else {
    if (peg$result !== peg$FAILED && peg$currPos < input.length) {
      peg$fail(peg$endExpectation());
    }
    throw peg$buildStructuredError(
      peg$maxFailExpected,
      peg$maxFailPos < input.length ? input.charAt(peg$maxFailPos) : null,
      peg$maxFailPos < input.length ? peg$computeLocation(peg$maxFailPos, peg$maxFailPos + 1) : peg$computeLocation(peg$maxFailPos, peg$maxFailPos)
    );
  }
}
var xychart = {
  parseMermaid(input) {
    return peg$parse5(input);
  },
  parseYaml(input) {
    return JSON.parse(input);
  },
  layout(ir, theme) {
    return layoutXYChart(ir, theme);
  }
};
function layoutQuadrant(ir, theme) {
  const { palette, typography, spacing } = theme;
  const p = pen(theme);
  const margin = spacing.diagramMargin;
  const title = ir.metadata.title;
  const titleH = title ? typography.titleFontSize + 18 : 0;
  const side = 460;
  const axisPad = 30;
  const plotLeft = margin + axisPad;
  const plotTop = margin + titleH;
  const plotRight = plotLeft + side;
  const plotBot = plotTop + side;
  const midX = rhuInt((plotLeft + plotRight) / 2);
  const midY = rhuInt((plotTop + plotBot) / 2);
  const half = side / 2;
  const px = (x) => rhu(plotLeft + x * side);
  const py = (y) => rhu(plotBot - y * side);
  const elements = [];
  if (title) {
    elements.push(
      p.text(
        title,
        rhuInt((plotLeft + plotRight) / 2),
        margin + typography.titleFontSize,
        typography.titleFontSize,
        palette.text,
        { weight: "bold", anchor: "middle" }
      )
    );
  }
  const quads = [
    { x: midX, y: plotTop, hue: 0 },
    // q1 top-right
    { x: plotLeft, y: plotTop, hue: 1 },
    // q2 top-left
    { x: plotLeft, y: midY, hue: 2 },
    // q3 bottom-left
    { x: midX, y: midY, hue: 3 }
    // q4 bottom-right
  ];
  quads.forEach((q, i) => {
    const qColor = categoricalHue(q.hue, theme);
    elements.push(
      p.rect(
        { x: q.x, y: q.y, width: half, height: half },
        qColor,
        qColor,
        0,
        { opacity: 0.12 }
      )
    );
    const label = ir.quadrants[i];
    if (label) {
      elements.push(
        p.text(
          label,
          rhuInt(q.x + half / 2),
          q.y + typography.baseFontSize + 12,
          typography.baseFontSize,
          qColor,
          { weight: "bold", anchor: "middle" }
        )
      );
    }
  });
  elements.push(
    p.rect({ x: plotLeft, y: plotTop, width: side, height: side }, "none", palette.border, 1.5)
  );
  elements.push(
    p.path(`M ${midX} ${plotTop} L ${midX} ${plotBot}`, palette.border, 1, { dash: "4 4" })
  );
  elements.push(
    p.path(`M ${plotLeft} ${midY} L ${plotRight} ${midY}`, palette.border, 1, { dash: "4 4" })
  );
  const dotFont = typography.smallFontSize;
  for (const pt of ir.points) {
    const x = px(pt.x), y = py(pt.y);
    elements.push(p.circle({ x, y }, 5, palette.primary, palette.background, 1.5));
    elements.push(p.text(pt.label, x + 9, y + dotFont * 0.35, dotFont, palette.text));
  }
  const axFont = typography.smallFontSize;
  const xY = plotBot + axFont + 8;
  if (ir.xAxisLeft)
    elements.push(
      p.text(ir.xAxisLeft, plotLeft, xY, axFont, palette.textMuted, { anchor: "start" })
    );
  if (ir.xAxisRight)
    elements.push(
      p.text(ir.xAxisRight, plotRight, xY, axFont, palette.textMuted, { anchor: "end" })
    );
  const yLx = margin + axFont;
  if (ir.yAxisBottom) {
    const yy = plotBot;
    elements.push(
      p.group([p.text(ir.yAxisBottom, yLx, yy, axFont, palette.textMuted, { anchor: "start" })], {
        transform: `rotate(-90 ${yLx} ${yy})`
      })
    );
  }
  if (ir.yAxisTop) {
    const yy = plotTop;
    elements.push(
      p.group([p.text(ir.yAxisTop, yLx, yy, axFont, palette.textMuted, { anchor: "end" })], {
        transform: `rotate(-90 ${yLx} ${yy})`
      })
    );
  }
  const totalW = rhuInt(plotRight + margin);
  const totalH = rhuInt(plotBot + axFont + 16 + margin);
  const scene = applyOverlays(
    {
      viewBox: { x: 0, y: 0, width: totalW, height: totalH },
      background: palette.background,
      elements
    },
    ir.overlays,
    theme
  );
  return { scene, anchors: {} };
}
function peg$subclass6(child, parent) {
  function C() {
    this.constructor = child;
  }
  C.prototype = parent.prototype;
  child.prototype = new C();
}
function peg$SyntaxError6(message, expected, found, location) {
  var self = Error.call(this, message);
  if (Object.setPrototypeOf) {
    Object.setPrototypeOf(self, peg$SyntaxError6.prototype);
  }
  self.expected = expected;
  self.found = found;
  self.location = location;
  self.name = "SyntaxError";
  return self;
}
peg$subclass6(peg$SyntaxError6, Error);
function peg$padEnd6(str, targetLength, padString) {
  padString = padString || " ";
  if (str.length > targetLength) {
    return str;
  }
  targetLength -= str.length;
  padString += padString.repeat(targetLength);
  return str + padString.slice(0, targetLength);
}
peg$SyntaxError6.prototype.format = function(sources) {
  var str = "Error: " + this.message;
  if (this.location) {
    var src = null;
    var k;
    for (k = 0; k < sources.length; k++) {
      if (sources[k].source === this.location.source) {
        src = sources[k].text.split(/\r\n|\n|\r/g);
        break;
      }
    }
    var s = this.location.start;
    var offset_s = this.location.source && typeof this.location.source.offset === "function" ? this.location.source.offset(s) : s;
    var loc = this.location.source + ":" + offset_s.line + ":" + offset_s.column;
    if (src) {
      var e = this.location.end;
      var filler = peg$padEnd6("", offset_s.line.toString().length, " ");
      var line = src[s.line - 1];
      var last = s.line === e.line ? e.column : line.length + 1;
      var hatLen = last - s.column || 1;
      str += "\n --> " + loc + "\n" + filler + " |\n" + offset_s.line + " | " + line + "\n" + filler + " | " + peg$padEnd6("", s.column - 1, " ") + peg$padEnd6("", hatLen, "^");
    } else {
      str += "\n at " + loc;
    }
  }
  return str;
};
peg$SyntaxError6.buildMessage = function(expected, found) {
  var DESCRIBE_EXPECTATION_FNS = {
    literal: function(expectation) {
      return '"' + literalEscape(expectation.text) + '"';
    },
    class: function(expectation) {
      var escapedParts = expectation.parts.map(function(part) {
        return Array.isArray(part) ? classEscape(part[0]) + "-" + classEscape(part[1]) : classEscape(part);
      });
      return "[" + (expectation.inverted ? "^" : "") + escapedParts.join("") + "]";
    },
    any: function() {
      return "any character";
    },
    end: function() {
      return "end of input";
    },
    other: function(expectation) {
      return expectation.description;
    }
  };
  function hex(ch) {
    return ch.charCodeAt(0).toString(16).toUpperCase();
  }
  function literalEscape(s) {
    return s.replace(/\\/g, "\\\\").replace(/"/g, '\\"').replace(/\0/g, "\\0").replace(/\t/g, "\\t").replace(/\n/g, "\\n").replace(/\r/g, "\\r").replace(/[\x00-\x0F]/g, function(ch) {
      return "\\x0" + hex(ch);
    }).replace(/[\x10-\x1F\x7F-\x9F]/g, function(ch) {
      return "\\x" + hex(ch);
    });
  }
  function classEscape(s) {
    return s.replace(/\\/g, "\\\\").replace(/\]/g, "\\]").replace(/\^/g, "\\^").replace(/-/g, "\\-").replace(/\0/g, "\\0").replace(/\t/g, "\\t").replace(/\n/g, "\\n").replace(/\r/g, "\\r").replace(/[\x00-\x0F]/g, function(ch) {
      return "\\x0" + hex(ch);
    }).replace(/[\x10-\x1F\x7F-\x9F]/g, function(ch) {
      return "\\x" + hex(ch);
    });
  }
  function describeExpectation(expectation) {
    return DESCRIBE_EXPECTATION_FNS[expectation.type](expectation);
  }
  function describeExpected(expected2) {
    var descriptions = expected2.map(describeExpectation);
    var i, j;
    descriptions.sort();
    if (descriptions.length > 0) {
      for (i = 1, j = 1; i < descriptions.length; i++) {
        if (descriptions[i - 1] !== descriptions[i]) {
          descriptions[j] = descriptions[i];
          j++;
        }
      }
      descriptions.length = j;
    }
    switch (descriptions.length) {
      case 1:
        return descriptions[0];
      case 2:
        return descriptions[0] + " or " + descriptions[1];
      default:
        return descriptions.slice(0, -1).join(", ") + ", or " + descriptions[descriptions.length - 1];
    }
  }
  function describeFound(found2) {
    return found2 ? '"' + literalEscape(found2) + '"' : "end of input";
  }
  return "Expected " + describeExpected(expected) + " but " + describeFound(found) + " found.";
};
function peg$parse6(input, options) {
  options = options !== void 0 ? options : {};
  var peg$FAILED = {};
  var peg$source = options.grammarSource;
  var peg$startRuleFunctions = { Document: peg$parseDocument };
  var peg$startRuleFunction = peg$parseDocument;
  var peg$c0 = "quadrantchart";
  var peg$c1 = "---";
  var peg$c2 = "title";
  var peg$c3 = "x-axis";
  var peg$c4 = "-->";
  var peg$c5 = "y-axis";
  var peg$c6 = "quadrant-";
  var peg$c7 = ":";
  var peg$c8 = "[";
  var peg$c9 = ",";
  var peg$c10 = "]";
  var peg$c11 = "-";
  var peg$c12 = ".";
  var peg$c13 = "\r";
  var peg$c14 = "\n";
  var peg$r0 = /^[^\n]/;
  var peg$r1 = /^[1-4]/;
  var peg$r2 = /^[^:\n]/;
  var peg$r3 = /^[0-9]/;
  var peg$r4 = /^[ \t]/;
  var peg$e0 = peg$literalExpectation("quadrantChart", true);
  var peg$e1 = peg$literalExpectation("---", false);
  var peg$e2 = peg$classExpectation(["\n"], true, false);
  var peg$e3 = peg$literalExpectation("title", true);
  var peg$e4 = peg$literalExpectation("x-axis", true);
  var peg$e5 = peg$literalExpectation("-->", false);
  var peg$e6 = peg$literalExpectation("y-axis", true);
  var peg$e7 = peg$literalExpectation("quadrant-", true);
  var peg$e8 = peg$classExpectation([["1", "4"]], false, false);
  var peg$e9 = peg$classExpectation([":", "\n"], true, false);
  var peg$e10 = peg$literalExpectation(":", false);
  var peg$e11 = peg$literalExpectation("[", false);
  var peg$e12 = peg$literalExpectation(",", false);
  var peg$e13 = peg$literalExpectation("]", false);
  var peg$e14 = peg$literalExpectation("-", false);
  var peg$e15 = peg$classExpectation([["0", "9"]], false, false);
  var peg$e16 = peg$literalExpectation(".", false);
  var peg$e17 = peg$literalExpectation("\r", false);
  var peg$e18 = peg$literalExpectation("\n", false);
  var peg$e19 = peg$anyExpectation();
  var peg$e20 = peg$classExpectation([" ", "	"], false, false);
  var peg$f0 = function(fm, stmts) {
    let title, xl, xr, yb, yt;
    const quadrants = [void 0, void 0, void 0, void 0];
    const points = [];
    for (const s of stmts.filter(Boolean)) {
      if (s.t === "title") title = s.v;
      else if (s.t === "x") {
        xl = s.left;
        xr = s.right;
      } else if (s.t === "y") {
        yb = s.left;
        yt = s.right;
      } else if (s.t === "q") quadrants[s.n - 1] = s.label;
      else if (s.t === "p") points.push({ label: s.label, x: s.x, y: s.y });
    }
    return {
      version: "1.0",
      metadata: { ...fm || {}, ...title ? { title } : {} },
      ...xl !== void 0 ? { xAxisLeft: xl } : {},
      ...xr !== void 0 ? { xAxisRight: xr } : {},
      ...yb !== void 0 ? { yAxisBottom: yb } : {},
      ...yt !== void 0 ? { yAxisTop: yt } : {},
      quadrants,
      points
    };
  };
  var peg$f1 = function(line) {
    return line;
  };
  var peg$f2 = function(lines2) {
    const meta = {};
    for (const line of lines2) {
      const m = line.match(/^\s*(\w+)\s*:\s*(.+?)\s*$/);
      if (m) meta[m[1]] = m[2];
    }
    return meta;
  };
  var peg$f3 = function(v) {
    return { t: "title", v: v.trim() };
  };
  var peg$f4 = function(left, r) {
    return r.trim();
  };
  var peg$f5 = function(left, right) {
    return { t: "x", left: left.trim(), right: right || void 0 };
  };
  var peg$f6 = function(left, r) {
    return r.trim();
  };
  var peg$f7 = function(left, right) {
    return { t: "y", left: left.trim(), right: right || void 0 };
  };
  var peg$f8 = function(n, label) {
    return { t: "q", n: parseInt(n, 10), label: label.trim() };
  };
  var peg$f9 = function(label, x, y) {
    return { t: "p", label: label.trim(), x, y };
  };
  var peg$f10 = function(n) {
    return parseFloat(n);
  };
  var peg$f11 = function() {
    return null;
  };
  var peg$currPos = options.peg$currPos | 0;
  var peg$savedPos = peg$currPos;
  var peg$posDetailsCache = [{ line: 1, column: 1 }];
  var peg$maxFailPos = peg$currPos;
  var peg$maxFailExpected = options.peg$maxFailExpected || [];
  var peg$silentFails = options.peg$silentFails | 0;
  var peg$result;
  if (options.startRule) {
    if (!(options.startRule in peg$startRuleFunctions)) {
      throw new Error(`Can't start parsing from rule "` + options.startRule + '".');
    }
    peg$startRuleFunction = peg$startRuleFunctions[options.startRule];
  }
  function text() {
    return input.substring(peg$savedPos, peg$currPos);
  }
  function offset() {
    return peg$savedPos;
  }
  function range() {
    return {
      source: peg$source,
      start: peg$savedPos,
      end: peg$currPos
    };
  }
  function location() {
    return peg$computeLocation(peg$savedPos, peg$currPos);
  }
  function expected(description, location2) {
    location2 = location2 !== void 0 ? location2 : peg$computeLocation(peg$savedPos, peg$currPos);
    throw peg$buildStructuredError(
      [peg$otherExpectation(description)],
      input.substring(peg$savedPos, peg$currPos),
      location2
    );
  }
  function error(message, location2) {
    location2 = location2 !== void 0 ? location2 : peg$computeLocation(peg$savedPos, peg$currPos);
    throw peg$buildSimpleError(message, location2);
  }
  function peg$literalExpectation(text2, ignoreCase) {
    return { type: "literal", text: text2, ignoreCase };
  }
  function peg$classExpectation(parts, inverted, ignoreCase) {
    return { type: "class", parts, inverted, ignoreCase };
  }
  function peg$anyExpectation() {
    return { type: "any" };
  }
  function peg$endExpectation() {
    return { type: "end" };
  }
  function peg$otherExpectation(description) {
    return { type: "other", description };
  }
  function peg$computePosDetails(pos) {
    var details = peg$posDetailsCache[pos];
    var p;
    if (details) {
      return details;
    } else {
      if (pos >= peg$posDetailsCache.length) {
        p = peg$posDetailsCache.length - 1;
      } else {
        p = pos;
        while (!peg$posDetailsCache[--p]) {
        }
      }
      details = peg$posDetailsCache[p];
      details = {
        line: details.line,
        column: details.column
      };
      while (p < pos) {
        if (input.charCodeAt(p) === 10) {
          details.line++;
          details.column = 1;
        } else {
          details.column++;
        }
        p++;
      }
      peg$posDetailsCache[pos] = details;
      return details;
    }
  }
  function peg$computeLocation(startPos, endPos, offset2) {
    var startPosDetails = peg$computePosDetails(startPos);
    var endPosDetails = peg$computePosDetails(endPos);
    var res = {
      source: peg$source,
      start: {
        offset: startPos,
        line: startPosDetails.line,
        column: startPosDetails.column
      },
      end: {
        offset: endPos,
        line: endPosDetails.line,
        column: endPosDetails.column
      }
    };
    if (offset2 && peg$source && typeof peg$source.offset === "function") {
      res.start = peg$source.offset(res.start);
      res.end = peg$source.offset(res.end);
    }
    return res;
  }
  function peg$fail(expected2) {
    if (peg$currPos < peg$maxFailPos) {
      return;
    }
    if (peg$currPos > peg$maxFailPos) {
      peg$maxFailPos = peg$currPos;
      peg$maxFailExpected = [];
    }
    peg$maxFailExpected.push(expected2);
  }
  function peg$buildSimpleError(message, location2) {
    return new peg$SyntaxError6(message, null, null, location2);
  }
  function peg$buildStructuredError(expected2, found, location2) {
    return new peg$SyntaxError6(
      peg$SyntaxError6.buildMessage(expected2, found),
      expected2,
      found,
      location2
    );
  }
  function peg$parseDocument() {
    var s0, s1, s2, s3, s4, s5, s6;
    s0 = peg$currPos;
    s1 = peg$parseFrontmatter();
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    s2 = peg$parse_();
    s3 = input.substr(peg$currPos, 13);
    if (s3.toLowerCase() === peg$c0) {
      peg$currPos += 13;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e0);
      }
    }
    if (s3 !== peg$FAILED) {
      s4 = peg$parseLineEnd();
      if (s4 !== peg$FAILED) {
        s5 = [];
        s6 = peg$parseStatement();
        while (s6 !== peg$FAILED) {
          s5.push(s6);
          s6 = peg$parseStatement();
        }
        s6 = peg$parse_();
        peg$savedPos = s0;
        s0 = peg$f0(s1, s5);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseFrontmatter() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 3) === peg$c1) {
      s1 = peg$c1;
      peg$currPos += 3;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e1);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$parseNL();
      if (s2 !== peg$FAILED) {
        s3 = [];
        s4 = peg$currPos;
        s5 = peg$currPos;
        peg$silentFails++;
        s6 = peg$currPos;
        if (input.substr(peg$currPos, 3) === peg$c1) {
          s7 = peg$c1;
          peg$currPos += 3;
        } else {
          s7 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e1);
          }
        }
        if (s7 !== peg$FAILED) {
          s8 = peg$parseNL();
          if (s8 !== peg$FAILED) {
            s7 = [s7, s8];
            s6 = s7;
          } else {
            peg$currPos = s6;
            s6 = peg$FAILED;
          }
        } else {
          peg$currPos = s6;
          s6 = peg$FAILED;
        }
        peg$silentFails--;
        if (s6 === peg$FAILED) {
          s5 = void 0;
        } else {
          peg$currPos = s5;
          s5 = peg$FAILED;
        }
        if (s5 !== peg$FAILED) {
          s6 = peg$currPos;
          s7 = [];
          s8 = input.charAt(peg$currPos);
          if (peg$r0.test(s8)) {
            peg$currPos++;
          } else {
            s8 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e2);
            }
          }
          while (s8 !== peg$FAILED) {
            s7.push(s8);
            s8 = input.charAt(peg$currPos);
            if (peg$r0.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e2);
              }
            }
          }
          s6 = input.substring(s6, peg$currPos);
          s7 = peg$parseNL();
          if (s7 !== peg$FAILED) {
            peg$savedPos = s4;
            s4 = peg$f1(s6);
          } else {
            peg$currPos = s4;
            s4 = peg$FAILED;
          }
        } else {
          peg$currPos = s4;
          s4 = peg$FAILED;
        }
        while (s4 !== peg$FAILED) {
          s3.push(s4);
          s4 = peg$currPos;
          s5 = peg$currPos;
          peg$silentFails++;
          s6 = peg$currPos;
          if (input.substr(peg$currPos, 3) === peg$c1) {
            s7 = peg$c1;
            peg$currPos += 3;
          } else {
            s7 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e1);
            }
          }
          if (s7 !== peg$FAILED) {
            s8 = peg$parseNL();
            if (s8 !== peg$FAILED) {
              s7 = [s7, s8];
              s6 = s7;
            } else {
              peg$currPos = s6;
              s6 = peg$FAILED;
            }
          } else {
            peg$currPos = s6;
            s6 = peg$FAILED;
          }
          peg$silentFails--;
          if (s6 === peg$FAILED) {
            s5 = void 0;
          } else {
            peg$currPos = s5;
            s5 = peg$FAILED;
          }
          if (s5 !== peg$FAILED) {
            s6 = peg$currPos;
            s7 = [];
            s8 = input.charAt(peg$currPos);
            if (peg$r0.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e2);
              }
            }
            while (s8 !== peg$FAILED) {
              s7.push(s8);
              s8 = input.charAt(peg$currPos);
              if (peg$r0.test(s8)) {
                peg$currPos++;
              } else {
                s8 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e2);
                }
              }
            }
            s6 = input.substring(s6, peg$currPos);
            s7 = peg$parseNL();
            if (s7 !== peg$FAILED) {
              peg$savedPos = s4;
              s4 = peg$f1(s6);
            } else {
              peg$currPos = s4;
              s4 = peg$FAILED;
            }
          } else {
            peg$currPos = s4;
            s4 = peg$FAILED;
          }
        }
        if (input.substr(peg$currPos, 3) === peg$c1) {
          s4 = peg$c1;
          peg$currPos += 3;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e1);
          }
        }
        if (s4 !== peg$FAILED) {
          s5 = peg$parseNL();
          if (s5 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f2(s3);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseStatement() {
    var s0;
    s0 = peg$parseTitle();
    if (s0 === peg$FAILED) {
      s0 = peg$parseXAxis();
      if (s0 === peg$FAILED) {
        s0 = peg$parseYAxis();
        if (s0 === peg$FAILED) {
          s0 = peg$parseQuadrant();
          if (s0 === peg$FAILED) {
            s0 = peg$parsePoint();
            if (s0 === peg$FAILED) {
              s0 = peg$parseBlankLine();
            }
          }
        }
      }
    }
    return s0;
  }
  function peg$parseTitle() {
    var s0, s1, s2, s3, s4, s5;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = input.substr(peg$currPos, 5);
    if (s2.toLowerCase() === peg$c2) {
      peg$currPos += 5;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e3);
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$parse_();
      s4 = peg$parseRestOfLine();
      if (s4 !== peg$FAILED) {
        s5 = peg$parseLineEnd();
        if (s5 !== peg$FAILED) {
          peg$savedPos = s0;
          s0 = peg$f3(s4);
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseXAxis() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = input.substr(peg$currPos, 6);
    if (s2.toLowerCase() === peg$c3) {
      peg$currPos += 6;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e4);
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$parse_();
      s4 = peg$parseAxisText();
      if (s4 !== peg$FAILED) {
        s5 = peg$currPos;
        s6 = peg$parse_();
        if (input.substr(peg$currPos, 3) === peg$c4) {
          s7 = peg$c4;
          peg$currPos += 3;
        } else {
          s7 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e5);
          }
        }
        if (s7 !== peg$FAILED) {
          s8 = peg$parse_();
          s9 = peg$parseRestOfLine();
          if (s9 !== peg$FAILED) {
            peg$savedPos = s5;
            s5 = peg$f4(s4, s9);
          } else {
            peg$currPos = s5;
            s5 = peg$FAILED;
          }
        } else {
          peg$currPos = s5;
          s5 = peg$FAILED;
        }
        if (s5 === peg$FAILED) {
          s5 = null;
        }
        s6 = peg$parseLineEnd();
        if (s6 !== peg$FAILED) {
          peg$savedPos = s0;
          s0 = peg$f5(s4, s5);
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseYAxis() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = input.substr(peg$currPos, 6);
    if (s2.toLowerCase() === peg$c5) {
      peg$currPos += 6;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e6);
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$parse_();
      s4 = peg$parseAxisText();
      if (s4 !== peg$FAILED) {
        s5 = peg$currPos;
        s6 = peg$parse_();
        if (input.substr(peg$currPos, 3) === peg$c4) {
          s7 = peg$c4;
          peg$currPos += 3;
        } else {
          s7 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e5);
          }
        }
        if (s7 !== peg$FAILED) {
          s8 = peg$parse_();
          s9 = peg$parseRestOfLine();
          if (s9 !== peg$FAILED) {
            peg$savedPos = s5;
            s5 = peg$f6(s4, s9);
          } else {
            peg$currPos = s5;
            s5 = peg$FAILED;
          }
        } else {
          peg$currPos = s5;
          s5 = peg$FAILED;
        }
        if (s5 === peg$FAILED) {
          s5 = null;
        }
        s6 = peg$parseLineEnd();
        if (s6 !== peg$FAILED) {
          peg$savedPos = s0;
          s0 = peg$f7(s4, s5);
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseQuadrant() {
    var s0, s1, s2, s3, s4, s5, s6;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = input.substr(peg$currPos, 9);
    if (s2.toLowerCase() === peg$c6) {
      peg$currPos += 9;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e7);
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = input.charAt(peg$currPos);
      if (peg$r1.test(s3)) {
        peg$currPos++;
      } else {
        s3 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e8);
        }
      }
      if (s3 !== peg$FAILED) {
        s4 = peg$parse_();
        s5 = peg$parseRestOfLine();
        if (s5 !== peg$FAILED) {
          s6 = peg$parseLineEnd();
          if (s6 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f8(s3, s5);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parsePoint() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9, s10, s11, s12, s13, s14, s15, s16;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = peg$currPos;
    s3 = [];
    s4 = input.charAt(peg$currPos);
    if (peg$r2.test(s4)) {
      peg$currPos++;
    } else {
      s4 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e9);
      }
    }
    if (s4 !== peg$FAILED) {
      while (s4 !== peg$FAILED) {
        s3.push(s4);
        s4 = input.charAt(peg$currPos);
        if (peg$r2.test(s4)) {
          peg$currPos++;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e9);
          }
        }
      }
    } else {
      s3 = peg$FAILED;
    }
    if (s3 !== peg$FAILED) {
      s2 = input.substring(s2, peg$currPos);
    } else {
      s2 = s3;
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$parse_();
      if (input.charCodeAt(peg$currPos) === 58) {
        s4 = peg$c7;
        peg$currPos++;
      } else {
        s4 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e10);
        }
      }
      if (s4 !== peg$FAILED) {
        s5 = peg$parse_();
        if (input.charCodeAt(peg$currPos) === 91) {
          s6 = peg$c8;
          peg$currPos++;
        } else {
          s6 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e11);
          }
        }
        if (s6 !== peg$FAILED) {
          s7 = peg$parse_();
          s8 = peg$parseNum();
          if (s8 !== peg$FAILED) {
            s9 = peg$parse_();
            if (input.charCodeAt(peg$currPos) === 44) {
              s10 = peg$c9;
              peg$currPos++;
            } else {
              s10 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e12);
              }
            }
            if (s10 !== peg$FAILED) {
              s11 = peg$parse_();
              s12 = peg$parseNum();
              if (s12 !== peg$FAILED) {
                s13 = peg$parse_();
                if (input.charCodeAt(peg$currPos) === 93) {
                  s14 = peg$c10;
                  peg$currPos++;
                } else {
                  s14 = peg$FAILED;
                  if (peg$silentFails === 0) {
                    peg$fail(peg$e13);
                  }
                }
                if (s14 !== peg$FAILED) {
                  s15 = peg$parse_();
                  s16 = peg$parseLineEnd();
                  if (s16 !== peg$FAILED) {
                    peg$savedPos = s0;
                    s0 = peg$f9(s2, s8, s12);
                  } else {
                    peg$currPos = s0;
                    s0 = peg$FAILED;
                  }
                } else {
                  peg$currPos = s0;
                  s0 = peg$FAILED;
                }
              } else {
                peg$currPos = s0;
                s0 = peg$FAILED;
              }
            } else {
              peg$currPos = s0;
              s0 = peg$FAILED;
            }
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseAxisText() {
    var s0, s1, s2, s3, s4;
    s0 = peg$currPos;
    s1 = [];
    s2 = peg$currPos;
    s3 = peg$currPos;
    peg$silentFails++;
    if (input.substr(peg$currPos, 3) === peg$c4) {
      s4 = peg$c4;
      peg$currPos += 3;
    } else {
      s4 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e5);
      }
    }
    peg$silentFails--;
    if (s4 === peg$FAILED) {
      s3 = void 0;
    } else {
      peg$currPos = s3;
      s3 = peg$FAILED;
    }
    if (s3 !== peg$FAILED) {
      s4 = input.charAt(peg$currPos);
      if (peg$r0.test(s4)) {
        peg$currPos++;
      } else {
        s4 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e2);
        }
      }
      if (s4 !== peg$FAILED) {
        s3 = [s3, s4];
        s2 = s3;
      } else {
        peg$currPos = s2;
        s2 = peg$FAILED;
      }
    } else {
      peg$currPos = s2;
      s2 = peg$FAILED;
    }
    if (s2 !== peg$FAILED) {
      while (s2 !== peg$FAILED) {
        s1.push(s2);
        s2 = peg$currPos;
        s3 = peg$currPos;
        peg$silentFails++;
        if (input.substr(peg$currPos, 3) === peg$c4) {
          s4 = peg$c4;
          peg$currPos += 3;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e5);
          }
        }
        peg$silentFails--;
        if (s4 === peg$FAILED) {
          s3 = void 0;
        } else {
          peg$currPos = s3;
          s3 = peg$FAILED;
        }
        if (s3 !== peg$FAILED) {
          s4 = input.charAt(peg$currPos);
          if (peg$r0.test(s4)) {
            peg$currPos++;
          } else {
            s4 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e2);
            }
          }
          if (s4 !== peg$FAILED) {
            s3 = [s3, s4];
            s2 = s3;
          } else {
            peg$currPos = s2;
            s2 = peg$FAILED;
          }
        } else {
          peg$currPos = s2;
          s2 = peg$FAILED;
        }
      }
    } else {
      s1 = peg$FAILED;
    }
    if (s1 !== peg$FAILED) {
      s0 = input.substring(s0, peg$currPos);
    } else {
      s0 = s1;
    }
    return s0;
  }
  function peg$parseNum() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8;
    s0 = peg$currPos;
    s1 = peg$currPos;
    s2 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 45) {
      s3 = peg$c11;
      peg$currPos++;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e14);
      }
    }
    if (s3 === peg$FAILED) {
      s3 = null;
    }
    s4 = [];
    s5 = input.charAt(peg$currPos);
    if (peg$r3.test(s5)) {
      peg$currPos++;
    } else {
      s5 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e15);
      }
    }
    if (s5 !== peg$FAILED) {
      while (s5 !== peg$FAILED) {
        s4.push(s5);
        s5 = input.charAt(peg$currPos);
        if (peg$r3.test(s5)) {
          peg$currPos++;
        } else {
          s5 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e15);
          }
        }
      }
    } else {
      s4 = peg$FAILED;
    }
    if (s4 !== peg$FAILED) {
      s5 = peg$currPos;
      if (input.charCodeAt(peg$currPos) === 46) {
        s6 = peg$c12;
        peg$currPos++;
      } else {
        s6 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e16);
        }
      }
      if (s6 !== peg$FAILED) {
        s7 = [];
        s8 = input.charAt(peg$currPos);
        if (peg$r3.test(s8)) {
          peg$currPos++;
        } else {
          s8 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e15);
          }
        }
        if (s8 !== peg$FAILED) {
          while (s8 !== peg$FAILED) {
            s7.push(s8);
            s8 = input.charAt(peg$currPos);
            if (peg$r3.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e15);
              }
            }
          }
        } else {
          s7 = peg$FAILED;
        }
        if (s7 !== peg$FAILED) {
          s6 = [s6, s7];
          s5 = s6;
        } else {
          peg$currPos = s5;
          s5 = peg$FAILED;
        }
      } else {
        peg$currPos = s5;
        s5 = peg$FAILED;
      }
      if (s5 === peg$FAILED) {
        s5 = null;
      }
      s3 = [s3, s4, s5];
      s2 = s3;
    } else {
      peg$currPos = s2;
      s2 = peg$FAILED;
    }
    if (s2 !== peg$FAILED) {
      s1 = input.substring(s1, peg$currPos);
    } else {
      s1 = s2;
    }
    if (s1 !== peg$FAILED) {
      peg$savedPos = s0;
      s1 = peg$f10(s1);
    }
    s0 = s1;
    return s0;
  }
  function peg$parseRestOfLine() {
    var s0, s1, s2;
    s0 = peg$currPos;
    s1 = [];
    s2 = input.charAt(peg$currPos);
    if (peg$r0.test(s2)) {
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e2);
      }
    }
    if (s2 !== peg$FAILED) {
      while (s2 !== peg$FAILED) {
        s1.push(s2);
        s2 = input.charAt(peg$currPos);
        if (peg$r0.test(s2)) {
          peg$currPos++;
        } else {
          s2 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e2);
          }
        }
      }
    } else {
      s1 = peg$FAILED;
    }
    if (s1 !== peg$FAILED) {
      s0 = input.substring(s0, peg$currPos);
    } else {
      s0 = s1;
    }
    return s0;
  }
  function peg$parseNL() {
    var s0, s1, s2;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 13) {
      s1 = peg$c13;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e17);
      }
    }
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s2 = peg$c14;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e18);
      }
    }
    if (s2 !== peg$FAILED) {
      s1 = [s1, s2];
      s0 = s1;
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseBlankLine() {
    var s0, s1, s2, s3;
    s0 = peg$currPos;
    s1 = peg$parse_();
    if (input.charCodeAt(peg$currPos) === 13) {
      s2 = peg$c13;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e17);
      }
    }
    if (s2 === peg$FAILED) {
      s2 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s3 = peg$c14;
      peg$currPos++;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e18);
      }
    }
    if (s3 !== peg$FAILED) {
      peg$savedPos = s0;
      s0 = peg$f11();
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseLineEnd() {
    var s0, s1, s2;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 13) {
      s1 = peg$c13;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e17);
      }
    }
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s2 = peg$c14;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e18);
      }
    }
    if (s2 !== peg$FAILED) {
      s1 = [s1, s2];
      s0 = s1;
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      peg$silentFails++;
      if (input.length > peg$currPos) {
        s1 = input.charAt(peg$currPos);
        peg$currPos++;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e19);
        }
      }
      peg$silentFails--;
      if (s1 === peg$FAILED) {
        s0 = void 0;
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    }
    return s0;
  }
  function peg$parse_() {
    var s0, s1;
    s0 = [];
    s1 = input.charAt(peg$currPos);
    if (peg$r4.test(s1)) {
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e20);
      }
    }
    while (s1 !== peg$FAILED) {
      s0.push(s1);
      s1 = input.charAt(peg$currPos);
      if (peg$r4.test(s1)) {
        peg$currPos++;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e20);
        }
      }
    }
    return s0;
  }
  peg$result = peg$startRuleFunction();
  if (options.peg$library) {
    return (
      /** @type {any} */
      {
        peg$result,
        peg$currPos,
        peg$FAILED,
        peg$maxFailExpected,
        peg$maxFailPos
      }
    );
  }
  if (peg$result !== peg$FAILED && peg$currPos === input.length) {
    return peg$result;
  } else {
    if (peg$result !== peg$FAILED && peg$currPos < input.length) {
      peg$fail(peg$endExpectation());
    }
    throw peg$buildStructuredError(
      peg$maxFailExpected,
      peg$maxFailPos < input.length ? input.charAt(peg$maxFailPos) : null,
      peg$maxFailPos < input.length ? peg$computeLocation(peg$maxFailPos, peg$maxFailPos + 1) : peg$computeLocation(peg$maxFailPos, peg$maxFailPos)
    );
  }
}
var quadrant = {
  parseMermaid(input) {
    return peg$parse6(input);
  },
  parseYaml(input) {
    return JSON.parse(input);
  },
  layout(ir, theme) {
    return layoutQuadrant(ir, theme);
  }
};
function layoutRadar(ir, theme) {
  const { palette, typography, spacing } = theme;
  const p = pen(theme);
  const margin = spacing.diagramMargin;
  const axes = ir.axes;
  const n = Math.max(axes.length, 1);
  const allVals = ir.curves.flatMap((c) => c.values);
  const min = ir.min ?? 0;
  const max = ir.max ?? (allVals.length ? Math.max(...allVals) : 1);
  const span = max - min || 1;
  const title = ir.metadata.title;
  const titleH = title ? typography.titleFontSize + 18 : 0;
  const R = 180;
  const labelPad = 90;
  const cx = margin + labelPad + R;
  const cy = margin + titleH + labelPad + R;
  const angle = (i) => -Math.PI / 2 + i / n * Math.PI * 2;
  const ptAt = (i, frac) => ({
    x: cx + Math.cos(angle(i)) * R * frac,
    y: cy + Math.sin(angle(i)) * R * frac
  });
  const elements = [];
  if (title) {
    elements.push(
      p.text(
        title,
        rhuInt(cx),
        margin + typography.titleFontSize,
        typography.titleFontSize,
        palette.text,
        { weight: "bold", anchor: "middle" }
      )
    );
  }
  const rings = 4;
  for (let r = 1; r <= rings; r++) {
    const frac = r / rings;
    const d = axes.map((_, i) => {
      const pt = ptAt(i, frac);
      return `${rhu(pt.x)} ${rhu(pt.y)}`;
    }).join(" L ");
    elements.push(p.path(`M ${d} Z`, palette.border, 1, { opacity: 0.5 }));
  }
  axes.forEach((ax, i) => {
    const outer = ptAt(i, 1);
    elements.push(
      p.path(`M ${rhu(cx)} ${rhu(cy)} L ${rhu(outer.x)} ${rhu(outer.y)}`, palette.border, 1, {
        opacity: 0.6
      })
    );
    const lp = ptAt(i, 1.12);
    const a = angle(i);
    const anchor = Math.abs(Math.cos(a)) < 0.3 ? "middle" : Math.cos(a) > 0 ? "start" : "end";
    elements.push(
      p.text(
        ax.label,
        rhu(lp.x),
        rhu(lp.y + typography.smallFontSize * 0.35),
        typography.smallFontSize,
        palette.text,
        { anchor }
      )
    );
  });
  ir.curves.forEach((curve, ci) => {
    const color = categoricalHue(ci, theme);
    const pts = axes.map((_, i) => {
      const v = curve.values[i] ?? min;
      const frac = Math.max(0, Math.min(1, (v - min) / span));
      const pt = ptAt(i, frac);
      return `${rhu(pt.x)} ${rhu(pt.y)}`;
    });
    if (pts.length) {
      elements.push(p.path(`M ${pts.join(" L ")} Z`, color, 2, { fill: color, opacity: 0.18 }));
      elements.push(p.path(`M ${pts.join(" L ")} Z`, color, 2));
    }
  });
  const legendFont = typography.baseFontSize;
  const swatch = 12;
  const legendX = cx + R + labelPad - 30;
  const legendTop = margin + titleH;
  let legendW = 0;
  ir.curves.forEach((curve, ci) => {
    const ly = legendTop + ci * (legendFont + 10);
    const swatchColor = categoricalHue(ci, theme);
    elements.push(
      p.rect(
        { x: legendX, y: ly, width: swatch, height: swatch },
        swatchColor,
        swatchColor,
        0,
        { rx: 2 }
      )
    );
    elements.push(
      p.text(curve.label, legendX + swatch + 8, ly + swatch - 2, legendFont, palette.text)
    );
    legendW = Math.max(legendW, swatch + 8 + measureText(curve.label, legendFont).width);
  });
  const ringRight = cx + R + labelPad;
  const totalW = rhuInt(Math.max(ringRight, legendX + legendW) + margin);
  const totalH = rhuInt(cy + R + labelPad + margin);
  const scene = applyOverlays(
    {
      viewBox: { x: 0, y: 0, width: totalW, height: totalH },
      background: palette.background,
      elements
    },
    ir.overlays,
    theme
  );
  return { scene, anchors: {} };
}
function peg$subclass7(child, parent) {
  function C() {
    this.constructor = child;
  }
  C.prototype = parent.prototype;
  child.prototype = new C();
}
function peg$SyntaxError7(message, expected, found, location) {
  var self = Error.call(this, message);
  if (Object.setPrototypeOf) {
    Object.setPrototypeOf(self, peg$SyntaxError7.prototype);
  }
  self.expected = expected;
  self.found = found;
  self.location = location;
  self.name = "SyntaxError";
  return self;
}
peg$subclass7(peg$SyntaxError7, Error);
function peg$padEnd7(str, targetLength, padString) {
  padString = padString || " ";
  if (str.length > targetLength) {
    return str;
  }
  targetLength -= str.length;
  padString += padString.repeat(targetLength);
  return str + padString.slice(0, targetLength);
}
peg$SyntaxError7.prototype.format = function(sources) {
  var str = "Error: " + this.message;
  if (this.location) {
    var src = null;
    var k;
    for (k = 0; k < sources.length; k++) {
      if (sources[k].source === this.location.source) {
        src = sources[k].text.split(/\r\n|\n|\r/g);
        break;
      }
    }
    var s = this.location.start;
    var offset_s = this.location.source && typeof this.location.source.offset === "function" ? this.location.source.offset(s) : s;
    var loc = this.location.source + ":" + offset_s.line + ":" + offset_s.column;
    if (src) {
      var e = this.location.end;
      var filler = peg$padEnd7("", offset_s.line.toString().length, " ");
      var line = src[s.line - 1];
      var last = s.line === e.line ? e.column : line.length + 1;
      var hatLen = last - s.column || 1;
      str += "\n --> " + loc + "\n" + filler + " |\n" + offset_s.line + " | " + line + "\n" + filler + " | " + peg$padEnd7("", s.column - 1, " ") + peg$padEnd7("", hatLen, "^");
    } else {
      str += "\n at " + loc;
    }
  }
  return str;
};
peg$SyntaxError7.buildMessage = function(expected, found) {
  var DESCRIBE_EXPECTATION_FNS = {
    literal: function(expectation) {
      return '"' + literalEscape(expectation.text) + '"';
    },
    class: function(expectation) {
      var escapedParts = expectation.parts.map(function(part) {
        return Array.isArray(part) ? classEscape(part[0]) + "-" + classEscape(part[1]) : classEscape(part);
      });
      return "[" + (expectation.inverted ? "^" : "") + escapedParts.join("") + "]";
    },
    any: function() {
      return "any character";
    },
    end: function() {
      return "end of input";
    },
    other: function(expectation) {
      return expectation.description;
    }
  };
  function hex(ch) {
    return ch.charCodeAt(0).toString(16).toUpperCase();
  }
  function literalEscape(s) {
    return s.replace(/\\/g, "\\\\").replace(/"/g, '\\"').replace(/\0/g, "\\0").replace(/\t/g, "\\t").replace(/\n/g, "\\n").replace(/\r/g, "\\r").replace(/[\x00-\x0F]/g, function(ch) {
      return "\\x0" + hex(ch);
    }).replace(/[\x10-\x1F\x7F-\x9F]/g, function(ch) {
      return "\\x" + hex(ch);
    });
  }
  function classEscape(s) {
    return s.replace(/\\/g, "\\\\").replace(/\]/g, "\\]").replace(/\^/g, "\\^").replace(/-/g, "\\-").replace(/\0/g, "\\0").replace(/\t/g, "\\t").replace(/\n/g, "\\n").replace(/\r/g, "\\r").replace(/[\x00-\x0F]/g, function(ch) {
      return "\\x0" + hex(ch);
    }).replace(/[\x10-\x1F\x7F-\x9F]/g, function(ch) {
      return "\\x" + hex(ch);
    });
  }
  function describeExpectation(expectation) {
    return DESCRIBE_EXPECTATION_FNS[expectation.type](expectation);
  }
  function describeExpected(expected2) {
    var descriptions = expected2.map(describeExpectation);
    var i, j;
    descriptions.sort();
    if (descriptions.length > 0) {
      for (i = 1, j = 1; i < descriptions.length; i++) {
        if (descriptions[i - 1] !== descriptions[i]) {
          descriptions[j] = descriptions[i];
          j++;
        }
      }
      descriptions.length = j;
    }
    switch (descriptions.length) {
      case 1:
        return descriptions[0];
      case 2:
        return descriptions[0] + " or " + descriptions[1];
      default:
        return descriptions.slice(0, -1).join(", ") + ", or " + descriptions[descriptions.length - 1];
    }
  }
  function describeFound(found2) {
    return found2 ? '"' + literalEscape(found2) + '"' : "end of input";
  }
  return "Expected " + describeExpected(expected) + " but " + describeFound(found) + " found.";
};
function peg$parse7(input, options) {
  options = options !== void 0 ? options : {};
  var peg$FAILED = {};
  var peg$source = options.grammarSource;
  var peg$startRuleFunctions = { Document: peg$parseDocument };
  var peg$startRuleFunction = peg$parseDocument;
  var peg$c0 = "radar-beta";
  var peg$c1 = "---";
  var peg$c2 = "title";
  var peg$c3 = "axis";
  var peg$c4 = ",";
  var peg$c5 = "curve";
  var peg$c6 = "{";
  var peg$c7 = "}";
  var peg$c8 = "max";
  var peg$c9 = "min";
  var peg$c10 = "[";
  var peg$c11 = "]";
  var peg$c12 = '"';
  var peg$c13 = "-";
  var peg$c14 = ".";
  var peg$c15 = "\r";
  var peg$c16 = "\n";
  var peg$r0 = /^[^\n]/;
  var peg$r1 = /^[^\]\n]/;
  var peg$r2 = /^[a-zA-Z0-9_]/;
  var peg$r3 = /^[^"]/;
  var peg$r4 = /^[0-9]/;
  var peg$r5 = /^[ \t]/;
  var peg$e0 = peg$literalExpectation("radar-beta", true);
  var peg$e1 = peg$literalExpectation("---", false);
  var peg$e2 = peg$classExpectation(["\n"], true, false);
  var peg$e3 = peg$literalExpectation("title", true);
  var peg$e4 = peg$literalExpectation("axis", true);
  var peg$e5 = peg$literalExpectation(",", false);
  var peg$e6 = peg$literalExpectation("curve", true);
  var peg$e7 = peg$literalExpectation("{", false);
  var peg$e8 = peg$literalExpectation("}", false);
  var peg$e9 = peg$literalExpectation("max", true);
  var peg$e10 = peg$literalExpectation("min", true);
  var peg$e11 = peg$literalExpectation("[", false);
  var peg$e12 = peg$literalExpectation("]", false);
  var peg$e13 = peg$classExpectation(["]", "\n"], true, false);
  var peg$e14 = peg$classExpectation([["a", "z"], ["A", "Z"], ["0", "9"], "_"], false, false);
  var peg$e15 = peg$literalExpectation('"', false);
  var peg$e16 = peg$classExpectation(['"'], true, false);
  var peg$e17 = peg$literalExpectation("-", false);
  var peg$e18 = peg$classExpectation([["0", "9"]], false, false);
  var peg$e19 = peg$literalExpectation(".", false);
  var peg$e20 = peg$literalExpectation("\r", false);
  var peg$e21 = peg$literalExpectation("\n", false);
  var peg$e22 = peg$anyExpectation();
  var peg$e23 = peg$classExpectation([" ", "	"], false, false);
  var peg$f0 = function(fm, stmts) {
    let title, min, max;
    const axes = [];
    const curves = [];
    for (const s of stmts.filter(Boolean)) {
      if (s.t === "title") title = s.v;
      else if (s.t === "axis") axes.push(...s.items);
      else if (s.t === "curve") curves.push({ id: s.id, label: s.label, values: s.values });
      else if (s.t === "max") max = s.v;
      else if (s.t === "min") min = s.v;
    }
    return {
      version: "1.0",
      metadata: { ...fm || {}, ...title ? { title } : {} },
      axes,
      curves,
      ...min !== void 0 ? { min } : {},
      ...max !== void 0 ? { max } : {}
    };
  };
  var peg$f1 = function(line) {
    return line;
  };
  var peg$f2 = function(lines2) {
    const meta = {};
    for (const line of lines2) {
      const m = line.match(/^\s*(\w+)\s*:\s*(.+?)\s*$/);
      if (m) meta[m[1]] = m[2];
    }
    return meta;
  };
  var peg$f3 = function(v) {
    return { t: "title", v: v.trim() };
  };
  var peg$f4 = function(first, a) {
    return a;
  };
  var peg$f5 = function(first, rest) {
    return { t: "axis", items: [first, ...rest] };
  };
  var peg$f6 = function(id, label, first, n) {
    return n;
  };
  var peg$f7 = function(id, label, first, rest) {
    return { t: "curve", id, label: label != null ? label : id, values: [first, ...rest] };
  };
  var peg$f8 = function(n) {
    return { t: "max", v: n };
  };
  var peg$f9 = function(n) {
    return { t: "min", v: n };
  };
  var peg$f10 = function(id, label) {
    return { id, label: label != null ? label : id };
  };
  var peg$f11 = function(q) {
    return q;
  };
  var peg$f12 = function(s) {
    return s.trim();
  };
  var peg$f13 = function(s) {
    return s;
  };
  var peg$f14 = function(n) {
    return parseFloat(n);
  };
  var peg$f15 = function() {
    return null;
  };
  var peg$currPos = options.peg$currPos | 0;
  var peg$savedPos = peg$currPos;
  var peg$posDetailsCache = [{ line: 1, column: 1 }];
  var peg$maxFailPos = peg$currPos;
  var peg$maxFailExpected = options.peg$maxFailExpected || [];
  var peg$silentFails = options.peg$silentFails | 0;
  var peg$result;
  if (options.startRule) {
    if (!(options.startRule in peg$startRuleFunctions)) {
      throw new Error(`Can't start parsing from rule "` + options.startRule + '".');
    }
    peg$startRuleFunction = peg$startRuleFunctions[options.startRule];
  }
  function text() {
    return input.substring(peg$savedPos, peg$currPos);
  }
  function offset() {
    return peg$savedPos;
  }
  function range() {
    return {
      source: peg$source,
      start: peg$savedPos,
      end: peg$currPos
    };
  }
  function location() {
    return peg$computeLocation(peg$savedPos, peg$currPos);
  }
  function expected(description, location2) {
    location2 = location2 !== void 0 ? location2 : peg$computeLocation(peg$savedPos, peg$currPos);
    throw peg$buildStructuredError(
      [peg$otherExpectation(description)],
      input.substring(peg$savedPos, peg$currPos),
      location2
    );
  }
  function error(message, location2) {
    location2 = location2 !== void 0 ? location2 : peg$computeLocation(peg$savedPos, peg$currPos);
    throw peg$buildSimpleError(message, location2);
  }
  function peg$literalExpectation(text2, ignoreCase) {
    return { type: "literal", text: text2, ignoreCase };
  }
  function peg$classExpectation(parts, inverted, ignoreCase) {
    return { type: "class", parts, inverted, ignoreCase };
  }
  function peg$anyExpectation() {
    return { type: "any" };
  }
  function peg$endExpectation() {
    return { type: "end" };
  }
  function peg$otherExpectation(description) {
    return { type: "other", description };
  }
  function peg$computePosDetails(pos) {
    var details = peg$posDetailsCache[pos];
    var p;
    if (details) {
      return details;
    } else {
      if (pos >= peg$posDetailsCache.length) {
        p = peg$posDetailsCache.length - 1;
      } else {
        p = pos;
        while (!peg$posDetailsCache[--p]) {
        }
      }
      details = peg$posDetailsCache[p];
      details = {
        line: details.line,
        column: details.column
      };
      while (p < pos) {
        if (input.charCodeAt(p) === 10) {
          details.line++;
          details.column = 1;
        } else {
          details.column++;
        }
        p++;
      }
      peg$posDetailsCache[pos] = details;
      return details;
    }
  }
  function peg$computeLocation(startPos, endPos, offset2) {
    var startPosDetails = peg$computePosDetails(startPos);
    var endPosDetails = peg$computePosDetails(endPos);
    var res = {
      source: peg$source,
      start: {
        offset: startPos,
        line: startPosDetails.line,
        column: startPosDetails.column
      },
      end: {
        offset: endPos,
        line: endPosDetails.line,
        column: endPosDetails.column
      }
    };
    if (offset2 && peg$source && typeof peg$source.offset === "function") {
      res.start = peg$source.offset(res.start);
      res.end = peg$source.offset(res.end);
    }
    return res;
  }
  function peg$fail(expected2) {
    if (peg$currPos < peg$maxFailPos) {
      return;
    }
    if (peg$currPos > peg$maxFailPos) {
      peg$maxFailPos = peg$currPos;
      peg$maxFailExpected = [];
    }
    peg$maxFailExpected.push(expected2);
  }
  function peg$buildSimpleError(message, location2) {
    return new peg$SyntaxError7(message, null, null, location2);
  }
  function peg$buildStructuredError(expected2, found, location2) {
    return new peg$SyntaxError7(
      peg$SyntaxError7.buildMessage(expected2, found),
      expected2,
      found,
      location2
    );
  }
  function peg$parseDocument() {
    var s0, s1, s2, s3, s4, s5, s6;
    s0 = peg$currPos;
    s1 = peg$parseFrontmatter();
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    s2 = peg$parse_();
    s3 = input.substr(peg$currPos, 10);
    if (s3.toLowerCase() === peg$c0) {
      peg$currPos += 10;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e0);
      }
    }
    if (s3 !== peg$FAILED) {
      s4 = peg$parseLineEnd();
      if (s4 !== peg$FAILED) {
        s5 = [];
        s6 = peg$parseStatement();
        while (s6 !== peg$FAILED) {
          s5.push(s6);
          s6 = peg$parseStatement();
        }
        s6 = peg$parse_();
        peg$savedPos = s0;
        s0 = peg$f0(s1, s5);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseFrontmatter() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 3) === peg$c1) {
      s1 = peg$c1;
      peg$currPos += 3;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e1);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$parseNL();
      if (s2 !== peg$FAILED) {
        s3 = [];
        s4 = peg$currPos;
        s5 = peg$currPos;
        peg$silentFails++;
        s6 = peg$currPos;
        if (input.substr(peg$currPos, 3) === peg$c1) {
          s7 = peg$c1;
          peg$currPos += 3;
        } else {
          s7 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e1);
          }
        }
        if (s7 !== peg$FAILED) {
          s8 = peg$parseNL();
          if (s8 !== peg$FAILED) {
            s7 = [s7, s8];
            s6 = s7;
          } else {
            peg$currPos = s6;
            s6 = peg$FAILED;
          }
        } else {
          peg$currPos = s6;
          s6 = peg$FAILED;
        }
        peg$silentFails--;
        if (s6 === peg$FAILED) {
          s5 = void 0;
        } else {
          peg$currPos = s5;
          s5 = peg$FAILED;
        }
        if (s5 !== peg$FAILED) {
          s6 = peg$currPos;
          s7 = [];
          s8 = input.charAt(peg$currPos);
          if (peg$r0.test(s8)) {
            peg$currPos++;
          } else {
            s8 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e2);
            }
          }
          while (s8 !== peg$FAILED) {
            s7.push(s8);
            s8 = input.charAt(peg$currPos);
            if (peg$r0.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e2);
              }
            }
          }
          s6 = input.substring(s6, peg$currPos);
          s7 = peg$parseNL();
          if (s7 !== peg$FAILED) {
            peg$savedPos = s4;
            s4 = peg$f1(s6);
          } else {
            peg$currPos = s4;
            s4 = peg$FAILED;
          }
        } else {
          peg$currPos = s4;
          s4 = peg$FAILED;
        }
        while (s4 !== peg$FAILED) {
          s3.push(s4);
          s4 = peg$currPos;
          s5 = peg$currPos;
          peg$silentFails++;
          s6 = peg$currPos;
          if (input.substr(peg$currPos, 3) === peg$c1) {
            s7 = peg$c1;
            peg$currPos += 3;
          } else {
            s7 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e1);
            }
          }
          if (s7 !== peg$FAILED) {
            s8 = peg$parseNL();
            if (s8 !== peg$FAILED) {
              s7 = [s7, s8];
              s6 = s7;
            } else {
              peg$currPos = s6;
              s6 = peg$FAILED;
            }
          } else {
            peg$currPos = s6;
            s6 = peg$FAILED;
          }
          peg$silentFails--;
          if (s6 === peg$FAILED) {
            s5 = void 0;
          } else {
            peg$currPos = s5;
            s5 = peg$FAILED;
          }
          if (s5 !== peg$FAILED) {
            s6 = peg$currPos;
            s7 = [];
            s8 = input.charAt(peg$currPos);
            if (peg$r0.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e2);
              }
            }
            while (s8 !== peg$FAILED) {
              s7.push(s8);
              s8 = input.charAt(peg$currPos);
              if (peg$r0.test(s8)) {
                peg$currPos++;
              } else {
                s8 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e2);
                }
              }
            }
            s6 = input.substring(s6, peg$currPos);
            s7 = peg$parseNL();
            if (s7 !== peg$FAILED) {
              peg$savedPos = s4;
              s4 = peg$f1(s6);
            } else {
              peg$currPos = s4;
              s4 = peg$FAILED;
            }
          } else {
            peg$currPos = s4;
            s4 = peg$FAILED;
          }
        }
        if (input.substr(peg$currPos, 3) === peg$c1) {
          s4 = peg$c1;
          peg$currPos += 3;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e1);
          }
        }
        if (s4 !== peg$FAILED) {
          s5 = peg$parseNL();
          if (s5 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f2(s3);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseStatement() {
    var s0;
    s0 = peg$parseTitle();
    if (s0 === peg$FAILED) {
      s0 = peg$parseAxisLine();
      if (s0 === peg$FAILED) {
        s0 = peg$parseCurveLine();
        if (s0 === peg$FAILED) {
          s0 = peg$parseMaxLine();
          if (s0 === peg$FAILED) {
            s0 = peg$parseMinLine();
            if (s0 === peg$FAILED) {
              s0 = peg$parseBlankLine();
            }
          }
        }
      }
    }
    return s0;
  }
  function peg$parseTitle() {
    var s0, s1, s2, s3, s4, s5;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = input.substr(peg$currPos, 5);
    if (s2.toLowerCase() === peg$c2) {
      peg$currPos += 5;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e3);
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$parse_();
      s4 = peg$parseRestOfLine();
      if (s4 !== peg$FAILED) {
        s5 = peg$parseLineEnd();
        if (s5 !== peg$FAILED) {
          peg$savedPos = s0;
          s0 = peg$f3(s4);
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseAxisLine() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9, s10;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = input.substr(peg$currPos, 4);
    if (s2.toLowerCase() === peg$c3) {
      peg$currPos += 4;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e4);
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$parse_();
      s4 = peg$parseAxisDef();
      if (s4 !== peg$FAILED) {
        s5 = [];
        s6 = peg$currPos;
        s7 = peg$parse_();
        if (input.charCodeAt(peg$currPos) === 44) {
          s8 = peg$c4;
          peg$currPos++;
        } else {
          s8 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e5);
          }
        }
        if (s8 !== peg$FAILED) {
          s9 = peg$parse_();
          s10 = peg$parseAxisDef();
          if (s10 !== peg$FAILED) {
            peg$savedPos = s6;
            s6 = peg$f4(s4, s10);
          } else {
            peg$currPos = s6;
            s6 = peg$FAILED;
          }
        } else {
          peg$currPos = s6;
          s6 = peg$FAILED;
        }
        while (s6 !== peg$FAILED) {
          s5.push(s6);
          s6 = peg$currPos;
          s7 = peg$parse_();
          if (input.charCodeAt(peg$currPos) === 44) {
            s8 = peg$c4;
            peg$currPos++;
          } else {
            s8 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e5);
            }
          }
          if (s8 !== peg$FAILED) {
            s9 = peg$parse_();
            s10 = peg$parseAxisDef();
            if (s10 !== peg$FAILED) {
              peg$savedPos = s6;
              s6 = peg$f4(s4, s10);
            } else {
              peg$currPos = s6;
              s6 = peg$FAILED;
            }
          } else {
            peg$currPos = s6;
            s6 = peg$FAILED;
          }
        }
        s6 = peg$parse_();
        s7 = peg$parseLineEnd();
        if (s7 !== peg$FAILED) {
          peg$savedPos = s0;
          s0 = peg$f5(s4, s5);
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseCurveLine() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9, s10, s11, s12, s13, s14, s15;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = input.substr(peg$currPos, 5);
    if (s2.toLowerCase() === peg$c5) {
      peg$currPos += 5;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e6);
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$parse_();
      s4 = peg$parseIdent();
      if (s4 !== peg$FAILED) {
        s5 = peg$parseLabelBracket();
        if (s5 === peg$FAILED) {
          s5 = null;
        }
        s6 = peg$parse_();
        if (input.charCodeAt(peg$currPos) === 123) {
          s7 = peg$c6;
          peg$currPos++;
        } else {
          s7 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e7);
          }
        }
        if (s7 !== peg$FAILED) {
          s8 = peg$parse_();
          s9 = peg$parseNum();
          if (s9 !== peg$FAILED) {
            s10 = [];
            s11 = peg$currPos;
            s12 = peg$parse_();
            if (input.charCodeAt(peg$currPos) === 44) {
              s13 = peg$c4;
              peg$currPos++;
            } else {
              s13 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e5);
              }
            }
            if (s13 !== peg$FAILED) {
              s14 = peg$parse_();
              s15 = peg$parseNum();
              if (s15 !== peg$FAILED) {
                peg$savedPos = s11;
                s11 = peg$f6(s4, s5, s9, s15);
              } else {
                peg$currPos = s11;
                s11 = peg$FAILED;
              }
            } else {
              peg$currPos = s11;
              s11 = peg$FAILED;
            }
            while (s11 !== peg$FAILED) {
              s10.push(s11);
              s11 = peg$currPos;
              s12 = peg$parse_();
              if (input.charCodeAt(peg$currPos) === 44) {
                s13 = peg$c4;
                peg$currPos++;
              } else {
                s13 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e5);
                }
              }
              if (s13 !== peg$FAILED) {
                s14 = peg$parse_();
                s15 = peg$parseNum();
                if (s15 !== peg$FAILED) {
                  peg$savedPos = s11;
                  s11 = peg$f6(s4, s5, s9, s15);
                } else {
                  peg$currPos = s11;
                  s11 = peg$FAILED;
                }
              } else {
                peg$currPos = s11;
                s11 = peg$FAILED;
              }
            }
            s11 = peg$parse_();
            if (input.charCodeAt(peg$currPos) === 125) {
              s12 = peg$c7;
              peg$currPos++;
            } else {
              s12 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e8);
              }
            }
            if (s12 !== peg$FAILED) {
              s13 = peg$parse_();
              s14 = peg$parseLineEnd();
              if (s14 !== peg$FAILED) {
                peg$savedPos = s0;
                s0 = peg$f7(s4, s5, s9, s10);
              } else {
                peg$currPos = s0;
                s0 = peg$FAILED;
              }
            } else {
              peg$currPos = s0;
              s0 = peg$FAILED;
            }
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseMaxLine() {
    var s0, s1, s2, s3, s4, s5;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = input.substr(peg$currPos, 3);
    if (s2.toLowerCase() === peg$c8) {
      peg$currPos += 3;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e9);
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$parse_();
      s4 = peg$parseNum();
      if (s4 !== peg$FAILED) {
        s5 = peg$parseLineEnd();
        if (s5 !== peg$FAILED) {
          peg$savedPos = s0;
          s0 = peg$f8(s4);
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseMinLine() {
    var s0, s1, s2, s3, s4, s5;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = input.substr(peg$currPos, 3);
    if (s2.toLowerCase() === peg$c9) {
      peg$currPos += 3;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e10);
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$parse_();
      s4 = peg$parseNum();
      if (s4 !== peg$FAILED) {
        s5 = peg$parseLineEnd();
        if (s5 !== peg$FAILED) {
          peg$savedPos = s0;
          s0 = peg$f9(s4);
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseAxisDef() {
    var s0, s1, s2;
    s0 = peg$currPos;
    s1 = peg$parseIdent();
    if (s1 !== peg$FAILED) {
      s2 = peg$parseLabelBracket();
      if (s2 === peg$FAILED) {
        s2 = null;
      }
      peg$savedPos = s0;
      s0 = peg$f10(s1, s2);
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseLabelBracket() {
    var s0, s1, s2, s3, s4, s5;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 91) {
      s1 = peg$c10;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e11);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$parse_();
      s3 = peg$parseQuoted();
      if (s3 !== peg$FAILED) {
        s4 = peg$parse_();
        if (input.charCodeAt(peg$currPos) === 93) {
          s5 = peg$c11;
          peg$currPos++;
        } else {
          s5 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e12);
          }
        }
        if (s5 !== peg$FAILED) {
          peg$savedPos = s0;
          s0 = peg$f11(s3);
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      if (input.charCodeAt(peg$currPos) === 91) {
        s1 = peg$c10;
        peg$currPos++;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e11);
        }
      }
      if (s1 !== peg$FAILED) {
        s2 = peg$parse_();
        s3 = peg$currPos;
        s4 = [];
        s5 = input.charAt(peg$currPos);
        if (peg$r1.test(s5)) {
          peg$currPos++;
        } else {
          s5 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e13);
          }
        }
        if (s5 !== peg$FAILED) {
          while (s5 !== peg$FAILED) {
            s4.push(s5);
            s5 = input.charAt(peg$currPos);
            if (peg$r1.test(s5)) {
              peg$currPos++;
            } else {
              s5 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e13);
              }
            }
          }
        } else {
          s4 = peg$FAILED;
        }
        if (s4 !== peg$FAILED) {
          s3 = input.substring(s3, peg$currPos);
        } else {
          s3 = s4;
        }
        if (s3 !== peg$FAILED) {
          if (input.charCodeAt(peg$currPos) === 93) {
            s4 = peg$c11;
            peg$currPos++;
          } else {
            s4 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e12);
            }
          }
          if (s4 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f12(s3);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    }
    return s0;
  }
  function peg$parseIdent() {
    var s0, s1, s2;
    s0 = peg$currPos;
    s1 = [];
    s2 = input.charAt(peg$currPos);
    if (peg$r2.test(s2)) {
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e14);
      }
    }
    if (s2 !== peg$FAILED) {
      while (s2 !== peg$FAILED) {
        s1.push(s2);
        s2 = input.charAt(peg$currPos);
        if (peg$r2.test(s2)) {
          peg$currPos++;
        } else {
          s2 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e14);
          }
        }
      }
    } else {
      s1 = peg$FAILED;
    }
    if (s1 !== peg$FAILED) {
      s0 = input.substring(s0, peg$currPos);
    } else {
      s0 = s1;
    }
    return s0;
  }
  function peg$parseQuoted() {
    var s0, s1, s2, s3, s4;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 34) {
      s1 = peg$c12;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e15);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$currPos;
      s3 = [];
      s4 = input.charAt(peg$currPos);
      if (peg$r3.test(s4)) {
        peg$currPos++;
      } else {
        s4 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e16);
        }
      }
      while (s4 !== peg$FAILED) {
        s3.push(s4);
        s4 = input.charAt(peg$currPos);
        if (peg$r3.test(s4)) {
          peg$currPos++;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e16);
          }
        }
      }
      s2 = input.substring(s2, peg$currPos);
      if (input.charCodeAt(peg$currPos) === 34) {
        s3 = peg$c12;
        peg$currPos++;
      } else {
        s3 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e15);
        }
      }
      if (s3 !== peg$FAILED) {
        peg$savedPos = s0;
        s0 = peg$f13(s2);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseNum() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8;
    s0 = peg$currPos;
    s1 = peg$currPos;
    s2 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 45) {
      s3 = peg$c13;
      peg$currPos++;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e17);
      }
    }
    if (s3 === peg$FAILED) {
      s3 = null;
    }
    s4 = [];
    s5 = input.charAt(peg$currPos);
    if (peg$r4.test(s5)) {
      peg$currPos++;
    } else {
      s5 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e18);
      }
    }
    if (s5 !== peg$FAILED) {
      while (s5 !== peg$FAILED) {
        s4.push(s5);
        s5 = input.charAt(peg$currPos);
        if (peg$r4.test(s5)) {
          peg$currPos++;
        } else {
          s5 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e18);
          }
        }
      }
    } else {
      s4 = peg$FAILED;
    }
    if (s4 !== peg$FAILED) {
      s5 = peg$currPos;
      if (input.charCodeAt(peg$currPos) === 46) {
        s6 = peg$c14;
        peg$currPos++;
      } else {
        s6 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e19);
        }
      }
      if (s6 !== peg$FAILED) {
        s7 = [];
        s8 = input.charAt(peg$currPos);
        if (peg$r4.test(s8)) {
          peg$currPos++;
        } else {
          s8 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e18);
          }
        }
        if (s8 !== peg$FAILED) {
          while (s8 !== peg$FAILED) {
            s7.push(s8);
            s8 = input.charAt(peg$currPos);
            if (peg$r4.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e18);
              }
            }
          }
        } else {
          s7 = peg$FAILED;
        }
        if (s7 !== peg$FAILED) {
          s6 = [s6, s7];
          s5 = s6;
        } else {
          peg$currPos = s5;
          s5 = peg$FAILED;
        }
      } else {
        peg$currPos = s5;
        s5 = peg$FAILED;
      }
      if (s5 === peg$FAILED) {
        s5 = null;
      }
      s3 = [s3, s4, s5];
      s2 = s3;
    } else {
      peg$currPos = s2;
      s2 = peg$FAILED;
    }
    if (s2 !== peg$FAILED) {
      s1 = input.substring(s1, peg$currPos);
    } else {
      s1 = s2;
    }
    if (s1 !== peg$FAILED) {
      peg$savedPos = s0;
      s1 = peg$f14(s1);
    }
    s0 = s1;
    return s0;
  }
  function peg$parseRestOfLine() {
    var s0, s1, s2;
    s0 = peg$currPos;
    s1 = [];
    s2 = input.charAt(peg$currPos);
    if (peg$r0.test(s2)) {
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e2);
      }
    }
    if (s2 !== peg$FAILED) {
      while (s2 !== peg$FAILED) {
        s1.push(s2);
        s2 = input.charAt(peg$currPos);
        if (peg$r0.test(s2)) {
          peg$currPos++;
        } else {
          s2 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e2);
          }
        }
      }
    } else {
      s1 = peg$FAILED;
    }
    if (s1 !== peg$FAILED) {
      s0 = input.substring(s0, peg$currPos);
    } else {
      s0 = s1;
    }
    return s0;
  }
  function peg$parseNL() {
    var s0, s1, s2;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 13) {
      s1 = peg$c15;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e20);
      }
    }
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s2 = peg$c16;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e21);
      }
    }
    if (s2 !== peg$FAILED) {
      s1 = [s1, s2];
      s0 = s1;
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseBlankLine() {
    var s0, s1, s2, s3;
    s0 = peg$currPos;
    s1 = peg$parse_();
    if (input.charCodeAt(peg$currPos) === 13) {
      s2 = peg$c15;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e20);
      }
    }
    if (s2 === peg$FAILED) {
      s2 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s3 = peg$c16;
      peg$currPos++;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e21);
      }
    }
    if (s3 !== peg$FAILED) {
      peg$savedPos = s0;
      s0 = peg$f15();
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseLineEnd() {
    var s0, s1, s2;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 13) {
      s1 = peg$c15;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e20);
      }
    }
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s2 = peg$c16;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e21);
      }
    }
    if (s2 !== peg$FAILED) {
      s1 = [s1, s2];
      s0 = s1;
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      peg$silentFails++;
      if (input.length > peg$currPos) {
        s1 = input.charAt(peg$currPos);
        peg$currPos++;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e22);
        }
      }
      peg$silentFails--;
      if (s1 === peg$FAILED) {
        s0 = void 0;
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    }
    return s0;
  }
  function peg$parse_() {
    var s0, s1;
    s0 = [];
    s1 = input.charAt(peg$currPos);
    if (peg$r5.test(s1)) {
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e23);
      }
    }
    while (s1 !== peg$FAILED) {
      s0.push(s1);
      s1 = input.charAt(peg$currPos);
      if (peg$r5.test(s1)) {
        peg$currPos++;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e23);
        }
      }
    }
    return s0;
  }
  peg$result = peg$startRuleFunction();
  if (options.peg$library) {
    return (
      /** @type {any} */
      {
        peg$result,
        peg$currPos,
        peg$FAILED,
        peg$maxFailExpected,
        peg$maxFailPos
      }
    );
  }
  if (peg$result !== peg$FAILED && peg$currPos === input.length) {
    return peg$result;
  } else {
    if (peg$result !== peg$FAILED && peg$currPos < input.length) {
      peg$fail(peg$endExpectation());
    }
    throw peg$buildStructuredError(
      peg$maxFailExpected,
      peg$maxFailPos < input.length ? input.charAt(peg$maxFailPos) : null,
      peg$maxFailPos < input.length ? peg$computeLocation(peg$maxFailPos, peg$maxFailPos + 1) : peg$computeLocation(peg$maxFailPos, peg$maxFailPos)
    );
  }
}
var radar = {
  parseMermaid(input) {
    return peg$parse7(input);
  },
  parseYaml(input) {
    return JSON.parse(input);
  },
  layout(ir, theme) {
    return layoutRadar(ir, theme);
  }
};
function layoutGantt(ir, theme) {
  const { palette, typography, spacing } = theme;
  const p = pen(theme);
  const margin = spacing.diagramMargin;
  const ords = [];
  for (const sec of ir.sections)
    for (const t of sec.tasks) {
      ords.push(startOrdinal(t.start), startOrdinal(t.end));
    }
  if (ords.length === 0) ords.push(0, 30);
  const minOrd = Math.min(...ords);
  const maxOrd = Math.max(...ords);
  const span = Math.max(1, maxOrd - minOrd);
  const unit = inferAxisUnit(span);
  const ticks = enumTicks(minOrd, maxOrd, unit);
  const title = ir.metadata.title;
  const titleH = title ? typography.titleFontSize + 18 : 0;
  const leftColW = 230;
  const gridLeft = margin + leftColW;
  const gridW = 720;
  const gridRight = gridLeft + gridW;
  const axisH = 26;
  const rowH = 26;
  const rowGap = 4;
  const secHdrH = 26;
  const gridTop = margin + titleH + axisH;
  const dateX = (ord) => rhu(gridLeft + (ord - minOrd) / span * gridW);
  const elements = [];
  if (title) {
    elements.push(
      p.text(
        title,
        margin,
        margin + typography.titleFontSize,
        typography.titleFontSize,
        palette.text,
        { weight: "bold" }
      )
    );
  }
  let rowCount = 0;
  for (const sec of ir.sections) rowCount += 1 + sec.tasks.length;
  const bodyH = rowCount * (rowH + rowGap);
  const gridBottom = gridTop + bodyH;
  ticks.forEach((t, i) => {
    const x = dateX(t.ordinal);
    elements.push(
      p.path(`M ${x} ${gridTop} L ${x} ${gridBottom}`, palette.border, 1, { opacity: 0.4 })
    );
    elements.push(
      p.text(
        formatTickLabel(t, unit, i),
        x,
        margin + titleH + axisH - 8,
        typography.smallFontSize,
        palette.textMuted,
        { anchor: "middle" }
      )
    );
  });
  elements.push(p.path(`M ${gridLeft} ${gridTop} L ${gridRight} ${gridTop}`, palette.border, 1.5));
  let y = gridTop;
  for (const sec of ir.sections) {
    elements.push(
      p.rect(
        { x: margin, y, width: leftColW + gridW, height: secHdrH },
        palette.surface,
        palette.surface,
        0
      )
    );
    elements.push(
      p.text(
        sec.label,
        margin + 6,
        y + secHdrH / 2 + typography.baseFontSize * 0.35,
        typography.baseFontSize,
        palette.text,
        { weight: "bold" }
      )
    );
    y += secHdrH + rowGap;
    for (const task of sec.tasks) {
      const x0 = dateX(startOrdinal(task.start));
      const color = statusColor(palette, task.status === "crit" ? "blocked" : task.status);
      elements.push(
        p.text(
          truncateText(task.label, typography.smallFontSize, leftColW - 12),
          margin + 4,
          y + rowH / 2 + typography.smallFontSize * 0.35,
          typography.smallFontSize,
          palette.text
        )
      );
      if (task.isMilestone) {
        const cx = x0, cy = y + rowH / 2, r = 8;
        elements.push(
          p.path(
            `M ${cx} ${cy - r} L ${cx + r} ${cy} L ${cx} ${cy + r} L ${cx - r} ${cy} Z`,
            palette.background,
            1.5,
            { fill: color }
          )
        );
      } else {
        const x1 = dateX(startOrdinal(task.end));
        const barW = Math.max(4, x1 - x0);
        elements.push(
          p.rect({ x: x0, y: y + 4, width: rhu(barW), height: rowH - 8 }, color, color, 0, {
            rx: 3
          })
        );
      }
      y += rowH + rowGap;
    }
  }
  const totalW = rhuInt(gridRight + margin);
  const totalH = rhuInt(gridBottom + margin);
  const scene = applyOverlays(
    {
      viewBox: { x: 0, y: 0, width: totalW, height: totalH },
      background: palette.background,
      elements
    },
    ir.overlays,
    theme
  );
  return { scene, anchors: {} };
}
function peg$subclass8(child, parent) {
  function C() {
    this.constructor = child;
  }
  C.prototype = parent.prototype;
  child.prototype = new C();
}
function peg$SyntaxError8(message, expected, found, location) {
  var self = Error.call(this, message);
  if (Object.setPrototypeOf) {
    Object.setPrototypeOf(self, peg$SyntaxError8.prototype);
  }
  self.expected = expected;
  self.found = found;
  self.location = location;
  self.name = "SyntaxError";
  return self;
}
peg$subclass8(peg$SyntaxError8, Error);
function peg$padEnd8(str, targetLength, padString) {
  padString = padString || " ";
  if (str.length > targetLength) {
    return str;
  }
  targetLength -= str.length;
  padString += padString.repeat(targetLength);
  return str + padString.slice(0, targetLength);
}
peg$SyntaxError8.prototype.format = function(sources) {
  var str = "Error: " + this.message;
  if (this.location) {
    var src = null;
    var k;
    for (k = 0; k < sources.length; k++) {
      if (sources[k].source === this.location.source) {
        src = sources[k].text.split(/\r\n|\n|\r/g);
        break;
      }
    }
    var s = this.location.start;
    var offset_s = this.location.source && typeof this.location.source.offset === "function" ? this.location.source.offset(s) : s;
    var loc = this.location.source + ":" + offset_s.line + ":" + offset_s.column;
    if (src) {
      var e = this.location.end;
      var filler = peg$padEnd8("", offset_s.line.toString().length, " ");
      var line = src[s.line - 1];
      var last = s.line === e.line ? e.column : line.length + 1;
      var hatLen = last - s.column || 1;
      str += "\n --> " + loc + "\n" + filler + " |\n" + offset_s.line + " | " + line + "\n" + filler + " | " + peg$padEnd8("", s.column - 1, " ") + peg$padEnd8("", hatLen, "^");
    } else {
      str += "\n at " + loc;
    }
  }
  return str;
};
peg$SyntaxError8.buildMessage = function(expected, found) {
  var DESCRIBE_EXPECTATION_FNS = {
    literal: function(expectation) {
      return '"' + literalEscape(expectation.text) + '"';
    },
    class: function(expectation) {
      var escapedParts = expectation.parts.map(function(part) {
        return Array.isArray(part) ? classEscape(part[0]) + "-" + classEscape(part[1]) : classEscape(part);
      });
      return "[" + (expectation.inverted ? "^" : "") + escapedParts.join("") + "]";
    },
    any: function() {
      return "any character";
    },
    end: function() {
      return "end of input";
    },
    other: function(expectation) {
      return expectation.description;
    }
  };
  function hex(ch) {
    return ch.charCodeAt(0).toString(16).toUpperCase();
  }
  function literalEscape(s) {
    return s.replace(/\\/g, "\\\\").replace(/"/g, '\\"').replace(/\0/g, "\\0").replace(/\t/g, "\\t").replace(/\n/g, "\\n").replace(/\r/g, "\\r").replace(/[\x00-\x0F]/g, function(ch) {
      return "\\x0" + hex(ch);
    }).replace(/[\x10-\x1F\x7F-\x9F]/g, function(ch) {
      return "\\x" + hex(ch);
    });
  }
  function classEscape(s) {
    return s.replace(/\\/g, "\\\\").replace(/\]/g, "\\]").replace(/\^/g, "\\^").replace(/-/g, "\\-").replace(/\0/g, "\\0").replace(/\t/g, "\\t").replace(/\n/g, "\\n").replace(/\r/g, "\\r").replace(/[\x00-\x0F]/g, function(ch) {
      return "\\x0" + hex(ch);
    }).replace(/[\x10-\x1F\x7F-\x9F]/g, function(ch) {
      return "\\x" + hex(ch);
    });
  }
  function describeExpectation(expectation) {
    return DESCRIBE_EXPECTATION_FNS[expectation.type](expectation);
  }
  function describeExpected(expected2) {
    var descriptions = expected2.map(describeExpectation);
    var i, j;
    descriptions.sort();
    if (descriptions.length > 0) {
      for (i = 1, j = 1; i < descriptions.length; i++) {
        if (descriptions[i - 1] !== descriptions[i]) {
          descriptions[j] = descriptions[i];
          j++;
        }
      }
      descriptions.length = j;
    }
    switch (descriptions.length) {
      case 1:
        return descriptions[0];
      case 2:
        return descriptions[0] + " or " + descriptions[1];
      default:
        return descriptions.slice(0, -1).join(", ") + ", or " + descriptions[descriptions.length - 1];
    }
  }
  function describeFound(found2) {
    return found2 ? '"' + literalEscape(found2) + '"' : "end of input";
  }
  return "Expected " + describeExpected(expected) + " but " + describeFound(found) + " found.";
};
function peg$parse8(input, options) {
  options = options !== void 0 ? options : {};
  var peg$FAILED = {};
  var peg$source = options.grammarSource;
  var peg$startRuleFunctions = { Document: peg$parseDocument };
  var peg$startRuleFunction = peg$parseDocument;
  var peg$c0 = "gantt";
  var peg$c1 = "---";
  var peg$c2 = "title";
  var peg$c3 = "dateformat";
  var peg$c4 = "excludes";
  var peg$c5 = "axisformat";
  var peg$c6 = "todaymarker";
  var peg$c7 = "tickinterval";
  var peg$c8 = "section";
  var peg$c9 = ":";
  var peg$c10 = "\r";
  var peg$c11 = "\n";
  var peg$r0 = /^[^\n]/;
  var peg$r1 = /^[^:\n]/;
  var peg$r2 = /^[ \t]/;
  var peg$e0 = peg$literalExpectation("gantt", true);
  var peg$e1 = peg$literalExpectation("---", false);
  var peg$e2 = peg$classExpectation(["\n"], true, false);
  var peg$e3 = peg$literalExpectation("title", true);
  var peg$e4 = peg$literalExpectation("dateFormat", true);
  var peg$e5 = peg$literalExpectation("excludes", true);
  var peg$e6 = peg$literalExpectation("axisFormat", true);
  var peg$e7 = peg$literalExpectation("todayMarker", true);
  var peg$e8 = peg$literalExpectation("tickInterval", true);
  var peg$e9 = peg$literalExpectation("section", true);
  var peg$e10 = peg$classExpectation([":", "\n"], true, false);
  var peg$e11 = peg$literalExpectation(":", false);
  var peg$e12 = peg$literalExpectation("\r", false);
  var peg$e13 = peg$literalExpectation("\n", false);
  var peg$e14 = peg$anyExpectation();
  var peg$e15 = peg$classExpectation([" ", "	"], false, false);
  var peg$f0 = function(fm, lines2) {
    let title, dateFormat;
    const sections2 = [];
    for (const l of lines2.filter(Boolean)) {
      if (l.t === "title") title = l.v;
      else if (l.t === "dateFormat") dateFormat = l.v;
      else if (l.t === "section") sections2.push({ label: l.label, tasks: [] });
      else if (l.t === "task") {
        if (sections2.length === 0) sections2.push({ label: "", tasks: [] });
        sections2[sections2.length - 1].tasks.push({ name: l.name, meta: l.meta });
      }
    }
    return {
      version: "1.0",
      metadata: { ...fm || {}, ...title ? { title } : {} },
      dateFormat: dateFormat || "YYYY-MM-DD",
      sections: sections2
    };
  };
  var peg$f1 = function(line) {
    return line;
  };
  var peg$f2 = function(lines2) {
    const meta = {};
    for (const line of lines2) {
      const m = line.match(/^\s*(\w+)\s*:\s*(.+?)\s*$/);
      if (m) meta[m[1]] = m[2];
    }
    return meta;
  };
  var peg$f3 = function(v) {
    return { t: "title", v: v.trim() };
  };
  var peg$f4 = function(v) {
    return { t: "dateFormat", v: v.trim() };
  };
  var peg$f5 = function() {
    return null;
  };
  var peg$f6 = function(label) {
    return { t: "section", label: label.trim() };
  };
  var peg$f7 = function(name, meta) {
    return { t: "task", name: name.trim(), meta: meta.trim() };
  };
  var peg$f8 = function() {
    return null;
  };
  var peg$currPos = options.peg$currPos | 0;
  var peg$savedPos = peg$currPos;
  var peg$posDetailsCache = [{ line: 1, column: 1 }];
  var peg$maxFailPos = peg$currPos;
  var peg$maxFailExpected = options.peg$maxFailExpected || [];
  var peg$silentFails = options.peg$silentFails | 0;
  var peg$result;
  if (options.startRule) {
    if (!(options.startRule in peg$startRuleFunctions)) {
      throw new Error(`Can't start parsing from rule "` + options.startRule + '".');
    }
    peg$startRuleFunction = peg$startRuleFunctions[options.startRule];
  }
  function text() {
    return input.substring(peg$savedPos, peg$currPos);
  }
  function offset() {
    return peg$savedPos;
  }
  function range() {
    return {
      source: peg$source,
      start: peg$savedPos,
      end: peg$currPos
    };
  }
  function location() {
    return peg$computeLocation(peg$savedPos, peg$currPos);
  }
  function expected(description, location2) {
    location2 = location2 !== void 0 ? location2 : peg$computeLocation(peg$savedPos, peg$currPos);
    throw peg$buildStructuredError(
      [peg$otherExpectation(description)],
      input.substring(peg$savedPos, peg$currPos),
      location2
    );
  }
  function error(message, location2) {
    location2 = location2 !== void 0 ? location2 : peg$computeLocation(peg$savedPos, peg$currPos);
    throw peg$buildSimpleError(message, location2);
  }
  function peg$literalExpectation(text2, ignoreCase) {
    return { type: "literal", text: text2, ignoreCase };
  }
  function peg$classExpectation(parts, inverted, ignoreCase) {
    return { type: "class", parts, inverted, ignoreCase };
  }
  function peg$anyExpectation() {
    return { type: "any" };
  }
  function peg$endExpectation() {
    return { type: "end" };
  }
  function peg$otherExpectation(description) {
    return { type: "other", description };
  }
  function peg$computePosDetails(pos) {
    var details = peg$posDetailsCache[pos];
    var p;
    if (details) {
      return details;
    } else {
      if (pos >= peg$posDetailsCache.length) {
        p = peg$posDetailsCache.length - 1;
      } else {
        p = pos;
        while (!peg$posDetailsCache[--p]) {
        }
      }
      details = peg$posDetailsCache[p];
      details = {
        line: details.line,
        column: details.column
      };
      while (p < pos) {
        if (input.charCodeAt(p) === 10) {
          details.line++;
          details.column = 1;
        } else {
          details.column++;
        }
        p++;
      }
      peg$posDetailsCache[pos] = details;
      return details;
    }
  }
  function peg$computeLocation(startPos, endPos, offset2) {
    var startPosDetails = peg$computePosDetails(startPos);
    var endPosDetails = peg$computePosDetails(endPos);
    var res = {
      source: peg$source,
      start: {
        offset: startPos,
        line: startPosDetails.line,
        column: startPosDetails.column
      },
      end: {
        offset: endPos,
        line: endPosDetails.line,
        column: endPosDetails.column
      }
    };
    if (offset2 && peg$source && typeof peg$source.offset === "function") {
      res.start = peg$source.offset(res.start);
      res.end = peg$source.offset(res.end);
    }
    return res;
  }
  function peg$fail(expected2) {
    if (peg$currPos < peg$maxFailPos) {
      return;
    }
    if (peg$currPos > peg$maxFailPos) {
      peg$maxFailPos = peg$currPos;
      peg$maxFailExpected = [];
    }
    peg$maxFailExpected.push(expected2);
  }
  function peg$buildSimpleError(message, location2) {
    return new peg$SyntaxError8(message, null, null, location2);
  }
  function peg$buildStructuredError(expected2, found, location2) {
    return new peg$SyntaxError8(
      peg$SyntaxError8.buildMessage(expected2, found),
      expected2,
      found,
      location2
    );
  }
  function peg$parseDocument() {
    var s0, s1, s2, s3, s4, s5, s6;
    s0 = peg$currPos;
    s1 = peg$parseFrontmatter();
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    s2 = peg$parse_();
    s3 = input.substr(peg$currPos, 5);
    if (s3.toLowerCase() === peg$c0) {
      peg$currPos += 5;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e0);
      }
    }
    if (s3 !== peg$FAILED) {
      s4 = peg$parseLineEnd();
      if (s4 !== peg$FAILED) {
        s5 = [];
        s6 = peg$parseLine();
        while (s6 !== peg$FAILED) {
          s5.push(s6);
          s6 = peg$parseLine();
        }
        s6 = peg$parse_();
        peg$savedPos = s0;
        s0 = peg$f0(s1, s5);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseFrontmatter() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 3) === peg$c1) {
      s1 = peg$c1;
      peg$currPos += 3;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e1);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$parseNL();
      if (s2 !== peg$FAILED) {
        s3 = [];
        s4 = peg$currPos;
        s5 = peg$currPos;
        peg$silentFails++;
        s6 = peg$currPos;
        if (input.substr(peg$currPos, 3) === peg$c1) {
          s7 = peg$c1;
          peg$currPos += 3;
        } else {
          s7 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e1);
          }
        }
        if (s7 !== peg$FAILED) {
          s8 = peg$parseNL();
          if (s8 !== peg$FAILED) {
            s7 = [s7, s8];
            s6 = s7;
          } else {
            peg$currPos = s6;
            s6 = peg$FAILED;
          }
        } else {
          peg$currPos = s6;
          s6 = peg$FAILED;
        }
        peg$silentFails--;
        if (s6 === peg$FAILED) {
          s5 = void 0;
        } else {
          peg$currPos = s5;
          s5 = peg$FAILED;
        }
        if (s5 !== peg$FAILED) {
          s6 = peg$currPos;
          s7 = [];
          s8 = input.charAt(peg$currPos);
          if (peg$r0.test(s8)) {
            peg$currPos++;
          } else {
            s8 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e2);
            }
          }
          while (s8 !== peg$FAILED) {
            s7.push(s8);
            s8 = input.charAt(peg$currPos);
            if (peg$r0.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e2);
              }
            }
          }
          s6 = input.substring(s6, peg$currPos);
          s7 = peg$parseNL();
          if (s7 !== peg$FAILED) {
            peg$savedPos = s4;
            s4 = peg$f1(s6);
          } else {
            peg$currPos = s4;
            s4 = peg$FAILED;
          }
        } else {
          peg$currPos = s4;
          s4 = peg$FAILED;
        }
        while (s4 !== peg$FAILED) {
          s3.push(s4);
          s4 = peg$currPos;
          s5 = peg$currPos;
          peg$silentFails++;
          s6 = peg$currPos;
          if (input.substr(peg$currPos, 3) === peg$c1) {
            s7 = peg$c1;
            peg$currPos += 3;
          } else {
            s7 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e1);
            }
          }
          if (s7 !== peg$FAILED) {
            s8 = peg$parseNL();
            if (s8 !== peg$FAILED) {
              s7 = [s7, s8];
              s6 = s7;
            } else {
              peg$currPos = s6;
              s6 = peg$FAILED;
            }
          } else {
            peg$currPos = s6;
            s6 = peg$FAILED;
          }
          peg$silentFails--;
          if (s6 === peg$FAILED) {
            s5 = void 0;
          } else {
            peg$currPos = s5;
            s5 = peg$FAILED;
          }
          if (s5 !== peg$FAILED) {
            s6 = peg$currPos;
            s7 = [];
            s8 = input.charAt(peg$currPos);
            if (peg$r0.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e2);
              }
            }
            while (s8 !== peg$FAILED) {
              s7.push(s8);
              s8 = input.charAt(peg$currPos);
              if (peg$r0.test(s8)) {
                peg$currPos++;
              } else {
                s8 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e2);
                }
              }
            }
            s6 = input.substring(s6, peg$currPos);
            s7 = peg$parseNL();
            if (s7 !== peg$FAILED) {
              peg$savedPos = s4;
              s4 = peg$f1(s6);
            } else {
              peg$currPos = s4;
              s4 = peg$FAILED;
            }
          } else {
            peg$currPos = s4;
            s4 = peg$FAILED;
          }
        }
        if (input.substr(peg$currPos, 3) === peg$c1) {
          s4 = peg$c1;
          peg$currPos += 3;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e1);
          }
        }
        if (s4 !== peg$FAILED) {
          s5 = peg$parseNL();
          if (s5 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f2(s3);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseLine() {
    var s0;
    s0 = peg$parseTitleLine();
    if (s0 === peg$FAILED) {
      s0 = peg$parseDateFormatLine();
      if (s0 === peg$FAILED) {
        s0 = peg$parseExcludesLine();
        if (s0 === peg$FAILED) {
          s0 = peg$parseSectionLine();
          if (s0 === peg$FAILED) {
            s0 = peg$parseTaskLine();
            if (s0 === peg$FAILED) {
              s0 = peg$parseBlankLine();
            }
          }
        }
      }
    }
    return s0;
  }
  function peg$parseTitleLine() {
    var s0, s1, s2, s3, s4, s5;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = input.substr(peg$currPos, 5);
    if (s2.toLowerCase() === peg$c2) {
      peg$currPos += 5;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e3);
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$parse__();
      if (s3 !== peg$FAILED) {
        s4 = peg$parseRestOfLine();
        if (s4 !== peg$FAILED) {
          s5 = peg$parseLineEnd();
          if (s5 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f3(s4);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseDateFormatLine() {
    var s0, s1, s2, s3, s4, s5;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = input.substr(peg$currPos, 10);
    if (s2.toLowerCase() === peg$c3) {
      peg$currPos += 10;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e4);
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$parse__();
      if (s3 !== peg$FAILED) {
        s4 = peg$parseRestOfLine();
        if (s4 !== peg$FAILED) {
          s5 = peg$parseLineEnd();
          if (s5 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f4(s4);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseExcludesLine() {
    var s0, s1, s2, s3, s4, s5;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = input.substr(peg$currPos, 8);
    if (s2.toLowerCase() === peg$c4) {
      peg$currPos += 8;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e5);
      }
    }
    if (s2 === peg$FAILED) {
      s2 = input.substr(peg$currPos, 10);
      if (s2.toLowerCase() === peg$c5) {
        peg$currPos += 10;
      } else {
        s2 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e6);
        }
      }
      if (s2 === peg$FAILED) {
        s2 = input.substr(peg$currPos, 11);
        if (s2.toLowerCase() === peg$c6) {
          peg$currPos += 11;
        } else {
          s2 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e7);
          }
        }
        if (s2 === peg$FAILED) {
          s2 = input.substr(peg$currPos, 12);
          if (s2.toLowerCase() === peg$c7) {
            peg$currPos += 12;
          } else {
            s2 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e8);
            }
          }
        }
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$parse__();
      if (s3 !== peg$FAILED) {
        s4 = peg$parseRestOfLine();
        if (s4 !== peg$FAILED) {
          s5 = peg$parseLineEnd();
          if (s5 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f5();
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseSectionLine() {
    var s0, s1, s2, s3, s4, s5;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = input.substr(peg$currPos, 7);
    if (s2.toLowerCase() === peg$c8) {
      peg$currPos += 7;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e9);
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$parse__();
      if (s3 !== peg$FAILED) {
        s4 = peg$parseRestOfLine();
        if (s4 !== peg$FAILED) {
          s5 = peg$parseLineEnd();
          if (s5 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f6(s4);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseTaskLine() {
    var s0, s1, s2, s3, s4, s5, s6;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = peg$currPos;
    s3 = [];
    s4 = input.charAt(peg$currPos);
    if (peg$r1.test(s4)) {
      peg$currPos++;
    } else {
      s4 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e10);
      }
    }
    if (s4 !== peg$FAILED) {
      while (s4 !== peg$FAILED) {
        s3.push(s4);
        s4 = input.charAt(peg$currPos);
        if (peg$r1.test(s4)) {
          peg$currPos++;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e10);
          }
        }
      }
    } else {
      s3 = peg$FAILED;
    }
    if (s3 !== peg$FAILED) {
      s2 = input.substring(s2, peg$currPos);
    } else {
      s2 = s3;
    }
    if (s2 !== peg$FAILED) {
      if (input.charCodeAt(peg$currPos) === 58) {
        s3 = peg$c9;
        peg$currPos++;
      } else {
        s3 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e11);
        }
      }
      if (s3 !== peg$FAILED) {
        s4 = peg$currPos;
        s5 = [];
        s6 = input.charAt(peg$currPos);
        if (peg$r0.test(s6)) {
          peg$currPos++;
        } else {
          s6 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e2);
          }
        }
        if (s6 !== peg$FAILED) {
          while (s6 !== peg$FAILED) {
            s5.push(s6);
            s6 = input.charAt(peg$currPos);
            if (peg$r0.test(s6)) {
              peg$currPos++;
            } else {
              s6 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e2);
              }
            }
          }
        } else {
          s5 = peg$FAILED;
        }
        if (s5 !== peg$FAILED) {
          s4 = input.substring(s4, peg$currPos);
        } else {
          s4 = s5;
        }
        if (s4 !== peg$FAILED) {
          s5 = peg$parseLineEnd();
          if (s5 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f7(s2, s4);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseRestOfLine() {
    var s0, s1, s2;
    s0 = peg$currPos;
    s1 = [];
    s2 = input.charAt(peg$currPos);
    if (peg$r0.test(s2)) {
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e2);
      }
    }
    if (s2 !== peg$FAILED) {
      while (s2 !== peg$FAILED) {
        s1.push(s2);
        s2 = input.charAt(peg$currPos);
        if (peg$r0.test(s2)) {
          peg$currPos++;
        } else {
          s2 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e2);
          }
        }
      }
    } else {
      s1 = peg$FAILED;
    }
    if (s1 !== peg$FAILED) {
      s0 = input.substring(s0, peg$currPos);
    } else {
      s0 = s1;
    }
    return s0;
  }
  function peg$parseBlankLine() {
    var s0, s1, s2, s3;
    s0 = peg$currPos;
    s1 = peg$parse_();
    if (input.charCodeAt(peg$currPos) === 13) {
      s2 = peg$c10;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e12);
      }
    }
    if (s2 === peg$FAILED) {
      s2 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s3 = peg$c11;
      peg$currPos++;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e13);
      }
    }
    if (s3 !== peg$FAILED) {
      peg$savedPos = s0;
      s0 = peg$f8();
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseNL() {
    var s0, s1, s2;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 13) {
      s1 = peg$c10;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e12);
      }
    }
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s2 = peg$c11;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e13);
      }
    }
    if (s2 !== peg$FAILED) {
      s1 = [s1, s2];
      s0 = s1;
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseLineEnd() {
    var s0, s1, s2;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 13) {
      s1 = peg$c10;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e12);
      }
    }
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s2 = peg$c11;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e13);
      }
    }
    if (s2 !== peg$FAILED) {
      s1 = [s1, s2];
      s0 = s1;
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      peg$silentFails++;
      if (input.length > peg$currPos) {
        s1 = input.charAt(peg$currPos);
        peg$currPos++;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e14);
        }
      }
      peg$silentFails--;
      if (s1 === peg$FAILED) {
        s0 = void 0;
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    }
    return s0;
  }
  function peg$parse_() {
    var s0, s1;
    s0 = [];
    s1 = input.charAt(peg$currPos);
    if (peg$r2.test(s1)) {
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e15);
      }
    }
    while (s1 !== peg$FAILED) {
      s0.push(s1);
      s1 = input.charAt(peg$currPos);
      if (peg$r2.test(s1)) {
        peg$currPos++;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e15);
        }
      }
    }
    return s0;
  }
  function peg$parse__() {
    var s0, s1;
    s0 = [];
    s1 = input.charAt(peg$currPos);
    if (peg$r2.test(s1)) {
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e15);
      }
    }
    if (s1 !== peg$FAILED) {
      while (s1 !== peg$FAILED) {
        s0.push(s1);
        s1 = input.charAt(peg$currPos);
        if (peg$r2.test(s1)) {
          peg$currPos++;
        } else {
          s1 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e15);
          }
        }
      }
    } else {
      s0 = peg$FAILED;
    }
    return s0;
  }
  peg$result = peg$startRuleFunction();
  if (options.peg$library) {
    return (
      /** @type {any} */
      {
        peg$result,
        peg$currPos,
        peg$FAILED,
        peg$maxFailExpected,
        peg$maxFailPos
      }
    );
  }
  if (peg$result !== peg$FAILED && peg$currPos === input.length) {
    return peg$result;
  } else {
    if (peg$result !== peg$FAILED && peg$currPos < input.length) {
      peg$fail(peg$endExpectation());
    }
    throw peg$buildStructuredError(
      peg$maxFailExpected,
      peg$maxFailPos < input.length ? input.charAt(peg$maxFailPos) : null,
      peg$maxFailPos < input.length ? peg$computeLocation(peg$maxFailPos, peg$maxFailPos + 1) : peg$computeLocation(peg$maxFailPos, peg$maxFailPos)
    );
  }
}
var STATUS_FLAGS = /* @__PURE__ */ new Set(["done", "active", "crit", "milestone"]);
function isoFromOrdinal(ord) {
  const [y, m, d] = ordinalToDate(ord);
  return `${y}-${String(m).padStart(2, "0")}-${String(d).padStart(2, "0")}`;
}
function durationDays(token) {
  const m = token.match(/^(\d+(?:\.\d+)?)\s*([dwhm]?)/i);
  if (!m) return 1;
  const n = parseFloat(m[1]);
  switch ((m[2] || "d").toLowerCase()) {
    case "w":
      return n * 7;
    case "h":
      return Math.max(1, Math.round(n / 24));
    default:
      return n;
  }
}
function dateOrdinal(date) {
  try {
    const [y, m, d] = coerceLeft(parseIRDate(date));
    return dateToOrdinal(y, m, d);
  } catch {
    return 0;
  }
}
function resolveTask(raw, endById) {
  const tokens = raw.meta.split(",").map((t) => t.trim()).filter(Boolean);
  const flags = [];
  const rest = [];
  for (const tok of tokens) {
    if (STATUS_FLAGS.has(tok.toLowerCase())) flags.push(tok.toLowerCase());
    else rest.push(tok);
  }
  let id;
  let startTok;
  let durTok;
  if (rest.length >= 3) {
    id = rest[0];
    startTok = rest[1];
    durTok = rest[2];
  } else {
    startTok = rest[0] ?? "";
    durTok = rest[1] ?? "0d";
  }
  const startOrd = /^after\s+/i.test(startTok) ? endById.get(startTok.replace(/^after\s+/i, "").trim()) ?? 0 : dateOrdinal(startTok);
  const isMilestone = flags.includes("milestone") || durationDays(durTok) === 0;
  const endOrd = isMilestone ? startOrd : startOrd + durationDays(durTok);
  if (id) endById.set(id, endOrd);
  const status = flags.includes("done") ? "done" : flags.includes("active") ? "active" : flags.includes("crit") ? "crit" : "default";
  return {
    ...id ? { id } : {},
    label: raw.name,
    status,
    start: isoFromOrdinal(startOrd),
    end: isoFromOrdinal(endOrd),
    isMilestone
  };
}
var gantt = {
  parseMermaid(input) {
    const raw = peg$parse8(input);
    const endById = /* @__PURE__ */ new Map();
    const sections2 = raw.sections.map((sec) => ({
      label: sec.label,
      tasks: sec.tasks.map((t) => resolveTask(t, endById))
    }));
    return { version: raw.version, metadata: raw.metadata, sections: sections2 };
  },
  parseYaml(input) {
    return JSON.parse(input);
  },
  layout(ir, theme) {
    return layoutGantt(ir, theme);
  }
};
var SCORE_MIN = 1;
var SCORE_MAX = 5;
function layoutJourney(ir, theme) {
  const { palette, typography, spacing } = theme;
  const p = pen(theme);
  const margin = spacing.diagramMargin;
  const scoreColor = (s) => {
    if (s <= 1.5) return palette.error;
    if (s <= 2.5) return palette.warning;
    if (s <= 3.5) return palette.secondary;
    if (s <= 4.5) return palette.primary;
    return palette.success;
  };
  const flat2 = [];
  ir.sections.forEach(
    (sec, si) => sec.tasks.forEach(
      (t) => flat2.push({ label: t.label, score: t.score, actors: t.actors, section: si })
    )
  );
  const n = Math.max(flat2.length, 1);
  const title = ir.metadata.title;
  const titleH = title ? typography.titleFontSize + 16 : 0;
  const sectionBandH = 30;
  const plotLeft = margin + 30;
  const plotTop = margin + titleH + sectionBandH + 16;
  const plotH = 240;
  const plotBottom = plotTop + plotH;
  const step = 150;
  const plotW = step * n;
  const plotRight = plotLeft + plotW;
  const taskX = (i) => plotLeft + (i + 0.5) * step;
  const scoreY = (s) => rhu(
    plotBottom - (Math.max(SCORE_MIN, Math.min(SCORE_MAX, s)) - SCORE_MIN) / (SCORE_MAX - SCORE_MIN) * plotH
  );
  const elements = [];
  if (title) {
    elements.push(
      p.text(
        title,
        margin,
        margin + typography.titleFontSize,
        typography.titleFontSize,
        palette.text,
        { weight: "bold" }
      )
    );
  }
  for (let s = SCORE_MIN; s <= SCORE_MAX; s++) {
    const y = scoreY(s);
    elements.push(
      p.path(`M ${plotLeft} ${y} L ${plotRight} ${y}`, palette.border, 1, { opacity: 0.4 })
    );
    elements.push(
      p.text(
        String(s),
        plotLeft - 10,
        y + typography.smallFontSize * 0.35,
        typography.smallFontSize,
        palette.textMuted,
        { anchor: "end" }
      )
    );
  }
  ir.sections.forEach((sec, si) => {
    const idxs = flat2.map((f2, i) => ({ f: f2, i })).filter(({ f: f2 }) => f2.section === si).map(({ i }) => i);
    if (idxs.length === 0) return;
    const x0 = taskX(idxs[0]) - step / 2;
    const x1 = taskX(idxs[idxs.length - 1]) + step / 2;
    const hue = categoricalHue(si, theme);
    elements.push(
      p.rect(
        { x: rhu(x0), y: margin + titleH, width: rhu(x1 - x0 - 6), height: sectionBandH },
        hue,
        hue,
        0,
        { rx: 6 }
      )
    );
    elements.push(
      p.text(
        sec.label,
        rhuInt((x0 + x1) / 2 - 3),
        margin + titleH + sectionBandH / 2 + typography.baseFontSize * 0.35,
        typography.baseFontSize,
        readableText(hue, theme),
        { weight: "bold", anchor: "middle" }
      )
    );
  });
  const nodePts = flat2.map((f2, i) => ({ x: rhu(taskX(i)), y: scoreY(f2.score) }));
  if (nodePts.length > 1) {
    elements.push(
      p.path(`M ${nodePts.map((pt) => `${pt.x} ${pt.y}`).join(" L ")}`, palette.textMuted, 2, {
        opacity: 0.6
      })
    );
  }
  flat2.forEach((f2, i) => {
    const x = rhu(taskX(i)), y = scoreY(f2.score);
    const color = scoreColor(f2.score);
    elements.push(p.circle({ x, y }, 16, color, palette.background, 2));
    elements.push(
      p.text(
        String(f2.score),
        x,
        y + typography.baseFontSize * 0.35,
        typography.baseFontSize,
        readableText(color, theme),
        { weight: "bold", anchor: "middle" }
      )
    );
    elements.push(
      p.text(
        f2.label,
        x,
        plotBottom + typography.smallFontSize + 10,
        typography.smallFontSize,
        palette.text,
        { anchor: "middle", weight: "bold" }
      )
    );
    f2.actors.forEach((a, ai) => {
      elements.push(
        p.text(
          a,
          x,
          plotBottom + typography.smallFontSize + 10 + (ai + 1) * (typography.smallFontSize + 4),
          typography.smallFontSize,
          palette.textMuted,
          { anchor: "middle" }
        )
      );
    });
  });
  const maxActors = Math.max(1, ...flat2.map((f2) => f2.actors.length));
  const totalW = rhuInt(plotRight + margin);
  const totalH = rhuInt(
    plotBottom + typography.smallFontSize + 10 + (maxActors + 1) * (typography.smallFontSize + 4) + margin
  );
  const scene = applyOverlays(
    {
      viewBox: { x: 0, y: 0, width: totalW, height: totalH },
      background: palette.background,
      elements
    },
    ir.overlays,
    theme
  );
  return { scene, anchors: {} };
}
function peg$subclass9(child, parent) {
  function C() {
    this.constructor = child;
  }
  C.prototype = parent.prototype;
  child.prototype = new C();
}
function peg$SyntaxError9(message, expected, found, location) {
  var self = Error.call(this, message);
  if (Object.setPrototypeOf) {
    Object.setPrototypeOf(self, peg$SyntaxError9.prototype);
  }
  self.expected = expected;
  self.found = found;
  self.location = location;
  self.name = "SyntaxError";
  return self;
}
peg$subclass9(peg$SyntaxError9, Error);
function peg$padEnd9(str, targetLength, padString) {
  padString = padString || " ";
  if (str.length > targetLength) {
    return str;
  }
  targetLength -= str.length;
  padString += padString.repeat(targetLength);
  return str + padString.slice(0, targetLength);
}
peg$SyntaxError9.prototype.format = function(sources) {
  var str = "Error: " + this.message;
  if (this.location) {
    var src = null;
    var k;
    for (k = 0; k < sources.length; k++) {
      if (sources[k].source === this.location.source) {
        src = sources[k].text.split(/\r\n|\n|\r/g);
        break;
      }
    }
    var s = this.location.start;
    var offset_s = this.location.source && typeof this.location.source.offset === "function" ? this.location.source.offset(s) : s;
    var loc = this.location.source + ":" + offset_s.line + ":" + offset_s.column;
    if (src) {
      var e = this.location.end;
      var filler = peg$padEnd9("", offset_s.line.toString().length, " ");
      var line = src[s.line - 1];
      var last = s.line === e.line ? e.column : line.length + 1;
      var hatLen = last - s.column || 1;
      str += "\n --> " + loc + "\n" + filler + " |\n" + offset_s.line + " | " + line + "\n" + filler + " | " + peg$padEnd9("", s.column - 1, " ") + peg$padEnd9("", hatLen, "^");
    } else {
      str += "\n at " + loc;
    }
  }
  return str;
};
peg$SyntaxError9.buildMessage = function(expected, found) {
  var DESCRIBE_EXPECTATION_FNS = {
    literal: function(expectation) {
      return '"' + literalEscape(expectation.text) + '"';
    },
    class: function(expectation) {
      var escapedParts = expectation.parts.map(function(part) {
        return Array.isArray(part) ? classEscape(part[0]) + "-" + classEscape(part[1]) : classEscape(part);
      });
      return "[" + (expectation.inverted ? "^" : "") + escapedParts.join("") + "]";
    },
    any: function() {
      return "any character";
    },
    end: function() {
      return "end of input";
    },
    other: function(expectation) {
      return expectation.description;
    }
  };
  function hex(ch) {
    return ch.charCodeAt(0).toString(16).toUpperCase();
  }
  function literalEscape(s) {
    return s.replace(/\\/g, "\\\\").replace(/"/g, '\\"').replace(/\0/g, "\\0").replace(/\t/g, "\\t").replace(/\n/g, "\\n").replace(/\r/g, "\\r").replace(/[\x00-\x0F]/g, function(ch) {
      return "\\x0" + hex(ch);
    }).replace(/[\x10-\x1F\x7F-\x9F]/g, function(ch) {
      return "\\x" + hex(ch);
    });
  }
  function classEscape(s) {
    return s.replace(/\\/g, "\\\\").replace(/\]/g, "\\]").replace(/\^/g, "\\^").replace(/-/g, "\\-").replace(/\0/g, "\\0").replace(/\t/g, "\\t").replace(/\n/g, "\\n").replace(/\r/g, "\\r").replace(/[\x00-\x0F]/g, function(ch) {
      return "\\x0" + hex(ch);
    }).replace(/[\x10-\x1F\x7F-\x9F]/g, function(ch) {
      return "\\x" + hex(ch);
    });
  }
  function describeExpectation(expectation) {
    return DESCRIBE_EXPECTATION_FNS[expectation.type](expectation);
  }
  function describeExpected(expected2) {
    var descriptions = expected2.map(describeExpectation);
    var i, j;
    descriptions.sort();
    if (descriptions.length > 0) {
      for (i = 1, j = 1; i < descriptions.length; i++) {
        if (descriptions[i - 1] !== descriptions[i]) {
          descriptions[j] = descriptions[i];
          j++;
        }
      }
      descriptions.length = j;
    }
    switch (descriptions.length) {
      case 1:
        return descriptions[0];
      case 2:
        return descriptions[0] + " or " + descriptions[1];
      default:
        return descriptions.slice(0, -1).join(", ") + ", or " + descriptions[descriptions.length - 1];
    }
  }
  function describeFound(found2) {
    return found2 ? '"' + literalEscape(found2) + '"' : "end of input";
  }
  return "Expected " + describeExpected(expected) + " but " + describeFound(found) + " found.";
};
function peg$parse9(input, options) {
  options = options !== void 0 ? options : {};
  var peg$FAILED = {};
  var peg$source = options.grammarSource;
  var peg$startRuleFunctions = { Document: peg$parseDocument };
  var peg$startRuleFunction = peg$parseDocument;
  var peg$c0 = "journey";
  var peg$c1 = "---";
  var peg$c2 = "title";
  var peg$c3 = "section";
  var peg$c4 = ":";
  var peg$c5 = ",";
  var peg$c6 = "-";
  var peg$c7 = ".";
  var peg$c8 = "\r";
  var peg$c9 = "\n";
  var peg$r0 = /^[^\n]/;
  var peg$r1 = /^[^:\n]/;
  var peg$r2 = /^[^,\n]/;
  var peg$r3 = /^[0-9]/;
  var peg$r4 = /^[ \t]/;
  var peg$e0 = peg$literalExpectation("journey", true);
  var peg$e1 = peg$literalExpectation("---", false);
  var peg$e2 = peg$classExpectation(["\n"], true, false);
  var peg$e3 = peg$literalExpectation("title", true);
  var peg$e4 = peg$literalExpectation("section", true);
  var peg$e5 = peg$classExpectation([":", "\n"], true, false);
  var peg$e6 = peg$literalExpectation(":", false);
  var peg$e7 = peg$literalExpectation(",", false);
  var peg$e8 = peg$classExpectation([",", "\n"], true, false);
  var peg$e9 = peg$literalExpectation("-", false);
  var peg$e10 = peg$classExpectation([["0", "9"]], false, false);
  var peg$e11 = peg$literalExpectation(".", false);
  var peg$e12 = peg$literalExpectation("\r", false);
  var peg$e13 = peg$literalExpectation("\n", false);
  var peg$e14 = peg$anyExpectation();
  var peg$e15 = peg$classExpectation([" ", "	"], false, false);
  var peg$f0 = function(fm, stmts) {
    let title;
    const sections2 = [];
    for (const s of stmts.filter(Boolean)) {
      if (s.t === "title") title = s.v;
      else if (s.t === "section") sections2.push({ label: s.label, tasks: [] });
      else if (s.t === "task") {
        if (sections2.length === 0) sections2.push({ label: "", tasks: [] });
        sections2[sections2.length - 1].tasks.push({ label: s.label, score: s.score, actors: s.actors });
      }
    }
    return { version: "1.0", metadata: { ...fm || {}, ...title ? { title } : {} }, sections: sections2 };
  };
  var peg$f1 = function(line) {
    return line;
  };
  var peg$f2 = function(lines2) {
    const meta = {};
    for (const line of lines2) {
      const m = line.match(/^\s*(\w+)\s*:\s*(.+?)\s*$/);
      if (m) meta[m[1]] = m[2];
    }
    return meta;
  };
  var peg$f3 = function(v) {
    return { t: "title", v: v.trim() };
  };
  var peg$f4 = function(label) {
    return { t: "section", label: label.trim() };
  };
  var peg$f5 = function(label, score, actors) {
    return { t: "task", label: label.trim(), score, actors };
  };
  var peg$f6 = function(first, a) {
    return a;
  };
  var peg$f7 = function(first, rest) {
    return [first, ...rest].filter(Boolean);
  };
  var peg$f8 = function(a) {
    return a.trim();
  };
  var peg$f9 = function(n) {
    return parseFloat(n);
  };
  var peg$f10 = function() {
    return null;
  };
  var peg$currPos = options.peg$currPos | 0;
  var peg$savedPos = peg$currPos;
  var peg$posDetailsCache = [{ line: 1, column: 1 }];
  var peg$maxFailPos = peg$currPos;
  var peg$maxFailExpected = options.peg$maxFailExpected || [];
  var peg$silentFails = options.peg$silentFails | 0;
  var peg$result;
  if (options.startRule) {
    if (!(options.startRule in peg$startRuleFunctions)) {
      throw new Error(`Can't start parsing from rule "` + options.startRule + '".');
    }
    peg$startRuleFunction = peg$startRuleFunctions[options.startRule];
  }
  function text() {
    return input.substring(peg$savedPos, peg$currPos);
  }
  function offset() {
    return peg$savedPos;
  }
  function range() {
    return {
      source: peg$source,
      start: peg$savedPos,
      end: peg$currPos
    };
  }
  function location() {
    return peg$computeLocation(peg$savedPos, peg$currPos);
  }
  function expected(description, location2) {
    location2 = location2 !== void 0 ? location2 : peg$computeLocation(peg$savedPos, peg$currPos);
    throw peg$buildStructuredError(
      [peg$otherExpectation(description)],
      input.substring(peg$savedPos, peg$currPos),
      location2
    );
  }
  function error(message, location2) {
    location2 = location2 !== void 0 ? location2 : peg$computeLocation(peg$savedPos, peg$currPos);
    throw peg$buildSimpleError(message, location2);
  }
  function peg$literalExpectation(text2, ignoreCase) {
    return { type: "literal", text: text2, ignoreCase };
  }
  function peg$classExpectation(parts, inverted, ignoreCase) {
    return { type: "class", parts, inverted, ignoreCase };
  }
  function peg$anyExpectation() {
    return { type: "any" };
  }
  function peg$endExpectation() {
    return { type: "end" };
  }
  function peg$otherExpectation(description) {
    return { type: "other", description };
  }
  function peg$computePosDetails(pos) {
    var details = peg$posDetailsCache[pos];
    var p;
    if (details) {
      return details;
    } else {
      if (pos >= peg$posDetailsCache.length) {
        p = peg$posDetailsCache.length - 1;
      } else {
        p = pos;
        while (!peg$posDetailsCache[--p]) {
        }
      }
      details = peg$posDetailsCache[p];
      details = {
        line: details.line,
        column: details.column
      };
      while (p < pos) {
        if (input.charCodeAt(p) === 10) {
          details.line++;
          details.column = 1;
        } else {
          details.column++;
        }
        p++;
      }
      peg$posDetailsCache[pos] = details;
      return details;
    }
  }
  function peg$computeLocation(startPos, endPos, offset2) {
    var startPosDetails = peg$computePosDetails(startPos);
    var endPosDetails = peg$computePosDetails(endPos);
    var res = {
      source: peg$source,
      start: {
        offset: startPos,
        line: startPosDetails.line,
        column: startPosDetails.column
      },
      end: {
        offset: endPos,
        line: endPosDetails.line,
        column: endPosDetails.column
      }
    };
    if (offset2 && peg$source && typeof peg$source.offset === "function") {
      res.start = peg$source.offset(res.start);
      res.end = peg$source.offset(res.end);
    }
    return res;
  }
  function peg$fail(expected2) {
    if (peg$currPos < peg$maxFailPos) {
      return;
    }
    if (peg$currPos > peg$maxFailPos) {
      peg$maxFailPos = peg$currPos;
      peg$maxFailExpected = [];
    }
    peg$maxFailExpected.push(expected2);
  }
  function peg$buildSimpleError(message, location2) {
    return new peg$SyntaxError9(message, null, null, location2);
  }
  function peg$buildStructuredError(expected2, found, location2) {
    return new peg$SyntaxError9(
      peg$SyntaxError9.buildMessage(expected2, found),
      expected2,
      found,
      location2
    );
  }
  function peg$parseDocument() {
    var s0, s1, s2, s3, s4, s5, s6;
    s0 = peg$currPos;
    s1 = peg$parseFrontmatter();
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    s2 = peg$parse_();
    s3 = input.substr(peg$currPos, 7);
    if (s3.toLowerCase() === peg$c0) {
      peg$currPos += 7;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e0);
      }
    }
    if (s3 !== peg$FAILED) {
      s4 = peg$parseLineEnd();
      if (s4 !== peg$FAILED) {
        s5 = [];
        s6 = peg$parseStatement();
        while (s6 !== peg$FAILED) {
          s5.push(s6);
          s6 = peg$parseStatement();
        }
        s6 = peg$parse_();
        peg$savedPos = s0;
        s0 = peg$f0(s1, s5);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseFrontmatter() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 3) === peg$c1) {
      s1 = peg$c1;
      peg$currPos += 3;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e1);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$parseNL();
      if (s2 !== peg$FAILED) {
        s3 = [];
        s4 = peg$currPos;
        s5 = peg$currPos;
        peg$silentFails++;
        s6 = peg$currPos;
        if (input.substr(peg$currPos, 3) === peg$c1) {
          s7 = peg$c1;
          peg$currPos += 3;
        } else {
          s7 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e1);
          }
        }
        if (s7 !== peg$FAILED) {
          s8 = peg$parseNL();
          if (s8 !== peg$FAILED) {
            s7 = [s7, s8];
            s6 = s7;
          } else {
            peg$currPos = s6;
            s6 = peg$FAILED;
          }
        } else {
          peg$currPos = s6;
          s6 = peg$FAILED;
        }
        peg$silentFails--;
        if (s6 === peg$FAILED) {
          s5 = void 0;
        } else {
          peg$currPos = s5;
          s5 = peg$FAILED;
        }
        if (s5 !== peg$FAILED) {
          s6 = peg$currPos;
          s7 = [];
          s8 = input.charAt(peg$currPos);
          if (peg$r0.test(s8)) {
            peg$currPos++;
          } else {
            s8 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e2);
            }
          }
          while (s8 !== peg$FAILED) {
            s7.push(s8);
            s8 = input.charAt(peg$currPos);
            if (peg$r0.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e2);
              }
            }
          }
          s6 = input.substring(s6, peg$currPos);
          s7 = peg$parseNL();
          if (s7 !== peg$FAILED) {
            peg$savedPos = s4;
            s4 = peg$f1(s6);
          } else {
            peg$currPos = s4;
            s4 = peg$FAILED;
          }
        } else {
          peg$currPos = s4;
          s4 = peg$FAILED;
        }
        while (s4 !== peg$FAILED) {
          s3.push(s4);
          s4 = peg$currPos;
          s5 = peg$currPos;
          peg$silentFails++;
          s6 = peg$currPos;
          if (input.substr(peg$currPos, 3) === peg$c1) {
            s7 = peg$c1;
            peg$currPos += 3;
          } else {
            s7 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e1);
            }
          }
          if (s7 !== peg$FAILED) {
            s8 = peg$parseNL();
            if (s8 !== peg$FAILED) {
              s7 = [s7, s8];
              s6 = s7;
            } else {
              peg$currPos = s6;
              s6 = peg$FAILED;
            }
          } else {
            peg$currPos = s6;
            s6 = peg$FAILED;
          }
          peg$silentFails--;
          if (s6 === peg$FAILED) {
            s5 = void 0;
          } else {
            peg$currPos = s5;
            s5 = peg$FAILED;
          }
          if (s5 !== peg$FAILED) {
            s6 = peg$currPos;
            s7 = [];
            s8 = input.charAt(peg$currPos);
            if (peg$r0.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e2);
              }
            }
            while (s8 !== peg$FAILED) {
              s7.push(s8);
              s8 = input.charAt(peg$currPos);
              if (peg$r0.test(s8)) {
                peg$currPos++;
              } else {
                s8 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e2);
                }
              }
            }
            s6 = input.substring(s6, peg$currPos);
            s7 = peg$parseNL();
            if (s7 !== peg$FAILED) {
              peg$savedPos = s4;
              s4 = peg$f1(s6);
            } else {
              peg$currPos = s4;
              s4 = peg$FAILED;
            }
          } else {
            peg$currPos = s4;
            s4 = peg$FAILED;
          }
        }
        if (input.substr(peg$currPos, 3) === peg$c1) {
          s4 = peg$c1;
          peg$currPos += 3;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e1);
          }
        }
        if (s4 !== peg$FAILED) {
          s5 = peg$parseNL();
          if (s5 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f2(s3);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseStatement() {
    var s0;
    s0 = peg$parseTitle();
    if (s0 === peg$FAILED) {
      s0 = peg$parseSection();
      if (s0 === peg$FAILED) {
        s0 = peg$parseTask();
        if (s0 === peg$FAILED) {
          s0 = peg$parseBlankLine();
        }
      }
    }
    return s0;
  }
  function peg$parseTitle() {
    var s0, s1, s2, s3, s4, s5;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = input.substr(peg$currPos, 5);
    if (s2.toLowerCase() === peg$c2) {
      peg$currPos += 5;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e3);
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$parse_();
      s4 = peg$parseRestOfLine();
      if (s4 !== peg$FAILED) {
        s5 = peg$parseLineEnd();
        if (s5 !== peg$FAILED) {
          peg$savedPos = s0;
          s0 = peg$f3(s4);
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseSection() {
    var s0, s1, s2, s3, s4, s5;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = input.substr(peg$currPos, 7);
    if (s2.toLowerCase() === peg$c3) {
      peg$currPos += 7;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e4);
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$parse_();
      s4 = peg$parseRestOfLine();
      if (s4 !== peg$FAILED) {
        s5 = peg$parseLineEnd();
        if (s5 !== peg$FAILED) {
          peg$savedPos = s0;
          s0 = peg$f4(s4);
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseTask() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9, s10, s11;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = peg$currPos;
    s3 = [];
    s4 = input.charAt(peg$currPos);
    if (peg$r1.test(s4)) {
      peg$currPos++;
    } else {
      s4 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e5);
      }
    }
    if (s4 !== peg$FAILED) {
      while (s4 !== peg$FAILED) {
        s3.push(s4);
        s4 = input.charAt(peg$currPos);
        if (peg$r1.test(s4)) {
          peg$currPos++;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e5);
          }
        }
      }
    } else {
      s3 = peg$FAILED;
    }
    if (s3 !== peg$FAILED) {
      s2 = input.substring(s2, peg$currPos);
    } else {
      s2 = s3;
    }
    if (s2 !== peg$FAILED) {
      if (input.charCodeAt(peg$currPos) === 58) {
        s3 = peg$c4;
        peg$currPos++;
      } else {
        s3 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e6);
        }
      }
      if (s3 !== peg$FAILED) {
        s4 = peg$parse_();
        s5 = peg$parseNum();
        if (s5 !== peg$FAILED) {
          s6 = peg$parse_();
          if (input.charCodeAt(peg$currPos) === 58) {
            s7 = peg$c4;
            peg$currPos++;
          } else {
            s7 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e6);
            }
          }
          if (s7 !== peg$FAILED) {
            s8 = peg$parse_();
            s9 = peg$parseActors();
            if (s9 !== peg$FAILED) {
              s10 = peg$parse_();
              s11 = peg$parseLineEnd();
              if (s11 !== peg$FAILED) {
                peg$savedPos = s0;
                s0 = peg$f5(s2, s5, s9);
              } else {
                peg$currPos = s0;
                s0 = peg$FAILED;
              }
            } else {
              peg$currPos = s0;
              s0 = peg$FAILED;
            }
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseActors() {
    var s0, s1, s2, s3, s4, s5, s6, s7;
    s0 = peg$currPos;
    s1 = peg$parseActor();
    if (s1 !== peg$FAILED) {
      s2 = [];
      s3 = peg$currPos;
      s4 = peg$parse_();
      if (input.charCodeAt(peg$currPos) === 44) {
        s5 = peg$c5;
        peg$currPos++;
      } else {
        s5 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e7);
        }
      }
      if (s5 !== peg$FAILED) {
        s6 = peg$parse_();
        s7 = peg$parseActor();
        if (s7 !== peg$FAILED) {
          peg$savedPos = s3;
          s3 = peg$f6(s1, s7);
        } else {
          peg$currPos = s3;
          s3 = peg$FAILED;
        }
      } else {
        peg$currPos = s3;
        s3 = peg$FAILED;
      }
      while (s3 !== peg$FAILED) {
        s2.push(s3);
        s3 = peg$currPos;
        s4 = peg$parse_();
        if (input.charCodeAt(peg$currPos) === 44) {
          s5 = peg$c5;
          peg$currPos++;
        } else {
          s5 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e7);
          }
        }
        if (s5 !== peg$FAILED) {
          s6 = peg$parse_();
          s7 = peg$parseActor();
          if (s7 !== peg$FAILED) {
            peg$savedPos = s3;
            s3 = peg$f6(s1, s7);
          } else {
            peg$currPos = s3;
            s3 = peg$FAILED;
          }
        } else {
          peg$currPos = s3;
          s3 = peg$FAILED;
        }
      }
      peg$savedPos = s0;
      s0 = peg$f7(s1, s2);
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseActor() {
    var s0, s1, s2, s3;
    s0 = peg$currPos;
    s1 = peg$currPos;
    s2 = [];
    s3 = input.charAt(peg$currPos);
    if (peg$r2.test(s3)) {
      peg$currPos++;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e8);
      }
    }
    if (s3 !== peg$FAILED) {
      while (s3 !== peg$FAILED) {
        s2.push(s3);
        s3 = input.charAt(peg$currPos);
        if (peg$r2.test(s3)) {
          peg$currPos++;
        } else {
          s3 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e8);
          }
        }
      }
    } else {
      s2 = peg$FAILED;
    }
    if (s2 !== peg$FAILED) {
      s1 = input.substring(s1, peg$currPos);
    } else {
      s1 = s2;
    }
    if (s1 !== peg$FAILED) {
      peg$savedPos = s0;
      s1 = peg$f8(s1);
    }
    s0 = s1;
    return s0;
  }
  function peg$parseNum() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8;
    s0 = peg$currPos;
    s1 = peg$currPos;
    s2 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 45) {
      s3 = peg$c6;
      peg$currPos++;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e9);
      }
    }
    if (s3 === peg$FAILED) {
      s3 = null;
    }
    s4 = [];
    s5 = input.charAt(peg$currPos);
    if (peg$r3.test(s5)) {
      peg$currPos++;
    } else {
      s5 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e10);
      }
    }
    if (s5 !== peg$FAILED) {
      while (s5 !== peg$FAILED) {
        s4.push(s5);
        s5 = input.charAt(peg$currPos);
        if (peg$r3.test(s5)) {
          peg$currPos++;
        } else {
          s5 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e10);
          }
        }
      }
    } else {
      s4 = peg$FAILED;
    }
    if (s4 !== peg$FAILED) {
      s5 = peg$currPos;
      if (input.charCodeAt(peg$currPos) === 46) {
        s6 = peg$c7;
        peg$currPos++;
      } else {
        s6 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e11);
        }
      }
      if (s6 !== peg$FAILED) {
        s7 = [];
        s8 = input.charAt(peg$currPos);
        if (peg$r3.test(s8)) {
          peg$currPos++;
        } else {
          s8 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e10);
          }
        }
        if (s8 !== peg$FAILED) {
          while (s8 !== peg$FAILED) {
            s7.push(s8);
            s8 = input.charAt(peg$currPos);
            if (peg$r3.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e10);
              }
            }
          }
        } else {
          s7 = peg$FAILED;
        }
        if (s7 !== peg$FAILED) {
          s6 = [s6, s7];
          s5 = s6;
        } else {
          peg$currPos = s5;
          s5 = peg$FAILED;
        }
      } else {
        peg$currPos = s5;
        s5 = peg$FAILED;
      }
      if (s5 === peg$FAILED) {
        s5 = null;
      }
      s3 = [s3, s4, s5];
      s2 = s3;
    } else {
      peg$currPos = s2;
      s2 = peg$FAILED;
    }
    if (s2 !== peg$FAILED) {
      s1 = input.substring(s1, peg$currPos);
    } else {
      s1 = s2;
    }
    if (s1 !== peg$FAILED) {
      peg$savedPos = s0;
      s1 = peg$f9(s1);
    }
    s0 = s1;
    return s0;
  }
  function peg$parseRestOfLine() {
    var s0, s1, s2;
    s0 = peg$currPos;
    s1 = [];
    s2 = input.charAt(peg$currPos);
    if (peg$r0.test(s2)) {
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e2);
      }
    }
    if (s2 !== peg$FAILED) {
      while (s2 !== peg$FAILED) {
        s1.push(s2);
        s2 = input.charAt(peg$currPos);
        if (peg$r0.test(s2)) {
          peg$currPos++;
        } else {
          s2 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e2);
          }
        }
      }
    } else {
      s1 = peg$FAILED;
    }
    if (s1 !== peg$FAILED) {
      s0 = input.substring(s0, peg$currPos);
    } else {
      s0 = s1;
    }
    return s0;
  }
  function peg$parseNL() {
    var s0, s1, s2;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 13) {
      s1 = peg$c8;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e12);
      }
    }
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s2 = peg$c9;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e13);
      }
    }
    if (s2 !== peg$FAILED) {
      s1 = [s1, s2];
      s0 = s1;
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseBlankLine() {
    var s0, s1, s2, s3;
    s0 = peg$currPos;
    s1 = peg$parse_();
    if (input.charCodeAt(peg$currPos) === 13) {
      s2 = peg$c8;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e12);
      }
    }
    if (s2 === peg$FAILED) {
      s2 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s3 = peg$c9;
      peg$currPos++;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e13);
      }
    }
    if (s3 !== peg$FAILED) {
      peg$savedPos = s0;
      s0 = peg$f10();
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseLineEnd() {
    var s0, s1, s2;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 13) {
      s1 = peg$c8;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e12);
      }
    }
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s2 = peg$c9;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e13);
      }
    }
    if (s2 !== peg$FAILED) {
      s1 = [s1, s2];
      s0 = s1;
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      peg$silentFails++;
      if (input.length > peg$currPos) {
        s1 = input.charAt(peg$currPos);
        peg$currPos++;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e14);
        }
      }
      peg$silentFails--;
      if (s1 === peg$FAILED) {
        s0 = void 0;
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    }
    return s0;
  }
  function peg$parse_() {
    var s0, s1;
    s0 = [];
    s1 = input.charAt(peg$currPos);
    if (peg$r4.test(s1)) {
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e15);
      }
    }
    while (s1 !== peg$FAILED) {
      s0.push(s1);
      s1 = input.charAt(peg$currPos);
      if (peg$r4.test(s1)) {
        peg$currPos++;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e15);
        }
      }
    }
    return s0;
  }
  peg$result = peg$startRuleFunction();
  if (options.peg$library) {
    return (
      /** @type {any} */
      {
        peg$result,
        peg$currPos,
        peg$FAILED,
        peg$maxFailExpected,
        peg$maxFailPos
      }
    );
  }
  if (peg$result !== peg$FAILED && peg$currPos === input.length) {
    return peg$result;
  } else {
    if (peg$result !== peg$FAILED && peg$currPos < input.length) {
      peg$fail(peg$endExpectation());
    }
    throw peg$buildStructuredError(
      peg$maxFailExpected,
      peg$maxFailPos < input.length ? input.charAt(peg$maxFailPos) : null,
      peg$maxFailPos < input.length ? peg$computeLocation(peg$maxFailPos, peg$maxFailPos + 1) : peg$computeLocation(peg$maxFailPos, peg$maxFailPos)
    );
  }
}
var journey = {
  parseMermaid(input) {
    return peg$parse9(input);
  },
  parseYaml(input) {
    return JSON.parse(input);
  },
  layout(ir, theme) {
    return layoutJourney(ir, theme);
  }
};
function layoutKanban(ir, theme) {
  const { palette, typography, spacing } = theme;
  const p = pen(theme);
  const margin = spacing.diagramMargin;
  const colW = 210;
  const colGap = 18;
  const headerH = 38;
  const cardPad = 10;
  const cardGap = 10;
  const cardFont = typography.baseFontSize;
  const smallFont = typography.smallFontSize;
  const lineH = rhuInt(cardFont * 1.3);
  const cardInnerW = colW - cardPad * 2;
  const title = ir.metadata.title;
  const titleH = title ? typography.titleFontSize + 16 : 0;
  const boardTop = margin + titleH;
  const columns = ir.columns.map((col) => ({
    label: col.label,
    cards: col.cards.map((c) => {
      const info = measureFormattedText(c.text, cardFont, smallFont, cardInnerW - 2, 6);
      const h2 = Math.max(cardPad * 2 + info.lineCount * lineH, 36);
      return { info, h: h2 };
    })
  }));
  const elements = [];
  if (title) {
    elements.push(
      p.text(
        title,
        margin,
        margin + typography.titleFontSize,
        typography.titleFontSize,
        palette.text,
        { weight: "bold" }
      )
    );
  }
  let maxColH = 0;
  columns.forEach((col, ci) => {
    const x = margin + ci * (colW + colGap);
    const hue = categoricalHue(ci, theme);
    elements.push(p.rect({ x, y: boardTop, width: colW, height: headerH }, hue, hue, 0, { rx: 6 }));
    elements.push(
      p.text(
        `${col.label}  (${col.cards.length})`,
        rhuInt(x + colW / 2),
        boardTop + headerH / 2 + cardFont * 0.35,
        typography.baseFontSize,
        "#FFFFFF",
        { weight: "bold", anchor: "middle" }
      )
    );
    let cy = boardTop + headerH + cardGap;
    for (const card of col.cards) {
      elements.push(
        p.rect({ x, y: cy, width: colW, height: card.h }, palette.surface, hue, 1, { rx: 6 })
      );
      elements.push(
        ...renderFormattedText(
          p,
          card.info,
          x,
          cy,
          colW,
          card.h,
          cardFont,
          smallFont,
          palette.text,
          palette.textMuted,
          { align: "start", paddingLeft: cardPad, paddingRight: cardPad }
        )
      );
      cy += card.h + cardGap;
    }
    maxColH = Math.max(maxColH, cy - boardTop);
  });
  const totalW = rhuInt(margin + columns.length * colW + (columns.length - 1) * colGap + margin);
  const totalH = rhuInt(boardTop + maxColH + margin);
  const scene = applyOverlays(
    {
      viewBox: { x: 0, y: 0, width: totalW, height: totalH },
      background: palette.background,
      elements
    },
    ir.overlays,
    theme
  );
  return { scene, anchors: {} };
}
function peg$subclass10(child, parent) {
  function C() {
    this.constructor = child;
  }
  C.prototype = parent.prototype;
  child.prototype = new C();
}
function peg$SyntaxError10(message, expected, found, location) {
  var self = Error.call(this, message);
  if (Object.setPrototypeOf) {
    Object.setPrototypeOf(self, peg$SyntaxError10.prototype);
  }
  self.expected = expected;
  self.found = found;
  self.location = location;
  self.name = "SyntaxError";
  return self;
}
peg$subclass10(peg$SyntaxError10, Error);
function peg$padEnd10(str, targetLength, padString) {
  padString = padString || " ";
  if (str.length > targetLength) {
    return str;
  }
  targetLength -= str.length;
  padString += padString.repeat(targetLength);
  return str + padString.slice(0, targetLength);
}
peg$SyntaxError10.prototype.format = function(sources) {
  var str = "Error: " + this.message;
  if (this.location) {
    var src = null;
    var k;
    for (k = 0; k < sources.length; k++) {
      if (sources[k].source === this.location.source) {
        src = sources[k].text.split(/\r\n|\n|\r/g);
        break;
      }
    }
    var s = this.location.start;
    var offset_s = this.location.source && typeof this.location.source.offset === "function" ? this.location.source.offset(s) : s;
    var loc = this.location.source + ":" + offset_s.line + ":" + offset_s.column;
    if (src) {
      var e = this.location.end;
      var filler = peg$padEnd10("", offset_s.line.toString().length, " ");
      var line = src[s.line - 1];
      var last = s.line === e.line ? e.column : line.length + 1;
      var hatLen = last - s.column || 1;
      str += "\n --> " + loc + "\n" + filler + " |\n" + offset_s.line + " | " + line + "\n" + filler + " | " + peg$padEnd10("", s.column - 1, " ") + peg$padEnd10("", hatLen, "^");
    } else {
      str += "\n at " + loc;
    }
  }
  return str;
};
peg$SyntaxError10.buildMessage = function(expected, found) {
  var DESCRIBE_EXPECTATION_FNS = {
    literal: function(expectation) {
      return '"' + literalEscape(expectation.text) + '"';
    },
    class: function(expectation) {
      var escapedParts = expectation.parts.map(function(part) {
        return Array.isArray(part) ? classEscape(part[0]) + "-" + classEscape(part[1]) : classEscape(part);
      });
      return "[" + (expectation.inverted ? "^" : "") + escapedParts.join("") + "]";
    },
    any: function() {
      return "any character";
    },
    end: function() {
      return "end of input";
    },
    other: function(expectation) {
      return expectation.description;
    }
  };
  function hex(ch) {
    return ch.charCodeAt(0).toString(16).toUpperCase();
  }
  function literalEscape(s) {
    return s.replace(/\\/g, "\\\\").replace(/"/g, '\\"').replace(/\0/g, "\\0").replace(/\t/g, "\\t").replace(/\n/g, "\\n").replace(/\r/g, "\\r").replace(/[\x00-\x0F]/g, function(ch) {
      return "\\x0" + hex(ch);
    }).replace(/[\x10-\x1F\x7F-\x9F]/g, function(ch) {
      return "\\x" + hex(ch);
    });
  }
  function classEscape(s) {
    return s.replace(/\\/g, "\\\\").replace(/\]/g, "\\]").replace(/\^/g, "\\^").replace(/-/g, "\\-").replace(/\0/g, "\\0").replace(/\t/g, "\\t").replace(/\n/g, "\\n").replace(/\r/g, "\\r").replace(/[\x00-\x0F]/g, function(ch) {
      return "\\x0" + hex(ch);
    }).replace(/[\x10-\x1F\x7F-\x9F]/g, function(ch) {
      return "\\x" + hex(ch);
    });
  }
  function describeExpectation(expectation) {
    return DESCRIBE_EXPECTATION_FNS[expectation.type](expectation);
  }
  function describeExpected(expected2) {
    var descriptions = expected2.map(describeExpectation);
    var i, j;
    descriptions.sort();
    if (descriptions.length > 0) {
      for (i = 1, j = 1; i < descriptions.length; i++) {
        if (descriptions[i - 1] !== descriptions[i]) {
          descriptions[j] = descriptions[i];
          j++;
        }
      }
      descriptions.length = j;
    }
    switch (descriptions.length) {
      case 1:
        return descriptions[0];
      case 2:
        return descriptions[0] + " or " + descriptions[1];
      default:
        return descriptions.slice(0, -1).join(", ") + ", or " + descriptions[descriptions.length - 1];
    }
  }
  function describeFound(found2) {
    return found2 ? '"' + literalEscape(found2) + '"' : "end of input";
  }
  return "Expected " + describeExpected(expected) + " but " + describeFound(found) + " found.";
};
function peg$parse10(input, options) {
  options = options !== void 0 ? options : {};
  var peg$FAILED = {};
  var peg$source = options.grammarSource;
  var peg$startRuleFunctions = { Document: peg$parseDocument };
  var peg$startRuleFunction = peg$parseDocument;
  var peg$c0 = "kanban";
  var peg$c1 = "---";
  var peg$c2 = "[";
  var peg$c3 = "]";
  var peg$c4 = "\r";
  var peg$c5 = "\n";
  var peg$r0 = /^[^\n]/;
  var peg$r1 = /^[^\]\n]/;
  var peg$r2 = /^[a-zA-Z0-9_\-]/;
  var peg$r3 = /^[ \t]/;
  var peg$e0 = peg$literalExpectation("kanban", true);
  var peg$e1 = peg$literalExpectation("---", false);
  var peg$e2 = peg$classExpectation(["\n"], true, false);
  var peg$e3 = peg$literalExpectation("[", false);
  var peg$e4 = peg$classExpectation(["]", "\n"], true, false);
  var peg$e5 = peg$literalExpectation("]", false);
  var peg$e6 = peg$classExpectation([["a", "z"], ["A", "Z"], ["0", "9"], "_", "-"], false, false);
  var peg$e7 = peg$literalExpectation("\r", false);
  var peg$e8 = peg$literalExpectation("\n", false);
  var peg$e9 = peg$anyExpectation();
  var peg$e10 = peg$classExpectation([" ", "	"], false, false);
  var peg$f0 = function(fm, rows) {
    const columns = [];
    for (const r of rows.filter(Boolean)) {
      if (r.t === "col") columns.push({ label: r.label, cards: [] });
      else if (r.t === "card") {
        if (columns.length === 0) columns.push({ label: "", cards: [] });
        columns[columns.length - 1].cards.push({ ...r.id ? { id: r.id } : {}, text: r.text });
      }
    }
    return { version: "1.0", metadata: { ...fm || {} }, columns };
  };
  var peg$f1 = function(line) {
    return line;
  };
  var peg$f2 = function(lines2) {
    const meta = {};
    for (const line of lines2) {
      const m = line.match(/^\s*(\w+)\s*:\s*(.+?)\s*$/);
      if (m) meta[m[1]] = m[2];
    }
    return meta;
  };
  var peg$f3 = function(id, text2) {
    return { t: "card", id: id || void 0, text: text2.trim() };
  };
  var peg$f4 = function(label) {
    return { t: "col", label: label.trim() };
  };
  var peg$f5 = function() {
    return null;
  };
  var peg$currPos = options.peg$currPos | 0;
  var peg$savedPos = peg$currPos;
  var peg$posDetailsCache = [{ line: 1, column: 1 }];
  var peg$maxFailPos = peg$currPos;
  var peg$maxFailExpected = options.peg$maxFailExpected || [];
  var peg$silentFails = options.peg$silentFails | 0;
  var peg$result;
  if (options.startRule) {
    if (!(options.startRule in peg$startRuleFunctions)) {
      throw new Error(`Can't start parsing from rule "` + options.startRule + '".');
    }
    peg$startRuleFunction = peg$startRuleFunctions[options.startRule];
  }
  function text() {
    return input.substring(peg$savedPos, peg$currPos);
  }
  function offset() {
    return peg$savedPos;
  }
  function range() {
    return {
      source: peg$source,
      start: peg$savedPos,
      end: peg$currPos
    };
  }
  function location() {
    return peg$computeLocation(peg$savedPos, peg$currPos);
  }
  function expected(description, location2) {
    location2 = location2 !== void 0 ? location2 : peg$computeLocation(peg$savedPos, peg$currPos);
    throw peg$buildStructuredError(
      [peg$otherExpectation(description)],
      input.substring(peg$savedPos, peg$currPos),
      location2
    );
  }
  function error(message, location2) {
    location2 = location2 !== void 0 ? location2 : peg$computeLocation(peg$savedPos, peg$currPos);
    throw peg$buildSimpleError(message, location2);
  }
  function peg$literalExpectation(text2, ignoreCase) {
    return { type: "literal", text: text2, ignoreCase };
  }
  function peg$classExpectation(parts, inverted, ignoreCase) {
    return { type: "class", parts, inverted, ignoreCase };
  }
  function peg$anyExpectation() {
    return { type: "any" };
  }
  function peg$endExpectation() {
    return { type: "end" };
  }
  function peg$otherExpectation(description) {
    return { type: "other", description };
  }
  function peg$computePosDetails(pos) {
    var details = peg$posDetailsCache[pos];
    var p;
    if (details) {
      return details;
    } else {
      if (pos >= peg$posDetailsCache.length) {
        p = peg$posDetailsCache.length - 1;
      } else {
        p = pos;
        while (!peg$posDetailsCache[--p]) {
        }
      }
      details = peg$posDetailsCache[p];
      details = {
        line: details.line,
        column: details.column
      };
      while (p < pos) {
        if (input.charCodeAt(p) === 10) {
          details.line++;
          details.column = 1;
        } else {
          details.column++;
        }
        p++;
      }
      peg$posDetailsCache[pos] = details;
      return details;
    }
  }
  function peg$computeLocation(startPos, endPos, offset2) {
    var startPosDetails = peg$computePosDetails(startPos);
    var endPosDetails = peg$computePosDetails(endPos);
    var res = {
      source: peg$source,
      start: {
        offset: startPos,
        line: startPosDetails.line,
        column: startPosDetails.column
      },
      end: {
        offset: endPos,
        line: endPosDetails.line,
        column: endPosDetails.column
      }
    };
    if (offset2 && peg$source && typeof peg$source.offset === "function") {
      res.start = peg$source.offset(res.start);
      res.end = peg$source.offset(res.end);
    }
    return res;
  }
  function peg$fail(expected2) {
    if (peg$currPos < peg$maxFailPos) {
      return;
    }
    if (peg$currPos > peg$maxFailPos) {
      peg$maxFailPos = peg$currPos;
      peg$maxFailExpected = [];
    }
    peg$maxFailExpected.push(expected2);
  }
  function peg$buildSimpleError(message, location2) {
    return new peg$SyntaxError10(message, null, null, location2);
  }
  function peg$buildStructuredError(expected2, found, location2) {
    return new peg$SyntaxError10(
      peg$SyntaxError10.buildMessage(expected2, found),
      expected2,
      found,
      location2
    );
  }
  function peg$parseDocument() {
    var s0, s1, s2, s3, s4, s5, s6;
    s0 = peg$currPos;
    s1 = peg$parseFrontmatter();
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    s2 = peg$parse_();
    s3 = input.substr(peg$currPos, 6);
    if (s3.toLowerCase() === peg$c0) {
      peg$currPos += 6;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e0);
      }
    }
    if (s3 !== peg$FAILED) {
      s4 = peg$parseLineEnd();
      if (s4 !== peg$FAILED) {
        s5 = [];
        s6 = peg$parseRow();
        while (s6 !== peg$FAILED) {
          s5.push(s6);
          s6 = peg$parseRow();
        }
        s6 = peg$parse_();
        peg$savedPos = s0;
        s0 = peg$f0(s1, s5);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseFrontmatter() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 3) === peg$c1) {
      s1 = peg$c1;
      peg$currPos += 3;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e1);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$parseNL();
      if (s2 !== peg$FAILED) {
        s3 = [];
        s4 = peg$currPos;
        s5 = peg$currPos;
        peg$silentFails++;
        s6 = peg$currPos;
        if (input.substr(peg$currPos, 3) === peg$c1) {
          s7 = peg$c1;
          peg$currPos += 3;
        } else {
          s7 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e1);
          }
        }
        if (s7 !== peg$FAILED) {
          s8 = peg$parseNL();
          if (s8 !== peg$FAILED) {
            s7 = [s7, s8];
            s6 = s7;
          } else {
            peg$currPos = s6;
            s6 = peg$FAILED;
          }
        } else {
          peg$currPos = s6;
          s6 = peg$FAILED;
        }
        peg$silentFails--;
        if (s6 === peg$FAILED) {
          s5 = void 0;
        } else {
          peg$currPos = s5;
          s5 = peg$FAILED;
        }
        if (s5 !== peg$FAILED) {
          s6 = peg$currPos;
          s7 = [];
          s8 = input.charAt(peg$currPos);
          if (peg$r0.test(s8)) {
            peg$currPos++;
          } else {
            s8 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e2);
            }
          }
          while (s8 !== peg$FAILED) {
            s7.push(s8);
            s8 = input.charAt(peg$currPos);
            if (peg$r0.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e2);
              }
            }
          }
          s6 = input.substring(s6, peg$currPos);
          s7 = peg$parseNL();
          if (s7 !== peg$FAILED) {
            peg$savedPos = s4;
            s4 = peg$f1(s6);
          } else {
            peg$currPos = s4;
            s4 = peg$FAILED;
          }
        } else {
          peg$currPos = s4;
          s4 = peg$FAILED;
        }
        while (s4 !== peg$FAILED) {
          s3.push(s4);
          s4 = peg$currPos;
          s5 = peg$currPos;
          peg$silentFails++;
          s6 = peg$currPos;
          if (input.substr(peg$currPos, 3) === peg$c1) {
            s7 = peg$c1;
            peg$currPos += 3;
          } else {
            s7 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e1);
            }
          }
          if (s7 !== peg$FAILED) {
            s8 = peg$parseNL();
            if (s8 !== peg$FAILED) {
              s7 = [s7, s8];
              s6 = s7;
            } else {
              peg$currPos = s6;
              s6 = peg$FAILED;
            }
          } else {
            peg$currPos = s6;
            s6 = peg$FAILED;
          }
          peg$silentFails--;
          if (s6 === peg$FAILED) {
            s5 = void 0;
          } else {
            peg$currPos = s5;
            s5 = peg$FAILED;
          }
          if (s5 !== peg$FAILED) {
            s6 = peg$currPos;
            s7 = [];
            s8 = input.charAt(peg$currPos);
            if (peg$r0.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e2);
              }
            }
            while (s8 !== peg$FAILED) {
              s7.push(s8);
              s8 = input.charAt(peg$currPos);
              if (peg$r0.test(s8)) {
                peg$currPos++;
              } else {
                s8 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e2);
                }
              }
            }
            s6 = input.substring(s6, peg$currPos);
            s7 = peg$parseNL();
            if (s7 !== peg$FAILED) {
              peg$savedPos = s4;
              s4 = peg$f1(s6);
            } else {
              peg$currPos = s4;
              s4 = peg$FAILED;
            }
          } else {
            peg$currPos = s4;
            s4 = peg$FAILED;
          }
        }
        if (input.substr(peg$currPos, 3) === peg$c1) {
          s4 = peg$c1;
          peg$currPos += 3;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e1);
          }
        }
        if (s4 !== peg$FAILED) {
          s5 = peg$parseNL();
          if (s5 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f2(s3);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseRow() {
    var s0;
    s0 = peg$parseCard();
    if (s0 === peg$FAILED) {
      s0 = peg$parseColumn();
      if (s0 === peg$FAILED) {
        s0 = peg$parseBlankLine();
      }
    }
    return s0;
  }
  function peg$parseCard() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = peg$parseIdent();
    if (s2 === peg$FAILED) {
      s2 = null;
    }
    if (input.charCodeAt(peg$currPos) === 91) {
      s3 = peg$c2;
      peg$currPos++;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e3);
      }
    }
    if (s3 !== peg$FAILED) {
      s4 = peg$currPos;
      s5 = [];
      s6 = input.charAt(peg$currPos);
      if (peg$r1.test(s6)) {
        peg$currPos++;
      } else {
        s6 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e4);
        }
      }
      if (s6 !== peg$FAILED) {
        while (s6 !== peg$FAILED) {
          s5.push(s6);
          s6 = input.charAt(peg$currPos);
          if (peg$r1.test(s6)) {
            peg$currPos++;
          } else {
            s6 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e4);
            }
          }
        }
      } else {
        s5 = peg$FAILED;
      }
      if (s5 !== peg$FAILED) {
        s4 = input.substring(s4, peg$currPos);
      } else {
        s4 = s5;
      }
      if (s4 !== peg$FAILED) {
        if (input.charCodeAt(peg$currPos) === 93) {
          s5 = peg$c3;
          peg$currPos++;
        } else {
          s5 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e5);
          }
        }
        if (s5 !== peg$FAILED) {
          s6 = peg$currPos;
          s7 = [];
          s8 = input.charAt(peg$currPos);
          if (peg$r0.test(s8)) {
            peg$currPos++;
          } else {
            s8 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e2);
            }
          }
          while (s8 !== peg$FAILED) {
            s7.push(s8);
            s8 = input.charAt(peg$currPos);
            if (peg$r0.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e2);
              }
            }
          }
          s6 = input.substring(s6, peg$currPos);
          s7 = peg$parseLineEnd();
          if (s7 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f3(s2, s4);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseColumn() {
    var s0, s1, s2, s3, s4;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = peg$currPos;
    s3 = [];
    s4 = input.charAt(peg$currPos);
    if (peg$r0.test(s4)) {
      peg$currPos++;
    } else {
      s4 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e2);
      }
    }
    if (s4 !== peg$FAILED) {
      while (s4 !== peg$FAILED) {
        s3.push(s4);
        s4 = input.charAt(peg$currPos);
        if (peg$r0.test(s4)) {
          peg$currPos++;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e2);
          }
        }
      }
    } else {
      s3 = peg$FAILED;
    }
    if (s3 !== peg$FAILED) {
      s2 = input.substring(s2, peg$currPos);
    } else {
      s2 = s3;
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$parseLineEnd();
      if (s3 !== peg$FAILED) {
        peg$savedPos = s0;
        s0 = peg$f4(s2);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseIdent() {
    var s0, s1, s2;
    s0 = peg$currPos;
    s1 = [];
    s2 = input.charAt(peg$currPos);
    if (peg$r2.test(s2)) {
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e6);
      }
    }
    if (s2 !== peg$FAILED) {
      while (s2 !== peg$FAILED) {
        s1.push(s2);
        s2 = input.charAt(peg$currPos);
        if (peg$r2.test(s2)) {
          peg$currPos++;
        } else {
          s2 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e6);
          }
        }
      }
    } else {
      s1 = peg$FAILED;
    }
    if (s1 !== peg$FAILED) {
      s0 = input.substring(s0, peg$currPos);
    } else {
      s0 = s1;
    }
    return s0;
  }
  function peg$parseNL() {
    var s0, s1, s2;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 13) {
      s1 = peg$c4;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e7);
      }
    }
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s2 = peg$c5;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e8);
      }
    }
    if (s2 !== peg$FAILED) {
      s1 = [s1, s2];
      s0 = s1;
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseBlankLine() {
    var s0, s1, s2, s3;
    s0 = peg$currPos;
    s1 = peg$parse_();
    if (input.charCodeAt(peg$currPos) === 13) {
      s2 = peg$c4;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e7);
      }
    }
    if (s2 === peg$FAILED) {
      s2 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s3 = peg$c5;
      peg$currPos++;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e8);
      }
    }
    if (s3 !== peg$FAILED) {
      peg$savedPos = s0;
      s0 = peg$f5();
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseLineEnd() {
    var s0, s1, s2;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 13) {
      s1 = peg$c4;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e7);
      }
    }
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s2 = peg$c5;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e8);
      }
    }
    if (s2 !== peg$FAILED) {
      s1 = [s1, s2];
      s0 = s1;
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      peg$silentFails++;
      if (input.length > peg$currPos) {
        s1 = input.charAt(peg$currPos);
        peg$currPos++;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e9);
        }
      }
      peg$silentFails--;
      if (s1 === peg$FAILED) {
        s0 = void 0;
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    }
    return s0;
  }
  function peg$parse_() {
    var s0, s1;
    s0 = [];
    s1 = input.charAt(peg$currPos);
    if (peg$r3.test(s1)) {
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e10);
      }
    }
    while (s1 !== peg$FAILED) {
      s0.push(s1);
      s1 = input.charAt(peg$currPos);
      if (peg$r3.test(s1)) {
        peg$currPos++;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e10);
        }
      }
    }
    return s0;
  }
  peg$result = peg$startRuleFunction();
  if (options.peg$library) {
    return (
      /** @type {any} */
      {
        peg$result,
        peg$currPos,
        peg$FAILED,
        peg$maxFailExpected,
        peg$maxFailPos
      }
    );
  }
  if (peg$result !== peg$FAILED && peg$currPos === input.length) {
    return peg$result;
  } else {
    if (peg$result !== peg$FAILED && peg$currPos < input.length) {
      peg$fail(peg$endExpectation());
    }
    throw peg$buildStructuredError(
      peg$maxFailExpected,
      peg$maxFailPos < input.length ? input.charAt(peg$maxFailPos) : null,
      peg$maxFailPos < input.length ? peg$computeLocation(peg$maxFailPos, peg$maxFailPos + 1) : peg$computeLocation(peg$maxFailPos, peg$maxFailPos)
    );
  }
}
var kanban = {
  parseMermaid(input) {
    return peg$parse10(input);
  },
  parseYaml(input) {
    return JSON.parse(input);
  },
  layout(ir, theme) {
    return layoutKanban(ir, theme);
  }
};
var ARROW_ID3 = "seq-arrow";
var OPEN_ID = "seq-open";
function layoutSequence(ir, theme) {
  const { palette, typography, spacing, edges: edges2 } = theme;
  const p = pen(theme);
  const margin = spacing.diagramMargin;
  const parts = ir.participants;
  const idx = new Map(parts.map((pt, i) => [pt.id, i]));
  const headFont = typography.baseFontSize;
  const headPadX = 14;
  const headH = 34;
  const colW = Math.max(
    110,
    ...parts.map(
      (pt) => measureText(pt.label, headFont).width + headPadX * 2 + (pt.isActor ? 8 : 0)
    )
  );
  const colGap = 60;
  const colPitch = colW + colGap;
  const colX = (i) => margin + i * colPitch + colW / 2;
  const partX = (id) => colX(idx.get(id) ?? 0);
  const titleH = ir.metadata.title ? typography.titleFontSize + 14 : 0;
  const headTop = margin + titleH;
  const bodyTop = headTop + headH + 26;
  const rowGap = 46;
  const elements = [];
  const lifelineEls = [];
  if (ir.metadata.title) {
    elements.push(
      p.text(
        ir.metadata.title,
        margin,
        margin + typography.titleFontSize,
        typography.titleFontSize,
        palette.text,
        { weight: "bold" }
      )
    );
  }
  const fragStack = [];
  const fragBoxes = [];
  const activations = /* @__PURE__ */ new Map();
  const actBars = [];
  let y = bodyTop;
  let msgNo = 0;
  const minX = colX(0);
  const maxX = colX(Math.max(parts.length - 1, 0));
  const pushAct = (id) => {
    (activations.get(id) ?? activations.set(id, []).get(id)).push(y);
  };
  const popAct = (id) => {
    const st = activations.get(id);
    if (st && st.length) {
      const y0 = st.pop();
      actBars.push({ x: partX(id), y0, y1: y });
    }
  };
  for (const ev of ir.events) {
    if (ev.kind === "message") {
      const m = ev;
      const x1 = partX(m.from), x2 = partX(m.to);
      const self = m.from === m.to;
      const label = ir.autonumber ? `${++msgNo}. ${m.text}` : m.text;
      const dash = m.line === "dashed" ? "5 4" : void 0;
      const markerEnd = m.head === "arrow" ? ARROW_ID3 : m.head === "open" || m.head === "async" ? OPEN_ID : void 0;
      if (m.activateTarget) pushAct(m.to);
      if (self) {
        const r = 26;
        elements.push(p.text(label, x1 + 12, y - 6, typography.smallFontSize, palette.text));
        elements.push(
          p.path(
            `M ${x1} ${rhu(y)} L ${x1 + r} ${rhu(y)} L ${x1 + r} ${rhu(y + 22)} L ${x1} ${rhu(y + 22)}`,
            palette.text,
            1.3,
            { ...dash ? { dash } : {}, ...markerEnd ? { markerEnd } : {} }
          )
        );
        y += 22 + rowGap;
      } else {
        const dir = x2 >= x1 ? 1 : -1;
        elements.push(
          p.text(label, rhuInt((x1 + x2) / 2), y - 7, typography.smallFontSize, palette.text, {
            anchor: "middle"
          })
        );
        if (m.head === "cross") {
          elements.push(
            p.path(
              `M ${rhu(x1)} ${rhu(y)} L ${rhu(x2 - dir * 6)} ${rhu(y)}`,
              palette.text,
              1.3,
              dash ? { dash } : {}
            )
          );
          const cxp = x2 - dir * 6;
          elements.push(
            p.path(
              `M ${rhu(cxp - 5)} ${rhu(y - 5)} L ${rhu(cxp + 5)} ${rhu(y + 5)} M ${rhu(cxp - 5)} ${rhu(y + 5)} L ${rhu(cxp + 5)} ${rhu(y - 5)}`,
              palette.text,
              1.5
            )
          );
        } else {
          elements.push(
            p.path(`M ${rhu(x1)} ${rhu(y)} L ${rhu(x2)} ${rhu(y)}`, palette.text, 1.3, {
              ...dash ? { dash } : {},
              ...markerEnd ? { markerEnd } : {}
            })
          );
        }
        y += rowGap;
      }
      if (m.deactivateSource) popAct(m.from);
    } else if (ev.kind === "note") {
      const note = ev;
      const xs = note.participants.map(partX);
      const c = xs.reduce((s2, v) => s2 + v, 0) / xs.length;
      const w = Math.max(
        measureText(note.text, typography.smallFontSize).width + 24,
        Math.max(...xs) - Math.min(...xs) + colW * 0.8
      );
      const nx = rhu(c - w / 2);
      elements.push(
        p.rect(
          { x: nx, y: rhu(y - 6), width: rhu(w), height: 30 },
          palette.warning + "22",
          palette.warning,
          1,
          { rx: 4 }
        )
      );
      elements.push(
        p.text(note.text, rhuInt(c), y + 13, typography.smallFontSize, palette.text, {
          anchor: "middle"
        })
      );
      y += 30 + rowGap - 10;
    } else if (ev.kind === "frag-start") {
      fragStack.push({
        type: ev.type,
        label: ev.label,
        top: y - 24,
        left: minX,
        right: maxX,
        elses: []
      });
      y += 14;
    } else if (ev.kind === "frag-else") {
      const f2 = fragStack[fragStack.length - 1];
      if (f2) f2.elses.push({ y: y - 18, label: ev.label });
      y += 10;
    } else if (ev.kind === "frag-end") {
      const f2 = fragStack.pop();
      if (f2) {
        f2.right = maxX;
        fragBoxes.push({
          ...f2,
          top: f2.top,
          left: minX - colW / 2 - 16,
          right: maxX + colW / 2 + 16
        });
      }
      y += 6;
    }
  }
  for (const [id, st] of activations)
    while (st.length) {
      const y0 = st.pop();
      actBars.push({ x: partX(id), y0, y1: y });
    }
  const bodyBottom = y + 6;
  const fragEls = [];
  for (const f2 of fragBoxes) {
    const top = f2.top, left = f2.left, right = f2.right;
    fragEls.push(
      p.rect(
        { x: rhu(left), y: rhu(top), width: rhu(right - left), height: rhu(bodyBottom - top) },
        "none",
        palette.border,
        1.5,
        { rx: 4 }
      )
    );
    const tabW = measureText(f2.type.toUpperCase(), typography.smallFontSize).width + 16;
    const tabFill = palette.surface;
    const tabTextColor = readableText(tabFill, theme);
    fragEls.push(
      p.rect(
        { x: rhu(left), y: rhu(top), width: rhu(tabW), height: 18 },
        tabFill,
        palette.border,
        1.5,
        { rx: 3 }
      )
    );
    fragEls.push(
      p.text(
        f2.type.toUpperCase(),
        rhu(left + 8),
        rhu(top + 13),
        typography.smallFontSize,
        tabTextColor,
        { weight: "bold" }
      )
    );
    if (f2.label)
      fragEls.push(
        p.text(
          `[${f2.label}]`,
          rhu(left + tabW + 8),
          rhu(top + 13),
          typography.smallFontSize,
          palette.text,
          { weight: "bold" }
        )
      );
    for (const e of f2.elses) {
      fragEls.push(
        p.path(`M ${rhu(left)} ${rhu(e.y)} L ${rhu(right)} ${rhu(e.y)}`, palette.border, 1.2, {
          dash: "5 4"
        })
      );
      if (e.label)
        fragEls.push(
          p.text(
            `[${e.label}]`,
            rhu(left + 8),
            rhu(e.y + 13),
            typography.smallFontSize,
            palette.text
          )
        );
    }
  }
  const actEls = actBars.map(
    (b) => p.rect(
      { x: rhu(b.x - 5), y: rhu(b.y0), width: 10, height: rhu(Math.max(8, b.y1 - b.y0)) },
      palette.surface,
      palette.primary,
      1,
      { rx: 1 }
    )
  );
  parts.forEach((pt, i) => {
    const x = colX(i);
    lifelineEls.push(
      p.path(
        `M ${rhu(x)} ${rhu(headTop + headH)} L ${rhu(x)} ${rhu(bodyBottom)}`,
        palette.border,
        1.5,
        { dash: "4 4" }
      )
    );
    const hx = rhu(x - colW / 2);
    const fill = pt.isActor ? palette.primary : palette.surface;
    const txtFill = pt.isActor ? readableText(palette.primary, theme) : palette.text;
    elements.push(
      p.rect(
        { x: hx, y: rhu(headTop), width: rhu(colW), height: headH },
        fill,
        palette.border,
        1.5,
        { rx: 6 }
      )
    );
    elements.push(
      p.text(pt.label, rhuInt(x), headTop + headH / 2 + headFont * 0.35, headFont, txtFill, {
        weight: "bold",
        anchor: "middle"
      })
    );
  });
  const ordered = [...lifelineEls, ...fragEls, ...actEls, ...elements];
  const totalW = rhuInt(maxX + colW / 2 + margin);
  const totalH = rhuInt(bodyBottom + margin);
  const s = edges2?.arrowSize ?? 8;
  const sH = rhu(s * 0.7);
  const sMidY = rhu(s * 0.35);
  const sRefX = rhu(s - 1);
  const openW = s + 1;
  const openH = rhu(s * 0.8);
  const openMidY = rhu(openH / 2);
  const defs = [
    `<marker id="${ARROW_ID3}" markerWidth="${s}" markerHeight="${sH}" refX="${sRefX}" refY="${sMidY}" orient="auto"><polygon points="0 0, ${s} ${sMidY}, 0 ${sH}" fill="${palette.text}" /></marker>`,
    `<marker id="${OPEN_ID}" markerWidth="${openW}" markerHeight="${openH}" refX="${s}" refY="${openMidY}" orient="auto"><polyline points="0 0, ${s} ${openMidY}, 0 ${openH}" fill="none" stroke="${palette.text}" stroke-width="1.3" /></marker>`
  ];
  const scene = applyOverlays(
    {
      viewBox: { x: 0, y: 0, width: totalW, height: totalH },
      background: palette.background,
      elements: ordered,
      defs
    },
    ir.overlays,
    theme
  );
  return { scene, anchors: {} };
}
function arrowStyle(a) {
  const line = a.startsWith("--") ? "dashed" : "solid";
  let head = "open";
  if (a.endsWith("x")) head = "cross";
  else if (a.endsWith(")")) head = "async";
  else if (a.endsWith(">>")) head = "arrow";
  else if (a.endsWith(">")) head = "open";
  return { line, head };
}
function peg$subclass11(child, parent) {
  function C() {
    this.constructor = child;
  }
  C.prototype = parent.prototype;
  child.prototype = new C();
}
function peg$SyntaxError11(message, expected, found, location) {
  var self = Error.call(this, message);
  if (Object.setPrototypeOf) {
    Object.setPrototypeOf(self, peg$SyntaxError11.prototype);
  }
  self.expected = expected;
  self.found = found;
  self.location = location;
  self.name = "SyntaxError";
  return self;
}
peg$subclass11(peg$SyntaxError11, Error);
function peg$padEnd11(str, targetLength, padString) {
  padString = padString || " ";
  if (str.length > targetLength) {
    return str;
  }
  targetLength -= str.length;
  padString += padString.repeat(targetLength);
  return str + padString.slice(0, targetLength);
}
peg$SyntaxError11.prototype.format = function(sources) {
  var str = "Error: " + this.message;
  if (this.location) {
    var src = null;
    var k;
    for (k = 0; k < sources.length; k++) {
      if (sources[k].source === this.location.source) {
        src = sources[k].text.split(/\r\n|\n|\r/g);
        break;
      }
    }
    var s = this.location.start;
    var offset_s = this.location.source && typeof this.location.source.offset === "function" ? this.location.source.offset(s) : s;
    var loc = this.location.source + ":" + offset_s.line + ":" + offset_s.column;
    if (src) {
      var e = this.location.end;
      var filler = peg$padEnd11("", offset_s.line.toString().length, " ");
      var line = src[s.line - 1];
      var last = s.line === e.line ? e.column : line.length + 1;
      var hatLen = last - s.column || 1;
      str += "\n --> " + loc + "\n" + filler + " |\n" + offset_s.line + " | " + line + "\n" + filler + " | " + peg$padEnd11("", s.column - 1, " ") + peg$padEnd11("", hatLen, "^");
    } else {
      str += "\n at " + loc;
    }
  }
  return str;
};
peg$SyntaxError11.buildMessage = function(expected, found) {
  var DESCRIBE_EXPECTATION_FNS = {
    literal: function(expectation) {
      return '"' + literalEscape(expectation.text) + '"';
    },
    class: function(expectation) {
      var escapedParts = expectation.parts.map(function(part) {
        return Array.isArray(part) ? classEscape(part[0]) + "-" + classEscape(part[1]) : classEscape(part);
      });
      return "[" + (expectation.inverted ? "^" : "") + escapedParts.join("") + "]";
    },
    any: function() {
      return "any character";
    },
    end: function() {
      return "end of input";
    },
    other: function(expectation) {
      return expectation.description;
    }
  };
  function hex(ch) {
    return ch.charCodeAt(0).toString(16).toUpperCase();
  }
  function literalEscape(s) {
    return s.replace(/\\/g, "\\\\").replace(/"/g, '\\"').replace(/\0/g, "\\0").replace(/\t/g, "\\t").replace(/\n/g, "\\n").replace(/\r/g, "\\r").replace(/[\x00-\x0F]/g, function(ch) {
      return "\\x0" + hex(ch);
    }).replace(/[\x10-\x1F\x7F-\x9F]/g, function(ch) {
      return "\\x" + hex(ch);
    });
  }
  function classEscape(s) {
    return s.replace(/\\/g, "\\\\").replace(/\]/g, "\\]").replace(/\^/g, "\\^").replace(/-/g, "\\-").replace(/\0/g, "\\0").replace(/\t/g, "\\t").replace(/\n/g, "\\n").replace(/\r/g, "\\r").replace(/[\x00-\x0F]/g, function(ch) {
      return "\\x0" + hex(ch);
    }).replace(/[\x10-\x1F\x7F-\x9F]/g, function(ch) {
      return "\\x" + hex(ch);
    });
  }
  function describeExpectation(expectation) {
    return DESCRIBE_EXPECTATION_FNS[expectation.type](expectation);
  }
  function describeExpected(expected2) {
    var descriptions = expected2.map(describeExpectation);
    var i, j;
    descriptions.sort();
    if (descriptions.length > 0) {
      for (i = 1, j = 1; i < descriptions.length; i++) {
        if (descriptions[i - 1] !== descriptions[i]) {
          descriptions[j] = descriptions[i];
          j++;
        }
      }
      descriptions.length = j;
    }
    switch (descriptions.length) {
      case 1:
        return descriptions[0];
      case 2:
        return descriptions[0] + " or " + descriptions[1];
      default:
        return descriptions.slice(0, -1).join(", ") + ", or " + descriptions[descriptions.length - 1];
    }
  }
  function describeFound(found2) {
    return found2 ? '"' + literalEscape(found2) + '"' : "end of input";
  }
  return "Expected " + describeExpected(expected) + " but " + describeFound(found) + " found.";
};
function peg$parse11(input, options) {
  options = options !== void 0 ? options : {};
  var peg$FAILED = {};
  var peg$source = options.grammarSource;
  var peg$startRuleFunctions = { Document: peg$parseDocument };
  var peg$startRuleFunction = peg$parseDocument;
  var peg$c0 = "sequencediagram";
  var peg$c1 = "---";
  var peg$c2 = "autonumber";
  var peg$c3 = "participant";
  var peg$c4 = "actor";
  var peg$c5 = "as";
  var peg$c6 = "note";
  var peg$c7 = ":";
  var peg$c8 = "over";
  var peg$c9 = "left of";
  var peg$c10 = "right of";
  var peg$c11 = ",";
  var peg$c12 = "alt";
  var peg$c13 = "opt";
  var peg$c14 = "loop";
  var peg$c15 = "par";
  var peg$c16 = "critical";
  var peg$c17 = "break";
  var peg$c18 = "else";
  var peg$c19 = "and";
  var peg$c20 = "end";
  var peg$c21 = "activate";
  var peg$c22 = "deactivate";
  var peg$c23 = "-->>";
  var peg$c24 = "->>";
  var peg$c25 = "--x";
  var peg$c26 = "-x";
  var peg$c27 = "--)";
  var peg$c28 = "-)";
  var peg$c29 = "-->";
  var peg$c30 = "->";
  var peg$c31 = "\r";
  var peg$c32 = "\n";
  var peg$r0 = /^[^\n]/;
  var peg$r1 = /^[+\-]/;
  var peg$r2 = /^[A-Za-z0-9_]/;
  var peg$r3 = /^[ \t]/;
  var peg$e0 = peg$literalExpectation("sequenceDiagram", true);
  var peg$e1 = peg$literalExpectation("---", false);
  var peg$e2 = peg$classExpectation(["\n"], true, false);
  var peg$e3 = peg$literalExpectation("autonumber", true);
  var peg$e4 = peg$literalExpectation("participant", true);
  var peg$e5 = peg$literalExpectation("actor", true);
  var peg$e6 = peg$literalExpectation("as", true);
  var peg$e7 = peg$literalExpectation("Note", true);
  var peg$e8 = peg$literalExpectation(":", false);
  var peg$e9 = peg$literalExpectation("over", true);
  var peg$e10 = peg$literalExpectation("left of", true);
  var peg$e11 = peg$literalExpectation("right of", true);
  var peg$e12 = peg$literalExpectation(",", false);
  var peg$e13 = peg$literalExpectation("alt", true);
  var peg$e14 = peg$literalExpectation("opt", true);
  var peg$e15 = peg$literalExpectation("loop", true);
  var peg$e16 = peg$literalExpectation("par", true);
  var peg$e17 = peg$literalExpectation("critical", true);
  var peg$e18 = peg$literalExpectation("break", true);
  var peg$e19 = peg$literalExpectation("else", true);
  var peg$e20 = peg$literalExpectation("and", true);
  var peg$e21 = peg$literalExpectation("end", true);
  var peg$e22 = peg$literalExpectation("activate", true);
  var peg$e23 = peg$literalExpectation("deactivate", true);
  var peg$e24 = peg$classExpectation(["+", "-"], false, false);
  var peg$e25 = peg$literalExpectation("-->>", false);
  var peg$e26 = peg$literalExpectation("->>", false);
  var peg$e27 = peg$literalExpectation("--x", false);
  var peg$e28 = peg$literalExpectation("-x", false);
  var peg$e29 = peg$literalExpectation("--)", false);
  var peg$e30 = peg$literalExpectation("-)", false);
  var peg$e31 = peg$literalExpectation("-->", false);
  var peg$e32 = peg$literalExpectation("->", false);
  var peg$e33 = peg$classExpectation([["A", "Z"], ["a", "z"], ["0", "9"], "_"], false, false);
  var peg$e34 = peg$literalExpectation("\r", false);
  var peg$e35 = peg$literalExpectation("\n", false);
  var peg$e36 = peg$anyExpectation();
  var peg$e37 = peg$classExpectation([" ", "	"], false, false);
  var peg$f0 = function(fm, stmts) {
    const participants = [];
    const events = [];
    let autonumber = false;
    const ensure = (id, label, isActor) => {
      let pp = participants.find((x) => x.id === id);
      if (!pp) {
        pp = { id, label: label || id, isActor: !!isActor };
        participants.push(pp);
      } else if (label) pp.label = label;
      return pp;
    };
    for (const s of stmts.filter(Boolean)) {
      if (s.t === "autonumber") autonumber = true;
      else if (s.t === "participant") ensure(s.id, s.label, s.isActor);
      else if (s.t === "message") {
        ensure(s.from);
        ensure(s.to);
        events.push(s.ev);
      } else if (s.t === "note") {
        for (const id of s.ev.participants) ensure(id);
        events.push(s.ev);
      } else if (s.t === "event") events.push(s.ev);
    }
    return { version: "1.0", metadata: { ...fm || {} }, participants, events, autonumber };
  };
  var peg$f1 = function(line) {
    return line;
  };
  var peg$f2 = function(lines2) {
    const meta = {};
    for (const line of lines2) {
      const m = line.match(/^\s*(\w+)\s*:\s*(.+?)\s*$/);
      if (m) meta[m[1]] = m[2];
    }
    return meta;
  };
  var peg$f3 = function() {
    return { t: "autonumber" };
  };
  var peg$f4 = function(kw, id, l) {
    return l.trim();
  };
  var peg$f5 = function(kw, id, label) {
    return { t: "participant", id, label: label || void 0, isActor: kw.toLowerCase() === "actor" };
  };
  var peg$f6 = function(placement, ids, text2) {
    return { t: "note", ev: { kind: "note", placement, participants: ids, text: text2.trim() } };
  };
  var peg$f7 = function() {
    return "over";
  };
  var peg$f8 = function() {
    return "left";
  };
  var peg$f9 = function() {
    return "right";
  };
  var peg$f10 = function(first, i) {
    return i;
  };
  var peg$f11 = function(first, rest) {
    return [first, ...rest];
  };
  var peg$f12 = function(type, l) {
    return l.trim();
  };
  var peg$f13 = function(type, label) {
    return { t: "event", ev: { kind: "frag-start", type: type.toLowerCase(), label: label || "" } };
  };
  var peg$f14 = function(l) {
    return l.trim();
  };
  var peg$f15 = function(label) {
    return { t: "event", ev: { kind: "frag-else", label: label || "" } };
  };
  var peg$f16 = function() {
    return { t: "event", ev: { kind: "frag-end" } };
  };
  var peg$f17 = function() {
    return { t: "ignore" };
  };
  var peg$f18 = function(from, arrow, act, to, text2) {
    const { line, head } = arrowStyle(arrow);
    return { t: "message", from, to, ev: {
      kind: "message",
      from,
      to,
      text: text2.trim(),
      line,
      head,
      ...act === "+" ? { activateTarget: true } : {},
      ...act === "-" ? { deactivateSource: true } : {}
    } };
  };
  var peg$f19 = function() {
    return null;
  };
  var peg$currPos = options.peg$currPos | 0;
  var peg$savedPos = peg$currPos;
  var peg$posDetailsCache = [{ line: 1, column: 1 }];
  var peg$maxFailPos = peg$currPos;
  var peg$maxFailExpected = options.peg$maxFailExpected || [];
  var peg$silentFails = options.peg$silentFails | 0;
  var peg$result;
  if (options.startRule) {
    if (!(options.startRule in peg$startRuleFunctions)) {
      throw new Error(`Can't start parsing from rule "` + options.startRule + '".');
    }
    peg$startRuleFunction = peg$startRuleFunctions[options.startRule];
  }
  function text() {
    return input.substring(peg$savedPos, peg$currPos);
  }
  function offset() {
    return peg$savedPos;
  }
  function range() {
    return {
      source: peg$source,
      start: peg$savedPos,
      end: peg$currPos
    };
  }
  function location() {
    return peg$computeLocation(peg$savedPos, peg$currPos);
  }
  function expected(description, location2) {
    location2 = location2 !== void 0 ? location2 : peg$computeLocation(peg$savedPos, peg$currPos);
    throw peg$buildStructuredError(
      [peg$otherExpectation(description)],
      input.substring(peg$savedPos, peg$currPos),
      location2
    );
  }
  function error(message, location2) {
    location2 = location2 !== void 0 ? location2 : peg$computeLocation(peg$savedPos, peg$currPos);
    throw peg$buildSimpleError(message, location2);
  }
  function peg$literalExpectation(text2, ignoreCase) {
    return { type: "literal", text: text2, ignoreCase };
  }
  function peg$classExpectation(parts, inverted, ignoreCase) {
    return { type: "class", parts, inverted, ignoreCase };
  }
  function peg$anyExpectation() {
    return { type: "any" };
  }
  function peg$endExpectation() {
    return { type: "end" };
  }
  function peg$otherExpectation(description) {
    return { type: "other", description };
  }
  function peg$computePosDetails(pos) {
    var details = peg$posDetailsCache[pos];
    var p;
    if (details) {
      return details;
    } else {
      if (pos >= peg$posDetailsCache.length) {
        p = peg$posDetailsCache.length - 1;
      } else {
        p = pos;
        while (!peg$posDetailsCache[--p]) {
        }
      }
      details = peg$posDetailsCache[p];
      details = {
        line: details.line,
        column: details.column
      };
      while (p < pos) {
        if (input.charCodeAt(p) === 10) {
          details.line++;
          details.column = 1;
        } else {
          details.column++;
        }
        p++;
      }
      peg$posDetailsCache[pos] = details;
      return details;
    }
  }
  function peg$computeLocation(startPos, endPos, offset2) {
    var startPosDetails = peg$computePosDetails(startPos);
    var endPosDetails = peg$computePosDetails(endPos);
    var res = {
      source: peg$source,
      start: {
        offset: startPos,
        line: startPosDetails.line,
        column: startPosDetails.column
      },
      end: {
        offset: endPos,
        line: endPosDetails.line,
        column: endPosDetails.column
      }
    };
    if (offset2 && peg$source && typeof peg$source.offset === "function") {
      res.start = peg$source.offset(res.start);
      res.end = peg$source.offset(res.end);
    }
    return res;
  }
  function peg$fail(expected2) {
    if (peg$currPos < peg$maxFailPos) {
      return;
    }
    if (peg$currPos > peg$maxFailPos) {
      peg$maxFailPos = peg$currPos;
      peg$maxFailExpected = [];
    }
    peg$maxFailExpected.push(expected2);
  }
  function peg$buildSimpleError(message, location2) {
    return new peg$SyntaxError11(message, null, null, location2);
  }
  function peg$buildStructuredError(expected2, found, location2) {
    return new peg$SyntaxError11(
      peg$SyntaxError11.buildMessage(expected2, found),
      expected2,
      found,
      location2
    );
  }
  function peg$parseDocument() {
    var s0, s1, s2, s3, s4, s5, s6;
    s0 = peg$currPos;
    s1 = peg$parseFrontmatter();
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    s2 = peg$parse_();
    s3 = input.substr(peg$currPos, 15);
    if (s3.toLowerCase() === peg$c0) {
      peg$currPos += 15;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e0);
      }
    }
    if (s3 !== peg$FAILED) {
      s4 = peg$parseLineEnd();
      if (s4 !== peg$FAILED) {
        s5 = [];
        s6 = peg$parseStatement();
        while (s6 !== peg$FAILED) {
          s5.push(s6);
          s6 = peg$parseStatement();
        }
        s6 = peg$parse_();
        peg$savedPos = s0;
        s0 = peg$f0(s1, s5);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseFrontmatter() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 3) === peg$c1) {
      s1 = peg$c1;
      peg$currPos += 3;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e1);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$parseNL();
      if (s2 !== peg$FAILED) {
        s3 = [];
        s4 = peg$currPos;
        s5 = peg$currPos;
        peg$silentFails++;
        s6 = peg$currPos;
        if (input.substr(peg$currPos, 3) === peg$c1) {
          s7 = peg$c1;
          peg$currPos += 3;
        } else {
          s7 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e1);
          }
        }
        if (s7 !== peg$FAILED) {
          s8 = peg$parseNL();
          if (s8 !== peg$FAILED) {
            s7 = [s7, s8];
            s6 = s7;
          } else {
            peg$currPos = s6;
            s6 = peg$FAILED;
          }
        } else {
          peg$currPos = s6;
          s6 = peg$FAILED;
        }
        peg$silentFails--;
        if (s6 === peg$FAILED) {
          s5 = void 0;
        } else {
          peg$currPos = s5;
          s5 = peg$FAILED;
        }
        if (s5 !== peg$FAILED) {
          s6 = peg$currPos;
          s7 = [];
          s8 = input.charAt(peg$currPos);
          if (peg$r0.test(s8)) {
            peg$currPos++;
          } else {
            s8 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e2);
            }
          }
          while (s8 !== peg$FAILED) {
            s7.push(s8);
            s8 = input.charAt(peg$currPos);
            if (peg$r0.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e2);
              }
            }
          }
          s6 = input.substring(s6, peg$currPos);
          s7 = peg$parseNL();
          if (s7 !== peg$FAILED) {
            peg$savedPos = s4;
            s4 = peg$f1(s6);
          } else {
            peg$currPos = s4;
            s4 = peg$FAILED;
          }
        } else {
          peg$currPos = s4;
          s4 = peg$FAILED;
        }
        while (s4 !== peg$FAILED) {
          s3.push(s4);
          s4 = peg$currPos;
          s5 = peg$currPos;
          peg$silentFails++;
          s6 = peg$currPos;
          if (input.substr(peg$currPos, 3) === peg$c1) {
            s7 = peg$c1;
            peg$currPos += 3;
          } else {
            s7 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e1);
            }
          }
          if (s7 !== peg$FAILED) {
            s8 = peg$parseNL();
            if (s8 !== peg$FAILED) {
              s7 = [s7, s8];
              s6 = s7;
            } else {
              peg$currPos = s6;
              s6 = peg$FAILED;
            }
          } else {
            peg$currPos = s6;
            s6 = peg$FAILED;
          }
          peg$silentFails--;
          if (s6 === peg$FAILED) {
            s5 = void 0;
          } else {
            peg$currPos = s5;
            s5 = peg$FAILED;
          }
          if (s5 !== peg$FAILED) {
            s6 = peg$currPos;
            s7 = [];
            s8 = input.charAt(peg$currPos);
            if (peg$r0.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e2);
              }
            }
            while (s8 !== peg$FAILED) {
              s7.push(s8);
              s8 = input.charAt(peg$currPos);
              if (peg$r0.test(s8)) {
                peg$currPos++;
              } else {
                s8 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e2);
                }
              }
            }
            s6 = input.substring(s6, peg$currPos);
            s7 = peg$parseNL();
            if (s7 !== peg$FAILED) {
              peg$savedPos = s4;
              s4 = peg$f1(s6);
            } else {
              peg$currPos = s4;
              s4 = peg$FAILED;
            }
          } else {
            peg$currPos = s4;
            s4 = peg$FAILED;
          }
        }
        if (input.substr(peg$currPos, 3) === peg$c1) {
          s4 = peg$c1;
          peg$currPos += 3;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e1);
          }
        }
        if (s4 !== peg$FAILED) {
          s5 = peg$parseNL();
          if (s5 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f2(s3);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseStatement() {
    var s0;
    s0 = peg$parseAutoNumber();
    if (s0 === peg$FAILED) {
      s0 = peg$parseParticipantDecl();
      if (s0 === peg$FAILED) {
        s0 = peg$parseNoteStmt();
        if (s0 === peg$FAILED) {
          s0 = peg$parseFragEnd();
          if (s0 === peg$FAILED) {
            s0 = peg$parseFragElse();
            if (s0 === peg$FAILED) {
              s0 = peg$parseFragStart();
              if (s0 === peg$FAILED) {
                s0 = peg$parseActivateStmt();
                if (s0 === peg$FAILED) {
                  s0 = peg$parseMessage();
                  if (s0 === peg$FAILED) {
                    s0 = peg$parseBlankLine();
                  }
                }
              }
            }
          }
        }
      }
    }
    return s0;
  }
  function peg$parseAutoNumber() {
    var s0, s1, s2, s3, s4, s5, s6, s7;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = input.substr(peg$currPos, 10);
    if (s2.toLowerCase() === peg$c2) {
      peg$currPos += 10;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e3);
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$currPos;
      s4 = peg$parse__();
      if (s4 !== peg$FAILED) {
        s5 = peg$currPos;
        s6 = [];
        s7 = input.charAt(peg$currPos);
        if (peg$r0.test(s7)) {
          peg$currPos++;
        } else {
          s7 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e2);
          }
        }
        while (s7 !== peg$FAILED) {
          s6.push(s7);
          s7 = input.charAt(peg$currPos);
          if (peg$r0.test(s7)) {
            peg$currPos++;
          } else {
            s7 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e2);
            }
          }
        }
        s5 = input.substring(s5, peg$currPos);
        s4 = [s4, s5];
        s3 = s4;
      } else {
        peg$currPos = s3;
        s3 = peg$FAILED;
      }
      if (s3 === peg$FAILED) {
        s3 = null;
      }
      s4 = peg$parseLineEnd();
      if (s4 !== peg$FAILED) {
        peg$savedPos = s0;
        s0 = peg$f3();
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseParticipantDecl() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = input.substr(peg$currPos, 11);
    if (s2.toLowerCase() === peg$c3) {
      peg$currPos += 11;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e4);
      }
    }
    if (s2 === peg$FAILED) {
      s2 = input.substr(peg$currPos, 5);
      if (s2.toLowerCase() === peg$c4) {
        peg$currPos += 5;
      } else {
        s2 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e5);
        }
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$parse__();
      if (s3 !== peg$FAILED) {
        s4 = peg$parseIdent();
        if (s4 !== peg$FAILED) {
          s5 = peg$currPos;
          s6 = peg$parse_();
          s7 = input.substr(peg$currPos, 2);
          if (s7.toLowerCase() === peg$c5) {
            peg$currPos += 2;
          } else {
            s7 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e6);
            }
          }
          if (s7 !== peg$FAILED) {
            s8 = peg$parse__();
            if (s8 !== peg$FAILED) {
              s9 = peg$parseRestOfLine();
              if (s9 !== peg$FAILED) {
                peg$savedPos = s5;
                s5 = peg$f4(s2, s4, s9);
              } else {
                peg$currPos = s5;
                s5 = peg$FAILED;
              }
            } else {
              peg$currPos = s5;
              s5 = peg$FAILED;
            }
          } else {
            peg$currPos = s5;
            s5 = peg$FAILED;
          }
          if (s5 === peg$FAILED) {
            s5 = null;
          }
          s6 = peg$parseLineEnd();
          if (s6 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f5(s2, s4, s5);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseNoteStmt() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9, s10, s11;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = input.substr(peg$currPos, 4);
    if (s2.toLowerCase() === peg$c6) {
      peg$currPos += 4;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e7);
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$parse__();
      if (s3 !== peg$FAILED) {
        s4 = peg$parseNotePlacement();
        if (s4 !== peg$FAILED) {
          s5 = peg$parse__();
          if (s5 !== peg$FAILED) {
            s6 = peg$parseNoteIds();
            if (s6 !== peg$FAILED) {
              s7 = peg$parse_();
              if (input.charCodeAt(peg$currPos) === 58) {
                s8 = peg$c7;
                peg$currPos++;
              } else {
                s8 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e8);
                }
              }
              if (s8 !== peg$FAILED) {
                s9 = peg$parse_();
                s10 = peg$parseRestOfLine();
                if (s10 !== peg$FAILED) {
                  s11 = peg$parseLineEnd();
                  if (s11 !== peg$FAILED) {
                    peg$savedPos = s0;
                    s0 = peg$f6(s4, s6, s10);
                  } else {
                    peg$currPos = s0;
                    s0 = peg$FAILED;
                  }
                } else {
                  peg$currPos = s0;
                  s0 = peg$FAILED;
                }
              } else {
                peg$currPos = s0;
                s0 = peg$FAILED;
              }
            } else {
              peg$currPos = s0;
              s0 = peg$FAILED;
            }
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseNotePlacement() {
    var s0, s1;
    s0 = peg$currPos;
    s1 = input.substr(peg$currPos, 4);
    if (s1.toLowerCase() === peg$c8) {
      peg$currPos += 4;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e9);
      }
    }
    if (s1 !== peg$FAILED) {
      peg$savedPos = s0;
      s1 = peg$f7();
    }
    s0 = s1;
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      s1 = input.substr(peg$currPos, 7);
      if (s1.toLowerCase() === peg$c9) {
        peg$currPos += 7;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e10);
        }
      }
      if (s1 !== peg$FAILED) {
        peg$savedPos = s0;
        s1 = peg$f8();
      }
      s0 = s1;
      if (s0 === peg$FAILED) {
        s0 = peg$currPos;
        s1 = input.substr(peg$currPos, 8);
        if (s1.toLowerCase() === peg$c10) {
          peg$currPos += 8;
        } else {
          s1 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e11);
          }
        }
        if (s1 !== peg$FAILED) {
          peg$savedPos = s0;
          s1 = peg$f9();
        }
        s0 = s1;
      }
    }
    return s0;
  }
  function peg$parseNoteIds() {
    var s0, s1, s2, s3, s4, s5, s6, s7;
    s0 = peg$currPos;
    s1 = peg$parseIdent();
    if (s1 !== peg$FAILED) {
      s2 = [];
      s3 = peg$currPos;
      s4 = peg$parse_();
      if (input.charCodeAt(peg$currPos) === 44) {
        s5 = peg$c11;
        peg$currPos++;
      } else {
        s5 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e12);
        }
      }
      if (s5 !== peg$FAILED) {
        s6 = peg$parse_();
        s7 = peg$parseIdent();
        if (s7 !== peg$FAILED) {
          peg$savedPos = s3;
          s3 = peg$f10(s1, s7);
        } else {
          peg$currPos = s3;
          s3 = peg$FAILED;
        }
      } else {
        peg$currPos = s3;
        s3 = peg$FAILED;
      }
      while (s3 !== peg$FAILED) {
        s2.push(s3);
        s3 = peg$currPos;
        s4 = peg$parse_();
        if (input.charCodeAt(peg$currPos) === 44) {
          s5 = peg$c11;
          peg$currPos++;
        } else {
          s5 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e12);
          }
        }
        if (s5 !== peg$FAILED) {
          s6 = peg$parse_();
          s7 = peg$parseIdent();
          if (s7 !== peg$FAILED) {
            peg$savedPos = s3;
            s3 = peg$f10(s1, s7);
          } else {
            peg$currPos = s3;
            s3 = peg$FAILED;
          }
        } else {
          peg$currPos = s3;
          s3 = peg$FAILED;
        }
      }
      peg$savedPos = s0;
      s0 = peg$f11(s1, s2);
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseFragStart() {
    var s0, s1, s2, s3, s4, s5;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = input.substr(peg$currPos, 3);
    if (s2.toLowerCase() === peg$c12) {
      peg$currPos += 3;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e13);
      }
    }
    if (s2 === peg$FAILED) {
      s2 = input.substr(peg$currPos, 3);
      if (s2.toLowerCase() === peg$c13) {
        peg$currPos += 3;
      } else {
        s2 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e14);
        }
      }
      if (s2 === peg$FAILED) {
        s2 = input.substr(peg$currPos, 4);
        if (s2.toLowerCase() === peg$c14) {
          peg$currPos += 4;
        } else {
          s2 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e15);
          }
        }
        if (s2 === peg$FAILED) {
          s2 = input.substr(peg$currPos, 3);
          if (s2.toLowerCase() === peg$c15) {
            peg$currPos += 3;
          } else {
            s2 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e16);
            }
          }
          if (s2 === peg$FAILED) {
            s2 = input.substr(peg$currPos, 8);
            if (s2.toLowerCase() === peg$c16) {
              peg$currPos += 8;
            } else {
              s2 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e17);
              }
            }
            if (s2 === peg$FAILED) {
              s2 = input.substr(peg$currPos, 5);
              if (s2.toLowerCase() === peg$c17) {
                peg$currPos += 5;
              } else {
                s2 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e18);
                }
              }
            }
          }
        }
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$currPos;
      s4 = peg$parse__();
      if (s4 !== peg$FAILED) {
        s5 = peg$parseRestOfLine();
        if (s5 !== peg$FAILED) {
          peg$savedPos = s3;
          s3 = peg$f12(s2, s5);
        } else {
          peg$currPos = s3;
          s3 = peg$FAILED;
        }
      } else {
        peg$currPos = s3;
        s3 = peg$FAILED;
      }
      if (s3 === peg$FAILED) {
        s3 = null;
      }
      s4 = peg$parseLineEnd();
      if (s4 !== peg$FAILED) {
        peg$savedPos = s0;
        s0 = peg$f13(s2, s3);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseFragElse() {
    var s0, s1, s2, s3, s4, s5;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = input.substr(peg$currPos, 4);
    if (s2.toLowerCase() === peg$c18) {
      peg$currPos += 4;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e19);
      }
    }
    if (s2 === peg$FAILED) {
      s2 = input.substr(peg$currPos, 3);
      if (s2.toLowerCase() === peg$c19) {
        peg$currPos += 3;
      } else {
        s2 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e20);
        }
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$currPos;
      s4 = peg$parse__();
      if (s4 !== peg$FAILED) {
        s5 = peg$parseRestOfLine();
        if (s5 !== peg$FAILED) {
          peg$savedPos = s3;
          s3 = peg$f14(s5);
        } else {
          peg$currPos = s3;
          s3 = peg$FAILED;
        }
      } else {
        peg$currPos = s3;
        s3 = peg$FAILED;
      }
      if (s3 === peg$FAILED) {
        s3 = null;
      }
      s4 = peg$parseLineEnd();
      if (s4 !== peg$FAILED) {
        peg$savedPos = s0;
        s0 = peg$f15(s3);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseFragEnd() {
    var s0, s1, s2, s3;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = input.substr(peg$currPos, 3);
    if (s2.toLowerCase() === peg$c20) {
      peg$currPos += 3;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e21);
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$parseLineEnd();
      if (s3 !== peg$FAILED) {
        peg$savedPos = s0;
        s0 = peg$f16();
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseActivateStmt() {
    var s0, s1, s2, s3, s4, s5;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = input.substr(peg$currPos, 8);
    if (s2.toLowerCase() === peg$c21) {
      peg$currPos += 8;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e22);
      }
    }
    if (s2 === peg$FAILED) {
      s2 = input.substr(peg$currPos, 10);
      if (s2.toLowerCase() === peg$c22) {
        peg$currPos += 10;
      } else {
        s2 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e23);
        }
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$parse__();
      if (s3 !== peg$FAILED) {
        s4 = peg$parseIdent();
        if (s4 !== peg$FAILED) {
          s5 = peg$parseLineEnd();
          if (s5 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f17();
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseMessage() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9, s10, s11, s12;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = peg$parseIdent();
    if (s2 !== peg$FAILED) {
      s3 = peg$parse_();
      s4 = peg$parseArrow();
      if (s4 !== peg$FAILED) {
        s5 = input.charAt(peg$currPos);
        if (peg$r1.test(s5)) {
          peg$currPos++;
        } else {
          s5 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e24);
          }
        }
        if (s5 === peg$FAILED) {
          s5 = null;
        }
        s6 = peg$parse_();
        s7 = peg$parseIdent();
        if (s7 !== peg$FAILED) {
          s8 = peg$parse_();
          if (input.charCodeAt(peg$currPos) === 58) {
            s9 = peg$c7;
            peg$currPos++;
          } else {
            s9 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e8);
            }
          }
          if (s9 !== peg$FAILED) {
            s10 = peg$parse_();
            s11 = peg$parseRestOfLine();
            if (s11 !== peg$FAILED) {
              s12 = peg$parseLineEnd();
              if (s12 !== peg$FAILED) {
                peg$savedPos = s0;
                s0 = peg$f18(s2, s4, s5, s7, s11);
              } else {
                peg$currPos = s0;
                s0 = peg$FAILED;
              }
            } else {
              peg$currPos = s0;
              s0 = peg$FAILED;
            }
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseArrow() {
    var s0, s1;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 4) === peg$c23) {
      s1 = peg$c23;
      peg$currPos += 4;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e25);
      }
    }
    if (s1 === peg$FAILED) {
      if (input.substr(peg$currPos, 3) === peg$c24) {
        s1 = peg$c24;
        peg$currPos += 3;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e26);
        }
      }
      if (s1 === peg$FAILED) {
        if (input.substr(peg$currPos, 3) === peg$c25) {
          s1 = peg$c25;
          peg$currPos += 3;
        } else {
          s1 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e27);
          }
        }
        if (s1 === peg$FAILED) {
          if (input.substr(peg$currPos, 2) === peg$c26) {
            s1 = peg$c26;
            peg$currPos += 2;
          } else {
            s1 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e28);
            }
          }
          if (s1 === peg$FAILED) {
            if (input.substr(peg$currPos, 3) === peg$c27) {
              s1 = peg$c27;
              peg$currPos += 3;
            } else {
              s1 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e29);
              }
            }
            if (s1 === peg$FAILED) {
              if (input.substr(peg$currPos, 2) === peg$c28) {
                s1 = peg$c28;
                peg$currPos += 2;
              } else {
                s1 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e30);
                }
              }
              if (s1 === peg$FAILED) {
                if (input.substr(peg$currPos, 3) === peg$c29) {
                  s1 = peg$c29;
                  peg$currPos += 3;
                } else {
                  s1 = peg$FAILED;
                  if (peg$silentFails === 0) {
                    peg$fail(peg$e31);
                  }
                }
                if (s1 === peg$FAILED) {
                  if (input.substr(peg$currPos, 2) === peg$c30) {
                    s1 = peg$c30;
                    peg$currPos += 2;
                  } else {
                    s1 = peg$FAILED;
                    if (peg$silentFails === 0) {
                      peg$fail(peg$e32);
                    }
                  }
                }
              }
            }
          }
        }
      }
    }
    if (s1 !== peg$FAILED) {
      s0 = input.substring(s0, peg$currPos);
    } else {
      s0 = s1;
    }
    return s0;
  }
  function peg$parseIdent() {
    var s0, s1, s2;
    s0 = peg$currPos;
    s1 = [];
    s2 = input.charAt(peg$currPos);
    if (peg$r2.test(s2)) {
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e33);
      }
    }
    if (s2 !== peg$FAILED) {
      while (s2 !== peg$FAILED) {
        s1.push(s2);
        s2 = input.charAt(peg$currPos);
        if (peg$r2.test(s2)) {
          peg$currPos++;
        } else {
          s2 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e33);
          }
        }
      }
    } else {
      s1 = peg$FAILED;
    }
    if (s1 !== peg$FAILED) {
      s0 = input.substring(s0, peg$currPos);
    } else {
      s0 = s1;
    }
    return s0;
  }
  function peg$parseRestOfLine() {
    var s0, s1, s2;
    s0 = peg$currPos;
    s1 = [];
    s2 = input.charAt(peg$currPos);
    if (peg$r0.test(s2)) {
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e2);
      }
    }
    if (s2 !== peg$FAILED) {
      while (s2 !== peg$FAILED) {
        s1.push(s2);
        s2 = input.charAt(peg$currPos);
        if (peg$r0.test(s2)) {
          peg$currPos++;
        } else {
          s2 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e2);
          }
        }
      }
    } else {
      s1 = peg$FAILED;
    }
    if (s1 !== peg$FAILED) {
      s0 = input.substring(s0, peg$currPos);
    } else {
      s0 = s1;
    }
    return s0;
  }
  function peg$parseBlankLine() {
    var s0, s1, s2, s3;
    s0 = peg$currPos;
    s1 = peg$parse_();
    if (input.charCodeAt(peg$currPos) === 13) {
      s2 = peg$c31;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e34);
      }
    }
    if (s2 === peg$FAILED) {
      s2 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s3 = peg$c32;
      peg$currPos++;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e35);
      }
    }
    if (s3 !== peg$FAILED) {
      peg$savedPos = s0;
      s0 = peg$f19();
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseNL() {
    var s0, s1, s2;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 13) {
      s1 = peg$c31;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e34);
      }
    }
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s2 = peg$c32;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e35);
      }
    }
    if (s2 !== peg$FAILED) {
      s1 = [s1, s2];
      s0 = s1;
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseLineEnd() {
    var s0, s1, s2;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 13) {
      s1 = peg$c31;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e34);
      }
    }
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s2 = peg$c32;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e35);
      }
    }
    if (s2 !== peg$FAILED) {
      s1 = [s1, s2];
      s0 = s1;
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      peg$silentFails++;
      if (input.length > peg$currPos) {
        s1 = input.charAt(peg$currPos);
        peg$currPos++;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e36);
        }
      }
      peg$silentFails--;
      if (s1 === peg$FAILED) {
        s0 = void 0;
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    }
    return s0;
  }
  function peg$parse_() {
    var s0, s1;
    s0 = [];
    s1 = input.charAt(peg$currPos);
    if (peg$r3.test(s1)) {
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e37);
      }
    }
    while (s1 !== peg$FAILED) {
      s0.push(s1);
      s1 = input.charAt(peg$currPos);
      if (peg$r3.test(s1)) {
        peg$currPos++;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e37);
        }
      }
    }
    return s0;
  }
  function peg$parse__() {
    var s0, s1;
    s0 = [];
    s1 = input.charAt(peg$currPos);
    if (peg$r3.test(s1)) {
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e37);
      }
    }
    if (s1 !== peg$FAILED) {
      while (s1 !== peg$FAILED) {
        s0.push(s1);
        s1 = input.charAt(peg$currPos);
        if (peg$r3.test(s1)) {
          peg$currPos++;
        } else {
          s1 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e37);
          }
        }
      }
    } else {
      s0 = peg$FAILED;
    }
    return s0;
  }
  peg$result = peg$startRuleFunction();
  if (options.peg$library) {
    return (
      /** @type {any} */
      {
        peg$result,
        peg$currPos,
        peg$FAILED,
        peg$maxFailExpected,
        peg$maxFailPos
      }
    );
  }
  if (peg$result !== peg$FAILED && peg$currPos === input.length) {
    return peg$result;
  } else {
    if (peg$result !== peg$FAILED && peg$currPos < input.length) {
      peg$fail(peg$endExpectation());
    }
    throw peg$buildStructuredError(
      peg$maxFailExpected,
      peg$maxFailPos < input.length ? input.charAt(peg$maxFailPos) : null,
      peg$maxFailPos < input.length ? peg$computeLocation(peg$maxFailPos, peg$maxFailPos + 1) : peg$computeLocation(peg$maxFailPos, peg$maxFailPos)
    );
  }
}
var sequence = {
  parseMermaid(input) {
    return peg$parse11(input);
  },
  parseYaml(input) {
    return JSON.parse(input);
  },
  layout(ir, theme) {
    return layoutSequence(ir, theme);
  }
};
function borderPoint(box, tx, ty) {
  const cx = box.x + box.width / 2;
  const cy = box.y + box.height / 2;
  const dx = tx - cx;
  const dy = ty - cy;
  if (dx === 0 && dy === 0) return { x: cx, y: cy };
  const hw = box.width / 2;
  const hh = box.height / 2;
  const scale = 1 / Math.max(Math.abs(dx) / hw, Math.abs(dy) / hh);
  return { x: cx + dx * scale, y: cy + dy * scale };
}
function connectSlots(from, to) {
  const fc = { x: from.x + from.width / 2, y: from.y + from.height / 2 };
  const tc = { x: to.x + to.width / 2, y: to.y + to.height / 2 };
  return {
    start: borderPoint(from, tc.x, tc.y),
    end: borderPoint(to, fc.x, fc.y)
  };
}
function makeDummy(edgeIdx, segIdx) {
  return {
    id: `__dummy_${edgeIdx}_${segIdx}`,
    width: 0,
    height: 0,
    isDummy: true,
    originalEdgeIndex: edgeIdx,
    segmentIndex: segIdx
  };
}
function assignLayers2(nodes, edges2) {
  const layer = /* @__PURE__ */ new Map();
  for (const n of nodes) layer.set(n.id, 0);
  const present = (id) => layer.has(id);
  for (let pass = 0; pass < nodes.length; pass++) {
    let changed = false;
    for (const e of edges2) {
      if (!present(e.from) || !present(e.to)) continue;
      const want = layer.get(e.from) + 1;
      if (layer.get(e.to) < want) {
        layer.set(e.to, want);
        changed = true;
      }
    }
    if (!changed) break;
  }
  return layer;
}
function detectBackEdges(nodes, edges2) {
  const adj = /* @__PURE__ */ new Map();
  for (const n of nodes) adj.set(n.id, []);
  edges2.forEach((e, i) => {
    const list2 = adj.get(e.from);
    if (list2) list2.push({ to: e.to, edgeIdx: i });
  });
  const visited = /* @__PURE__ */ new Set();
  const onStack = /* @__PURE__ */ new Set();
  const backEdges = /* @__PURE__ */ new Set();
  for (const startNode of nodes) {
    if (visited.has(startNode.id)) continue;
    const stack2 = [];
    visited.add(startNode.id);
    onStack.add(startNode.id);
    stack2.push({ id: startNode.id, adjIdx: 0 });
    while (stack2.length > 0) {
      const frame = stack2[stack2.length - 1];
      const neighbors = adj.get(frame.id) ?? [];
      if (frame.adjIdx >= neighbors.length) {
        onStack.delete(frame.id);
        stack2.pop();
      } else {
        const { to, edgeIdx } = neighbors[frame.adjIdx];
        frame.adjIdx++;
        if (onStack.has(to)) {
          backEdges.add(edgeIdx);
        } else if (!visited.has(to)) {
          visited.add(to);
          onStack.add(to);
          stack2.push({ id: to, adjIdx: 0 });
        }
      }
    }
  }
  return backEdges;
}
function insertDummyNodes(nodes, edges2, layer, backEdgeSet) {
  const newNodes = [...nodes];
  const newEdges = [];
  const newBackEdgeSet = /* @__PURE__ */ new Set();
  const dummyChains = /* @__PURE__ */ new Map();
  for (let i = 0; i < edges2.length; i++) {
    const e = edges2[i];
    if (backEdgeSet.has(i)) {
      newBackEdgeSet.add(newEdges.length);
      newEdges.push(e);
      continue;
    }
    const lu = layer.get(e.from) ?? 0;
    const lv = layer.get(e.to) ?? 0;
    const span = lv - lu;
    if (span <= 1) {
      newEdges.push(e);
      continue;
    }
    const dummies = [];
    for (let seg = 0; seg < span - 1; seg++) {
      const d = makeDummy(i, seg);
      newNodes.push(d);
      layer.set(d.id, lu + 1 + seg);
      dummies.push(d.id);
    }
    dummyChains.set(i, dummies);
    const chain = [e.from, ...dummies, e.to];
    for (let s = 0; s < chain.length - 1; s++) {
      newEdges.push({ from: chain[s], to: chain[s + 1] });
    }
  }
  return { newNodes, newEdges, newBackEdgeSet, dummyChains };
}
function crossCount(byLayer, edges2, backEdgeSet) {
  const layerKeys = [...byLayer.keys()].sort((a, b) => a - b);
  const succList = /* @__PURE__ */ new Map();
  for (const [, ns] of byLayer) for (const n of ns) succList.set(n.id, []);
  edges2.forEach((e, i) => {
    if (backEdgeSet.has(i) || e.from === e.to) return;
    succList.get(e.from)?.push(e.to);
  });
  let cc = 0;
  for (let i = 1; i < layerKeys.length; i++) {
    cc += bilayerCrossCount(byLayer.get(layerKeys[i - 1]), byLayer.get(layerKeys[i]), succList);
  }
  return cc;
}
function bilayerCrossCount(northLayer, southLayer, succList) {
  if (southLayer.length === 0) return 0;
  const southPos = /* @__PURE__ */ new Map();
  southLayer.forEach((n, i) => southPos.set(n.id, i));
  const positions = northLayer.flatMap(
    (n) => (succList.get(n.id) ?? []).filter((sid) => southPos.has(sid)).map((sid) => southPos.get(sid)).sort((a, b) => a - b)
  );
  if (positions.length === 0) return 0;
  let firstIndex = 1;
  while (firstIndex < southLayer.length) firstIndex <<= 1;
  const treeSize = 2 * firstIndex - 1;
  firstIndex -= 1;
  const tree2 = new Array(treeSize).fill(0);
  let cc = 0;
  for (const pos of positions) {
    let idx = pos + firstIndex;
    tree2[idx] = (tree2[idx] ?? 0) + 1;
    let weightSum = 0;
    while (idx > 0) {
      if (idx % 2 === 1) weightSum += tree2[idx + 1];
      idx = idx - 1 >> 1;
      tree2[idx] = (tree2[idx] ?? 0) + 1;
    }
    cc += weightSum;
  }
  return cc;
}
function minimizeCrossings2(byLayer, edges2, backEdgeSet) {
  const predIds = /* @__PURE__ */ new Map();
  const succIds = /* @__PURE__ */ new Map();
  for (const [, ns] of byLayer) {
    for (const n of ns) {
      predIds.set(n.id, []);
      succIds.set(n.id, []);
    }
  }
  edges2.forEach((e, i) => {
    if (backEdgeSet.has(i) || e.from === e.to) return;
    predIds.get(e.to)?.push(e.from);
    succIds.get(e.from)?.push(e.to);
  });
  const layerKeys = [...byLayer.keys()].sort((a, b) => a - b);
  const nodeById2 = /* @__PURE__ */ new Map();
  const nodeLayer2 = /* @__PURE__ */ new Map();
  for (const [lk, ns] of byLayer)
    for (const n of ns) {
      nodeById2.set(n.id, n);
      nodeLayer2.set(n.id, lk);
    }
  function initOrder() {
    const visited = /* @__PURE__ */ new Set();
    const layers = /* @__PURE__ */ new Map();
    for (const k of layerKeys) layers.set(k, []);
    function dfs(v) {
      if (visited.has(v)) return;
      visited.add(v);
      const lk = nodeLayer2.get(v);
      if (lk !== void 0) layers.get(lk)?.push(nodeById2.get(v));
      for (const w of succIds.get(v) ?? []) dfs(w);
    }
    const allNodes = [...nodeById2.keys()].sort(
      (a, b) => (nodeLayer2.get(a) ?? 0) - (nodeLayer2.get(b) ?? 0)
    );
    for (const v of allNodes) dfs(v);
    return layers;
  }
  const order = initOrder();
  const posInLayer = /* @__PURE__ */ new Map();
  function rebuildPos() {
    for (const [, ns] of order) ns.forEach((n, i) => posInLayer.set(n.id, i));
  }
  rebuildPos();
  function sortLayer(layerIdx, neighborMap, biasRight) {
    const curr = order.get(layerIdx);
    const entries = curr.map((node, origIdx) => {
      const nbrs = (neighborMap.get(node.id) ?? []).filter((w) => posInLayer.has(w));
      if (nbrs.length === 0) return { node, barycenter: void 0, origIdx };
      const sum = nbrs.reduce((s, nid) => s + posInLayer.get(nid), 0);
      return { node, barycenter: sum / nbrs.length, origIdx };
    });
    const sortable = entries.filter((e) => e.barycenter !== void 0);
    const unsortable = entries.filter((e) => e.barycenter === void 0).sort((a, b) => b.origIdx - a.origIdx);
    sortable.sort((a, b) => {
      if (a.barycenter !== b.barycenter) return a.barycenter - b.barycenter;
      return biasRight ? b.origIdx - a.origIdx : a.origIdx - b.origIdx;
    });
    const result = [];
    let vsIndex = 0;
    function consumeUnsortable() {
      while (unsortable.length && unsortable[unsortable.length - 1].origIdx <= vsIndex) {
        result.push(unsortable.pop().node);
        vsIndex++;
      }
    }
    consumeUnsortable();
    for (const entry of sortable) {
      result.push(entry.node);
      vsIndex++;
      consumeUnsortable();
    }
    order.set(layerIdx, result);
    rebuildPos();
  }
  let bestCC = crossCount(order, edges2, backEdgeSet);
  const best = /* @__PURE__ */ new Map();
  for (const [k, v] of order) best.set(k, [...v]);
  for (let pass = 0, lastBest = 0; lastBest < 4; pass++, lastBest++) {
    const biasRight = pass % 4 >= 2;
    if (pass % 2 === 0) {
      for (let li = 1; li < layerKeys.length; li++) sortLayer(layerKeys[li], predIds, biasRight);
    } else {
      for (let li = layerKeys.length - 2; li >= 0; li--)
        sortLayer(layerKeys[li], succIds, biasRight);
    }
    const cc = crossCount(order, edges2, backEdgeSet);
    if (cc < bestCC) {
      lastBest = 0;
      bestCC = cc;
      for (const [k, v] of order) best.set(k, [...v]);
    } else if (cc === bestCC) {
      for (const [k, v] of order) best.set(k, [...v]);
    }
  }
  return best;
}
function assignCoordinatesBK4(byLayer, edges2, backEdgeSet, isLR, nodeGap, layerGap, margin) {
  const cross = (n) => isLR ? n.height : n.width;
  const along = (n) => isLR ? n.width : n.height;
  const isDummy = (id) => id.startsWith("__dummy_");
  const nodeById = /* @__PURE__ */ new Map();
  for (const [, ns] of byLayer) for (const n of ns) nodeById.set(n.id, n);
  function sep(a, b) {
    if (isDummy(a) || isDummy(b)) return 0;
    const an = nodeById.get(a);
    const bn = nodeById.get(b);
    return cross(an) / 2 + nodeGap + cross(bn) / 2;
  }
  const layerKeys = [...byLayer.keys()].sort((a, b) => a - b);
  const numLayers = layerKeys.length;
  const baseLayers = layerKeys.map((lk) => byLayer.get(lk).map((n) => n.id));
  const nodeLayerIdx = /* @__PURE__ */ new Map();
  for (let li = 0; li < numLayers; li++) {
    for (const id of baseLayers[li]) nodeLayerIdx.set(id, li);
  }
  const predMap = /* @__PURE__ */ new Map();
  const succMap = /* @__PURE__ */ new Map();
  for (const [, ns] of byLayer)
    for (const n of ns) {
      predMap.set(n.id, []);
      succMap.set(n.id, []);
    }
  edges2.forEach((e, i) => {
    if (backEdgeSet.has(i) || e.from === e.to) return;
    predMap.get(e.to)?.push(e.from);
    succMap.get(e.from)?.push(e.to);
  });
  const conflicts = /* @__PURE__ */ new Set();
  const ck = (u, v) => u < v ? `${u}\0${v}` : `${v}\0${u}`;
  const addConflict = (u, v) => conflicts.add(ck(u, v));
  const hasConflict = (u, v) => conflicts.has(ck(u, v));
  for (let li = 1; li < numLayers; li++) {
    const prevLayer = baseLayers[li - 1];
    const layer = baseLayers[li];
    const prevPos = /* @__PURE__ */ new Map();
    prevLayer.forEach((id, i) => prevPos.set(id, i));
    let k0 = 0;
    let scanPos = 0;
    const lastNode = layer[layer.length - 1];
    layer.forEach((v, i) => {
      let innerPredPos;
      if (isDummy(v)) {
        for (const u of predMap.get(v) ?? []) {
          if (isDummy(u) && prevPos.has(u)) {
            innerPredPos = prevPos.get(u);
            break;
          }
        }
      }
      const k1 = innerPredPos !== void 0 ? innerPredPos : prevLayer.length;
      if (innerPredPos !== void 0 || v === lastNode) {
        for (let si = scanPos; si <= i; si++) {
          const sn = layer[si];
          for (const u of predMap.get(sn) ?? []) {
            if (!prevPos.has(u)) continue;
            const uPos = prevPos.get(u);
            if ((uPos < k0 || uPos > k1) && !(isDummy(u) && isDummy(sn))) {
              addConflict(u, sn);
            }
          }
        }
        scanPos = i + 1;
        k0 = k1;
      }
    });
  }
  function verticalAlignment(sweepLayers, neighborFn) {
    const root = /* @__PURE__ */ new Map();
    const align = /* @__PURE__ */ new Map();
    const pos = /* @__PURE__ */ new Map();
    for (const layer of sweepLayers) {
      layer.forEach((v, i) => {
        root.set(v, v);
        align.set(v, v);
        pos.set(v, i);
      });
    }
    for (const layer of sweepLayers) {
      let prevIdx = -1;
      for (const v of layer) {
        const wsRaw = neighborFn(v);
        if (!wsRaw.length) continue;
        const ws = wsRaw.filter((w) => pos.has(w)).sort((a, b) => pos.get(a) - pos.get(b));
        if (ws.length === 0) continue;
        const mp = (ws.length - 1) / 2;
        for (let mi = Math.floor(mp), mEnd = Math.ceil(mp); mi <= mEnd; mi++) {
          const w = ws[mi];
          const wPos = pos.get(w);
          if (align.get(v) === v && prevIdx < wPos && !hasConflict(v, w)) {
            align.set(w, v);
            root.set(v, root.get(w));
            align.set(v, root.get(v));
            prevIdx = wPos;
          }
        }
      }
    }
    return { root, align };
  }
  function horizontalCompaction(sweepLayers, root) {
    const blockSucc = /* @__PURE__ */ new Map();
    const blockPred = /* @__PURE__ */ new Map();
    function ensureBlock(r) {
      if (!blockSucc.has(r)) {
        blockSucc.set(r, /* @__PURE__ */ new Map());
        blockPred.set(r, /* @__PURE__ */ new Map());
      }
    }
    for (const layer of sweepLayers) {
      let prevId;
      for (const v of layer) {
        const rv = root.get(v);
        ensureBlock(rv);
        if (prevId !== void 0) {
          const rp = root.get(prevId);
          if (rp !== rv) {
            const w = sep(prevId, v);
            const curW = blockSucc.get(rp).get(rv) ?? 0;
            if (w > curW) {
              blockSucc.get(rp).set(rv, w);
              blockPred.get(rv).set(rp, w);
            }
          }
        }
        prevId = v;
      }
    }
    const xs = /* @__PURE__ */ new Map();
    function iterate(applyFn, nextFn) {
      const stack2 = [...blockSucc.keys()];
      const visited = /* @__PURE__ */ new Set();
      while (stack2.length > 0) {
        const elem = stack2[stack2.length - 1];
        if (visited.has(elem)) {
          stack2.pop();
          applyFn(elem);
        } else {
          visited.add(elem);
          for (const nxt of nextFn(elem)) stack2.push(nxt);
        }
      }
    }
    iterate(
      (elem) => {
        let max = 0;
        for (const [from, w] of blockPred.get(elem) ?? /* @__PURE__ */ new Map())
          max = Math.max(max, (xs.get(from) ?? 0) + w);
        xs.set(elem, max);
      },
      (elem) => blockPred.get(elem)?.keys() ?? []
    );
    iterate(
      (elem) => {
        const succs = blockSucc.get(elem);
        if (succs && succs.size > 0) {
          let min = Infinity;
          for (const [to, w] of succs) min = Math.min(min, (xs.get(to) ?? 0) - w);
          if (min !== Infinity) xs.set(elem, Math.max(xs.get(elem) ?? 0, min));
        }
      },
      (elem) => blockSucc.get(elem)?.keys() ?? []
    );
    for (const layer of sweepLayers) {
      for (const v of layer) xs.set(v, xs.get(root.get(v)));
    }
    return xs;
  }
  const xss = /* @__PURE__ */ new Map();
  for (const vert of ["u", "d"]) {
    const vertLayers = vert === "u" ? baseLayers : [...baseLayers].reverse();
    for (const horiz of ["l", "r"]) {
      const sweepLayers = horiz === "r" ? vertLayers.map((layer) => [...layer].reverse()) : vertLayers;
      const neighborFn = vert === "u" ? (v) => predMap.get(v) ?? [] : (v) => succMap.get(v) ?? [];
      const { root } = verticalAlignment(sweepLayers, neighborFn);
      let xs = horizontalCompaction(sweepLayers, root);
      if (horiz === "r") {
        const neg = /* @__PURE__ */ new Map();
        for (const [k, v] of xs) neg.set(k, -v);
        xs = neg;
      }
      xss.set(vert + horiz, xs);
    }
  }
  const SWEEP_KEYS = ["ul", "ur", "dl", "dr"];
  const dummySweepXs = /* @__PURE__ */ new Map();
  for (const id of nodeById.keys()) {
    if (!isDummy(id)) continue;
    const vals = SWEEP_KEYS.map((k) => xss.get(k)?.get(id) ?? 0);
    dummySweepXs.set(id, vals);
  }
  let minSpan = Infinity;
  let smallestXs = xss.get("ul");
  for (const [, xs] of xss) {
    let lo = Infinity, hi = -Infinity;
    for (const [id, x] of xs) {
      const n = nodeById.get(id);
      if (!n) continue;
      const half = cross(n) / 2;
      lo = Math.min(lo, x - half);
      hi = Math.max(hi, x + half);
    }
    if (hi - lo < minSpan) {
      minSpan = hi - lo;
      smallestXs = xs;
    }
  }
  const refVals = [...smallestXs.values()];
  const refMin = Math.min(...refVals);
  const refMax = Math.max(...refVals);
  for (const [key, xs] of xss) {
    if (xs === smallestXs) continue;
    const vals = [...xs.values()];
    const isRight = key.endsWith("r");
    const delta = isRight ? refMax - Math.max(...vals) : refMin - Math.min(...vals);
    if (delta !== 0) {
      const shifted = /* @__PURE__ */ new Map();
      for (const [id, x] of xs) shifted.set(id, x + delta);
      xss.set(key, shifted);
    }
  }
  const balanced = /* @__PURE__ */ new Map();
  for (const [id] of xss.get("ul")) {
    const vals = [...xss.values()].map((xs) => xs.get(id) ?? 0).sort((a, b) => a - b);
    balanced.set(id, ((vals[1] ?? 0) + (vals[2] ?? 0)) / 2);
  }
  const COLUMN_SNAP_EPSILON = 6;
  const csParent = /* @__PURE__ */ new Map();
  const csFind = (id) => {
    let r = csParent.get(id) ?? id;
    while (csParent.get(r) !== void 0 && csParent.get(r) !== r) r = csParent.get(r);
    csParent.set(id, r);
    return r;
  };
  const csUnion = (a, b) => {
    const ra = csFind(a), rb = csFind(b);
    if (ra !== rb) csParent.set(ra, rb);
  };
  edges2.forEach((edge, i) => {
    if (backEdgeSet.has(i) || edge.from === edge.to) return;
    if (isDummy(edge.from) || isDummy(edge.to)) return;
    const fromLayer = nodeLayerIdx.get(edge.from);
    const toLayer = nodeLayerIdx.get(edge.to);
    if (fromLayer === void 0 || toLayer === void 0) return;
    if (Math.abs(toLayer - fromLayer) !== 1) return;
    const fromX = balanced.get(edge.from);
    const toX = balanced.get(edge.to);
    if (fromX === void 0 || toX === void 0) return;
    if (Math.abs(fromX - toX) > COLUMN_SNAP_EPSILON) return;
    csUnion(edge.from, edge.to);
  });
  const csGroups = /* @__PURE__ */ new Map();
  for (const id of balanced.keys()) {
    if (isDummy(id)) continue;
    const root = csFind(id);
    if (!csGroups.has(root)) csGroups.set(root, []);
    csGroups.get(root).push(id);
  }
  for (const members of csGroups.values()) {
    if (members.length < 2) continue;
    const xs = members.map((id) => balanced.get(id)).sort((a, b) => a - b);
    const medX = xs.length % 2 === 1 ? xs[Math.floor(xs.length / 2)] : (xs[xs.length / 2 - 1] + xs[xs.length / 2]) / 2;
    for (const id of members) balanced.set(id, medX);
  }
  let minLeft = Infinity;
  for (const [id, cx] of balanced) {
    const n = nodeById.get(id);
    if (n) minLeft = Math.min(minLeft, cx - cross(n) / 2);
  }
  const shift = margin - (isFinite(minLeft) ? minLeft : 0);
  const nodePos = /* @__PURE__ */ new Map();
  let alongCursor = margin;
  for (let li = 0; li < numLayers; li++) {
    const layerIdx = layerKeys[li];
    const ns = byLayer.get(layerIdx);
    const layerSize = ns.length > 0 ? Math.max(...ns.map(along)) : 0;
    for (const node of ns) {
      const cx = (balanced.get(node.id) ?? 0) + shift;
      const crossLeft = cx - cross(node) / 2;
      const alongPos = isDummy(node.id) && li > 0 ? alongCursor - layerGap / 2 : alongCursor + (layerSize - along(node)) / 2;
      nodePos.set(node.id, {
        id: node.id,
        x: isLR ? alongPos : crossLeft,
        y: isLR ? crossLeft : alongPos,
        width: node.width,
        height: node.height
      });
    }
    alongCursor += layerSize + layerGap;
  }
  return { boxes: nodePos, dummySweepXs };
}
function layeredLayout(nodes, edges2, options = {}) {
  const direction = options.direction ?? "TB";
  const layerGap = options.layerGap ?? 70;
  const nodeGap = options.nodeGap ?? 40;
  const margin = options.margin ?? 32;
  const isLR = direction === "LR";
  if (nodes.length === 0)
    return {
      boxes: /* @__PURE__ */ new Map(),
      width: margin * 2,
      height: margin * 2,
      edgeBends: /* @__PURE__ */ new Map(),
      dummySweepXs: /* @__PURE__ */ new Map(),
      dummyChainIds: /* @__PURE__ */ new Map()
    };
  const layer = assignLayers2(nodes, edges2);
  const backEdges = detectBackEdges(nodes, edges2);
  const { newNodes, newEdges, newBackEdgeSet, dummyChains } = insertDummyNodes(
    nodes,
    edges2,
    layer,
    backEdges
  );
  const newMaxLayer = Math.max(...newNodes.map((n) => layer.get(n.id)));
  const byLayerArr = /* @__PURE__ */ new Map();
  for (let i = 0; i <= newMaxLayer; i++) byLayerArr.set(i, []);
  for (const n of newNodes) byLayerArr.get(layer.get(n.id)).push(n);
  const orderedByLayer = minimizeCrossings2(byLayerArr, newEdges, newBackEdgeSet);
  const { boxes: allBoxesMap, dummySweepXs } = assignCoordinatesBK4(
    orderedByLayer,
    newEdges,
    newBackEdgeSet,
    isLR,
    nodeGap,
    layerGap,
    margin
  );
  const edgeBends = /* @__PURE__ */ new Map();
  for (const [origEdgeIdx, dummyIds] of dummyChains) {
    const bends = dummyIds.map((id) => {
      const b = allBoxesMap.get(id);
      return { x: b.x + b.width / 2, y: b.y + b.height / 2 };
    });
    edgeBends.set(origEdgeIdx, bends);
  }
  const boxes = /* @__PURE__ */ new Map();
  for (const [id, box] of allBoxesMap) {
    if (!id.startsWith("__dummy_")) boxes.set(id, box);
  }
  if (boxes.size > 1) {
    const uf = new Map(nodes.map((n) => [n.id, n.id]));
    const find2 = (x) => {
      while (uf.get(x) !== x) {
        uf.set(x, uf.get(uf.get(x)));
        x = uf.get(x);
      }
      return x;
    };
    for (const e of edges2) {
      if (boxes.has(e.from) && boxes.has(e.to)) {
        const ra = find2(e.from), rb = find2(e.to);
        if (ra !== rb) uf.set(ra, rb);
      }
    }
    const comps = /* @__PURE__ */ new Map();
    for (const id of boxes.keys()) {
      const r = find2(id);
      if (!comps.has(r)) comps.set(r, []);
      comps.get(r).push(id);
    }
    if (comps.size > 1) {
      const compInfos = [...comps.values()].map((ids) => ({
        ids,
        left: Math.min(...ids.map((id) => boxes.get(id).x)),
        right: Math.max(...ids.map((id) => boxes.get(id).x + boxes.get(id).width))
      })).sort((a, b) => a.left - b.left);
      let cursor = compInfos[0].right;
      for (let ci = 1; ci < compInfos.length; ci++) {
        const comp = compInfos[ci];
        const gap = comp.left - cursor;
        const targetGap = nodeGap * 2;
        if (gap > targetGap) {
          const dx = -(gap - targetGap);
          for (const id of comp.ids) {
            const b = boxes.get(id);
            boxes.set(id, { id: b.id, x: b.x + dx, y: b.y, width: b.width, height: b.height });
          }
          comp.left += dx;
          comp.right += dx;
        }
        cursor = comp.right;
      }
    }
  }
  const allBoxes = [...boxes.values()];
  const width = allBoxes.length > 0 ? Math.max(...allBoxes.map((b) => b.x + b.width)) + margin : margin * 2;
  const height = allBoxes.length > 0 ? Math.max(...allBoxes.map((b) => b.y + b.height)) + margin : margin * 2;
  return { boxes, width, height, edgeBends, dummySweepXs, dummyChainIds: dummyChains };
}
function straightLineObstacleFree(p1, p2, obstacles, padding) {
  const dx = p2.x - p1.x, dy = p2.y - p1.y;
  for (const obs of obstacles) {
    const xmin = obs.x - padding, xmax = obs.x + obs.width + padding;
    const ymin = obs.y - padding, ymax = obs.y + obs.height + padding;
    let tmin = 0, tmax = 1;
    const clips = [
      { p: -dx, q: p1.x - xmin },
      { p: dx, q: xmax - p1.x },
      { p: -dy, q: p1.y - ymin },
      { p: dy, q: ymax - p1.y }
    ];
    let crosses = true;
    for (const { p, q } of clips) {
      if (Math.abs(p) < 1e-10) {
        if (q <= 0) {
          crosses = false;
          break;
        }
      } else {
        const t = q / p;
        if (p < 0) tmin = Math.max(tmin, t);
        else tmax = Math.min(tmax, t);
        if (tmin >= tmax) {
          crosses = false;
          break;
        }
      }
    }
    if (crosses) return false;
  }
  return true;
}
function routeEdge(fromBox, toBox, allBoxes, yOff = 0, fromPt, toPt, forceOrthogonal = false) {
  const fromRect = {
    x: fromBox.x,
    y: fromBox.y + yOff,
    width: fromBox.width,
    height: fromBox.height
  };
  const toRect2 = { x: toBox.x, y: toBox.y + yOff, width: toBox.width, height: toBox.height };
  const fromCx = fromRect.x + fromRect.width / 2;
  const fromCy = fromRect.y + fromRect.height / 2;
  const toCx = toRect2.x + toRect2.width / 2;
  const toCy = toRect2.y + toRect2.height / 2;
  const dx = toCx - fromCx;
  const dy = toCy - fromCy;
  let fromDir;
  let toDir;
  if (Math.abs(dy) >= Math.abs(dx)) {
    fromDir = dy > 0 ? "S" : "N";
    toDir = dy > 0 ? "N" : "S";
  } else {
    fromDir = dx > 0 ? "E" : "W";
    toDir = dx > 0 ? "W" : "E";
  }
  const pa = fromPt ?? borderPoint(fromRect, toCx, toCy);
  const pb = toPt ?? borderPoint(toRect2, fromCx, fromCy);
  const fromId = fromBox.id;
  const toId = toBox.id;
  const obstacles = allBoxes.filter((b) => b.id !== fromId && b.id !== toId).map((b) => ({ x: b.x, y: b.y + yOff, width: b.width, height: b.height }));
  if (!forceOrthogonal && (obstacles.length === 0 || straightLineObstacleFree(pa, pb, obstacles, 10))) {
    return {
      path: `M ${pa.x} ${pa.y} L ${pb.x} ${pb.y}`,
      labelMidpoint: { x: (pa.x + pb.x) / 2, y: (pa.y + pb.y) / 2 }
    };
  }
  const route = orthogonalRouter.route({
    from: pa,
    to: pb,
    style: "orthogonal",
    obstacles,
    padding: 10,
    fromDir,
    toDir
  });
  const path = route.path || (forceOrthogonal ? `M ${pa.x} ${pa.y} L ${pa.x} ${pb.y} L ${pb.x} ${pb.y}` : `M ${pa.x} ${pa.y} L ${pb.x} ${pb.y}`);
  return { path, labelMidpoint: route.labelPosition };
}
function segmentIntersectsBox(x1, y1, x2, y2, box) {
  const bx1 = box.x, bx2 = box.x + box.width;
  const by1 = box.y, by2 = box.y + box.height;
  if (y1 === y2) {
    const lx = Math.min(x1, x2), rx = Math.max(x1, x2);
    return y1 > by1 && y1 < by2 && lx < bx2 && rx > bx1;
  }
  const ty = Math.min(y1, y2), by = Math.max(y1, y2);
  return x1 > bx1 && x1 < bx2 && ty < by2 && by > by1;
}
function toRect(x1, y1, x2, y2) {
  const W = 1;
  return {
    x1: Math.min(x1, x2) - W,
    y1: Math.min(y1, y2) - W,
    x2: Math.max(x1, x2) + W,
    y2: Math.max(y1, y2) + W
  };
}
function rectsOverlapLength(a, b) {
  const ox = Math.min(a.x2, b.x2) - Math.max(a.x1, b.x1);
  const oy = Math.min(a.y2, b.y2) - Math.max(a.y1, b.y1);
  if (ox <= 0 || oy <= 0) return 0;
  return Math.max(ox, oy);
}
var CLEARANCE = 12;
var LANE_CLEARANCE = 48;
function labelInBox(lx, ly, boxes, padding = 8) {
  return boxes.some(
    (b) => lx > b.x - padding && lx < b.x + b.width + padding && ly > b.y - padding && ly < b.y + b.height + padding
  );
}
function dedup(arr, tol) {
  const result = [];
  for (const v of arr) {
    if (!result.some((r) => Math.abs(r - v) < tol)) result.push(v);
  }
  return result;
}
function segmentsToPath(segs) {
  if (segs.length === 0) return "";
  if (segs.length === 1) {
    return `M ${rhu(segs[0][0])} ${rhu(segs[0][1])} L ${rhu(segs[0][2])} ${rhu(segs[0][3])}`;
  }
  const pts = [`M ${rhu(segs[0][0])} ${rhu(segs[0][1])}`];
  for (const [, , x2, y2] of segs) pts.push(`L ${rhu(x2)} ${rhu(y2)}`);
  return pts.join(" ");
}
function classifyEdge(a, b, bends) {
  const cxA = a.x + a.width / 2;
  const cxB = b.x + b.width / 2;
  const sameCol = Math.abs(cxA - cxB) < 8;
  const isSkip = bends != null && bends.length > 0;
  if (isSkip) return sameCol ? "skip-same-column" : "skip-cross-column";
  return sameCol ? "direct-same-column" : "direct-cross-column";
}
var MIN_PORT_GAP = 32;
var WALL_MARGIN = 16;
function cascadePorts(ideals, lo, hi, minGap) {
  const n = ideals.length;
  if (n === 0) return [];
  if (n === 1) return [Math.max(lo, Math.min(hi, ideals[0]))];
  if ((n - 1) * minGap > hi - lo) {
    const step = (hi - lo) / (n + 1);
    return Array.from({ length: n }, (_, i) => lo + step * (i + 1));
  }
  const pos = ideals.map((v) => Math.max(lo, Math.min(hi, v)));
  for (let iter = 0; iter < 5; iter++) {
    let changed = false;
    for (let i = 1; i < n; i++) {
      const minI = pos[i - 1] + minGap;
      if (pos[i] < minI) {
        pos[i] = minI;
        changed = true;
      }
    }
    for (let i = n - 1; i >= 0; i--) {
      const maxI = i === n - 1 ? hi : pos[i + 1] - minGap;
      if (pos[i] > maxI) {
        pos[i] = maxI;
        changed = true;
      }
    }
    if (pos[0] < lo) {
      pos[0] = lo;
      changed = true;
    }
    if (!changed) break;
  }
  return pos;
}
function assignGroupPorts(box, wall, group, yOff) {
  const result = /* @__PURE__ */ new Map();
  if (group.length === 0) return result;
  const sorted = [...group].sort((a, b) => a.sourceCenter - b.sourceCenter);
  let wallBase, wallLen, fixedCoord, isHorizontal;
  switch (wall) {
    case "top":
      wallBase = box.x;
      wallLen = box.width;
      fixedCoord = box.y + yOff;
      isHorizontal = true;
      break;
    case "bottom":
      wallBase = box.x;
      wallLen = box.width;
      fixedCoord = box.y + yOff + box.height;
      isHorizontal = true;
      break;
    case "left":
      wallBase = box.y + yOff;
      wallLen = box.height;
      fixedCoord = box.x;
      isHorizontal = false;
      break;
    default:
      wallBase = box.y + yOff;
      wallLen = box.height;
      fixedCoord = box.x + box.width;
      isHorizontal = false;
      break;
  }
  const lo = wallBase + WALL_MARGIN;
  const hi = wallBase + wallLen - WALL_MARGIN;
  const ideals = sorted.map((e) => Math.max(lo, Math.min(hi, e.sourceCenter)));
  const positions = cascadePorts(ideals, lo, hi, MIN_PORT_GAP);
  for (let i = 0; i < sorted.length; i++) {
    const p = positions[i];
    result.set(sorted[i].ri, isHorizontal ? { x: p, y: fixedCoord } : { x: fixedCoord, y: p });
  }
  return result;
}
function layoutClass(ir, theme) {
  const { palette, typography, spacing } = theme;
  const p = pen(theme);
  const margin = spacing.diagramMargin;
  const memFont = typography.smallFontSize;
  const nameFont = typography.baseFontSize;
  const lineH = rhuInt(memFont * 1.6);
  const headH = rhuInt(nameFont * 1.6);
  const sizeOf = (c) => {
    const memberTexts = [
      ...c.stereotype ? [`\xAB${c.stereotype}\xBB`] : [],
      ...c.attributes.map((m) => m.text),
      ...c.methods.map((m) => m.text)
    ];
    const nameW = measureText(c.name, nameFont).width + 24;
    const w = Math.max(130, nameW, ...memberTexts.map((t) => measureText(t, memFont).width + 24));
    const stereoH = c.stereotype ? memFont + 4 : 0;
    const attrH = c.attributes.length * lineH + 8;
    const methH = c.methods.length * lineH + 8;
    const h2 = headH + stereoH + attrH + methH;
    return { w, h: h2, attrH: headH + stereoH + attrH };
  };
  const sizes = new Map(ir.classes.map((c) => [c.name, sizeOf(c)]));
  const nodes = ir.classes.map((c) => ({
    id: c.name,
    width: sizes.get(c.name).w,
    height: sizes.get(c.name).h
  }));
  const edges2 = ir.relations.map(
    (r) => r.leftHead === "triangle" ? { from: r.right, to: r.left } : { from: r.left, to: r.right }
  );
  const LAYER_GAP = 64;
  const laid = layeredLayout(nodes, edges2, {
    direction: "TB",
    layerGap: LAYER_GAP,
    nodeGap: 46,
    margin
  });
  const canvasWidth = laid.width + margin * 2;
  const allRealBoxes = [...laid.boxes.values()];
  const routedSegments = [];
  const realColumnXs = [...new Set(allRealBoxes.map((b) => b.x + b.width / 2))].sort(
    (a, b) => a - b
  );
  const interColMidpoints = [];
  for (let ci = 0; ci < realColumnXs.length - 1; ci++) {
    interColMidpoints.push((realColumnXs[ci] + realColumnXs[ci + 1]) / 2);
  }
  function scoreLane(laneX, segments, interBoxes, routed, canvasW, realMinX, wallPairPenalty = 0, sameWallBonus = 0, straightBonus = 0, labelMid = null) {
    if (laneX > canvasW) return Infinity;
    let pathLength = 0;
    let segCount = segments.length;
    let boxHits = 0;
    let overlapHits = 0;
    for (const [x1, y1, x2, y2] of segments) {
      const dx = x2 - x1, dy = y2 - y1;
      pathLength += Math.sqrt(dx * dx + dy * dy);
      for (const nb of interBoxes) {
        if (segmentIntersectsBox(x1, y1, x2, y2, nb)) boxHits++;
      }
      const segRect = toRect(x1, y1, x2, y2);
      for (const rs of routed) {
        if (rectsOverlapLength(segRect, rs) >= 10) overlapHits++;
      }
    }
    const dirPenalty = laneX <= realMinX - CLEARANCE ? 0 : 5;
    const expansionPenalty = laneX < realMinX ? (realMinX - laneX) * 0.05 : 0;
    const labelPenalty = labelMid != null && labelInBox(labelMid.x, labelMid.y, allRealBoxes) ? 200 : 0;
    return 0.3 * pathLength + 10 * segCount + 1e3 * boxHits + 50 * overlapHits + dirPenalty + expansionPenalty + wallPairPenalty - sameWallBonus - // bonus for same-wall same-column routing (B/C)
    straightBonus + // reward for single-segment straight vertical
    labelPenalty;
  }
  const title = ir.metadata.title;
  const titleH = title ? typography.titleFontSize + 14 : 0;
  const yOff = titleH;
  const elements = [];
  if (title)
    elements.push(
      p.text(
        title,
        margin,
        margin + typography.titleFontSize,
        typography.titleFontSize,
        palette.text,
        { weight: "bold" }
      )
    );
  const allBoxes = [...laid.boxes.values()];
  const approachWall = (from, to) => {
    if (from.y + from.height <= to.y) return "top";
    if (to.y + to.height <= from.y) return "bottom";
    const dx = to.x + to.width / 2 - (from.x + from.width / 2);
    const dy = to.y + to.height / 2 - (from.y + from.height / 2);
    if (Math.abs(dy) >= Math.abs(dx)) return dy >= 0 ? "top" : "bottom";
    return dx >= 0 ? "left" : "right";
  };
  const wallPoint2 = (box, wall, t, yOff_) => {
    const bx = box.x, by = box.y + yOff_, bw = box.width, bh = box.height;
    switch (wall) {
      case "top":
        return { x: bx + t * bw, y: by };
      case "bottom":
        return { x: bx + t * bw, y: by + bh };
      case "left":
        return { x: bx, y: by + t * bh };
      case "right":
        return { x: bx + bw, y: by + t * bh };
    }
  };
  const toGroupAccum = /* @__PURE__ */ new Map();
  for (let ri = 0; ri < ir.relations.length; ri++) {
    const r = ir.relations[ri];
    const isFlipped = r.leftHead === "triangle";
    const a = laid.boxes.get(isFlipped ? r.right : r.left), b = laid.boxes.get(isFlipped ? r.left : r.right);
    if (!a || !b) continue;
    const wall = approachWall(a, b);
    const key = `${b.id}:${wall}`;
    const bends = laid.edgeBends.get(ri);
    const sourceCenter = wall === "top" || wall === "bottom" ? bends && bends.length > 0 ? bends[0].x : a.x + a.width / 2 : a.y + a.height / 2 + yOff;
    if (!toGroupAccum.has(key)) toGroupAccum.set(key, []);
    toGroupAccum.get(key).push({ ri, sourceCenter });
  }
  const toPortMap2 = /* @__PURE__ */ new Map();
  for (const [key, group] of toGroupAccum) {
    const nodeId = key.split(":")[0];
    const wall = key.split(":")[1];
    toPortMap2.set(key, assignGroupPorts(laid.boxes.get(nodeId), wall, group, yOff));
  }
  const fromGroupAccum = /* @__PURE__ */ new Map();
  for (let ri = 0; ri < ir.relations.length; ri++) {
    const r = ir.relations[ri];
    const isFlipped = r.leftHead === "triangle";
    const a = laid.boxes.get(isFlipped ? r.right : r.left), b = laid.boxes.get(isFlipped ? r.left : r.right);
    if (!a || !b) continue;
    const wall = approachWall(b, a);
    const key = `${a.id}:${wall}`;
    const bends = laid.edgeBends.get(ri);
    const targetCenter = wall === "top" || wall === "bottom" ? bends && bends.length > 0 ? bends[0].x : b.x + b.width / 2 : b.y + b.height / 2 + yOff;
    if (!fromGroupAccum.has(key)) fromGroupAccum.set(key, []);
    fromGroupAccum.get(key).push({ ri, sourceCenter: targetCenter });
  }
  const fromPortMap2 = /* @__PURE__ */ new Map();
  for (const [key, group] of fromGroupAccum) {
    const nodeId = key.split(":")[0];
    const wall = key.split(":")[1];
    fromPortMap2.set(key, assignGroupPorts(laid.boxes.get(nodeId), wall, group, yOff));
  }
  const wallDir = (wall, pt) => {
    switch (wall) {
      case "top":
        return { x: pt.x, y: pt.y - 1 };
      case "bottom":
        return { x: pt.x, y: pt.y + 1 };
      case "left":
        return { x: pt.x - 1, y: pt.y };
      case "right":
        return { x: pt.x + 1, y: pt.y };
    }
  };
  const edgeEntries = [];
  for (let ri_ = 0; ri_ < ir.relations.length; ri_++) {
    const r_ = ir.relations[ri_];
    const isFlipped_ = r_.leftHead === "triangle";
    const a_ = laid.boxes.get(isFlipped_ ? r_.right : r_.left), b_ = laid.boxes.get(isFlipped_ ? r_.left : r_.right);
    if (!a_ || !b_) continue;
    const bends_ = laid.edgeBends.get(ri_);
    const cls_ = classifyEdge(a_, b_, bends_);
    const span_ = Math.abs(a_.x + a_.width / 2 - (b_.x + b_.width / 2)) + Math.abs(a_.y + a_.height / 2 - (b_.y + b_.height / 2));
    edgeEntries.push({ ri: ri_, cls: cls_, span: span_ });
  }
  const classOrder = {
    "skip-cross-column": 0,
    "skip-same-column": 1,
    "direct-cross-column": 2,
    "direct-same-column": 3
  };
  edgeEntries.sort((ea, eb) => classOrder[ea.cls] - classOrder[eb.cls] || eb.span - ea.span);
  for (const { ri, cls } of edgeEntries) {
    const r = ir.relations[ri];
    const isFlipped = r.leftHead === "triangle";
    const a = laid.boxes.get(isFlipped ? r.right : r.left), b = laid.boxes.get(isFlipped ? r.left : r.right);
    if (!a || !b) continue;
    const toWall = approachWall(a, b);
    const toPt = toPortMap2.get(`${b.id}:${toWall}`)?.get(ri) ?? wallPoint2(b, toWall, 0.5, yOff);
    const fromWall = approachWall(b, a);
    const fromPt = fromPortMap2.get(`${a.id}:${fromWall}`)?.get(ri) ?? borderPoint({ ...a, y: a.y + yOff }, toPt.x, toPt.y);
    const bends = laid.edgeBends.get(ri);
    let safePath;
    let labelMid;
    let skipEdgeLateralStrategy = false;
    let effectiveFromPt = fromPt;
    let effectiveFromWall = fromWall;
    let effectiveToPt = toPt;
    let effectiveToWall = toWall;
    if (bends && bends.length > 0) {
      let buildSegmentsA = function(laneX) {
        const fx = fromPt.x, fy = fromPt.y;
        const tx = toPt.x, ty = toPt.y;
        if (Math.abs(laneX - fx) < 1 && Math.abs(laneX - tx) < 1) {
          return [[fx, fy, tx, ty]];
        }
        return [
          [fx, fy, fx, exitY],
          [fx, exitY, laneX, exitY],
          [laneX, exitY, laneX, entryY],
          [laneX, entryY, tx, entryY],
          [tx, entryY, tx, ty]
        ];
      }, buildSegmentsB = function(laneX) {
        return [
          [srcLeft_, srcMidY_, laneX, srcMidY_],
          [laneX, srcMidY_, laneX, tgtMidY_],
          [laneX, tgtMidY_, tgtLeft_, tgtMidY_]
        ];
      }, buildSegmentsC = function(laneX) {
        return [
          [srcRight_, srcMidY_, laneX, srcMidY_],
          [laneX, srcMidY_, laneX, tgtMidY_],
          [laneX, tgtMidY_, tgtRight_, tgtMidY_]
        ];
      }, buildSegmentsD = function(laneX) {
        return [
          [srcLeft_, srcMidY_, laneX, srcMidY_],
          [laneX, srcMidY_, laneX, entryY],
          [laneX, entryY, toPt.x, entryY],
          [toPt.x, entryY, toPt.x, toPt.y]
        ];
      }, buildSegmentsE = function(laneX) {
        return [
          [srcRight_, srcMidY_, laneX, srcMidY_],
          [laneX, srcMidY_, laneX, entryY],
          [laneX, entryY, toPt.x, entryY],
          [toPt.x, entryY, toPt.x, toPt.y]
        ];
      }, buildSegmentsF = function(laneX) {
        return [
          [fromPt.x, fromPt.y, fromPt.x, exitY],
          [fromPt.x, exitY, laneX, exitY],
          [laneX, exitY, laneX, tgtMidY_],
          [laneX, tgtMidY_, tgtLeft_, tgtMidY_]
        ];
      };
      const srcLayerY = a.y;
      const tgtLayerY = b.y;
      const minY = Math.min(srcLayerY, tgtLayerY);
      const maxY = Math.max(srcLayerY, tgtLayerY);
      const interBoxes = allRealBoxes.filter((nb) => {
        const cy = nb.y + nb.height / 2;
        return cy > minY && cy < maxY;
      });
      const firstDummyId = laid.dummyChainIds.get(ri)?.[0] ?? null;
      const sweepCandidates = firstDummyId ? laid.dummySweepXs.get(firstDummyId) ?? [] : [];
      const rightMarginX = Math.max(...allRealBoxes.map((b2) => b2.x + b2.width)) + CLEARANCE;
      const sourceX = fromPt.x;
      const realMinX = Math.min(...allRealBoxes.map((b2) => b2.x));
      const exitY = bends[0].y + yOff;
      const entryY = toPt.y - LAYER_GAP / 2;
      const blockingAtExit = interBoxes.filter((b2) => exitY > b2.y && exitY < b2.y + b2.height);
      const blockingAtEntry = interBoxes.filter((b2) => entryY > b2.y && entryY < b2.y + b2.height);
      const allBlocking = [...blockingAtExit, ...blockingAtEntry];
      const adaptiveLeftX = allBlocking.length > 0 ? Math.min(...allBlocking.map((b2) => b2.x)) - CLEARANCE : realMinX - CLEARANCE;
      const candidates_A = [
        ...sweepCandidates,
        adaptiveLeftX,
        rightMarginX,
        sourceX,
        ...interColMidpoints
      ];
      const srcMidY_ = a.y + a.height / 2 + yOff;
      const tgtMidY_ = b.y + b.height / 2 + yOff;
      const srcLeft_ = a.x;
      const srcRight_ = a.x + a.width;
      const tgtLeft_ = b.x;
      const tgtRight_ = b.x + b.width;
      const interBoxesExt = allRealBoxes.filter((nb) => {
        const cy = nb.y + nb.height / 2;
        return cy > a.y && cy < b.y + b.height && nb.id !== a.id && nb.id !== b.id;
      });
      const blockingBD = allRealBoxes.filter((nb) => {
        const ny = nb.y, nh = nb.height;
        return srcMidY_ > ny && srcMidY_ < ny + nh || tgtMidY_ > ny && tgtMidY_ < ny + nh;
      });
      const adaptiveLeftX_BD = blockingBD.length > 0 ? Math.min(...blockingBD.map((nb) => nb.x)) - LANE_CLEARANCE : realMinX - LANE_CLEARANCE;
      const adaptiveRightX_CE = blockingBD.length > 0 ? Math.max(...blockingBD.map((nb) => nb.x + nb.width)) + LANE_CLEARANCE : rightMarginX;
      const blockingF = allRealBoxes.filter((nb) => {
        return exitY > nb.y && exitY < nb.y + nb.height || tgtMidY_ > nb.y && tgtMidY_ < nb.y + nb.height;
      });
      const adaptiveLeftX_F = blockingF.length > 0 ? Math.min(...blockingF.map((nb) => nb.x)) - LANE_CLEARANCE : realMinX - LANE_CLEARANCE;
      const allCandidates = [];
      for (const laneX of candidates_A) {
        allCandidates.push({
          strategy: "A",
          laneX,
          isMixed: false,
          segments: buildSegmentsA(laneX),
          labelMid: { x: laneX, y: (exitY + entryY) / 2 }
        });
      }
      const minAllowedLaneX = Math.min(srcLeft_, tgtLeft_) - LANE_CLEARANCE;
      const laneXsB = [
        adaptiveLeftX_BD,
        // LANE_CLEARANCE from nearest box
        realMinX - LANE_CLEARANCE * 1.5,
        realMinX - LANE_CLEARANCE * 2,
        realMinX - LANE_CLEARANCE * 3,
        ...sweepCandidates
      ].filter((x) => x <= minAllowedLaneX);
      for (const laneX of laneXsB) {
        allCandidates.push({
          strategy: "B",
          laneX,
          isMixed: false,
          segments: buildSegmentsB(laneX),
          labelMid: { x: laneX, y: (srcMidY_ + tgtMidY_) / 2 }
        });
      }
      const laneXsC = [
        adaptiveRightX_CE,
        rightMarginX,
        rightMarginX + LANE_CLEARANCE * 0.5,
        rightMarginX + LANE_CLEARANCE,
        ...sweepCandidates
      ].filter((x) => x > Math.max(srcRight_, tgtRight_));
      for (const laneX of laneXsC) {
        allCandidates.push({
          strategy: "C",
          laneX,
          isMixed: false,
          segments: buildSegmentsC(laneX),
          labelMid: { x: laneX, y: (srcMidY_ + tgtMidY_) / 2 }
        });
      }
      const laneXsD = [adaptiveLeftX_BD, realMinX - CLEARANCE, ...sweepCandidates].filter(
        (x) => x < srcLeft_
      );
      for (const laneX of laneXsD) {
        allCandidates.push({
          strategy: "D",
          laneX,
          isMixed: true,
          segments: buildSegmentsD(laneX),
          labelMid: { x: laneX, y: (srcMidY_ + entryY) / 2 }
        });
      }
      const laneXsE = [adaptiveRightX_CE, rightMarginX, ...sweepCandidates].filter(
        (x) => x > srcRight_
      );
      for (const laneX of laneXsE) {
        allCandidates.push({
          strategy: "E",
          laneX,
          isMixed: true,
          segments: buildSegmentsE(laneX),
          labelMid: { x: laneX, y: (srcMidY_ + entryY) / 2 }
        });
      }
      const laneXsF = [adaptiveLeftX_F, realMinX - CLEARANCE, ...sweepCandidates].filter(
        (x) => x < tgtLeft_
      );
      for (const laneX of laneXsF) {
        allCandidates.push({
          strategy: "F",
          laneX,
          isMixed: true,
          segments: buildSegmentsF(laneX),
          labelMid: { x: laneX, y: (exitY + tgtMidY_) / 2 }
        });
      }
      let bestScore = Infinity;
      let bestCandidate = allCandidates[0] ?? {
        strategy: "A",
        laneX: bends[0].x,
        isMixed: false,
        segments: buildSegmentsA(bends[0].x),
        labelMid: { x: bends[0].x, y: (exitY + entryY) / 2 }
      };
      for (const c of allCandidates) {
        const sameWallBonus = c.strategy === "B" || c.strategy === "C" ? 30 : 0;
        const score = scoreLane(
          c.laneX,
          c.segments,
          interBoxesExt,
          routedSegments,
          canvasWidth,
          realMinX,
          c.isMixed ? 2 : 0,
          sameWallBonus,
          0,
          c.labelMid
        );
        if (score < bestScore) {
          bestScore = score;
          bestCandidate = c;
        }
      }
      for (const [x1, y1, x2, y2] of bestCandidate.segments) {
        routedSegments.push(toRect(x1, y1, x2, y2));
      }
      skipEdgeLateralStrategy = bestCandidate.strategy === "B" || bestCandidate.strategy === "C";
      switch (bestCandidate.strategy) {
        case "B":
          effectiveFromPt = { x: srcLeft_, y: srcMidY_ };
          effectiveFromWall = "left";
          effectiveToPt = { x: tgtLeft_, y: tgtMidY_ };
          effectiveToWall = "left";
          break;
        case "C":
          effectiveFromPt = { x: srcRight_, y: srcMidY_ };
          effectiveFromWall = "right";
          effectiveToPt = { x: tgtRight_, y: tgtMidY_ };
          effectiveToWall = "right";
          break;
        case "D":
          effectiveFromPt = { x: srcLeft_, y: srcMidY_ };
          effectiveFromWall = "left";
          break;
        case "E":
          effectiveFromPt = { x: srcRight_, y: srcMidY_ };
          effectiveFromWall = "right";
          break;
        case "F":
          effectiveToPt = { x: tgtLeft_, y: tgtMidY_ };
          effectiveToWall = "left";
          break;
      }
      const segs = bestCandidate.segments;
      if (segs.length === 1) {
        safePath = `M ${rhu(segs[0][0])} ${rhu(segs[0][1])} L ${rhu(segs[0][2])} ${rhu(segs[0][3])}`;
      } else {
        const pts = [`M ${rhu(segs[0][0])} ${rhu(segs[0][1])}`];
        for (const [, , x2, y2] of segs) {
          pts.push(`L ${rhu(x2)} ${rhu(y2)}`);
        }
        safePath = pts.join(" ");
      }
      labelMid = bestCandidate.labelMid;
      elements.push(p.path(safePath, palette.textMuted, 1.3, r.dashed ? { dash: "6 4" } : {}));
      const headFrom = isFlipped ? r.rightHead : r.leftHead;
      const headTo = isFlipped ? r.leftHead : r.rightHead;
      elements.push(
        ...endMarker(
          p,
          effectiveFromPt,
          wallDir(effectiveFromWall, effectiveFromPt),
          headFrom,
          palette
        )
      );
      elements.push(
        ...endMarker(p, effectiveToPt, wallDir(effectiveToWall, effectiveToPt), headTo, palette)
      );
    } else {
      const fx = fromPt.x, fy = fromPt.y, tx = toPt.x, ty = toPt.y;
      const realMinX_d = Math.min(...allRealBoxes.map((nb) => nb.x));
      const rightMarginX_d = Math.max(...allRealBoxes.map((nb) => nb.x + nb.width)) + CLEARANCE;
      const interBoxesDirect = allRealBoxes.filter((nb) => nb.id !== a.id && nb.id !== b.id);
      const directCandidates = [];
      if (cls === "direct-same-column") {
        directCandidates.push({
          strategy: "V",
          laneX: fx,
          isMixed: false,
          segments: [[fx, fy, tx, ty]],
          labelMid: { x: fx, y: (fy + ty) / 2 }
        });
        const srcMidY_d = a.y + a.height / 2 + yOff;
        const tgtMidY_d = b.y + b.height / 2 + yOff;
        const srcLeft_d = a.x;
        const srcRight_d = a.x + a.width;
        const tgtLeft_d = b.x;
        const tgtRight_d = b.x + b.width;
        const blockingBD_d = allRealBoxes.filter((nb) => {
          const ny = nb.y, nh = nb.height;
          return srcMidY_d > ny && srcMidY_d < ny + nh || tgtMidY_d > ny && tgtMidY_d < ny + nh;
        });
        const adaptiveLeftX_BD_d = blockingBD_d.length > 0 ? Math.min(...blockingBD_d.map((nb) => nb.x)) - LANE_CLEARANCE : realMinX_d - LANE_CLEARANCE;
        const adaptiveRightX_CE_d = blockingBD_d.length > 0 ? Math.max(...blockingBD_d.map((nb) => nb.x + nb.width)) + LANE_CLEARANCE : rightMarginX_d;
        if (adaptiveLeftX_BD_d < srcLeft_d) {
          directCandidates.push({
            strategy: "B",
            laneX: adaptiveLeftX_BD_d,
            isMixed: false,
            segments: [
              [srcLeft_d, srcMidY_d, adaptiveLeftX_BD_d, srcMidY_d],
              [adaptiveLeftX_BD_d, srcMidY_d, adaptiveLeftX_BD_d, tgtMidY_d],
              [adaptiveLeftX_BD_d, tgtMidY_d, tgtLeft_d, tgtMidY_d]
            ],
            labelMid: { x: adaptiveLeftX_BD_d, y: (srcMidY_d + tgtMidY_d) / 2 }
          });
        }
        if (adaptiveRightX_CE_d > srcRight_d) {
          directCandidates.push({
            strategy: "C",
            laneX: adaptiveRightX_CE_d,
            isMixed: false,
            segments: [
              [srcRight_d, srcMidY_d, adaptiveRightX_CE_d, srcMidY_d],
              [adaptiveRightX_CE_d, srcMidY_d, adaptiveRightX_CE_d, tgtMidY_d],
              [adaptiveRightX_CE_d, tgtMidY_d, tgtRight_d, tgtMidY_d]
            ],
            labelMid: { x: adaptiveRightX_CE_d, y: (srcMidY_d + tgtMidY_d) / 2 }
          });
        }
      } else {
        const midYRaw = [(fy + ty) / 2, fy + LAYER_GAP / 2, ty - LAYER_GAP / 2];
        for (const midY of dedup(midYRaw, 2)) {
          const segsX1 = Math.abs(fx - tx) < 1 ? [[fx, fy, tx, ty]] : [
            [fx, fy, fx, midY],
            [fx, midY, tx, midY],
            [tx, midY, tx, ty]
          ];
          directCandidates.push({
            strategy: "X1",
            laneX: (fx + tx) / 2,
            isMixed: false,
            segments: segsX1,
            labelMid: { x: (fx + tx) / 2, y: midY }
          });
        }
        const midXRaw = [
          ...interColMidpoints.filter((x) => x > Math.min(fx, tx) && x < Math.max(fx, tx)),
          realMinX_d - CLEARANCE,
          rightMarginX_d
        ];
        for (const midX of dedup(midXRaw, 2)) {
          const segsX2 = Math.abs(fy - ty) < 1 ? [[fx, fy, tx, ty]] : [
            [fx, fy, midX, fy],
            [midX, fy, midX, ty],
            [midX, ty, tx, ty]
          ];
          directCandidates.push({
            strategy: "X2",
            laneX: midX,
            isMixed: false,
            segments: segsX2,
            labelMid: { x: midX, y: (fy + ty) / 2 }
          });
        }
      }
      let bestScoreDirect = Infinity;
      let bestDirect = directCandidates[0] ?? null;
      for (const c of directCandidates) {
        const straightBns = c.strategy === "V" ? 40 : 0;
        const boxesForScoring = c.strategy === "X1" || c.strategy === "X2" ? allRealBoxes : interBoxesDirect;
        const score = scoreLane(
          c.laneX,
          c.segments,
          boxesForScoring,
          routedSegments,
          canvasWidth,
          realMinX_d,
          c.isMixed ? 2 : 0,
          0,
          straightBns,
          c.labelMid
        );
        if (score < bestScoreDirect) {
          bestScoreDirect = score;
          bestDirect = c;
        }
      }
      if (bestScoreDirect === Infinity || bestDirect == null || cls === "direct-same-column") {
        const routed = routeEdge(a, b, allBoxes, yOff, fromPt, toPt, true);
        safePath = routed.path || `M ${fromPt.x} ${fromPt.y} L ${toPt.x} ${toPt.y}`;
        labelMid = routed.labelMidpoint;
        routedSegments.push(toRect(fromPt.x, fromPt.y, toPt.x, toPt.y));
      } else {
        for (const [x1, y1, x2, y2] of bestDirect.segments) {
          routedSegments.push(toRect(x1, y1, x2, y2));
        }
        safePath = segmentsToPath(bestDirect.segments);
        labelMid = bestDirect.labelMid;
      }
      elements.push(p.path(safePath, palette.textMuted, 1.3, r.dashed ? { dash: "6 4" } : {}));
      elements.push(
        ...endMarker(
          p,
          effectiveFromPt,
          wallDir(effectiveFromWall, effectiveFromPt),
          isFlipped ? r.rightHead : r.leftHead,
          palette
        )
      );
      elements.push(
        ...endMarker(
          p,
          effectiveToPt,
          wallDir(effectiveToWall, effectiveToPt),
          isFlipped ? r.leftHead : r.rightHead,
          palette
        )
      );
    }
    const mx = labelMid.x, my = labelMid.y;
    if (r.label) {
      let labelX, labelAnchor2;
      if (skipEdgeLateralStrategy && labelMid.x < Math.min(...allRealBoxes.map((b2) => b2.x))) {
        labelX = mx - 4;
        labelAnchor2 = "end";
      } else if (skipEdgeLateralStrategy) {
        labelX = mx + 8;
        labelAnchor2 = "start";
      } else {
        labelX = mx;
        labelAnchor2 = "middle";
      }
      elements.push(
        p.text(r.label, rhuInt(labelX), rhuInt(my - 4), memFont, palette.textMuted, {
          anchor: labelAnchor2
        })
      );
    }
    const cardOffset = (wall, pt) => {
      switch (wall) {
        case "top":
          return { cx: pt.x + 10, cy: pt.y - 10 };
        case "bottom":
          return { cx: pt.x + 10, cy: pt.y + 10 };
        case "left":
          return { cx: pt.x - 10, cy: pt.y - 10 };
        default:
          return { cx: pt.x + 10, cy: pt.y - 10 };
      }
    };
    if (r.leftCard) {
      const o = cardOffset(
        isFlipped ? effectiveToWall : effectiveFromWall,
        isFlipped ? effectiveToPt : effectiveFromPt
      );
      elements.push(p.text(r.leftCard, rhu(o.cx), rhu(o.cy), memFont, palette.textMuted));
    }
    if (r.rightCard) {
      const o = cardOffset(
        isFlipped ? effectiveFromWall : effectiveToWall,
        isFlipped ? effectiveFromPt : effectiveToPt
      );
      elements.push(p.text(r.rightCard, rhu(o.cx), rhu(o.cy), memFont, palette.textMuted));
    }
  }
  for (const c of ir.classes) {
    const box = laid.boxes.get(c.name);
    const s = sizes.get(c.name);
    const x = box.x, y = box.y + yOff;
    elements.push(
      p.rect(
        { x: rhu(x), y: rhu(y), width: rhu(s.w), height: rhu(s.h) },
        palette.surface,
        palette.border,
        1.4,
        { rx: 4 }
      )
    );
    const headerMidY = y + headH / 2;
    let ty = headerMidY + nameFont * 0.35;
    elements.push(
      p.text(c.name, rhuInt(x + s.w / 2), rhu(ty), nameFont, palette.text, {
        weight: "bold",
        anchor: "middle"
      })
    );
    if (c.stereotype) {
      ty += memFont + 2;
      elements.push(
        p.text(`\xAB${c.stereotype}\xBB`, rhuInt(x + s.w / 2), rhu(ty), memFont, palette.textMuted, {
          anchor: "middle"
        })
      );
    }
    const headerBottom = y + headH + (c.stereotype ? memFont + 4 : 0);
    elements.push(
      p.path(
        `M ${rhu(x)} ${rhu(headerBottom)} L ${rhu(x + s.w)} ${rhu(headerBottom)}`,
        palette.border,
        1
      )
    );
    let ay = headerBottom + lineH - 4;
    for (const m of c.attributes) {
      elements.push(p.text(m.text, rhu(x + 8), rhu(ay), memFont, palette.text));
      ay += lineH;
    }
    const attrBottom = y + s.attrH;
    elements.push(
      p.path(
        `M ${rhu(x)} ${rhu(attrBottom)} L ${rhu(x + s.w)} ${rhu(attrBottom)}`,
        palette.border,
        1
      )
    );
    let my2 = attrBottom + lineH - 4;
    for (const m of c.methods) {
      elements.push(p.text(m.text, rhu(x + 8), rhu(my2), memFont, palette.text));
      my2 += lineH;
    }
  }
  const totalW = rhuInt(laid.width + margin);
  const totalH = rhuInt(laid.height + yOff + margin);
  const LABEL_EXTRA = 48;
  const routingMinX = Math.min(0, ...routedSegments.map((s) => s.x1));
  const leftOvershoot = routingMinX < margin ? Math.ceil(margin - routingMinX) + LABEL_EXTRA : 0;
  const scene = applyOverlays(
    {
      viewBox: { x: -leftOvershoot, y: 0, width: totalW + leftOvershoot, height: totalH },
      background: palette.background,
      elements
    },
    ir.overlays,
    theme
  );
  return { scene, anchors: {} };
}
function endMarker(p, at, toward, type, palette) {
  if (type === "none") return [];
  const ang = Math.atan2(at.y - toward.y, at.x - toward.x);
  const back = ang + Math.PI;
  const at2 = (len, spread) => ({
    x: at.x + Math.cos(back + spread) * len,
    y: at.y + Math.sin(back + spread) * len
  });
  const tip = `${rhu(at.x)} ${rhu(at.y)}`;
  if (type === "arrow") {
    const a2 = at2(12, 0.4), b2 = at2(12, -0.4);
    return [
      p.path(
        `M ${rhu(a2.x)} ${rhu(a2.y)} L ${tip} L ${rhu(b2.x)} ${rhu(b2.y)}`,
        palette.textMuted,
        1.4
      )
    ];
  }
  if (type === "triangle") {
    const a2 = at2(14, 0.45), b2 = at2(14, -0.45);
    return [
      p.path(
        `M ${tip} L ${rhu(a2.x)} ${rhu(a2.y)} L ${rhu(b2.x)} ${rhu(b2.y)} Z`,
        palette.textMuted,
        1.3,
        { fill: palette.background }
      )
    ];
  }
  const near = at2(11, 0);
  const a = at2(8, 0.9), b = at2(8, -0.9);
  const fill = type === "diamondF" ? palette.textMuted : palette.background;
  return [
    p.path(
      `M ${tip} L ${rhu(a.x)} ${rhu(a.y)} L ${rhu(near.x)} ${rhu(near.y)} L ${rhu(b.x)} ${rhu(b.y)} Z`,
      palette.textMuted,
      1.3,
      { fill }
    )
  ];
}
function relHeads(tok) {
  const dashed = tok.includes("..");
  let leftHead = "none", rightHead = "none";
  if (tok.startsWith("<|")) leftHead = "triangle";
  else if (tok.startsWith("*")) leftHead = "diamondF";
  else if (tok.startsWith("o")) leftHead = "diamondO";
  else if (tok.startsWith("<")) leftHead = "arrow";
  if (tok.endsWith("|>")) rightHead = "triangle";
  else if (tok.endsWith("*")) rightHead = "diamondF";
  else if (tok.endsWith("o")) rightHead = "diamondO";
  else if (tok.endsWith(">")) rightHead = "arrow";
  return { leftHead, rightHead, dashed };
}
function peg$subclass12(child, parent) {
  function C() {
    this.constructor = child;
  }
  C.prototype = parent.prototype;
  child.prototype = new C();
}
function peg$SyntaxError12(message, expected, found, location) {
  var self = Error.call(this, message);
  if (Object.setPrototypeOf) {
    Object.setPrototypeOf(self, peg$SyntaxError12.prototype);
  }
  self.expected = expected;
  self.found = found;
  self.location = location;
  self.name = "SyntaxError";
  return self;
}
peg$subclass12(peg$SyntaxError12, Error);
function peg$padEnd12(str, targetLength, padString) {
  padString = padString || " ";
  if (str.length > targetLength) {
    return str;
  }
  targetLength -= str.length;
  padString += padString.repeat(targetLength);
  return str + padString.slice(0, targetLength);
}
peg$SyntaxError12.prototype.format = function(sources) {
  var str = "Error: " + this.message;
  if (this.location) {
    var src = null;
    var k;
    for (k = 0; k < sources.length; k++) {
      if (sources[k].source === this.location.source) {
        src = sources[k].text.split(/\r\n|\n|\r/g);
        break;
      }
    }
    var s = this.location.start;
    var offset_s = this.location.source && typeof this.location.source.offset === "function" ? this.location.source.offset(s) : s;
    var loc = this.location.source + ":" + offset_s.line + ":" + offset_s.column;
    if (src) {
      var e = this.location.end;
      var filler = peg$padEnd12("", offset_s.line.toString().length, " ");
      var line = src[s.line - 1];
      var last = s.line === e.line ? e.column : line.length + 1;
      var hatLen = last - s.column || 1;
      str += "\n --> " + loc + "\n" + filler + " |\n" + offset_s.line + " | " + line + "\n" + filler + " | " + peg$padEnd12("", s.column - 1, " ") + peg$padEnd12("", hatLen, "^");
    } else {
      str += "\n at " + loc;
    }
  }
  return str;
};
peg$SyntaxError12.buildMessage = function(expected, found) {
  var DESCRIBE_EXPECTATION_FNS = {
    literal: function(expectation) {
      return '"' + literalEscape(expectation.text) + '"';
    },
    class: function(expectation) {
      var escapedParts = expectation.parts.map(function(part) {
        return Array.isArray(part) ? classEscape(part[0]) + "-" + classEscape(part[1]) : classEscape(part);
      });
      return "[" + (expectation.inverted ? "^" : "") + escapedParts.join("") + "]";
    },
    any: function() {
      return "any character";
    },
    end: function() {
      return "end of input";
    },
    other: function(expectation) {
      return expectation.description;
    }
  };
  function hex(ch) {
    return ch.charCodeAt(0).toString(16).toUpperCase();
  }
  function literalEscape(s) {
    return s.replace(/\\/g, "\\\\").replace(/"/g, '\\"').replace(/\0/g, "\\0").replace(/\t/g, "\\t").replace(/\n/g, "\\n").replace(/\r/g, "\\r").replace(/[\x00-\x0F]/g, function(ch) {
      return "\\x0" + hex(ch);
    }).replace(/[\x10-\x1F\x7F-\x9F]/g, function(ch) {
      return "\\x" + hex(ch);
    });
  }
  function classEscape(s) {
    return s.replace(/\\/g, "\\\\").replace(/\]/g, "\\]").replace(/\^/g, "\\^").replace(/-/g, "\\-").replace(/\0/g, "\\0").replace(/\t/g, "\\t").replace(/\n/g, "\\n").replace(/\r/g, "\\r").replace(/[\x00-\x0F]/g, function(ch) {
      return "\\x0" + hex(ch);
    }).replace(/[\x10-\x1F\x7F-\x9F]/g, function(ch) {
      return "\\x" + hex(ch);
    });
  }
  function describeExpectation(expectation) {
    return DESCRIBE_EXPECTATION_FNS[expectation.type](expectation);
  }
  function describeExpected(expected2) {
    var descriptions = expected2.map(describeExpectation);
    var i, j;
    descriptions.sort();
    if (descriptions.length > 0) {
      for (i = 1, j = 1; i < descriptions.length; i++) {
        if (descriptions[i - 1] !== descriptions[i]) {
          descriptions[j] = descriptions[i];
          j++;
        }
      }
      descriptions.length = j;
    }
    switch (descriptions.length) {
      case 1:
        return descriptions[0];
      case 2:
        return descriptions[0] + " or " + descriptions[1];
      default:
        return descriptions.slice(0, -1).join(", ") + ", or " + descriptions[descriptions.length - 1];
    }
  }
  function describeFound(found2) {
    return found2 ? '"' + literalEscape(found2) + '"' : "end of input";
  }
  return "Expected " + describeExpected(expected) + " but " + describeFound(found) + " found.";
};
function peg$parse12(input, options) {
  options = options !== void 0 ? options : {};
  var peg$FAILED = {};
  var peg$source = options.grammarSource;
  var peg$startRuleFunctions = { Document: peg$parseDocument };
  var peg$startRuleFunction = peg$parseDocument;
  var peg$c0 = "classdiagram";
  var peg$c1 = "-v2";
  var peg$c2 = "---";
  var peg$c3 = "class";
  var peg$c4 = "{";
  var peg$c5 = "\r";
  var peg$c6 = "\n";
  var peg$c7 = "}";
  var peg$c8 = ":";
  var peg$c9 = "note";
  var peg$c10 = "<|--";
  var peg$c11 = "<|..";
  var peg$c12 = "--|>";
  var peg$c13 = "..|>";
  var peg$c14 = "*--";
  var peg$c15 = "--*";
  var peg$c16 = "o--";
  var peg$c17 = "--o";
  var peg$c18 = "<--";
  var peg$c19 = "-->";
  var peg$c20 = "<..";
  var peg$c21 = "..>";
  var peg$c22 = "..";
  var peg$c23 = "--";
  var peg$c24 = '"';
  var peg$r0 = /^[^\n]/;
  var peg$r1 = /^[^"]/;
  var peg$r2 = /^[A-Za-z0-9_~]/;
  var peg$r3 = /^[ \t]/;
  var peg$e0 = peg$literalExpectation("classDiagram", true);
  var peg$e1 = peg$literalExpectation("-v2", false);
  var peg$e2 = peg$literalExpectation("---", false);
  var peg$e3 = peg$classExpectation(["\n"], true, false);
  var peg$e4 = peg$literalExpectation("class", true);
  var peg$e5 = peg$literalExpectation("{", false);
  var peg$e6 = peg$literalExpectation("\r", false);
  var peg$e7 = peg$literalExpectation("\n", false);
  var peg$e8 = peg$literalExpectation("}", false);
  var peg$e9 = peg$literalExpectation(":", false);
  var peg$e10 = peg$literalExpectation("note", true);
  var peg$e11 = peg$literalExpectation("<|--", false);
  var peg$e12 = peg$literalExpectation("<|..", false);
  var peg$e13 = peg$literalExpectation("--|>", false);
  var peg$e14 = peg$literalExpectation("..|>", false);
  var peg$e15 = peg$literalExpectation("*--", false);
  var peg$e16 = peg$literalExpectation("--*", false);
  var peg$e17 = peg$literalExpectation("o--", false);
  var peg$e18 = peg$literalExpectation("--o", false);
  var peg$e19 = peg$literalExpectation("<--", false);
  var peg$e20 = peg$literalExpectation("-->", false);
  var peg$e21 = peg$literalExpectation("<..", false);
  var peg$e22 = peg$literalExpectation("..>", false);
  var peg$e23 = peg$literalExpectation("..", false);
  var peg$e24 = peg$literalExpectation("--", false);
  var peg$e25 = peg$literalExpectation('"', false);
  var peg$e26 = peg$classExpectation(['"'], true, false);
  var peg$e27 = peg$classExpectation([["A", "Z"], ["a", "z"], ["0", "9"], "_", "~"], false, false);
  var peg$e28 = peg$anyExpectation();
  var peg$e29 = peg$classExpectation([" ", "	"], false, false);
  var peg$f0 = function(fm, stmts) {
    const classes = [];
    const relations = [];
    const ensure = (name) => {
      let c = classes.find((x) => x.name === name);
      if (!c) {
        c = { name, attributes: [], methods: [] };
        classes.push(c);
      }
      return c;
    };
    const addMember = (c, raw) => {
      const s = raw.trim();
      if (!s) return;
      if (s.startsWith("<<")) {
        c.stereotype = s.replace(/[<>]/g, "").trim();
        return;
      }
      const isMethod = s.includes("(");
      (isMethod ? c.methods : c.attributes).push({ text: s, isMethod });
    };
    for (const st of stmts.filter(Boolean)) {
      if (st.t === "class") {
        const c = ensure(st.name);
        for (const m of st.members) addMember(c, m);
      } else if (st.t === "member") addMember(ensure(st.name), st.content);
      else if (st.t === "relation") {
        ensure(st.left);
        ensure(st.right);
        const { leftHead, rightHead, dashed } = relHeads(st.tok);
        relations.push({
          left: st.left,
          right: st.right,
          leftHead,
          rightHead,
          dashed,
          ...st.label ? { label: st.label } : {},
          ...st.lc ? { leftCard: st.lc } : {},
          ...st.rc ? { rightCard: st.rc } : {}
        });
      }
    }
    return { version: "1.0", metadata: { ...fm || {} }, classes, relations };
  };
  var peg$f1 = function(line) {
    return line;
  };
  var peg$f2 = function(lines2) {
    const meta = {};
    for (const line of lines2) {
      const m = line.match(/^\s*(\w+)\s*:\s*(.+?)\s*$/);
      if (m) meta[m[1]] = m[2];
    }
    return meta;
  };
  var peg$f3 = function(name, rows) {
    return { t: "class", name, members: rows.filter(Boolean) };
  };
  var peg$f4 = function(content) {
    return content.trim();
  };
  var peg$f5 = function() {
    return null;
  };
  var peg$f6 = function(name, content) {
    return { t: "member", name, content: content.trim() };
  };
  var peg$f7 = function(left, lc, tok, rc, right, l) {
    return l.trim();
  };
  var peg$f8 = function(left, lc, tok, rc, right, label) {
    return { t: "relation", left, right, tok, lc: lc || void 0, rc: rc || void 0, label: label || void 0 };
  };
  var peg$f9 = function() {
    return null;
  };
  var peg$f10 = function(s) {
    return s;
  };
  var peg$f11 = function() {
    return null;
  };
  var peg$currPos = options.peg$currPos | 0;
  var peg$savedPos = peg$currPos;
  var peg$posDetailsCache = [{ line: 1, column: 1 }];
  var peg$maxFailPos = peg$currPos;
  var peg$maxFailExpected = options.peg$maxFailExpected || [];
  var peg$silentFails = options.peg$silentFails | 0;
  var peg$result;
  if (options.startRule) {
    if (!(options.startRule in peg$startRuleFunctions)) {
      throw new Error(`Can't start parsing from rule "` + options.startRule + '".');
    }
    peg$startRuleFunction = peg$startRuleFunctions[options.startRule];
  }
  function text() {
    return input.substring(peg$savedPos, peg$currPos);
  }
  function offset() {
    return peg$savedPos;
  }
  function range() {
    return {
      source: peg$source,
      start: peg$savedPos,
      end: peg$currPos
    };
  }
  function location() {
    return peg$computeLocation(peg$savedPos, peg$currPos);
  }
  function expected(description, location2) {
    location2 = location2 !== void 0 ? location2 : peg$computeLocation(peg$savedPos, peg$currPos);
    throw peg$buildStructuredError(
      [peg$otherExpectation(description)],
      input.substring(peg$savedPos, peg$currPos),
      location2
    );
  }
  function error(message, location2) {
    location2 = location2 !== void 0 ? location2 : peg$computeLocation(peg$savedPos, peg$currPos);
    throw peg$buildSimpleError(message, location2);
  }
  function peg$literalExpectation(text2, ignoreCase) {
    return { type: "literal", text: text2, ignoreCase };
  }
  function peg$classExpectation(parts, inverted, ignoreCase) {
    return { type: "class", parts, inverted, ignoreCase };
  }
  function peg$anyExpectation() {
    return { type: "any" };
  }
  function peg$endExpectation() {
    return { type: "end" };
  }
  function peg$otherExpectation(description) {
    return { type: "other", description };
  }
  function peg$computePosDetails(pos) {
    var details = peg$posDetailsCache[pos];
    var p;
    if (details) {
      return details;
    } else {
      if (pos >= peg$posDetailsCache.length) {
        p = peg$posDetailsCache.length - 1;
      } else {
        p = pos;
        while (!peg$posDetailsCache[--p]) {
        }
      }
      details = peg$posDetailsCache[p];
      details = {
        line: details.line,
        column: details.column
      };
      while (p < pos) {
        if (input.charCodeAt(p) === 10) {
          details.line++;
          details.column = 1;
        } else {
          details.column++;
        }
        p++;
      }
      peg$posDetailsCache[pos] = details;
      return details;
    }
  }
  function peg$computeLocation(startPos, endPos, offset2) {
    var startPosDetails = peg$computePosDetails(startPos);
    var endPosDetails = peg$computePosDetails(endPos);
    var res = {
      source: peg$source,
      start: {
        offset: startPos,
        line: startPosDetails.line,
        column: startPosDetails.column
      },
      end: {
        offset: endPos,
        line: endPosDetails.line,
        column: endPosDetails.column
      }
    };
    if (offset2 && peg$source && typeof peg$source.offset === "function") {
      res.start = peg$source.offset(res.start);
      res.end = peg$source.offset(res.end);
    }
    return res;
  }
  function peg$fail(expected2) {
    if (peg$currPos < peg$maxFailPos) {
      return;
    }
    if (peg$currPos > peg$maxFailPos) {
      peg$maxFailPos = peg$currPos;
      peg$maxFailExpected = [];
    }
    peg$maxFailExpected.push(expected2);
  }
  function peg$buildSimpleError(message, location2) {
    return new peg$SyntaxError12(message, null, null, location2);
  }
  function peg$buildStructuredError(expected2, found, location2) {
    return new peg$SyntaxError12(
      peg$SyntaxError12.buildMessage(expected2, found),
      expected2,
      found,
      location2
    );
  }
  function peg$parseDocument() {
    var s0, s1, s2, s3, s4, s5, s6, s7;
    s0 = peg$currPos;
    s1 = peg$parseFrontmatter();
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    s2 = peg$parse_();
    s3 = input.substr(peg$currPos, 12);
    if (s3.toLowerCase() === peg$c0) {
      peg$currPos += 12;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e0);
      }
    }
    if (s3 !== peg$FAILED) {
      if (input.substr(peg$currPos, 3) === peg$c1) {
        s4 = peg$c1;
        peg$currPos += 3;
      } else {
        s4 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e1);
        }
      }
      if (s4 === peg$FAILED) {
        s4 = null;
      }
      s5 = peg$parseLineEnd();
      if (s5 !== peg$FAILED) {
        s6 = [];
        s7 = peg$parseStatement();
        while (s7 !== peg$FAILED) {
          s6.push(s7);
          s7 = peg$parseStatement();
        }
        s7 = peg$parse_();
        peg$savedPos = s0;
        s0 = peg$f0(s1, s6);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseFrontmatter() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 3) === peg$c2) {
      s1 = peg$c2;
      peg$currPos += 3;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e2);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$parseNL();
      if (s2 !== peg$FAILED) {
        s3 = [];
        s4 = peg$currPos;
        s5 = peg$currPos;
        peg$silentFails++;
        s6 = peg$currPos;
        if (input.substr(peg$currPos, 3) === peg$c2) {
          s7 = peg$c2;
          peg$currPos += 3;
        } else {
          s7 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e2);
          }
        }
        if (s7 !== peg$FAILED) {
          s8 = peg$parseNL();
          if (s8 !== peg$FAILED) {
            s7 = [s7, s8];
            s6 = s7;
          } else {
            peg$currPos = s6;
            s6 = peg$FAILED;
          }
        } else {
          peg$currPos = s6;
          s6 = peg$FAILED;
        }
        peg$silentFails--;
        if (s6 === peg$FAILED) {
          s5 = void 0;
        } else {
          peg$currPos = s5;
          s5 = peg$FAILED;
        }
        if (s5 !== peg$FAILED) {
          s6 = peg$currPos;
          s7 = [];
          s8 = input.charAt(peg$currPos);
          if (peg$r0.test(s8)) {
            peg$currPos++;
          } else {
            s8 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e3);
            }
          }
          while (s8 !== peg$FAILED) {
            s7.push(s8);
            s8 = input.charAt(peg$currPos);
            if (peg$r0.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e3);
              }
            }
          }
          s6 = input.substring(s6, peg$currPos);
          s7 = peg$parseNL();
          if (s7 !== peg$FAILED) {
            peg$savedPos = s4;
            s4 = peg$f1(s6);
          } else {
            peg$currPos = s4;
            s4 = peg$FAILED;
          }
        } else {
          peg$currPos = s4;
          s4 = peg$FAILED;
        }
        while (s4 !== peg$FAILED) {
          s3.push(s4);
          s4 = peg$currPos;
          s5 = peg$currPos;
          peg$silentFails++;
          s6 = peg$currPos;
          if (input.substr(peg$currPos, 3) === peg$c2) {
            s7 = peg$c2;
            peg$currPos += 3;
          } else {
            s7 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e2);
            }
          }
          if (s7 !== peg$FAILED) {
            s8 = peg$parseNL();
            if (s8 !== peg$FAILED) {
              s7 = [s7, s8];
              s6 = s7;
            } else {
              peg$currPos = s6;
              s6 = peg$FAILED;
            }
          } else {
            peg$currPos = s6;
            s6 = peg$FAILED;
          }
          peg$silentFails--;
          if (s6 === peg$FAILED) {
            s5 = void 0;
          } else {
            peg$currPos = s5;
            s5 = peg$FAILED;
          }
          if (s5 !== peg$FAILED) {
            s6 = peg$currPos;
            s7 = [];
            s8 = input.charAt(peg$currPos);
            if (peg$r0.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e3);
              }
            }
            while (s8 !== peg$FAILED) {
              s7.push(s8);
              s8 = input.charAt(peg$currPos);
              if (peg$r0.test(s8)) {
                peg$currPos++;
              } else {
                s8 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e3);
                }
              }
            }
            s6 = input.substring(s6, peg$currPos);
            s7 = peg$parseNL();
            if (s7 !== peg$FAILED) {
              peg$savedPos = s4;
              s4 = peg$f1(s6);
            } else {
              peg$currPos = s4;
              s4 = peg$FAILED;
            }
          } else {
            peg$currPos = s4;
            s4 = peg$FAILED;
          }
        }
        if (input.substr(peg$currPos, 3) === peg$c2) {
          s4 = peg$c2;
          peg$currPos += 3;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e2);
          }
        }
        if (s4 !== peg$FAILED) {
          s5 = peg$parseNL();
          if (s5 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f2(s3);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseStatement() {
    var s0;
    s0 = peg$parseClassBlock();
    if (s0 === peg$FAILED) {
      s0 = peg$parseRelation();
      if (s0 === peg$FAILED) {
        s0 = peg$parseClassMemberLine();
        if (s0 === peg$FAILED) {
          s0 = peg$parseNoteLine();
          if (s0 === peg$FAILED) {
            s0 = peg$parseBlankLine();
          }
        }
      }
    }
    return s0;
  }
  function peg$parseClassBlock() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9, s10, s11, s12, s13;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = input.substr(peg$currPos, 5);
    if (s2.toLowerCase() === peg$c3) {
      peg$currPos += 5;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e4);
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$parse__();
      if (s3 !== peg$FAILED) {
        s4 = peg$parseIdent();
        if (s4 !== peg$FAILED) {
          s5 = peg$parse_();
          if (input.charCodeAt(peg$currPos) === 123) {
            s6 = peg$c4;
            peg$currPos++;
          } else {
            s6 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e5);
            }
          }
          if (s6 !== peg$FAILED) {
            s7 = peg$parse_();
            if (input.charCodeAt(peg$currPos) === 13) {
              s8 = peg$c5;
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e6);
              }
            }
            if (s8 === peg$FAILED) {
              s8 = null;
            }
            if (input.charCodeAt(peg$currPos) === 10) {
              s9 = peg$c6;
              peg$currPos++;
            } else {
              s9 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e7);
              }
            }
            if (s9 !== peg$FAILED) {
              s10 = [];
              s11 = peg$parseMemberRow();
              while (s11 !== peg$FAILED) {
                s10.push(s11);
                s11 = peg$parseMemberRow();
              }
              s11 = peg$parse_();
              if (input.charCodeAt(peg$currPos) === 125) {
                s12 = peg$c7;
                peg$currPos++;
              } else {
                s12 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e8);
                }
              }
              if (s12 !== peg$FAILED) {
                s13 = peg$parseLineEnd();
                if (s13 !== peg$FAILED) {
                  peg$savedPos = s0;
                  s0 = peg$f3(s4, s10);
                } else {
                  peg$currPos = s0;
                  s0 = peg$FAILED;
                }
              } else {
                peg$currPos = s0;
                s0 = peg$FAILED;
              }
            } else {
              peg$currPos = s0;
              s0 = peg$FAILED;
            }
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseMemberRow() {
    var s0, s1, s2, s3, s4, s5;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = peg$currPos;
    peg$silentFails++;
    if (input.charCodeAt(peg$currPos) === 125) {
      s3 = peg$c7;
      peg$currPos++;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e8);
      }
    }
    peg$silentFails--;
    if (s3 === peg$FAILED) {
      s2 = void 0;
    } else {
      peg$currPos = s2;
      s2 = peg$FAILED;
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$currPos;
      s4 = [];
      s5 = input.charAt(peg$currPos);
      if (peg$r0.test(s5)) {
        peg$currPos++;
      } else {
        s5 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e3);
        }
      }
      if (s5 !== peg$FAILED) {
        while (s5 !== peg$FAILED) {
          s4.push(s5);
          s5 = input.charAt(peg$currPos);
          if (peg$r0.test(s5)) {
            peg$currPos++;
          } else {
            s5 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e3);
            }
          }
        }
      } else {
        s4 = peg$FAILED;
      }
      if (s4 !== peg$FAILED) {
        s3 = input.substring(s3, peg$currPos);
      } else {
        s3 = s4;
      }
      if (s3 !== peg$FAILED) {
        if (input.charCodeAt(peg$currPos) === 13) {
          s4 = peg$c5;
          peg$currPos++;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e6);
          }
        }
        if (s4 === peg$FAILED) {
          s4 = null;
        }
        if (input.charCodeAt(peg$currPos) === 10) {
          s5 = peg$c6;
          peg$currPos++;
        } else {
          s5 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e7);
          }
        }
        if (s5 !== peg$FAILED) {
          peg$savedPos = s0;
          s0 = peg$f4(s3);
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      s1 = peg$parse_();
      if (input.charCodeAt(peg$currPos) === 13) {
        s2 = peg$c5;
        peg$currPos++;
      } else {
        s2 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e6);
        }
      }
      if (s2 === peg$FAILED) {
        s2 = null;
      }
      if (input.charCodeAt(peg$currPos) === 10) {
        s3 = peg$c6;
        peg$currPos++;
      } else {
        s3 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e7);
        }
      }
      if (s3 !== peg$FAILED) {
        peg$savedPos = s0;
        s0 = peg$f5();
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    }
    return s0;
  }
  function peg$parseClassMemberLine() {
    var s0, s1, s2, s3, s4, s5, s6, s7;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = peg$parseIdent();
    if (s2 !== peg$FAILED) {
      s3 = peg$parse_();
      if (input.charCodeAt(peg$currPos) === 58) {
        s4 = peg$c8;
        peg$currPos++;
      } else {
        s4 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e9);
        }
      }
      if (s4 !== peg$FAILED) {
        s5 = peg$parse_();
        s6 = peg$parseRestOfLine();
        if (s6 !== peg$FAILED) {
          s7 = peg$parseLineEnd();
          if (s7 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f6(s2, s6);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseRelation() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9, s10, s11, s12, s13, s14, s15;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = peg$parseIdent();
    if (s2 !== peg$FAILED) {
      s3 = peg$parse_();
      s4 = peg$parseCardStr();
      if (s4 === peg$FAILED) {
        s4 = null;
      }
      s5 = peg$parse_();
      s6 = peg$parseRelTok();
      if (s6 !== peg$FAILED) {
        s7 = peg$parse_();
        s8 = peg$parseCardStr();
        if (s8 === peg$FAILED) {
          s8 = null;
        }
        s9 = peg$parse_();
        s10 = peg$parseIdent();
        if (s10 !== peg$FAILED) {
          s11 = peg$currPos;
          s12 = peg$parse_();
          if (input.charCodeAt(peg$currPos) === 58) {
            s13 = peg$c8;
            peg$currPos++;
          } else {
            s13 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e9);
            }
          }
          if (s13 !== peg$FAILED) {
            s14 = peg$parse_();
            s15 = peg$parseRestOfLine();
            if (s15 !== peg$FAILED) {
              peg$savedPos = s11;
              s11 = peg$f7(s2, s4, s6, s8, s10, s15);
            } else {
              peg$currPos = s11;
              s11 = peg$FAILED;
            }
          } else {
            peg$currPos = s11;
            s11 = peg$FAILED;
          }
          if (s11 === peg$FAILED) {
            s11 = null;
          }
          s12 = peg$parseLineEnd();
          if (s12 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f8(s2, s4, s6, s8, s10, s11);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseNoteLine() {
    var s0, s1, s2, s3, s4, s5, s6;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = input.substr(peg$currPos, 4);
    if (s2.toLowerCase() === peg$c9) {
      peg$currPos += 4;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e10);
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$parse__();
      if (s3 !== peg$FAILED) {
        s4 = peg$currPos;
        s5 = [];
        s6 = input.charAt(peg$currPos);
        if (peg$r0.test(s6)) {
          peg$currPos++;
        } else {
          s6 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e3);
          }
        }
        if (s6 !== peg$FAILED) {
          while (s6 !== peg$FAILED) {
            s5.push(s6);
            s6 = input.charAt(peg$currPos);
            if (peg$r0.test(s6)) {
              peg$currPos++;
            } else {
              s6 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e3);
              }
            }
          }
        } else {
          s5 = peg$FAILED;
        }
        if (s5 !== peg$FAILED) {
          s4 = input.substring(s4, peg$currPos);
        } else {
          s4 = s5;
        }
        if (s4 !== peg$FAILED) {
          s5 = peg$parseLineEnd();
          if (s5 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f9();
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseRelTok() {
    var s0, s1;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 4) === peg$c10) {
      s1 = peg$c10;
      peg$currPos += 4;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e11);
      }
    }
    if (s1 === peg$FAILED) {
      if (input.substr(peg$currPos, 4) === peg$c11) {
        s1 = peg$c11;
        peg$currPos += 4;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e12);
        }
      }
      if (s1 === peg$FAILED) {
        if (input.substr(peg$currPos, 4) === peg$c12) {
          s1 = peg$c12;
          peg$currPos += 4;
        } else {
          s1 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e13);
          }
        }
        if (s1 === peg$FAILED) {
          if (input.substr(peg$currPos, 4) === peg$c13) {
            s1 = peg$c13;
            peg$currPos += 4;
          } else {
            s1 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e14);
            }
          }
          if (s1 === peg$FAILED) {
            if (input.substr(peg$currPos, 3) === peg$c14) {
              s1 = peg$c14;
              peg$currPos += 3;
            } else {
              s1 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e15);
              }
            }
            if (s1 === peg$FAILED) {
              if (input.substr(peg$currPos, 3) === peg$c15) {
                s1 = peg$c15;
                peg$currPos += 3;
              } else {
                s1 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e16);
                }
              }
              if (s1 === peg$FAILED) {
                if (input.substr(peg$currPos, 3) === peg$c16) {
                  s1 = peg$c16;
                  peg$currPos += 3;
                } else {
                  s1 = peg$FAILED;
                  if (peg$silentFails === 0) {
                    peg$fail(peg$e17);
                  }
                }
                if (s1 === peg$FAILED) {
                  if (input.substr(peg$currPos, 3) === peg$c17) {
                    s1 = peg$c17;
                    peg$currPos += 3;
                  } else {
                    s1 = peg$FAILED;
                    if (peg$silentFails === 0) {
                      peg$fail(peg$e18);
                    }
                  }
                  if (s1 === peg$FAILED) {
                    if (input.substr(peg$currPos, 3) === peg$c18) {
                      s1 = peg$c18;
                      peg$currPos += 3;
                    } else {
                      s1 = peg$FAILED;
                      if (peg$silentFails === 0) {
                        peg$fail(peg$e19);
                      }
                    }
                    if (s1 === peg$FAILED) {
                      if (input.substr(peg$currPos, 3) === peg$c19) {
                        s1 = peg$c19;
                        peg$currPos += 3;
                      } else {
                        s1 = peg$FAILED;
                        if (peg$silentFails === 0) {
                          peg$fail(peg$e20);
                        }
                      }
                      if (s1 === peg$FAILED) {
                        if (input.substr(peg$currPos, 3) === peg$c20) {
                          s1 = peg$c20;
                          peg$currPos += 3;
                        } else {
                          s1 = peg$FAILED;
                          if (peg$silentFails === 0) {
                            peg$fail(peg$e21);
                          }
                        }
                        if (s1 === peg$FAILED) {
                          if (input.substr(peg$currPos, 3) === peg$c21) {
                            s1 = peg$c21;
                            peg$currPos += 3;
                          } else {
                            s1 = peg$FAILED;
                            if (peg$silentFails === 0) {
                              peg$fail(peg$e22);
                            }
                          }
                          if (s1 === peg$FAILED) {
                            if (input.substr(peg$currPos, 2) === peg$c22) {
                              s1 = peg$c22;
                              peg$currPos += 2;
                            } else {
                              s1 = peg$FAILED;
                              if (peg$silentFails === 0) {
                                peg$fail(peg$e23);
                              }
                            }
                            if (s1 === peg$FAILED) {
                              if (input.substr(peg$currPos, 2) === peg$c23) {
                                s1 = peg$c23;
                                peg$currPos += 2;
                              } else {
                                s1 = peg$FAILED;
                                if (peg$silentFails === 0) {
                                  peg$fail(peg$e24);
                                }
                              }
                            }
                          }
                        }
                      }
                    }
                  }
                }
              }
            }
          }
        }
      }
    }
    if (s1 !== peg$FAILED) {
      s0 = input.substring(s0, peg$currPos);
    } else {
      s0 = s1;
    }
    return s0;
  }
  function peg$parseCardStr() {
    var s0, s1, s2, s3, s4;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 34) {
      s1 = peg$c24;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e25);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$currPos;
      s3 = [];
      s4 = input.charAt(peg$currPos);
      if (peg$r1.test(s4)) {
        peg$currPos++;
      } else {
        s4 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e26);
        }
      }
      while (s4 !== peg$FAILED) {
        s3.push(s4);
        s4 = input.charAt(peg$currPos);
        if (peg$r1.test(s4)) {
          peg$currPos++;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e26);
          }
        }
      }
      s2 = input.substring(s2, peg$currPos);
      if (input.charCodeAt(peg$currPos) === 34) {
        s3 = peg$c24;
        peg$currPos++;
      } else {
        s3 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e25);
        }
      }
      if (s3 !== peg$FAILED) {
        peg$savedPos = s0;
        s0 = peg$f10(s2);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseIdent() {
    var s0, s1, s2;
    s0 = peg$currPos;
    s1 = [];
    s2 = input.charAt(peg$currPos);
    if (peg$r2.test(s2)) {
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e27);
      }
    }
    if (s2 !== peg$FAILED) {
      while (s2 !== peg$FAILED) {
        s1.push(s2);
        s2 = input.charAt(peg$currPos);
        if (peg$r2.test(s2)) {
          peg$currPos++;
        } else {
          s2 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e27);
          }
        }
      }
    } else {
      s1 = peg$FAILED;
    }
    if (s1 !== peg$FAILED) {
      s0 = input.substring(s0, peg$currPos);
    } else {
      s0 = s1;
    }
    return s0;
  }
  function peg$parseRestOfLine() {
    var s0, s1, s2;
    s0 = peg$currPos;
    s1 = [];
    s2 = input.charAt(peg$currPos);
    if (peg$r0.test(s2)) {
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e3);
      }
    }
    if (s2 !== peg$FAILED) {
      while (s2 !== peg$FAILED) {
        s1.push(s2);
        s2 = input.charAt(peg$currPos);
        if (peg$r0.test(s2)) {
          peg$currPos++;
        } else {
          s2 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e3);
          }
        }
      }
    } else {
      s1 = peg$FAILED;
    }
    if (s1 !== peg$FAILED) {
      s0 = input.substring(s0, peg$currPos);
    } else {
      s0 = s1;
    }
    return s0;
  }
  function peg$parseBlankLine() {
    var s0, s1, s2, s3;
    s0 = peg$currPos;
    s1 = peg$parse_();
    if (input.charCodeAt(peg$currPos) === 13) {
      s2 = peg$c5;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e6);
      }
    }
    if (s2 === peg$FAILED) {
      s2 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s3 = peg$c6;
      peg$currPos++;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e7);
      }
    }
    if (s3 !== peg$FAILED) {
      peg$savedPos = s0;
      s0 = peg$f11();
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseNL() {
    var s0, s1, s2;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 13) {
      s1 = peg$c5;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e6);
      }
    }
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s2 = peg$c6;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e7);
      }
    }
    if (s2 !== peg$FAILED) {
      s1 = [s1, s2];
      s0 = s1;
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseLineEnd() {
    var s0, s1, s2;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 13) {
      s1 = peg$c5;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e6);
      }
    }
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s2 = peg$c6;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e7);
      }
    }
    if (s2 !== peg$FAILED) {
      s1 = [s1, s2];
      s0 = s1;
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      peg$silentFails++;
      if (input.length > peg$currPos) {
        s1 = input.charAt(peg$currPos);
        peg$currPos++;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e28);
        }
      }
      peg$silentFails--;
      if (s1 === peg$FAILED) {
        s0 = void 0;
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    }
    return s0;
  }
  function peg$parse_() {
    var s0, s1;
    s0 = [];
    s1 = input.charAt(peg$currPos);
    if (peg$r3.test(s1)) {
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e29);
      }
    }
    while (s1 !== peg$FAILED) {
      s0.push(s1);
      s1 = input.charAt(peg$currPos);
      if (peg$r3.test(s1)) {
        peg$currPos++;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e29);
        }
      }
    }
    return s0;
  }
  function peg$parse__() {
    var s0, s1;
    s0 = [];
    s1 = input.charAt(peg$currPos);
    if (peg$r3.test(s1)) {
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e29);
      }
    }
    if (s1 !== peg$FAILED) {
      while (s1 !== peg$FAILED) {
        s0.push(s1);
        s1 = input.charAt(peg$currPos);
        if (peg$r3.test(s1)) {
          peg$currPos++;
        } else {
          s1 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e29);
          }
        }
      }
    } else {
      s0 = peg$FAILED;
    }
    return s0;
  }
  peg$result = peg$startRuleFunction();
  if (options.peg$library) {
    return (
      /** @type {any} */
      {
        peg$result,
        peg$currPos,
        peg$FAILED,
        peg$maxFailExpected,
        peg$maxFailPos
      }
    );
  }
  if (peg$result !== peg$FAILED && peg$currPos === input.length) {
    return peg$result;
  } else {
    if (peg$result !== peg$FAILED && peg$currPos < input.length) {
      peg$fail(peg$endExpectation());
    }
    throw peg$buildStructuredError(
      peg$maxFailExpected,
      peg$maxFailPos < input.length ? input.charAt(peg$maxFailPos) : null,
      peg$maxFailPos < input.length ? peg$computeLocation(peg$maxFailPos, peg$maxFailPos + 1) : peg$computeLocation(peg$maxFailPos, peg$maxFailPos)
    );
  }
}
var classDiagram = {
  parseMermaid(input) {
    return peg$parse12(input);
  },
  parseYaml(input) {
    return JSON.parse(input);
  },
  layout(ir, theme) {
    return layoutClass(ir, theme);
  }
};
var ARROW_ID4 = "state-arrow";
function layoutState(ir, theme) {
  const { palette, typography, spacing } = theme;
  const p = pen(theme);
  const margin = spacing.diagramMargin;
  const font = typography.baseFontSize;
  const smallFont = typography.smallFontSize;
  const stateInfos = /* @__PURE__ */ new Map();
  const sizeOf = (s2) => {
    if (s2.kind === "start" || s2.kind === "end") return { w: 22, h: 22 };
    if (s2.kind === "choice") return { w: 34, h: 34 };
    const info = measureFormattedText(s2.label, font, smallFont, 240, 4);
    stateInfos.set(s2.id, info);
    const w = Math.max(76, Math.ceil(info.maxLineWidth + 28));
    const h2 = info.lineCount > 1 ? rhu(info.lineCount * (font * 1.25) + 16) : 38;
    return { w, h: h2 };
  };
  const sizes = new Map(ir.states.map((s2) => [s2.id, sizeOf(s2)]));
  const composites = ir.composites ?? [];
  const compositeIds = new Set(composites.map((c) => c.id));
  const compositeMap = new Map(composites.map((c) => [c.id, c]));
  const innerPseudostates = /* @__PURE__ */ new Set();
  for (const comp of composites) {
    for (const id of comp.nodeIds) {
      if (id.startsWith("__start_") || id.startsWith("__end_")) {
        innerPseudostates.add(id);
      }
    }
  }
  const compositeMemberIds = /* @__PURE__ */ new Map();
  for (const comp of composites) {
    const realMembers = comp.nodeIds.filter((id) => !innerPseudostates.has(id));
    compositeMemberIds.set(comp.id, realMembers.length > 0 ? realMembers : comp.nodeIds);
  }
  const layoutStates = ir.states.filter(
    (s2) => !compositeIds.has(s2.id) && !innerPseudostates.has(s2.id)
  );
  const nodes = layoutStates.map((s2) => ({
    id: s2.id,
    width: sizes.get(s2.id).w,
    height: sizes.get(s2.id).h
  }));
  const edges2 = [];
  for (const t of ir.transitions) {
    if (innerPseudostates.has(t.from) || innerPseudostates.has(t.to)) {
      continue;
    }
    let from = t.from;
    let to = t.to;
    const fromMembers = compositeMemberIds.get(from);
    if (fromMembers && fromMembers.length > 0) {
      from = fromMembers[fromMembers.length - 1];
    }
    const toMembers = compositeMemberIds.get(to);
    if (toMembers && toMembers.length > 0) {
      to = toMembers[0];
    }
    edges2.push({ from, to });
  }
  const laid = layeredLayout(nodes, edges2, { direction: "TB", layerGap: 56, nodeGap: 40, margin });
  const title = ir.metadata.title;
  const titleH = title ? typography.titleFontSize + 14 : 0;
  const yOff = titleH;
  const elements = [];
  if (title)
    elements.push(
      p.text(
        title,
        margin,
        margin + typography.titleFontSize,
        typography.titleFontSize,
        palette.text,
        { weight: "bold" }
      )
    );
  const containerRect = /* @__PURE__ */ new Map();
  const hidden = /* @__PURE__ */ new Set();
  for (const comp of composites) {
    const members = compositeMemberIds.get(comp.id) ?? comp.nodeIds;
    const memberIdSet = new Set(members);
    const rects = members.map((id) => laid.boxes.get(id)).filter((r) => !!r);
    if (rects.length === 0) continue;
    const pad = 16;
    let minX = Math.min(...rects.map((r) => r.x)) - pad;
    let minY = Math.min(...rects.map((r) => r.y)) - pad - 14 + yOff;
    let maxX = Math.max(...rects.map((r) => r.x + r.width)) + pad;
    let maxY = Math.max(...rects.map((r) => r.y + r.height)) + pad + yOff;
    for (const [id, box] of laid.boxes) {
      if (memberIdSet.has(id) || hidden.has(id)) continue;
      const nmMinY = box.y;
      const nmMaxY = box.y + box.height;
      const rawMinY = Math.min(...rects.map((r) => r.y)) - pad;
      const rawMaxY = Math.max(...rects.map((r) => r.y + r.height)) + pad;
      if (nmMaxY <= rawMinY || nmMinY >= rawMaxY) continue;
      const nmLeft = box.x, nmRight = box.x + box.width;
      const memberCentreX = (Math.min(...rects.map((r) => r.x)) + Math.max(...rects.map((r) => r.x + r.width))) / 2;
      if (nmLeft < maxX && nmRight > minX) {
        if (nmRight <= memberCentreX) {
          minX = Math.max(minX, nmRight + 4);
        } else {
          maxX = Math.min(maxX, nmLeft - 4);
        }
      }
    }
    const rect = { x: minX, y: minY, width: maxX - minX, height: maxY - minY };
    containerRect.set(comp.id, rect);
    hidden.add(comp.id);
    elements.push(
      p.rect(
        { x: rhu(minX), y: rhu(minY), width: rhu(rect.width), height: rhu(rect.height) },
        palette.surface,
        palette.primary,
        1.4,
        { rx: 10, opacity: 0.5 }
      )
    );
    elements.push(
      p.text(comp.label, rhu(minX + 12), rhu(minY + 16), typography.smallFontSize, palette.text, {
        weight: "bold"
      })
    );
  }
  const rectFor = (id) => {
    const c = containerRect.get(id);
    if (c) return c;
    const b = laid.boxes.get(id);
    return b ? { x: b.x, y: b.y + yOff, width: b.width, height: b.height } : void 0;
  };
  const allBoxes = [...laid.boxes.values()];
  let validEdgeIdx = 0;
  for (const t of ir.transitions) {
    if (innerPseudostates.has(t.from) || innerPseudostates.has(t.to)) continue;
    const currentEdgeIdx = validEdgeIdx++;
    const a = rectFor(t.from), b = rectFor(t.to);
    if (!a || !b) continue;
    const ac = { x: a.x + a.width / 2, y: a.y + a.height / 2 };
    const bc = { x: b.x + b.width / 2, y: b.y + b.height / 2 };
    const pa = borderPoint(a, bc.x, bc.y);
    const pb = borderPoint(b, ac.x, ac.y);
    const fromBox = laid.boxes.get(t.from), toBox = laid.boxes.get(t.to);
    let edgePath;
    const bends = laid.edgeBends.get(currentEdgeIdx);
    if (bends && bends.length > 0 && fromBox && toBox) {
      const fromPt = { x: a.x + a.width / 2, y: a.y + a.height };
      const boxesAbove = allBoxes.filter((box) => box.y + box.height + yOff <= b.y);
      const lowestObstacleBottom = boxesAbove.length > 0 ? Math.max(...boxesAbove.map((box) => box.y + box.height + yOff)) : fromPt.y;
      const turnY = (lowestObstacleBottom + b.y) / 2;
      const targetPt = borderPoint(b, fromPt.x, turnY);
      const pts = [fromPt, { x: fromPt.x, y: turnY }, targetPt];
      edgePath = pts.map((p2, idx) => `${idx === 0 ? "M" : "L"} ${rhu(p2.x)} ${rhu(p2.y)}`).join(" ");
    } else if (fromBox && toBox) {
      ({ path: edgePath } = routeEdge(fromBox, toBox, allBoxes, yOff));
    } else {
      edgePath = `M ${rhu(pa.x)} ${rhu(pa.y)} L ${rhu(pb.x)} ${rhu(pb.y)}`;
    }
    elements.push(p.path(edgePath, palette.textMuted, 1.4, { markerEnd: ARROW_ID4 }));
    if (t.label) {
      let mx = (pa.x + pb.x) / 2, my = (pa.y + pb.y) / 2;
      for (const [compId, cr] of containerRect) {
        const comp = composites.find((c) => c.id === compId);
        if (!comp) continue;
        const fromInside = comp.nodeIds.includes(t.from);
        const toInside = comp.nodeIds.includes(t.to);
        if (fromInside && toInside) continue;
        if (!fromInside && !toInside) continue;
        if (mx > cr.x && mx < cr.x + cr.width && my > cr.y && my < cr.y + cr.height) {
          my = cr.y - typography.smallFontSize - 4;
          break;
        }
      }
      const w = measureText(t.label, typography.smallFontSize).width + 8;
      elements.push(
        p.rect(
          {
            x: rhu(mx - w / 2),
            y: rhu(my - typography.smallFontSize),
            width: rhu(w),
            height: typography.smallFontSize + 4
          },
          palette.background,
          palette.background,
          0
        )
      );
      elements.push(
        p.text(t.label, rhuInt(mx), rhu(my - 1), typography.smallFontSize, palette.textMuted, {
          anchor: "middle"
        })
      );
    }
  }
  for (const s2 of ir.states) {
    if (hidden.has(s2.id) || innerPseudostates.has(s2.id)) continue;
    const box = laid.boxes.get(s2.id);
    const x = box.x, y = box.y + yOff;
    const cx = x + box.width / 2, cy = y + box.height / 2;
    if (s2.kind === "start") {
      elements.push(p.circle({ x: rhu(cx), y: rhu(cy) }, 8, palette.text, palette.text, 0));
    } else if (s2.kind === "end") {
      elements.push(p.circle({ x: rhu(cx), y: rhu(cy) }, 10, "none", palette.text, 1.5));
      elements.push(p.circle({ x: rhu(cx), y: rhu(cy) }, 5, palette.text, palette.text, 0));
    } else if (s2.kind === "choice") {
      const r = box.width / 2;
      elements.push(
        p.path(
          `M ${rhu(cx)} ${rhu(cy - r)} L ${rhu(cx + r)} ${rhu(cy)} L ${rhu(cx)} ${rhu(cy + r)} L ${rhu(cx - r)} ${rhu(cy)} Z`,
          palette.border,
          1.4,
          { fill: palette.surface }
        )
      );
    } else {
      elements.push(
        p.rect(
          { x: rhu(x), y: rhu(y), width: rhu(box.width), height: rhu(box.height) },
          palette.surface,
          palette.primary,
          1.4,
          { rx: 10 }
        )
      );
      const info = stateInfos.get(s2.id);
      if (info) {
        elements.push(
          ...renderFormattedText(
            p,
            info,
            x,
            y,
            box.width,
            box.height,
            font,
            smallFont,
            palette.text,
            palette.textMuted,
            { align: "middle", defaultBold: true }
          )
        );
      }
    }
  }
  const totalW = rhuInt(laid.width + margin);
  const totalH = rhuInt(laid.height + yOff + margin);
  const s = theme.edges?.arrowSize ?? 8;
  const sH = rhu(s * 0.7);
  const sMidY = rhu(s * 0.35);
  const sRefX = rhu(s - 1);
  const defs = [
    `<marker id="${ARROW_ID4}" markerWidth="${s}" markerHeight="${sH}" refX="${sRefX}" refY="${sMidY}" orient="auto"><polygon points="0 0, ${s} ${sMidY}, 0 ${sH}" fill="${palette.textMuted}" /></marker>`
  ];
  const scene = applyOverlays(
    {
      viewBox: { x: 0, y: 0, width: totalW, height: totalH },
      background: palette.background,
      elements,
      defs
    },
    ir.overlays,
    theme
  );
  return { scene, anchors: {} };
}
function collect(stmts, states, transitions, composites, scope) {
  const ensure = (id, label, kind) => {
    let s = states.find((x) => x.id === id);
    if (!s) {
      s = { id, label: label || id, kind: kind || "normal" };
      states.push(s);
    } else {
      if (label) s.label = label;
      if (kind && s.kind === "normal") s.kind = kind;
    }
    return s;
  };
  const ref = (r, side) => {
    if (r === "[*]") {
      const id = side === "from" ? "__start__" : "__end__";
      ensure(id, "", side === "from" ? "start" : "end");
      return id;
    }
    ensure(r);
    return r;
  };
  const localIds = /* @__PURE__ */ new Set();
  for (const st of stmts.filter(Boolean)) {
    if (st.t === "choice") {
      ensure(st.id, st.id, "choice");
      localIds.add(st.id);
    } else if (st.t === "state") {
      ensure(st.id, st.label || st.id, "normal");
      localIds.add(st.id);
    } else if (st.t === "trans") {
      if (scope && (st.from === "[*]" || st.to === "[*]")) {
        if (st.from !== "[*]") {
          ensure(st.from);
          localIds.add(st.from);
        }
        if (st.to !== "[*]") {
          ensure(st.to);
          localIds.add(st.to);
        }
      } else {
        const f2 = ref(st.from, "from"), to = ref(st.to, "to");
        transitions.push({ from: f2, to, ...st.label ? { label: st.label } : {} });
        if (f2 !== "__start__" && f2 !== "__end__") localIds.add(f2);
        if (to !== "__start__" && to !== "__end__") localIds.add(to);
      }
    } else if (st.t === "block") {
      const innerIds = collect(st.inner, states, transitions, composites, st.name);
      const memberIds = [...innerIds];
      composites.push({ id: st.name, label: st.name, nodeIds: memberIds });
      for (const id of innerIds) localIds.add(id);
    }
  }
  return localIds;
}
function peg$subclass13(child, parent) {
  function C() {
    this.constructor = child;
  }
  C.prototype = parent.prototype;
  child.prototype = new C();
}
function peg$SyntaxError13(message, expected, found, location) {
  var self = Error.call(this, message);
  if (Object.setPrototypeOf) {
    Object.setPrototypeOf(self, peg$SyntaxError13.prototype);
  }
  self.expected = expected;
  self.found = found;
  self.location = location;
  self.name = "SyntaxError";
  return self;
}
peg$subclass13(peg$SyntaxError13, Error);
function peg$padEnd13(str, targetLength, padString) {
  padString = padString || " ";
  if (str.length > targetLength) {
    return str;
  }
  targetLength -= str.length;
  padString += padString.repeat(targetLength);
  return str + padString.slice(0, targetLength);
}
peg$SyntaxError13.prototype.format = function(sources) {
  var str = "Error: " + this.message;
  if (this.location) {
    var src = null;
    var k;
    for (k = 0; k < sources.length; k++) {
      if (sources[k].source === this.location.source) {
        src = sources[k].text.split(/\r\n|\n|\r/g);
        break;
      }
    }
    var s = this.location.start;
    var offset_s = this.location.source && typeof this.location.source.offset === "function" ? this.location.source.offset(s) : s;
    var loc = this.location.source + ":" + offset_s.line + ":" + offset_s.column;
    if (src) {
      var e = this.location.end;
      var filler = peg$padEnd13("", offset_s.line.toString().length, " ");
      var line = src[s.line - 1];
      var last = s.line === e.line ? e.column : line.length + 1;
      var hatLen = last - s.column || 1;
      str += "\n --> " + loc + "\n" + filler + " |\n" + offset_s.line + " | " + line + "\n" + filler + " | " + peg$padEnd13("", s.column - 1, " ") + peg$padEnd13("", hatLen, "^");
    } else {
      str += "\n at " + loc;
    }
  }
  return str;
};
peg$SyntaxError13.buildMessage = function(expected, found) {
  var DESCRIBE_EXPECTATION_FNS = {
    literal: function(expectation) {
      return '"' + literalEscape(expectation.text) + '"';
    },
    class: function(expectation) {
      var escapedParts = expectation.parts.map(function(part) {
        return Array.isArray(part) ? classEscape(part[0]) + "-" + classEscape(part[1]) : classEscape(part);
      });
      return "[" + (expectation.inverted ? "^" : "") + escapedParts.join("") + "]";
    },
    any: function() {
      return "any character";
    },
    end: function() {
      return "end of input";
    },
    other: function(expectation) {
      return expectation.description;
    }
  };
  function hex(ch) {
    return ch.charCodeAt(0).toString(16).toUpperCase();
  }
  function literalEscape(s) {
    return s.replace(/\\/g, "\\\\").replace(/"/g, '\\"').replace(/\0/g, "\\0").replace(/\t/g, "\\t").replace(/\n/g, "\\n").replace(/\r/g, "\\r").replace(/[\x00-\x0F]/g, function(ch) {
      return "\\x0" + hex(ch);
    }).replace(/[\x10-\x1F\x7F-\x9F]/g, function(ch) {
      return "\\x" + hex(ch);
    });
  }
  function classEscape(s) {
    return s.replace(/\\/g, "\\\\").replace(/\]/g, "\\]").replace(/\^/g, "\\^").replace(/-/g, "\\-").replace(/\0/g, "\\0").replace(/\t/g, "\\t").replace(/\n/g, "\\n").replace(/\r/g, "\\r").replace(/[\x00-\x0F]/g, function(ch) {
      return "\\x0" + hex(ch);
    }).replace(/[\x10-\x1F\x7F-\x9F]/g, function(ch) {
      return "\\x" + hex(ch);
    });
  }
  function describeExpectation(expectation) {
    return DESCRIBE_EXPECTATION_FNS[expectation.type](expectation);
  }
  function describeExpected(expected2) {
    var descriptions = expected2.map(describeExpectation);
    var i, j;
    descriptions.sort();
    if (descriptions.length > 0) {
      for (i = 1, j = 1; i < descriptions.length; i++) {
        if (descriptions[i - 1] !== descriptions[i]) {
          descriptions[j] = descriptions[i];
          j++;
        }
      }
      descriptions.length = j;
    }
    switch (descriptions.length) {
      case 1:
        return descriptions[0];
      case 2:
        return descriptions[0] + " or " + descriptions[1];
      default:
        return descriptions.slice(0, -1).join(", ") + ", or " + descriptions[descriptions.length - 1];
    }
  }
  function describeFound(found2) {
    return found2 ? '"' + literalEscape(found2) + '"' : "end of input";
  }
  return "Expected " + describeExpected(expected) + " but " + describeFound(found) + " found.";
};
function peg$parse13(input, options) {
  options = options !== void 0 ? options : {};
  var peg$FAILED = {};
  var peg$source = options.grammarSource;
  var peg$startRuleFunctions = { Document: peg$parseDocument };
  var peg$startRuleFunction = peg$parseDocument;
  var peg$c0 = "statediagram";
  var peg$c1 = "-v2";
  var peg$c2 = "---";
  var peg$c3 = "state";
  var peg$c4 = "{";
  var peg$c5 = "\r";
  var peg$c6 = "\n";
  var peg$c7 = "}";
  var peg$c8 = "<<";
  var peg$c9 = "choice";
  var peg$c10 = "fork";
  var peg$c11 = "join";
  var peg$c12 = ">>";
  var peg$c13 = "-->";
  var peg$c14 = ":";
  var peg$c15 = "as";
  var peg$c16 = "direction";
  var peg$c17 = "note";
  var peg$c18 = "[*]";
  var peg$c19 = '"';
  var peg$r0 = /^[^\n]/;
  var peg$r1 = /^[^"]/;
  var peg$r2 = /^[A-Za-z0-9_]/;
  var peg$r3 = /^[ \t]/;
  var peg$e0 = peg$literalExpectation("stateDiagram", true);
  var peg$e1 = peg$literalExpectation("-v2", false);
  var peg$e2 = peg$literalExpectation("---", false);
  var peg$e3 = peg$classExpectation(["\n"], true, false);
  var peg$e4 = peg$literalExpectation("state", true);
  var peg$e5 = peg$literalExpectation("{", false);
  var peg$e6 = peg$literalExpectation("\r", false);
  var peg$e7 = peg$literalExpectation("\n", false);
  var peg$e8 = peg$literalExpectation("}", false);
  var peg$e9 = peg$literalExpectation("<<", false);
  var peg$e10 = peg$literalExpectation("choice", false);
  var peg$e11 = peg$literalExpectation("fork", false);
  var peg$e12 = peg$literalExpectation("join", false);
  var peg$e13 = peg$literalExpectation(">>", false);
  var peg$e14 = peg$literalExpectation("-->", false);
  var peg$e15 = peg$literalExpectation(":", false);
  var peg$e16 = peg$literalExpectation("as", true);
  var peg$e17 = peg$literalExpectation("direction", true);
  var peg$e18 = peg$literalExpectation("note", true);
  var peg$e19 = peg$literalExpectation("[*]", false);
  var peg$e20 = peg$literalExpectation('"', false);
  var peg$e21 = peg$classExpectation(['"'], true, false);
  var peg$e22 = peg$classExpectation([["A", "Z"], ["a", "z"], ["0", "9"], "_"], false, false);
  var peg$e23 = peg$anyExpectation();
  var peg$e24 = peg$classExpectation([" ", "	"], false, false);
  var peg$f0 = function(fm, stmts) {
    const states = [];
    const transitions = [];
    const composites = [];
    collect(stmts, states, transitions, composites);
    return { version: "1.0", metadata: { ...fm || {} }, states, transitions, ...composites.length ? { composites } : {} };
  };
  var peg$f1 = function(line) {
    return line;
  };
  var peg$f2 = function(lines2) {
    const meta = {};
    for (const line of lines2) {
      const m = line.match(/^\s*(\w+)\s*:\s*(.+?)\s*$/);
      if (m) meta[m[1]] = m[2];
    }
    return meta;
  };
  var peg$f3 = function(name, inner) {
    return { t: "block", name, inner };
  };
  var peg$f4 = function(id) {
    return { t: "choice", id };
  };
  var peg$f5 = function(from, to, l) {
    return l.trim();
  };
  var peg$f6 = function(from, to, label) {
    return { t: "trans", from, to, label: label || void 0 };
  };
  var peg$f7 = function(label, id) {
    return { t: "state", id, label };
  };
  var peg$f8 = function(id) {
    return { t: "state", id, label: id };
  };
  var peg$f9 = function(id, label) {
    return { t: "state", id, label: label.trim() };
  };
  var peg$f10 = function() {
    return null;
  };
  var peg$f11 = function() {
    return "[*]";
  };
  var peg$f12 = function(s) {
    return s;
  };
  var peg$f13 = function() {
    return null;
  };
  var peg$currPos = options.peg$currPos | 0;
  var peg$savedPos = peg$currPos;
  var peg$posDetailsCache = [{ line: 1, column: 1 }];
  var peg$maxFailPos = peg$currPos;
  var peg$maxFailExpected = options.peg$maxFailExpected || [];
  var peg$silentFails = options.peg$silentFails | 0;
  var peg$result;
  if (options.startRule) {
    if (!(options.startRule in peg$startRuleFunctions)) {
      throw new Error(`Can't start parsing from rule "` + options.startRule + '".');
    }
    peg$startRuleFunction = peg$startRuleFunctions[options.startRule];
  }
  function text() {
    return input.substring(peg$savedPos, peg$currPos);
  }
  function offset() {
    return peg$savedPos;
  }
  function range() {
    return {
      source: peg$source,
      start: peg$savedPos,
      end: peg$currPos
    };
  }
  function location() {
    return peg$computeLocation(peg$savedPos, peg$currPos);
  }
  function expected(description, location2) {
    location2 = location2 !== void 0 ? location2 : peg$computeLocation(peg$savedPos, peg$currPos);
    throw peg$buildStructuredError(
      [peg$otherExpectation(description)],
      input.substring(peg$savedPos, peg$currPos),
      location2
    );
  }
  function error(message, location2) {
    location2 = location2 !== void 0 ? location2 : peg$computeLocation(peg$savedPos, peg$currPos);
    throw peg$buildSimpleError(message, location2);
  }
  function peg$literalExpectation(text2, ignoreCase) {
    return { type: "literal", text: text2, ignoreCase };
  }
  function peg$classExpectation(parts, inverted, ignoreCase) {
    return { type: "class", parts, inverted, ignoreCase };
  }
  function peg$anyExpectation() {
    return { type: "any" };
  }
  function peg$endExpectation() {
    return { type: "end" };
  }
  function peg$otherExpectation(description) {
    return { type: "other", description };
  }
  function peg$computePosDetails(pos) {
    var details = peg$posDetailsCache[pos];
    var p;
    if (details) {
      return details;
    } else {
      if (pos >= peg$posDetailsCache.length) {
        p = peg$posDetailsCache.length - 1;
      } else {
        p = pos;
        while (!peg$posDetailsCache[--p]) {
        }
      }
      details = peg$posDetailsCache[p];
      details = {
        line: details.line,
        column: details.column
      };
      while (p < pos) {
        if (input.charCodeAt(p) === 10) {
          details.line++;
          details.column = 1;
        } else {
          details.column++;
        }
        p++;
      }
      peg$posDetailsCache[pos] = details;
      return details;
    }
  }
  function peg$computeLocation(startPos, endPos, offset2) {
    var startPosDetails = peg$computePosDetails(startPos);
    var endPosDetails = peg$computePosDetails(endPos);
    var res = {
      source: peg$source,
      start: {
        offset: startPos,
        line: startPosDetails.line,
        column: startPosDetails.column
      },
      end: {
        offset: endPos,
        line: endPosDetails.line,
        column: endPosDetails.column
      }
    };
    if (offset2 && peg$source && typeof peg$source.offset === "function") {
      res.start = peg$source.offset(res.start);
      res.end = peg$source.offset(res.end);
    }
    return res;
  }
  function peg$fail(expected2) {
    if (peg$currPos < peg$maxFailPos) {
      return;
    }
    if (peg$currPos > peg$maxFailPos) {
      peg$maxFailPos = peg$currPos;
      peg$maxFailExpected = [];
    }
    peg$maxFailExpected.push(expected2);
  }
  function peg$buildSimpleError(message, location2) {
    return new peg$SyntaxError13(message, null, null, location2);
  }
  function peg$buildStructuredError(expected2, found, location2) {
    return new peg$SyntaxError13(
      peg$SyntaxError13.buildMessage(expected2, found),
      expected2,
      found,
      location2
    );
  }
  function peg$parseDocument() {
    var s0, s1, s2, s3, s4, s5, s6, s7;
    s0 = peg$currPos;
    s1 = peg$parseFrontmatter();
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    s2 = peg$parse_();
    s3 = input.substr(peg$currPos, 12);
    if (s3.toLowerCase() === peg$c0) {
      peg$currPos += 12;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e0);
      }
    }
    if (s3 !== peg$FAILED) {
      if (input.substr(peg$currPos, 3) === peg$c1) {
        s4 = peg$c1;
        peg$currPos += 3;
      } else {
        s4 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e1);
        }
      }
      if (s4 === peg$FAILED) {
        s4 = null;
      }
      s5 = peg$parseLineEnd();
      if (s5 !== peg$FAILED) {
        s6 = [];
        s7 = peg$parseStatement();
        while (s7 !== peg$FAILED) {
          s6.push(s7);
          s7 = peg$parseStatement();
        }
        s7 = peg$parse_();
        peg$savedPos = s0;
        s0 = peg$f0(s1, s6);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseFrontmatter() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 3) === peg$c2) {
      s1 = peg$c2;
      peg$currPos += 3;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e2);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$parseNL();
      if (s2 !== peg$FAILED) {
        s3 = [];
        s4 = peg$currPos;
        s5 = peg$currPos;
        peg$silentFails++;
        s6 = peg$currPos;
        if (input.substr(peg$currPos, 3) === peg$c2) {
          s7 = peg$c2;
          peg$currPos += 3;
        } else {
          s7 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e2);
          }
        }
        if (s7 !== peg$FAILED) {
          s8 = peg$parseNL();
          if (s8 !== peg$FAILED) {
            s7 = [s7, s8];
            s6 = s7;
          } else {
            peg$currPos = s6;
            s6 = peg$FAILED;
          }
        } else {
          peg$currPos = s6;
          s6 = peg$FAILED;
        }
        peg$silentFails--;
        if (s6 === peg$FAILED) {
          s5 = void 0;
        } else {
          peg$currPos = s5;
          s5 = peg$FAILED;
        }
        if (s5 !== peg$FAILED) {
          s6 = peg$currPos;
          s7 = [];
          s8 = input.charAt(peg$currPos);
          if (peg$r0.test(s8)) {
            peg$currPos++;
          } else {
            s8 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e3);
            }
          }
          while (s8 !== peg$FAILED) {
            s7.push(s8);
            s8 = input.charAt(peg$currPos);
            if (peg$r0.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e3);
              }
            }
          }
          s6 = input.substring(s6, peg$currPos);
          s7 = peg$parseNL();
          if (s7 !== peg$FAILED) {
            peg$savedPos = s4;
            s4 = peg$f1(s6);
          } else {
            peg$currPos = s4;
            s4 = peg$FAILED;
          }
        } else {
          peg$currPos = s4;
          s4 = peg$FAILED;
        }
        while (s4 !== peg$FAILED) {
          s3.push(s4);
          s4 = peg$currPos;
          s5 = peg$currPos;
          peg$silentFails++;
          s6 = peg$currPos;
          if (input.substr(peg$currPos, 3) === peg$c2) {
            s7 = peg$c2;
            peg$currPos += 3;
          } else {
            s7 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e2);
            }
          }
          if (s7 !== peg$FAILED) {
            s8 = peg$parseNL();
            if (s8 !== peg$FAILED) {
              s7 = [s7, s8];
              s6 = s7;
            } else {
              peg$currPos = s6;
              s6 = peg$FAILED;
            }
          } else {
            peg$currPos = s6;
            s6 = peg$FAILED;
          }
          peg$silentFails--;
          if (s6 === peg$FAILED) {
            s5 = void 0;
          } else {
            peg$currPos = s5;
            s5 = peg$FAILED;
          }
          if (s5 !== peg$FAILED) {
            s6 = peg$currPos;
            s7 = [];
            s8 = input.charAt(peg$currPos);
            if (peg$r0.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e3);
              }
            }
            while (s8 !== peg$FAILED) {
              s7.push(s8);
              s8 = input.charAt(peg$currPos);
              if (peg$r0.test(s8)) {
                peg$currPos++;
              } else {
                s8 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e3);
                }
              }
            }
            s6 = input.substring(s6, peg$currPos);
            s7 = peg$parseNL();
            if (s7 !== peg$FAILED) {
              peg$savedPos = s4;
              s4 = peg$f1(s6);
            } else {
              peg$currPos = s4;
              s4 = peg$FAILED;
            }
          } else {
            peg$currPos = s4;
            s4 = peg$FAILED;
          }
        }
        if (input.substr(peg$currPos, 3) === peg$c2) {
          s4 = peg$c2;
          peg$currPos += 3;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e2);
          }
        }
        if (s4 !== peg$FAILED) {
          s5 = peg$parseNL();
          if (s5 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f2(s3);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseStatement() {
    var s0;
    s0 = peg$parseStateBlock();
    if (s0 === peg$FAILED) {
      s0 = peg$parseChoiceDecl();
      if (s0 === peg$FAILED) {
        s0 = peg$parseTransition();
        if (s0 === peg$FAILED) {
          s0 = peg$parseStateDecl();
          if (s0 === peg$FAILED) {
            s0 = peg$parseStateDescDecl();
            if (s0 === peg$FAILED) {
              s0 = peg$parseDirectiveLine();
              if (s0 === peg$FAILED) {
                s0 = peg$parseBlankLine();
              }
            }
          }
        }
      }
    }
    return s0;
  }
  function peg$parseStateBlock() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9, s10, s11, s12, s13;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = input.substr(peg$currPos, 5);
    if (s2.toLowerCase() === peg$c3) {
      peg$currPos += 5;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e4);
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$parse__();
      if (s3 !== peg$FAILED) {
        s4 = peg$parseIdent();
        if (s4 !== peg$FAILED) {
          s5 = peg$parse_();
          if (input.charCodeAt(peg$currPos) === 123) {
            s6 = peg$c4;
            peg$currPos++;
          } else {
            s6 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e5);
            }
          }
          if (s6 !== peg$FAILED) {
            s7 = peg$parse_();
            if (input.charCodeAt(peg$currPos) === 13) {
              s8 = peg$c5;
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e6);
              }
            }
            if (s8 === peg$FAILED) {
              s8 = null;
            }
            if (input.charCodeAt(peg$currPos) === 10) {
              s9 = peg$c6;
              peg$currPos++;
            } else {
              s9 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e7);
              }
            }
            if (s9 !== peg$FAILED) {
              s10 = [];
              s11 = peg$parseStatement();
              while (s11 !== peg$FAILED) {
                s10.push(s11);
                s11 = peg$parseStatement();
              }
              s11 = peg$parse_();
              if (input.charCodeAt(peg$currPos) === 125) {
                s12 = peg$c7;
                peg$currPos++;
              } else {
                s12 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e8);
                }
              }
              if (s12 !== peg$FAILED) {
                s13 = peg$parseLineEnd();
                if (s13 !== peg$FAILED) {
                  peg$savedPos = s0;
                  s0 = peg$f3(s4, s10);
                } else {
                  peg$currPos = s0;
                  s0 = peg$FAILED;
                }
              } else {
                peg$currPos = s0;
                s0 = peg$FAILED;
              }
            } else {
              peg$currPos = s0;
              s0 = peg$FAILED;
            }
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseChoiceDecl() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = input.substr(peg$currPos, 5);
    if (s2.toLowerCase() === peg$c3) {
      peg$currPos += 5;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e4);
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$parse__();
      if (s3 !== peg$FAILED) {
        s4 = peg$parseIdent();
        if (s4 !== peg$FAILED) {
          s5 = peg$parse__();
          if (s5 !== peg$FAILED) {
            if (input.substr(peg$currPos, 2) === peg$c8) {
              s6 = peg$c8;
              peg$currPos += 2;
            } else {
              s6 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e9);
              }
            }
            if (s6 !== peg$FAILED) {
              if (input.substr(peg$currPos, 6) === peg$c9) {
                s7 = peg$c9;
                peg$currPos += 6;
              } else {
                s7 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e10);
                }
              }
              if (s7 === peg$FAILED) {
                if (input.substr(peg$currPos, 4) === peg$c10) {
                  s7 = peg$c10;
                  peg$currPos += 4;
                } else {
                  s7 = peg$FAILED;
                  if (peg$silentFails === 0) {
                    peg$fail(peg$e11);
                  }
                }
                if (s7 === peg$FAILED) {
                  if (input.substr(peg$currPos, 4) === peg$c11) {
                    s7 = peg$c11;
                    peg$currPos += 4;
                  } else {
                    s7 = peg$FAILED;
                    if (peg$silentFails === 0) {
                      peg$fail(peg$e12);
                    }
                  }
                }
              }
              if (s7 !== peg$FAILED) {
                if (input.substr(peg$currPos, 2) === peg$c12) {
                  s8 = peg$c12;
                  peg$currPos += 2;
                } else {
                  s8 = peg$FAILED;
                  if (peg$silentFails === 0) {
                    peg$fail(peg$e13);
                  }
                }
                if (s8 !== peg$FAILED) {
                  s9 = peg$parseLineEnd();
                  if (s9 !== peg$FAILED) {
                    peg$savedPos = s0;
                    s0 = peg$f4(s4);
                  } else {
                    peg$currPos = s0;
                    s0 = peg$FAILED;
                  }
                } else {
                  peg$currPos = s0;
                  s0 = peg$FAILED;
                }
              } else {
                peg$currPos = s0;
                s0 = peg$FAILED;
              }
            } else {
              peg$currPos = s0;
              s0 = peg$FAILED;
            }
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseTransition() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9, s10, s11;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = peg$parseStateRef();
    if (s2 !== peg$FAILED) {
      s3 = peg$parse_();
      if (input.substr(peg$currPos, 3) === peg$c13) {
        s4 = peg$c13;
        peg$currPos += 3;
      } else {
        s4 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e14);
        }
      }
      if (s4 !== peg$FAILED) {
        s5 = peg$parse_();
        s6 = peg$parseStateRef();
        if (s6 !== peg$FAILED) {
          s7 = peg$currPos;
          s8 = peg$parse_();
          if (input.charCodeAt(peg$currPos) === 58) {
            s9 = peg$c14;
            peg$currPos++;
          } else {
            s9 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e15);
            }
          }
          if (s9 !== peg$FAILED) {
            s10 = peg$parse_();
            s11 = peg$parseRestOfLine();
            if (s11 !== peg$FAILED) {
              peg$savedPos = s7;
              s7 = peg$f5(s2, s6, s11);
            } else {
              peg$currPos = s7;
              s7 = peg$FAILED;
            }
          } else {
            peg$currPos = s7;
            s7 = peg$FAILED;
          }
          if (s7 === peg$FAILED) {
            s7 = null;
          }
          s8 = peg$parseLineEnd();
          if (s8 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f6(s2, s6, s7);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseStateDecl() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = input.substr(peg$currPos, 5);
    if (s2.toLowerCase() === peg$c3) {
      peg$currPos += 5;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e4);
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$parse__();
      if (s3 !== peg$FAILED) {
        s4 = peg$parseQuotedOrId();
        if (s4 !== peg$FAILED) {
          s5 = peg$parse__();
          if (s5 !== peg$FAILED) {
            s6 = input.substr(peg$currPos, 2);
            if (s6.toLowerCase() === peg$c15) {
              peg$currPos += 2;
            } else {
              s6 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e16);
              }
            }
            if (s6 !== peg$FAILED) {
              s7 = peg$parse__();
              if (s7 !== peg$FAILED) {
                s8 = peg$parseIdent();
                if (s8 !== peg$FAILED) {
                  s9 = peg$parseLineEnd();
                  if (s9 !== peg$FAILED) {
                    peg$savedPos = s0;
                    s0 = peg$f7(s4, s8);
                  } else {
                    peg$currPos = s0;
                    s0 = peg$FAILED;
                  }
                } else {
                  peg$currPos = s0;
                  s0 = peg$FAILED;
                }
              } else {
                peg$currPos = s0;
                s0 = peg$FAILED;
              }
            } else {
              peg$currPos = s0;
              s0 = peg$FAILED;
            }
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      s1 = peg$parse_();
      s2 = input.substr(peg$currPos, 5);
      if (s2.toLowerCase() === peg$c3) {
        peg$currPos += 5;
      } else {
        s2 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e4);
        }
      }
      if (s2 !== peg$FAILED) {
        s3 = peg$parse__();
        if (s3 !== peg$FAILED) {
          s4 = peg$parseIdent();
          if (s4 !== peg$FAILED) {
            s5 = peg$parseLineEnd();
            if (s5 !== peg$FAILED) {
              peg$savedPos = s0;
              s0 = peg$f8(s4);
            } else {
              peg$currPos = s0;
              s0 = peg$FAILED;
            }
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    }
    return s0;
  }
  function peg$parseStateDescDecl() {
    var s0, s1, s2, s3, s4, s5, s6, s7;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = peg$parseIdent();
    if (s2 !== peg$FAILED) {
      s3 = peg$parse_();
      if (input.charCodeAt(peg$currPos) === 58) {
        s4 = peg$c14;
        peg$currPos++;
      } else {
        s4 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e15);
        }
      }
      if (s4 !== peg$FAILED) {
        s5 = peg$parse_();
        s6 = peg$parseRestOfLine();
        if (s6 !== peg$FAILED) {
          s7 = peg$parseLineEnd();
          if (s7 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f9(s2, s6);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseDirectiveLine() {
    var s0, s1, s2, s3, s4, s5, s6;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = input.substr(peg$currPos, 9);
    if (s2.toLowerCase() === peg$c16) {
      peg$currPos += 9;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e17);
      }
    }
    if (s2 === peg$FAILED) {
      s2 = input.substr(peg$currPos, 4);
      if (s2.toLowerCase() === peg$c17) {
        peg$currPos += 4;
      } else {
        s2 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e18);
        }
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$parse__();
      if (s3 !== peg$FAILED) {
        s4 = peg$currPos;
        s5 = [];
        s6 = input.charAt(peg$currPos);
        if (peg$r0.test(s6)) {
          peg$currPos++;
        } else {
          s6 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e3);
          }
        }
        if (s6 !== peg$FAILED) {
          while (s6 !== peg$FAILED) {
            s5.push(s6);
            s6 = input.charAt(peg$currPos);
            if (peg$r0.test(s6)) {
              peg$currPos++;
            } else {
              s6 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e3);
              }
            }
          }
        } else {
          s5 = peg$FAILED;
        }
        if (s5 !== peg$FAILED) {
          s4 = input.substring(s4, peg$currPos);
        } else {
          s4 = s5;
        }
        if (s4 !== peg$FAILED) {
          s5 = peg$parseLineEnd();
          if (s5 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f10();
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseStateRef() {
    var s0, s1;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 3) === peg$c18) {
      s1 = peg$c18;
      peg$currPos += 3;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e19);
      }
    }
    if (s1 !== peg$FAILED) {
      peg$savedPos = s0;
      s1 = peg$f11();
    }
    s0 = s1;
    if (s0 === peg$FAILED) {
      s0 = peg$parseIdent();
    }
    return s0;
  }
  function peg$parseQuotedOrId() {
    var s0, s1, s2, s3, s4;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 34) {
      s1 = peg$c19;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e20);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$currPos;
      s3 = [];
      s4 = input.charAt(peg$currPos);
      if (peg$r1.test(s4)) {
        peg$currPos++;
      } else {
        s4 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e21);
        }
      }
      while (s4 !== peg$FAILED) {
        s3.push(s4);
        s4 = input.charAt(peg$currPos);
        if (peg$r1.test(s4)) {
          peg$currPos++;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e21);
          }
        }
      }
      s2 = input.substring(s2, peg$currPos);
      if (input.charCodeAt(peg$currPos) === 34) {
        s3 = peg$c19;
        peg$currPos++;
      } else {
        s3 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e20);
        }
      }
      if (s3 !== peg$FAILED) {
        peg$savedPos = s0;
        s0 = peg$f12(s2);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    if (s0 === peg$FAILED) {
      s0 = peg$parseIdent();
    }
    return s0;
  }
  function peg$parseIdent() {
    var s0, s1, s2;
    s0 = peg$currPos;
    s1 = [];
    s2 = input.charAt(peg$currPos);
    if (peg$r2.test(s2)) {
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e22);
      }
    }
    if (s2 !== peg$FAILED) {
      while (s2 !== peg$FAILED) {
        s1.push(s2);
        s2 = input.charAt(peg$currPos);
        if (peg$r2.test(s2)) {
          peg$currPos++;
        } else {
          s2 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e22);
          }
        }
      }
    } else {
      s1 = peg$FAILED;
    }
    if (s1 !== peg$FAILED) {
      s0 = input.substring(s0, peg$currPos);
    } else {
      s0 = s1;
    }
    return s0;
  }
  function peg$parseRestOfLine() {
    var s0, s1, s2;
    s0 = peg$currPos;
    s1 = [];
    s2 = input.charAt(peg$currPos);
    if (peg$r0.test(s2)) {
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e3);
      }
    }
    if (s2 !== peg$FAILED) {
      while (s2 !== peg$FAILED) {
        s1.push(s2);
        s2 = input.charAt(peg$currPos);
        if (peg$r0.test(s2)) {
          peg$currPos++;
        } else {
          s2 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e3);
          }
        }
      }
    } else {
      s1 = peg$FAILED;
    }
    if (s1 !== peg$FAILED) {
      s0 = input.substring(s0, peg$currPos);
    } else {
      s0 = s1;
    }
    return s0;
  }
  function peg$parseBlankLine() {
    var s0, s1, s2, s3;
    s0 = peg$currPos;
    s1 = peg$parse_();
    if (input.charCodeAt(peg$currPos) === 13) {
      s2 = peg$c5;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e6);
      }
    }
    if (s2 === peg$FAILED) {
      s2 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s3 = peg$c6;
      peg$currPos++;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e7);
      }
    }
    if (s3 !== peg$FAILED) {
      peg$savedPos = s0;
      s0 = peg$f13();
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseNL() {
    var s0, s1, s2;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 13) {
      s1 = peg$c5;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e6);
      }
    }
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s2 = peg$c6;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e7);
      }
    }
    if (s2 !== peg$FAILED) {
      s1 = [s1, s2];
      s0 = s1;
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseLineEnd() {
    var s0, s1, s2;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 13) {
      s1 = peg$c5;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e6);
      }
    }
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s2 = peg$c6;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e7);
      }
    }
    if (s2 !== peg$FAILED) {
      s1 = [s1, s2];
      s0 = s1;
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      peg$silentFails++;
      if (input.length > peg$currPos) {
        s1 = input.charAt(peg$currPos);
        peg$currPos++;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e23);
        }
      }
      peg$silentFails--;
      if (s1 === peg$FAILED) {
        s0 = void 0;
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    }
    return s0;
  }
  function peg$parse_() {
    var s0, s1;
    s0 = [];
    s1 = input.charAt(peg$currPos);
    if (peg$r3.test(s1)) {
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e24);
      }
    }
    while (s1 !== peg$FAILED) {
      s0.push(s1);
      s1 = input.charAt(peg$currPos);
      if (peg$r3.test(s1)) {
        peg$currPos++;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e24);
        }
      }
    }
    return s0;
  }
  function peg$parse__() {
    var s0, s1;
    s0 = [];
    s1 = input.charAt(peg$currPos);
    if (peg$r3.test(s1)) {
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e24);
      }
    }
    if (s1 !== peg$FAILED) {
      while (s1 !== peg$FAILED) {
        s0.push(s1);
        s1 = input.charAt(peg$currPos);
        if (peg$r3.test(s1)) {
          peg$currPos++;
        } else {
          s1 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e24);
          }
        }
      }
    } else {
      s0 = peg$FAILED;
    }
    return s0;
  }
  peg$result = peg$startRuleFunction();
  if (options.peg$library) {
    return (
      /** @type {any} */
      {
        peg$result,
        peg$currPos,
        peg$FAILED,
        peg$maxFailExpected,
        peg$maxFailPos
      }
    );
  }
  if (peg$result !== peg$FAILED && peg$currPos === input.length) {
    return peg$result;
  } else {
    if (peg$result !== peg$FAILED && peg$currPos < input.length) {
      peg$fail(peg$endExpectation());
    }
    throw peg$buildStructuredError(
      peg$maxFailExpected,
      peg$maxFailPos < input.length ? input.charAt(peg$maxFailPos) : null,
      peg$maxFailPos < input.length ? peg$computeLocation(peg$maxFailPos, peg$maxFailPos + 1) : peg$computeLocation(peg$maxFailPos, peg$maxFailPos)
    );
  }
}
var state = {
  parseMermaid(input) {
    return peg$parse13(input);
  },
  parseYaml(input) {
    return JSON.parse(input);
  },
  layout(ir, theme) {
    return layoutState(ir, theme);
  }
};
function layoutEr(ir, theme) {
  const { palette, typography, spacing } = theme;
  const p = pen(theme);
  const margin = spacing.diagramMargin;
  const nameFont = typography.baseFontSize;
  const rowFont = typography.smallFontSize;
  const rowH = rhuInt(rowFont * 1.7);
  const headH = rhuInt(nameFont * 1.8);
  const sizeOf = (e) => {
    const rowText = (a) => `${a.type}  ${a.name}${a.key ? "  " + a.key : ""}`;
    const w = Math.max(
      140,
      measureText(e.name, nameFont).width + 28,
      ...e.attributes.map((a) => measureText(rowText(a), rowFont).width + 28)
    );
    const h2 = headH + e.attributes.length * rowH + (e.attributes.length ? 6 : 0);
    return { w, h: h2 };
  };
  const sizes = new Map(ir.entities.map((e) => [e.name, sizeOf(e)]));
  const nodes = ir.entities.map((e) => ({
    id: e.name,
    width: sizes.get(e.name).w,
    height: sizes.get(e.name).h
  }));
  const edges2 = ir.relations.map((r) => ({ from: r.left, to: r.right }));
  const laid = layeredLayout(nodes, edges2, { direction: "TB", layerGap: 70, nodeGap: 50, margin });
  const title = ir.metadata.title;
  const titleH = title ? typography.titleFontSize + 14 : 0;
  const yOff = titleH;
  const elements = [];
  if (title)
    elements.push(
      p.text(
        title,
        margin,
        margin + typography.titleFontSize,
        typography.titleFontSize,
        palette.text,
        { weight: "bold" }
      )
    );
  const allBoxes = [...laid.boxes.values()];
  for (const r of ir.relations) {
    const a = laid.boxes.get(r.left), b = laid.boxes.get(r.right);
    if (!a || !b) continue;
    const ac = { x: a.x + a.width / 2, y: a.y + a.height / 2 + yOff };
    const bc = { x: b.x + b.width / 2, y: b.y + b.height / 2 + yOff };
    const pa = borderPoint({ ...a, y: a.y + yOff }, bc.x, bc.y);
    const pb = borderPoint({ ...b, y: b.y + yOff }, ac.x, ac.y);
    const { path } = routeEdge(a, b, allBoxes, yOff);
    elements.push(p.path(path, palette.textMuted, 1.3, r.dashed ? { dash: "6 4" } : {}));
    elements.push(...crowFoot(p, pa, bc, r.leftCard, palette));
    elements.push(...crowFoot(p, pb, ac, r.rightCard, palette));
    if (r.label) {
      const mx = (pa.x + pb.x) / 2, my = (pa.y + pb.y) / 2;
      const w = measureText(r.label, rowFont).width + 8;
      elements.push(
        p.rect(
          { x: rhu(mx - w / 2), y: rhu(my - rowFont), width: rhu(w), height: rowFont + 4 },
          palette.background,
          palette.background,
          0
        )
      );
      elements.push(
        p.text(r.label, rhuInt(mx), rhu(my - 1), rowFont, palette.textMuted, { anchor: "middle" })
      );
    }
  }
  for (const e of ir.entities) {
    const box = laid.boxes.get(e.name);
    const s = sizes.get(e.name);
    const x = box.x, y = box.y + yOff;
    elements.push(
      p.rect(
        { x: rhu(x), y: rhu(y), width: rhu(s.w), height: rhu(s.h) },
        palette.surface,
        palette.border,
        1.4,
        { rx: 4 }
      )
    );
    elements.push(
      p.rect(
        { x: rhu(x), y: rhu(y), width: rhu(s.w), height: headH },
        palette.primary,
        palette.border,
        1.4,
        { rx: 4 }
      )
    );
    elements.push(
      p.text(
        e.name,
        rhuInt(x + s.w / 2),
        rhu(y + headH / 2 + nameFont * 0.35),
        nameFont,
        readableText(palette.primary, theme),
        { weight: "bold", anchor: "middle" }
      )
    );
    let ry = y + headH + rowH - 5;
    for (const a of e.attributes) {
      elements.push(p.text(`${a.type}  ${a.name}`, rhu(x + 8), rhu(ry), rowFont, palette.text));
      if (a.key)
        elements.push(
          p.text(a.key, rhu(x + s.w - 8), rhu(ry), rowFont, palette.secondary, {
            anchor: "end",
            weight: "bold"
          })
        );
      ry += rowH;
    }
  }
  const totalW = rhuInt(laid.width + margin);
  const totalH = rhuInt(laid.height + yOff + margin);
  const scene = applyOverlays(
    {
      viewBox: { x: 0, y: 0, width: totalW, height: totalH },
      background: palette.background,
      elements
    },
    ir.overlays,
    theme
  );
  return { scene, anchors: {} };
}
function crowFoot(p, at, toward, card, palette) {
  const len = Math.hypot(toward.x - at.x, toward.y - at.y) || 1;
  const ux = (toward.x - at.x) / len, uy = (toward.y - at.y) / len;
  const px = -uy, py = ux;
  const at2 = (d, s) => ({ x: at.x + ux * d + px * s, y: at.y + uy * d + py * s });
  const out = [];
  const color = palette.textMuted;
  const many = /[}{]/.test(card);
  const zero = /o/.test(card);
  const one = /\|/.test(card);
  if (many) {
    const base = at2(14, 0);
    const a = at2(0, 6), b = at2(0, -6);
    out.push(p.path(`M ${rhu(base.x)} ${rhu(base.y)} L ${rhu(a.x)} ${rhu(a.y)}`, color, 1.2));
    out.push(p.path(`M ${rhu(base.x)} ${rhu(base.y)} L ${rhu(b.x)} ${rhu(b.y)}`, color, 1.2));
    out.push(p.path(`M ${rhu(base.x)} ${rhu(base.y)} L ${rhu(at.x)} ${rhu(at.y)}`, color, 1.2));
  } else if (one) {
    const c1 = at2(10, 6), c2 = at2(10, -6);
    out.push(p.path(`M ${rhu(c1.x)} ${rhu(c1.y)} L ${rhu(c2.x)} ${rhu(c2.y)}`, color, 1.3));
  }
  if (zero) {
    const c = at2(many ? 20 : 16, 0);
    out.push(p.circle({ x: rhu(c.x), y: rhu(c.y) }, 4, palette.background, color, 1.2));
  } else if (one && !many) {
    const c1 = at2(15, 5), c2 = at2(15, -5);
    out.push(p.path(`M ${rhu(c1.x)} ${rhu(c1.y)} L ${rhu(c2.x)} ${rhu(c2.y)}`, color, 1.3));
  }
  return out;
}
function peg$subclass14(child, parent) {
  function C() {
    this.constructor = child;
  }
  C.prototype = parent.prototype;
  child.prototype = new C();
}
function peg$SyntaxError14(message, expected, found, location) {
  var self = Error.call(this, message);
  if (Object.setPrototypeOf) {
    Object.setPrototypeOf(self, peg$SyntaxError14.prototype);
  }
  self.expected = expected;
  self.found = found;
  self.location = location;
  self.name = "SyntaxError";
  return self;
}
peg$subclass14(peg$SyntaxError14, Error);
function peg$padEnd14(str, targetLength, padString) {
  padString = padString || " ";
  if (str.length > targetLength) {
    return str;
  }
  targetLength -= str.length;
  padString += padString.repeat(targetLength);
  return str + padString.slice(0, targetLength);
}
peg$SyntaxError14.prototype.format = function(sources) {
  var str = "Error: " + this.message;
  if (this.location) {
    var src = null;
    var k;
    for (k = 0; k < sources.length; k++) {
      if (sources[k].source === this.location.source) {
        src = sources[k].text.split(/\r\n|\n|\r/g);
        break;
      }
    }
    var s = this.location.start;
    var offset_s = this.location.source && typeof this.location.source.offset === "function" ? this.location.source.offset(s) : s;
    var loc = this.location.source + ":" + offset_s.line + ":" + offset_s.column;
    if (src) {
      var e = this.location.end;
      var filler = peg$padEnd14("", offset_s.line.toString().length, " ");
      var line = src[s.line - 1];
      var last = s.line === e.line ? e.column : line.length + 1;
      var hatLen = last - s.column || 1;
      str += "\n --> " + loc + "\n" + filler + " |\n" + offset_s.line + " | " + line + "\n" + filler + " | " + peg$padEnd14("", s.column - 1, " ") + peg$padEnd14("", hatLen, "^");
    } else {
      str += "\n at " + loc;
    }
  }
  return str;
};
peg$SyntaxError14.buildMessage = function(expected, found) {
  var DESCRIBE_EXPECTATION_FNS = {
    literal: function(expectation) {
      return '"' + literalEscape(expectation.text) + '"';
    },
    class: function(expectation) {
      var escapedParts = expectation.parts.map(function(part) {
        return Array.isArray(part) ? classEscape(part[0]) + "-" + classEscape(part[1]) : classEscape(part);
      });
      return "[" + (expectation.inverted ? "^" : "") + escapedParts.join("") + "]";
    },
    any: function() {
      return "any character";
    },
    end: function() {
      return "end of input";
    },
    other: function(expectation) {
      return expectation.description;
    }
  };
  function hex(ch) {
    return ch.charCodeAt(0).toString(16).toUpperCase();
  }
  function literalEscape(s) {
    return s.replace(/\\/g, "\\\\").replace(/"/g, '\\"').replace(/\0/g, "\\0").replace(/\t/g, "\\t").replace(/\n/g, "\\n").replace(/\r/g, "\\r").replace(/[\x00-\x0F]/g, function(ch) {
      return "\\x0" + hex(ch);
    }).replace(/[\x10-\x1F\x7F-\x9F]/g, function(ch) {
      return "\\x" + hex(ch);
    });
  }
  function classEscape(s) {
    return s.replace(/\\/g, "\\\\").replace(/\]/g, "\\]").replace(/\^/g, "\\^").replace(/-/g, "\\-").replace(/\0/g, "\\0").replace(/\t/g, "\\t").replace(/\n/g, "\\n").replace(/\r/g, "\\r").replace(/[\x00-\x0F]/g, function(ch) {
      return "\\x0" + hex(ch);
    }).replace(/[\x10-\x1F\x7F-\x9F]/g, function(ch) {
      return "\\x" + hex(ch);
    });
  }
  function describeExpectation(expectation) {
    return DESCRIBE_EXPECTATION_FNS[expectation.type](expectation);
  }
  function describeExpected(expected2) {
    var descriptions = expected2.map(describeExpectation);
    var i, j;
    descriptions.sort();
    if (descriptions.length > 0) {
      for (i = 1, j = 1; i < descriptions.length; i++) {
        if (descriptions[i - 1] !== descriptions[i]) {
          descriptions[j] = descriptions[i];
          j++;
        }
      }
      descriptions.length = j;
    }
    switch (descriptions.length) {
      case 1:
        return descriptions[0];
      case 2:
        return descriptions[0] + " or " + descriptions[1];
      default:
        return descriptions.slice(0, -1).join(", ") + ", or " + descriptions[descriptions.length - 1];
    }
  }
  function describeFound(found2) {
    return found2 ? '"' + literalEscape(found2) + '"' : "end of input";
  }
  return "Expected " + describeExpected(expected) + " but " + describeFound(found) + " found.";
};
function peg$parse14(input, options) {
  options = options !== void 0 ? options : {};
  var peg$FAILED = {};
  var peg$source = options.grammarSource;
  var peg$startRuleFunctions = { Document: peg$parseDocument };
  var peg$startRuleFunction = peg$parseDocument;
  var peg$c0 = "erdiagram";
  var peg$c1 = "---";
  var peg$c2 = "{";
  var peg$c3 = "\r";
  var peg$c4 = "\n";
  var peg$c5 = "}";
  var peg$c6 = '"';
  var peg$c7 = ":";
  var peg$c8 = "--";
  var peg$c9 = "..";
  var peg$c10 = "PK";
  var peg$c11 = "FK";
  var peg$c12 = "UK";
  var peg$r0 = /^[^\n]/;
  var peg$r1 = /^[^"]/;
  var peg$r2 = /^[|}{o]/;
  var peg$r3 = /^[A-Za-z0-9_]/;
  var peg$r4 = /^[ \t]/;
  var peg$e0 = peg$literalExpectation("erDiagram", true);
  var peg$e1 = peg$literalExpectation("---", false);
  var peg$e2 = peg$classExpectation(["\n"], true, false);
  var peg$e3 = peg$literalExpectation("{", false);
  var peg$e4 = peg$literalExpectation("\r", false);
  var peg$e5 = peg$literalExpectation("\n", false);
  var peg$e6 = peg$literalExpectation("}", false);
  var peg$e7 = peg$literalExpectation('"', false);
  var peg$e8 = peg$classExpectation(['"'], true, false);
  var peg$e9 = peg$literalExpectation(":", false);
  var peg$e10 = peg$classExpectation(["|", "}", "{", "o"], false, false);
  var peg$e11 = peg$literalExpectation("--", false);
  var peg$e12 = peg$literalExpectation("..", false);
  var peg$e13 = peg$literalExpectation("PK", false);
  var peg$e14 = peg$literalExpectation("FK", false);
  var peg$e15 = peg$literalExpectation("UK", false);
  var peg$e16 = peg$classExpectation([["A", "Z"], ["a", "z"], ["0", "9"], "_"], false, false);
  var peg$e17 = peg$anyExpectation();
  var peg$e18 = peg$classExpectation([" ", "	"], false, false);
  var peg$f0 = function(fm, stmts) {
    const entities = [];
    const relations = [];
    const ensure = (name) => {
      let e = entities.find((x) => x.name === name);
      if (!e) {
        e = { name, attributes: [] };
        entities.push(e);
      }
      return e;
    };
    for (const st of stmts.filter(Boolean)) {
      if (st.t === "entity") {
        const e = ensure(st.name);
        for (const a of st.attrs) e.attributes.push(a);
      } else if (st.t === "rel") {
        ensure(st.left);
        ensure(st.right);
        const dashed = st.tok.includes("..");
        const leftCard = st.tok.slice(0, 2);
        const rightCard = st.tok.slice(-2);
        relations.push({ left: st.left, right: st.right, leftCard, rightCard, dashed, ...st.label ? { label: st.label } : {} });
      }
    }
    return { version: "1.0", metadata: { ...fm || {} }, entities, relations };
  };
  var peg$f1 = function(line) {
    return line;
  };
  var peg$f2 = function(lines2) {
    const meta = {};
    for (const line of lines2) {
      const m = line.match(/^\s*(\w+)\s*:\s*(.+?)\s*$/);
      if (m) meta[m[1]] = m[2];
    }
    return meta;
  };
  var peg$f3 = function(name, rows) {
    return { t: "entity", name, attrs: rows.filter(Boolean) };
  };
  var peg$f4 = function(type, name, k) {
    return k;
  };
  var peg$f5 = function(type, name, key) {
    return { type, name, ...key ? { key } : {} };
  };
  var peg$f6 = function() {
    return null;
  };
  var peg$f7 = function(left, tok, right, label) {
    return { t: "rel", left, right, tok, label };
  };
  var peg$f8 = function(s) {
    return s;
  };
  var peg$f9 = function(s) {
    return s.trim();
  };
  var peg$f10 = function() {
    return null;
  };
  var peg$currPos = options.peg$currPos | 0;
  var peg$savedPos = peg$currPos;
  var peg$posDetailsCache = [{ line: 1, column: 1 }];
  var peg$maxFailPos = peg$currPos;
  var peg$maxFailExpected = options.peg$maxFailExpected || [];
  var peg$silentFails = options.peg$silentFails | 0;
  var peg$result;
  if (options.startRule) {
    if (!(options.startRule in peg$startRuleFunctions)) {
      throw new Error(`Can't start parsing from rule "` + options.startRule + '".');
    }
    peg$startRuleFunction = peg$startRuleFunctions[options.startRule];
  }
  function text() {
    return input.substring(peg$savedPos, peg$currPos);
  }
  function offset() {
    return peg$savedPos;
  }
  function range() {
    return {
      source: peg$source,
      start: peg$savedPos,
      end: peg$currPos
    };
  }
  function location() {
    return peg$computeLocation(peg$savedPos, peg$currPos);
  }
  function expected(description, location2) {
    location2 = location2 !== void 0 ? location2 : peg$computeLocation(peg$savedPos, peg$currPos);
    throw peg$buildStructuredError(
      [peg$otherExpectation(description)],
      input.substring(peg$savedPos, peg$currPos),
      location2
    );
  }
  function error(message, location2) {
    location2 = location2 !== void 0 ? location2 : peg$computeLocation(peg$savedPos, peg$currPos);
    throw peg$buildSimpleError(message, location2);
  }
  function peg$literalExpectation(text2, ignoreCase) {
    return { type: "literal", text: text2, ignoreCase };
  }
  function peg$classExpectation(parts, inverted, ignoreCase) {
    return { type: "class", parts, inverted, ignoreCase };
  }
  function peg$anyExpectation() {
    return { type: "any" };
  }
  function peg$endExpectation() {
    return { type: "end" };
  }
  function peg$otherExpectation(description) {
    return { type: "other", description };
  }
  function peg$computePosDetails(pos) {
    var details = peg$posDetailsCache[pos];
    var p;
    if (details) {
      return details;
    } else {
      if (pos >= peg$posDetailsCache.length) {
        p = peg$posDetailsCache.length - 1;
      } else {
        p = pos;
        while (!peg$posDetailsCache[--p]) {
        }
      }
      details = peg$posDetailsCache[p];
      details = {
        line: details.line,
        column: details.column
      };
      while (p < pos) {
        if (input.charCodeAt(p) === 10) {
          details.line++;
          details.column = 1;
        } else {
          details.column++;
        }
        p++;
      }
      peg$posDetailsCache[pos] = details;
      return details;
    }
  }
  function peg$computeLocation(startPos, endPos, offset2) {
    var startPosDetails = peg$computePosDetails(startPos);
    var endPosDetails = peg$computePosDetails(endPos);
    var res = {
      source: peg$source,
      start: {
        offset: startPos,
        line: startPosDetails.line,
        column: startPosDetails.column
      },
      end: {
        offset: endPos,
        line: endPosDetails.line,
        column: endPosDetails.column
      }
    };
    if (offset2 && peg$source && typeof peg$source.offset === "function") {
      res.start = peg$source.offset(res.start);
      res.end = peg$source.offset(res.end);
    }
    return res;
  }
  function peg$fail(expected2) {
    if (peg$currPos < peg$maxFailPos) {
      return;
    }
    if (peg$currPos > peg$maxFailPos) {
      peg$maxFailPos = peg$currPos;
      peg$maxFailExpected = [];
    }
    peg$maxFailExpected.push(expected2);
  }
  function peg$buildSimpleError(message, location2) {
    return new peg$SyntaxError14(message, null, null, location2);
  }
  function peg$buildStructuredError(expected2, found, location2) {
    return new peg$SyntaxError14(
      peg$SyntaxError14.buildMessage(expected2, found),
      expected2,
      found,
      location2
    );
  }
  function peg$parseDocument() {
    var s0, s1, s2, s3, s4, s5, s6;
    s0 = peg$currPos;
    s1 = peg$parseFrontmatter();
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    s2 = peg$parse_();
    s3 = input.substr(peg$currPos, 9);
    if (s3.toLowerCase() === peg$c0) {
      peg$currPos += 9;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e0);
      }
    }
    if (s3 !== peg$FAILED) {
      s4 = peg$parseLineEnd();
      if (s4 !== peg$FAILED) {
        s5 = [];
        s6 = peg$parseStatement();
        while (s6 !== peg$FAILED) {
          s5.push(s6);
          s6 = peg$parseStatement();
        }
        s6 = peg$parse_();
        peg$savedPos = s0;
        s0 = peg$f0(s1, s5);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseFrontmatter() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 3) === peg$c1) {
      s1 = peg$c1;
      peg$currPos += 3;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e1);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$parseNL();
      if (s2 !== peg$FAILED) {
        s3 = [];
        s4 = peg$currPos;
        s5 = peg$currPos;
        peg$silentFails++;
        s6 = peg$currPos;
        if (input.substr(peg$currPos, 3) === peg$c1) {
          s7 = peg$c1;
          peg$currPos += 3;
        } else {
          s7 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e1);
          }
        }
        if (s7 !== peg$FAILED) {
          s8 = peg$parseNL();
          if (s8 !== peg$FAILED) {
            s7 = [s7, s8];
            s6 = s7;
          } else {
            peg$currPos = s6;
            s6 = peg$FAILED;
          }
        } else {
          peg$currPos = s6;
          s6 = peg$FAILED;
        }
        peg$silentFails--;
        if (s6 === peg$FAILED) {
          s5 = void 0;
        } else {
          peg$currPos = s5;
          s5 = peg$FAILED;
        }
        if (s5 !== peg$FAILED) {
          s6 = peg$currPos;
          s7 = [];
          s8 = input.charAt(peg$currPos);
          if (peg$r0.test(s8)) {
            peg$currPos++;
          } else {
            s8 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e2);
            }
          }
          while (s8 !== peg$FAILED) {
            s7.push(s8);
            s8 = input.charAt(peg$currPos);
            if (peg$r0.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e2);
              }
            }
          }
          s6 = input.substring(s6, peg$currPos);
          s7 = peg$parseNL();
          if (s7 !== peg$FAILED) {
            peg$savedPos = s4;
            s4 = peg$f1(s6);
          } else {
            peg$currPos = s4;
            s4 = peg$FAILED;
          }
        } else {
          peg$currPos = s4;
          s4 = peg$FAILED;
        }
        while (s4 !== peg$FAILED) {
          s3.push(s4);
          s4 = peg$currPos;
          s5 = peg$currPos;
          peg$silentFails++;
          s6 = peg$currPos;
          if (input.substr(peg$currPos, 3) === peg$c1) {
            s7 = peg$c1;
            peg$currPos += 3;
          } else {
            s7 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e1);
            }
          }
          if (s7 !== peg$FAILED) {
            s8 = peg$parseNL();
            if (s8 !== peg$FAILED) {
              s7 = [s7, s8];
              s6 = s7;
            } else {
              peg$currPos = s6;
              s6 = peg$FAILED;
            }
          } else {
            peg$currPos = s6;
            s6 = peg$FAILED;
          }
          peg$silentFails--;
          if (s6 === peg$FAILED) {
            s5 = void 0;
          } else {
            peg$currPos = s5;
            s5 = peg$FAILED;
          }
          if (s5 !== peg$FAILED) {
            s6 = peg$currPos;
            s7 = [];
            s8 = input.charAt(peg$currPos);
            if (peg$r0.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e2);
              }
            }
            while (s8 !== peg$FAILED) {
              s7.push(s8);
              s8 = input.charAt(peg$currPos);
              if (peg$r0.test(s8)) {
                peg$currPos++;
              } else {
                s8 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e2);
                }
              }
            }
            s6 = input.substring(s6, peg$currPos);
            s7 = peg$parseNL();
            if (s7 !== peg$FAILED) {
              peg$savedPos = s4;
              s4 = peg$f1(s6);
            } else {
              peg$currPos = s4;
              s4 = peg$FAILED;
            }
          } else {
            peg$currPos = s4;
            s4 = peg$FAILED;
          }
        }
        if (input.substr(peg$currPos, 3) === peg$c1) {
          s4 = peg$c1;
          peg$currPos += 3;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e1);
          }
        }
        if (s4 !== peg$FAILED) {
          s5 = peg$parseNL();
          if (s5 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f2(s3);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseStatement() {
    var s0;
    s0 = peg$parseEntityBlock();
    if (s0 === peg$FAILED) {
      s0 = peg$parseRelation();
      if (s0 === peg$FAILED) {
        s0 = peg$parseBlankLine();
      }
    }
    return s0;
  }
  function peg$parseEntityBlock() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9, s10, s11;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = peg$parseIdent();
    if (s2 !== peg$FAILED) {
      s3 = peg$parse_();
      if (input.charCodeAt(peg$currPos) === 123) {
        s4 = peg$c2;
        peg$currPos++;
      } else {
        s4 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e3);
        }
      }
      if (s4 !== peg$FAILED) {
        s5 = peg$parse_();
        if (input.charCodeAt(peg$currPos) === 13) {
          s6 = peg$c3;
          peg$currPos++;
        } else {
          s6 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e4);
          }
        }
        if (s6 === peg$FAILED) {
          s6 = null;
        }
        if (input.charCodeAt(peg$currPos) === 10) {
          s7 = peg$c4;
          peg$currPos++;
        } else {
          s7 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e5);
          }
        }
        if (s7 !== peg$FAILED) {
          s8 = [];
          s9 = peg$parseAttrRow();
          while (s9 !== peg$FAILED) {
            s8.push(s9);
            s9 = peg$parseAttrRow();
          }
          s9 = peg$parse_();
          if (input.charCodeAt(peg$currPos) === 125) {
            s10 = peg$c5;
            peg$currPos++;
          } else {
            s10 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e6);
            }
          }
          if (s10 !== peg$FAILED) {
            s11 = peg$parseLineEnd();
            if (s11 !== peg$FAILED) {
              peg$savedPos = s0;
              s0 = peg$f3(s2, s8);
            } else {
              peg$currPos = s0;
              s0 = peg$FAILED;
            }
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseAttrRow() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9, s10, s11;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = peg$currPos;
    peg$silentFails++;
    if (input.charCodeAt(peg$currPos) === 125) {
      s3 = peg$c5;
      peg$currPos++;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e6);
      }
    }
    peg$silentFails--;
    if (s3 === peg$FAILED) {
      s2 = void 0;
    } else {
      peg$currPos = s2;
      s2 = peg$FAILED;
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$parseIdent();
      if (s3 !== peg$FAILED) {
        s4 = peg$parse__();
        if (s4 !== peg$FAILED) {
          s5 = peg$parseIdent();
          if (s5 !== peg$FAILED) {
            s6 = peg$currPos;
            s7 = peg$parse__();
            if (s7 !== peg$FAILED) {
              s8 = peg$parseKey();
              if (s8 !== peg$FAILED) {
                peg$savedPos = s6;
                s6 = peg$f4(s3, s5, s8);
              } else {
                peg$currPos = s6;
                s6 = peg$FAILED;
              }
            } else {
              peg$currPos = s6;
              s6 = peg$FAILED;
            }
            if (s6 === peg$FAILED) {
              s6 = null;
            }
            s7 = peg$currPos;
            s8 = peg$parse__();
            if (s8 !== peg$FAILED) {
              if (input.charCodeAt(peg$currPos) === 34) {
                s9 = peg$c6;
                peg$currPos++;
              } else {
                s9 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e7);
                }
              }
              if (s9 !== peg$FAILED) {
                s10 = [];
                s11 = input.charAt(peg$currPos);
                if (peg$r1.test(s11)) {
                  peg$currPos++;
                } else {
                  s11 = peg$FAILED;
                  if (peg$silentFails === 0) {
                    peg$fail(peg$e8);
                  }
                }
                while (s11 !== peg$FAILED) {
                  s10.push(s11);
                  s11 = input.charAt(peg$currPos);
                  if (peg$r1.test(s11)) {
                    peg$currPos++;
                  } else {
                    s11 = peg$FAILED;
                    if (peg$silentFails === 0) {
                      peg$fail(peg$e8);
                    }
                  }
                }
                if (input.charCodeAt(peg$currPos) === 34) {
                  s11 = peg$c6;
                  peg$currPos++;
                } else {
                  s11 = peg$FAILED;
                  if (peg$silentFails === 0) {
                    peg$fail(peg$e7);
                  }
                }
                if (s11 !== peg$FAILED) {
                  s8 = [s8, s9, s10, s11];
                  s7 = s8;
                } else {
                  peg$currPos = s7;
                  s7 = peg$FAILED;
                }
              } else {
                peg$currPos = s7;
                s7 = peg$FAILED;
              }
            } else {
              peg$currPos = s7;
              s7 = peg$FAILED;
            }
            if (s7 === peg$FAILED) {
              s7 = null;
            }
            s8 = peg$parse_();
            if (input.charCodeAt(peg$currPos) === 13) {
              s9 = peg$c3;
              peg$currPos++;
            } else {
              s9 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e4);
              }
            }
            if (s9 === peg$FAILED) {
              s9 = null;
            }
            if (input.charCodeAt(peg$currPos) === 10) {
              s10 = peg$c4;
              peg$currPos++;
            } else {
              s10 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e5);
              }
            }
            if (s10 !== peg$FAILED) {
              peg$savedPos = s0;
              s0 = peg$f5(s3, s5, s6);
            } else {
              peg$currPos = s0;
              s0 = peg$FAILED;
            }
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      s1 = peg$parse_();
      if (input.charCodeAt(peg$currPos) === 13) {
        s2 = peg$c3;
        peg$currPos++;
      } else {
        s2 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e4);
        }
      }
      if (s2 === peg$FAILED) {
        s2 = null;
      }
      if (input.charCodeAt(peg$currPos) === 10) {
        s3 = peg$c4;
        peg$currPos++;
      } else {
        s3 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e5);
        }
      }
      if (s3 !== peg$FAILED) {
        peg$savedPos = s0;
        s0 = peg$f6();
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    }
    return s0;
  }
  function peg$parseRelation() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9, s10, s11;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = peg$parseIdent();
    if (s2 !== peg$FAILED) {
      s3 = peg$parse__();
      if (s3 !== peg$FAILED) {
        s4 = peg$parseErTok();
        if (s4 !== peg$FAILED) {
          s5 = peg$parse__();
          if (s5 !== peg$FAILED) {
            s6 = peg$parseIdent();
            if (s6 !== peg$FAILED) {
              s7 = peg$parse_();
              if (input.charCodeAt(peg$currPos) === 58) {
                s8 = peg$c7;
                peg$currPos++;
              } else {
                s8 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e9);
                }
              }
              if (s8 !== peg$FAILED) {
                s9 = peg$parse_();
                s10 = peg$parseRelLabel();
                if (s10 !== peg$FAILED) {
                  s11 = peg$parseLineEnd();
                  if (s11 !== peg$FAILED) {
                    peg$savedPos = s0;
                    s0 = peg$f7(s2, s4, s6, s10);
                  } else {
                    peg$currPos = s0;
                    s0 = peg$FAILED;
                  }
                } else {
                  peg$currPos = s0;
                  s0 = peg$FAILED;
                }
              } else {
                peg$currPos = s0;
                s0 = peg$FAILED;
              }
            } else {
              peg$currPos = s0;
              s0 = peg$FAILED;
            }
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseRelLabel() {
    var s0, s1, s2, s3, s4;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 34) {
      s1 = peg$c6;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e7);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$currPos;
      s3 = [];
      s4 = input.charAt(peg$currPos);
      if (peg$r1.test(s4)) {
        peg$currPos++;
      } else {
        s4 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e8);
        }
      }
      while (s4 !== peg$FAILED) {
        s3.push(s4);
        s4 = input.charAt(peg$currPos);
        if (peg$r1.test(s4)) {
          peg$currPos++;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e8);
          }
        }
      }
      s2 = input.substring(s2, peg$currPos);
      if (input.charCodeAt(peg$currPos) === 34) {
        s3 = peg$c6;
        peg$currPos++;
      } else {
        s3 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e7);
        }
      }
      if (s3 !== peg$FAILED) {
        peg$savedPos = s0;
        s0 = peg$f8(s2);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      s1 = peg$currPos;
      s2 = [];
      s3 = input.charAt(peg$currPos);
      if (peg$r0.test(s3)) {
        peg$currPos++;
      } else {
        s3 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e2);
        }
      }
      if (s3 !== peg$FAILED) {
        while (s3 !== peg$FAILED) {
          s2.push(s3);
          s3 = input.charAt(peg$currPos);
          if (peg$r0.test(s3)) {
            peg$currPos++;
          } else {
            s3 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e2);
            }
          }
        }
      } else {
        s2 = peg$FAILED;
      }
      if (s2 !== peg$FAILED) {
        s1 = input.substring(s1, peg$currPos);
      } else {
        s1 = s2;
      }
      if (s1 !== peg$FAILED) {
        peg$savedPos = s0;
        s1 = peg$f9(s1);
      }
      s0 = s1;
    }
    return s0;
  }
  function peg$parseErTok() {
    var s0, s1, s2, s3, s4, s5, s6;
    s0 = peg$currPos;
    s1 = peg$currPos;
    s2 = input.charAt(peg$currPos);
    if (peg$r2.test(s2)) {
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e10);
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = input.charAt(peg$currPos);
      if (peg$r2.test(s3)) {
        peg$currPos++;
      } else {
        s3 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e10);
        }
      }
      if (s3 !== peg$FAILED) {
        if (input.substr(peg$currPos, 2) === peg$c8) {
          s4 = peg$c8;
          peg$currPos += 2;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e11);
          }
        }
        if (s4 === peg$FAILED) {
          if (input.substr(peg$currPos, 2) === peg$c9) {
            s4 = peg$c9;
            peg$currPos += 2;
          } else {
            s4 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e12);
            }
          }
        }
        if (s4 !== peg$FAILED) {
          s5 = input.charAt(peg$currPos);
          if (peg$r2.test(s5)) {
            peg$currPos++;
          } else {
            s5 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e10);
            }
          }
          if (s5 !== peg$FAILED) {
            s6 = input.charAt(peg$currPos);
            if (peg$r2.test(s6)) {
              peg$currPos++;
            } else {
              s6 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e10);
              }
            }
            if (s6 !== peg$FAILED) {
              s2 = [s2, s3, s4, s5, s6];
              s1 = s2;
            } else {
              peg$currPos = s1;
              s1 = peg$FAILED;
            }
          } else {
            peg$currPos = s1;
            s1 = peg$FAILED;
          }
        } else {
          peg$currPos = s1;
          s1 = peg$FAILED;
        }
      } else {
        peg$currPos = s1;
        s1 = peg$FAILED;
      }
    } else {
      peg$currPos = s1;
      s1 = peg$FAILED;
    }
    if (s1 !== peg$FAILED) {
      s0 = input.substring(s0, peg$currPos);
    } else {
      s0 = s1;
    }
    return s0;
  }
  function peg$parseKey() {
    var s0;
    if (input.substr(peg$currPos, 2) === peg$c10) {
      s0 = peg$c10;
      peg$currPos += 2;
    } else {
      s0 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e13);
      }
    }
    if (s0 === peg$FAILED) {
      if (input.substr(peg$currPos, 2) === peg$c11) {
        s0 = peg$c11;
        peg$currPos += 2;
      } else {
        s0 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e14);
        }
      }
      if (s0 === peg$FAILED) {
        if (input.substr(peg$currPos, 2) === peg$c12) {
          s0 = peg$c12;
          peg$currPos += 2;
        } else {
          s0 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e15);
          }
        }
      }
    }
    return s0;
  }
  function peg$parseIdent() {
    var s0, s1, s2;
    s0 = peg$currPos;
    s1 = [];
    s2 = input.charAt(peg$currPos);
    if (peg$r3.test(s2)) {
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e16);
      }
    }
    if (s2 !== peg$FAILED) {
      while (s2 !== peg$FAILED) {
        s1.push(s2);
        s2 = input.charAt(peg$currPos);
        if (peg$r3.test(s2)) {
          peg$currPos++;
        } else {
          s2 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e16);
          }
        }
      }
    } else {
      s1 = peg$FAILED;
    }
    if (s1 !== peg$FAILED) {
      s0 = input.substring(s0, peg$currPos);
    } else {
      s0 = s1;
    }
    return s0;
  }
  function peg$parseNL() {
    var s0, s1, s2;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 13) {
      s1 = peg$c3;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e4);
      }
    }
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s2 = peg$c4;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e5);
      }
    }
    if (s2 !== peg$FAILED) {
      s1 = [s1, s2];
      s0 = s1;
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseBlankLine() {
    var s0, s1, s2, s3;
    s0 = peg$currPos;
    s1 = peg$parse_();
    if (input.charCodeAt(peg$currPos) === 13) {
      s2 = peg$c3;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e4);
      }
    }
    if (s2 === peg$FAILED) {
      s2 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s3 = peg$c4;
      peg$currPos++;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e5);
      }
    }
    if (s3 !== peg$FAILED) {
      peg$savedPos = s0;
      s0 = peg$f10();
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseLineEnd() {
    var s0, s1, s2;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 13) {
      s1 = peg$c3;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e4);
      }
    }
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s2 = peg$c4;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e5);
      }
    }
    if (s2 !== peg$FAILED) {
      s1 = [s1, s2];
      s0 = s1;
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      peg$silentFails++;
      if (input.length > peg$currPos) {
        s1 = input.charAt(peg$currPos);
        peg$currPos++;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e17);
        }
      }
      peg$silentFails--;
      if (s1 === peg$FAILED) {
        s0 = void 0;
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    }
    return s0;
  }
  function peg$parse_() {
    var s0, s1;
    s0 = [];
    s1 = input.charAt(peg$currPos);
    if (peg$r4.test(s1)) {
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e18);
      }
    }
    while (s1 !== peg$FAILED) {
      s0.push(s1);
      s1 = input.charAt(peg$currPos);
      if (peg$r4.test(s1)) {
        peg$currPos++;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e18);
        }
      }
    }
    return s0;
  }
  function peg$parse__() {
    var s0, s1;
    s0 = [];
    s1 = input.charAt(peg$currPos);
    if (peg$r4.test(s1)) {
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e18);
      }
    }
    if (s1 !== peg$FAILED) {
      while (s1 !== peg$FAILED) {
        s0.push(s1);
        s1 = input.charAt(peg$currPos);
        if (peg$r4.test(s1)) {
          peg$currPos++;
        } else {
          s1 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e18);
          }
        }
      }
    } else {
      s0 = peg$FAILED;
    }
    return s0;
  }
  peg$result = peg$startRuleFunction();
  if (options.peg$library) {
    return (
      /** @type {any} */
      {
        peg$result,
        peg$currPos,
        peg$FAILED,
        peg$maxFailExpected,
        peg$maxFailPos
      }
    );
  }
  if (peg$result !== peg$FAILED && peg$currPos === input.length) {
    return peg$result;
  } else {
    if (peg$result !== peg$FAILED && peg$currPos < input.length) {
      peg$fail(peg$endExpectation());
    }
    throw peg$buildStructuredError(
      peg$maxFailExpected,
      peg$maxFailPos < input.length ? input.charAt(peg$maxFailPos) : null,
      peg$maxFailPos < input.length ? peg$computeLocation(peg$maxFailPos, peg$maxFailPos + 1) : peg$computeLocation(peg$maxFailPos, peg$maxFailPos)
    );
  }
}
var er = {
  parseMermaid(input) {
    return peg$parse14(input);
  },
  parseYaml(input) {
    return JSON.parse(input);
  },
  layout(ir, theme) {
    return layoutEr(ir, theme);
  }
};
var ARROW_END_ID = "block-arrow";
var ARROW_START_ID = "block-arrow-start";
function edgeDash(style) {
  switch (style) {
    case "dotted":
      return "6 3";
    case "dashed":
      return "8 4";
    default:
      return void 0;
  }
}
function edgeStrokeWidth(style, base) {
  return style === "thick" ? base * 2 : base;
}
function layoutBlock(ir, theme) {
  const { palette, typography, spacing } = theme;
  const p = pen(theme);
  const margin = spacing.diagramMargin;
  const font = typography.baseFontSize;
  const smallFont = typography.smallFontSize;
  const cols = Math.max(1, ir.columns);
  const cellW = 150;
  const gap = 40;
  const defaultRowH = 56;
  const titleH = ir.metadata.title ? typography.titleFontSize + 14 : 0;
  const top = margin + titleH;
  const blockInfos = /* @__PURE__ */ new Map();
  for (const b of ir.blocks) {
    const span = Math.min(b.span, cols);
    const w = span * cellW + (span - 1) * gap;
    const info = measureFormattedText(b.label, font, smallFont, w - 24, 4);
    blockInfos.set(b.id, info);
  }
  const rects = /* @__PURE__ */ new Map();
  let col = 0, row = 0;
  const rowItems = [];
  const rowMaxLines = /* @__PURE__ */ new Map();
  for (const b of ir.blocks) {
    const span = Math.min(b.span, cols);
    if (col + span > cols) {
      col = 0;
      row += 1;
    }
    const info = blockInfos.get(b.id);
    rowMaxLines.set(row, Math.max(rowMaxLines.get(row) ?? 1, info.lineCount));
    rowItems.push({ b, span, col, row });
    col += span;
  }
  const rowY = /* @__PURE__ */ new Map();
  const rowHeightMap = /* @__PURE__ */ new Map();
  let curY = top;
  for (let r = 0; r <= row; r++) {
    const maxL = rowMaxLines.get(r) ?? 1;
    const h2 = maxL > 1 ? rhu(maxL * (font * 1.25) + 20) : defaultRowH;
    rowY.set(r, curY);
    rowHeightMap.set(r, h2);
    curY += h2 + gap;
  }
  for (const item of rowItems) {
    const x = margin + item.col * (cellW + gap);
    const w = item.span * cellW + (item.span - 1) * gap;
    const y = rowY.get(item.row);
    const h2 = rowHeightMap.get(item.row);
    rects.set(item.b.id, { x, y, width: w, height: h2 });
  }
  const elements = [];
  if (ir.metadata.title)
    elements.push(
      p.text(
        ir.metadata.title,
        margin,
        margin + typography.titleFontSize,
        typography.titleFontSize,
        palette.text,
        { weight: "bold" }
      )
    );
  for (const e of ir.edges) {
    const a = rects.get(e.from), b = rects.get(e.to);
    if (!a || !b) continue;
    const ac = { x: a.x + a.width / 2, y: a.y + a.height / 2 };
    const bc = { x: b.x + b.width / 2, y: b.y + b.height / 2 };
    const pa = borderPoint(a, bc.x, bc.y);
    const pb = borderPoint(b, ac.x, ac.y);
    const style = e.style ?? "solid";
    const pathOpts = {};
    if ((e.endMarker ?? "arrow") === "arrow") pathOpts.markerEnd = ARROW_END_ID;
    if (e.startMarker === "arrow") pathOpts.markerStart = ARROW_START_ID;
    const dash = edgeDash(style);
    if (dash) pathOpts.dash = dash;
    let anim;
    if (e.animation === "none") anim = void 0;
    else if (e.animation) anim = e.animation;
    else if (style === "dotted" || style === "dashed") anim = "march";
    if (anim) pathOpts.animated = anim;
    const path = style === "wavy" ? wavifyPath([pa, pb], 3, 12) : `M ${rhu(pa.x)} ${rhu(pa.y)} L ${rhu(pb.x)} ${rhu(pb.y)}`;
    elements.push(p.path(path, palette.primary, edgeStrokeWidth(style, 1.6), pathOpts));
    if (e.label)
      elements.push(
        p.text(
          e.label,
          rhuInt((pa.x + pb.x) / 2),
          rhuInt((pa.y + pb.y) / 2 - 4),
          typography.smallFontSize,
          palette.textMuted,
          { anchor: "middle" }
        )
      );
  }
  ir.blocks.forEach((b, i) => {
    const r = rects.get(b.id);
    const hue = categoricalHue(i, theme);
    const info = blockInfos.get(b.id);
    elements.push(
      p.rect(
        { x: rhu(r.x), y: rhu(r.y), width: rhu(r.width), height: rhu(r.height) },
        palette.surface,
        hue,
        1.6,
        { rx: 8 }
      )
    );
    if (info) {
      elements.push(
        ...renderFormattedText(
          p,
          info,
          r.x,
          r.y,
          r.width,
          r.height,
          font,
          smallFont,
          palette.text,
          palette.textMuted,
          { align: "middle", defaultBold: true }
        )
      );
    }
  });
  const maxRight2 = Math.max(margin, ...[...rects.values()].map((r) => r.x + r.width));
  const maxBottom2 = Math.max(top, ...[...rects.values()].map((r) => r.y + r.height));
  const s = theme.edges?.arrowSize ?? 8;
  const sH = rhu(s * 0.7);
  const sMidY = rhu(s * 0.35);
  const sRefX = rhu(s - 1);
  const defs = [
    `<marker id="${ARROW_END_ID}" markerWidth="${s}" markerHeight="${sH}" refX="${sRefX}" refY="${sMidY}" orient="auto" markerUnits="userSpaceOnUse"><polygon points="0 0, ${s} ${sMidY}, 0 ${sH}" fill="${palette.primary}" /></marker>`,
    `<marker id="${ARROW_START_ID}" markerWidth="${s}" markerHeight="${sH}" refX="${sRefX}" refY="${sMidY}" orient="auto-start-reverse" markerUnits="userSpaceOnUse"><polygon points="0 0, ${s} ${sMidY}, 0 ${sH}" fill="${palette.primary}" /></marker>`
  ];
  const scene = applyOverlays(
    {
      viewBox: { x: 0, y: 0, width: rhuInt(maxRight2 + margin), height: rhuInt(maxBottom2 + margin) },
      background: palette.background,
      elements,
      defs
    },
    ir.overlays,
    theme
  );
  return { scene, anchors: {} };
}
var ANIMATION_SET = new Set(CONNECTOR_ANIMATIONS);
var CONNECTOR_TOKEN_MAP = {
  "<-->": { style: "solid", startMarker: "arrow", endMarker: "arrow" },
  "<-.->": { style: "dotted", startMarker: "arrow", endMarker: "arrow" },
  "<==>": { style: "thick", startMarker: "arrow", endMarker: "arrow" },
  "<-_->": { style: "dashed", startMarker: "arrow", endMarker: "arrow" },
  "<-~->": { style: "wavy", startMarker: "arrow", endMarker: "arrow" },
  "-->": { style: "solid", startMarker: "none", endMarker: "arrow" },
  "-.->": { style: "dotted", startMarker: "none", endMarker: "arrow" },
  "==>": { style: "thick", startMarker: "none", endMarker: "arrow" },
  "-_->": { style: "dashed", startMarker: "none", endMarker: "arrow" },
  "-~->": { style: "wavy", startMarker: "none", endMarker: "arrow" },
  "---": { style: "solid", startMarker: "none", endMarker: "none" },
  "-.-": { style: "dotted", startMarker: "none", endMarker: "none" },
  "===": { style: "thick", startMarker: "none", endMarker: "none" },
  "-_-": { style: "dashed", startMarker: "none", endMarker: "none" },
  "-~-": { style: "wavy", startMarker: "none", endMarker: "none" }
};
function assertAnim(value) {
  if (value === "none") return value;
  if (!ANIMATION_SET.has(value)) {
    throw new Error(`Invalid connector animation "${value}"`);
  }
  return value;
}
function peg$subclass15(child, parent) {
  function C() {
    this.constructor = child;
  }
  C.prototype = parent.prototype;
  child.prototype = new C();
}
function peg$SyntaxError15(message, expected, found, location) {
  var self = Error.call(this, message);
  if (Object.setPrototypeOf) {
    Object.setPrototypeOf(self, peg$SyntaxError15.prototype);
  }
  self.expected = expected;
  self.found = found;
  self.location = location;
  self.name = "SyntaxError";
  return self;
}
peg$subclass15(peg$SyntaxError15, Error);
function peg$padEnd15(str, targetLength, padString) {
  padString = padString || " ";
  if (str.length > targetLength) {
    return str;
  }
  targetLength -= str.length;
  padString += padString.repeat(targetLength);
  return str + padString.slice(0, targetLength);
}
peg$SyntaxError15.prototype.format = function(sources) {
  var str = "Error: " + this.message;
  if (this.location) {
    var src = null;
    var k;
    for (k = 0; k < sources.length; k++) {
      if (sources[k].source === this.location.source) {
        src = sources[k].text.split(/\r\n|\n|\r/g);
        break;
      }
    }
    var s = this.location.start;
    var offset_s = this.location.source && typeof this.location.source.offset === "function" ? this.location.source.offset(s) : s;
    var loc = this.location.source + ":" + offset_s.line + ":" + offset_s.column;
    if (src) {
      var e = this.location.end;
      var filler = peg$padEnd15("", offset_s.line.toString().length, " ");
      var line = src[s.line - 1];
      var last = s.line === e.line ? e.column : line.length + 1;
      var hatLen = last - s.column || 1;
      str += "\n --> " + loc + "\n" + filler + " |\n" + offset_s.line + " | " + line + "\n" + filler + " | " + peg$padEnd15("", s.column - 1, " ") + peg$padEnd15("", hatLen, "^");
    } else {
      str += "\n at " + loc;
    }
  }
  return str;
};
peg$SyntaxError15.buildMessage = function(expected, found) {
  var DESCRIBE_EXPECTATION_FNS = {
    literal: function(expectation) {
      return '"' + literalEscape(expectation.text) + '"';
    },
    class: function(expectation) {
      var escapedParts = expectation.parts.map(function(part) {
        return Array.isArray(part) ? classEscape(part[0]) + "-" + classEscape(part[1]) : classEscape(part);
      });
      return "[" + (expectation.inverted ? "^" : "") + escapedParts.join("") + "]";
    },
    any: function() {
      return "any character";
    },
    end: function() {
      return "end of input";
    },
    other: function(expectation) {
      return expectation.description;
    }
  };
  function hex(ch) {
    return ch.charCodeAt(0).toString(16).toUpperCase();
  }
  function literalEscape(s) {
    return s.replace(/\\/g, "\\\\").replace(/"/g, '\\"').replace(/\0/g, "\\0").replace(/\t/g, "\\t").replace(/\n/g, "\\n").replace(/\r/g, "\\r").replace(/[\x00-\x0F]/g, function(ch) {
      return "\\x0" + hex(ch);
    }).replace(/[\x10-\x1F\x7F-\x9F]/g, function(ch) {
      return "\\x" + hex(ch);
    });
  }
  function classEscape(s) {
    return s.replace(/\\/g, "\\\\").replace(/\]/g, "\\]").replace(/\^/g, "\\^").replace(/-/g, "\\-").replace(/\0/g, "\\0").replace(/\t/g, "\\t").replace(/\n/g, "\\n").replace(/\r/g, "\\r").replace(/[\x00-\x0F]/g, function(ch) {
      return "\\x0" + hex(ch);
    }).replace(/[\x10-\x1F\x7F-\x9F]/g, function(ch) {
      return "\\x" + hex(ch);
    });
  }
  function describeExpectation(expectation) {
    return DESCRIBE_EXPECTATION_FNS[expectation.type](expectation);
  }
  function describeExpected(expected2) {
    var descriptions = expected2.map(describeExpectation);
    var i, j;
    descriptions.sort();
    if (descriptions.length > 0) {
      for (i = 1, j = 1; i < descriptions.length; i++) {
        if (descriptions[i - 1] !== descriptions[i]) {
          descriptions[j] = descriptions[i];
          j++;
        }
      }
      descriptions.length = j;
    }
    switch (descriptions.length) {
      case 1:
        return descriptions[0];
      case 2:
        return descriptions[0] + " or " + descriptions[1];
      default:
        return descriptions.slice(0, -1).join(", ") + ", or " + descriptions[descriptions.length - 1];
    }
  }
  function describeFound(found2) {
    return found2 ? '"' + literalEscape(found2) + '"' : "end of input";
  }
  return "Expected " + describeExpected(expected) + " but " + describeFound(found) + " found.";
};
function peg$parse15(input, options) {
  options = options !== void 0 ? options : {};
  var peg$FAILED = {};
  var peg$source = options.grammarSource;
  var peg$startRuleFunctions = { Document: peg$parseDocument };
  var peg$startRuleFunction = peg$parseDocument;
  var peg$c0 = "block-beta";
  var peg$c1 = "---";
  var peg$c2 = "columns";
  var peg$c3 = "|";
  var peg$c4 = "<-~->";
  var peg$c5 = "<-_->";
  var peg$c6 = "<-.->";
  var peg$c7 = "<==>";
  var peg$c8 = "<-->";
  var peg$c9 = "-~->";
  var peg$c10 = "-_->";
  var peg$c11 = "-.->";
  var peg$c12 = "==>";
  var peg$c13 = "-->";
  var peg$c14 = "-~-";
  var peg$c15 = "-_-";
  var peg$c16 = "-.-";
  var peg$c17 = "===";
  var peg$c18 = "@";
  var peg$c19 = "anim:";
  var peg$c20 = "{";
  var peg$c21 = ",";
  var peg$c22 = "}";
  var peg$c23 = ":";
  var peg$c24 = "[";
  var peg$c25 = "]";
  var peg$c26 = "\r";
  var peg$c27 = "\n";
  var peg$r0 = /^[^\n]/;
  var peg$r1 = /^[^|\n]/;
  var peg$r2 = /^[^\]\n]/;
  var peg$r3 = /^[A-Za-z0-9_]/;
  var peg$r4 = /^[0-9]/;
  var peg$r5 = /^[ \t]/;
  var peg$e0 = peg$literalExpectation("block-beta", true);
  var peg$e1 = peg$literalExpectation("---", false);
  var peg$e2 = peg$classExpectation(["\n"], true, false);
  var peg$e3 = peg$literalExpectation("columns", true);
  var peg$e4 = peg$literalExpectation("|", false);
  var peg$e5 = peg$classExpectation(["|", "\n"], true, false);
  var peg$e6 = peg$literalExpectation("<-~->", false);
  var peg$e7 = peg$literalExpectation("<-_->", false);
  var peg$e8 = peg$literalExpectation("<-.->", false);
  var peg$e9 = peg$literalExpectation("<==>", false);
  var peg$e10 = peg$literalExpectation("<-->", false);
  var peg$e11 = peg$literalExpectation("-~->", false);
  var peg$e12 = peg$literalExpectation("-_->", false);
  var peg$e13 = peg$literalExpectation("-.->", false);
  var peg$e14 = peg$literalExpectation("==>", false);
  var peg$e15 = peg$literalExpectation("-->", false);
  var peg$e16 = peg$literalExpectation("-~-", false);
  var peg$e17 = peg$literalExpectation("-_-", false);
  var peg$e18 = peg$literalExpectation("-.-", false);
  var peg$e19 = peg$literalExpectation("===", false);
  var peg$e20 = peg$literalExpectation("@", false);
  var peg$e21 = peg$literalExpectation("anim:", false);
  var peg$e22 = peg$literalExpectation("{", false);
  var peg$e23 = peg$literalExpectation(",", false);
  var peg$e24 = peg$literalExpectation("}", false);
  var peg$e25 = peg$literalExpectation(":", false);
  var peg$e26 = peg$literalExpectation("[", false);
  var peg$e27 = peg$classExpectation(["]", "\n"], true, false);
  var peg$e28 = peg$literalExpectation("]", false);
  var peg$e29 = peg$classExpectation([["A", "Z"], ["a", "z"], ["0", "9"], "_"], false, false);
  var peg$e30 = peg$classExpectation([["0", "9"]], false, false);
  var peg$e31 = peg$literalExpectation("\r", false);
  var peg$e32 = peg$literalExpectation("\n", false);
  var peg$e33 = peg$anyExpectation();
  var peg$e34 = peg$classExpectation([" ", "	"], false, false);
  var peg$f0 = function(fm, stmts) {
    let columns = 1;
    const blocks = [];
    const edges2 = [];
    for (const s of stmts.filter(Boolean)) {
      if (s.t === "columns") columns = s.n;
      else if (s.t === "blocks") for (const b of s.items) blocks.push(b);
      else if (s.t === "edge") edges2.push({ from: s.from, to: s.to, ...s.label ? { label: s.label } : {}, style: s.style, startMarker: s.startMarker, endMarker: s.endMarker, ...s.animation !== void 0 ? { animation: s.animation } : {} });
    }
    return { version: "1.0", metadata: { ...fm || {} }, columns, blocks, edges: edges2 };
  };
  var peg$f1 = function(line) {
    return line;
  };
  var peg$f2 = function(lines2) {
    const meta = {};
    for (const line of lines2) {
      const m = line.match(/^\s*(\w+)\s*:\s*(.+?)\s*$/);
      if (m) meta[m[1]] = m[2];
    }
    return meta;
  };
  var peg$f3 = function(n) {
    return { t: "columns", n };
  };
  var peg$f4 = function(from, conn, l) {
    return l.trim();
  };
  var peg$f5 = function(from, conn, label, to, tail) {
    return { t: "edge", from, to, label: label || void 0, ...conn, ...tail.animation !== void 0 ? { animation: tail.animation } : {} };
  };
  var peg$f6 = function(token) {
    return CONNECTOR_TOKEN_MAP[token];
  };
  var peg$f7 = function(anns, block2, rest) {
    if (/@anim:/.test(rest)) {
      throw new Error("Invalid connector annotation");
    }
    const result = {};
    if (block2) {
      const b = block2[1];
      if (b.anim !== void 0) result.animation = assertAnim(b.anim);
    }
    for (const ann of anns) {
      Object.assign(result, ann[2]);
    }
    return result;
  };
  var peg$f8 = function(value) {
    return { animation: value };
  };
  var peg$f9 = function(value) {
    return assertAnim(value);
  };
  var peg$f10 = function(first, rest) {
    const result = {};
    result[first.key] = first.value;
    for (const r of rest) {
      result[r[3].key] = r[3].value;
    }
    return result;
  };
  var peg$f11 = function(key, value) {
    return { key, value };
  };
  var peg$f12 = function(value) {
    return value;
  };
  var peg$f13 = function(first, b) {
    return b;
  };
  var peg$f14 = function(first, rest) {
    return { t: "blocks", items: [first, ...rest] };
  };
  var peg$f15 = function(id, t) {
    return t;
  };
  var peg$f16 = function(id, label, n) {
    return n;
  };
  var peg$f17 = function(id, label, span) {
    return { id, label: label != null ? label.replace(/^["']|["']$/g, "") : id, span: span || 1 };
  };
  var peg$f18 = function(n) {
    return parseInt(n, 10);
  };
  var peg$f19 = function() {
    return null;
  };
  var peg$currPos = options.peg$currPos | 0;
  var peg$savedPos = peg$currPos;
  var peg$posDetailsCache = [{ line: 1, column: 1 }];
  var peg$maxFailPos = peg$currPos;
  var peg$maxFailExpected = options.peg$maxFailExpected || [];
  var peg$silentFails = options.peg$silentFails | 0;
  var peg$result;
  if (options.startRule) {
    if (!(options.startRule in peg$startRuleFunctions)) {
      throw new Error(`Can't start parsing from rule "` + options.startRule + '".');
    }
    peg$startRuleFunction = peg$startRuleFunctions[options.startRule];
  }
  function text() {
    return input.substring(peg$savedPos, peg$currPos);
  }
  function offset() {
    return peg$savedPos;
  }
  function range() {
    return {
      source: peg$source,
      start: peg$savedPos,
      end: peg$currPos
    };
  }
  function location() {
    return peg$computeLocation(peg$savedPos, peg$currPos);
  }
  function expected(description, location2) {
    location2 = location2 !== void 0 ? location2 : peg$computeLocation(peg$savedPos, peg$currPos);
    throw peg$buildStructuredError(
      [peg$otherExpectation(description)],
      input.substring(peg$savedPos, peg$currPos),
      location2
    );
  }
  function error(message, location2) {
    location2 = location2 !== void 0 ? location2 : peg$computeLocation(peg$savedPos, peg$currPos);
    throw peg$buildSimpleError(message, location2);
  }
  function peg$literalExpectation(text2, ignoreCase) {
    return { type: "literal", text: text2, ignoreCase };
  }
  function peg$classExpectation(parts, inverted, ignoreCase) {
    return { type: "class", parts, inverted, ignoreCase };
  }
  function peg$anyExpectation() {
    return { type: "any" };
  }
  function peg$endExpectation() {
    return { type: "end" };
  }
  function peg$otherExpectation(description) {
    return { type: "other", description };
  }
  function peg$computePosDetails(pos) {
    var details = peg$posDetailsCache[pos];
    var p;
    if (details) {
      return details;
    } else {
      if (pos >= peg$posDetailsCache.length) {
        p = peg$posDetailsCache.length - 1;
      } else {
        p = pos;
        while (!peg$posDetailsCache[--p]) {
        }
      }
      details = peg$posDetailsCache[p];
      details = {
        line: details.line,
        column: details.column
      };
      while (p < pos) {
        if (input.charCodeAt(p) === 10) {
          details.line++;
          details.column = 1;
        } else {
          details.column++;
        }
        p++;
      }
      peg$posDetailsCache[pos] = details;
      return details;
    }
  }
  function peg$computeLocation(startPos, endPos, offset2) {
    var startPosDetails = peg$computePosDetails(startPos);
    var endPosDetails = peg$computePosDetails(endPos);
    var res = {
      source: peg$source,
      start: {
        offset: startPos,
        line: startPosDetails.line,
        column: startPosDetails.column
      },
      end: {
        offset: endPos,
        line: endPosDetails.line,
        column: endPosDetails.column
      }
    };
    if (offset2 && peg$source && typeof peg$source.offset === "function") {
      res.start = peg$source.offset(res.start);
      res.end = peg$source.offset(res.end);
    }
    return res;
  }
  function peg$fail(expected2) {
    if (peg$currPos < peg$maxFailPos) {
      return;
    }
    if (peg$currPos > peg$maxFailPos) {
      peg$maxFailPos = peg$currPos;
      peg$maxFailExpected = [];
    }
    peg$maxFailExpected.push(expected2);
  }
  function peg$buildSimpleError(message, location2) {
    return new peg$SyntaxError15(message, null, null, location2);
  }
  function peg$buildStructuredError(expected2, found, location2) {
    return new peg$SyntaxError15(
      peg$SyntaxError15.buildMessage(expected2, found),
      expected2,
      found,
      location2
    );
  }
  function peg$parseDocument() {
    var s0, s1, s2, s3, s4, s5, s6;
    s0 = peg$currPos;
    s1 = peg$parseFrontmatter();
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    s2 = peg$parse_();
    s3 = input.substr(peg$currPos, 10);
    if (s3.toLowerCase() === peg$c0) {
      peg$currPos += 10;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e0);
      }
    }
    if (s3 !== peg$FAILED) {
      s4 = peg$parseLineEnd();
      if (s4 !== peg$FAILED) {
        s5 = [];
        s6 = peg$parseStatement();
        while (s6 !== peg$FAILED) {
          s5.push(s6);
          s6 = peg$parseStatement();
        }
        s6 = peg$parse_();
        peg$savedPos = s0;
        s0 = peg$f0(s1, s5);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseFrontmatter() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 3) === peg$c1) {
      s1 = peg$c1;
      peg$currPos += 3;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e1);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$parseNL();
      if (s2 !== peg$FAILED) {
        s3 = [];
        s4 = peg$currPos;
        s5 = peg$currPos;
        peg$silentFails++;
        s6 = peg$currPos;
        if (input.substr(peg$currPos, 3) === peg$c1) {
          s7 = peg$c1;
          peg$currPos += 3;
        } else {
          s7 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e1);
          }
        }
        if (s7 !== peg$FAILED) {
          s8 = peg$parseNL();
          if (s8 !== peg$FAILED) {
            s7 = [s7, s8];
            s6 = s7;
          } else {
            peg$currPos = s6;
            s6 = peg$FAILED;
          }
        } else {
          peg$currPos = s6;
          s6 = peg$FAILED;
        }
        peg$silentFails--;
        if (s6 === peg$FAILED) {
          s5 = void 0;
        } else {
          peg$currPos = s5;
          s5 = peg$FAILED;
        }
        if (s5 !== peg$FAILED) {
          s6 = peg$currPos;
          s7 = [];
          s8 = input.charAt(peg$currPos);
          if (peg$r0.test(s8)) {
            peg$currPos++;
          } else {
            s8 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e2);
            }
          }
          while (s8 !== peg$FAILED) {
            s7.push(s8);
            s8 = input.charAt(peg$currPos);
            if (peg$r0.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e2);
              }
            }
          }
          s6 = input.substring(s6, peg$currPos);
          s7 = peg$parseNL();
          if (s7 !== peg$FAILED) {
            peg$savedPos = s4;
            s4 = peg$f1(s6);
          } else {
            peg$currPos = s4;
            s4 = peg$FAILED;
          }
        } else {
          peg$currPos = s4;
          s4 = peg$FAILED;
        }
        while (s4 !== peg$FAILED) {
          s3.push(s4);
          s4 = peg$currPos;
          s5 = peg$currPos;
          peg$silentFails++;
          s6 = peg$currPos;
          if (input.substr(peg$currPos, 3) === peg$c1) {
            s7 = peg$c1;
            peg$currPos += 3;
          } else {
            s7 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e1);
            }
          }
          if (s7 !== peg$FAILED) {
            s8 = peg$parseNL();
            if (s8 !== peg$FAILED) {
              s7 = [s7, s8];
              s6 = s7;
            } else {
              peg$currPos = s6;
              s6 = peg$FAILED;
            }
          } else {
            peg$currPos = s6;
            s6 = peg$FAILED;
          }
          peg$silentFails--;
          if (s6 === peg$FAILED) {
            s5 = void 0;
          } else {
            peg$currPos = s5;
            s5 = peg$FAILED;
          }
          if (s5 !== peg$FAILED) {
            s6 = peg$currPos;
            s7 = [];
            s8 = input.charAt(peg$currPos);
            if (peg$r0.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e2);
              }
            }
            while (s8 !== peg$FAILED) {
              s7.push(s8);
              s8 = input.charAt(peg$currPos);
              if (peg$r0.test(s8)) {
                peg$currPos++;
              } else {
                s8 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e2);
                }
              }
            }
            s6 = input.substring(s6, peg$currPos);
            s7 = peg$parseNL();
            if (s7 !== peg$FAILED) {
              peg$savedPos = s4;
              s4 = peg$f1(s6);
            } else {
              peg$currPos = s4;
              s4 = peg$FAILED;
            }
          } else {
            peg$currPos = s4;
            s4 = peg$FAILED;
          }
        }
        if (input.substr(peg$currPos, 3) === peg$c1) {
          s4 = peg$c1;
          peg$currPos += 3;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e1);
          }
        }
        if (s4 !== peg$FAILED) {
          s5 = peg$parseNL();
          if (s5 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f2(s3);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseStatement() {
    var s0;
    s0 = peg$parseColumnsLine();
    if (s0 === peg$FAILED) {
      s0 = peg$parseEdgeLine();
      if (s0 === peg$FAILED) {
        s0 = peg$parseBlockLine();
        if (s0 === peg$FAILED) {
          s0 = peg$parseBlankLine();
        }
      }
    }
    return s0;
  }
  function peg$parseColumnsLine() {
    var s0, s1, s2, s3, s4, s5;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = input.substr(peg$currPos, 7);
    if (s2.toLowerCase() === peg$c2) {
      peg$currPos += 7;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e3);
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$parse__();
      if (s3 !== peg$FAILED) {
        s4 = peg$parseInteger();
        if (s4 !== peg$FAILED) {
          s5 = peg$parseLineEnd();
          if (s5 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f3(s4);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseEdgeLine() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9, s10;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = peg$parseIdent();
    if (s2 !== peg$FAILED) {
      s3 = peg$parse_();
      s4 = peg$parseConnector();
      if (s4 !== peg$FAILED) {
        s5 = peg$parse_();
        s6 = peg$currPos;
        if (input.charCodeAt(peg$currPos) === 124) {
          s7 = peg$c3;
          peg$currPos++;
        } else {
          s7 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e4);
          }
        }
        if (s7 !== peg$FAILED) {
          s8 = peg$currPos;
          s9 = [];
          s10 = input.charAt(peg$currPos);
          if (peg$r1.test(s10)) {
            peg$currPos++;
          } else {
            s10 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e5);
            }
          }
          if (s10 !== peg$FAILED) {
            while (s10 !== peg$FAILED) {
              s9.push(s10);
              s10 = input.charAt(peg$currPos);
              if (peg$r1.test(s10)) {
                peg$currPos++;
              } else {
                s10 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e5);
                }
              }
            }
          } else {
            s9 = peg$FAILED;
          }
          if (s9 !== peg$FAILED) {
            s8 = input.substring(s8, peg$currPos);
          } else {
            s8 = s9;
          }
          if (s8 !== peg$FAILED) {
            if (input.charCodeAt(peg$currPos) === 124) {
              s9 = peg$c3;
              peg$currPos++;
            } else {
              s9 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e4);
              }
            }
            if (s9 !== peg$FAILED) {
              peg$savedPos = s6;
              s6 = peg$f4(s2, s4, s8);
            } else {
              peg$currPos = s6;
              s6 = peg$FAILED;
            }
          } else {
            peg$currPos = s6;
            s6 = peg$FAILED;
          }
        } else {
          peg$currPos = s6;
          s6 = peg$FAILED;
        }
        if (s6 === peg$FAILED) {
          s6 = null;
        }
        s7 = peg$parse_();
        s8 = peg$parseIdent();
        if (s8 !== peg$FAILED) {
          s9 = peg$parseEdgeTail();
          s10 = peg$parseLineEnd();
          if (s10 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f5(s2, s4, s6, s8, s9);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseConnector() {
    var s0, s1;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 5) === peg$c4) {
      s1 = peg$c4;
      peg$currPos += 5;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e6);
      }
    }
    if (s1 === peg$FAILED) {
      if (input.substr(peg$currPos, 5) === peg$c5) {
        s1 = peg$c5;
        peg$currPos += 5;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e7);
        }
      }
      if (s1 === peg$FAILED) {
        if (input.substr(peg$currPos, 5) === peg$c6) {
          s1 = peg$c6;
          peg$currPos += 5;
        } else {
          s1 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e8);
          }
        }
        if (s1 === peg$FAILED) {
          if (input.substr(peg$currPos, 4) === peg$c7) {
            s1 = peg$c7;
            peg$currPos += 4;
          } else {
            s1 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e9);
            }
          }
          if (s1 === peg$FAILED) {
            if (input.substr(peg$currPos, 4) === peg$c8) {
              s1 = peg$c8;
              peg$currPos += 4;
            } else {
              s1 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e10);
              }
            }
            if (s1 === peg$FAILED) {
              if (input.substr(peg$currPos, 4) === peg$c9) {
                s1 = peg$c9;
                peg$currPos += 4;
              } else {
                s1 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e11);
                }
              }
              if (s1 === peg$FAILED) {
                if (input.substr(peg$currPos, 4) === peg$c10) {
                  s1 = peg$c10;
                  peg$currPos += 4;
                } else {
                  s1 = peg$FAILED;
                  if (peg$silentFails === 0) {
                    peg$fail(peg$e12);
                  }
                }
                if (s1 === peg$FAILED) {
                  if (input.substr(peg$currPos, 4) === peg$c11) {
                    s1 = peg$c11;
                    peg$currPos += 4;
                  } else {
                    s1 = peg$FAILED;
                    if (peg$silentFails === 0) {
                      peg$fail(peg$e13);
                    }
                  }
                  if (s1 === peg$FAILED) {
                    if (input.substr(peg$currPos, 3) === peg$c12) {
                      s1 = peg$c12;
                      peg$currPos += 3;
                    } else {
                      s1 = peg$FAILED;
                      if (peg$silentFails === 0) {
                        peg$fail(peg$e14);
                      }
                    }
                    if (s1 === peg$FAILED) {
                      if (input.substr(peg$currPos, 3) === peg$c13) {
                        s1 = peg$c13;
                        peg$currPos += 3;
                      } else {
                        s1 = peg$FAILED;
                        if (peg$silentFails === 0) {
                          peg$fail(peg$e15);
                        }
                      }
                      if (s1 === peg$FAILED) {
                        if (input.substr(peg$currPos, 3) === peg$c14) {
                          s1 = peg$c14;
                          peg$currPos += 3;
                        } else {
                          s1 = peg$FAILED;
                          if (peg$silentFails === 0) {
                            peg$fail(peg$e16);
                          }
                        }
                        if (s1 === peg$FAILED) {
                          if (input.substr(peg$currPos, 3) === peg$c15) {
                            s1 = peg$c15;
                            peg$currPos += 3;
                          } else {
                            s1 = peg$FAILED;
                            if (peg$silentFails === 0) {
                              peg$fail(peg$e17);
                            }
                          }
                          if (s1 === peg$FAILED) {
                            if (input.substr(peg$currPos, 3) === peg$c16) {
                              s1 = peg$c16;
                              peg$currPos += 3;
                            } else {
                              s1 = peg$FAILED;
                              if (peg$silentFails === 0) {
                                peg$fail(peg$e18);
                              }
                            }
                            if (s1 === peg$FAILED) {
                              if (input.substr(peg$currPos, 3) === peg$c17) {
                                s1 = peg$c17;
                                peg$currPos += 3;
                              } else {
                                s1 = peg$FAILED;
                                if (peg$silentFails === 0) {
                                  peg$fail(peg$e19);
                                }
                              }
                              if (s1 === peg$FAILED) {
                                if (input.substr(peg$currPos, 3) === peg$c1) {
                                  s1 = peg$c1;
                                  peg$currPos += 3;
                                } else {
                                  s1 = peg$FAILED;
                                  if (peg$silentFails === 0) {
                                    peg$fail(peg$e1);
                                  }
                                }
                              }
                            }
                          }
                        }
                      }
                    }
                  }
                }
              }
            }
          }
        }
      }
    }
    if (s1 !== peg$FAILED) {
      peg$savedPos = s0;
      s1 = peg$f6(s1);
    }
    s0 = s1;
    return s0;
  }
  function peg$parseEdgeTail() {
    var s0, s1, s2, s3, s4, s5;
    s0 = peg$currPos;
    s1 = [];
    s2 = peg$currPos;
    s3 = peg$parse_();
    if (input.charCodeAt(peg$currPos) === 64) {
      s4 = peg$c18;
      peg$currPos++;
    } else {
      s4 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e20);
      }
    }
    if (s4 !== peg$FAILED) {
      s5 = peg$parseAnnotation();
      if (s5 !== peg$FAILED) {
        s3 = [s3, s4, s5];
        s2 = s3;
      } else {
        peg$currPos = s2;
        s2 = peg$FAILED;
      }
    } else {
      peg$currPos = s2;
      s2 = peg$FAILED;
    }
    while (s2 !== peg$FAILED) {
      s1.push(s2);
      s2 = peg$currPos;
      s3 = peg$parse_();
      if (input.charCodeAt(peg$currPos) === 64) {
        s4 = peg$c18;
        peg$currPos++;
      } else {
        s4 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e20);
        }
      }
      if (s4 !== peg$FAILED) {
        s5 = peg$parseAnnotation();
        if (s5 !== peg$FAILED) {
          s3 = [s3, s4, s5];
          s2 = s3;
        } else {
          peg$currPos = s2;
          s2 = peg$FAILED;
        }
      } else {
        peg$currPos = s2;
        s2 = peg$FAILED;
      }
    }
    s2 = peg$currPos;
    s3 = peg$parse_();
    s4 = peg$parsePropBlock();
    if (s4 !== peg$FAILED) {
      s3 = [s3, s4];
      s2 = s3;
    } else {
      peg$currPos = s2;
      s2 = peg$FAILED;
    }
    if (s2 === peg$FAILED) {
      s2 = null;
    }
    s3 = peg$currPos;
    s4 = [];
    s5 = input.charAt(peg$currPos);
    if (peg$r0.test(s5)) {
      peg$currPos++;
    } else {
      s5 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e2);
      }
    }
    while (s5 !== peg$FAILED) {
      s4.push(s5);
      s5 = input.charAt(peg$currPos);
      if (peg$r0.test(s5)) {
        peg$currPos++;
      } else {
        s5 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e2);
        }
      }
    }
    s3 = input.substring(s3, peg$currPos);
    peg$savedPos = s0;
    s0 = peg$f7(s1, s2, s3);
    return s0;
  }
  function peg$parseAnnotation() {
    var s0, s1, s2;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 5) === peg$c19) {
      s1 = peg$c19;
      peg$currPos += 5;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e21);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$parseAnimValue();
      if (s2 !== peg$FAILED) {
        peg$savedPos = s0;
        s0 = peg$f8(s2);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseAnimValue() {
    var s0, s1;
    s0 = peg$currPos;
    s1 = peg$parseIdent();
    if (s1 !== peg$FAILED) {
      peg$savedPos = s0;
      s1 = peg$f9(s1);
    }
    s0 = s1;
    return s0;
  }
  function peg$parsePropBlock() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 123) {
      s1 = peg$c20;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e22);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$parse_();
      s3 = peg$parsePropEntry();
      if (s3 !== peg$FAILED) {
        s4 = [];
        s5 = peg$currPos;
        s6 = peg$parse_();
        if (input.charCodeAt(peg$currPos) === 44) {
          s7 = peg$c21;
          peg$currPos++;
        } else {
          s7 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e23);
          }
        }
        if (s7 !== peg$FAILED) {
          s8 = peg$parse_();
          s9 = peg$parsePropEntry();
          if (s9 !== peg$FAILED) {
            s6 = [s6, s7, s8, s9];
            s5 = s6;
          } else {
            peg$currPos = s5;
            s5 = peg$FAILED;
          }
        } else {
          peg$currPos = s5;
          s5 = peg$FAILED;
        }
        while (s5 !== peg$FAILED) {
          s4.push(s5);
          s5 = peg$currPos;
          s6 = peg$parse_();
          if (input.charCodeAt(peg$currPos) === 44) {
            s7 = peg$c21;
            peg$currPos++;
          } else {
            s7 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e23);
            }
          }
          if (s7 !== peg$FAILED) {
            s8 = peg$parse_();
            s9 = peg$parsePropEntry();
            if (s9 !== peg$FAILED) {
              s6 = [s6, s7, s8, s9];
              s5 = s6;
            } else {
              peg$currPos = s5;
              s5 = peg$FAILED;
            }
          } else {
            peg$currPos = s5;
            s5 = peg$FAILED;
          }
        }
        s5 = peg$parse_();
        if (input.charCodeAt(peg$currPos) === 125) {
          s6 = peg$c22;
          peg$currPos++;
        } else {
          s6 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e24);
          }
        }
        if (s6 !== peg$FAILED) {
          peg$savedPos = s0;
          s0 = peg$f10(s3, s4);
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parsePropEntry() {
    var s0, s1, s2, s3, s4, s5;
    s0 = peg$currPos;
    s1 = peg$parseIdent();
    if (s1 !== peg$FAILED) {
      s2 = peg$parse_();
      if (input.charCodeAt(peg$currPos) === 58) {
        s3 = peg$c23;
        peg$currPos++;
      } else {
        s3 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e25);
        }
      }
      if (s3 !== peg$FAILED) {
        s4 = peg$parse_();
        s5 = peg$parsePropValue();
        if (s5 !== peg$FAILED) {
          peg$savedPos = s0;
          s0 = peg$f11(s1, s5);
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parsePropValue() {
    var s0, s1;
    s0 = peg$currPos;
    s1 = peg$parseIdent();
    if (s1 !== peg$FAILED) {
      peg$savedPos = s0;
      s1 = peg$f12(s1);
    }
    s0 = s1;
    return s0;
  }
  function peg$parseBlockLine() {
    var s0, s1, s2, s3, s4, s5, s6;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = peg$parseBlockDef();
    if (s2 !== peg$FAILED) {
      s3 = [];
      s4 = peg$currPos;
      s5 = peg$parse__();
      if (s5 !== peg$FAILED) {
        s6 = peg$parseBlockDef();
        if (s6 !== peg$FAILED) {
          peg$savedPos = s4;
          s4 = peg$f13(s2, s6);
        } else {
          peg$currPos = s4;
          s4 = peg$FAILED;
        }
      } else {
        peg$currPos = s4;
        s4 = peg$FAILED;
      }
      while (s4 !== peg$FAILED) {
        s3.push(s4);
        s4 = peg$currPos;
        s5 = peg$parse__();
        if (s5 !== peg$FAILED) {
          s6 = peg$parseBlockDef();
          if (s6 !== peg$FAILED) {
            peg$savedPos = s4;
            s4 = peg$f13(s2, s6);
          } else {
            peg$currPos = s4;
            s4 = peg$FAILED;
          }
        } else {
          peg$currPos = s4;
          s4 = peg$FAILED;
        }
      }
      s4 = peg$parse_();
      s5 = peg$parseLineEnd();
      if (s5 !== peg$FAILED) {
        peg$savedPos = s0;
        s0 = peg$f14(s2, s3);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseBlockDef() {
    var s0, s1, s2, s3, s4, s5, s6;
    s0 = peg$currPos;
    s1 = peg$parseIdent();
    if (s1 !== peg$FAILED) {
      s2 = peg$currPos;
      if (input.charCodeAt(peg$currPos) === 91) {
        s3 = peg$c24;
        peg$currPos++;
      } else {
        s3 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e26);
        }
      }
      if (s3 !== peg$FAILED) {
        s4 = peg$currPos;
        s5 = [];
        s6 = input.charAt(peg$currPos);
        if (peg$r2.test(s6)) {
          peg$currPos++;
        } else {
          s6 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e27);
          }
        }
        if (s6 !== peg$FAILED) {
          while (s6 !== peg$FAILED) {
            s5.push(s6);
            s6 = input.charAt(peg$currPos);
            if (peg$r2.test(s6)) {
              peg$currPos++;
            } else {
              s6 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e27);
              }
            }
          }
        } else {
          s5 = peg$FAILED;
        }
        if (s5 !== peg$FAILED) {
          s4 = input.substring(s4, peg$currPos);
        } else {
          s4 = s5;
        }
        if (s4 !== peg$FAILED) {
          if (input.charCodeAt(peg$currPos) === 93) {
            s5 = peg$c25;
            peg$currPos++;
          } else {
            s5 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e28);
            }
          }
          if (s5 !== peg$FAILED) {
            peg$savedPos = s2;
            s2 = peg$f15(s1, s4);
          } else {
            peg$currPos = s2;
            s2 = peg$FAILED;
          }
        } else {
          peg$currPos = s2;
          s2 = peg$FAILED;
        }
      } else {
        peg$currPos = s2;
        s2 = peg$FAILED;
      }
      if (s2 === peg$FAILED) {
        s2 = null;
      }
      s3 = peg$currPos;
      if (input.charCodeAt(peg$currPos) === 58) {
        s4 = peg$c23;
        peg$currPos++;
      } else {
        s4 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e25);
        }
      }
      if (s4 !== peg$FAILED) {
        s5 = peg$parseInteger();
        if (s5 !== peg$FAILED) {
          peg$savedPos = s3;
          s3 = peg$f16(s1, s2, s5);
        } else {
          peg$currPos = s3;
          s3 = peg$FAILED;
        }
      } else {
        peg$currPos = s3;
        s3 = peg$FAILED;
      }
      if (s3 === peg$FAILED) {
        s3 = null;
      }
      peg$savedPos = s0;
      s0 = peg$f17(s1, s2, s3);
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseIdent() {
    var s0, s1, s2;
    s0 = peg$currPos;
    s1 = [];
    s2 = input.charAt(peg$currPos);
    if (peg$r3.test(s2)) {
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e29);
      }
    }
    if (s2 !== peg$FAILED) {
      while (s2 !== peg$FAILED) {
        s1.push(s2);
        s2 = input.charAt(peg$currPos);
        if (peg$r3.test(s2)) {
          peg$currPos++;
        } else {
          s2 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e29);
          }
        }
      }
    } else {
      s1 = peg$FAILED;
    }
    if (s1 !== peg$FAILED) {
      s0 = input.substring(s0, peg$currPos);
    } else {
      s0 = s1;
    }
    return s0;
  }
  function peg$parseInteger() {
    var s0, s1, s2, s3;
    s0 = peg$currPos;
    s1 = peg$currPos;
    s2 = [];
    s3 = input.charAt(peg$currPos);
    if (peg$r4.test(s3)) {
      peg$currPos++;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e30);
      }
    }
    if (s3 !== peg$FAILED) {
      while (s3 !== peg$FAILED) {
        s2.push(s3);
        s3 = input.charAt(peg$currPos);
        if (peg$r4.test(s3)) {
          peg$currPos++;
        } else {
          s3 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e30);
          }
        }
      }
    } else {
      s2 = peg$FAILED;
    }
    if (s2 !== peg$FAILED) {
      s1 = input.substring(s1, peg$currPos);
    } else {
      s1 = s2;
    }
    if (s1 !== peg$FAILED) {
      peg$savedPos = s0;
      s1 = peg$f18(s1);
    }
    s0 = s1;
    return s0;
  }
  function peg$parseNL() {
    var s0, s1, s2;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 13) {
      s1 = peg$c26;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e31);
      }
    }
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s2 = peg$c27;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e32);
      }
    }
    if (s2 !== peg$FAILED) {
      s1 = [s1, s2];
      s0 = s1;
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseBlankLine() {
    var s0, s1, s2, s3;
    s0 = peg$currPos;
    s1 = peg$parse_();
    if (input.charCodeAt(peg$currPos) === 13) {
      s2 = peg$c26;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e31);
      }
    }
    if (s2 === peg$FAILED) {
      s2 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s3 = peg$c27;
      peg$currPos++;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e32);
      }
    }
    if (s3 !== peg$FAILED) {
      peg$savedPos = s0;
      s0 = peg$f19();
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseLineEnd() {
    var s0, s1, s2;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 13) {
      s1 = peg$c26;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e31);
      }
    }
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s2 = peg$c27;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e32);
      }
    }
    if (s2 !== peg$FAILED) {
      s1 = [s1, s2];
      s0 = s1;
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      peg$silentFails++;
      if (input.length > peg$currPos) {
        s1 = input.charAt(peg$currPos);
        peg$currPos++;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e33);
        }
      }
      peg$silentFails--;
      if (s1 === peg$FAILED) {
        s0 = void 0;
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    }
    return s0;
  }
  function peg$parse_() {
    var s0, s1;
    s0 = [];
    s1 = input.charAt(peg$currPos);
    if (peg$r5.test(s1)) {
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e34);
      }
    }
    while (s1 !== peg$FAILED) {
      s0.push(s1);
      s1 = input.charAt(peg$currPos);
      if (peg$r5.test(s1)) {
        peg$currPos++;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e34);
        }
      }
    }
    return s0;
  }
  function peg$parse__() {
    var s0, s1;
    s0 = [];
    s1 = input.charAt(peg$currPos);
    if (peg$r5.test(s1)) {
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e34);
      }
    }
    if (s1 !== peg$FAILED) {
      while (s1 !== peg$FAILED) {
        s0.push(s1);
        s1 = input.charAt(peg$currPos);
        if (peg$r5.test(s1)) {
          peg$currPos++;
        } else {
          s1 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e34);
          }
        }
      }
    } else {
      s0 = peg$FAILED;
    }
    return s0;
  }
  peg$result = peg$startRuleFunction();
  if (options.peg$library) {
    return (
      /** @type {any} */
      {
        peg$result,
        peg$currPos,
        peg$FAILED,
        peg$maxFailExpected,
        peg$maxFailPos
      }
    );
  }
  if (peg$result !== peg$FAILED && peg$currPos === input.length) {
    return peg$result;
  } else {
    if (peg$result !== peg$FAILED && peg$currPos < input.length) {
      peg$fail(peg$endExpectation());
    }
    throw peg$buildStructuredError(
      peg$maxFailExpected,
      peg$maxFailPos < input.length ? input.charAt(peg$maxFailPos) : null,
      peg$maxFailPos < input.length ? peg$computeLocation(peg$maxFailPos, peg$maxFailPos + 1) : peg$computeLocation(peg$maxFailPos, peg$maxFailPos)
    );
  }
}
var block = {
  parseMermaid(input) {
    return peg$parse15(input);
  },
  parseYaml(input) {
    return JSON.parse(input);
  },
  layout(ir, theme) {
    return layoutBlock(ir, theme);
  }
};
var ARROW_ID5 = "req-arrow";
var BOX_W = 230;
function layoutRequirement(ir, theme) {
  const { palette, typography, spacing } = theme;
  const p = pen(theme);
  const margin = spacing.diagramMargin;
  const nameFont = typography.baseFontSize;
  const fieldFont = typography.smallFontSize;
  const lineH = rhuInt(fieldFont * 1.5);
  const prepared = /* @__PURE__ */ new Map();
  const sizeOf = (n) => {
    const lines2 = [];
    for (const f2 of n.fields) {
      const wrapped = wrapText2(`${f2.key}: ${f2.value}`, fieldFont, BOX_W - 20, 3).lines;
      lines2.push(...wrapped);
    }
    const headH = rhuInt(nameFont * 1.5) + fieldFont + 8;
    const h2 = headH + lines2.length * lineH + 8;
    prepared.set(n.id, { lines: lines2, h: h2 });
    return { w: BOX_W, h: h2 };
  };
  const sizes = new Map(ir.nodes.map((n) => [n.id, sizeOf(n)]));
  const nodes = ir.nodes.map((n) => ({
    id: n.id,
    width: BOX_W,
    height: sizes.get(n.id).h
  }));
  const edges2 = ir.relations.map((r) => ({ from: r.from, to: r.to }));
  const laid = layeredLayout(nodes, edges2, { direction: "TB", layerGap: 60, nodeGap: 44, margin });
  const elements = [];
  const allBoxes = [...laid.boxes.values()];
  ir.relations.forEach((r, ri) => {
    const a = laid.boxes.get(r.from), b = laid.boxes.get(r.to);
    if (!a || !b) return;
    let path;
    let labelMidpoint;
    const bends = laid.edgeBends.get(ri);
    if (bends && bends.length > 0) {
      const fromPt = { x: a.x + a.width / 2, y: a.y + a.height };
      const toPt = { x: b.x + b.width / 2, y: b.y };
      const pts = [fromPt];
      for (const pt of bends) {
        pts.push({ x: pt.x, y: pt.y });
      }
      const lastBend = bends[bends.length - 1];
      const boxesAbove = allBoxes.filter((box) => box.y + box.height <= toPt.y);
      const lowestObstacleBottom = boxesAbove.length > 0 ? Math.max(...boxesAbove.map((box) => box.y + box.height)) : fromPt.y;
      const turnY = (lowestObstacleBottom + toPt.y) / 2;
      pts.length = 0;
      pts.push(fromPt);
      pts.push({ x: fromPt.x, y: turnY });
      pts.push({ x: toPt.x, y: turnY });
      pts.push(toPt);
      path = pts.map((p2, idx) => `${idx === 0 ? "M" : "L"} ${rhu(p2.x)} ${rhu(p2.y)}`).join(" ");
      labelMidpoint = { x: (fromPt.x + toPt.x) / 2, y: turnY };
    } else {
      ({ path, labelMidpoint } = routeEdge(a, b, allBoxes));
    }
    elements.push(p.path(path, palette.textMuted, 1.3, { dash: "6 4", markerEnd: ARROW_ID5 }));
    const mx = labelMidpoint.x, my = labelMidpoint.y;
    const w = measureText(`\xAB${r.type}\xBB`, fieldFont).width + 8;
    elements.push(
      p.rect(
        { x: rhu(mx - w / 2), y: rhu(my - fieldFont), width: rhu(w), height: fieldFont + 4 },
        palette.background,
        palette.background,
        0
      )
    );
    elements.push(
      p.text(`\xAB${r.type}\xBB`, rhuInt(mx), rhu(my - 1), fieldFont, palette.secondary, {
        anchor: "middle"
      })
    );
  });
  for (const n of ir.nodes) {
    const box = laid.boxes.get(n.id);
    const prep = prepared.get(n.id);
    const { x, y } = box;
    const headH = rhuInt(nameFont * 1.5) + fieldFont + 8;
    elements.push(
      p.rect(
        { x: rhu(x), y: rhu(y), width: BOX_W, height: rhu(box.height) },
        palette.surface,
        palette.primary,
        1.4,
        { rx: 6 }
      )
    );
    elements.push(
      p.text(
        `\xAB${n.kind}\xBB`,
        rhuInt(x + BOX_W / 2),
        rhu(y + fieldFont + 4),
        fieldFont,
        palette.textMuted,
        { anchor: "middle" }
      )
    );
    elements.push(
      p.text(
        n.id,
        rhuInt(x + BOX_W / 2),
        rhu(y + fieldFont + 6 + nameFont),
        nameFont,
        palette.text,
        { weight: "bold", anchor: "middle" }
      )
    );
    elements.push(
      p.path(
        `M ${rhu(x)} ${rhu(y + headH)} L ${rhu(x + BOX_W)} ${rhu(y + headH)}`,
        palette.border,
        1
      )
    );
    let fy = y + headH + lineH - 3;
    for (const ln of prep.lines) {
      elements.push(p.text(ln, rhu(x + 10), rhu(fy), fieldFont, palette.text));
      fy += lineH;
    }
  }
  const s = theme.edges?.arrowSize ?? 8;
  const openW = s + 1;
  const openH = rhu(s * 0.8);
  const openMidY = rhu(openH / 2);
  const defs = [
    `<marker id="${ARROW_ID5}" markerWidth="${openW}" markerHeight="${openH}" refX="${s}" refY="${openMidY}" orient="auto"><polyline points="0 0, ${s} ${openMidY}, 0 ${openH}" fill="none" stroke="${palette.textMuted}" stroke-width="1.3" /></marker>`
  ];
  const scene = applyOverlays(
    {
      viewBox: {
        x: 0,
        y: 0,
        width: rhuInt(laid.width + margin),
        height: rhuInt(laid.height + margin)
      },
      background: palette.background,
      elements,
      defs
    },
    ir.overlays,
    theme
  );
  return { scene, anchors: {} };
}
function peg$subclass16(child, parent) {
  function C() {
    this.constructor = child;
  }
  C.prototype = parent.prototype;
  child.prototype = new C();
}
function peg$SyntaxError16(message, expected, found, location) {
  var self = Error.call(this, message);
  if (Object.setPrototypeOf) {
    Object.setPrototypeOf(self, peg$SyntaxError16.prototype);
  }
  self.expected = expected;
  self.found = found;
  self.location = location;
  self.name = "SyntaxError";
  return self;
}
peg$subclass16(peg$SyntaxError16, Error);
function peg$padEnd16(str, targetLength, padString) {
  padString = padString || " ";
  if (str.length > targetLength) {
    return str;
  }
  targetLength -= str.length;
  padString += padString.repeat(targetLength);
  return str + padString.slice(0, targetLength);
}
peg$SyntaxError16.prototype.format = function(sources) {
  var str = "Error: " + this.message;
  if (this.location) {
    var src = null;
    var k;
    for (k = 0; k < sources.length; k++) {
      if (sources[k].source === this.location.source) {
        src = sources[k].text.split(/\r\n|\n|\r/g);
        break;
      }
    }
    var s = this.location.start;
    var offset_s = this.location.source && typeof this.location.source.offset === "function" ? this.location.source.offset(s) : s;
    var loc = this.location.source + ":" + offset_s.line + ":" + offset_s.column;
    if (src) {
      var e = this.location.end;
      var filler = peg$padEnd16("", offset_s.line.toString().length, " ");
      var line = src[s.line - 1];
      var last = s.line === e.line ? e.column : line.length + 1;
      var hatLen = last - s.column || 1;
      str += "\n --> " + loc + "\n" + filler + " |\n" + offset_s.line + " | " + line + "\n" + filler + " | " + peg$padEnd16("", s.column - 1, " ") + peg$padEnd16("", hatLen, "^");
    } else {
      str += "\n at " + loc;
    }
  }
  return str;
};
peg$SyntaxError16.buildMessage = function(expected, found) {
  var DESCRIBE_EXPECTATION_FNS = {
    literal: function(expectation) {
      return '"' + literalEscape(expectation.text) + '"';
    },
    class: function(expectation) {
      var escapedParts = expectation.parts.map(function(part) {
        return Array.isArray(part) ? classEscape(part[0]) + "-" + classEscape(part[1]) : classEscape(part);
      });
      return "[" + (expectation.inverted ? "^" : "") + escapedParts.join("") + "]";
    },
    any: function() {
      return "any character";
    },
    end: function() {
      return "end of input";
    },
    other: function(expectation) {
      return expectation.description;
    }
  };
  function hex(ch) {
    return ch.charCodeAt(0).toString(16).toUpperCase();
  }
  function literalEscape(s) {
    return s.replace(/\\/g, "\\\\").replace(/"/g, '\\"').replace(/\0/g, "\\0").replace(/\t/g, "\\t").replace(/\n/g, "\\n").replace(/\r/g, "\\r").replace(/[\x00-\x0F]/g, function(ch) {
      return "\\x0" + hex(ch);
    }).replace(/[\x10-\x1F\x7F-\x9F]/g, function(ch) {
      return "\\x" + hex(ch);
    });
  }
  function classEscape(s) {
    return s.replace(/\\/g, "\\\\").replace(/\]/g, "\\]").replace(/\^/g, "\\^").replace(/-/g, "\\-").replace(/\0/g, "\\0").replace(/\t/g, "\\t").replace(/\n/g, "\\n").replace(/\r/g, "\\r").replace(/[\x00-\x0F]/g, function(ch) {
      return "\\x0" + hex(ch);
    }).replace(/[\x10-\x1F\x7F-\x9F]/g, function(ch) {
      return "\\x" + hex(ch);
    });
  }
  function describeExpectation(expectation) {
    return DESCRIBE_EXPECTATION_FNS[expectation.type](expectation);
  }
  function describeExpected(expected2) {
    var descriptions = expected2.map(describeExpectation);
    var i, j;
    descriptions.sort();
    if (descriptions.length > 0) {
      for (i = 1, j = 1; i < descriptions.length; i++) {
        if (descriptions[i - 1] !== descriptions[i]) {
          descriptions[j] = descriptions[i];
          j++;
        }
      }
      descriptions.length = j;
    }
    switch (descriptions.length) {
      case 1:
        return descriptions[0];
      case 2:
        return descriptions[0] + " or " + descriptions[1];
      default:
        return descriptions.slice(0, -1).join(", ") + ", or " + descriptions[descriptions.length - 1];
    }
  }
  function describeFound(found2) {
    return found2 ? '"' + literalEscape(found2) + '"' : "end of input";
  }
  return "Expected " + describeExpected(expected) + " but " + describeFound(found) + " found.";
};
function peg$parse16(input, options) {
  options = options !== void 0 ? options : {};
  var peg$FAILED = {};
  var peg$source = options.grammarSource;
  var peg$startRuleFunctions = { Document: peg$parseDocument };
  var peg$startRuleFunction = peg$parseDocument;
  var peg$c0 = "requirementdiagram";
  var peg$c1 = "requirement";
  var peg$c2 = "---";
  var peg$c3 = "{";
  var peg$c4 = "\r";
  var peg$c5 = "\n";
  var peg$c6 = "}";
  var peg$c7 = ":";
  var peg$c8 = "-";
  var peg$c9 = "->";
  var peg$c10 = "functionalrequirement";
  var peg$c11 = "interfacerequirement";
  var peg$c12 = "performancerequirement";
  var peg$c13 = "physicalrequirement";
  var peg$c14 = "designconstraint";
  var peg$c15 = "element";
  var peg$r0 = /^[^\n]/;
  var peg$r1 = /^[^:\n]/;
  var peg$r2 = /^[A-Za-z0-9_]/;
  var peg$r3 = /^[ \t]/;
  var peg$e0 = peg$literalExpectation("requirementDiagram", true);
  var peg$e1 = peg$literalExpectation("requirement", true);
  var peg$e2 = peg$literalExpectation("---", false);
  var peg$e3 = peg$classExpectation(["\n"], true, false);
  var peg$e4 = peg$literalExpectation("{", false);
  var peg$e5 = peg$literalExpectation("\r", false);
  var peg$e6 = peg$literalExpectation("\n", false);
  var peg$e7 = peg$literalExpectation("}", false);
  var peg$e8 = peg$classExpectation([":", "\n"], true, false);
  var peg$e9 = peg$literalExpectation(":", false);
  var peg$e10 = peg$literalExpectation("-", false);
  var peg$e11 = peg$literalExpectation("->", false);
  var peg$e12 = peg$literalExpectation("functionalRequirement", true);
  var peg$e13 = peg$literalExpectation("interfaceRequirement", true);
  var peg$e14 = peg$literalExpectation("performanceRequirement", true);
  var peg$e15 = peg$literalExpectation("physicalRequirement", true);
  var peg$e16 = peg$literalExpectation("designConstraint", true);
  var peg$e17 = peg$literalExpectation("element", true);
  var peg$e18 = peg$classExpectation([["A", "Z"], ["a", "z"], ["0", "9"], "_"], false, false);
  var peg$e19 = peg$anyExpectation();
  var peg$e20 = peg$classExpectation([" ", "	"], false, false);
  var peg$f0 = function(fm, stmts) {
    const nodes = [];
    const relations = [];
    for (const s of stmts.filter(Boolean)) {
      if (s.t === "node") nodes.push({ id: s.name, kind: s.kind, fields: s.fields });
      else if (s.t === "rel") relations.push({ from: s.from, to: s.to, type: s.type });
    }
    return { version: "1.0", metadata: { ...fm || {} }, nodes, relations };
  };
  var peg$f1 = function(line) {
    return line;
  };
  var peg$f2 = function(lines2) {
    const meta = {};
    for (const line of lines2) {
      const m = line.match(/^\s*(\w+)\s*:\s*(.+?)\s*$/);
      if (m) meta[m[1]] = m[2];
    }
    return meta;
  };
  var peg$f3 = function(kind, name, rows) {
    return { t: "node", kind, name, fields: rows.filter(Boolean) };
  };
  var peg$f4 = function(key, val) {
    return { key: key.trim(), value: val.trim() };
  };
  var peg$f5 = function() {
    return null;
  };
  var peg$f6 = function(from, type, to) {
    return { t: "rel", from, to, type };
  };
  var peg$f7 = function() {
    return null;
  };
  var peg$currPos = options.peg$currPos | 0;
  var peg$savedPos = peg$currPos;
  var peg$posDetailsCache = [{ line: 1, column: 1 }];
  var peg$maxFailPos = peg$currPos;
  var peg$maxFailExpected = options.peg$maxFailExpected || [];
  var peg$silentFails = options.peg$silentFails | 0;
  var peg$result;
  if (options.startRule) {
    if (!(options.startRule in peg$startRuleFunctions)) {
      throw new Error(`Can't start parsing from rule "` + options.startRule + '".');
    }
    peg$startRuleFunction = peg$startRuleFunctions[options.startRule];
  }
  function text() {
    return input.substring(peg$savedPos, peg$currPos);
  }
  function offset() {
    return peg$savedPos;
  }
  function range() {
    return {
      source: peg$source,
      start: peg$savedPos,
      end: peg$currPos
    };
  }
  function location() {
    return peg$computeLocation(peg$savedPos, peg$currPos);
  }
  function expected(description, location2) {
    location2 = location2 !== void 0 ? location2 : peg$computeLocation(peg$savedPos, peg$currPos);
    throw peg$buildStructuredError(
      [peg$otherExpectation(description)],
      input.substring(peg$savedPos, peg$currPos),
      location2
    );
  }
  function error(message, location2) {
    location2 = location2 !== void 0 ? location2 : peg$computeLocation(peg$savedPos, peg$currPos);
    throw peg$buildSimpleError(message, location2);
  }
  function peg$literalExpectation(text2, ignoreCase) {
    return { type: "literal", text: text2, ignoreCase };
  }
  function peg$classExpectation(parts, inverted, ignoreCase) {
    return { type: "class", parts, inverted, ignoreCase };
  }
  function peg$anyExpectation() {
    return { type: "any" };
  }
  function peg$endExpectation() {
    return { type: "end" };
  }
  function peg$otherExpectation(description) {
    return { type: "other", description };
  }
  function peg$computePosDetails(pos) {
    var details = peg$posDetailsCache[pos];
    var p;
    if (details) {
      return details;
    } else {
      if (pos >= peg$posDetailsCache.length) {
        p = peg$posDetailsCache.length - 1;
      } else {
        p = pos;
        while (!peg$posDetailsCache[--p]) {
        }
      }
      details = peg$posDetailsCache[p];
      details = {
        line: details.line,
        column: details.column
      };
      while (p < pos) {
        if (input.charCodeAt(p) === 10) {
          details.line++;
          details.column = 1;
        } else {
          details.column++;
        }
        p++;
      }
      peg$posDetailsCache[pos] = details;
      return details;
    }
  }
  function peg$computeLocation(startPos, endPos, offset2) {
    var startPosDetails = peg$computePosDetails(startPos);
    var endPosDetails = peg$computePosDetails(endPos);
    var res = {
      source: peg$source,
      start: {
        offset: startPos,
        line: startPosDetails.line,
        column: startPosDetails.column
      },
      end: {
        offset: endPos,
        line: endPosDetails.line,
        column: endPosDetails.column
      }
    };
    if (offset2 && peg$source && typeof peg$source.offset === "function") {
      res.start = peg$source.offset(res.start);
      res.end = peg$source.offset(res.end);
    }
    return res;
  }
  function peg$fail(expected2) {
    if (peg$currPos < peg$maxFailPos) {
      return;
    }
    if (peg$currPos > peg$maxFailPos) {
      peg$maxFailPos = peg$currPos;
      peg$maxFailExpected = [];
    }
    peg$maxFailExpected.push(expected2);
  }
  function peg$buildSimpleError(message, location2) {
    return new peg$SyntaxError16(message, null, null, location2);
  }
  function peg$buildStructuredError(expected2, found, location2) {
    return new peg$SyntaxError16(
      peg$SyntaxError16.buildMessage(expected2, found),
      expected2,
      found,
      location2
    );
  }
  function peg$parseDocument() {
    var s0, s1, s2, s3, s4, s5, s6;
    s0 = peg$currPos;
    s1 = peg$parseFrontmatter();
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    s2 = peg$parse_();
    s3 = input.substr(peg$currPos, 18);
    if (s3.toLowerCase() === peg$c0) {
      peg$currPos += 18;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e0);
      }
    }
    if (s3 === peg$FAILED) {
      s3 = input.substr(peg$currPos, 11);
      if (s3.toLowerCase() === peg$c1) {
        peg$currPos += 11;
      } else {
        s3 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e1);
        }
      }
    }
    if (s3 !== peg$FAILED) {
      s4 = peg$parseLineEnd();
      if (s4 !== peg$FAILED) {
        s5 = [];
        s6 = peg$parseStatement();
        while (s6 !== peg$FAILED) {
          s5.push(s6);
          s6 = peg$parseStatement();
        }
        s6 = peg$parse_();
        peg$savedPos = s0;
        s0 = peg$f0(s1, s5);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseFrontmatter() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 3) === peg$c2) {
      s1 = peg$c2;
      peg$currPos += 3;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e2);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$parseNL();
      if (s2 !== peg$FAILED) {
        s3 = [];
        s4 = peg$currPos;
        s5 = peg$currPos;
        peg$silentFails++;
        s6 = peg$currPos;
        if (input.substr(peg$currPos, 3) === peg$c2) {
          s7 = peg$c2;
          peg$currPos += 3;
        } else {
          s7 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e2);
          }
        }
        if (s7 !== peg$FAILED) {
          s8 = peg$parseNL();
          if (s8 !== peg$FAILED) {
            s7 = [s7, s8];
            s6 = s7;
          } else {
            peg$currPos = s6;
            s6 = peg$FAILED;
          }
        } else {
          peg$currPos = s6;
          s6 = peg$FAILED;
        }
        peg$silentFails--;
        if (s6 === peg$FAILED) {
          s5 = void 0;
        } else {
          peg$currPos = s5;
          s5 = peg$FAILED;
        }
        if (s5 !== peg$FAILED) {
          s6 = peg$currPos;
          s7 = [];
          s8 = input.charAt(peg$currPos);
          if (peg$r0.test(s8)) {
            peg$currPos++;
          } else {
            s8 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e3);
            }
          }
          while (s8 !== peg$FAILED) {
            s7.push(s8);
            s8 = input.charAt(peg$currPos);
            if (peg$r0.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e3);
              }
            }
          }
          s6 = input.substring(s6, peg$currPos);
          s7 = peg$parseNL();
          if (s7 !== peg$FAILED) {
            peg$savedPos = s4;
            s4 = peg$f1(s6);
          } else {
            peg$currPos = s4;
            s4 = peg$FAILED;
          }
        } else {
          peg$currPos = s4;
          s4 = peg$FAILED;
        }
        while (s4 !== peg$FAILED) {
          s3.push(s4);
          s4 = peg$currPos;
          s5 = peg$currPos;
          peg$silentFails++;
          s6 = peg$currPos;
          if (input.substr(peg$currPos, 3) === peg$c2) {
            s7 = peg$c2;
            peg$currPos += 3;
          } else {
            s7 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e2);
            }
          }
          if (s7 !== peg$FAILED) {
            s8 = peg$parseNL();
            if (s8 !== peg$FAILED) {
              s7 = [s7, s8];
              s6 = s7;
            } else {
              peg$currPos = s6;
              s6 = peg$FAILED;
            }
          } else {
            peg$currPos = s6;
            s6 = peg$FAILED;
          }
          peg$silentFails--;
          if (s6 === peg$FAILED) {
            s5 = void 0;
          } else {
            peg$currPos = s5;
            s5 = peg$FAILED;
          }
          if (s5 !== peg$FAILED) {
            s6 = peg$currPos;
            s7 = [];
            s8 = input.charAt(peg$currPos);
            if (peg$r0.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e3);
              }
            }
            while (s8 !== peg$FAILED) {
              s7.push(s8);
              s8 = input.charAt(peg$currPos);
              if (peg$r0.test(s8)) {
                peg$currPos++;
              } else {
                s8 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e3);
                }
              }
            }
            s6 = input.substring(s6, peg$currPos);
            s7 = peg$parseNL();
            if (s7 !== peg$FAILED) {
              peg$savedPos = s4;
              s4 = peg$f1(s6);
            } else {
              peg$currPos = s4;
              s4 = peg$FAILED;
            }
          } else {
            peg$currPos = s4;
            s4 = peg$FAILED;
          }
        }
        if (input.substr(peg$currPos, 3) === peg$c2) {
          s4 = peg$c2;
          peg$currPos += 3;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e2);
          }
        }
        if (s4 !== peg$FAILED) {
          s5 = peg$parseNL();
          if (s5 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f2(s3);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseStatement() {
    var s0;
    s0 = peg$parseBlock();
    if (s0 === peg$FAILED) {
      s0 = peg$parseRelation();
      if (s0 === peg$FAILED) {
        s0 = peg$parseBlankLine();
      }
    }
    return s0;
  }
  function peg$parseBlock() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9, s10, s11, s12, s13;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = peg$parseReqKind();
    if (s2 !== peg$FAILED) {
      s3 = peg$parse__();
      if (s3 !== peg$FAILED) {
        s4 = peg$parseIdent();
        if (s4 !== peg$FAILED) {
          s5 = peg$parse_();
          if (input.charCodeAt(peg$currPos) === 123) {
            s6 = peg$c3;
            peg$currPos++;
          } else {
            s6 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e4);
            }
          }
          if (s6 !== peg$FAILED) {
            s7 = peg$parse_();
            if (input.charCodeAt(peg$currPos) === 13) {
              s8 = peg$c4;
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e5);
              }
            }
            if (s8 === peg$FAILED) {
              s8 = null;
            }
            if (input.charCodeAt(peg$currPos) === 10) {
              s9 = peg$c5;
              peg$currPos++;
            } else {
              s9 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e6);
              }
            }
            if (s9 !== peg$FAILED) {
              s10 = [];
              s11 = peg$parseFieldRow();
              while (s11 !== peg$FAILED) {
                s10.push(s11);
                s11 = peg$parseFieldRow();
              }
              s11 = peg$parse_();
              if (input.charCodeAt(peg$currPos) === 125) {
                s12 = peg$c6;
                peg$currPos++;
              } else {
                s12 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e7);
                }
              }
              if (s12 !== peg$FAILED) {
                s13 = peg$parseLineEnd();
                if (s13 !== peg$FAILED) {
                  peg$savedPos = s0;
                  s0 = peg$f3(s2, s4, s10);
                } else {
                  peg$currPos = s0;
                  s0 = peg$FAILED;
                }
              } else {
                peg$currPos = s0;
                s0 = peg$FAILED;
              }
            } else {
              peg$currPos = s0;
              s0 = peg$FAILED;
            }
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseFieldRow() {
    var s0, s1, s2, s3, s4, s5, s6, s7;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = peg$currPos;
    peg$silentFails++;
    if (input.charCodeAt(peg$currPos) === 125) {
      s3 = peg$c6;
      peg$currPos++;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e7);
      }
    }
    peg$silentFails--;
    if (s3 === peg$FAILED) {
      s2 = void 0;
    } else {
      peg$currPos = s2;
      s2 = peg$FAILED;
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$currPos;
      s4 = [];
      s5 = input.charAt(peg$currPos);
      if (peg$r1.test(s5)) {
        peg$currPos++;
      } else {
        s5 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e8);
        }
      }
      if (s5 !== peg$FAILED) {
        while (s5 !== peg$FAILED) {
          s4.push(s5);
          s5 = input.charAt(peg$currPos);
          if (peg$r1.test(s5)) {
            peg$currPos++;
          } else {
            s5 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e8);
            }
          }
        }
      } else {
        s4 = peg$FAILED;
      }
      if (s4 !== peg$FAILED) {
        s3 = input.substring(s3, peg$currPos);
      } else {
        s3 = s4;
      }
      if (s3 !== peg$FAILED) {
        if (input.charCodeAt(peg$currPos) === 58) {
          s4 = peg$c7;
          peg$currPos++;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e9);
          }
        }
        if (s4 !== peg$FAILED) {
          s5 = peg$currPos;
          s6 = [];
          s7 = input.charAt(peg$currPos);
          if (peg$r0.test(s7)) {
            peg$currPos++;
          } else {
            s7 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e3);
            }
          }
          if (s7 !== peg$FAILED) {
            while (s7 !== peg$FAILED) {
              s6.push(s7);
              s7 = input.charAt(peg$currPos);
              if (peg$r0.test(s7)) {
                peg$currPos++;
              } else {
                s7 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e3);
                }
              }
            }
          } else {
            s6 = peg$FAILED;
          }
          if (s6 !== peg$FAILED) {
            s5 = input.substring(s5, peg$currPos);
          } else {
            s5 = s6;
          }
          if (s5 !== peg$FAILED) {
            if (input.charCodeAt(peg$currPos) === 13) {
              s6 = peg$c4;
              peg$currPos++;
            } else {
              s6 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e5);
              }
            }
            if (s6 === peg$FAILED) {
              s6 = null;
            }
            if (input.charCodeAt(peg$currPos) === 10) {
              s7 = peg$c5;
              peg$currPos++;
            } else {
              s7 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e6);
              }
            }
            if (s7 !== peg$FAILED) {
              peg$savedPos = s0;
              s0 = peg$f4(s3, s5);
            } else {
              peg$currPos = s0;
              s0 = peg$FAILED;
            }
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      s1 = peg$parse_();
      if (input.charCodeAt(peg$currPos) === 13) {
        s2 = peg$c4;
        peg$currPos++;
      } else {
        s2 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e5);
        }
      }
      if (s2 === peg$FAILED) {
        s2 = null;
      }
      if (input.charCodeAt(peg$currPos) === 10) {
        s3 = peg$c5;
        peg$currPos++;
      } else {
        s3 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e6);
        }
      }
      if (s3 !== peg$FAILED) {
        peg$savedPos = s0;
        s0 = peg$f5();
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    }
    return s0;
  }
  function peg$parseRelation() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9, s10, s11, s12;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = peg$parseIdent();
    if (s2 !== peg$FAILED) {
      s3 = peg$parse_();
      if (input.charCodeAt(peg$currPos) === 45) {
        s4 = peg$c8;
        peg$currPos++;
      } else {
        s4 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e10);
        }
      }
      if (s4 !== peg$FAILED) {
        s5 = peg$parse_();
        s6 = peg$parseIdent();
        if (s6 !== peg$FAILED) {
          s7 = peg$parse_();
          if (input.substr(peg$currPos, 2) === peg$c9) {
            s8 = peg$c9;
            peg$currPos += 2;
          } else {
            s8 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e11);
            }
          }
          if (s8 !== peg$FAILED) {
            s9 = peg$parse_();
            s10 = peg$parseIdent();
            if (s10 !== peg$FAILED) {
              s11 = peg$parse_();
              s12 = peg$parseLineEnd();
              if (s12 !== peg$FAILED) {
                peg$savedPos = s0;
                s0 = peg$f6(s2, s6, s10);
              } else {
                peg$currPos = s0;
                s0 = peg$FAILED;
              }
            } else {
              peg$currPos = s0;
              s0 = peg$FAILED;
            }
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseReqKind() {
    var s0;
    s0 = input.substr(peg$currPos, 21);
    if (s0.toLowerCase() === peg$c10) {
      peg$currPos += 21;
    } else {
      s0 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e12);
      }
    }
    if (s0 === peg$FAILED) {
      s0 = input.substr(peg$currPos, 20);
      if (s0.toLowerCase() === peg$c11) {
        peg$currPos += 20;
      } else {
        s0 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e13);
        }
      }
      if (s0 === peg$FAILED) {
        s0 = input.substr(peg$currPos, 22);
        if (s0.toLowerCase() === peg$c12) {
          peg$currPos += 22;
        } else {
          s0 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e14);
          }
        }
        if (s0 === peg$FAILED) {
          s0 = input.substr(peg$currPos, 19);
          if (s0.toLowerCase() === peg$c13) {
            peg$currPos += 19;
          } else {
            s0 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e15);
            }
          }
          if (s0 === peg$FAILED) {
            s0 = input.substr(peg$currPos, 16);
            if (s0.toLowerCase() === peg$c14) {
              peg$currPos += 16;
            } else {
              s0 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e16);
              }
            }
            if (s0 === peg$FAILED) {
              s0 = input.substr(peg$currPos, 11);
              if (s0.toLowerCase() === peg$c1) {
                peg$currPos += 11;
              } else {
                s0 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e1);
                }
              }
              if (s0 === peg$FAILED) {
                s0 = input.substr(peg$currPos, 7);
                if (s0.toLowerCase() === peg$c15) {
                  peg$currPos += 7;
                } else {
                  s0 = peg$FAILED;
                  if (peg$silentFails === 0) {
                    peg$fail(peg$e17);
                  }
                }
              }
            }
          }
        }
      }
    }
    return s0;
  }
  function peg$parseIdent() {
    var s0, s1, s2;
    s0 = peg$currPos;
    s1 = [];
    s2 = input.charAt(peg$currPos);
    if (peg$r2.test(s2)) {
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e18);
      }
    }
    if (s2 !== peg$FAILED) {
      while (s2 !== peg$FAILED) {
        s1.push(s2);
        s2 = input.charAt(peg$currPos);
        if (peg$r2.test(s2)) {
          peg$currPos++;
        } else {
          s2 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e18);
          }
        }
      }
    } else {
      s1 = peg$FAILED;
    }
    if (s1 !== peg$FAILED) {
      s0 = input.substring(s0, peg$currPos);
    } else {
      s0 = s1;
    }
    return s0;
  }
  function peg$parseNL() {
    var s0, s1, s2;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 13) {
      s1 = peg$c4;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e5);
      }
    }
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s2 = peg$c5;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e6);
      }
    }
    if (s2 !== peg$FAILED) {
      s1 = [s1, s2];
      s0 = s1;
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseBlankLine() {
    var s0, s1, s2, s3;
    s0 = peg$currPos;
    s1 = peg$parse_();
    if (input.charCodeAt(peg$currPos) === 13) {
      s2 = peg$c4;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e5);
      }
    }
    if (s2 === peg$FAILED) {
      s2 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s3 = peg$c5;
      peg$currPos++;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e6);
      }
    }
    if (s3 !== peg$FAILED) {
      peg$savedPos = s0;
      s0 = peg$f7();
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseLineEnd() {
    var s0, s1, s2;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 13) {
      s1 = peg$c4;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e5);
      }
    }
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s2 = peg$c5;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e6);
      }
    }
    if (s2 !== peg$FAILED) {
      s1 = [s1, s2];
      s0 = s1;
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      peg$silentFails++;
      if (input.length > peg$currPos) {
        s1 = input.charAt(peg$currPos);
        peg$currPos++;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e19);
        }
      }
      peg$silentFails--;
      if (s1 === peg$FAILED) {
        s0 = void 0;
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    }
    return s0;
  }
  function peg$parse_() {
    var s0, s1;
    s0 = [];
    s1 = input.charAt(peg$currPos);
    if (peg$r3.test(s1)) {
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e20);
      }
    }
    while (s1 !== peg$FAILED) {
      s0.push(s1);
      s1 = input.charAt(peg$currPos);
      if (peg$r3.test(s1)) {
        peg$currPos++;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e20);
        }
      }
    }
    return s0;
  }
  function peg$parse__() {
    var s0, s1;
    s0 = [];
    s1 = input.charAt(peg$currPos);
    if (peg$r3.test(s1)) {
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e20);
      }
    }
    if (s1 !== peg$FAILED) {
      while (s1 !== peg$FAILED) {
        s0.push(s1);
        s1 = input.charAt(peg$currPos);
        if (peg$r3.test(s1)) {
          peg$currPos++;
        } else {
          s1 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e20);
          }
        }
      }
    } else {
      s0 = peg$FAILED;
    }
    return s0;
  }
  peg$result = peg$startRuleFunction();
  if (options.peg$library) {
    return (
      /** @type {any} */
      {
        peg$result,
        peg$currPos,
        peg$FAILED,
        peg$maxFailExpected,
        peg$maxFailPos
      }
    );
  }
  if (peg$result !== peg$FAILED && peg$currPos === input.length) {
    return peg$result;
  } else {
    if (peg$result !== peg$FAILED && peg$currPos < input.length) {
      peg$fail(peg$endExpectation());
    }
    throw peg$buildStructuredError(
      peg$maxFailExpected,
      peg$maxFailPos < input.length ? input.charAt(peg$maxFailPos) : null,
      peg$maxFailPos < input.length ? peg$computeLocation(peg$maxFailPos, peg$maxFailPos + 1) : peg$computeLocation(peg$maxFailPos, peg$maxFailPos)
    );
  }
}
var requirement = {
  parseMermaid(input) {
    return peg$parse16(input);
  },
  parseYaml(input) {
    return JSON.parse(input);
  },
  layout(ir, theme) {
    return layoutRequirement(ir, theme);
  }
};
function layoutSankey(ir, theme) {
  const { palette, typography, spacing } = theme;
  const p = pen(theme);
  const margin = spacing.diagramMargin;
  const order = [];
  const seen = /* @__PURE__ */ new Set();
  const add = (n) => {
    if (!seen.has(n)) {
      seen.add(n);
      order.push(n);
    }
  };
  for (const l of ir.links) {
    add(l.source);
    add(l.target);
  }
  const layer = new Map(order.map((n) => [n, 0]));
  for (let pass = 0; pass < order.length; pass++) {
    let changed = false;
    for (const l of ir.links) {
      const want = layer.get(l.source) + 1;
      if (layer.get(l.target) < want) {
        layer.set(l.target, want);
        changed = true;
      }
    }
    if (!changed) break;
  }
  const maxLayer = Math.max(0, ...order.map((n) => layer.get(n)));
  const sumOut = /* @__PURE__ */ new Map();
  const sumIn = /* @__PURE__ */ new Map();
  for (const l of ir.links) {
    sumOut.set(l.source, (sumOut.get(l.source) ?? 0) + l.value);
    sumIn.set(l.target, (sumIn.get(l.target) ?? 0) + l.value);
  }
  const nodeValue = (n) => Math.max(sumOut.get(n) ?? 0, sumIn.get(n) ?? 0);
  const byLayer = Array.from({ length: maxLayer + 1 }, () => []);
  for (const n of order) byLayer[layer.get(n)].push(n);
  const colSum = byLayer.map((col) => col.reduce((s, n) => s + nodeValue(n), 0));
  const maxColSum = Math.max(1, ...colSum);
  const title = ir.metadata.title;
  const titleH = title ? typography.titleFontSize + 16 : 0;
  const plotH = 480;
  const nodeW = 18;
  const colGap = 220;
  const nodeGap = 14;
  const unit = (plotH - 0) / maxColSum;
  const plotLeft = margin + 4;
  const plotTop = margin + titleH + 6;
  const colX = (li) => plotLeft + li * (nodeW + colGap);
  const boxes = /* @__PURE__ */ new Map();
  byLayer.forEach((col, li) => {
    const totalH2 = col.reduce((s, n) => s + nodeValue(n) * unit, 0) + (col.length - 1) * nodeGap;
    let y = plotTop + (plotH - totalH2) / 2;
    for (const n of col) {
      const h2 = Math.max(2, nodeValue(n) * unit);
      boxes.set(n, { x: colX(li), y, h: h2 });
      y += h2 + nodeGap;
    }
  });
  const elements = [];
  if (title)
    elements.push(
      p.text(
        title,
        margin,
        margin + typography.titleFontSize,
        typography.titleFontSize,
        palette.text,
        { weight: "bold" }
      )
    );
  const outOff = /* @__PURE__ */ new Map();
  const inOff = /* @__PURE__ */ new Map();
  const hueOf = new Map(order.map((n, i) => [n, categoricalHue(i, theme)]));
  for (const l of ir.links) {
    const sb = boxes.get(l.source), tb = boxes.get(l.target);
    const lh = Math.max(1, l.value * unit);
    const so = outOff.get(l.source) ?? 0;
    outOff.set(l.source, so + lh);
    const io = inOff.get(l.target) ?? 0;
    inOff.set(l.target, io + lh);
    const sx = sb.x + nodeW, tx = tb.x;
    const sy0 = sb.y + so, sy1 = sy0 + lh;
    const ty0 = tb.y + io, ty1 = ty0 + lh;
    const mx = (sx + tx) / 2;
    const d = `M ${rhu(sx)} ${rhu(sy0)} C ${rhu(mx)} ${rhu(sy0)}, ${rhu(mx)} ${rhu(ty0)}, ${rhu(tx)} ${rhu(ty0)} L ${rhu(tx)} ${rhu(ty1)} C ${rhu(mx)} ${rhu(ty1)}, ${rhu(mx)} ${rhu(sy1)}, ${rhu(sx)} ${rhu(sy1)} Z`;
    elements.push(p.path(d, "none", 0, { fill: hueOf.get(l.source), opacity: 0.4 }));
  }
  byLayer.forEach((col, li) => {
    const lastCol = li === maxLayer;
    for (const n of col) {
      const b = boxes.get(n);
      elements.push(
        p.rect(
          { x: rhu(b.x), y: rhu(b.y), width: nodeW, height: rhu(b.h) },
          hueOf.get(n),
          hueOf.get(n),
          0,
          { rx: 2 }
        )
      );
      const ly = rhu(b.y + b.h / 2 + typography.smallFontSize * 0.35);
      if (lastCol)
        elements.push(
          p.text(n, rhu(b.x - 6), ly, typography.smallFontSize, palette.text, { anchor: "end" })
        );
      else
        elements.push(p.text(n, rhu(b.x + nodeW + 6), ly, typography.smallFontSize, palette.text));
    }
  });
  const lastLabelW = Math.max(
    0,
    ...byLayer[maxLayer].map((n) => measureText(n, typography.smallFontSize).width)
  );
  const otherLabelW = 140;
  const totalW = rhuInt(
    colX(maxLayer) + nodeW + (maxLayer === 0 ? otherLabelW : Math.max(otherLabelW, 0)) + margin
  );
  const totalH = rhuInt(plotTop + plotH + margin);
  void lastLabelW;
  const scene = applyOverlays(
    {
      viewBox: { x: 0, y: 0, width: totalW, height: totalH },
      background: palette.background,
      elements
    },
    ir.overlays,
    theme
  );
  return { scene, anchors: {} };
}
function peg$subclass17(child, parent) {
  function C() {
    this.constructor = child;
  }
  C.prototype = parent.prototype;
  child.prototype = new C();
}
function peg$SyntaxError17(message, expected, found, location) {
  var self = Error.call(this, message);
  if (Object.setPrototypeOf) {
    Object.setPrototypeOf(self, peg$SyntaxError17.prototype);
  }
  self.expected = expected;
  self.found = found;
  self.location = location;
  self.name = "SyntaxError";
  return self;
}
peg$subclass17(peg$SyntaxError17, Error);
function peg$padEnd17(str, targetLength, padString) {
  padString = padString || " ";
  if (str.length > targetLength) {
    return str;
  }
  targetLength -= str.length;
  padString += padString.repeat(targetLength);
  return str + padString.slice(0, targetLength);
}
peg$SyntaxError17.prototype.format = function(sources) {
  var str = "Error: " + this.message;
  if (this.location) {
    var src = null;
    var k;
    for (k = 0; k < sources.length; k++) {
      if (sources[k].source === this.location.source) {
        src = sources[k].text.split(/\r\n|\n|\r/g);
        break;
      }
    }
    var s = this.location.start;
    var offset_s = this.location.source && typeof this.location.source.offset === "function" ? this.location.source.offset(s) : s;
    var loc = this.location.source + ":" + offset_s.line + ":" + offset_s.column;
    if (src) {
      var e = this.location.end;
      var filler = peg$padEnd17("", offset_s.line.toString().length, " ");
      var line = src[s.line - 1];
      var last = s.line === e.line ? e.column : line.length + 1;
      var hatLen = last - s.column || 1;
      str += "\n --> " + loc + "\n" + filler + " |\n" + offset_s.line + " | " + line + "\n" + filler + " | " + peg$padEnd17("", s.column - 1, " ") + peg$padEnd17("", hatLen, "^");
    } else {
      str += "\n at " + loc;
    }
  }
  return str;
};
peg$SyntaxError17.buildMessage = function(expected, found) {
  var DESCRIBE_EXPECTATION_FNS = {
    literal: function(expectation) {
      return '"' + literalEscape(expectation.text) + '"';
    },
    class: function(expectation) {
      var escapedParts = expectation.parts.map(function(part) {
        return Array.isArray(part) ? classEscape(part[0]) + "-" + classEscape(part[1]) : classEscape(part);
      });
      return "[" + (expectation.inverted ? "^" : "") + escapedParts.join("") + "]";
    },
    any: function() {
      return "any character";
    },
    end: function() {
      return "end of input";
    },
    other: function(expectation) {
      return expectation.description;
    }
  };
  function hex(ch) {
    return ch.charCodeAt(0).toString(16).toUpperCase();
  }
  function literalEscape(s) {
    return s.replace(/\\/g, "\\\\").replace(/"/g, '\\"').replace(/\0/g, "\\0").replace(/\t/g, "\\t").replace(/\n/g, "\\n").replace(/\r/g, "\\r").replace(/[\x00-\x0F]/g, function(ch) {
      return "\\x0" + hex(ch);
    }).replace(/[\x10-\x1F\x7F-\x9F]/g, function(ch) {
      return "\\x" + hex(ch);
    });
  }
  function classEscape(s) {
    return s.replace(/\\/g, "\\\\").replace(/\]/g, "\\]").replace(/\^/g, "\\^").replace(/-/g, "\\-").replace(/\0/g, "\\0").replace(/\t/g, "\\t").replace(/\n/g, "\\n").replace(/\r/g, "\\r").replace(/[\x00-\x0F]/g, function(ch) {
      return "\\x0" + hex(ch);
    }).replace(/[\x10-\x1F\x7F-\x9F]/g, function(ch) {
      return "\\x" + hex(ch);
    });
  }
  function describeExpectation(expectation) {
    return DESCRIBE_EXPECTATION_FNS[expectation.type](expectation);
  }
  function describeExpected(expected2) {
    var descriptions = expected2.map(describeExpectation);
    var i, j;
    descriptions.sort();
    if (descriptions.length > 0) {
      for (i = 1, j = 1; i < descriptions.length; i++) {
        if (descriptions[i - 1] !== descriptions[i]) {
          descriptions[j] = descriptions[i];
          j++;
        }
      }
      descriptions.length = j;
    }
    switch (descriptions.length) {
      case 1:
        return descriptions[0];
      case 2:
        return descriptions[0] + " or " + descriptions[1];
      default:
        return descriptions.slice(0, -1).join(", ") + ", or " + descriptions[descriptions.length - 1];
    }
  }
  function describeFound(found2) {
    return found2 ? '"' + literalEscape(found2) + '"' : "end of input";
  }
  return "Expected " + describeExpected(expected) + " but " + describeFound(found) + " found.";
};
function peg$parse17(input, options) {
  options = options !== void 0 ? options : {};
  var peg$FAILED = {};
  var peg$source = options.grammarSource;
  var peg$startRuleFunctions = { Document: peg$parseDocument };
  var peg$startRuleFunction = peg$parseDocument;
  var peg$c0 = "sankey-beta";
  var peg$c1 = "---";
  var peg$c2 = ",";
  var peg$c3 = "\r";
  var peg$c4 = "\n";
  var peg$c5 = "%%";
  var peg$c6 = "-";
  var peg$c7 = ".";
  var peg$r0 = /^[^\n]/;
  var peg$r1 = /^[^,\n]/;
  var peg$r2 = /^[0-9]/;
  var peg$r3 = /^[ \t]/;
  var peg$e0 = peg$literalExpectation("sankey-beta", true);
  var peg$e1 = peg$literalExpectation("---", false);
  var peg$e2 = peg$classExpectation(["\n"], true, false);
  var peg$e3 = peg$literalExpectation(",", false);
  var peg$e4 = peg$literalExpectation("\r", false);
  var peg$e5 = peg$literalExpectation("\n", false);
  var peg$e6 = peg$literalExpectation("%%", false);
  var peg$e7 = peg$anyExpectation();
  var peg$e8 = peg$classExpectation([",", "\n"], true, false);
  var peg$e9 = peg$literalExpectation("-", false);
  var peg$e10 = peg$classExpectation([["0", "9"]], false, false);
  var peg$e11 = peg$literalExpectation(".", false);
  var peg$e12 = peg$classExpectation([" ", "	"], false, false);
  var peg$f0 = function(fm, rows) {
    const links2 = rows.filter(Boolean);
    return { version: "1.0", metadata: { ...fm || {} }, links: links2 };
  };
  var peg$f1 = function(line) {
    return line;
  };
  var peg$f2 = function(lines2) {
    const meta = {};
    for (const line of lines2) {
      const m = line.match(/^\s*(\w+)\s*:\s*(.+?)\s*$/);
      if (m) meta[m[1]] = m[2];
    }
    return meta;
  };
  var peg$f3 = function(src, tgt, val) {
    return { source: src.trim(), target: tgt.trim(), value: val };
  };
  var peg$f4 = function() {
    return null;
  };
  var peg$f5 = function() {
    return null;
  };
  var peg$f6 = function(n) {
    return parseFloat(n);
  };
  var peg$currPos = options.peg$currPos | 0;
  var peg$savedPos = peg$currPos;
  var peg$posDetailsCache = [{ line: 1, column: 1 }];
  var peg$maxFailPos = peg$currPos;
  var peg$maxFailExpected = options.peg$maxFailExpected || [];
  var peg$silentFails = options.peg$silentFails | 0;
  var peg$result;
  if (options.startRule) {
    if (!(options.startRule in peg$startRuleFunctions)) {
      throw new Error(`Can't start parsing from rule "` + options.startRule + '".');
    }
    peg$startRuleFunction = peg$startRuleFunctions[options.startRule];
  }
  function text() {
    return input.substring(peg$savedPos, peg$currPos);
  }
  function offset() {
    return peg$savedPos;
  }
  function range() {
    return {
      source: peg$source,
      start: peg$savedPos,
      end: peg$currPos
    };
  }
  function location() {
    return peg$computeLocation(peg$savedPos, peg$currPos);
  }
  function expected(description, location2) {
    location2 = location2 !== void 0 ? location2 : peg$computeLocation(peg$savedPos, peg$currPos);
    throw peg$buildStructuredError(
      [peg$otherExpectation(description)],
      input.substring(peg$savedPos, peg$currPos),
      location2
    );
  }
  function error(message, location2) {
    location2 = location2 !== void 0 ? location2 : peg$computeLocation(peg$savedPos, peg$currPos);
    throw peg$buildSimpleError(message, location2);
  }
  function peg$literalExpectation(text2, ignoreCase) {
    return { type: "literal", text: text2, ignoreCase };
  }
  function peg$classExpectation(parts, inverted, ignoreCase) {
    return { type: "class", parts, inverted, ignoreCase };
  }
  function peg$anyExpectation() {
    return { type: "any" };
  }
  function peg$endExpectation() {
    return { type: "end" };
  }
  function peg$otherExpectation(description) {
    return { type: "other", description };
  }
  function peg$computePosDetails(pos) {
    var details = peg$posDetailsCache[pos];
    var p;
    if (details) {
      return details;
    } else {
      if (pos >= peg$posDetailsCache.length) {
        p = peg$posDetailsCache.length - 1;
      } else {
        p = pos;
        while (!peg$posDetailsCache[--p]) {
        }
      }
      details = peg$posDetailsCache[p];
      details = {
        line: details.line,
        column: details.column
      };
      while (p < pos) {
        if (input.charCodeAt(p) === 10) {
          details.line++;
          details.column = 1;
        } else {
          details.column++;
        }
        p++;
      }
      peg$posDetailsCache[pos] = details;
      return details;
    }
  }
  function peg$computeLocation(startPos, endPos, offset2) {
    var startPosDetails = peg$computePosDetails(startPos);
    var endPosDetails = peg$computePosDetails(endPos);
    var res = {
      source: peg$source,
      start: {
        offset: startPos,
        line: startPosDetails.line,
        column: startPosDetails.column
      },
      end: {
        offset: endPos,
        line: endPosDetails.line,
        column: endPosDetails.column
      }
    };
    if (offset2 && peg$source && typeof peg$source.offset === "function") {
      res.start = peg$source.offset(res.start);
      res.end = peg$source.offset(res.end);
    }
    return res;
  }
  function peg$fail(expected2) {
    if (peg$currPos < peg$maxFailPos) {
      return;
    }
    if (peg$currPos > peg$maxFailPos) {
      peg$maxFailPos = peg$currPos;
      peg$maxFailExpected = [];
    }
    peg$maxFailExpected.push(expected2);
  }
  function peg$buildSimpleError(message, location2) {
    return new peg$SyntaxError17(message, null, null, location2);
  }
  function peg$buildStructuredError(expected2, found, location2) {
    return new peg$SyntaxError17(
      peg$SyntaxError17.buildMessage(expected2, found),
      expected2,
      found,
      location2
    );
  }
  function peg$parseDocument() {
    var s0, s1, s2, s3, s4, s5, s6;
    s0 = peg$currPos;
    s1 = peg$parseFrontmatter();
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    s2 = peg$parse_();
    s3 = input.substr(peg$currPos, 11);
    if (s3.toLowerCase() === peg$c0) {
      peg$currPos += 11;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e0);
      }
    }
    if (s3 !== peg$FAILED) {
      s4 = peg$parseLineEnd();
      if (s4 !== peg$FAILED) {
        s5 = [];
        s6 = peg$parseRow();
        while (s6 !== peg$FAILED) {
          s5.push(s6);
          s6 = peg$parseRow();
        }
        s6 = peg$parse_();
        peg$savedPos = s0;
        s0 = peg$f0(s1, s5);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseFrontmatter() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 3) === peg$c1) {
      s1 = peg$c1;
      peg$currPos += 3;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e1);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$parseNL();
      if (s2 !== peg$FAILED) {
        s3 = [];
        s4 = peg$currPos;
        s5 = peg$currPos;
        peg$silentFails++;
        s6 = peg$currPos;
        if (input.substr(peg$currPos, 3) === peg$c1) {
          s7 = peg$c1;
          peg$currPos += 3;
        } else {
          s7 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e1);
          }
        }
        if (s7 !== peg$FAILED) {
          s8 = peg$parseNL();
          if (s8 !== peg$FAILED) {
            s7 = [s7, s8];
            s6 = s7;
          } else {
            peg$currPos = s6;
            s6 = peg$FAILED;
          }
        } else {
          peg$currPos = s6;
          s6 = peg$FAILED;
        }
        peg$silentFails--;
        if (s6 === peg$FAILED) {
          s5 = void 0;
        } else {
          peg$currPos = s5;
          s5 = peg$FAILED;
        }
        if (s5 !== peg$FAILED) {
          s6 = peg$currPos;
          s7 = [];
          s8 = input.charAt(peg$currPos);
          if (peg$r0.test(s8)) {
            peg$currPos++;
          } else {
            s8 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e2);
            }
          }
          while (s8 !== peg$FAILED) {
            s7.push(s8);
            s8 = input.charAt(peg$currPos);
            if (peg$r0.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e2);
              }
            }
          }
          s6 = input.substring(s6, peg$currPos);
          s7 = peg$parseNL();
          if (s7 !== peg$FAILED) {
            peg$savedPos = s4;
            s4 = peg$f1(s6);
          } else {
            peg$currPos = s4;
            s4 = peg$FAILED;
          }
        } else {
          peg$currPos = s4;
          s4 = peg$FAILED;
        }
        while (s4 !== peg$FAILED) {
          s3.push(s4);
          s4 = peg$currPos;
          s5 = peg$currPos;
          peg$silentFails++;
          s6 = peg$currPos;
          if (input.substr(peg$currPos, 3) === peg$c1) {
            s7 = peg$c1;
            peg$currPos += 3;
          } else {
            s7 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e1);
            }
          }
          if (s7 !== peg$FAILED) {
            s8 = peg$parseNL();
            if (s8 !== peg$FAILED) {
              s7 = [s7, s8];
              s6 = s7;
            } else {
              peg$currPos = s6;
              s6 = peg$FAILED;
            }
          } else {
            peg$currPos = s6;
            s6 = peg$FAILED;
          }
          peg$silentFails--;
          if (s6 === peg$FAILED) {
            s5 = void 0;
          } else {
            peg$currPos = s5;
            s5 = peg$FAILED;
          }
          if (s5 !== peg$FAILED) {
            s6 = peg$currPos;
            s7 = [];
            s8 = input.charAt(peg$currPos);
            if (peg$r0.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e2);
              }
            }
            while (s8 !== peg$FAILED) {
              s7.push(s8);
              s8 = input.charAt(peg$currPos);
              if (peg$r0.test(s8)) {
                peg$currPos++;
              } else {
                s8 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e2);
                }
              }
            }
            s6 = input.substring(s6, peg$currPos);
            s7 = peg$parseNL();
            if (s7 !== peg$FAILED) {
              peg$savedPos = s4;
              s4 = peg$f1(s6);
            } else {
              peg$currPos = s4;
              s4 = peg$FAILED;
            }
          } else {
            peg$currPos = s4;
            s4 = peg$FAILED;
          }
        }
        if (input.substr(peg$currPos, 3) === peg$c1) {
          s4 = peg$c1;
          peg$currPos += 3;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e1);
          }
        }
        if (s4 !== peg$FAILED) {
          s5 = peg$parseNL();
          if (s5 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f2(s3);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseRow() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9;
    s0 = peg$parseComment();
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      s1 = peg$parse_();
      s2 = peg$parseField();
      if (s2 !== peg$FAILED) {
        if (input.charCodeAt(peg$currPos) === 44) {
          s3 = peg$c2;
          peg$currPos++;
        } else {
          s3 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e3);
          }
        }
        if (s3 !== peg$FAILED) {
          s4 = peg$parseField();
          if (s4 !== peg$FAILED) {
            if (input.charCodeAt(peg$currPos) === 44) {
              s5 = peg$c2;
              peg$currPos++;
            } else {
              s5 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e3);
              }
            }
            if (s5 !== peg$FAILED) {
              s6 = peg$parse_();
              s7 = peg$parseNum();
              if (s7 !== peg$FAILED) {
                s8 = peg$parse_();
                s9 = peg$parseLineEnd();
                if (s9 !== peg$FAILED) {
                  peg$savedPos = s0;
                  s0 = peg$f3(s2, s4, s7);
                } else {
                  peg$currPos = s0;
                  s0 = peg$FAILED;
                }
              } else {
                peg$currPos = s0;
                s0 = peg$FAILED;
              }
            } else {
              peg$currPos = s0;
              s0 = peg$FAILED;
            }
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
      if (s0 === peg$FAILED) {
        s0 = peg$currPos;
        s1 = peg$parse_();
        if (input.charCodeAt(peg$currPos) === 13) {
          s2 = peg$c3;
          peg$currPos++;
        } else {
          s2 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e4);
          }
        }
        if (s2 === peg$FAILED) {
          s2 = null;
        }
        if (input.charCodeAt(peg$currPos) === 10) {
          s3 = peg$c4;
          peg$currPos++;
        } else {
          s3 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e5);
          }
        }
        if (s3 !== peg$FAILED) {
          peg$savedPos = s0;
          s0 = peg$f4();
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      }
    }
    return s0;
  }
  function peg$parseComment() {
    var s0, s1, s2, s3, s4, s5, s6;
    s0 = peg$currPos;
    s1 = peg$parse_();
    if (input.substr(peg$currPos, 2) === peg$c5) {
      s2 = peg$c5;
      peg$currPos += 2;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e6);
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$currPos;
      s4 = [];
      s5 = input.charAt(peg$currPos);
      if (peg$r0.test(s5)) {
        peg$currPos++;
      } else {
        s5 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e2);
        }
      }
      while (s5 !== peg$FAILED) {
        s4.push(s5);
        s5 = input.charAt(peg$currPos);
        if (peg$r0.test(s5)) {
          peg$currPos++;
        } else {
          s5 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e2);
          }
        }
      }
      s3 = input.substring(s3, peg$currPos);
      s4 = peg$currPos;
      if (input.charCodeAt(peg$currPos) === 13) {
        s5 = peg$c3;
        peg$currPos++;
      } else {
        s5 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e4);
        }
      }
      if (s5 === peg$FAILED) {
        s5 = null;
      }
      if (input.charCodeAt(peg$currPos) === 10) {
        s6 = peg$c4;
        peg$currPos++;
      } else {
        s6 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e5);
        }
      }
      if (s6 !== peg$FAILED) {
        s5 = [s5, s6];
        s4 = s5;
      } else {
        peg$currPos = s4;
        s4 = peg$FAILED;
      }
      if (s4 === peg$FAILED) {
        s4 = peg$currPos;
        peg$silentFails++;
        if (input.length > peg$currPos) {
          s5 = input.charAt(peg$currPos);
          peg$currPos++;
        } else {
          s5 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e7);
          }
        }
        peg$silentFails--;
        if (s5 === peg$FAILED) {
          s4 = void 0;
        } else {
          peg$currPos = s4;
          s4 = peg$FAILED;
        }
      }
      if (s4 !== peg$FAILED) {
        peg$savedPos = s0;
        s0 = peg$f5();
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseField() {
    var s0, s1, s2;
    s0 = peg$currPos;
    s1 = [];
    s2 = input.charAt(peg$currPos);
    if (peg$r1.test(s2)) {
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e8);
      }
    }
    if (s2 !== peg$FAILED) {
      while (s2 !== peg$FAILED) {
        s1.push(s2);
        s2 = input.charAt(peg$currPos);
        if (peg$r1.test(s2)) {
          peg$currPos++;
        } else {
          s2 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e8);
          }
        }
      }
    } else {
      s1 = peg$FAILED;
    }
    if (s1 !== peg$FAILED) {
      s0 = input.substring(s0, peg$currPos);
    } else {
      s0 = s1;
    }
    return s0;
  }
  function peg$parseNum() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8;
    s0 = peg$currPos;
    s1 = peg$currPos;
    s2 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 45) {
      s3 = peg$c6;
      peg$currPos++;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e9);
      }
    }
    if (s3 === peg$FAILED) {
      s3 = null;
    }
    s4 = [];
    s5 = input.charAt(peg$currPos);
    if (peg$r2.test(s5)) {
      peg$currPos++;
    } else {
      s5 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e10);
      }
    }
    if (s5 !== peg$FAILED) {
      while (s5 !== peg$FAILED) {
        s4.push(s5);
        s5 = input.charAt(peg$currPos);
        if (peg$r2.test(s5)) {
          peg$currPos++;
        } else {
          s5 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e10);
          }
        }
      }
    } else {
      s4 = peg$FAILED;
    }
    if (s4 !== peg$FAILED) {
      s5 = peg$currPos;
      if (input.charCodeAt(peg$currPos) === 46) {
        s6 = peg$c7;
        peg$currPos++;
      } else {
        s6 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e11);
        }
      }
      if (s6 !== peg$FAILED) {
        s7 = [];
        s8 = input.charAt(peg$currPos);
        if (peg$r2.test(s8)) {
          peg$currPos++;
        } else {
          s8 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e10);
          }
        }
        if (s8 !== peg$FAILED) {
          while (s8 !== peg$FAILED) {
            s7.push(s8);
            s8 = input.charAt(peg$currPos);
            if (peg$r2.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e10);
              }
            }
          }
        } else {
          s7 = peg$FAILED;
        }
        if (s7 !== peg$FAILED) {
          s6 = [s6, s7];
          s5 = s6;
        } else {
          peg$currPos = s5;
          s5 = peg$FAILED;
        }
      } else {
        peg$currPos = s5;
        s5 = peg$FAILED;
      }
      if (s5 === peg$FAILED) {
        s5 = null;
      }
      s3 = [s3, s4, s5];
      s2 = s3;
    } else {
      peg$currPos = s2;
      s2 = peg$FAILED;
    }
    if (s2 !== peg$FAILED) {
      s1 = input.substring(s1, peg$currPos);
    } else {
      s1 = s2;
    }
    if (s1 !== peg$FAILED) {
      peg$savedPos = s0;
      s1 = peg$f6(s1);
    }
    s0 = s1;
    return s0;
  }
  function peg$parseNL() {
    var s0, s1, s2;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 13) {
      s1 = peg$c3;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e4);
      }
    }
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s2 = peg$c4;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e5);
      }
    }
    if (s2 !== peg$FAILED) {
      s1 = [s1, s2];
      s0 = s1;
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseLineEnd() {
    var s0, s1, s2;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 13) {
      s1 = peg$c3;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e4);
      }
    }
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s2 = peg$c4;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e5);
      }
    }
    if (s2 !== peg$FAILED) {
      s1 = [s1, s2];
      s0 = s1;
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      peg$silentFails++;
      if (input.length > peg$currPos) {
        s1 = input.charAt(peg$currPos);
        peg$currPos++;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e7);
        }
      }
      peg$silentFails--;
      if (s1 === peg$FAILED) {
        s0 = void 0;
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    }
    return s0;
  }
  function peg$parse_() {
    var s0, s1;
    s0 = [];
    s1 = input.charAt(peg$currPos);
    if (peg$r3.test(s1)) {
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e12);
      }
    }
    while (s1 !== peg$FAILED) {
      s0.push(s1);
      s1 = input.charAt(peg$currPos);
      if (peg$r3.test(s1)) {
        peg$currPos++;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e12);
        }
      }
    }
    return s0;
  }
  peg$result = peg$startRuleFunction();
  if (options.peg$library) {
    return (
      /** @type {any} */
      {
        peg$result,
        peg$currPos,
        peg$FAILED,
        peg$maxFailExpected,
        peg$maxFailPos
      }
    );
  }
  if (peg$result !== peg$FAILED && peg$currPos === input.length) {
    return peg$result;
  } else {
    if (peg$result !== peg$FAILED && peg$currPos < input.length) {
      peg$fail(peg$endExpectation());
    }
    throw peg$buildStructuredError(
      peg$maxFailExpected,
      peg$maxFailPos < input.length ? input.charAt(peg$maxFailPos) : null,
      peg$maxFailPos < input.length ? peg$computeLocation(peg$maxFailPos, peg$maxFailPos + 1) : peg$computeLocation(peg$maxFailPos, peg$maxFailPos)
    );
  }
}
var sankey = {
  parseMermaid(input) {
    return peg$parse17(input);
  },
  parseYaml(input) {
    return JSON.parse(input);
  },
  layout(ir, theme) {
    return layoutSankey(ir, theme);
  }
};
function layoutMindmap(ir, theme) {
  const { palette, typography, spacing } = theme;
  const p = pen(theme);
  const margin = spacing.diagramMargin;
  if (!ir.root) {
    return {
      scene: {
        viewBox: { x: 0, y: 0, width: 100, height: 60 },
        background: palette.background,
        elements: []
      },
      anchors: {}
    };
  }
  const font = typography.baseFontSize;
  const smallFont = typography.smallFontSize;
  const levelGap = 70;
  const titleH = ir.metadata.title ? typography.titleFontSize + 14 : 0;
  const fontFor = (d) => d === 0 ? typography.titleFontSize : d === 1 ? font : smallFont;
  const smallFontFor = (d) => d === 0 ? font : smallFont;
  let leafCursor = margin + titleH + 20;
  const place = (node, depth, hue) => {
    const f2 = fontFor(depth);
    const sf = smallFontFor(depth);
    const info = measureFormattedText(node.label, f2, sf);
    const padX = depth === 0 ? 40 : 28;
    const w = Math.max(rhu(info.maxLineWidth + padX), depth === 0 ? 60 : 40);
    const baseH = depth === 0 ? 44 : depth === 1 ? 34 : 26;
    const h2 = info.lineCount > 1 ? rhu(info.lineCount * (f2 * 1.25) + 14) : baseH;
    const kids = node.children.map(
      (c, i) => place(c, depth + 1, depth === 0 ? categoricalHue(i, theme) : hue)
    );
    let y;
    if (kids.length === 0) {
      y = leafCursor + h2 / 2;
      leafCursor += Math.max(30, h2 + 10);
    } else {
      y = (kids[0].y + kids[kids.length - 1].y) / 2;
    }
    return {
      label: node.label,
      depth,
      x: 0,
      y,
      w,
      h: h2,
      hue,
      info,
      ...node.icon ? { icon: node.icon } : {},
      children: kids
    };
  };
  const root = place(ir.root, 0, palette.primary);
  const all = flat(root);
  const depthMax = maxDepth(root);
  const maxW = [];
  for (const n of all) maxW[n.depth] = Math.max(maxW[n.depth] ?? 0, n.w);
  const colStart = [margin];
  for (let d = 1; d <= depthMax; d++)
    colStart[d] = colStart[d - 1] + (maxW[d - 1] ?? 0) + levelGap;
  for (const n of all) n.x = colStart[n.depth];
  const elements = [];
  if (ir.metadata.title) {
    elements.push(
      p.text(
        ir.metadata.title,
        margin,
        margin + typography.titleFontSize,
        typography.titleFontSize,
        palette.text,
        {
          weight: "bold"
        }
      )
    );
  }
  const drawEdges = (n) => {
    for (const c of n.children) {
      const x1 = n.x + n.w, y1 = n.y, x2 = c.x, y2 = c.y;
      const mx = (x1 + x2) / 2;
      elements.push(
        p.path(
          `M ${rhu(x1)} ${rhu(y1)} C ${rhu(mx)} ${rhu(y1)}, ${rhu(mx)} ${rhu(y2)}, ${rhu(x2)} ${rhu(y2)}`,
          c.hue,
          2
        )
      );
      drawEdges(c);
    }
  };
  drawEdges(root);
  const drawNode = (n) => {
    const h2 = n.h;
    const cy = n.y;
    const f2 = fontFor(n.depth);
    const sf = smallFontFor(n.depth);
    const x = rhu(n.x);
    const y = rhu(cy - h2 / 2);
    if (n.depth === 0) {
      elements.push(
        p.rect({ x, y, width: rhu(n.w), height: h2 }, palette.primary, palette.primary, 0, {
          rx: Math.min(h2 / 2, 22)
        })
      );
      elements.push(
        ...renderFormattedText(
          p,
          n.info,
          x,
          y,
          n.w,
          h2,
          f2,
          sf,
          "#FFFFFF",
          "rgba(255, 255, 255, 0.8)",
          {
            align: "middle",
            defaultBold: true
          }
        )
      );
    } else if (n.depth === 1) {
      elements.push(p.rect({ x, y, width: rhu(n.w), height: h2 }, n.hue, n.hue, 0, { rx: 8 }));
      elements.push(
        ...renderFormattedText(
          p,
          n.info,
          x,
          y,
          n.w,
          h2,
          f2,
          sf,
          "#FFFFFF",
          "rgba(255, 255, 255, 0.8)",
          {
            align: "middle",
            defaultBold: true
          }
        )
      );
    } else {
      elements.push(
        p.rect({ x, y, width: rhu(n.w), height: h2 }, palette.surface, n.hue, 1.4, { rx: 6 })
      );
      elements.push(
        ...renderFormattedText(p, n.info, x, y, n.w, h2, f2, sf, palette.text, palette.textMuted, {
          align: "middle"
        })
      );
    }
    if (n.icon)
      elements.push(p.circle({ x: rhu(n.x - 7), y: rhu(cy) }, 4, n.hue, palette.background, 1.5));
    for (const c of n.children) drawNode(c);
  };
  drawNode(root);
  const totalW = rhuInt(Math.max(...all.map((n) => n.x + n.w)) + margin);
  const totalH = rhuInt(leafCursor + margin);
  const scene = applyOverlays(
    {
      viewBox: { x: 0, y: 0, width: totalW, height: totalH },
      background: palette.background,
      elements
    },
    ir.overlays,
    theme
  );
  return { scene, anchors: {} };
}
function flat(n, acc = []) {
  acc.push(n);
  for (const c of n.children) flat(c, acc);
  return acc;
}
function maxDepth(n) {
  return n.children.length ? Math.max(...n.children.map(maxDepth)) : n.depth;
}
function peg$subclass18(child, parent) {
  function C() {
    this.constructor = child;
  }
  C.prototype = parent.prototype;
  child.prototype = new C();
}
function peg$SyntaxError18(message, expected, found, location) {
  var self = Error.call(this, message);
  if (Object.setPrototypeOf) {
    Object.setPrototypeOf(self, peg$SyntaxError18.prototype);
  }
  self.expected = expected;
  self.found = found;
  self.location = location;
  self.name = "SyntaxError";
  return self;
}
peg$subclass18(peg$SyntaxError18, Error);
function peg$padEnd18(str, targetLength, padString) {
  padString = padString || " ";
  if (str.length > targetLength) {
    return str;
  }
  targetLength -= str.length;
  padString += padString.repeat(targetLength);
  return str + padString.slice(0, targetLength);
}
peg$SyntaxError18.prototype.format = function(sources) {
  var str = "Error: " + this.message;
  if (this.location) {
    var src = null;
    var k;
    for (k = 0; k < sources.length; k++) {
      if (sources[k].source === this.location.source) {
        src = sources[k].text.split(/\r\n|\n|\r/g);
        break;
      }
    }
    var s = this.location.start;
    var offset_s = this.location.source && typeof this.location.source.offset === "function" ? this.location.source.offset(s) : s;
    var loc = this.location.source + ":" + offset_s.line + ":" + offset_s.column;
    if (src) {
      var e = this.location.end;
      var filler = peg$padEnd18("", offset_s.line.toString().length, " ");
      var line = src[s.line - 1];
      var last = s.line === e.line ? e.column : line.length + 1;
      var hatLen = last - s.column || 1;
      str += "\n --> " + loc + "\n" + filler + " |\n" + offset_s.line + " | " + line + "\n" + filler + " | " + peg$padEnd18("", s.column - 1, " ") + peg$padEnd18("", hatLen, "^");
    } else {
      str += "\n at " + loc;
    }
  }
  return str;
};
peg$SyntaxError18.buildMessage = function(expected, found) {
  var DESCRIBE_EXPECTATION_FNS = {
    literal: function(expectation) {
      return '"' + literalEscape(expectation.text) + '"';
    },
    class: function(expectation) {
      var escapedParts = expectation.parts.map(function(part) {
        return Array.isArray(part) ? classEscape(part[0]) + "-" + classEscape(part[1]) : classEscape(part);
      });
      return "[" + (expectation.inverted ? "^" : "") + escapedParts.join("") + "]";
    },
    any: function() {
      return "any character";
    },
    end: function() {
      return "end of input";
    },
    other: function(expectation) {
      return expectation.description;
    }
  };
  function hex(ch) {
    return ch.charCodeAt(0).toString(16).toUpperCase();
  }
  function literalEscape(s) {
    return s.replace(/\\/g, "\\\\").replace(/"/g, '\\"').replace(/\0/g, "\\0").replace(/\t/g, "\\t").replace(/\n/g, "\\n").replace(/\r/g, "\\r").replace(/[\x00-\x0F]/g, function(ch) {
      return "\\x0" + hex(ch);
    }).replace(/[\x10-\x1F\x7F-\x9F]/g, function(ch) {
      return "\\x" + hex(ch);
    });
  }
  function classEscape(s) {
    return s.replace(/\\/g, "\\\\").replace(/\]/g, "\\]").replace(/\^/g, "\\^").replace(/-/g, "\\-").replace(/\0/g, "\\0").replace(/\t/g, "\\t").replace(/\n/g, "\\n").replace(/\r/g, "\\r").replace(/[\x00-\x0F]/g, function(ch) {
      return "\\x0" + hex(ch);
    }).replace(/[\x10-\x1F\x7F-\x9F]/g, function(ch) {
      return "\\x" + hex(ch);
    });
  }
  function describeExpectation(expectation) {
    return DESCRIBE_EXPECTATION_FNS[expectation.type](expectation);
  }
  function describeExpected(expected2) {
    var descriptions = expected2.map(describeExpectation);
    var i, j;
    descriptions.sort();
    if (descriptions.length > 0) {
      for (i = 1, j = 1; i < descriptions.length; i++) {
        if (descriptions[i - 1] !== descriptions[i]) {
          descriptions[j] = descriptions[i];
          j++;
        }
      }
      descriptions.length = j;
    }
    switch (descriptions.length) {
      case 1:
        return descriptions[0];
      case 2:
        return descriptions[0] + " or " + descriptions[1];
      default:
        return descriptions.slice(0, -1).join(", ") + ", or " + descriptions[descriptions.length - 1];
    }
  }
  function describeFound(found2) {
    return found2 ? '"' + literalEscape(found2) + '"' : "end of input";
  }
  return "Expected " + describeExpected(expected) + " but " + describeFound(found) + " found.";
};
function peg$parse18(input, options) {
  options = options !== void 0 ? options : {};
  var peg$FAILED = {};
  var peg$source = options.grammarSource;
  var peg$startRuleFunctions = { Document: peg$parseDocument };
  var peg$startRuleFunction = peg$parseDocument;
  var peg$c0 = "mindmap";
  var peg$c1 = "---";
  var peg$c2 = "\r";
  var peg$c3 = "\n";
  var peg$r0 = /^[^\n]/;
  var peg$r1 = /^[ \t]/;
  var peg$r2 = /^[^ \t\n]/;
  var peg$e0 = peg$literalExpectation("mindmap", true);
  var peg$e1 = peg$literalExpectation("---", false);
  var peg$e2 = peg$classExpectation(["\n"], true, false);
  var peg$e3 = peg$classExpectation([" ", "	"], false, false);
  var peg$e4 = peg$literalExpectation("\r", false);
  var peg$e5 = peg$literalExpectation("\n", false);
  var peg$e6 = peg$anyExpectation();
  var peg$e7 = peg$classExpectation([" ", "	", "\n"], true, false);
  var peg$f0 = function(fm, lines2) {
    return { version: "1.0", metadata: { ...fm || {} }, lines: lines2.filter(Boolean) };
  };
  var peg$f1 = function(line) {
    return line;
  };
  var peg$f2 = function(fmlines) {
    const meta = {};
    for (const line of fmlines) {
      const m = line.match(/^\s*(\w+)\s*:\s*(.+?)\s*$/);
      if (m) meta[m[1]] = m[2];
    }
    return meta;
  };
  var peg$f3 = function(indent, content) {
    return { indent: indent.length, raw: content.trim() };
  };
  var peg$f4 = function(content) {
    return { indent: 0, raw: content.trim() };
  };
  var peg$f5 = function() {
    return null;
  };
  var peg$currPos = options.peg$currPos | 0;
  var peg$savedPos = peg$currPos;
  var peg$posDetailsCache = [{ line: 1, column: 1 }];
  var peg$maxFailPos = peg$currPos;
  var peg$maxFailExpected = options.peg$maxFailExpected || [];
  var peg$silentFails = options.peg$silentFails | 0;
  var peg$result;
  if (options.startRule) {
    if (!(options.startRule in peg$startRuleFunctions)) {
      throw new Error(`Can't start parsing from rule "` + options.startRule + '".');
    }
    peg$startRuleFunction = peg$startRuleFunctions[options.startRule];
  }
  function text() {
    return input.substring(peg$savedPos, peg$currPos);
  }
  function offset() {
    return peg$savedPos;
  }
  function range() {
    return {
      source: peg$source,
      start: peg$savedPos,
      end: peg$currPos
    };
  }
  function location() {
    return peg$computeLocation(peg$savedPos, peg$currPos);
  }
  function expected(description, location2) {
    location2 = location2 !== void 0 ? location2 : peg$computeLocation(peg$savedPos, peg$currPos);
    throw peg$buildStructuredError(
      [peg$otherExpectation(description)],
      input.substring(peg$savedPos, peg$currPos),
      location2
    );
  }
  function error(message, location2) {
    location2 = location2 !== void 0 ? location2 : peg$computeLocation(peg$savedPos, peg$currPos);
    throw peg$buildSimpleError(message, location2);
  }
  function peg$literalExpectation(text2, ignoreCase) {
    return { type: "literal", text: text2, ignoreCase };
  }
  function peg$classExpectation(parts, inverted, ignoreCase) {
    return { type: "class", parts, inverted, ignoreCase };
  }
  function peg$anyExpectation() {
    return { type: "any" };
  }
  function peg$endExpectation() {
    return { type: "end" };
  }
  function peg$otherExpectation(description) {
    return { type: "other", description };
  }
  function peg$computePosDetails(pos) {
    var details = peg$posDetailsCache[pos];
    var p;
    if (details) {
      return details;
    } else {
      if (pos >= peg$posDetailsCache.length) {
        p = peg$posDetailsCache.length - 1;
      } else {
        p = pos;
        while (!peg$posDetailsCache[--p]) {
        }
      }
      details = peg$posDetailsCache[p];
      details = {
        line: details.line,
        column: details.column
      };
      while (p < pos) {
        if (input.charCodeAt(p) === 10) {
          details.line++;
          details.column = 1;
        } else {
          details.column++;
        }
        p++;
      }
      peg$posDetailsCache[pos] = details;
      return details;
    }
  }
  function peg$computeLocation(startPos, endPos, offset2) {
    var startPosDetails = peg$computePosDetails(startPos);
    var endPosDetails = peg$computePosDetails(endPos);
    var res = {
      source: peg$source,
      start: {
        offset: startPos,
        line: startPosDetails.line,
        column: startPosDetails.column
      },
      end: {
        offset: endPos,
        line: endPosDetails.line,
        column: endPosDetails.column
      }
    };
    if (offset2 && peg$source && typeof peg$source.offset === "function") {
      res.start = peg$source.offset(res.start);
      res.end = peg$source.offset(res.end);
    }
    return res;
  }
  function peg$fail(expected2) {
    if (peg$currPos < peg$maxFailPos) {
      return;
    }
    if (peg$currPos > peg$maxFailPos) {
      peg$maxFailPos = peg$currPos;
      peg$maxFailExpected = [];
    }
    peg$maxFailExpected.push(expected2);
  }
  function peg$buildSimpleError(message, location2) {
    return new peg$SyntaxError18(message, null, null, location2);
  }
  function peg$buildStructuredError(expected2, found, location2) {
    return new peg$SyntaxError18(
      peg$SyntaxError18.buildMessage(expected2, found),
      expected2,
      found,
      location2
    );
  }
  function peg$parseDocument() {
    var s0, s1, s2, s3, s4, s5, s6;
    s0 = peg$currPos;
    s1 = peg$parseFrontmatter();
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    s2 = peg$parse_();
    s3 = input.substr(peg$currPos, 7);
    if (s3.toLowerCase() === peg$c0) {
      peg$currPos += 7;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e0);
      }
    }
    if (s3 !== peg$FAILED) {
      s4 = peg$parseLineEnd();
      if (s4 !== peg$FAILED) {
        s5 = [];
        s6 = peg$parseLine();
        while (s6 !== peg$FAILED) {
          s5.push(s6);
          s6 = peg$parseLine();
        }
        s6 = peg$parse_();
        peg$savedPos = s0;
        s0 = peg$f0(s1, s5);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseFrontmatter() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 3) === peg$c1) {
      s1 = peg$c1;
      peg$currPos += 3;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e1);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$parseNL();
      if (s2 !== peg$FAILED) {
        s3 = [];
        s4 = peg$currPos;
        s5 = peg$currPos;
        peg$silentFails++;
        s6 = peg$currPos;
        if (input.substr(peg$currPos, 3) === peg$c1) {
          s7 = peg$c1;
          peg$currPos += 3;
        } else {
          s7 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e1);
          }
        }
        if (s7 !== peg$FAILED) {
          s8 = peg$parseNL();
          if (s8 !== peg$FAILED) {
            s7 = [s7, s8];
            s6 = s7;
          } else {
            peg$currPos = s6;
            s6 = peg$FAILED;
          }
        } else {
          peg$currPos = s6;
          s6 = peg$FAILED;
        }
        peg$silentFails--;
        if (s6 === peg$FAILED) {
          s5 = void 0;
        } else {
          peg$currPos = s5;
          s5 = peg$FAILED;
        }
        if (s5 !== peg$FAILED) {
          s6 = peg$currPos;
          s7 = [];
          s8 = input.charAt(peg$currPos);
          if (peg$r0.test(s8)) {
            peg$currPos++;
          } else {
            s8 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e2);
            }
          }
          while (s8 !== peg$FAILED) {
            s7.push(s8);
            s8 = input.charAt(peg$currPos);
            if (peg$r0.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e2);
              }
            }
          }
          s6 = input.substring(s6, peg$currPos);
          s7 = peg$parseNL();
          if (s7 !== peg$FAILED) {
            peg$savedPos = s4;
            s4 = peg$f1(s6);
          } else {
            peg$currPos = s4;
            s4 = peg$FAILED;
          }
        } else {
          peg$currPos = s4;
          s4 = peg$FAILED;
        }
        while (s4 !== peg$FAILED) {
          s3.push(s4);
          s4 = peg$currPos;
          s5 = peg$currPos;
          peg$silentFails++;
          s6 = peg$currPos;
          if (input.substr(peg$currPos, 3) === peg$c1) {
            s7 = peg$c1;
            peg$currPos += 3;
          } else {
            s7 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e1);
            }
          }
          if (s7 !== peg$FAILED) {
            s8 = peg$parseNL();
            if (s8 !== peg$FAILED) {
              s7 = [s7, s8];
              s6 = s7;
            } else {
              peg$currPos = s6;
              s6 = peg$FAILED;
            }
          } else {
            peg$currPos = s6;
            s6 = peg$FAILED;
          }
          peg$silentFails--;
          if (s6 === peg$FAILED) {
            s5 = void 0;
          } else {
            peg$currPos = s5;
            s5 = peg$FAILED;
          }
          if (s5 !== peg$FAILED) {
            s6 = peg$currPos;
            s7 = [];
            s8 = input.charAt(peg$currPos);
            if (peg$r0.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e2);
              }
            }
            while (s8 !== peg$FAILED) {
              s7.push(s8);
              s8 = input.charAt(peg$currPos);
              if (peg$r0.test(s8)) {
                peg$currPos++;
              } else {
                s8 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e2);
                }
              }
            }
            s6 = input.substring(s6, peg$currPos);
            s7 = peg$parseNL();
            if (s7 !== peg$FAILED) {
              peg$savedPos = s4;
              s4 = peg$f1(s6);
            } else {
              peg$currPos = s4;
              s4 = peg$FAILED;
            }
          } else {
            peg$currPos = s4;
            s4 = peg$FAILED;
          }
        }
        if (input.substr(peg$currPos, 3) === peg$c1) {
          s4 = peg$c1;
          peg$currPos += 3;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e1);
          }
        }
        if (s4 !== peg$FAILED) {
          s5 = peg$parseNL();
          if (s5 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f2(s3);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseLine() {
    var s0, s1, s2, s3, s4, s5;
    s0 = peg$currPos;
    s1 = peg$currPos;
    s2 = [];
    s3 = input.charAt(peg$currPos);
    if (peg$r1.test(s3)) {
      peg$currPos++;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e3);
      }
    }
    if (s3 !== peg$FAILED) {
      while (s3 !== peg$FAILED) {
        s2.push(s3);
        s3 = input.charAt(peg$currPos);
        if (peg$r1.test(s3)) {
          peg$currPos++;
        } else {
          s3 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e3);
          }
        }
      }
    } else {
      s2 = peg$FAILED;
    }
    if (s2 !== peg$FAILED) {
      s1 = input.substring(s1, peg$currPos);
    } else {
      s1 = s2;
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$currPos;
      s3 = [];
      s4 = input.charAt(peg$currPos);
      if (peg$r0.test(s4)) {
        peg$currPos++;
      } else {
        s4 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e2);
        }
      }
      if (s4 !== peg$FAILED) {
        while (s4 !== peg$FAILED) {
          s3.push(s4);
          s4 = input.charAt(peg$currPos);
          if (peg$r0.test(s4)) {
            peg$currPos++;
          } else {
            s4 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e2);
            }
          }
        }
      } else {
        s3 = peg$FAILED;
      }
      if (s3 !== peg$FAILED) {
        s2 = input.substring(s2, peg$currPos);
      } else {
        s2 = s3;
      }
      if (s2 !== peg$FAILED) {
        s3 = peg$currPos;
        if (input.charCodeAt(peg$currPos) === 13) {
          s4 = peg$c2;
          peg$currPos++;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e4);
          }
        }
        if (s4 === peg$FAILED) {
          s4 = null;
        }
        if (input.charCodeAt(peg$currPos) === 10) {
          s5 = peg$c3;
          peg$currPos++;
        } else {
          s5 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e5);
          }
        }
        if (s5 !== peg$FAILED) {
          s4 = [s4, s5];
          s3 = s4;
        } else {
          peg$currPos = s3;
          s3 = peg$FAILED;
        }
        if (s3 === peg$FAILED) {
          s3 = peg$currPos;
          peg$silentFails++;
          if (input.length > peg$currPos) {
            s4 = input.charAt(peg$currPos);
            peg$currPos++;
          } else {
            s4 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e6);
            }
          }
          peg$silentFails--;
          if (s4 === peg$FAILED) {
            s3 = void 0;
          } else {
            peg$currPos = s3;
            s3 = peg$FAILED;
          }
        }
        if (s3 !== peg$FAILED) {
          peg$savedPos = s0;
          s0 = peg$f3(s1, s2);
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      s1 = input.charAt(peg$currPos);
      if (peg$r2.test(s1)) {
        peg$currPos++;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e7);
        }
      }
      if (s1 !== peg$FAILED) {
        s2 = [];
        s3 = input.charAt(peg$currPos);
        if (peg$r0.test(s3)) {
          peg$currPos++;
        } else {
          s3 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e2);
          }
        }
        while (s3 !== peg$FAILED) {
          s2.push(s3);
          s3 = input.charAt(peg$currPos);
          if (peg$r0.test(s3)) {
            peg$currPos++;
          } else {
            s3 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e2);
            }
          }
        }
        s3 = peg$currPos;
        if (input.charCodeAt(peg$currPos) === 13) {
          s4 = peg$c2;
          peg$currPos++;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e4);
          }
        }
        if (s4 === peg$FAILED) {
          s4 = null;
        }
        if (input.charCodeAt(peg$currPos) === 10) {
          s5 = peg$c3;
          peg$currPos++;
        } else {
          s5 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e5);
          }
        }
        if (s5 !== peg$FAILED) {
          s4 = [s4, s5];
          s3 = s4;
        } else {
          peg$currPos = s3;
          s3 = peg$FAILED;
        }
        if (s3 === peg$FAILED) {
          s3 = peg$currPos;
          peg$silentFails++;
          if (input.length > peg$currPos) {
            s4 = input.charAt(peg$currPos);
            peg$currPos++;
          } else {
            s4 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e6);
            }
          }
          peg$silentFails--;
          if (s4 === peg$FAILED) {
            s3 = void 0;
          } else {
            peg$currPos = s3;
            s3 = peg$FAILED;
          }
        }
        if (s3 !== peg$FAILED) {
          peg$savedPos = s0;
          s0 = peg$f4(s1);
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
      if (s0 === peg$FAILED) {
        s0 = peg$currPos;
        s1 = [];
        s2 = input.charAt(peg$currPos);
        if (peg$r1.test(s2)) {
          peg$currPos++;
        } else {
          s2 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e3);
          }
        }
        while (s2 !== peg$FAILED) {
          s1.push(s2);
          s2 = input.charAt(peg$currPos);
          if (peg$r1.test(s2)) {
            peg$currPos++;
          } else {
            s2 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e3);
            }
          }
        }
        if (input.charCodeAt(peg$currPos) === 13) {
          s2 = peg$c2;
          peg$currPos++;
        } else {
          s2 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e4);
          }
        }
        if (s2 === peg$FAILED) {
          s2 = null;
        }
        if (input.charCodeAt(peg$currPos) === 10) {
          s3 = peg$c3;
          peg$currPos++;
        } else {
          s3 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e5);
          }
        }
        if (s3 !== peg$FAILED) {
          peg$savedPos = s0;
          s0 = peg$f5();
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      }
    }
    return s0;
  }
  function peg$parseNL() {
    var s0, s1, s2;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 13) {
      s1 = peg$c2;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e4);
      }
    }
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s2 = peg$c3;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e5);
      }
    }
    if (s2 !== peg$FAILED) {
      s1 = [s1, s2];
      s0 = s1;
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseLineEnd() {
    var s0, s1, s2;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 13) {
      s1 = peg$c2;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e4);
      }
    }
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s2 = peg$c3;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e5);
      }
    }
    if (s2 !== peg$FAILED) {
      s1 = [s1, s2];
      s0 = s1;
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      peg$silentFails++;
      if (input.length > peg$currPos) {
        s1 = input.charAt(peg$currPos);
        peg$currPos++;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e6);
        }
      }
      peg$silentFails--;
      if (s1 === peg$FAILED) {
        s0 = void 0;
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    }
    return s0;
  }
  function peg$parse_() {
    var s0, s1;
    s0 = [];
    s1 = input.charAt(peg$currPos);
    if (peg$r1.test(s1)) {
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e3);
      }
    }
    while (s1 !== peg$FAILED) {
      s0.push(s1);
      s1 = input.charAt(peg$currPos);
      if (peg$r1.test(s1)) {
        peg$currPos++;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e3);
        }
      }
    }
    return s0;
  }
  peg$result = peg$startRuleFunction();
  if (options.peg$library) {
    return (
      /** @type {any} */
      {
        peg$result,
        peg$currPos,
        peg$FAILED,
        peg$maxFailExpected,
        peg$maxFailPos
      }
    );
  }
  if (peg$result !== peg$FAILED && peg$currPos === input.length) {
    return peg$result;
  } else {
    if (peg$result !== peg$FAILED && peg$currPos < input.length) {
      peg$fail(peg$endExpectation());
    }
    throw peg$buildStructuredError(
      peg$maxFailExpected,
      peg$maxFailPos < input.length ? input.charAt(peg$maxFailPos) : null,
      peg$maxFailPos < input.length ? peg$computeLocation(peg$maxFailPos, peg$maxFailPos + 1) : peg$computeLocation(peg$maxFailPos, peg$maxFailPos)
    );
  }
}
function cleanLabel(raw) {
  const m = raw.match(/^[^([{]*[([{]+(.+?)[)\]}]+$/);
  return (m ? m[1] : raw).trim();
}
function buildTree(lines2) {
  if (lines2.length === 0) return void 0;
  const first = lines2.find((l) => !l.raw.startsWith("::"));
  if (!first) return void 0;
  const root = { label: cleanLabel(first.raw), children: [] };
  const stack2 = [{ node: root, indent: first.indent }];
  let last = root;
  let started = false;
  for (const ln of lines2) {
    if (!started) {
      if (ln === first) started = true;
      continue;
    }
    const iconMatch = ln.raw.match(/^::icon\(([^)]*)\)/);
    if (iconMatch) {
      last.icon = iconMatch[1].trim();
      continue;
    }
    const node = { label: cleanLabel(ln.raw), children: [] };
    while (stack2.length > 1 && stack2[stack2.length - 1].indent >= ln.indent) stack2.pop();
    stack2[stack2.length - 1].node.children.push(node);
    stack2.push({ node, indent: ln.indent });
    last = node;
  }
  return root;
}
var mindmap = {
  parseMermaid(input) {
    const raw = peg$parse18(input);
    const root = buildTree(raw.lines);
    return { version: raw.version, metadata: raw.metadata, ...root ? { root } : {} };
  },
  parseYaml(input) {
    return JSON.parse(input);
  },
  layout(ir, theme) {
    return layoutMindmap(ir, theme);
  }
};
function layoutGitgraph(ir, theme) {
  const { palette, typography, spacing } = theme;
  const p = pen(theme);
  const margin = spacing.diagramMargin;
  const titleH = ir.metadata.title ? typography.titleFontSize + 14 : 0;
  const xGap = 70;
  const laneGap = 60;
  const leftPad = margin + 70;
  const topPad = margin + titleH + 24;
  const maxX = Math.max(0, ...ir.commits.map((c) => c.x));
  const cx = (x) => leftPad + x * xGap;
  const cy = (lane) => topPad + lane * laneGap;
  const hue = (lane) => categoricalHue(lane, theme);
  const elements = [];
  if (ir.metadata.title)
    elements.push(
      p.text(
        ir.metadata.title,
        margin,
        margin + typography.titleFontSize,
        typography.titleFontSize,
        palette.text,
        { weight: "bold" }
      )
    );
  for (let lane = 0; lane < ir.lanes; lane++) {
    const laneCommits = ir.commits.filter((c) => c.lane === lane);
    if (laneCommits.length === 0 && lane !== 0) continue;
    const xs = laneCommits.map((c) => c.x);
    const bp = ir.branchPoints.find((b) => b.lane === lane);
    const startX = bp ? Math.min(bp.x, ...xs.length ? xs : [bp.x]) : Math.min(0, ...xs.length ? xs : [0]);
    const endX = xs.length ? Math.max(...xs) : startX;
    elements.push(
      p.path(
        `M ${rhu(cx(startX))} ${rhu(cy(lane))} L ${rhu(cx(endX))} ${rhu(cy(lane))}`,
        hue(lane),
        3,
        { opacity: 0.55 }
      )
    );
    const name = ir.laneNames[lane];
    if (name)
      elements.push(
        p.text(
          name,
          margin,
          rhu(cy(lane) + typography.smallFontSize * 0.35),
          typography.smallFontSize,
          hue(lane),
          { weight: "bold" }
        )
      );
  }
  for (const b of ir.branchPoints) {
    const x1 = cx(b.parentX), y1 = cy(b.parentLane);
    const x2 = cx(b.x), y2 = cy(b.lane);
    const mx = (x1 + x2) / 2;
    elements.push(
      p.path(
        `M ${rhu(x1)} ${rhu(y1)} C ${rhu(mx)} ${rhu(y1)}, ${rhu(mx)} ${rhu(y2)}, ${rhu(x2)} ${rhu(y2)}`,
        hue(b.lane),
        2.5,
        { opacity: 0.55 }
      )
    );
  }
  for (const c of ir.commits) {
    if (!c.isMerge || c.fromLane === void 0 || c.fromX === void 0) continue;
    const x1 = cx(c.fromX), y1 = cy(c.fromLane);
    const x2 = cx(c.x), y2 = cy(c.lane);
    const mx = (x1 + x2) / 2;
    elements.push(
      p.path(
        `M ${rhu(x1)} ${rhu(y1)} C ${rhu(mx)} ${rhu(y1)}, ${rhu(mx)} ${rhu(y2)}, ${rhu(x2)} ${rhu(y2)}`,
        hue(c.fromLane),
        2.5,
        { opacity: 0.6 }
      )
    );
  }
  for (const c of ir.commits) {
    const x = cx(c.x), y = cy(c.lane);
    const col = hue(c.lane);
    if (c.type === "highlight") {
      elements.push(
        p.rect(
          { x: rhu(x - 9), y: rhu(y - 9), width: 18, height: 18 },
          palette.warning,
          palette.background,
          2,
          { rx: 3 }
        )
      );
    } else if (c.isMerge) {
      elements.push(p.circle({ x: rhu(x), y: rhu(y) }, 9, palette.background, col, 2.5));
      elements.push(p.circle({ x: rhu(x), y: rhu(y) }, 3.5, col, col, 0));
    } else {
      elements.push(p.circle({ x: rhu(x), y: rhu(y) }, 8, col, palette.background, 2));
    }
    elements.push(
      p.text(c.id, rhu(x), rhu(y + 22), typography.smallFontSize, palette.textMuted, {
        anchor: "middle"
      })
    );
    if (c.tag) {
      const w = measureText(c.tag, typography.smallFontSize).width + 12;
      elements.push(
        p.rect(
          { x: rhu(x - w / 2), y: rhu(y - 34), width: rhu(w), height: 17 },
          palette.warning + "22",
          palette.warning,
          1,
          { rx: 3 }
        )
      );
      elements.push(
        p.text(c.tag, rhu(x), rhu(y - 22), typography.smallFontSize, palette.text, {
          anchor: "middle",
          weight: "bold"
        })
      );
    }
  }
  const totalW = rhuInt(cx(maxX) + 80 + margin);
  const totalH = rhuInt(cy(ir.lanes - 1) + laneGap + margin);
  const scene = applyOverlays(
    {
      viewBox: { x: 0, y: 0, width: totalW, height: totalH },
      background: palette.background,
      elements
    },
    ir.overlays,
    theme
  );
  return { scene, anchors: {} };
}
function opts(list2) {
  const o = {};
  for (const it of list2) o[it.k.toLowerCase()] = it.v;
  return o;
}
function peg$subclass19(child, parent) {
  function C() {
    this.constructor = child;
  }
  C.prototype = parent.prototype;
  child.prototype = new C();
}
function peg$SyntaxError19(message, expected, found, location) {
  var self = Error.call(this, message);
  if (Object.setPrototypeOf) {
    Object.setPrototypeOf(self, peg$SyntaxError19.prototype);
  }
  self.expected = expected;
  self.found = found;
  self.location = location;
  self.name = "SyntaxError";
  return self;
}
peg$subclass19(peg$SyntaxError19, Error);
function peg$padEnd19(str, targetLength, padString) {
  padString = padString || " ";
  if (str.length > targetLength) {
    return str;
  }
  targetLength -= str.length;
  padString += padString.repeat(targetLength);
  return str + padString.slice(0, targetLength);
}
peg$SyntaxError19.prototype.format = function(sources) {
  var str = "Error: " + this.message;
  if (this.location) {
    var src = null;
    var k;
    for (k = 0; k < sources.length; k++) {
      if (sources[k].source === this.location.source) {
        src = sources[k].text.split(/\r\n|\n|\r/g);
        break;
      }
    }
    var s = this.location.start;
    var offset_s = this.location.source && typeof this.location.source.offset === "function" ? this.location.source.offset(s) : s;
    var loc = this.location.source + ":" + offset_s.line + ":" + offset_s.column;
    if (src) {
      var e = this.location.end;
      var filler = peg$padEnd19("", offset_s.line.toString().length, " ");
      var line = src[s.line - 1];
      var last = s.line === e.line ? e.column : line.length + 1;
      var hatLen = last - s.column || 1;
      str += "\n --> " + loc + "\n" + filler + " |\n" + offset_s.line + " | " + line + "\n" + filler + " | " + peg$padEnd19("", s.column - 1, " ") + peg$padEnd19("", hatLen, "^");
    } else {
      str += "\n at " + loc;
    }
  }
  return str;
};
peg$SyntaxError19.buildMessage = function(expected, found) {
  var DESCRIBE_EXPECTATION_FNS = {
    literal: function(expectation) {
      return '"' + literalEscape(expectation.text) + '"';
    },
    class: function(expectation) {
      var escapedParts = expectation.parts.map(function(part) {
        return Array.isArray(part) ? classEscape(part[0]) + "-" + classEscape(part[1]) : classEscape(part);
      });
      return "[" + (expectation.inverted ? "^" : "") + escapedParts.join("") + "]";
    },
    any: function() {
      return "any character";
    },
    end: function() {
      return "end of input";
    },
    other: function(expectation) {
      return expectation.description;
    }
  };
  function hex(ch) {
    return ch.charCodeAt(0).toString(16).toUpperCase();
  }
  function literalEscape(s) {
    return s.replace(/\\/g, "\\\\").replace(/"/g, '\\"').replace(/\0/g, "\\0").replace(/\t/g, "\\t").replace(/\n/g, "\\n").replace(/\r/g, "\\r").replace(/[\x00-\x0F]/g, function(ch) {
      return "\\x0" + hex(ch);
    }).replace(/[\x10-\x1F\x7F-\x9F]/g, function(ch) {
      return "\\x" + hex(ch);
    });
  }
  function classEscape(s) {
    return s.replace(/\\/g, "\\\\").replace(/\]/g, "\\]").replace(/\^/g, "\\^").replace(/-/g, "\\-").replace(/\0/g, "\\0").replace(/\t/g, "\\t").replace(/\n/g, "\\n").replace(/\r/g, "\\r").replace(/[\x00-\x0F]/g, function(ch) {
      return "\\x0" + hex(ch);
    }).replace(/[\x10-\x1F\x7F-\x9F]/g, function(ch) {
      return "\\x" + hex(ch);
    });
  }
  function describeExpectation(expectation) {
    return DESCRIBE_EXPECTATION_FNS[expectation.type](expectation);
  }
  function describeExpected(expected2) {
    var descriptions = expected2.map(describeExpectation);
    var i, j;
    descriptions.sort();
    if (descriptions.length > 0) {
      for (i = 1, j = 1; i < descriptions.length; i++) {
        if (descriptions[i - 1] !== descriptions[i]) {
          descriptions[j] = descriptions[i];
          j++;
        }
      }
      descriptions.length = j;
    }
    switch (descriptions.length) {
      case 1:
        return descriptions[0];
      case 2:
        return descriptions[0] + " or " + descriptions[1];
      default:
        return descriptions.slice(0, -1).join(", ") + ", or " + descriptions[descriptions.length - 1];
    }
  }
  function describeFound(found2) {
    return found2 ? '"' + literalEscape(found2) + '"' : "end of input";
  }
  return "Expected " + describeExpected(expected) + " but " + describeFound(found) + " found.";
};
function peg$parse19(input, options) {
  options = options !== void 0 ? options : {};
  var peg$FAILED = {};
  var peg$source = options.grammarSource;
  var peg$startRuleFunctions = { Document: peg$parseDocument };
  var peg$startRuleFunction = peg$parseDocument;
  var peg$c0 = "gitgraph";
  var peg$c1 = ":";
  var peg$c2 = "---";
  var peg$c3 = "commit";
  var peg$c4 = "branch";
  var peg$c5 = "order:";
  var peg$c6 = "checkout";
  var peg$c7 = "switch";
  var peg$c8 = "merge";
  var peg$c9 = "id";
  var peg$c10 = "tag";
  var peg$c11 = "type";
  var peg$c12 = '"';
  var peg$c13 = "\r";
  var peg$c14 = "\n";
  var peg$r0 = /^[^\n]/;
  var peg$r1 = /^[0-9]/;
  var peg$r2 = /^[^"]/;
  var peg$r3 = /^[A-Za-z0-9_]/;
  var peg$r4 = /^[A-Za-z0-9_\/.\-]/;
  var peg$r5 = /^[ \t]/;
  var peg$e0 = peg$literalExpectation("gitGraph", true);
  var peg$e1 = peg$literalExpectation(":", false);
  var peg$e2 = peg$classExpectation(["\n"], true, false);
  var peg$e3 = peg$literalExpectation("---", false);
  var peg$e4 = peg$literalExpectation("commit", true);
  var peg$e5 = peg$literalExpectation("branch", true);
  var peg$e6 = peg$literalExpectation("order:", false);
  var peg$e7 = peg$classExpectation([["0", "9"]], false, false);
  var peg$e8 = peg$literalExpectation("checkout", true);
  var peg$e9 = peg$literalExpectation("switch", true);
  var peg$e10 = peg$literalExpectation("merge", true);
  var peg$e11 = peg$literalExpectation("id", true);
  var peg$e12 = peg$literalExpectation("tag", true);
  var peg$e13 = peg$literalExpectation("type", true);
  var peg$e14 = peg$literalExpectation('"', false);
  var peg$e15 = peg$classExpectation(['"'], true, false);
  var peg$e16 = peg$classExpectation([["A", "Z"], ["a", "z"], ["0", "9"], "_"], false, false);
  var peg$e17 = peg$classExpectation([["A", "Z"], ["a", "z"], ["0", "9"], "_", "/", ".", "-"], false, false);
  var peg$e18 = peg$literalExpectation("\r", false);
  var peg$e19 = peg$literalExpectation("\n", false);
  var peg$e20 = peg$anyExpectation();
  var peg$e21 = peg$classExpectation([" ", "	"], false, false);
  var peg$f0 = function(fm, stmts) {
    return { version: "1.0", metadata: { ...fm || {} }, statements: stmts.filter(Boolean) };
  };
  var peg$f1 = function(line) {
    return line;
  };
  var peg$f2 = function(lines2) {
    const meta = {};
    for (const line of lines2) {
      const m = line.match(/^\s*(\w+)\s*:\s*(.+?)\s*$/);
      if (m) meta[m[1]] = m[2];
    }
    return meta;
  };
  var peg$f3 = function(o) {
    const x = opts(o);
    return { t: "commit", id: x.id, tag: x.tag, type: x.type };
  };
  var peg$f4 = function(name) {
    return { t: "branch", name };
  };
  var peg$f5 = function(name) {
    return { t: "checkout", name };
  };
  var peg$f6 = function(name, o) {
    const x = opts(o);
    return { t: "merge", name, id: x.id, tag: x.tag, type: x.type };
  };
  var peg$f7 = function(k, v) {
    return { k, v };
  };
  var peg$f8 = function(s) {
    return s;
  };
  var peg$f9 = function() {
    return null;
  };
  var peg$currPos = options.peg$currPos | 0;
  var peg$savedPos = peg$currPos;
  var peg$posDetailsCache = [{ line: 1, column: 1 }];
  var peg$maxFailPos = peg$currPos;
  var peg$maxFailExpected = options.peg$maxFailExpected || [];
  var peg$silentFails = options.peg$silentFails | 0;
  var peg$result;
  if (options.startRule) {
    if (!(options.startRule in peg$startRuleFunctions)) {
      throw new Error(`Can't start parsing from rule "` + options.startRule + '".');
    }
    peg$startRuleFunction = peg$startRuleFunctions[options.startRule];
  }
  function text() {
    return input.substring(peg$savedPos, peg$currPos);
  }
  function offset() {
    return peg$savedPos;
  }
  function range() {
    return {
      source: peg$source,
      start: peg$savedPos,
      end: peg$currPos
    };
  }
  function location() {
    return peg$computeLocation(peg$savedPos, peg$currPos);
  }
  function expected(description, location2) {
    location2 = location2 !== void 0 ? location2 : peg$computeLocation(peg$savedPos, peg$currPos);
    throw peg$buildStructuredError(
      [peg$otherExpectation(description)],
      input.substring(peg$savedPos, peg$currPos),
      location2
    );
  }
  function error(message, location2) {
    location2 = location2 !== void 0 ? location2 : peg$computeLocation(peg$savedPos, peg$currPos);
    throw peg$buildSimpleError(message, location2);
  }
  function peg$literalExpectation(text2, ignoreCase) {
    return { type: "literal", text: text2, ignoreCase };
  }
  function peg$classExpectation(parts, inverted, ignoreCase) {
    return { type: "class", parts, inverted, ignoreCase };
  }
  function peg$anyExpectation() {
    return { type: "any" };
  }
  function peg$endExpectation() {
    return { type: "end" };
  }
  function peg$otherExpectation(description) {
    return { type: "other", description };
  }
  function peg$computePosDetails(pos) {
    var details = peg$posDetailsCache[pos];
    var p;
    if (details) {
      return details;
    } else {
      if (pos >= peg$posDetailsCache.length) {
        p = peg$posDetailsCache.length - 1;
      } else {
        p = pos;
        while (!peg$posDetailsCache[--p]) {
        }
      }
      details = peg$posDetailsCache[p];
      details = {
        line: details.line,
        column: details.column
      };
      while (p < pos) {
        if (input.charCodeAt(p) === 10) {
          details.line++;
          details.column = 1;
        } else {
          details.column++;
        }
        p++;
      }
      peg$posDetailsCache[pos] = details;
      return details;
    }
  }
  function peg$computeLocation(startPos, endPos, offset2) {
    var startPosDetails = peg$computePosDetails(startPos);
    var endPosDetails = peg$computePosDetails(endPos);
    var res = {
      source: peg$source,
      start: {
        offset: startPos,
        line: startPosDetails.line,
        column: startPosDetails.column
      },
      end: {
        offset: endPos,
        line: endPosDetails.line,
        column: endPosDetails.column
      }
    };
    if (offset2 && peg$source && typeof peg$source.offset === "function") {
      res.start = peg$source.offset(res.start);
      res.end = peg$source.offset(res.end);
    }
    return res;
  }
  function peg$fail(expected2) {
    if (peg$currPos < peg$maxFailPos) {
      return;
    }
    if (peg$currPos > peg$maxFailPos) {
      peg$maxFailPos = peg$currPos;
      peg$maxFailExpected = [];
    }
    peg$maxFailExpected.push(expected2);
  }
  function peg$buildSimpleError(message, location2) {
    return new peg$SyntaxError19(message, null, null, location2);
  }
  function peg$buildStructuredError(expected2, found, location2) {
    return new peg$SyntaxError19(
      peg$SyntaxError19.buildMessage(expected2, found),
      expected2,
      found,
      location2
    );
  }
  function peg$parseDocument() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9, s10;
    s0 = peg$currPos;
    s1 = peg$parseFrontmatter();
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    s2 = peg$parse_();
    s3 = input.substr(peg$currPos, 8);
    if (s3.toLowerCase() === peg$c0) {
      peg$currPos += 8;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e0);
      }
    }
    if (s3 !== peg$FAILED) {
      s4 = peg$currPos;
      s5 = peg$parse_();
      if (input.charCodeAt(peg$currPos) === 58) {
        s6 = peg$c1;
        peg$currPos++;
      } else {
        s6 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e1);
        }
      }
      if (s6 === peg$FAILED) {
        s6 = null;
      }
      s7 = peg$parse_();
      s8 = peg$currPos;
      s9 = [];
      s10 = input.charAt(peg$currPos);
      if (peg$r0.test(s10)) {
        peg$currPos++;
      } else {
        s10 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e2);
        }
      }
      while (s10 !== peg$FAILED) {
        s9.push(s10);
        s10 = input.charAt(peg$currPos);
        if (peg$r0.test(s10)) {
          peg$currPos++;
        } else {
          s10 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e2);
          }
        }
      }
      s8 = input.substring(s8, peg$currPos);
      s5 = [s5, s6, s7, s8];
      s4 = s5;
      s5 = peg$parseLineEnd();
      if (s5 !== peg$FAILED) {
        s6 = [];
        s7 = peg$parseStatement();
        while (s7 !== peg$FAILED) {
          s6.push(s7);
          s7 = peg$parseStatement();
        }
        s7 = peg$parse_();
        peg$savedPos = s0;
        s0 = peg$f0(s1, s6);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseFrontmatter() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 3) === peg$c2) {
      s1 = peg$c2;
      peg$currPos += 3;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e3);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$parseNL();
      if (s2 !== peg$FAILED) {
        s3 = [];
        s4 = peg$currPos;
        s5 = peg$currPos;
        peg$silentFails++;
        s6 = peg$currPos;
        if (input.substr(peg$currPos, 3) === peg$c2) {
          s7 = peg$c2;
          peg$currPos += 3;
        } else {
          s7 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e3);
          }
        }
        if (s7 !== peg$FAILED) {
          s8 = peg$parseNL();
          if (s8 !== peg$FAILED) {
            s7 = [s7, s8];
            s6 = s7;
          } else {
            peg$currPos = s6;
            s6 = peg$FAILED;
          }
        } else {
          peg$currPos = s6;
          s6 = peg$FAILED;
        }
        peg$silentFails--;
        if (s6 === peg$FAILED) {
          s5 = void 0;
        } else {
          peg$currPos = s5;
          s5 = peg$FAILED;
        }
        if (s5 !== peg$FAILED) {
          s6 = peg$currPos;
          s7 = [];
          s8 = input.charAt(peg$currPos);
          if (peg$r0.test(s8)) {
            peg$currPos++;
          } else {
            s8 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e2);
            }
          }
          while (s8 !== peg$FAILED) {
            s7.push(s8);
            s8 = input.charAt(peg$currPos);
            if (peg$r0.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e2);
              }
            }
          }
          s6 = input.substring(s6, peg$currPos);
          s7 = peg$parseNL();
          if (s7 !== peg$FAILED) {
            peg$savedPos = s4;
            s4 = peg$f1(s6);
          } else {
            peg$currPos = s4;
            s4 = peg$FAILED;
          }
        } else {
          peg$currPos = s4;
          s4 = peg$FAILED;
        }
        while (s4 !== peg$FAILED) {
          s3.push(s4);
          s4 = peg$currPos;
          s5 = peg$currPos;
          peg$silentFails++;
          s6 = peg$currPos;
          if (input.substr(peg$currPos, 3) === peg$c2) {
            s7 = peg$c2;
            peg$currPos += 3;
          } else {
            s7 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e3);
            }
          }
          if (s7 !== peg$FAILED) {
            s8 = peg$parseNL();
            if (s8 !== peg$FAILED) {
              s7 = [s7, s8];
              s6 = s7;
            } else {
              peg$currPos = s6;
              s6 = peg$FAILED;
            }
          } else {
            peg$currPos = s6;
            s6 = peg$FAILED;
          }
          peg$silentFails--;
          if (s6 === peg$FAILED) {
            s5 = void 0;
          } else {
            peg$currPos = s5;
            s5 = peg$FAILED;
          }
          if (s5 !== peg$FAILED) {
            s6 = peg$currPos;
            s7 = [];
            s8 = input.charAt(peg$currPos);
            if (peg$r0.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e2);
              }
            }
            while (s8 !== peg$FAILED) {
              s7.push(s8);
              s8 = input.charAt(peg$currPos);
              if (peg$r0.test(s8)) {
                peg$currPos++;
              } else {
                s8 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e2);
                }
              }
            }
            s6 = input.substring(s6, peg$currPos);
            s7 = peg$parseNL();
            if (s7 !== peg$FAILED) {
              peg$savedPos = s4;
              s4 = peg$f1(s6);
            } else {
              peg$currPos = s4;
              s4 = peg$FAILED;
            }
          } else {
            peg$currPos = s4;
            s4 = peg$FAILED;
          }
        }
        if (input.substr(peg$currPos, 3) === peg$c2) {
          s4 = peg$c2;
          peg$currPos += 3;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e3);
          }
        }
        if (s4 !== peg$FAILED) {
          s5 = peg$parseNL();
          if (s5 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f2(s3);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseStatement() {
    var s0;
    s0 = peg$parseCommit();
    if (s0 === peg$FAILED) {
      s0 = peg$parseBranch();
      if (s0 === peg$FAILED) {
        s0 = peg$parseCheckout();
        if (s0 === peg$FAILED) {
          s0 = peg$parseMerge();
          if (s0 === peg$FAILED) {
            s0 = peg$parseBlankLine();
          }
        }
      }
    }
    return s0;
  }
  function peg$parseCommit() {
    var s0, s1, s2, s3, s4;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = input.substr(peg$currPos, 6);
    if (s2.toLowerCase() === peg$c3) {
      peg$currPos += 6;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e4);
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = [];
      s4 = peg$parseOpt();
      while (s4 !== peg$FAILED) {
        s3.push(s4);
        s4 = peg$parseOpt();
      }
      s4 = peg$parseLineEnd();
      if (s4 !== peg$FAILED) {
        peg$savedPos = s0;
        s0 = peg$f3(s3);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseBranch() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9, s10;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = input.substr(peg$currPos, 6);
    if (s2.toLowerCase() === peg$c4) {
      peg$currPos += 6;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e5);
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$parse__();
      if (s3 !== peg$FAILED) {
        s4 = peg$parseBranchName();
        if (s4 !== peg$FAILED) {
          s5 = peg$currPos;
          s6 = peg$parse_();
          if (input.substr(peg$currPos, 6) === peg$c5) {
            s7 = peg$c5;
            peg$currPos += 6;
          } else {
            s7 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e6);
            }
          }
          if (s7 !== peg$FAILED) {
            s8 = peg$parse_();
            s9 = [];
            s10 = input.charAt(peg$currPos);
            if (peg$r1.test(s10)) {
              peg$currPos++;
            } else {
              s10 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e7);
              }
            }
            if (s10 !== peg$FAILED) {
              while (s10 !== peg$FAILED) {
                s9.push(s10);
                s10 = input.charAt(peg$currPos);
                if (peg$r1.test(s10)) {
                  peg$currPos++;
                } else {
                  s10 = peg$FAILED;
                  if (peg$silentFails === 0) {
                    peg$fail(peg$e7);
                  }
                }
              }
            } else {
              s9 = peg$FAILED;
            }
            if (s9 !== peg$FAILED) {
              s6 = [s6, s7, s8, s9];
              s5 = s6;
            } else {
              peg$currPos = s5;
              s5 = peg$FAILED;
            }
          } else {
            peg$currPos = s5;
            s5 = peg$FAILED;
          }
          if (s5 === peg$FAILED) {
            s5 = null;
          }
          s6 = peg$parseLineEnd();
          if (s6 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f4(s4);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseCheckout() {
    var s0, s1, s2, s3, s4, s5;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = input.substr(peg$currPos, 8);
    if (s2.toLowerCase() === peg$c6) {
      peg$currPos += 8;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e8);
      }
    }
    if (s2 === peg$FAILED) {
      s2 = input.substr(peg$currPos, 6);
      if (s2.toLowerCase() === peg$c7) {
        peg$currPos += 6;
      } else {
        s2 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e9);
        }
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$parse__();
      if (s3 !== peg$FAILED) {
        s4 = peg$parseBranchName();
        if (s4 !== peg$FAILED) {
          s5 = peg$parseLineEnd();
          if (s5 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f5(s4);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseMerge() {
    var s0, s1, s2, s3, s4, s5, s6;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = input.substr(peg$currPos, 5);
    if (s2.toLowerCase() === peg$c8) {
      peg$currPos += 5;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e10);
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$parse__();
      if (s3 !== peg$FAILED) {
        s4 = peg$parseBranchName();
        if (s4 !== peg$FAILED) {
          s5 = [];
          s6 = peg$parseOpt();
          while (s6 !== peg$FAILED) {
            s5.push(s6);
            s6 = peg$parseOpt();
          }
          s6 = peg$parseLineEnd();
          if (s6 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f6(s4, s5);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseOpt() {
    var s0, s1, s2, s3, s4, s5, s6;
    s0 = peg$currPos;
    s1 = peg$parse__();
    if (s1 !== peg$FAILED) {
      s2 = input.substr(peg$currPos, 2);
      if (s2.toLowerCase() === peg$c9) {
        peg$currPos += 2;
      } else {
        s2 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e11);
        }
      }
      if (s2 === peg$FAILED) {
        s2 = input.substr(peg$currPos, 3);
        if (s2.toLowerCase() === peg$c10) {
          peg$currPos += 3;
        } else {
          s2 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e12);
          }
        }
        if (s2 === peg$FAILED) {
          s2 = input.substr(peg$currPos, 4);
          if (s2.toLowerCase() === peg$c11) {
            peg$currPos += 4;
          } else {
            s2 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e13);
            }
          }
        }
      }
      if (s2 !== peg$FAILED) {
        s3 = peg$parse_();
        if (input.charCodeAt(peg$currPos) === 58) {
          s4 = peg$c1;
          peg$currPos++;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e1);
          }
        }
        if (s4 !== peg$FAILED) {
          s5 = peg$parse_();
          s6 = peg$parseOptVal();
          if (s6 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f7(s2, s6);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseOptVal() {
    var s0, s1, s2, s3, s4;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 34) {
      s1 = peg$c12;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e14);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$currPos;
      s3 = [];
      s4 = input.charAt(peg$currPos);
      if (peg$r2.test(s4)) {
        peg$currPos++;
      } else {
        s4 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e15);
        }
      }
      while (s4 !== peg$FAILED) {
        s3.push(s4);
        s4 = input.charAt(peg$currPos);
        if (peg$r2.test(s4)) {
          peg$currPos++;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e15);
          }
        }
      }
      s2 = input.substring(s2, peg$currPos);
      if (input.charCodeAt(peg$currPos) === 34) {
        s3 = peg$c12;
        peg$currPos++;
      } else {
        s3 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e14);
        }
      }
      if (s3 !== peg$FAILED) {
        peg$savedPos = s0;
        s0 = peg$f8(s2);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      s1 = [];
      s2 = input.charAt(peg$currPos);
      if (peg$r3.test(s2)) {
        peg$currPos++;
      } else {
        s2 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e16);
        }
      }
      if (s2 !== peg$FAILED) {
        while (s2 !== peg$FAILED) {
          s1.push(s2);
          s2 = input.charAt(peg$currPos);
          if (peg$r3.test(s2)) {
            peg$currPos++;
          } else {
            s2 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e16);
            }
          }
        }
      } else {
        s1 = peg$FAILED;
      }
      if (s1 !== peg$FAILED) {
        s0 = input.substring(s0, peg$currPos);
      } else {
        s0 = s1;
      }
    }
    return s0;
  }
  function peg$parseBranchName() {
    var s0, s1, s2;
    s0 = peg$currPos;
    s1 = [];
    s2 = input.charAt(peg$currPos);
    if (peg$r4.test(s2)) {
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e17);
      }
    }
    if (s2 !== peg$FAILED) {
      while (s2 !== peg$FAILED) {
        s1.push(s2);
        s2 = input.charAt(peg$currPos);
        if (peg$r4.test(s2)) {
          peg$currPos++;
        } else {
          s2 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e17);
          }
        }
      }
    } else {
      s1 = peg$FAILED;
    }
    if (s1 !== peg$FAILED) {
      s0 = input.substring(s0, peg$currPos);
    } else {
      s0 = s1;
    }
    return s0;
  }
  function peg$parseNL() {
    var s0, s1, s2;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 13) {
      s1 = peg$c13;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e18);
      }
    }
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s2 = peg$c14;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e19);
      }
    }
    if (s2 !== peg$FAILED) {
      s1 = [s1, s2];
      s0 = s1;
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseBlankLine() {
    var s0, s1, s2, s3;
    s0 = peg$currPos;
    s1 = peg$parse_();
    if (input.charCodeAt(peg$currPos) === 13) {
      s2 = peg$c13;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e18);
      }
    }
    if (s2 === peg$FAILED) {
      s2 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s3 = peg$c14;
      peg$currPos++;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e19);
      }
    }
    if (s3 !== peg$FAILED) {
      peg$savedPos = s0;
      s0 = peg$f9();
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseLineEnd() {
    var s0, s1, s2;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 13) {
      s1 = peg$c13;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e18);
      }
    }
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s2 = peg$c14;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e19);
      }
    }
    if (s2 !== peg$FAILED) {
      s1 = [s1, s2];
      s0 = s1;
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      peg$silentFails++;
      if (input.length > peg$currPos) {
        s1 = input.charAt(peg$currPos);
        peg$currPos++;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e20);
        }
      }
      peg$silentFails--;
      if (s1 === peg$FAILED) {
        s0 = void 0;
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    }
    return s0;
  }
  function peg$parse_() {
    var s0, s1;
    s0 = [];
    s1 = input.charAt(peg$currPos);
    if (peg$r5.test(s1)) {
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e21);
      }
    }
    while (s1 !== peg$FAILED) {
      s0.push(s1);
      s1 = input.charAt(peg$currPos);
      if (peg$r5.test(s1)) {
        peg$currPos++;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e21);
        }
      }
    }
    return s0;
  }
  function peg$parse__() {
    var s0, s1;
    s0 = [];
    s1 = input.charAt(peg$currPos);
    if (peg$r5.test(s1)) {
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e21);
      }
    }
    if (s1 !== peg$FAILED) {
      while (s1 !== peg$FAILED) {
        s0.push(s1);
        s1 = input.charAt(peg$currPos);
        if (peg$r5.test(s1)) {
          peg$currPos++;
        } else {
          s1 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e21);
          }
        }
      }
    } else {
      s0 = peg$FAILED;
    }
    return s0;
  }
  peg$result = peg$startRuleFunction();
  if (options.peg$library) {
    return (
      /** @type {any} */
      {
        peg$result,
        peg$currPos,
        peg$FAILED,
        peg$maxFailExpected,
        peg$maxFailPos
      }
    );
  }
  if (peg$result !== peg$FAILED && peg$currPos === input.length) {
    return peg$result;
  } else {
    if (peg$result !== peg$FAILED && peg$currPos < input.length) {
      peg$fail(peg$endExpectation());
    }
    throw peg$buildStructuredError(
      peg$maxFailExpected,
      peg$maxFailPos < input.length ? input.charAt(peg$maxFailPos) : null,
      peg$maxFailPos < input.length ? peg$computeLocation(peg$maxFailPos, peg$maxFailPos + 1) : peg$computeLocation(peg$maxFailPos, peg$maxFailPos)
    );
  }
}
function commitType(raw) {
  const t = (raw || "").toUpperCase();
  return t === "HIGHLIGHT" ? "highlight" : t === "REVERSE" ? "reverse" : "normal";
}
var gitgraph = {
  parseMermaid(input) {
    const raw = peg$parse19(input);
    const laneOf = /* @__PURE__ */ new Map([["main", 0]]);
    const laneNames = ["main"];
    const lastX = /* @__PURE__ */ new Map();
    const commits = [];
    const branchPoints = [];
    let current = "main";
    let nextLane = 1;
    let order = 0;
    let auto = 0;
    for (const s of raw.statements) {
      if (s.t === "commit") {
        const lane = laneOf.get(current);
        commits.push({
          id: s.id || `c${auto++}`,
          lane,
          x: order,
          type: commitType(s.type),
          isMerge: false,
          ...s.tag ? { tag: s.tag } : {}
        });
        lastX.set(lane, order);
        order++;
      } else if (s.t === "branch") {
        if (!laneOf.has(s.name)) {
          laneOf.set(s.name, nextLane);
          laneNames[nextLane] = s.name;
          nextLane++;
        }
        const lane = laneOf.get(s.name);
        const parentLane = laneOf.get(current);
        branchPoints.push({ lane, parentLane, x: order, parentX: lastX.get(parentLane) ?? 0 });
        current = s.name;
      } else if (s.t === "checkout") {
        if (laneOf.has(s.name)) current = s.name;
      } else if (s.t === "merge") {
        const lane = laneOf.get(current);
        const fromLane = laneOf.get(s.name) ?? lane;
        commits.push({
          id: s.id || `m${auto++}`,
          lane,
          x: order,
          type: commitType(s.type),
          isMerge: true,
          fromLane,
          fromX: lastX.get(fromLane) ?? order,
          ...s.tag ? { tag: s.tag } : {}
        });
        lastX.set(lane, order);
        order++;
      }
    }
    return {
      version: raw.version,
      metadata: raw.metadata,
      lanes: nextLane,
      laneNames,
      commits,
      branchPoints
    };
  },
  parseYaml(input) {
    return JSON.parse(input);
  },
  layout(ir, theme) {
    return layoutGitgraph(ir, theme);
  }
};
var ARROW_ID6 = "c4-arrow";
var NODE_W2 = 180;
function layoutC4(ir, theme) {
  const { palette, typography, spacing } = theme;
  const p = pen(theme);
  const margin = spacing.diagramMargin;
  const font = typography.smallFontSize;
  const isPerson = (k) => k === "person" || k === "person_ext";
  const isExt = (k) => k.endsWith("_ext");
  const prep = /* @__PURE__ */ new Map();
  for (const n of ir.nodes) {
    const labelInfo = measureFormattedText(n.label, typography.baseFontSize, font, NODE_W2 - 20, 4);
    const descrInfo = n.descr ? measureFormattedText(n.descr, font, font, NODE_W2 - 20, 4) : void 0;
    const labelH = labelInfo.lineCount * rhuInt(typography.baseFontSize * 1.25);
    const descrH = descrInfo ? descrInfo.lineCount * rhuInt(font * 1.3) + 4 : 0;
    const h2 = 14 + labelH + 4 + descrH + 14 + (isPerson(n.kind) ? 10 : 0);
    prep.set(n.id, { labelInfo, descrInfo, h: h2 });
  }
  const nodes = ir.nodes.map((n) => ({
    id: n.id,
    width: NODE_W2,
    height: prep.get(n.id).h
  }));
  const edges2 = ir.rels.map((r) => ({ from: r.from, to: r.to }));
  const laid = layeredLayout(nodes, edges2, {
    direction: "TB",
    layerGap: 80,
    nodeGap: 50,
    margin: margin + 24
  });
  const title = ir.metadata.title;
  const titleH = title ? typography.titleFontSize + 16 : 0;
  const yOff = titleH + 12;
  const elements = [];
  if (title)
    elements.push(
      p.text(
        title,
        margin,
        margin + typography.titleFontSize,
        typography.titleFontSize,
        palette.text,
        { weight: "bold" }
      )
    );
  const sorted = [...ir.boundaries].sort((a, b) => a.depth - b.depth);
  for (const bnd of sorted) {
    const rects = bnd.nodeIds.map((id) => laid.boxes.get(id)).filter((r) => !!r);
    if (rects.length === 0) continue;
    const pad = 18 + bnd.depth * 4;
    const minX = Math.min(...rects.map((r) => r.x)) - pad;
    const minY = Math.min(...rects.map((r) => r.y)) - pad - 16 + yOff;
    const maxX = Math.max(...rects.map((r) => r.x + r.width)) + pad;
    const maxY = Math.max(...rects.map((r) => r.y + r.height)) + pad + yOff;
    const bx = rhu(minX), by = rhu(minY), bw = rhu(maxX - minX), bh = rhu(maxY - minY);
    elements.push(
      p.path(`M ${bx} ${by} h ${bw} v ${bh} h ${-bw} Z`, palette.textMuted, 1.3, { dash: "7 5" })
    );
    elements.push(
      p.text(bnd.label, rhu(minX + 10), rhu(minY + 16), font, palette.textMuted, {
        weight: "bold"
      })
    );
  }
  const allBoxes = [...laid.boxes.values()];
  for (const r of ir.rels) {
    const a = laid.boxes.get(r.from), b = laid.boxes.get(r.to);
    if (!a || !b) continue;
    const ao = { ...a, y: a.y + yOff }, bo = { ...b, y: b.y + yOff };
    const ac = { x: ao.x + ao.width / 2, y: ao.y + ao.height / 2 };
    const bc = { x: bo.x + bo.width / 2, y: bo.y + bo.height / 2 };
    const pa = borderPoint(ao, bc.x, bc.y);
    const pb = borderPoint(bo, ac.x, ac.y);
    const { path, labelMidpoint } = routeEdge(a, b, allBoxes, yOff);
    elements.push(
      p.path(path, palette.textMuted, 1.4, {
        ...r.ext ? { dash: "6 4" } : {},
        markerEnd: ARROW_ID6
      })
    );
    const mx = labelMidpoint.x, my = labelMidpoint.y;
    const lbl = r.tech ? `${r.label ?? ""} [${r.tech}]` : r.label ?? "";
    if (lbl) {
      const w = measureText(lbl, font).width + 8;
      elements.push(
        p.rect(
          { x: rhu(mx - w / 2), y: rhu(my - font), width: rhu(w), height: font + 4 },
          palette.background,
          palette.background,
          0
        )
      );
      elements.push(
        p.text(lbl, rhuInt(mx), rhu(my - 1), font, palette.textMuted, { anchor: "middle" })
      );
    }
  }
  for (const n of ir.nodes) {
    const box = laid.boxes.get(n.id);
    const pr = prep.get(n.id);
    const x = box.x, y = box.y + yOff, w = NODE_W2, h2 = box.height;
    const fill = isExt(n.kind) ? palette.surface : isPerson(n.kind) ? palette.secondary : palette.primary;
    const txt = isExt(n.kind) ? palette.text : "#FFFFFF";
    const mutedTxt = isExt(n.kind) ? palette.textMuted : "rgba(255, 255, 255, 0.8)";
    const rx = isPerson(n.kind) ? 16 : 6;
    elements.push(
      p.rect(
        { x: rhu(x), y: rhu(y), width: w, height: rhu(h2) },
        fill,
        isExt(n.kind) ? palette.textMuted : fill,
        isExt(n.kind) ? 1.4 : 0,
        { rx }
      )
    );
    let ty = y + 16;
    const labelH = pr.labelInfo.lineCount * rhuInt(typography.baseFontSize * 1.25);
    elements.push(
      ...renderFormattedText(
        p,
        pr.labelInfo,
        x,
        ty - 14,
        w,
        labelH + 14,
        typography.baseFontSize,
        font,
        txt,
        mutedTxt,
        { align: "middle", defaultBold: true }
      )
    );
    ty += labelH;
    elements.push(
      p.text(
        `[${n.kind.replace("_", " ")}]`,
        rhuInt(x + w / 2),
        rhu(ty),
        font,
        isExt(n.kind) ? palette.textMuted : "#FFFFFF",
        { anchor: "middle" }
      )
    );
    ty += rhuInt(font * 1.3) + 2;
    if (pr.descrInfo) {
      const descrH = pr.descrInfo.lineCount * rhuInt(font * 1.3);
      elements.push(
        ...renderFormattedText(
          p,
          pr.descrInfo,
          x,
          ty - 10,
          w,
          descrH + 10,
          font,
          font,
          isExt(n.kind) ? palette.textMuted : "#FFFFFF",
          isExt(n.kind) ? palette.textMuted : "rgba(255, 255, 255, 0.8)",
          { align: "middle", opacity: 0.9 }
        )
      );
    }
  }
  const allRects = ir.nodes.map((n) => {
    const b = laid.boxes.get(n.id);
    return { x: b.x, y: b.y + yOff, width: b.width, height: b.height };
  });
  const totalW = rhuInt(Math.max(laid.width, ...allRects.map((r) => r.x + r.width)) + margin + 24);
  const totalH = rhuInt(Math.max(...allRects.map((r) => r.y + r.height)) + margin + 24);
  const s = theme.edges?.arrowSize ?? 8;
  const sH = rhu(s * 0.7);
  const sMidY = rhu(s * 0.35);
  const sRefX = rhu(s - 1);
  const defs = [
    `<marker id="${ARROW_ID6}" markerWidth="${s}" markerHeight="${sH}" refX="${sRefX}" refY="${sMidY}" orient="auto"><polygon points="0 0, ${s} ${sMidY}, 0 ${sH}" fill="${palette.textMuted}" /></marker>`
  ];
  const scene = applyOverlays(
    {
      viewBox: { x: 0, y: 0, width: totalW, height: totalH },
      background: palette.background,
      elements,
      defs
    },
    ir.overlays,
    theme
  );
  return { scene, anchors: {} };
}
function kindOf(k) {
  const s = k.toLowerCase();
  if (s === "person") return "person";
  if (s === "person_ext") return "person_ext";
  if (s === "system_ext") return "system_ext";
  if (s === "systemdb" || s === "containerdb") return "db";
  if (s.startsWith("container")) return s.endsWith("_ext") ? "container_ext" : "container";
  if (s.startsWith("component")) return "component";
  return "system";
}
function peg$subclass20(child, parent) {
  function C() {
    this.constructor = child;
  }
  C.prototype = parent.prototype;
  child.prototype = new C();
}
function peg$SyntaxError20(message, expected, found, location) {
  var self = Error.call(this, message);
  if (Object.setPrototypeOf) {
    Object.setPrototypeOf(self, peg$SyntaxError20.prototype);
  }
  self.expected = expected;
  self.found = found;
  self.location = location;
  self.name = "SyntaxError";
  return self;
}
peg$subclass20(peg$SyntaxError20, Error);
function peg$padEnd20(str, targetLength, padString) {
  padString = padString || " ";
  if (str.length > targetLength) {
    return str;
  }
  targetLength -= str.length;
  padString += padString.repeat(targetLength);
  return str + padString.slice(0, targetLength);
}
peg$SyntaxError20.prototype.format = function(sources) {
  var str = "Error: " + this.message;
  if (this.location) {
    var src = null;
    var k;
    for (k = 0; k < sources.length; k++) {
      if (sources[k].source === this.location.source) {
        src = sources[k].text.split(/\r\n|\n|\r/g);
        break;
      }
    }
    var s = this.location.start;
    var offset_s = this.location.source && typeof this.location.source.offset === "function" ? this.location.source.offset(s) : s;
    var loc = this.location.source + ":" + offset_s.line + ":" + offset_s.column;
    if (src) {
      var e = this.location.end;
      var filler = peg$padEnd20("", offset_s.line.toString().length, " ");
      var line = src[s.line - 1];
      var last = s.line === e.line ? e.column : line.length + 1;
      var hatLen = last - s.column || 1;
      str += "\n --> " + loc + "\n" + filler + " |\n" + offset_s.line + " | " + line + "\n" + filler + " | " + peg$padEnd20("", s.column - 1, " ") + peg$padEnd20("", hatLen, "^");
    } else {
      str += "\n at " + loc;
    }
  }
  return str;
};
peg$SyntaxError20.buildMessage = function(expected, found) {
  var DESCRIBE_EXPECTATION_FNS = {
    literal: function(expectation) {
      return '"' + literalEscape(expectation.text) + '"';
    },
    class: function(expectation) {
      var escapedParts = expectation.parts.map(function(part) {
        return Array.isArray(part) ? classEscape(part[0]) + "-" + classEscape(part[1]) : classEscape(part);
      });
      return "[" + (expectation.inverted ? "^" : "") + escapedParts.join("") + "]";
    },
    any: function() {
      return "any character";
    },
    end: function() {
      return "end of input";
    },
    other: function(expectation) {
      return expectation.description;
    }
  };
  function hex(ch) {
    return ch.charCodeAt(0).toString(16).toUpperCase();
  }
  function literalEscape(s) {
    return s.replace(/\\/g, "\\\\").replace(/"/g, '\\"').replace(/\0/g, "\\0").replace(/\t/g, "\\t").replace(/\n/g, "\\n").replace(/\r/g, "\\r").replace(/[\x00-\x0F]/g, function(ch) {
      return "\\x0" + hex(ch);
    }).replace(/[\x10-\x1F\x7F-\x9F]/g, function(ch) {
      return "\\x" + hex(ch);
    });
  }
  function classEscape(s) {
    return s.replace(/\\/g, "\\\\").replace(/\]/g, "\\]").replace(/\^/g, "\\^").replace(/-/g, "\\-").replace(/\0/g, "\\0").replace(/\t/g, "\\t").replace(/\n/g, "\\n").replace(/\r/g, "\\r").replace(/[\x00-\x0F]/g, function(ch) {
      return "\\x0" + hex(ch);
    }).replace(/[\x10-\x1F\x7F-\x9F]/g, function(ch) {
      return "\\x" + hex(ch);
    });
  }
  function describeExpectation(expectation) {
    return DESCRIBE_EXPECTATION_FNS[expectation.type](expectation);
  }
  function describeExpected(expected2) {
    var descriptions = expected2.map(describeExpectation);
    var i, j;
    descriptions.sort();
    if (descriptions.length > 0) {
      for (i = 1, j = 1; i < descriptions.length; i++) {
        if (descriptions[i - 1] !== descriptions[i]) {
          descriptions[j] = descriptions[i];
          j++;
        }
      }
      descriptions.length = j;
    }
    switch (descriptions.length) {
      case 1:
        return descriptions[0];
      case 2:
        return descriptions[0] + " or " + descriptions[1];
      default:
        return descriptions.slice(0, -1).join(", ") + ", or " + descriptions[descriptions.length - 1];
    }
  }
  function describeFound(found2) {
    return found2 ? '"' + literalEscape(found2) + '"' : "end of input";
  }
  return "Expected " + describeExpected(expected) + " but " + describeFound(found) + " found.";
};
function peg$parse20(input, options) {
  options = options !== void 0 ? options : {};
  var peg$FAILED = {};
  var peg$source = options.grammarSource;
  var peg$startRuleFunctions = { Document: peg$parseDocument };
  var peg$startRuleFunction = peg$parseDocument;
  var peg$c0 = "c4";
  var peg$c1 = "context";
  var peg$c2 = "container";
  var peg$c3 = "component";
  var peg$c4 = "dynamic";
  var peg$c5 = "deployment";
  var peg$c6 = "---";
  var peg$c7 = "title";
  var peg$c8 = "enterprise_boundary";
  var peg$c9 = "system_boundary";
  var peg$c10 = "container_boundary";
  var peg$c11 = "boundary";
  var peg$c12 = "(";
  var peg$c13 = ",";
  var peg$c14 = ")";
  var peg$c15 = "{";
  var peg$c16 = "\r";
  var peg$c17 = "\n";
  var peg$c18 = "}";
  var peg$c19 = "rel_ext";
  var peg$c20 = "birel";
  var peg$c21 = "rel";
  var peg$c22 = '"';
  var peg$r0 = /^[^\n]/;
  var peg$r1 = /^[A-Za-z]/;
  var peg$r2 = /^[A-Za-z0-9_]/;
  var peg$r3 = /^[^"]/;
  var peg$r4 = /^[ \t]/;
  var peg$e0 = peg$literalExpectation("C4", true);
  var peg$e1 = peg$literalExpectation("Context", true);
  var peg$e2 = peg$literalExpectation("Container", true);
  var peg$e3 = peg$literalExpectation("Component", true);
  var peg$e4 = peg$literalExpectation("Dynamic", true);
  var peg$e5 = peg$literalExpectation("Deployment", true);
  var peg$e6 = peg$literalExpectation("---", false);
  var peg$e7 = peg$classExpectation(["\n"], true, false);
  var peg$e8 = peg$literalExpectation("title", true);
  var peg$e9 = peg$literalExpectation("Enterprise_Boundary", true);
  var peg$e10 = peg$literalExpectation("System_Boundary", true);
  var peg$e11 = peg$literalExpectation("Container_Boundary", true);
  var peg$e12 = peg$literalExpectation("Boundary", true);
  var peg$e13 = peg$literalExpectation("(", false);
  var peg$e14 = peg$literalExpectation(",", false);
  var peg$e15 = peg$literalExpectation(")", false);
  var peg$e16 = peg$literalExpectation("{", false);
  var peg$e17 = peg$literalExpectation("\r", false);
  var peg$e18 = peg$literalExpectation("\n", false);
  var peg$e19 = peg$literalExpectation("}", false);
  var peg$e20 = peg$literalExpectation("Rel_Ext", true);
  var peg$e21 = peg$literalExpectation("BiRel", true);
  var peg$e22 = peg$literalExpectation("Rel", true);
  var peg$e23 = peg$classExpectation([["A", "Z"], ["a", "z"]], false, false);
  var peg$e24 = peg$classExpectation([["A", "Z"], ["a", "z"], ["0", "9"], "_"], false, false);
  var peg$e25 = peg$literalExpectation('"', false);
  var peg$e26 = peg$classExpectation(['"'], true, false);
  var peg$e27 = peg$anyExpectation();
  var peg$e28 = peg$classExpectation([" ", "	"], false, false);
  var peg$f0 = function(fm, stmts) {
    const nodes = [];
    const boundaries = [];
    const rels = [];
    let title;
    const walk = (list2, depth) => {
      const ids = [];
      for (const s of list2.filter(Boolean)) {
        if (s.t === "title") title = s.v;
        else if (s.t === "node") {
          nodes.push({ id: s.id, label: s.label, kind: s.kind, ...s.descr ? { descr: s.descr } : {} });
          ids.push(s.id);
        } else if (s.t === "rel") rels.push({ from: s.from, to: s.to, ext: s.ext, ...s.label ? { label: s.label } : {}, ...s.tech ? { tech: s.tech } : {} });
        else if (s.t === "boundary") {
          const inner = walk(s.inner, depth + 1);
          boundaries.push({ id: s.id, label: s.label, depth, nodeIds: inner });
          for (const id of inner) ids.push(id);
        }
      }
      return ids;
    };
    walk(stmts, 0);
    return { version: "1.0", metadata: { ...fm || {}, ...title ? { title } : {} }, nodes, boundaries, rels };
  };
  var peg$f1 = function(line) {
    return line;
  };
  var peg$f2 = function(lines2) {
    const meta = {};
    for (const line of lines2) {
      const m = line.match(/^\s*(\w+)\s*:\s*(.+?)\s*$/);
      if (m) meta[m[1]] = m[2];
    }
    return meta;
  };
  var peg$f3 = function(v) {
    return { t: "title", v: v.trim() };
  };
  var peg$f4 = function(id, label, inner) {
    return { t: "boundary", id, label, inner };
  };
  var peg$f5 = function(k, id, label, d) {
    return d;
  };
  var peg$f6 = function(k, id, label, descr) {
    return { t: "node", kind: kindOf(k), id, label, descr: descr || void 0 };
  };
  var peg$f7 = function(kind, from, to, l) {
    return l;
  };
  var peg$f8 = function(kind, from, to, label, t) {
    return t;
  };
  var peg$f9 = function(kind, from, to, label, tech) {
    return { t: "rel", from, to, label: label || void 0, tech: tech || void 0, ext: kind.toLowerCase() === "rel_ext" };
  };
  var peg$f10 = function(s) {
    return s.replace(/<br\s*\/?>/gi, " ").replace(/\s+/g, " ").trim();
  };
  var peg$f11 = function() {
    return null;
  };
  var peg$currPos = options.peg$currPos | 0;
  var peg$savedPos = peg$currPos;
  var peg$posDetailsCache = [{ line: 1, column: 1 }];
  var peg$maxFailPos = peg$currPos;
  var peg$maxFailExpected = options.peg$maxFailExpected || [];
  var peg$silentFails = options.peg$silentFails | 0;
  var peg$result;
  if (options.startRule) {
    if (!(options.startRule in peg$startRuleFunctions)) {
      throw new Error(`Can't start parsing from rule "` + options.startRule + '".');
    }
    peg$startRuleFunction = peg$startRuleFunctions[options.startRule];
  }
  function text() {
    return input.substring(peg$savedPos, peg$currPos);
  }
  function offset() {
    return peg$savedPos;
  }
  function range() {
    return {
      source: peg$source,
      start: peg$savedPos,
      end: peg$currPos
    };
  }
  function location() {
    return peg$computeLocation(peg$savedPos, peg$currPos);
  }
  function expected(description, location2) {
    location2 = location2 !== void 0 ? location2 : peg$computeLocation(peg$savedPos, peg$currPos);
    throw peg$buildStructuredError(
      [peg$otherExpectation(description)],
      input.substring(peg$savedPos, peg$currPos),
      location2
    );
  }
  function error(message, location2) {
    location2 = location2 !== void 0 ? location2 : peg$computeLocation(peg$savedPos, peg$currPos);
    throw peg$buildSimpleError(message, location2);
  }
  function peg$literalExpectation(text2, ignoreCase) {
    return { type: "literal", text: text2, ignoreCase };
  }
  function peg$classExpectation(parts, inverted, ignoreCase) {
    return { type: "class", parts, inverted, ignoreCase };
  }
  function peg$anyExpectation() {
    return { type: "any" };
  }
  function peg$endExpectation() {
    return { type: "end" };
  }
  function peg$otherExpectation(description) {
    return { type: "other", description };
  }
  function peg$computePosDetails(pos) {
    var details = peg$posDetailsCache[pos];
    var p;
    if (details) {
      return details;
    } else {
      if (pos >= peg$posDetailsCache.length) {
        p = peg$posDetailsCache.length - 1;
      } else {
        p = pos;
        while (!peg$posDetailsCache[--p]) {
        }
      }
      details = peg$posDetailsCache[p];
      details = {
        line: details.line,
        column: details.column
      };
      while (p < pos) {
        if (input.charCodeAt(p) === 10) {
          details.line++;
          details.column = 1;
        } else {
          details.column++;
        }
        p++;
      }
      peg$posDetailsCache[pos] = details;
      return details;
    }
  }
  function peg$computeLocation(startPos, endPos, offset2) {
    var startPosDetails = peg$computePosDetails(startPos);
    var endPosDetails = peg$computePosDetails(endPos);
    var res = {
      source: peg$source,
      start: {
        offset: startPos,
        line: startPosDetails.line,
        column: startPosDetails.column
      },
      end: {
        offset: endPos,
        line: endPosDetails.line,
        column: endPosDetails.column
      }
    };
    if (offset2 && peg$source && typeof peg$source.offset === "function") {
      res.start = peg$source.offset(res.start);
      res.end = peg$source.offset(res.end);
    }
    return res;
  }
  function peg$fail(expected2) {
    if (peg$currPos < peg$maxFailPos) {
      return;
    }
    if (peg$currPos > peg$maxFailPos) {
      peg$maxFailPos = peg$currPos;
      peg$maxFailExpected = [];
    }
    peg$maxFailExpected.push(expected2);
  }
  function peg$buildSimpleError(message, location2) {
    return new peg$SyntaxError20(message, null, null, location2);
  }
  function peg$buildStructuredError(expected2, found, location2) {
    return new peg$SyntaxError20(
      peg$SyntaxError20.buildMessage(expected2, found),
      expected2,
      found,
      location2
    );
  }
  function peg$parseDocument() {
    var s0, s1, s2, s3, s4, s5, s6, s7;
    s0 = peg$currPos;
    s1 = peg$parseFrontmatter();
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    s2 = peg$parse_();
    s3 = input.substr(peg$currPos, 2);
    if (s3.toLowerCase() === peg$c0) {
      peg$currPos += 2;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e0);
      }
    }
    if (s3 !== peg$FAILED) {
      s4 = input.substr(peg$currPos, 7);
      if (s4.toLowerCase() === peg$c1) {
        peg$currPos += 7;
      } else {
        s4 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e1);
        }
      }
      if (s4 === peg$FAILED) {
        s4 = input.substr(peg$currPos, 9);
        if (s4.toLowerCase() === peg$c2) {
          peg$currPos += 9;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e2);
          }
        }
        if (s4 === peg$FAILED) {
          s4 = input.substr(peg$currPos, 9);
          if (s4.toLowerCase() === peg$c3) {
            peg$currPos += 9;
          } else {
            s4 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e3);
            }
          }
          if (s4 === peg$FAILED) {
            s4 = input.substr(peg$currPos, 7);
            if (s4.toLowerCase() === peg$c4) {
              peg$currPos += 7;
            } else {
              s4 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e4);
              }
            }
            if (s4 === peg$FAILED) {
              s4 = input.substr(peg$currPos, 10);
              if (s4.toLowerCase() === peg$c5) {
                peg$currPos += 10;
              } else {
                s4 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e5);
                }
              }
            }
          }
        }
      }
      if (s4 !== peg$FAILED) {
        s5 = peg$parseLineEnd();
        if (s5 !== peg$FAILED) {
          s6 = [];
          s7 = peg$parseStatement();
          while (s7 !== peg$FAILED) {
            s6.push(s7);
            s7 = peg$parseStatement();
          }
          s7 = peg$parse_();
          peg$savedPos = s0;
          s0 = peg$f0(s1, s6);
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseFrontmatter() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 3) === peg$c6) {
      s1 = peg$c6;
      peg$currPos += 3;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e6);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$parseNL();
      if (s2 !== peg$FAILED) {
        s3 = [];
        s4 = peg$currPos;
        s5 = peg$currPos;
        peg$silentFails++;
        s6 = peg$currPos;
        if (input.substr(peg$currPos, 3) === peg$c6) {
          s7 = peg$c6;
          peg$currPos += 3;
        } else {
          s7 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e6);
          }
        }
        if (s7 !== peg$FAILED) {
          s8 = peg$parseNL();
          if (s8 !== peg$FAILED) {
            s7 = [s7, s8];
            s6 = s7;
          } else {
            peg$currPos = s6;
            s6 = peg$FAILED;
          }
        } else {
          peg$currPos = s6;
          s6 = peg$FAILED;
        }
        peg$silentFails--;
        if (s6 === peg$FAILED) {
          s5 = void 0;
        } else {
          peg$currPos = s5;
          s5 = peg$FAILED;
        }
        if (s5 !== peg$FAILED) {
          s6 = peg$currPos;
          s7 = [];
          s8 = input.charAt(peg$currPos);
          if (peg$r0.test(s8)) {
            peg$currPos++;
          } else {
            s8 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e7);
            }
          }
          while (s8 !== peg$FAILED) {
            s7.push(s8);
            s8 = input.charAt(peg$currPos);
            if (peg$r0.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e7);
              }
            }
          }
          s6 = input.substring(s6, peg$currPos);
          s7 = peg$parseNL();
          if (s7 !== peg$FAILED) {
            peg$savedPos = s4;
            s4 = peg$f1(s6);
          } else {
            peg$currPos = s4;
            s4 = peg$FAILED;
          }
        } else {
          peg$currPos = s4;
          s4 = peg$FAILED;
        }
        while (s4 !== peg$FAILED) {
          s3.push(s4);
          s4 = peg$currPos;
          s5 = peg$currPos;
          peg$silentFails++;
          s6 = peg$currPos;
          if (input.substr(peg$currPos, 3) === peg$c6) {
            s7 = peg$c6;
            peg$currPos += 3;
          } else {
            s7 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e6);
            }
          }
          if (s7 !== peg$FAILED) {
            s8 = peg$parseNL();
            if (s8 !== peg$FAILED) {
              s7 = [s7, s8];
              s6 = s7;
            } else {
              peg$currPos = s6;
              s6 = peg$FAILED;
            }
          } else {
            peg$currPos = s6;
            s6 = peg$FAILED;
          }
          peg$silentFails--;
          if (s6 === peg$FAILED) {
            s5 = void 0;
          } else {
            peg$currPos = s5;
            s5 = peg$FAILED;
          }
          if (s5 !== peg$FAILED) {
            s6 = peg$currPos;
            s7 = [];
            s8 = input.charAt(peg$currPos);
            if (peg$r0.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e7);
              }
            }
            while (s8 !== peg$FAILED) {
              s7.push(s8);
              s8 = input.charAt(peg$currPos);
              if (peg$r0.test(s8)) {
                peg$currPos++;
              } else {
                s8 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e7);
                }
              }
            }
            s6 = input.substring(s6, peg$currPos);
            s7 = peg$parseNL();
            if (s7 !== peg$FAILED) {
              peg$savedPos = s4;
              s4 = peg$f1(s6);
            } else {
              peg$currPos = s4;
              s4 = peg$FAILED;
            }
          } else {
            peg$currPos = s4;
            s4 = peg$FAILED;
          }
        }
        if (input.substr(peg$currPos, 3) === peg$c6) {
          s4 = peg$c6;
          peg$currPos += 3;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e6);
          }
        }
        if (s4 !== peg$FAILED) {
          s5 = peg$parseNL();
          if (s5 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f2(s3);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseStatement() {
    var s0;
    s0 = peg$parseTitleLine();
    if (s0 === peg$FAILED) {
      s0 = peg$parseBoundaryBlock();
      if (s0 === peg$FAILED) {
        s0 = peg$parseRelDecl();
        if (s0 === peg$FAILED) {
          s0 = peg$parseNodeDecl();
          if (s0 === peg$FAILED) {
            s0 = peg$parseBlankLine();
          }
        }
      }
    }
    return s0;
  }
  function peg$parseTitleLine() {
    var s0, s1, s2, s3, s4, s5;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = input.substr(peg$currPos, 5);
    if (s2.toLowerCase() === peg$c7) {
      peg$currPos += 5;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e8);
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$parse__();
      if (s3 !== peg$FAILED) {
        s4 = peg$parseRestOfLine();
        if (s4 !== peg$FAILED) {
          s5 = peg$parseLineEnd();
          if (s5 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f3(s4);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseBoundaryBlock() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9, s10, s11, s12, s13, s14, s15, s16, s17, s18, s19, s20, s21;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = input.substr(peg$currPos, 19);
    if (s2.toLowerCase() === peg$c8) {
      peg$currPos += 19;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e9);
      }
    }
    if (s2 === peg$FAILED) {
      s2 = input.substr(peg$currPos, 15);
      if (s2.toLowerCase() === peg$c9) {
        peg$currPos += 15;
      } else {
        s2 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e10);
        }
      }
      if (s2 === peg$FAILED) {
        s2 = input.substr(peg$currPos, 18);
        if (s2.toLowerCase() === peg$c10) {
          peg$currPos += 18;
        } else {
          s2 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e11);
          }
        }
        if (s2 === peg$FAILED) {
          s2 = input.substr(peg$currPos, 8);
          if (s2.toLowerCase() === peg$c11) {
            peg$currPos += 8;
          } else {
            s2 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e12);
            }
          }
        }
      }
    }
    if (s2 !== peg$FAILED) {
      if (input.charCodeAt(peg$currPos) === 40) {
        s3 = peg$c12;
        peg$currPos++;
      } else {
        s3 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e13);
        }
      }
      if (s3 !== peg$FAILED) {
        s4 = peg$parse_();
        s5 = peg$parseIdent();
        if (s5 !== peg$FAILED) {
          s6 = peg$parse_();
          if (input.charCodeAt(peg$currPos) === 44) {
            s7 = peg$c13;
            peg$currPos++;
          } else {
            s7 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e14);
            }
          }
          if (s7 !== peg$FAILED) {
            s8 = peg$parse_();
            s9 = peg$parseQuoted();
            if (s9 !== peg$FAILED) {
              s10 = peg$currPos;
              s11 = peg$parse_();
              if (input.charCodeAt(peg$currPos) === 44) {
                s12 = peg$c13;
                peg$currPos++;
              } else {
                s12 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e14);
                }
              }
              if (s12 !== peg$FAILED) {
                s13 = peg$parse_();
                s14 = peg$parseQuoted();
                if (s14 !== peg$FAILED) {
                  s11 = [s11, s12, s13, s14];
                  s10 = s11;
                } else {
                  peg$currPos = s10;
                  s10 = peg$FAILED;
                }
              } else {
                peg$currPos = s10;
                s10 = peg$FAILED;
              }
              if (s10 === peg$FAILED) {
                s10 = null;
              }
              s11 = peg$parse_();
              if (input.charCodeAt(peg$currPos) === 41) {
                s12 = peg$c14;
                peg$currPos++;
              } else {
                s12 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e15);
                }
              }
              if (s12 !== peg$FAILED) {
                s13 = peg$parse_();
                if (input.charCodeAt(peg$currPos) === 123) {
                  s14 = peg$c15;
                  peg$currPos++;
                } else {
                  s14 = peg$FAILED;
                  if (peg$silentFails === 0) {
                    peg$fail(peg$e16);
                  }
                }
                if (s14 !== peg$FAILED) {
                  s15 = peg$parse_();
                  if (input.charCodeAt(peg$currPos) === 13) {
                    s16 = peg$c16;
                    peg$currPos++;
                  } else {
                    s16 = peg$FAILED;
                    if (peg$silentFails === 0) {
                      peg$fail(peg$e17);
                    }
                  }
                  if (s16 === peg$FAILED) {
                    s16 = null;
                  }
                  if (input.charCodeAt(peg$currPos) === 10) {
                    s17 = peg$c17;
                    peg$currPos++;
                  } else {
                    s17 = peg$FAILED;
                    if (peg$silentFails === 0) {
                      peg$fail(peg$e18);
                    }
                  }
                  if (s17 !== peg$FAILED) {
                    s18 = [];
                    s19 = peg$parseStatement();
                    while (s19 !== peg$FAILED) {
                      s18.push(s19);
                      s19 = peg$parseStatement();
                    }
                    s19 = peg$parse_();
                    if (input.charCodeAt(peg$currPos) === 125) {
                      s20 = peg$c18;
                      peg$currPos++;
                    } else {
                      s20 = peg$FAILED;
                      if (peg$silentFails === 0) {
                        peg$fail(peg$e19);
                      }
                    }
                    if (s20 !== peg$FAILED) {
                      s21 = peg$parseLineEnd();
                      if (s21 !== peg$FAILED) {
                        peg$savedPos = s0;
                        s0 = peg$f4(s5, s9, s18);
                      } else {
                        peg$currPos = s0;
                        s0 = peg$FAILED;
                      }
                    } else {
                      peg$currPos = s0;
                      s0 = peg$FAILED;
                    }
                  } else {
                    peg$currPos = s0;
                    s0 = peg$FAILED;
                  }
                } else {
                  peg$currPos = s0;
                  s0 = peg$FAILED;
                }
              } else {
                peg$currPos = s0;
                s0 = peg$FAILED;
              }
            } else {
              peg$currPos = s0;
              s0 = peg$FAILED;
            }
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseNodeDecl() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9, s10, s11, s12, s13, s14, s15, s16;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = peg$parseNodeKind();
    if (s2 !== peg$FAILED) {
      if (input.charCodeAt(peg$currPos) === 40) {
        s3 = peg$c12;
        peg$currPos++;
      } else {
        s3 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e13);
        }
      }
      if (s3 !== peg$FAILED) {
        s4 = peg$parse_();
        s5 = peg$parseIdent();
        if (s5 !== peg$FAILED) {
          s6 = peg$parse_();
          if (input.charCodeAt(peg$currPos) === 44) {
            s7 = peg$c13;
            peg$currPos++;
          } else {
            s7 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e14);
            }
          }
          if (s7 !== peg$FAILED) {
            s8 = peg$parse_();
            s9 = peg$parseQuoted();
            if (s9 !== peg$FAILED) {
              s10 = peg$currPos;
              s11 = peg$parse_();
              if (input.charCodeAt(peg$currPos) === 44) {
                s12 = peg$c13;
                peg$currPos++;
              } else {
                s12 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e14);
                }
              }
              if (s12 !== peg$FAILED) {
                s13 = peg$parse_();
                s14 = peg$parseQuoted();
                if (s14 !== peg$FAILED) {
                  peg$savedPos = s10;
                  s10 = peg$f5(s2, s5, s9, s14);
                } else {
                  peg$currPos = s10;
                  s10 = peg$FAILED;
                }
              } else {
                peg$currPos = s10;
                s10 = peg$FAILED;
              }
              if (s10 === peg$FAILED) {
                s10 = null;
              }
              s11 = [];
              s12 = peg$currPos;
              s13 = peg$parse_();
              if (input.charCodeAt(peg$currPos) === 44) {
                s14 = peg$c13;
                peg$currPos++;
              } else {
                s14 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e14);
                }
              }
              if (s14 !== peg$FAILED) {
                s15 = peg$parse_();
                s16 = peg$parseQuoted();
                if (s16 !== peg$FAILED) {
                  s13 = [s13, s14, s15, s16];
                  s12 = s13;
                } else {
                  peg$currPos = s12;
                  s12 = peg$FAILED;
                }
              } else {
                peg$currPos = s12;
                s12 = peg$FAILED;
              }
              while (s12 !== peg$FAILED) {
                s11.push(s12);
                s12 = peg$currPos;
                s13 = peg$parse_();
                if (input.charCodeAt(peg$currPos) === 44) {
                  s14 = peg$c13;
                  peg$currPos++;
                } else {
                  s14 = peg$FAILED;
                  if (peg$silentFails === 0) {
                    peg$fail(peg$e14);
                  }
                }
                if (s14 !== peg$FAILED) {
                  s15 = peg$parse_();
                  s16 = peg$parseQuoted();
                  if (s16 !== peg$FAILED) {
                    s13 = [s13, s14, s15, s16];
                    s12 = s13;
                  } else {
                    peg$currPos = s12;
                    s12 = peg$FAILED;
                  }
                } else {
                  peg$currPos = s12;
                  s12 = peg$FAILED;
                }
              }
              s12 = peg$parse_();
              if (input.charCodeAt(peg$currPos) === 41) {
                s13 = peg$c14;
                peg$currPos++;
              } else {
                s13 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e15);
                }
              }
              if (s13 !== peg$FAILED) {
                s14 = peg$parseLineEnd();
                if (s14 !== peg$FAILED) {
                  peg$savedPos = s0;
                  s0 = peg$f6(s2, s5, s9, s10);
                } else {
                  peg$currPos = s0;
                  s0 = peg$FAILED;
                }
              } else {
                peg$currPos = s0;
                s0 = peg$FAILED;
              }
            } else {
              peg$currPos = s0;
              s0 = peg$FAILED;
            }
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseRelDecl() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9, s10, s11, s12, s13, s14, s15, s16, s17;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = input.substr(peg$currPos, 7);
    if (s2.toLowerCase() === peg$c19) {
      peg$currPos += 7;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e20);
      }
    }
    if (s2 === peg$FAILED) {
      s2 = input.substr(peg$currPos, 5);
      if (s2.toLowerCase() === peg$c20) {
        peg$currPos += 5;
      } else {
        s2 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e21);
        }
      }
      if (s2 === peg$FAILED) {
        s2 = input.substr(peg$currPos, 3);
        if (s2.toLowerCase() === peg$c21) {
          peg$currPos += 3;
        } else {
          s2 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e22);
          }
        }
      }
    }
    if (s2 !== peg$FAILED) {
      if (input.charCodeAt(peg$currPos) === 40) {
        s3 = peg$c12;
        peg$currPos++;
      } else {
        s3 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e13);
        }
      }
      if (s3 !== peg$FAILED) {
        s4 = peg$parse_();
        s5 = peg$parseIdent();
        if (s5 !== peg$FAILED) {
          s6 = peg$parse_();
          if (input.charCodeAt(peg$currPos) === 44) {
            s7 = peg$c13;
            peg$currPos++;
          } else {
            s7 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e14);
            }
          }
          if (s7 !== peg$FAILED) {
            s8 = peg$parse_();
            s9 = peg$parseIdent();
            if (s9 !== peg$FAILED) {
              s10 = peg$currPos;
              s11 = peg$parse_();
              if (input.charCodeAt(peg$currPos) === 44) {
                s12 = peg$c13;
                peg$currPos++;
              } else {
                s12 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e14);
                }
              }
              if (s12 !== peg$FAILED) {
                s13 = peg$parse_();
                s14 = peg$parseQuoted();
                if (s14 !== peg$FAILED) {
                  peg$savedPos = s10;
                  s10 = peg$f7(s2, s5, s9, s14);
                } else {
                  peg$currPos = s10;
                  s10 = peg$FAILED;
                }
              } else {
                peg$currPos = s10;
                s10 = peg$FAILED;
              }
              if (s10 === peg$FAILED) {
                s10 = null;
              }
              s11 = peg$currPos;
              s12 = peg$parse_();
              if (input.charCodeAt(peg$currPos) === 44) {
                s13 = peg$c13;
                peg$currPos++;
              } else {
                s13 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e14);
                }
              }
              if (s13 !== peg$FAILED) {
                s14 = peg$parse_();
                s15 = peg$parseQuoted();
                if (s15 !== peg$FAILED) {
                  peg$savedPos = s11;
                  s11 = peg$f8(s2, s5, s9, s10, s15);
                } else {
                  peg$currPos = s11;
                  s11 = peg$FAILED;
                }
              } else {
                peg$currPos = s11;
                s11 = peg$FAILED;
              }
              if (s11 === peg$FAILED) {
                s11 = null;
              }
              s12 = [];
              s13 = peg$currPos;
              s14 = peg$parse_();
              if (input.charCodeAt(peg$currPos) === 44) {
                s15 = peg$c13;
                peg$currPos++;
              } else {
                s15 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e14);
                }
              }
              if (s15 !== peg$FAILED) {
                s16 = peg$parse_();
                s17 = peg$parseQuoted();
                if (s17 !== peg$FAILED) {
                  s14 = [s14, s15, s16, s17];
                  s13 = s14;
                } else {
                  peg$currPos = s13;
                  s13 = peg$FAILED;
                }
              } else {
                peg$currPos = s13;
                s13 = peg$FAILED;
              }
              while (s13 !== peg$FAILED) {
                s12.push(s13);
                s13 = peg$currPos;
                s14 = peg$parse_();
                if (input.charCodeAt(peg$currPos) === 44) {
                  s15 = peg$c13;
                  peg$currPos++;
                } else {
                  s15 = peg$FAILED;
                  if (peg$silentFails === 0) {
                    peg$fail(peg$e14);
                  }
                }
                if (s15 !== peg$FAILED) {
                  s16 = peg$parse_();
                  s17 = peg$parseQuoted();
                  if (s17 !== peg$FAILED) {
                    s14 = [s14, s15, s16, s17];
                    s13 = s14;
                  } else {
                    peg$currPos = s13;
                    s13 = peg$FAILED;
                  }
                } else {
                  peg$currPos = s13;
                  s13 = peg$FAILED;
                }
              }
              s13 = peg$parse_();
              if (input.charCodeAt(peg$currPos) === 41) {
                s14 = peg$c14;
                peg$currPos++;
              } else {
                s14 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e15);
                }
              }
              if (s14 !== peg$FAILED) {
                s15 = peg$parseLineEnd();
                if (s15 !== peg$FAILED) {
                  peg$savedPos = s0;
                  s0 = peg$f9(s2, s5, s9, s10, s11);
                } else {
                  peg$currPos = s0;
                  s0 = peg$FAILED;
                }
              } else {
                peg$currPos = s0;
                s0 = peg$FAILED;
              }
            } else {
              peg$currPos = s0;
              s0 = peg$FAILED;
            }
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseNodeKind() {
    var s0, s1, s2, s3, s4;
    s0 = peg$currPos;
    s1 = peg$currPos;
    s2 = input.charAt(peg$currPos);
    if (peg$r1.test(s2)) {
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e23);
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = [];
      s4 = input.charAt(peg$currPos);
      if (peg$r2.test(s4)) {
        peg$currPos++;
      } else {
        s4 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e24);
        }
      }
      while (s4 !== peg$FAILED) {
        s3.push(s4);
        s4 = input.charAt(peg$currPos);
        if (peg$r2.test(s4)) {
          peg$currPos++;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e24);
          }
        }
      }
      s2 = [s2, s3];
      s1 = s2;
    } else {
      peg$currPos = s1;
      s1 = peg$FAILED;
    }
    if (s1 !== peg$FAILED) {
      s0 = input.substring(s0, peg$currPos);
    } else {
      s0 = s1;
    }
    return s0;
  }
  function peg$parseQuoted() {
    var s0, s1, s2, s3, s4;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 34) {
      s1 = peg$c22;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e25);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$currPos;
      s3 = [];
      s4 = input.charAt(peg$currPos);
      if (peg$r3.test(s4)) {
        peg$currPos++;
      } else {
        s4 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e26);
        }
      }
      while (s4 !== peg$FAILED) {
        s3.push(s4);
        s4 = input.charAt(peg$currPos);
        if (peg$r3.test(s4)) {
          peg$currPos++;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e26);
          }
        }
      }
      s2 = input.substring(s2, peg$currPos);
      if (input.charCodeAt(peg$currPos) === 34) {
        s3 = peg$c22;
        peg$currPos++;
      } else {
        s3 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e25);
        }
      }
      if (s3 !== peg$FAILED) {
        peg$savedPos = s0;
        s0 = peg$f10(s2);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseIdent() {
    var s0, s1, s2;
    s0 = peg$currPos;
    s1 = [];
    s2 = input.charAt(peg$currPos);
    if (peg$r2.test(s2)) {
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e24);
      }
    }
    if (s2 !== peg$FAILED) {
      while (s2 !== peg$FAILED) {
        s1.push(s2);
        s2 = input.charAt(peg$currPos);
        if (peg$r2.test(s2)) {
          peg$currPos++;
        } else {
          s2 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e24);
          }
        }
      }
    } else {
      s1 = peg$FAILED;
    }
    if (s1 !== peg$FAILED) {
      s0 = input.substring(s0, peg$currPos);
    } else {
      s0 = s1;
    }
    return s0;
  }
  function peg$parseRestOfLine() {
    var s0, s1, s2;
    s0 = peg$currPos;
    s1 = [];
    s2 = input.charAt(peg$currPos);
    if (peg$r0.test(s2)) {
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e7);
      }
    }
    if (s2 !== peg$FAILED) {
      while (s2 !== peg$FAILED) {
        s1.push(s2);
        s2 = input.charAt(peg$currPos);
        if (peg$r0.test(s2)) {
          peg$currPos++;
        } else {
          s2 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e7);
          }
        }
      }
    } else {
      s1 = peg$FAILED;
    }
    if (s1 !== peg$FAILED) {
      s0 = input.substring(s0, peg$currPos);
    } else {
      s0 = s1;
    }
    return s0;
  }
  function peg$parseBlankLine() {
    var s0, s1, s2, s3;
    s0 = peg$currPos;
    s1 = peg$parse_();
    if (input.charCodeAt(peg$currPos) === 13) {
      s2 = peg$c16;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e17);
      }
    }
    if (s2 === peg$FAILED) {
      s2 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s3 = peg$c17;
      peg$currPos++;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e18);
      }
    }
    if (s3 !== peg$FAILED) {
      peg$savedPos = s0;
      s0 = peg$f11();
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseNL() {
    var s0, s1, s2;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 13) {
      s1 = peg$c16;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e17);
      }
    }
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s2 = peg$c17;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e18);
      }
    }
    if (s2 !== peg$FAILED) {
      s1 = [s1, s2];
      s0 = s1;
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseLineEnd() {
    var s0, s1, s2;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 13) {
      s1 = peg$c16;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e17);
      }
    }
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s2 = peg$c17;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e18);
      }
    }
    if (s2 !== peg$FAILED) {
      s1 = [s1, s2];
      s0 = s1;
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      peg$silentFails++;
      if (input.length > peg$currPos) {
        s1 = input.charAt(peg$currPos);
        peg$currPos++;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e27);
        }
      }
      peg$silentFails--;
      if (s1 === peg$FAILED) {
        s0 = void 0;
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    }
    return s0;
  }
  function peg$parse_() {
    var s0, s1;
    s0 = [];
    s1 = input.charAt(peg$currPos);
    if (peg$r4.test(s1)) {
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e28);
      }
    }
    while (s1 !== peg$FAILED) {
      s0.push(s1);
      s1 = input.charAt(peg$currPos);
      if (peg$r4.test(s1)) {
        peg$currPos++;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e28);
        }
      }
    }
    return s0;
  }
  function peg$parse__() {
    var s0, s1;
    s0 = [];
    s1 = input.charAt(peg$currPos);
    if (peg$r4.test(s1)) {
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e28);
      }
    }
    if (s1 !== peg$FAILED) {
      while (s1 !== peg$FAILED) {
        s0.push(s1);
        s1 = input.charAt(peg$currPos);
        if (peg$r4.test(s1)) {
          peg$currPos++;
        } else {
          s1 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e28);
          }
        }
      }
    } else {
      s0 = peg$FAILED;
    }
    return s0;
  }
  peg$result = peg$startRuleFunction();
  if (options.peg$library) {
    return (
      /** @type {any} */
      {
        peg$result,
        peg$currPos,
        peg$FAILED,
        peg$maxFailExpected,
        peg$maxFailPos
      }
    );
  }
  if (peg$result !== peg$FAILED && peg$currPos === input.length) {
    return peg$result;
  } else {
    if (peg$result !== peg$FAILED && peg$currPos < input.length) {
      peg$fail(peg$endExpectation());
    }
    throw peg$buildStructuredError(
      peg$maxFailExpected,
      peg$maxFailPos < input.length ? input.charAt(peg$maxFailPos) : null,
      peg$maxFailPos < input.length ? peg$computeLocation(peg$maxFailPos, peg$maxFailPos + 1) : peg$computeLocation(peg$maxFailPos, peg$maxFailPos)
    );
  }
}
var c4 = {
  parseMermaid(input) {
    return peg$parse20(input);
  },
  parseYaml(input) {
    return JSON.parse(input);
  },
  layout(ir, theme) {
    return layoutC4(ir, theme);
  }
};
var CLUSTER_ROUTING_LANE_GAP = 1;
var DELTA = {
  LR: [-1, 0],
  RL: [1, 0],
  TB: [0, -1],
  BT: [0, 1],
  LT: [-1, -1],
  LB: [-1, 1],
  RT: [1, -1],
  RB: [1, 1],
  TL: [-1, -1],
  TR: [1, -1],
  BL: [-1, 1],
  BR: [1, 1]
};
function upperSide(side) {
  const s = side.toUpperCase();
  return s === "L" || s === "R" || s === "T" || s === "B" ? s : void 0;
}
function groupAwareDirectionalGridPlacer(ir) {
  const nodeIds = /* @__PURE__ */ new Set([
    ...ir.services.map((s) => s.id),
    ...ir.junctions.map((j) => j.id)
  ]);
  if (nodeIds.size === 0) return /* @__PURE__ */ new Map();
  const groupIds = new Set(ir.groups.map((g) => g.id));
  const parentOfGroup = /* @__PURE__ */ new Map();
  for (const g of ir.groups) {
    parentOfGroup.set(g.id, g.parent && groupIds.has(g.parent) ? g.parent : "__root__");
  }
  const ownerOfNode = /* @__PURE__ */ new Map();
  for (const s of ir.services)
    ownerOfNode.set(s.id, s.group && groupIds.has(s.group) ? s.group : "__root__");
  for (const j of ir.junctions)
    ownerOfNode.set(j.id, j.group && groupIds.has(j.group) ? j.group : "__root__");
  const groupOrder = /* @__PURE__ */ new Map();
  ir.groups.forEach((g, i) => groupOrder.set(g.id, i));
  const serviceOrder = /* @__PURE__ */ new Map();
  ir.services.forEach((s, i) => serviceOrder.set(s.id, i));
  const junctionOrder = /* @__PURE__ */ new Map();
  ir.junctions.forEach((j, i) => junctionOrder.set(j.id, i));
  const clusterByContainer = /* @__PURE__ */ new Map();
  function isDescendantNode(nodeId, container) {
    if (!nodeIds.has(nodeId)) return false;
    if (container === "__root__") return true;
    let owner = ownerOfNode.get(nodeId);
    while (owner && owner !== "__root__") {
      if (owner === container) return true;
      owner = parentOfGroup.get(owner);
    }
    return false;
  }
  function directChildItem(container, nodeId) {
    if (!nodeIds.has(nodeId)) return void 0;
    const owner = ownerOfNode.get(nodeId) ?? "__root__";
    if (owner === container) return `node:${nodeId}`;
    if (owner === "__root__") return void 0;
    let childGroup = owner;
    let parent = parentOfGroup.get(childGroup) ?? "__root__";
    while (parent !== container) {
      if (parent === "__root__") return void 0;
      childGroup = parent;
      parent = parentOfGroup.get(childGroup) ?? "__root__";
    }
    return `group:${childGroup}`;
  }
  function ancestorContainers(nodeId) {
    const ancestors = ["__root__"];
    const chain = [];
    let owner = ownerOfNode.get(nodeId);
    while (owner && owner !== "__root__") {
      chain.push(owner);
      owner = parentOfGroup.get(owner);
    }
    ancestors.push(...chain.reverse());
    return ancestors;
  }
  function leastCommonContainer(memberIds) {
    const known = memberIds.filter((id) => nodeIds.has(id));
    if (known.length < 2) return void 0;
    const chains = known.map(ancestorContainers);
    let lca = "__root__";
    const minLen = Math.min(...chains.map((c) => c.length));
    for (let i = 0; i < minLen; i++) {
      const candidate = chains[0][i];
      if (chains.every((c) => c[i] === candidate)) lca = candidate;
      else break;
    }
    return lca;
  }
  function buildCluster(container) {
    const cached = clusterByContainer.get(container);
    if (cached) return cached;
    const items = [];
    for (const g of ir.groups) {
      if ((parentOfGroup.get(g.id) ?? "__root__") !== container) continue;
      const child = buildCluster(g.id);
      if (child.width === 0 || child.height === 0) continue;
      items.push({
        id: `group:${g.id}`,
        kind: "group",
        sourceId: g.id,
        width: child.width,
        height: child.height,
        order: groupOrder.get(g.id) ?? 0,
        cluster: child
      });
    }
    for (const s of ir.services) {
      if ((ownerOfNode.get(s.id) ?? "__root__") === container) {
        items.push({
          id: `node:${s.id}`,
          kind: "leaf",
          sourceId: s.id,
          width: 1,
          height: 1,
          order: serviceOrder.get(s.id) ?? 0
        });
      }
    }
    for (const j of ir.junctions) {
      if ((ownerOfNode.get(j.id) ?? "__root__") === container) {
        items.push({
          id: `node:${j.id}`,
          kind: "leaf",
          sourceId: j.id,
          width: 1,
          height: 1,
          order: junctionOrder.get(j.id) ?? 0
        });
      }
    }
    const constraints = [];
    ir.edges.forEach((e, edgeOrder) => {
      if (!isDescendantNode(e.from, container) || !isDescendantNode(e.to, container)) return;
      const fromSide = upperSide(e.fromSide);
      const toSide = upperSide(e.toSide);
      if (!fromSide || !toSide) return;
      const from = directChildItem(container, e.from);
      const to = directChildItem(container, e.to);
      if (!from || !to || from === to) return;
      constraints.push({
        from,
        to,
        fromSide,
        toSide,
        fromInner: innerLocalCell(from, e.from),
        toInner: innerLocalCell(to, e.to),
        edgeOrder
      });
    });
    const itemPos = placeItemsAsRectangles(items, constraints);
    applyContainerAligns(
      container,
      items,
      itemPos,
      ir.aligns,
      leastCommonContainer,
      directChildItem
    );
    const nodePos = /* @__PURE__ */ new Map();
    for (const item of items) {
      const base = itemPos.get(item.id);
      if (!base) continue;
      if (item.kind === "leaf") {
        nodePos.set(item.sourceId, { col: base.col, row: base.row });
      } else {
        for (const [nodeId, local] of item.cluster.nodePos) {
          nodePos.set(nodeId, { col: base.col + local.col, row: base.row + local.row });
        }
      }
    }
    normalizePositions(itemPos, nodePos);
    const bounds = boundsForItems(items, itemPos);
    const cluster = {
      containerId: container,
      items,
      itemPos,
      nodePos,
      width: bounds.width,
      height: bounds.height
    };
    clusterByContainer.set(container, cluster);
    return cluster;
  }
  function innerLocalCell(itemId, nodeId) {
    if (itemId.startsWith("node:")) return { col: 0, row: 0 };
    const groupId = itemId.slice("group:".length);
    const local = clusterByContainer.get(groupId)?.nodePos.get(nodeId);
    return local ? { col: local.col, row: local.row } : { col: 0, row: 0 };
  }
  const root = buildCluster("__root__");
  return new Map([...root.nodePos].map(([id, cell]) => [id, { col: cell.col, row: cell.row }]));
}
function placeItemsAsRectangles(items, constraints) {
  const pos = /* @__PURE__ */ new Map();
  if (items.length === 0) return pos;
  const itemById = new Map(items.map((i) => [i.id, i]));
  const adj = buildAdjacency(items, constraints);
  const occupied = [];
  const connected = /* @__PURE__ */ new Set();
  for (const c of constraints) {
    if (itemById.has(c.from) && itemById.has(c.to)) {
      connected.add(c.from);
      connected.add(c.to);
    }
  }
  const connectedSeeds = items.filter((i) => connected.has(i.id));
  const seeds = connectedSeeds.length > 0 ? [...connectedSeeds, ...items.filter((i) => !connected.has(i.id))] : [...items];
  function place(item, cell) {
    pos.set(item.id, { col: cell.col, row: cell.row });
    occupied.push({
      id: item.id,
      col: cell.col,
      row: cell.row,
      width: item.width,
      height: item.height
    });
  }
  for (const seed of seeds) {
    if (!pos.has(seed.id)) {
      const hasPlacedGroup = occupied.some((r) => itemById.get(r.id)?.kind === "group");
      const laneGap = seed.kind === "group" || hasPlacedGroup ? CLUSTER_ROUTING_LANE_GAP : 0;
      const seedCell = occupied.length === 0 ? { col: 0, row: 0 } : { col: maxRight(occupied) + laneGap, row: 0 };
      place(seed, firstFreeRect(seedCell, seed, occupied));
    }
    const queue2 = [seed];
    while (queue2.length > 0) {
      const curr = queue2.shift();
      const currPos = pos.get(curr.id);
      if (!currPos) continue;
      for (const c of adj.get(curr.id) ?? []) {
        const next = itemById.get(c.to);
        if (!next || pos.has(next.id)) continue;
        const candidate = candidateFromSidePair(
          curr,
          currPos,
          next,
          c.fromSide,
          c.toSide,
          c.currInner,
          c.nextInner
        );
        const free = firstNonOverlappingCandidate(candidate, curr, next, c, occupied);
        place(next, free);
        queue2.push(next);
      }
    }
  }
  normalizeItemPositions(pos);
  return pos;
}
function buildAdjacency(items, constraints) {
  const itemIds = new Set(items.map((i) => i.id));
  const firstWins = /* @__PURE__ */ new Map();
  for (const item of items) firstWins.set(item.id, /* @__PURE__ */ new Map());
  for (const c of [...constraints].sort((a, b) => a.edgeOrder - b.edgeOrder)) {
    if (!itemIds.has(c.from) || !itemIds.has(c.to)) continue;
    addAdj(firstWins, c.from, c.fromSide + c.toSide, {
      to: c.to,
      fromSide: c.fromSide,
      toSide: c.toSide,
      currInner: c.fromInner,
      nextInner: c.toInner,
      edgeOrder: c.edgeOrder
    });
    addAdj(firstWins, c.to, c.toSide + c.fromSide, {
      to: c.from,
      fromSide: c.toSide,
      toSide: c.fromSide,
      currInner: c.toInner,
      nextInner: c.fromInner,
      edgeOrder: c.edgeOrder
    });
  }
  return new Map(
    [...firstWins].map(([id, byPair]) => [
      id,
      [...byPair.values()].sort((a, b) => a.edgeOrder - b.edgeOrder)
    ])
  );
}
function addAdj(adj, from, pair, c) {
  const byPair = adj.get(from);
  if (!byPair) return;
  if (byPair.has(pair) && byPair.get(pair).to !== c.to) {
    console.warn(
      `[gridPlacer] Contradictory constraint on "${from}" direction ${pair} \u2014 first-wins`
    );
    return;
  }
  if (!byPair.has(pair)) byPair.set(pair, c);
}
function candidateFromSidePair(curr, currPos, next, fromSide, toSide, currInner, nextInner) {
  const d = DELTA[fromSide + toSide];
  if (!d) return { col: currPos.col, row: currPos.row };
  const laneGap = curr.kind === "group" || next.kind === "group" ? CLUSTER_ROUTING_LANE_GAP : 0;
  if (isMixedSidePair(fromSide, toSide)) {
    return mixedSideCandidate(curr, currPos, next, fromSide, toSide, currInner, nextInner, laneGap);
  }
  let col = currPos.col;
  let row = currPos.row;
  if (d[0] < 0) col = currPos.col - next.width - laneGap;
  else if (d[0] > 0) col = currPos.col + curr.width + laneGap;
  if (d[1] < 0) row = currPos.row - next.height - laneGap;
  else if (d[1] > 0) row = currPos.row + curr.height + laneGap;
  return { col, row };
}
function isHorizontalSide(side) {
  return side === "L" || side === "R";
}
function isMixedSidePair(fromSide, toSide) {
  return isHorizontalSide(fromSide) !== isHorizontalSide(toSide);
}
function mixedSideCandidate(curr, currPos, next, fromSide, toSide, currInner, nextInner, laneGap) {
  let col = currPos.col;
  let row = currPos.row;
  const innerCol = currPos.col + currInner.col;
  const innerRow = currPos.row + currInner.row;
  switch (fromSide) {
    case "R":
      col = currPos.col + curr.width + laneGap;
      break;
    case "L":
      col = currPos.col - next.width - laneGap;
      break;
    case "B":
      row = currPos.row + curr.height + laneGap;
      break;
    case "T":
      row = currPos.row - next.height - laneGap;
      break;
  }
  switch (toSide) {
    case "R":
      col = innerCol - 1 - nextInner.col;
      break;
    case "L":
      col = innerCol + 1 - nextInner.col;
      break;
    case "B":
      row = innerRow - 1 - nextInner.row;
      break;
    case "T":
      row = innerRow + 1 - nextInner.row;
      break;
  }
  return { col, row };
}
function firstNonOverlappingCandidate(candidate, curr, next, c, occupied) {
  if (!overlapsAny(candidate, next, occupied)) return candidate;
  const d = placementHalfPlaneDelta(c.fromSide, c.toSide);
  const currRect = occupied.find((r) => r.id === curr.id);
  const limit = Math.max(
    12,
    occupied.length * 4 + maxRight(occupied) + maxBottom(occupied) + next.width + next.height
  );
  const offsets = orderedOffsets(limit);
  if (d[0] !== 0 && d[1] === 0) {
    for (const off of offsets) {
      const cell = { col: candidate.col, row: candidate.row + off };
      if (preservesHalfPlane(cell, next, currRect, d) && !overlapsAny(cell, next, occupied))
        return cell;
    }
  } else if (d[0] === 0 && d[1] !== 0) {
    for (const off of offsets) {
      const cell = { col: candidate.col + off, row: candidate.row };
      if (preservesHalfPlane(cell, next, currRect, d) && !overlapsAny(cell, next, occupied))
        return cell;
    }
  } else {
    for (let radius = 0; radius <= limit; radius++) {
      for (let dx = -radius; dx <= radius; dx++) {
        const dyAbs = radius - Math.abs(dx);
        for (const dy of dyAbs === 0 ? [0] : [-dyAbs, dyAbs]) {
          const cell = { col: candidate.col + dx, row: candidate.row + dy };
          if (preservesHalfPlane(cell, next, currRect, d) && !overlapsAny(cell, next, occupied))
            return cell;
        }
      }
    }
  }
  console.warn(
    `[gridPlacer] Rectangle collision near (${candidate.col},${candidate.row}), placing "${next.id}" after occupied extent`
  );
  return firstFreeRect({ col: maxRight(occupied), row: 0 }, next, occupied);
}
function placementHalfPlaneDelta(fromSide, toSide) {
  if (isMixedSidePair(fromSide, toSide)) {
    return isHorizontalSide(fromSide) ? [fromSide === "L" ? -1 : 1, 0] : [0, fromSide === "T" ? -1 : 1];
  }
  return DELTA[fromSide + toSide] ?? [0, 0];
}
function orderedOffsets(limit) {
  const out = [0];
  for (let i = 1; i <= limit; i++) out.push(i, -i);
  return out;
}
function preservesHalfPlane(cell, item, curr, d) {
  if (!curr) return true;
  const dx = d[0] ?? 0;
  const dy = d[1] ?? 0;
  if (dx < 0 && cell.col + item.width > curr.col) return false;
  if (dx > 0 && cell.col < curr.col + curr.width) return false;
  if (dy < 0 && cell.row + item.height > curr.row) return false;
  if (dy > 0 && cell.row < curr.row + curr.height) return false;
  return true;
}
function firstFreeRect(start, item, occupied) {
  let row = start.row;
  const widthLimit = Math.max(start.col + 1, maxRight(occupied) + item.width + occupied.length + 2);
  for (; ; ) {
    for (let col = start.col; col <= widthLimit; col++) {
      const cell = { col, row };
      if (!overlapsAny(cell, item, occupied)) return cell;
    }
    row++;
  }
}
function overlapsAny(cell, item, occupied, ignore = /* @__PURE__ */ new Set()) {
  return occupied.some(
    (r) => !ignore.has(r.id) && rectsOverlap2(cell.col, cell.row, item.width, item.height, r.col, r.row, r.width, r.height)
  );
}
function rectsOverlap2(ax, ay, aw, ah, bx, by, bw, bh) {
  return ax < bx + bw && ax + aw > bx && ay < by + bh && ay + ah > by;
}
function maxRight(rects) {
  return rects.length === 0 ? 0 : Math.max(...rects.map((r) => r.col + r.width));
}
function maxBottom(rects) {
  return rects.length === 0 ? 0 : Math.max(...rects.map((r) => r.row + r.height));
}
function normalizeItemPositions(pos) {
  if (pos.size === 0) return;
  const minCol = Math.min(...[...pos.values()].map((p) => p.col));
  const minRow = Math.min(...[...pos.values()].map((p) => p.row));
  for (const p of pos.values()) {
    p.col -= minCol;
    p.row -= minRow;
  }
}
function normalizePositions(itemPos, nodePos) {
  if (itemPos.size === 0 && nodePos.size === 0) return;
  const vals = [...itemPos.values(), ...nodePos.values()];
  const minCol = Math.min(...vals.map((p) => p.col));
  const minRow = Math.min(...vals.map((p) => p.row));
  for (const p of itemPos.values()) {
    p.col -= minCol;
    p.row -= minRow;
  }
  for (const p of nodePos.values()) {
    p.col -= minCol;
    p.row -= minRow;
  }
}
function boundsForItems(items, itemPos) {
  if (items.length === 0) return { width: 0, height: 0 };
  let width = 0;
  let height = 0;
  for (const item of items) {
    const p = itemPos.get(item.id);
    if (!p) continue;
    width = Math.max(width, p.col + item.width);
    height = Math.max(height, p.row + item.height);
  }
  return { width, height };
}
function applyContainerAligns(container, items, itemPos, aligns, leastCommonContainer, directChildItem) {
  const itemById = new Map(items.map((i) => [i.id, i]));
  for (const align of aligns) {
    if (leastCommonContainer(align.members) !== container) continue;
    const itemIds = [
      ...new Set(
        align.members.map((id) => directChildItem(container, id)).filter((id) => !!id)
      )
    ];
    if (itemIds.length < 2) continue;
    if (!itemIds.every((id) => itemById.has(id) && itemPos.has(id))) continue;
    const coords = itemIds.map((id) => itemPos.get(id)[align.axis === "row" ? "row" : "col"]).sort((a, b) => a - b);
    const target = coords[Math.floor(coords.length / 2)];
    const proposed = new Map(
      [...itemPos].map(([id, p]) => [id, { col: p.col, row: p.row }])
    );
    for (const id of itemIds) {
      const p = proposed.get(id);
      if (align.axis === "row") p.row = target;
      else p.col = target;
    }
    if (positionsOverlap(items, proposed)) {
      console.warn(
        `[gridPlacer] Skipping align ${align.axis} [${align.members.join(", ")}] to preserve containment`
      );
      continue;
    }
    for (const [id, p] of proposed) itemPos.set(id, p);
  }
  normalizeItemPositions(itemPos);
}
function positionsOverlap(items, pos) {
  const rects = [];
  for (const item of items) {
    const p = pos.get(item.id);
    if (!p) continue;
    if (overlapsAny(p, item, rects)) return true;
    rects.push({ id: item.id, col: p.col, row: p.row, width: item.width, height: item.height });
  }
  return false;
}
var ARROW_END_ID2 = "arch-arrow-end";
var ARROW_START_ID2 = "arch-arrow-start";
var MIN_SERVICE_W = 130;
var MIN_SERVICE_H = 56;
var SERVICE_PAD = 12;
var ICON_SIZE = 24;
var ICON_LANE_W = 34;
var ICON_BAND_H = 30;
var SERVICE_TOP_BAR_H = 8;
var CENTER_ICON_GAP = 8;
function normalizedIconAlign(align) {
  return align ?? "N";
}
function isSideIconAlign(align) {
  return align === "E" || align === "W";
}
function serviceSize(label, align, font, smallFont) {
  const a = normalizedIconAlign(align);
  const info = measureFormattedText(label, font, smallFont, 220, 4);
  const laneW = isSideIconAlign(a) ? ICON_LANE_W : 0;
  const width = Math.ceil(Math.max(MIN_SERVICE_W, laneW + info.maxLineWidth + SERVICE_PAD * 2));
  const textH = info.lineCount > 1 ? info.lineCount * (font * 1.25) : font * 1.2;
  const baseH = a === "C" ? Math.ceil(
    Math.max(
      MIN_SERVICE_H,
      SERVICE_TOP_BAR_H + ICON_SIZE + CENTER_ICON_GAP + textH + SERVICE_PAD
    )
  ) : MIN_SERVICE_H;
  const height = info.lineCount > 1 ? Math.max(
    baseH,
    Math.ceil(
      SERVICE_TOP_BAR_H + textH + (a === "N" || a === "NE" || a === "NW" ? ICON_BAND_H : 0) + SERVICE_PAD
    )
  ) : baseH;
  return { width, height, info };
}
function port(r, side, t = 0.5) {
  switch (side.toUpperCase()) {
    case "L":
      return { x: r.x, y: r.y + r.height * t };
    case "R":
      return { x: r.x + r.width, y: r.y + r.height * t };
    case "T":
      return { x: r.x + r.width * t, y: r.y };
    default:
      return { x: r.x + r.width * t, y: r.y + r.height };
  }
}
function sideToDir(side) {
  switch (side.toUpperCase()) {
    case "L":
      return "W";
    case "R":
      return "E";
    case "T":
      return "N";
    default:
      return "S";
  }
}
function dirToSide(dir) {
  switch (dir) {
    case "W":
      return "L";
    case "E":
      return "R";
    case "N":
      return "T";
    case "S":
      return "B";
  }
}
function rectCenter2(r) {
  return { x: r.x + r.width / 2, y: r.y + r.height / 2 };
}
function endpointSide(edge, isSource) {
  const wall = isSource ? edge.exitWall : edge.entryWall;
  return wall ? dirToSide(wall) : isSource ? edge.fromSide : edge.toSide;
}
function tangentKeyForSide(side, otherCenter) {
  switch (side.toUpperCase()) {
    case "T":
    case "B":
      return otherCenter.x;
    default:
      return otherCenter.y;
  }
}
function edgeDash2(style) {
  switch (style) {
    case "dotted":
      return "6 3";
    case "dashed":
      return "8 4";
    default:
      return void 0;
  }
}
function edgeStrokeWidth2(style, base) {
  return style === "thick" ? base * 2 : base;
}
function iconCenter(r, align) {
  const a = normalizedIconAlign(align);
  const sideY = r.y + SERVICE_TOP_BAR_H + (r.height - SERVICE_TOP_BAR_H) / 2;
  const topY = r.y + SERVICE_TOP_BAR_H + ICON_SIZE / 2;
  const bottomY = r.y + r.height - ICON_SIZE / 2 - 4;
  switch (a) {
    case "S":
      return { x: r.x + r.width / 2, y: bottomY };
    case "E":
      return { x: r.x + r.width - ICON_LANE_W / 2, y: sideY };
    case "W":
      return { x: r.x + ICON_LANE_W / 2, y: sideY };
    case "NE":
      return { x: r.x + r.width - ICON_LANE_W / 2, y: topY };
    case "NW":
      return { x: r.x + ICON_LANE_W / 2, y: topY };
    case "SE":
      return { x: r.x + r.width - ICON_LANE_W / 2, y: bottomY };
    case "SW":
      return { x: r.x + ICON_LANE_W / 2, y: bottomY };
    case "C":
      return { x: r.x + r.width / 2, y: topY };
    case "N":
    default:
      return { x: r.x + r.width / 2, y: topY };
  }
}
function serviceLabelArea(r, align) {
  const a = normalizedIconAlign(align);
  switch (a) {
    case "W":
      return {
        x: r.x + ICON_LANE_W + SERVICE_PAD,
        y: r.y + SERVICE_TOP_BAR_H,
        width: r.width - ICON_LANE_W - SERVICE_PAD * 2,
        height: r.height - SERVICE_TOP_BAR_H,
        anchor: "start"
      };
    case "E":
      return {
        x: r.x + SERVICE_PAD,
        y: r.y + SERVICE_TOP_BAR_H,
        width: r.width - ICON_LANE_W - SERVICE_PAD * 2,
        height: r.height - SERVICE_TOP_BAR_H,
        anchor: "start"
      };
    case "S":
    case "SE":
    case "SW":
      return {
        x: r.x + SERVICE_PAD,
        y: r.y + SERVICE_TOP_BAR_H,
        width: r.width - SERVICE_PAD * 2,
        height: r.height - SERVICE_TOP_BAR_H - ICON_BAND_H,
        anchor: "middle"
      };
    case "C": {
      const top = r.y + SERVICE_TOP_BAR_H + ICON_SIZE + CENTER_ICON_GAP;
      return {
        x: r.x + SERVICE_PAD,
        y: top,
        width: r.width - SERVICE_PAD * 2,
        height: r.y + r.height - SERVICE_PAD - top,
        anchor: "middle"
      };
    }
    case "N":
    case "NE":
    case "NW":
    default: {
      const top = r.y + SERVICE_TOP_BAR_H + ICON_BAND_H;
      return {
        x: r.x + SERVICE_PAD,
        y: top,
        width: r.width - SERVICE_PAD * 2,
        height: r.y + r.height - top,
        anchor: "middle"
      };
    }
  }
}
var GROUP_HEADER_ICON_X = 18;
var GROUP_HEADER_LABEL_GAP_X = 34;
var GROUP_HEADER_CENTER_Y = 16;
function isRightGroupIconAlign(align) {
  return align === "E" || align === "NE" || align === "SE";
}
function groupHeaderPlacement(r, align, hasIcon) {
  const y = r.y + GROUP_HEADER_CENTER_Y;
  if (hasIcon && isRightGroupIconAlign(align)) {
    return {
      icon: { x: r.x + r.width - GROUP_HEADER_ICON_X, y },
      label: { x: r.x + r.width - GROUP_HEADER_LABEL_GAP_X, y, anchor: "end" }
    };
  }
  return {
    icon: { x: r.x + GROUP_HEADER_ICON_X, y },
    label: { x: r.x + (hasIcon ? GROUP_HEADER_LABEL_GAP_X : 12), y, anchor: "start" }
  };
}
function groupsByDepth(groups) {
  const result = [];
  const added = /* @__PURE__ */ new Set();
  function add(g) {
    if (added.has(g.id)) return;
    if (g.parent) {
      const par = groups.find((pg) => pg.id === g.parent);
      if (par) add(par);
    }
    result.push(g);
    added.add(g.id);
  }
  for (const g of groups) add(g);
  return result;
}
function layoutArchitecture(ir, theme, options) {
  const { palette, typography, spacing } = theme;
  const p = pen(theme);
  const margin = spacing.diagramMargin;
  const font = typography.baseFontSize;
  const smallFont = typography.smallFontSize;
  const iconPacks = options?.icons;
  const jctW = 16, jctH = 16;
  const nodeSizes = /* @__PURE__ */ new Map();
  const serviceInfos = /* @__PURE__ */ new Map();
  for (const s2 of ir.services) {
    const sz = serviceSize(s2.label, s2.iconAlign, font, smallFont);
    nodeSizes.set(s2.id, sz);
    serviceInfos.set(s2.id, sz.info);
  }
  for (const j of ir.junctions) nodeSizes.set(j.id, { width: jctW, height: jctH });
  const colGap = 90, rowGap = 64;
  const gridCells = groupAwareDirectionalGridPlacer(ir);
  const colWidths = /* @__PURE__ */ new Map();
  const rowHeights = /* @__PURE__ */ new Map();
  for (const [id, cell] of gridCells) {
    const sz = nodeSizes.get(id);
    if (!sz) continue;
    colWidths.set(cell.col, Math.max(colWidths.get(cell.col) ?? MIN_SERVICE_W, sz.width));
    rowHeights.set(cell.row, Math.max(rowHeights.get(cell.row) ?? MIN_SERVICE_H, sz.height));
  }
  const cells2 = [...gridCells.values()];
  const maxCol = Math.max(0, ...cells2.map((c) => c.col));
  const maxRow = Math.max(0, ...cells2.map((c) => c.row));
  const colX = /* @__PURE__ */ new Map();
  const rowY = /* @__PURE__ */ new Map();
  let xCursor = margin;
  for (let c = 0; c <= maxCol; c++) {
    colX.set(c, xCursor);
    xCursor += (colWidths.get(c) ?? MIN_SERVICE_W) + colGap;
  }
  let yCursor = margin;
  for (let r = 0; r <= maxRow; r++) {
    rowY.set(r, yCursor);
    yCursor += (rowHeights.get(r) ?? MIN_SERVICE_H) + rowGap;
  }
  const positions = /* @__PURE__ */ new Map();
  for (const [id, cell] of gridCells) {
    positions.set(id, {
      x: colX.get(cell.col) ?? margin,
      y: rowY.get(cell.row) ?? margin
    });
  }
  const titleH = ir.metadata.title ? typography.titleFontSize + 14 : 0;
  const yOff = titleH;
  const rectOf = (id) => {
    const pos = positions.get(id);
    const sz = nodeSizes.get(id);
    const cell = gridCells.get(id);
    if (!pos || !sz) return void 0;
    const isJct = ir.junctions.some((j) => j.id === id);
    const width = isJct ? sz.width : cell ? colWidths.get(cell.col) ?? sz.width : sz.width;
    return { x: pos.x, y: pos.y + yOff, width, height: sz.height };
  };
  const groupRectCache = /* @__PURE__ */ new Map();
  const computeGroupRect = (gId) => {
    if (groupRectCache.has(gId)) return groupRectCache.get(gId);
    const pad = 20;
    const memberRects = [
      ...ir.services.filter((s2) => s2.group === gId).map((s2) => rectOf(s2.id)).filter((r) => !!r),
      ...ir.junctions.filter((j) => j.group === gId).map((j) => rectOf(j.id)).filter((r) => !!r),
      ...ir.groups.filter((g) => g.parent === gId).map((g) => computeGroupRect(g.id)).filter((r) => !!r)
    ];
    if (memberRects.length === 0) return void 0;
    const minX = Math.min(...memberRects.map((r) => r.x)) - pad;
    const minY = Math.min(...memberRects.map((r) => r.y)) - pad - 14;
    const maxX = Math.max(...memberRects.map((r) => r.x + r.width)) + pad;
    const maxY = Math.max(...memberRects.map((r) => r.y + r.height)) + pad;
    const rect = { x: minX, y: minY, width: maxX - minX, height: maxY - minY };
    groupRectCache.set(gId, rect);
    return rect;
  };
  for (const g of ir.groups) computeGroupRect(g.id);
  const elements = [];
  const warnedIcons = /* @__PURE__ */ new Set();
  if (ir.metadata.title) {
    elements.push(
      p.text(
        String(ir.metadata.title),
        margin,
        margin + typography.titleFontSize,
        typography.titleFontSize,
        palette.text,
        { weight: "bold" }
      )
    );
  }
  const isMono = theme.name === "bw-light" || theme.name === "bw-dark" || theme.name === "minimal";
  const orderedGroups = groupsByDepth(ir.groups);
  orderedGroups.forEach((g, gi) => {
    const r = computeGroupRect(g.id);
    if (!r) return;
    const hue = categoricalHue(gi, theme);
    const groupStroke = isMono ? palette.border : hue;
    const groupFill = isMono ? palette.surface : hue + "14";
    const groupLabelColor = isMono ? palette.text : hue;
    elements.push(
      p.rect(
        { x: rhu(r.x), y: rhu(r.y), width: rhu(r.width), height: rhu(r.height) },
        groupFill,
        groupStroke,
        1.4,
        { rx: 10 }
      )
    );
    const header = groupHeaderPlacement(r, g.iconAlign, !!g.iconAlign);
    if (g.iconAlign) {
      elements.push(
        ...resolveIconElems(
          p,
          g.icon,
          header.icon.x,
          header.icon.y,
          isMono ? palette.textMuted : hue,
          palette,
          iconPacks,
          warnedIcons
        )
      );
    }
    elements.push(
      p.text(g.label, rhu(header.label.x), rhu(header.label.y), typography.smallFontSize, groupLabelColor, {
        weight: "bold",
        anchor: header.label.anchor
      })
    );
  });
  const allBoxes = [];
  for (const [id, pos] of positions) {
    const sz = nodeSizes.get(id);
    if (sz) allBoxes.push({ id, x: pos.x, y: pos.y, width: sz.width, height: sz.height });
  }
  const endpointRect = (edge, isSource) => {
    const id = isSource ? edge.from : edge.to;
    let r = rectOf(id);
    if (!r) return void 0;
    const useGroup = isSource ? edge.fromGroup : edge.toGroup;
    if (useGroup) {
      const svc = ir.services.find((s2) => s2.id === id);
      if (svc?.group) {
        const gr = computeGroupRect(svc.group);
        if (gr) r = gr;
      }
    }
    return r;
  };
  const endpointGroups = /* @__PURE__ */ new Map();
  const endpointSlots = /* @__PURE__ */ new Map();
  ir.edges.forEach((e, edgeIndex) => {
    const fromRect = endpointRect(e, true);
    const toRect2 = endpointRect(e, false);
    if (!fromRect || !toRect2) return;
    const fromSide = endpointSide(e, true);
    const toSide = endpointSide(e, false);
    const fromCenter = rectCenter2(fromRect);
    const toCenter = rectCenter2(toRect2);
    const sourceKey = `${e.from}:${fromSide.toUpperCase()}`;
    const targetKey = `${e.to}:${toSide.toUpperCase()}`;
    if (!endpointGroups.has(sourceKey)) endpointGroups.set(sourceKey, []);
    if (!endpointGroups.has(targetKey)) endpointGroups.set(targetKey, []);
    endpointGroups.get(sourceKey).push({ edgeIndex, isSource: true, tangentKey: tangentKeyForSide(fromSide, toCenter) });
    endpointGroups.get(targetKey).push({ edgeIndex, isSource: false, tangentKey: tangentKeyForSide(toSide, fromCenter) });
  });
  for (const members of endpointGroups.values()) {
    members.sort(
      (a, b) => a.tangentKey - b.tangentKey || a.edgeIndex - b.edgeIndex || Number(a.isSource) - Number(b.isSource)
    );
    const n = members.length;
    for (let k = 0; k < n; k++) {
      const member = members[k];
      const t = (k + 1) / (n + 1);
      const slot = endpointSlots.get(member.edgeIndex) ?? {};
      endpointSlots.set(
        member.edgeIndex,
        member.isSource ? { ...slot, sourceT: t } : { ...slot, targetT: t }
      );
    }
  }
  ir.edges.forEach((e, edgeIndex) => {
    const fromRect = endpointRect(e, true);
    const toRect2 = endpointRect(e, false);
    if (!fromRect || !toRect2) return;
    const fromSide = endpointSide(e, true).toUpperCase();
    const toSide = endpointSide(e, false).toUpperCase();
    const fromCenter = rectCenter2(fromRect);
    const toCenter = rectCenter2(toRect2);
    const isVerticalStraight = (fromSide === "B" && toSide === "T" || fromSide === "T" && toSide === "B") && Math.abs(fromCenter.x - toCenter.x) < 4;
    const isHorizontalStraight = (fromSide === "R" && toSide === "L" || fromSide === "L" && toSide === "R") && Math.abs(fromCenter.y - toCenter.y) < 4;
    if (isVerticalStraight || isHorizontalStraight) {
      const sourceKey = `${e.from}:${fromSide}`;
      const targetKey = `${e.to}:${toSide}`;
      const sourceCount = endpointGroups.get(sourceKey)?.length ?? 1;
      const targetCount = endpointGroups.get(targetKey)?.length ?? 1;
      const slot = endpointSlots.get(edgeIndex);
      if (sourceCount > 1 && targetCount === 1 && slot?.sourceT !== void 0) {
        endpointSlots.set(edgeIndex, { ...slot, targetT: slot.sourceT });
      } else if (targetCount > 1 && sourceCount === 1 && slot?.targetT !== void 0) {
        endpointSlots.set(edgeIndex, { ...slot, sourceT: slot.targetT });
      }
    }
  });
  for (let edgeIndex = 0; edgeIndex < ir.edges.length; edgeIndex++) {
    const e = ir.edges[edgeIndex];
    let fromRect = endpointRect(e, true);
    if (!fromRect) continue;
    let toRect2 = endpointRect(e, false);
    if (!toRect2) continue;
    const fromDir = e.exitWall ?? sideToDir(e.fromSide);
    const toDir = e.entryWall ?? sideToDir(e.toSide);
    const slots = endpointSlots.get(edgeIndex);
    const pa = port(fromRect, endpointSide(e, true), slots?.sourceT);
    const pb = port(toRect2, endpointSide(e, false), slots?.targetT);
    const obstacles = allBoxes.filter((bx) => bx.id !== e.from && bx.id !== e.to).map((bx) => ({ x: bx.x, y: bx.y + yOff, width: bx.width, height: bx.height }));
    const routing = e.routing ?? "orthogonal";
    const route = createRouter(routing).route({
      from: pa,
      to: pb,
      style: routing,
      obstacles,
      padding: 10,
      fromDir,
      toDir
    });
    const routedPoints = routing === "orthogonal" && isHorizontalDir(fromDir) !== isHorizontalDir(toDir) ? mixedOrthogonalRoutePoints(pa, pb, fromDir, toDir, obstacles, 10) : route.points;
    const routePoints = cleanRoutePoints(routedPoints);
    const style = e.style ?? "solid";
    const pathOpts = {};
    if (e.endMarker === "arrow" || !e.endMarker && e.arrowRight)
      pathOpts.markerEnd = ARROW_END_ID2;
    if (e.startMarker === "arrow" || !e.startMarker && e.arrowLeft)
      pathOpts.markerStart = ARROW_START_ID2;
    const dash = edgeDash2(style);
    if (dash) pathOpts.dash = dash;
    if (e.animation && e.animation !== "none") pathOpts.animated = e.animation;
    const path = style === "wavy" ? wavifyPath(routePoints, 3, 12) : routing === "bezier" ? route.path : routePointsToPath(routePoints, route.path);
    elements.push(p.path(path, palette.primary, edgeStrokeWidth2(style, 1.6), pathOpts));
  }
  ir.services.forEach((s2, i) => {
    const r = rectOf(s2.id);
    if (!r) return;
    const hue = categoricalHue(i, theme);
    elements.push(
      p.rect(
        { x: rhu(r.x), y: rhu(r.y), width: rhu(r.width), height: rhu(r.height) },
        palette.surface,
        palette.border,
        1.4,
        { rx: 8 }
      )
    );
    elements.push(
      p.rect({ x: rhu(r.x), y: rhu(r.y), width: rhu(r.width), height: 8 }, hue, hue, 0, { rx: 4 })
    );
    const iconPos = iconCenter(r, s2.iconAlign);
    const iconElems = resolveIconElems(
      p,
      s2.icon,
      iconPos.x,
      iconPos.y,
      isMono ? palette.text : hue,
      palette,
      iconPacks,
      warnedIcons
    );
    elements.push(...iconElems);
    const area = serviceLabelArea(r, s2.iconAlign);
    const info = serviceInfos.get(s2.id);
    if (info) {
      elements.push(
        ...renderFormattedText(
          p,
          info,
          area.x,
          area.y,
          area.width,
          area.height,
          font,
          smallFont,
          palette.text,
          palette.textMuted,
          { align: area.anchor, defaultBold: true }
        )
      );
    }
  });
  ir.junctions.forEach((j) => {
    const r = rectOf(j.id);
    if (!r) return;
    const cx = rhu(r.x + r.width / 2);
    const cy = rhu(r.y + r.height / 2);
    const R = 4;
    elements.push(p.circle({ x: cx, y: cy }, R, palette.primary, palette.primary, 0));
    elements.push(
      p.path(
        `M ${rhu(cx - R)} ${cy} L ${rhu(cx + R)} ${cy} M ${cx} ${rhu(cy - R)} L ${cx} ${rhu(cy + R)}`,
        palette.primary,
        1.6
      )
    );
  });
  const allNodeRects = [
    ...ir.services.map((s2) => rectOf(s2.id)).filter((r) => !!r),
    ...ir.junctions.map((j) => rectOf(j.id)).filter((r) => !!r)
  ];
  const allGroupRects = [...groupRectCache.values()];
  const allRects = [...allNodeRects, ...allGroupRects];
  if (allRects.length === 0) {
    const scene2 = applyOverlays(
      {
        viewBox: { x: 0, y: 0, width: 200, height: 100 },
        background: palette.background,
        elements,
        defs: []
      },
      ir.overlays,
      theme
    );
    return { scene: scene2, anchors: {} };
  }
  const totalW = rhuInt(Math.max(...allRects.map((r) => r.x + r.width)) + margin + 26);
  const totalH = rhuInt(Math.max(...allRects.map((r) => r.y + r.height)) + margin + 26);
  const vbX = rhuInt(Math.min(0, ...allRects.map((r) => r.x)) - margin);
  const vbY = rhuInt(Math.min(0, ...allRects.map((r) => r.y)) - margin);
  const s = theme.edges?.arrowSize ?? 8;
  const sW = s * 2;
  const sH = rhu(sW * 0.8125);
  const sMidY = rhu(sH / 2);
  const sRefX = rhu(sW * 0.9);
  const sRefXStart = rhu(sW * 0.1);
  const defs = [
    `<marker id="${ARROW_END_ID2}" markerUnits="userSpaceOnUse" markerWidth="${sW}" markerHeight="${sH}" refX="${sRefX}" refY="${sMidY}" orient="auto"><polygon points="0 0, ${sW} ${sMidY}, 0 ${sH}" fill="${palette.primary}" /></marker>`,
    `<marker id="${ARROW_START_ID2}" markerUnits="userSpaceOnUse" markerWidth="${sW}" markerHeight="${sH}" refX="${sRefXStart}" refY="${sMidY}" orient="auto-start-reverse"><polygon points="0 0, ${sW} ${sMidY}, 0 ${sH}" fill="${palette.primary}" /></marker>`
  ];
  const scene = applyOverlays(
    {
      viewBox: { x: vbX, y: vbY, width: totalW - vbX, height: totalH - vbY },
      background: palette.background,
      elements,
      defs
    },
    ir.overlays,
    theme
  );
  return { scene, anchors: {} };
}
function cleanRoutePoints(points) {
  const out = [];
  for (const p of points) {
    const prev = out[out.length - 1];
    if (!prev || prev.x !== p.x || prev.y !== p.y) out.push(p);
  }
  return out;
}
function routePointsToPath(points, fallback) {
  if (points.length === 0) return fallback;
  if (points.length === 1) return `M ${points[0].x} ${points[0].y}`;
  return points.map((p, i) => `${i === 0 ? "M" : "L"} ${p.x} ${p.y}`).join(" ");
}
function isHorizontalDir(dir) {
  return dir === "E" || dir === "W";
}
function mixedOrthogonalRoutePoints(from, to, fromDir, toDir, obstacles, pad) {
  const directCorner = isHorizontalDir(fromDir) ? { x: to.x, y: from.y } : { x: from.x, y: to.y };
  const simpleL = [from, directCorner, to];
  if (routeCollisionCount(simpleL, obstacles) === 0) {
    return simpleL;
  }
  const stubLen = Math.max(pad, 20);
  const stub1 = offsetPoint(from, fromDir, stubLen);
  const stub2 = offsetPoint(to, toDir, stubLen);
  if (isHorizontalDir(fromDir)) {
    let bendX2 = (from.x + to.x) / 2;
    const blockingObstacles2 = obstacles.filter(
      (o) => (fromDir === "E" && o.x > from.x && o.x < to.x || fromDir === "W" && o.x + o.width < from.x && o.x + o.width > to.x) && from.y >= o.y - pad && from.y <= o.y + o.height + pad
    );
    if (blockingObstacles2.length > 0) {
      if (fromDir === "E") {
        const nearest = Math.min(...blockingObstacles2.map((o) => o.x));
        bendX2 = (from.x + nearest) / 2;
      } else {
        const nearest = Math.max(...blockingObstacles2.map((o) => o.x + o.width));
        bendX2 = (from.x + nearest) / 2;
      }
    }
    const candidates2 = [
      (from.y + to.y) / 2,
      (stub1.y + stub2.y) / 2,
      ...obstacles.flatMap((o) => [
        o.y - pad,
        o.y + o.height + pad,
        (o.y + to.y) / 2,
        (o.y + o.height + to.y) / 2
      ])
    ];
    const bendY2 = bestBend(
      candidates2,
      (y) => [from, { x: bendX2, y: from.y }, { x: bendX2, y }, { x: to.x, y }, to],
      obstacles
    );
    const candidate2 = [
      from,
      { x: bendX2, y: from.y },
      { x: bendX2, y: bendY2 },
      { x: to.x, y: bendY2 },
      to
    ];
    return clearMixedRoute(candidate2, from, to, stub1, stub2, obstacles, pad);
  }
  let bendY = (from.y + to.y) / 2;
  const blockingObstacles = obstacles.filter(
    (o) => (fromDir === "S" && o.y > from.y && o.y < to.y || fromDir === "N" && o.y + o.height < from.y && o.y + o.height > to.y) && from.x >= o.x - pad && from.x <= o.x + o.width + pad
  );
  if (blockingObstacles.length > 0) {
    if (fromDir === "S") {
      const nearest = Math.min(...blockingObstacles.map((o) => o.y));
      bendY = (from.y + nearest) / 2;
    } else {
      const nearest = Math.max(...blockingObstacles.map((o) => o.y + o.height));
      bendY = (from.y + nearest) / 2;
    }
  }
  const candidates = [
    (from.x + to.x) / 2,
    (stub1.x + stub2.x) / 2,
    ...obstacles.flatMap((o) => [
      o.x - pad,
      o.x + o.width + pad,
      (o.x + to.x) / 2,
      (o.x + o.width + to.x) / 2
    ])
  ];
  const bendX = bestBend(
    candidates,
    (x) => [from, { x: from.x, y: bendY }, { x, y: bendY }, { x, y: to.y }, to],
    obstacles
  );
  const candidate = [
    from,
    { x: from.x, y: bendY },
    { x: bendX, y: bendY },
    { x: bendX, y: to.y },
    to
  ];
  return clearMixedRoute(candidate, from, to, stub1, stub2, obstacles, pad);
}
function clearMixedRoute(candidate, from, to, stub1, stub2, obstacles, pad) {
  if (routeCollisionCount(candidate, obstacles) === 0) return candidate;
  const middle = createRouter("orthogonal").route({
    from: stub1,
    to: stub2,
    style: "orthogonal",
    obstacles,
    padding: pad
  }).points;
  const detour = [from, ...middle, to];
  return routeCollisionCount(detour, obstacles) < routeCollisionCount(candidate, obstacles) ? detour : candidate;
}
function offsetPoint(p, dir, amount) {
  switch (dir) {
    case "W":
      return { x: p.x - amount, y: p.y };
    case "E":
      return { x: p.x + amount, y: p.y };
    case "N":
      return { x: p.x, y: p.y - amount };
    case "S":
      return { x: p.x, y: p.y + amount };
  }
}
function bestBend(candidates, build, obstacles) {
  let best = candidates[0] ?? 0;
  let bestHits = Infinity;
  let bestLen = Infinity;
  for (const c of [...new Set(candidates)]) {
    const points = build(c);
    const hits = routeCollisionCount(points, obstacles);
    const len = routeLength(points);
    if (hits < bestHits || hits === bestHits && len < bestLen) {
      best = c;
      bestHits = hits;
      bestLen = len;
    }
  }
  return best;
}
function routeCollisionCount(points, obstacles) {
  let count = 0;
  for (let i = 0; i < points.length - 1; i++) {
    for (const obs of obstacles) {
      if (segmentCrossesRectInterior(points[i], points[i + 1], obs)) count++;
    }
  }
  return count;
}
function routeLength(points) {
  let len = 0;
  for (let i = 0; i < points.length - 1; i++) {
    len += Math.abs(points[i + 1].x - points[i].x) + Math.abs(points[i + 1].y - points[i].y);
  }
  return len;
}
function segmentCrossesRectInterior(a, b, r) {
  const xMin = r.x, xMax = r.x + r.width, yMin = r.y, yMax = r.y + r.height;
  if (a.x === b.x) {
    if (a.x <= xMin || a.x >= xMax) return false;
    return Math.max(a.y, b.y) > yMin && Math.min(a.y, b.y) < yMax;
  }
  if (a.y === b.y) {
    if (a.y <= yMin || a.y >= yMax) return false;
    return Math.max(a.x, b.x) > xMin && Math.min(a.x, b.x) < xMax;
  }
  return false;
}
function resolveIconElems(p, icon, cx, cy, hue, palette, iconPacks, warnedIcons) {
  if (icon.includes(":") && iconPacks) {
    const ref = parseIconRef(icon);
    if (ref.ok) {
      const resolved = resolveIcon(ref.value, iconPacks);
      if (resolved.ok) {
        const size = 24;
        return [
          p.icon(resolved.value, rhu(cx - size / 2), rhu(cy - size / 2), size, { color: hue })
        ];
      }
    }
    if (!warnedIcons.has(icon)) {
      warnedIcons.add(icon);
      console.warn(`[architecture] icon "${icon}" could not be resolved \u2014 falling back to glyph.`);
    }
  } else if (icon.includes(":") && !iconPacks) {
    if (!warnedIcons.has(icon)) {
      warnedIcons.add(icon);
      console.warn(
        `[architecture] icon "${icon}" is an iconify token but no icon packs were loaded.`
      );
    }
  }
  return iconGlyph(p, icon, cx, cy, hue, palette);
}
function iconGlyph(p, icon, cx, cy, hue, palette) {
  const name = icon.toLowerCase();
  const out = [];
  const stroke = hue;
  if (name.includes("server") || name.includes("compute")) {
    for (let i = 0; i < 3; i++)
      out.push(
        p.rect(
          { x: rhu(cx - 10), y: rhu(cy - 9 + i * 7), width: 20, height: 5 },
          palette.surface,
          stroke,
          1.3,
          { rx: 1 }
        )
      );
  } else if (name.includes("database") || name.includes("db") || name.includes("datastore") || name.includes("storage") || name.includes("disk")) {
    out.push(
      p.rect(
        { x: rhu(cx - 9), y: rhu(cy - 9), width: 18, height: 18 },
        palette.surface,
        stroke,
        1.3,
        { rx: 5 }
      )
    );
    out.push(
      p.path(
        `M ${rhu(cx - 9)} ${rhu(cy - 3)} Q ${rhu(cx)} ${rhu(cy + 1)}, ${rhu(cx + 9)} ${rhu(cy - 3)}`,
        stroke,
        1.3
      )
    );
  } else if (name.includes("cloud")) {
    out.push(p.circle({ x: rhu(cx - 6), y: rhu(cy + 2) }, 6, palette.surface, stroke, 1.3));
    out.push(p.circle({ x: rhu(cx + 6), y: rhu(cy + 2) }, 6, palette.surface, stroke, 1.3));
    out.push(p.circle({ x: rhu(cx), y: rhu(cy - 3) }, 7, palette.surface, stroke, 1.3));
    out.push(
      p.rect(
        { x: rhu(cx - 10), y: rhu(cy + 2), width: 20, height: 6 },
        palette.surface,
        palette.surface,
        0
      )
    );
  } else if (name.includes("internet") || name.includes("globe") || name.includes("web") || name.includes("client")) {
    out.push(p.circle({ x: rhu(cx), y: rhu(cy) }, 9, palette.surface, stroke, 1.3));
    out.push(
      p.path(
        `M ${rhu(cx)} ${rhu(cy - 9)} L ${rhu(cx)} ${rhu(cy + 9)} M ${rhu(cx - 9)} ${rhu(cy)} L ${rhu(cx + 9)} ${rhu(cy)}`,
        stroke,
        1
      )
    );
    out.push(
      p.path(
        `M ${rhu(cx - 9)} ${rhu(cy)} Q ${rhu(cx)} ${rhu(cy - 11)}, ${rhu(cx + 9)} ${rhu(cy)} Q ${rhu(cx)} ${rhu(cy + 11)}, ${rhu(cx - 9)} ${rhu(cy)}`,
        stroke,
        1
      )
    );
  } else {
    out.push(
      p.rect(
        { x: rhu(cx - 9), y: rhu(cy - 9), width: 18, height: 18 },
        palette.surface,
        stroke,
        1.3,
        { rx: 4 }
      )
    );
  }
  return out;
}
var ANIMATION_SET2 = /* @__PURE__ */ new Set([...CONNECTOR_ANIMATIONS, "none"]);
var ROUTE_STYLE_SET = /* @__PURE__ */ new Set(["straight", "orthogonal", "bezier", "polyline"]);
var ICON_ALIGN_SET = /* @__PURE__ */ new Set(["N", "S", "E", "W", "NE", "NW", "SE", "SW", "C"]);
var CONNECTOR_TOKEN_MAP2 = {
  "<-->": { direction: "bidirectional", style: "solid", startMarker: "arrow", endMarker: "arrow" },
  "<-.->": { direction: "bidirectional", style: "dotted", startMarker: "arrow", endMarker: "arrow" },
  "<==>": { direction: "bidirectional", style: "thick", startMarker: "arrow", endMarker: "arrow" },
  "<-_->": { direction: "bidirectional", style: "dashed", startMarker: "arrow", endMarker: "arrow" },
  "<-~->": { direction: "bidirectional", style: "wavy", startMarker: "arrow", endMarker: "arrow" },
  "-->": { direction: "directed", style: "solid", startMarker: "none", endMarker: "arrow" },
  "-.->": { direction: "directed", style: "dotted", startMarker: "none", endMarker: "arrow" },
  "==>": { direction: "directed", style: "thick", startMarker: "none", endMarker: "arrow" },
  "-_->": { direction: "directed", style: "dashed", startMarker: "none", endMarker: "arrow" },
  "-~->": { direction: "directed", style: "wavy", startMarker: "none", endMarker: "arrow" },
  "---": { direction: "undirected", style: "solid", startMarker: "none", endMarker: "none" },
  "-.-": { direction: "undirected", style: "dotted", startMarker: "none", endMarker: "none" },
  "===": { direction: "undirected", style: "thick", startMarker: "none", endMarker: "none" },
  "-_-": { direction: "undirected", style: "dashed", startMarker: "none", endMarker: "none" },
  "-~-": { direction: "undirected", style: "wavy", startMarker: "none", endMarker: "none" }
};
function edgeFromConnectorToken(token) {
  const desc = CONNECTOR_TOKEN_MAP2[token];
  return {
    left: desc.startMarker === "arrow",
    right: desc.endMarker === "arrow",
    style: desc.style,
    startMarker: desc.startMarker,
    endMarker: desc.endMarker
  };
}
function assertAnim2(value) {
  if (!ANIMATION_SET2.has(value)) {
    throw new Error(`Invalid connector animation "${value}"`);
  }
  return value;
}
function assertRouteStyle(value) {
  if (!ROUTE_STYLE_SET.has(value)) {
    throw new Error(`Invalid connector route style "${value}"`);
  }
  return value;
}
function assertIconAlign(value) {
  const upper = String(value).toUpperCase();
  if (!ICON_ALIGN_SET.has(upper)) {
    throw new Error(`Invalid icon alignment "${value}"`);
  }
  return upper;
}
function peg$subclass21(child, parent) {
  function C() {
    this.constructor = child;
  }
  C.prototype = parent.prototype;
  child.prototype = new C();
}
function peg$SyntaxError21(message, expected, found, location) {
  var self = Error.call(this, message);
  if (Object.setPrototypeOf) {
    Object.setPrototypeOf(self, peg$SyntaxError21.prototype);
  }
  self.expected = expected;
  self.found = found;
  self.location = location;
  self.name = "SyntaxError";
  return self;
}
peg$subclass21(peg$SyntaxError21, Error);
function peg$padEnd21(str, targetLength, padString) {
  padString = padString || " ";
  if (str.length > targetLength) {
    return str;
  }
  targetLength -= str.length;
  padString += padString.repeat(targetLength);
  return str + padString.slice(0, targetLength);
}
peg$SyntaxError21.prototype.format = function(sources) {
  var str = "Error: " + this.message;
  if (this.location) {
    var src = null;
    var k;
    for (k = 0; k < sources.length; k++) {
      if (sources[k].source === this.location.source) {
        src = sources[k].text.split(/\r\n|\n|\r/g);
        break;
      }
    }
    var s = this.location.start;
    var offset_s = this.location.source && typeof this.location.source.offset === "function" ? this.location.source.offset(s) : s;
    var loc = this.location.source + ":" + offset_s.line + ":" + offset_s.column;
    if (src) {
      var e = this.location.end;
      var filler = peg$padEnd21("", offset_s.line.toString().length, " ");
      var line = src[s.line - 1];
      var last = s.line === e.line ? e.column : line.length + 1;
      var hatLen = last - s.column || 1;
      str += "\n --> " + loc + "\n" + filler + " |\n" + offset_s.line + " | " + line + "\n" + filler + " | " + peg$padEnd21("", s.column - 1, " ") + peg$padEnd21("", hatLen, "^");
    } else {
      str += "\n at " + loc;
    }
  }
  return str;
};
peg$SyntaxError21.buildMessage = function(expected, found) {
  var DESCRIBE_EXPECTATION_FNS = {
    literal: function(expectation) {
      return '"' + literalEscape(expectation.text) + '"';
    },
    class: function(expectation) {
      var escapedParts = expectation.parts.map(function(part) {
        return Array.isArray(part) ? classEscape(part[0]) + "-" + classEscape(part[1]) : classEscape(part);
      });
      return "[" + (expectation.inverted ? "^" : "") + escapedParts.join("") + "]";
    },
    any: function() {
      return "any character";
    },
    end: function() {
      return "end of input";
    },
    other: function(expectation) {
      return expectation.description;
    }
  };
  function hex(ch) {
    return ch.charCodeAt(0).toString(16).toUpperCase();
  }
  function literalEscape(s) {
    return s.replace(/\\/g, "\\\\").replace(/"/g, '\\"').replace(/\0/g, "\\0").replace(/\t/g, "\\t").replace(/\n/g, "\\n").replace(/\r/g, "\\r").replace(/[\x00-\x0F]/g, function(ch) {
      return "\\x0" + hex(ch);
    }).replace(/[\x10-\x1F\x7F-\x9F]/g, function(ch) {
      return "\\x" + hex(ch);
    });
  }
  function classEscape(s) {
    return s.replace(/\\/g, "\\\\").replace(/\]/g, "\\]").replace(/\^/g, "\\^").replace(/-/g, "\\-").replace(/\0/g, "\\0").replace(/\t/g, "\\t").replace(/\n/g, "\\n").replace(/\r/g, "\\r").replace(/[\x00-\x0F]/g, function(ch) {
      return "\\x0" + hex(ch);
    }).replace(/[\x10-\x1F\x7F-\x9F]/g, function(ch) {
      return "\\x" + hex(ch);
    });
  }
  function describeExpectation(expectation) {
    return DESCRIBE_EXPECTATION_FNS[expectation.type](expectation);
  }
  function describeExpected(expected2) {
    var descriptions = expected2.map(describeExpectation);
    var i, j;
    descriptions.sort();
    if (descriptions.length > 0) {
      for (i = 1, j = 1; i < descriptions.length; i++) {
        if (descriptions[i - 1] !== descriptions[i]) {
          descriptions[j] = descriptions[i];
          j++;
        }
      }
      descriptions.length = j;
    }
    switch (descriptions.length) {
      case 1:
        return descriptions[0];
      case 2:
        return descriptions[0] + " or " + descriptions[1];
      default:
        return descriptions.slice(0, -1).join(", ") + ", or " + descriptions[descriptions.length - 1];
    }
  }
  function describeFound(found2) {
    return found2 ? '"' + literalEscape(found2) + '"' : "end of input";
  }
  return "Expected " + describeExpected(expected) + " but " + describeFound(found) + " found.";
};
function peg$parse21(input, options) {
  options = options !== void 0 ? options : {};
  var peg$FAILED = {};
  var peg$source = options.grammarSource;
  var peg$startRuleFunctions = { Document: peg$parseDocument };
  var peg$startRuleFunction = peg$parseDocument;
  var peg$c0 = "architecture-beta";
  var peg$c1 = "---";
  var peg$c2 = "group";
  var peg$c3 = "(";
  var peg$c4 = ")";
  var peg$c5 = "[";
  var peg$c6 = "]";
  var peg$c7 = "in";
  var peg$c8 = "service";
  var peg$c9 = "junction";
  var peg$c10 = ":";
  var peg$c11 = "{group}";
  var peg$c12 = "<-~->";
  var peg$c13 = "<-_->";
  var peg$c14 = "<-.->";
  var peg$c15 = "<==>";
  var peg$c16 = "<-->";
  var peg$c17 = "-~->";
  var peg$c18 = "-_->";
  var peg$c19 = "-.->";
  var peg$c20 = "==>";
  var peg$c21 = "-->";
  var peg$c22 = "<--";
  var peg$c23 = "-~-";
  var peg$c24 = "-_-";
  var peg$c25 = "-.-";
  var peg$c26 = "===";
  var peg$c27 = "--";
  var peg$c28 = "@";
  var peg$c29 = "anim:";
  var peg$c30 = "route:";
  var peg$c31 = "orthogonal";
  var peg$c32 = "bezier";
  var peg$c33 = "straight";
  var peg$c34 = "polyline";
  var peg$c35 = "{";
  var peg$c36 = ",";
  var peg$c37 = "}";
  var peg$c38 = "iconalign:";
  var peg$c39 = "align";
  var peg$c40 = "row";
  var peg$c41 = "column";
  var peg$c42 = "\r";
  var peg$c43 = "\n";
  var peg$r0 = /^[^\n]/;
  var peg$r1 = /^[ \t]/;
  var peg$r2 = /^[^)\n]/;
  var peg$r3 = /^[^\]\n]/;
  var peg$r4 = /^[A-Za-z0-9_]/;
  var peg$r5 = /^[ENSW]/;
  var peg$r6 = /^[LRTBlrtb]/;
  var peg$e0 = peg$literalExpectation("architecture-beta", true);
  var peg$e1 = peg$literalExpectation("---", false);
  var peg$e2 = peg$classExpectation(["\n"], true, false);
  var peg$e3 = peg$classExpectation([" ", "	"], false, false);
  var peg$e4 = peg$literalExpectation("group", true);
  var peg$e5 = peg$literalExpectation("(", false);
  var peg$e6 = peg$classExpectation([")", "\n"], true, false);
  var peg$e7 = peg$literalExpectation(")", false);
  var peg$e8 = peg$literalExpectation("[", false);
  var peg$e9 = peg$classExpectation(["]", "\n"], true, false);
  var peg$e10 = peg$literalExpectation("]", false);
  var peg$e11 = peg$literalExpectation("in", true);
  var peg$e12 = peg$literalExpectation("service", true);
  var peg$e13 = peg$literalExpectation("junction", true);
  var peg$e14 = peg$literalExpectation(":", false);
  var peg$e15 = peg$literalExpectation("{group}", false);
  var peg$e16 = peg$literalExpectation("<-~->", false);
  var peg$e17 = peg$literalExpectation("<-_->", false);
  var peg$e18 = peg$literalExpectation("<-.->", false);
  var peg$e19 = peg$literalExpectation("<==>", false);
  var peg$e20 = peg$literalExpectation("<-->", false);
  var peg$e21 = peg$literalExpectation("-~->", false);
  var peg$e22 = peg$literalExpectation("-_->", false);
  var peg$e23 = peg$literalExpectation("-.->", false);
  var peg$e24 = peg$literalExpectation("==>", false);
  var peg$e25 = peg$literalExpectation("-->", false);
  var peg$e26 = peg$literalExpectation("<--", false);
  var peg$e27 = peg$literalExpectation("-~-", false);
  var peg$e28 = peg$literalExpectation("-_-", false);
  var peg$e29 = peg$literalExpectation("-.-", false);
  var peg$e30 = peg$literalExpectation("===", false);
  var peg$e31 = peg$literalExpectation("--", false);
  var peg$e32 = peg$literalExpectation("@", false);
  var peg$e33 = peg$literalExpectation("anim:", false);
  var peg$e34 = peg$literalExpectation("route:", false);
  var peg$e35 = peg$literalExpectation("orthogonal", false);
  var peg$e36 = peg$literalExpectation("bezier", false);
  var peg$e37 = peg$literalExpectation("straight", false);
  var peg$e38 = peg$literalExpectation("polyline", false);
  var peg$e39 = peg$classExpectation([["A", "Z"], ["a", "z"], ["0", "9"], "_"], false, false);
  var peg$e40 = peg$classExpectation(["E", "N", "S", "W"], false, false);
  var peg$e41 = peg$literalExpectation("{", false);
  var peg$e42 = peg$literalExpectation(",", false);
  var peg$e43 = peg$literalExpectation("}", false);
  var peg$e44 = peg$literalExpectation("iconalign:", false);
  var peg$e45 = peg$literalExpectation("align", true);
  var peg$e46 = peg$literalExpectation("row", true);
  var peg$e47 = peg$literalExpectation("column", true);
  var peg$e48 = peg$classExpectation(["L", "R", "T", "B", "l", "r", "t", "b"], false, false);
  var peg$e49 = peg$literalExpectation("\r", false);
  var peg$e50 = peg$literalExpectation("\n", false);
  var peg$e51 = peg$anyExpectation();
  var peg$f0 = function(fm, lines2) {
    const groups = [];
    const services = [];
    const junctions = [];
    const edges2 = [];
    const aligns = [];
    let curGroup, curIndent = -1;
    for (const l of lines2.filter(Boolean)) {
      if (l.t === "group") {
        groups.push({ id: l.id, label: l.label, icon: l.icon, ...l.inGroup ? { parent: l.inGroup } : {}, ...l.iconAlign !== void 0 ? { iconAlign: l.iconAlign } : {} });
        curGroup = l.id;
        curIndent = l.indent;
      } else if (l.t === "service") {
        const group = l.inGroup || (l.indent > curIndent && curGroup ? curGroup : void 0);
        services.push({ id: l.id, label: l.label, icon: l.icon, ...group ? { group } : {}, ...l.iconAlign !== void 0 ? { iconAlign: l.iconAlign } : {} });
      } else if (l.t === "junction") {
        const group = l.inGroup || (l.indent > curIndent && curGroup ? curGroup : void 0);
        junctions.push({ id: l.id, ...group ? { group } : {} });
      } else if (l.t === "edge") {
        edges2.push({ from: l.from, fromSide: l.fromSide, fromGroup: l.fromGroup, to: l.to, toSide: l.toSide, toGroup: l.toGroup, arrowLeft: l.arrowLeft, arrowRight: l.arrowRight, style: l.style, startMarker: l.startMarker, endMarker: l.endMarker, ...l.animation !== void 0 ? { animation: l.animation } : {}, ...l.routing !== void 0 ? { routing: l.routing } : {}, ...l.exitWall !== void 0 ? { exitWall: l.exitWall } : {}, ...l.entryWall !== void 0 ? { entryWall: l.entryWall } : {} });
      } else if (l.t === "align") {
        aligns.push({ axis: l.axis, members: l.members });
      }
    }
    return { version: "1.0", metadata: { ...fm || {} }, groups, services, junctions, edges: edges2, aligns };
  };
  var peg$f1 = function(line) {
    return line;
  };
  var peg$f2 = function(lines2) {
    const meta = {};
    for (const line of lines2) {
      const m = line.match(/^\s*(\w+)\s*:\s*(.+?)\s*$/);
      if (m) meta[m[1]] = m[2];
    }
    return meta;
  };
  var peg$f3 = function(ind, id, icon, label, g) {
    return g;
  };
  var peg$f4 = function(ind, id, icon, label, inG, tail) {
    return { t: "group", indent: ind.length, id, icon: icon.trim(), label: label.trim(), inGroup: inG || void 0, ...tail.iconAlign !== void 0 ? { iconAlign: tail.iconAlign } : {} };
  };
  var peg$f5 = function(ind, id, icon, label, g) {
    return g;
  };
  var peg$f6 = function(ind, id, icon, label, inG, tail) {
    return { t: "service", indent: ind.length, id, icon: icon.trim(), label: label.trim(), inGroup: inG || void 0, ...tail.iconAlign !== void 0 ? { iconAlign: tail.iconAlign } : {} };
  };
  var peg$f7 = function(ind, id, g) {
    return g;
  };
  var peg$f8 = function(ind, id, inG) {
    return { t: "junction", indent: ind.length, id, inGroup: inG || void 0 };
  };
  var peg$f9 = function(fromEp, fromSide, arrow, toSide, toEp, tail) {
    const extra = tail || {};
    return {
      t: "edge",
      from: fromEp.id,
      fromSide,
      fromGroup: fromEp.grp,
      to: toEp.id,
      toSide,
      toGroup: toEp.grp,
      arrowLeft: arrow.left,
      arrowRight: arrow.right,
      style: arrow.style,
      startMarker: arrow.startMarker,
      endMarker: arrow.endMarker,
      ...extra.animation !== void 0 ? { animation: extra.animation } : {},
      ...extra.routing !== void 0 ? { routing: extra.routing } : {},
      ...extra.exitWall !== void 0 ? { exitWall: extra.exitWall } : {},
      ...extra.entryWall !== void 0 ? { entryWall: extra.entryWall } : {}
    };
  };
  var peg$f10 = function(id) {
    return true;
  };
  var peg$f11 = function(id, grp) {
    return { id, grp: grp === true };
  };
  var peg$f12 = function(token) {
    if (token === "<--") return { left: true, right: false, style: "solid", startMarker: "arrow", endMarker: "none" };
    if (token === "--") return { left: false, right: false, style: "solid", startMarker: "none", endMarker: "none" };
    return edgeFromConnectorToken(token);
  };
  var peg$f13 = function(anns, block2, rest) {
    if (/@(?:anim|route|orthogonal|bezier|straight|polyline):/.test(rest)) {
      throw new Error("Invalid connector annotation");
    }
    const result = {};
    if (block2) {
      const b = block2[1];
      if (b.anim !== void 0) result.animation = assertAnim2(b.anim);
      if (b.route !== void 0) result.routing = assertRouteStyle(b.route);
    }
    for (const ann of anns) {
      Object.assign(result, ann[2]);
    }
    return result;
  };
  var peg$f14 = function(value) {
    return { animation: value };
  };
  var peg$f15 = function(value) {
    return { routing: value };
  };
  var peg$f16 = function(style, walls) {
    return { routing: style, ...walls };
  };
  var peg$f17 = function(value) {
    return assertAnim2(value);
  };
  var peg$f18 = function(value) {
    return assertRouteStyle(value);
  };
  var peg$f19 = function() {
    return "orthogonal";
  };
  var peg$f20 = function() {
    return "bezier";
  };
  var peg$f21 = function() {
    return "straight";
  };
  var peg$f22 = function() {
    return "polyline";
  };
  var peg$f23 = function(exit, entry) {
    return { exitWall: exit, entryWall: entry };
  };
  var peg$f24 = function(exit) {
    return { exitWall: exit };
  };
  var peg$f25 = function(w) {
    return w;
  };
  var peg$f26 = function(first, rest) {
    const result = {};
    result[first.key] = first.value;
    for (const r of rest) {
      result[r[3].key] = r[3].value;
    }
    return result;
  };
  var peg$f27 = function(key, value) {
    return { key, value };
  };
  var peg$f28 = function(value) {
    return value;
  };
  var peg$f29 = function(anns, block2, rest) {
    if (/@iconalign:/.test(rest)) {
      throw new Error("Invalid icon alignment annotation");
    }
    const result = {};
    if (block2) {
      const b = block2[1];
      if (b.iconalign !== void 0) result.iconAlign = assertIconAlign(b.iconalign);
    }
    for (const ann of anns) {
      Object.assign(result, ann[2]);
    }
    return result;
  };
  var peg$f30 = function(value) {
    return { iconAlign: value };
  };
  var peg$f31 = function(value) {
    return assertIconAlign(value);
  };
  var peg$f32 = function(axis, head, id) {
    return id;
  };
  var peg$f33 = function(axis, head, tail) {
    return { t: "align", axis: axis.toLowerCase(), members: [head, ...tail] };
  };
  var peg$f34 = function() {
    return null;
  };
  var peg$currPos = options.peg$currPos | 0;
  var peg$savedPos = peg$currPos;
  var peg$posDetailsCache = [{ line: 1, column: 1 }];
  var peg$maxFailPos = peg$currPos;
  var peg$maxFailExpected = options.peg$maxFailExpected || [];
  var peg$silentFails = options.peg$silentFails | 0;
  var peg$result;
  if (options.startRule) {
    if (!(options.startRule in peg$startRuleFunctions)) {
      throw new Error(`Can't start parsing from rule "` + options.startRule + '".');
    }
    peg$startRuleFunction = peg$startRuleFunctions[options.startRule];
  }
  function text() {
    return input.substring(peg$savedPos, peg$currPos);
  }
  function offset() {
    return peg$savedPos;
  }
  function range() {
    return {
      source: peg$source,
      start: peg$savedPos,
      end: peg$currPos
    };
  }
  function location() {
    return peg$computeLocation(peg$savedPos, peg$currPos);
  }
  function expected(description, location2) {
    location2 = location2 !== void 0 ? location2 : peg$computeLocation(peg$savedPos, peg$currPos);
    throw peg$buildStructuredError(
      [peg$otherExpectation(description)],
      input.substring(peg$savedPos, peg$currPos),
      location2
    );
  }
  function error(message, location2) {
    location2 = location2 !== void 0 ? location2 : peg$computeLocation(peg$savedPos, peg$currPos);
    throw peg$buildSimpleError(message, location2);
  }
  function peg$literalExpectation(text2, ignoreCase) {
    return { type: "literal", text: text2, ignoreCase };
  }
  function peg$classExpectation(parts, inverted, ignoreCase) {
    return { type: "class", parts, inverted, ignoreCase };
  }
  function peg$anyExpectation() {
    return { type: "any" };
  }
  function peg$endExpectation() {
    return { type: "end" };
  }
  function peg$otherExpectation(description) {
    return { type: "other", description };
  }
  function peg$computePosDetails(pos) {
    var details = peg$posDetailsCache[pos];
    var p;
    if (details) {
      return details;
    } else {
      if (pos >= peg$posDetailsCache.length) {
        p = peg$posDetailsCache.length - 1;
      } else {
        p = pos;
        while (!peg$posDetailsCache[--p]) {
        }
      }
      details = peg$posDetailsCache[p];
      details = {
        line: details.line,
        column: details.column
      };
      while (p < pos) {
        if (input.charCodeAt(p) === 10) {
          details.line++;
          details.column = 1;
        } else {
          details.column++;
        }
        p++;
      }
      peg$posDetailsCache[pos] = details;
      return details;
    }
  }
  function peg$computeLocation(startPos, endPos, offset2) {
    var startPosDetails = peg$computePosDetails(startPos);
    var endPosDetails = peg$computePosDetails(endPos);
    var res = {
      source: peg$source,
      start: {
        offset: startPos,
        line: startPosDetails.line,
        column: startPosDetails.column
      },
      end: {
        offset: endPos,
        line: endPosDetails.line,
        column: endPosDetails.column
      }
    };
    if (offset2 && peg$source && typeof peg$source.offset === "function") {
      res.start = peg$source.offset(res.start);
      res.end = peg$source.offset(res.end);
    }
    return res;
  }
  function peg$fail(expected2) {
    if (peg$currPos < peg$maxFailPos) {
      return;
    }
    if (peg$currPos > peg$maxFailPos) {
      peg$maxFailPos = peg$currPos;
      peg$maxFailExpected = [];
    }
    peg$maxFailExpected.push(expected2);
  }
  function peg$buildSimpleError(message, location2) {
    return new peg$SyntaxError21(message, null, null, location2);
  }
  function peg$buildStructuredError(expected2, found, location2) {
    return new peg$SyntaxError21(
      peg$SyntaxError21.buildMessage(expected2, found),
      expected2,
      found,
      location2
    );
  }
  function peg$parseDocument() {
    var s0, s1, s2, s3, s4, s5, s6;
    s0 = peg$currPos;
    s1 = peg$parseFrontmatter();
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    s2 = peg$parse_();
    s3 = input.substr(peg$currPos, 17);
    if (s3.toLowerCase() === peg$c0) {
      peg$currPos += 17;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e0);
      }
    }
    if (s3 !== peg$FAILED) {
      s4 = peg$parseLineEnd();
      if (s4 !== peg$FAILED) {
        s5 = [];
        s6 = peg$parseLine();
        while (s6 !== peg$FAILED) {
          s5.push(s6);
          s6 = peg$parseLine();
        }
        s6 = peg$parse_();
        peg$savedPos = s0;
        s0 = peg$f0(s1, s5);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseFrontmatter() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 3) === peg$c1) {
      s1 = peg$c1;
      peg$currPos += 3;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e1);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$parseNL();
      if (s2 !== peg$FAILED) {
        s3 = [];
        s4 = peg$currPos;
        s5 = peg$currPos;
        peg$silentFails++;
        s6 = peg$currPos;
        if (input.substr(peg$currPos, 3) === peg$c1) {
          s7 = peg$c1;
          peg$currPos += 3;
        } else {
          s7 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e1);
          }
        }
        if (s7 !== peg$FAILED) {
          s8 = peg$parseNL();
          if (s8 !== peg$FAILED) {
            s7 = [s7, s8];
            s6 = s7;
          } else {
            peg$currPos = s6;
            s6 = peg$FAILED;
          }
        } else {
          peg$currPos = s6;
          s6 = peg$FAILED;
        }
        peg$silentFails--;
        if (s6 === peg$FAILED) {
          s5 = void 0;
        } else {
          peg$currPos = s5;
          s5 = peg$FAILED;
        }
        if (s5 !== peg$FAILED) {
          s6 = peg$currPos;
          s7 = [];
          s8 = input.charAt(peg$currPos);
          if (peg$r0.test(s8)) {
            peg$currPos++;
          } else {
            s8 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e2);
            }
          }
          while (s8 !== peg$FAILED) {
            s7.push(s8);
            s8 = input.charAt(peg$currPos);
            if (peg$r0.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e2);
              }
            }
          }
          s6 = input.substring(s6, peg$currPos);
          s7 = peg$parseNL();
          if (s7 !== peg$FAILED) {
            peg$savedPos = s4;
            s4 = peg$f1(s6);
          } else {
            peg$currPos = s4;
            s4 = peg$FAILED;
          }
        } else {
          peg$currPos = s4;
          s4 = peg$FAILED;
        }
        while (s4 !== peg$FAILED) {
          s3.push(s4);
          s4 = peg$currPos;
          s5 = peg$currPos;
          peg$silentFails++;
          s6 = peg$currPos;
          if (input.substr(peg$currPos, 3) === peg$c1) {
            s7 = peg$c1;
            peg$currPos += 3;
          } else {
            s7 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e1);
            }
          }
          if (s7 !== peg$FAILED) {
            s8 = peg$parseNL();
            if (s8 !== peg$FAILED) {
              s7 = [s7, s8];
              s6 = s7;
            } else {
              peg$currPos = s6;
              s6 = peg$FAILED;
            }
          } else {
            peg$currPos = s6;
            s6 = peg$FAILED;
          }
          peg$silentFails--;
          if (s6 === peg$FAILED) {
            s5 = void 0;
          } else {
            peg$currPos = s5;
            s5 = peg$FAILED;
          }
          if (s5 !== peg$FAILED) {
            s6 = peg$currPos;
            s7 = [];
            s8 = input.charAt(peg$currPos);
            if (peg$r0.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e2);
              }
            }
            while (s8 !== peg$FAILED) {
              s7.push(s8);
              s8 = input.charAt(peg$currPos);
              if (peg$r0.test(s8)) {
                peg$currPos++;
              } else {
                s8 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e2);
                }
              }
            }
            s6 = input.substring(s6, peg$currPos);
            s7 = peg$parseNL();
            if (s7 !== peg$FAILED) {
              peg$savedPos = s4;
              s4 = peg$f1(s6);
            } else {
              peg$currPos = s4;
              s4 = peg$FAILED;
            }
          } else {
            peg$currPos = s4;
            s4 = peg$FAILED;
          }
        }
        if (input.substr(peg$currPos, 3) === peg$c1) {
          s4 = peg$c1;
          peg$currPos += 3;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e1);
          }
        }
        if (s4 !== peg$FAILED) {
          s5 = peg$parseNL();
          if (s5 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f2(s3);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseLine() {
    var s0;
    s0 = peg$parseGroupLine();
    if (s0 === peg$FAILED) {
      s0 = peg$parseServiceLine();
      if (s0 === peg$FAILED) {
        s0 = peg$parseJunctionLine();
        if (s0 === peg$FAILED) {
          s0 = peg$parseAlignLine();
          if (s0 === peg$FAILED) {
            s0 = peg$parseEdgeLine();
            if (s0 === peg$FAILED) {
              s0 = peg$parseBlankLine();
            }
          }
        }
      }
    }
    return s0;
  }
  function peg$parseGroupLine() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9, s10, s11, s12, s13, s14, s15;
    s0 = peg$currPos;
    s1 = peg$currPos;
    s2 = [];
    s3 = input.charAt(peg$currPos);
    if (peg$r1.test(s3)) {
      peg$currPos++;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e3);
      }
    }
    while (s3 !== peg$FAILED) {
      s2.push(s3);
      s3 = input.charAt(peg$currPos);
      if (peg$r1.test(s3)) {
        peg$currPos++;
      } else {
        s3 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e3);
        }
      }
    }
    s1 = input.substring(s1, peg$currPos);
    s2 = input.substr(peg$currPos, 5);
    if (s2.toLowerCase() === peg$c2) {
      peg$currPos += 5;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e4);
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$parse__();
      if (s3 !== peg$FAILED) {
        s4 = peg$parseIdent();
        if (s4 !== peg$FAILED) {
          if (input.charCodeAt(peg$currPos) === 40) {
            s5 = peg$c3;
            peg$currPos++;
          } else {
            s5 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e5);
            }
          }
          if (s5 !== peg$FAILED) {
            s6 = peg$currPos;
            s7 = [];
            s8 = input.charAt(peg$currPos);
            if (peg$r2.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e6);
              }
            }
            while (s8 !== peg$FAILED) {
              s7.push(s8);
              s8 = input.charAt(peg$currPos);
              if (peg$r2.test(s8)) {
                peg$currPos++;
              } else {
                s8 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e6);
                }
              }
            }
            s6 = input.substring(s6, peg$currPos);
            if (input.charCodeAt(peg$currPos) === 41) {
              s7 = peg$c4;
              peg$currPos++;
            } else {
              s7 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e7);
              }
            }
            if (s7 !== peg$FAILED) {
              if (input.charCodeAt(peg$currPos) === 91) {
                s8 = peg$c5;
                peg$currPos++;
              } else {
                s8 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e8);
                }
              }
              if (s8 !== peg$FAILED) {
                s9 = peg$currPos;
                s10 = [];
                s11 = input.charAt(peg$currPos);
                if (peg$r3.test(s11)) {
                  peg$currPos++;
                } else {
                  s11 = peg$FAILED;
                  if (peg$silentFails === 0) {
                    peg$fail(peg$e9);
                  }
                }
                while (s11 !== peg$FAILED) {
                  s10.push(s11);
                  s11 = input.charAt(peg$currPos);
                  if (peg$r3.test(s11)) {
                    peg$currPos++;
                  } else {
                    s11 = peg$FAILED;
                    if (peg$silentFails === 0) {
                      peg$fail(peg$e9);
                    }
                  }
                }
                s9 = input.substring(s9, peg$currPos);
                if (input.charCodeAt(peg$currPos) === 93) {
                  s10 = peg$c6;
                  peg$currPos++;
                } else {
                  s10 = peg$FAILED;
                  if (peg$silentFails === 0) {
                    peg$fail(peg$e10);
                  }
                }
                if (s10 !== peg$FAILED) {
                  s11 = peg$currPos;
                  s12 = peg$parse__();
                  if (s12 !== peg$FAILED) {
                    s13 = input.substr(peg$currPos, 2);
                    if (s13.toLowerCase() === peg$c7) {
                      peg$currPos += 2;
                    } else {
                      s13 = peg$FAILED;
                      if (peg$silentFails === 0) {
                        peg$fail(peg$e11);
                      }
                    }
                    if (s13 !== peg$FAILED) {
                      s14 = peg$parse__();
                      if (s14 !== peg$FAILED) {
                        s15 = peg$parseIdent();
                        if (s15 !== peg$FAILED) {
                          peg$savedPos = s11;
                          s11 = peg$f3(s1, s4, s6, s9, s15);
                        } else {
                          peg$currPos = s11;
                          s11 = peg$FAILED;
                        }
                      } else {
                        peg$currPos = s11;
                        s11 = peg$FAILED;
                      }
                    } else {
                      peg$currPos = s11;
                      s11 = peg$FAILED;
                    }
                  } else {
                    peg$currPos = s11;
                    s11 = peg$FAILED;
                  }
                  if (s11 === peg$FAILED) {
                    s11 = null;
                  }
                  s12 = peg$parseNodeTail();
                  s13 = peg$parseLineEnd();
                  if (s13 !== peg$FAILED) {
                    peg$savedPos = s0;
                    s0 = peg$f4(s1, s4, s6, s9, s11, s12);
                  } else {
                    peg$currPos = s0;
                    s0 = peg$FAILED;
                  }
                } else {
                  peg$currPos = s0;
                  s0 = peg$FAILED;
                }
              } else {
                peg$currPos = s0;
                s0 = peg$FAILED;
              }
            } else {
              peg$currPos = s0;
              s0 = peg$FAILED;
            }
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseServiceLine() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9, s10, s11, s12, s13, s14, s15;
    s0 = peg$currPos;
    s1 = peg$currPos;
    s2 = [];
    s3 = input.charAt(peg$currPos);
    if (peg$r1.test(s3)) {
      peg$currPos++;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e3);
      }
    }
    while (s3 !== peg$FAILED) {
      s2.push(s3);
      s3 = input.charAt(peg$currPos);
      if (peg$r1.test(s3)) {
        peg$currPos++;
      } else {
        s3 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e3);
        }
      }
    }
    s1 = input.substring(s1, peg$currPos);
    s2 = input.substr(peg$currPos, 7);
    if (s2.toLowerCase() === peg$c8) {
      peg$currPos += 7;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e12);
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$parse__();
      if (s3 !== peg$FAILED) {
        s4 = peg$parseIdent();
        if (s4 !== peg$FAILED) {
          if (input.charCodeAt(peg$currPos) === 40) {
            s5 = peg$c3;
            peg$currPos++;
          } else {
            s5 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e5);
            }
          }
          if (s5 !== peg$FAILED) {
            s6 = peg$currPos;
            s7 = [];
            s8 = input.charAt(peg$currPos);
            if (peg$r2.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e6);
              }
            }
            while (s8 !== peg$FAILED) {
              s7.push(s8);
              s8 = input.charAt(peg$currPos);
              if (peg$r2.test(s8)) {
                peg$currPos++;
              } else {
                s8 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e6);
                }
              }
            }
            s6 = input.substring(s6, peg$currPos);
            if (input.charCodeAt(peg$currPos) === 41) {
              s7 = peg$c4;
              peg$currPos++;
            } else {
              s7 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e7);
              }
            }
            if (s7 !== peg$FAILED) {
              if (input.charCodeAt(peg$currPos) === 91) {
                s8 = peg$c5;
                peg$currPos++;
              } else {
                s8 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e8);
                }
              }
              if (s8 !== peg$FAILED) {
                s9 = peg$currPos;
                s10 = [];
                s11 = input.charAt(peg$currPos);
                if (peg$r3.test(s11)) {
                  peg$currPos++;
                } else {
                  s11 = peg$FAILED;
                  if (peg$silentFails === 0) {
                    peg$fail(peg$e9);
                  }
                }
                while (s11 !== peg$FAILED) {
                  s10.push(s11);
                  s11 = input.charAt(peg$currPos);
                  if (peg$r3.test(s11)) {
                    peg$currPos++;
                  } else {
                    s11 = peg$FAILED;
                    if (peg$silentFails === 0) {
                      peg$fail(peg$e9);
                    }
                  }
                }
                s9 = input.substring(s9, peg$currPos);
                if (input.charCodeAt(peg$currPos) === 93) {
                  s10 = peg$c6;
                  peg$currPos++;
                } else {
                  s10 = peg$FAILED;
                  if (peg$silentFails === 0) {
                    peg$fail(peg$e10);
                  }
                }
                if (s10 !== peg$FAILED) {
                  s11 = peg$currPos;
                  s12 = peg$parse__();
                  if (s12 !== peg$FAILED) {
                    s13 = input.substr(peg$currPos, 2);
                    if (s13.toLowerCase() === peg$c7) {
                      peg$currPos += 2;
                    } else {
                      s13 = peg$FAILED;
                      if (peg$silentFails === 0) {
                        peg$fail(peg$e11);
                      }
                    }
                    if (s13 !== peg$FAILED) {
                      s14 = peg$parse__();
                      if (s14 !== peg$FAILED) {
                        s15 = peg$parseIdent();
                        if (s15 !== peg$FAILED) {
                          peg$savedPos = s11;
                          s11 = peg$f5(s1, s4, s6, s9, s15);
                        } else {
                          peg$currPos = s11;
                          s11 = peg$FAILED;
                        }
                      } else {
                        peg$currPos = s11;
                        s11 = peg$FAILED;
                      }
                    } else {
                      peg$currPos = s11;
                      s11 = peg$FAILED;
                    }
                  } else {
                    peg$currPos = s11;
                    s11 = peg$FAILED;
                  }
                  if (s11 === peg$FAILED) {
                    s11 = null;
                  }
                  s12 = peg$parseNodeTail();
                  s13 = peg$parseLineEnd();
                  if (s13 !== peg$FAILED) {
                    peg$savedPos = s0;
                    s0 = peg$f6(s1, s4, s6, s9, s11, s12);
                  } else {
                    peg$currPos = s0;
                    s0 = peg$FAILED;
                  }
                } else {
                  peg$currPos = s0;
                  s0 = peg$FAILED;
                }
              } else {
                peg$currPos = s0;
                s0 = peg$FAILED;
              }
            } else {
              peg$currPos = s0;
              s0 = peg$FAILED;
            }
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseJunctionLine() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9;
    s0 = peg$currPos;
    s1 = peg$currPos;
    s2 = [];
    s3 = input.charAt(peg$currPos);
    if (peg$r1.test(s3)) {
      peg$currPos++;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e3);
      }
    }
    while (s3 !== peg$FAILED) {
      s2.push(s3);
      s3 = input.charAt(peg$currPos);
      if (peg$r1.test(s3)) {
        peg$currPos++;
      } else {
        s3 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e3);
        }
      }
    }
    s1 = input.substring(s1, peg$currPos);
    s2 = input.substr(peg$currPos, 8);
    if (s2.toLowerCase() === peg$c9) {
      peg$currPos += 8;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e13);
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$parse__();
      if (s3 !== peg$FAILED) {
        s4 = peg$parseIdent();
        if (s4 !== peg$FAILED) {
          s5 = peg$currPos;
          s6 = peg$parse__();
          if (s6 !== peg$FAILED) {
            s7 = input.substr(peg$currPos, 2);
            if (s7.toLowerCase() === peg$c7) {
              peg$currPos += 2;
            } else {
              s7 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e11);
              }
            }
            if (s7 !== peg$FAILED) {
              s8 = peg$parse__();
              if (s8 !== peg$FAILED) {
                s9 = peg$parseIdent();
                if (s9 !== peg$FAILED) {
                  peg$savedPos = s5;
                  s5 = peg$f7(s1, s4, s9);
                } else {
                  peg$currPos = s5;
                  s5 = peg$FAILED;
                }
              } else {
                peg$currPos = s5;
                s5 = peg$FAILED;
              }
            } else {
              peg$currPos = s5;
              s5 = peg$FAILED;
            }
          } else {
            peg$currPos = s5;
            s5 = peg$FAILED;
          }
          if (s5 === peg$FAILED) {
            s5 = null;
          }
          s6 = peg$parseRestIgnore();
          s7 = peg$parseLineEnd();
          if (s7 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f8(s1, s4, s5);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseEdgeLine() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9, s10, s11, s12;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = peg$parseEdgeEndpoint();
    if (s2 !== peg$FAILED) {
      if (input.charCodeAt(peg$currPos) === 58) {
        s3 = peg$c10;
        peg$currPos++;
      } else {
        s3 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e14);
        }
      }
      if (s3 !== peg$FAILED) {
        s4 = peg$parseSide();
        if (s4 !== peg$FAILED) {
          s5 = peg$parse_();
          s6 = peg$parseConnectorArrow();
          if (s6 !== peg$FAILED) {
            s7 = peg$parse_();
            s8 = peg$parseSide();
            if (s8 !== peg$FAILED) {
              if (input.charCodeAt(peg$currPos) === 58) {
                s9 = peg$c10;
                peg$currPos++;
              } else {
                s9 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e14);
                }
              }
              if (s9 !== peg$FAILED) {
                s10 = peg$parseEdgeEndpoint();
                if (s10 !== peg$FAILED) {
                  s11 = peg$parseEdgeTail();
                  s12 = peg$parseLineEnd();
                  if (s12 !== peg$FAILED) {
                    peg$savedPos = s0;
                    s0 = peg$f9(s2, s4, s6, s8, s10, s11);
                  } else {
                    peg$currPos = s0;
                    s0 = peg$FAILED;
                  }
                } else {
                  peg$currPos = s0;
                  s0 = peg$FAILED;
                }
              } else {
                peg$currPos = s0;
                s0 = peg$FAILED;
              }
            } else {
              peg$currPos = s0;
              s0 = peg$FAILED;
            }
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseEdgeEndpoint() {
    var s0, s1, s2, s3;
    s0 = peg$currPos;
    s1 = peg$parseIdent();
    if (s1 !== peg$FAILED) {
      s2 = peg$currPos;
      if (input.substr(peg$currPos, 7) === peg$c11) {
        s3 = peg$c11;
        peg$currPos += 7;
      } else {
        s3 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e15);
        }
      }
      if (s3 !== peg$FAILED) {
        peg$savedPos = s2;
        s3 = peg$f10(s1);
      }
      s2 = s3;
      if (s2 === peg$FAILED) {
        s2 = null;
      }
      peg$savedPos = s0;
      s0 = peg$f11(s1, s2);
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseConnectorArrow() {
    var s0, s1;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 5) === peg$c12) {
      s1 = peg$c12;
      peg$currPos += 5;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e16);
      }
    }
    if (s1 === peg$FAILED) {
      if (input.substr(peg$currPos, 5) === peg$c13) {
        s1 = peg$c13;
        peg$currPos += 5;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e17);
        }
      }
      if (s1 === peg$FAILED) {
        if (input.substr(peg$currPos, 5) === peg$c14) {
          s1 = peg$c14;
          peg$currPos += 5;
        } else {
          s1 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e18);
          }
        }
        if (s1 === peg$FAILED) {
          if (input.substr(peg$currPos, 4) === peg$c15) {
            s1 = peg$c15;
            peg$currPos += 4;
          } else {
            s1 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e19);
            }
          }
          if (s1 === peg$FAILED) {
            if (input.substr(peg$currPos, 4) === peg$c16) {
              s1 = peg$c16;
              peg$currPos += 4;
            } else {
              s1 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e20);
              }
            }
            if (s1 === peg$FAILED) {
              if (input.substr(peg$currPos, 4) === peg$c17) {
                s1 = peg$c17;
                peg$currPos += 4;
              } else {
                s1 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e21);
                }
              }
              if (s1 === peg$FAILED) {
                if (input.substr(peg$currPos, 4) === peg$c18) {
                  s1 = peg$c18;
                  peg$currPos += 4;
                } else {
                  s1 = peg$FAILED;
                  if (peg$silentFails === 0) {
                    peg$fail(peg$e22);
                  }
                }
                if (s1 === peg$FAILED) {
                  if (input.substr(peg$currPos, 4) === peg$c19) {
                    s1 = peg$c19;
                    peg$currPos += 4;
                  } else {
                    s1 = peg$FAILED;
                    if (peg$silentFails === 0) {
                      peg$fail(peg$e23);
                    }
                  }
                  if (s1 === peg$FAILED) {
                    if (input.substr(peg$currPos, 3) === peg$c20) {
                      s1 = peg$c20;
                      peg$currPos += 3;
                    } else {
                      s1 = peg$FAILED;
                      if (peg$silentFails === 0) {
                        peg$fail(peg$e24);
                      }
                    }
                    if (s1 === peg$FAILED) {
                      if (input.substr(peg$currPos, 3) === peg$c21) {
                        s1 = peg$c21;
                        peg$currPos += 3;
                      } else {
                        s1 = peg$FAILED;
                        if (peg$silentFails === 0) {
                          peg$fail(peg$e25);
                        }
                      }
                      if (s1 === peg$FAILED) {
                        if (input.substr(peg$currPos, 3) === peg$c22) {
                          s1 = peg$c22;
                          peg$currPos += 3;
                        } else {
                          s1 = peg$FAILED;
                          if (peg$silentFails === 0) {
                            peg$fail(peg$e26);
                          }
                        }
                        if (s1 === peg$FAILED) {
                          if (input.substr(peg$currPos, 3) === peg$c23) {
                            s1 = peg$c23;
                            peg$currPos += 3;
                          } else {
                            s1 = peg$FAILED;
                            if (peg$silentFails === 0) {
                              peg$fail(peg$e27);
                            }
                          }
                          if (s1 === peg$FAILED) {
                            if (input.substr(peg$currPos, 3) === peg$c24) {
                              s1 = peg$c24;
                              peg$currPos += 3;
                            } else {
                              s1 = peg$FAILED;
                              if (peg$silentFails === 0) {
                                peg$fail(peg$e28);
                              }
                            }
                            if (s1 === peg$FAILED) {
                              if (input.substr(peg$currPos, 3) === peg$c25) {
                                s1 = peg$c25;
                                peg$currPos += 3;
                              } else {
                                s1 = peg$FAILED;
                                if (peg$silentFails === 0) {
                                  peg$fail(peg$e29);
                                }
                              }
                              if (s1 === peg$FAILED) {
                                if (input.substr(peg$currPos, 3) === peg$c26) {
                                  s1 = peg$c26;
                                  peg$currPos += 3;
                                } else {
                                  s1 = peg$FAILED;
                                  if (peg$silentFails === 0) {
                                    peg$fail(peg$e30);
                                  }
                                }
                                if (s1 === peg$FAILED) {
                                  if (input.substr(peg$currPos, 3) === peg$c1) {
                                    s1 = peg$c1;
                                    peg$currPos += 3;
                                  } else {
                                    s1 = peg$FAILED;
                                    if (peg$silentFails === 0) {
                                      peg$fail(peg$e1);
                                    }
                                  }
                                  if (s1 === peg$FAILED) {
                                    if (input.substr(peg$currPos, 2) === peg$c27) {
                                      s1 = peg$c27;
                                      peg$currPos += 2;
                                    } else {
                                      s1 = peg$FAILED;
                                      if (peg$silentFails === 0) {
                                        peg$fail(peg$e31);
                                      }
                                    }
                                  }
                                }
                              }
                            }
                          }
                        }
                      }
                    }
                  }
                }
              }
            }
          }
        }
      }
    }
    if (s1 !== peg$FAILED) {
      peg$savedPos = s0;
      s1 = peg$f12(s1);
    }
    s0 = s1;
    return s0;
  }
  function peg$parseEdgeTail() {
    var s0, s1, s2, s3, s4, s5;
    s0 = peg$currPos;
    s1 = [];
    s2 = peg$currPos;
    s3 = peg$parse_();
    if (input.charCodeAt(peg$currPos) === 64) {
      s4 = peg$c28;
      peg$currPos++;
    } else {
      s4 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e32);
      }
    }
    if (s4 !== peg$FAILED) {
      s5 = peg$parseAnnotation();
      if (s5 !== peg$FAILED) {
        s3 = [s3, s4, s5];
        s2 = s3;
      } else {
        peg$currPos = s2;
        s2 = peg$FAILED;
      }
    } else {
      peg$currPos = s2;
      s2 = peg$FAILED;
    }
    while (s2 !== peg$FAILED) {
      s1.push(s2);
      s2 = peg$currPos;
      s3 = peg$parse_();
      if (input.charCodeAt(peg$currPos) === 64) {
        s4 = peg$c28;
        peg$currPos++;
      } else {
        s4 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e32);
        }
      }
      if (s4 !== peg$FAILED) {
        s5 = peg$parseAnnotation();
        if (s5 !== peg$FAILED) {
          s3 = [s3, s4, s5];
          s2 = s3;
        } else {
          peg$currPos = s2;
          s2 = peg$FAILED;
        }
      } else {
        peg$currPos = s2;
        s2 = peg$FAILED;
      }
    }
    s2 = peg$currPos;
    s3 = peg$parse_();
    s4 = peg$parsePropBlock();
    if (s4 !== peg$FAILED) {
      s3 = [s3, s4];
      s2 = s3;
    } else {
      peg$currPos = s2;
      s2 = peg$FAILED;
    }
    if (s2 === peg$FAILED) {
      s2 = null;
    }
    s3 = peg$currPos;
    s4 = [];
    s5 = input.charAt(peg$currPos);
    if (peg$r0.test(s5)) {
      peg$currPos++;
    } else {
      s5 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e2);
      }
    }
    while (s5 !== peg$FAILED) {
      s4.push(s5);
      s5 = input.charAt(peg$currPos);
      if (peg$r0.test(s5)) {
        peg$currPos++;
      } else {
        s5 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e2);
        }
      }
    }
    s3 = input.substring(s3, peg$currPos);
    peg$savedPos = s0;
    s0 = peg$f13(s1, s2, s3);
    return s0;
  }
  function peg$parseAnnotation() {
    var s0, s1, s2, s3;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 5) === peg$c29) {
      s1 = peg$c29;
      peg$currPos += 5;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e33);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$parseAnimValue();
      if (s2 !== peg$FAILED) {
        peg$savedPos = s0;
        s0 = peg$f14(s2);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      if (input.substr(peg$currPos, 6) === peg$c30) {
        s1 = peg$c30;
        peg$currPos += 6;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e34);
        }
      }
      if (s1 !== peg$FAILED) {
        s2 = peg$parseRouteStyleValue();
        if (s2 !== peg$FAILED) {
          peg$savedPos = s0;
          s0 = peg$f15(s2);
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
      if (s0 === peg$FAILED) {
        s0 = peg$currPos;
        s1 = peg$parseWallRouteStyle();
        if (s1 !== peg$FAILED) {
          if (input.charCodeAt(peg$currPos) === 58) {
            s2 = peg$c10;
            peg$currPos++;
          } else {
            s2 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e14);
            }
          }
          if (s2 !== peg$FAILED) {
            s3 = peg$parseWallPair();
            if (s3 !== peg$FAILED) {
              peg$savedPos = s0;
              s0 = peg$f16(s1, s3);
            } else {
              peg$currPos = s0;
              s0 = peg$FAILED;
            }
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      }
    }
    return s0;
  }
  function peg$parseAnimValue() {
    var s0, s1;
    s0 = peg$currPos;
    s1 = peg$parseIdent();
    if (s1 !== peg$FAILED) {
      peg$savedPos = s0;
      s1 = peg$f17(s1);
    }
    s0 = s1;
    return s0;
  }
  function peg$parseRouteStyleValue() {
    var s0, s1;
    s0 = peg$currPos;
    s1 = peg$parseIdent();
    if (s1 !== peg$FAILED) {
      peg$savedPos = s0;
      s1 = peg$f18(s1);
    }
    s0 = s1;
    return s0;
  }
  function peg$parseWallRouteStyle() {
    var s0, s1;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 10) === peg$c31) {
      s1 = peg$c31;
      peg$currPos += 10;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e35);
      }
    }
    if (s1 !== peg$FAILED) {
      peg$savedPos = s0;
      s1 = peg$f19();
    }
    s0 = s1;
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      if (input.substr(peg$currPos, 6) === peg$c32) {
        s1 = peg$c32;
        peg$currPos += 6;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e36);
        }
      }
      if (s1 !== peg$FAILED) {
        peg$savedPos = s0;
        s1 = peg$f20();
      }
      s0 = s1;
      if (s0 === peg$FAILED) {
        s0 = peg$currPos;
        if (input.substr(peg$currPos, 8) === peg$c33) {
          s1 = peg$c33;
          peg$currPos += 8;
        } else {
          s1 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e37);
          }
        }
        if (s1 !== peg$FAILED) {
          peg$savedPos = s0;
          s1 = peg$f21();
        }
        s0 = s1;
        if (s0 === peg$FAILED) {
          s0 = peg$currPos;
          if (input.substr(peg$currPos, 8) === peg$c34) {
            s1 = peg$c34;
            peg$currPos += 8;
          } else {
            s1 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e38);
            }
          }
          if (s1 !== peg$FAILED) {
            peg$savedPos = s0;
            s1 = peg$f22();
          }
          s0 = s1;
        }
      }
    }
    return s0;
  }
  function peg$parseWallPair() {
    var s0, s1, s2, s3;
    s0 = peg$currPos;
    s1 = peg$parseWall();
    if (s1 !== peg$FAILED) {
      s2 = peg$parseWall();
      if (s2 !== peg$FAILED) {
        peg$savedPos = s0;
        s0 = peg$f23(s1, s2);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      s1 = peg$parseWall();
      if (s1 !== peg$FAILED) {
        s2 = peg$currPos;
        peg$silentFails++;
        s3 = input.charAt(peg$currPos);
        if (peg$r4.test(s3)) {
          peg$currPos++;
        } else {
          s3 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e39);
          }
        }
        peg$silentFails--;
        if (s3 === peg$FAILED) {
          s2 = void 0;
        } else {
          peg$currPos = s2;
          s2 = peg$FAILED;
        }
        if (s2 !== peg$FAILED) {
          peg$savedPos = s0;
          s0 = peg$f24(s1);
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    }
    return s0;
  }
  function peg$parseWall() {
    var s0, s1;
    s0 = peg$currPos;
    s1 = input.charAt(peg$currPos);
    if (peg$r5.test(s1)) {
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e40);
      }
    }
    if (s1 !== peg$FAILED) {
      peg$savedPos = s0;
      s1 = peg$f25(s1);
    }
    s0 = s1;
    return s0;
  }
  function peg$parsePropBlock() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 123) {
      s1 = peg$c35;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e41);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$parse_();
      s3 = peg$parsePropEntry();
      if (s3 !== peg$FAILED) {
        s4 = [];
        s5 = peg$currPos;
        s6 = peg$parse_();
        if (input.charCodeAt(peg$currPos) === 44) {
          s7 = peg$c36;
          peg$currPos++;
        } else {
          s7 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e42);
          }
        }
        if (s7 !== peg$FAILED) {
          s8 = peg$parse_();
          s9 = peg$parsePropEntry();
          if (s9 !== peg$FAILED) {
            s6 = [s6, s7, s8, s9];
            s5 = s6;
          } else {
            peg$currPos = s5;
            s5 = peg$FAILED;
          }
        } else {
          peg$currPos = s5;
          s5 = peg$FAILED;
        }
        while (s5 !== peg$FAILED) {
          s4.push(s5);
          s5 = peg$currPos;
          s6 = peg$parse_();
          if (input.charCodeAt(peg$currPos) === 44) {
            s7 = peg$c36;
            peg$currPos++;
          } else {
            s7 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e42);
            }
          }
          if (s7 !== peg$FAILED) {
            s8 = peg$parse_();
            s9 = peg$parsePropEntry();
            if (s9 !== peg$FAILED) {
              s6 = [s6, s7, s8, s9];
              s5 = s6;
            } else {
              peg$currPos = s5;
              s5 = peg$FAILED;
            }
          } else {
            peg$currPos = s5;
            s5 = peg$FAILED;
          }
        }
        s5 = peg$parse_();
        if (input.charCodeAt(peg$currPos) === 125) {
          s6 = peg$c37;
          peg$currPos++;
        } else {
          s6 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e43);
          }
        }
        if (s6 !== peg$FAILED) {
          peg$savedPos = s0;
          s0 = peg$f26(s3, s4);
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parsePropEntry() {
    var s0, s1, s2, s3, s4, s5;
    s0 = peg$currPos;
    s1 = peg$parseIdent();
    if (s1 !== peg$FAILED) {
      s2 = peg$parse_();
      if (input.charCodeAt(peg$currPos) === 58) {
        s3 = peg$c10;
        peg$currPos++;
      } else {
        s3 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e14);
        }
      }
      if (s3 !== peg$FAILED) {
        s4 = peg$parse_();
        s5 = peg$parsePropValue();
        if (s5 !== peg$FAILED) {
          peg$savedPos = s0;
          s0 = peg$f27(s1, s5);
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parsePropValue() {
    var s0, s1;
    s0 = peg$currPos;
    s1 = peg$parseIdent();
    if (s1 !== peg$FAILED) {
      peg$savedPos = s0;
      s1 = peg$f28(s1);
    }
    s0 = s1;
    return s0;
  }
  function peg$parseNodeTail() {
    var s0, s1, s2, s3, s4, s5;
    s0 = peg$currPos;
    s1 = [];
    s2 = peg$currPos;
    s3 = peg$parse_();
    if (input.charCodeAt(peg$currPos) === 64) {
      s4 = peg$c28;
      peg$currPos++;
    } else {
      s4 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e32);
      }
    }
    if (s4 !== peg$FAILED) {
      s5 = peg$parseNodeAnnotation();
      if (s5 !== peg$FAILED) {
        s3 = [s3, s4, s5];
        s2 = s3;
      } else {
        peg$currPos = s2;
        s2 = peg$FAILED;
      }
    } else {
      peg$currPos = s2;
      s2 = peg$FAILED;
    }
    while (s2 !== peg$FAILED) {
      s1.push(s2);
      s2 = peg$currPos;
      s3 = peg$parse_();
      if (input.charCodeAt(peg$currPos) === 64) {
        s4 = peg$c28;
        peg$currPos++;
      } else {
        s4 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e32);
        }
      }
      if (s4 !== peg$FAILED) {
        s5 = peg$parseNodeAnnotation();
        if (s5 !== peg$FAILED) {
          s3 = [s3, s4, s5];
          s2 = s3;
        } else {
          peg$currPos = s2;
          s2 = peg$FAILED;
        }
      } else {
        peg$currPos = s2;
        s2 = peg$FAILED;
      }
    }
    s2 = peg$currPos;
    s3 = peg$parse_();
    s4 = peg$parsePropBlock();
    if (s4 !== peg$FAILED) {
      s3 = [s3, s4];
      s2 = s3;
    } else {
      peg$currPos = s2;
      s2 = peg$FAILED;
    }
    if (s2 === peg$FAILED) {
      s2 = null;
    }
    s3 = peg$currPos;
    s4 = [];
    s5 = input.charAt(peg$currPos);
    if (peg$r0.test(s5)) {
      peg$currPos++;
    } else {
      s5 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e2);
      }
    }
    while (s5 !== peg$FAILED) {
      s4.push(s5);
      s5 = input.charAt(peg$currPos);
      if (peg$r0.test(s5)) {
        peg$currPos++;
      } else {
        s5 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e2);
        }
      }
    }
    s3 = input.substring(s3, peg$currPos);
    peg$savedPos = s0;
    s0 = peg$f29(s1, s2, s3);
    return s0;
  }
  function peg$parseNodeAnnotation() {
    var s0, s1, s2;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 10) === peg$c38) {
      s1 = peg$c38;
      peg$currPos += 10;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e44);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$parseIconAlignValue();
      if (s2 !== peg$FAILED) {
        peg$savedPos = s0;
        s0 = peg$f30(s2);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseIconAlignValue() {
    var s0, s1;
    s0 = peg$currPos;
    s1 = peg$parseIdent();
    if (s1 !== peg$FAILED) {
      peg$savedPos = s0;
      s1 = peg$f31(s1);
    }
    s0 = s1;
    return s0;
  }
  function peg$parseAlignLine() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9, s10;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = input.substr(peg$currPos, 5);
    if (s2.toLowerCase() === peg$c39) {
      peg$currPos += 5;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e45);
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$parse__();
      if (s3 !== peg$FAILED) {
        s4 = input.substr(peg$currPos, 3);
        if (s4.toLowerCase() === peg$c40) {
          peg$currPos += 3;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e46);
          }
        }
        if (s4 === peg$FAILED) {
          s4 = input.substr(peg$currPos, 6);
          if (s4.toLowerCase() === peg$c41) {
            peg$currPos += 6;
          } else {
            s4 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e47);
            }
          }
        }
        if (s4 !== peg$FAILED) {
          s5 = peg$parse__();
          if (s5 !== peg$FAILED) {
            s6 = peg$parseIdent();
            if (s6 !== peg$FAILED) {
              s7 = [];
              s8 = peg$currPos;
              s9 = peg$parse__();
              if (s9 !== peg$FAILED) {
                s10 = peg$parseIdent();
                if (s10 !== peg$FAILED) {
                  peg$savedPos = s8;
                  s8 = peg$f32(s4, s6, s10);
                } else {
                  peg$currPos = s8;
                  s8 = peg$FAILED;
                }
              } else {
                peg$currPos = s8;
                s8 = peg$FAILED;
              }
              if (s8 !== peg$FAILED) {
                while (s8 !== peg$FAILED) {
                  s7.push(s8);
                  s8 = peg$currPos;
                  s9 = peg$parse__();
                  if (s9 !== peg$FAILED) {
                    s10 = peg$parseIdent();
                    if (s10 !== peg$FAILED) {
                      peg$savedPos = s8;
                      s8 = peg$f32(s4, s6, s10);
                    } else {
                      peg$currPos = s8;
                      s8 = peg$FAILED;
                    }
                  } else {
                    peg$currPos = s8;
                    s8 = peg$FAILED;
                  }
                }
              } else {
                s7 = peg$FAILED;
              }
              if (s7 !== peg$FAILED) {
                s8 = peg$parseRestIgnore();
                s9 = peg$parseLineEnd();
                if (s9 !== peg$FAILED) {
                  peg$savedPos = s0;
                  s0 = peg$f33(s4, s6, s7);
                } else {
                  peg$currPos = s0;
                  s0 = peg$FAILED;
                }
              } else {
                peg$currPos = s0;
                s0 = peg$FAILED;
              }
            } else {
              peg$currPos = s0;
              s0 = peg$FAILED;
            }
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseSide() {
    var s0;
    s0 = input.charAt(peg$currPos);
    if (peg$r6.test(s0)) {
      peg$currPos++;
    } else {
      s0 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e48);
      }
    }
    return s0;
  }
  function peg$parseRestIgnore() {
    var s0, s1, s2;
    s0 = peg$currPos;
    s1 = [];
    s2 = input.charAt(peg$currPos);
    if (peg$r0.test(s2)) {
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e2);
      }
    }
    while (s2 !== peg$FAILED) {
      s1.push(s2);
      s2 = input.charAt(peg$currPos);
      if (peg$r0.test(s2)) {
        peg$currPos++;
      } else {
        s2 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e2);
        }
      }
    }
    s0 = input.substring(s0, peg$currPos);
    return s0;
  }
  function peg$parseIdent() {
    var s0, s1, s2;
    s0 = peg$currPos;
    s1 = [];
    s2 = input.charAt(peg$currPos);
    if (peg$r4.test(s2)) {
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e39);
      }
    }
    if (s2 !== peg$FAILED) {
      while (s2 !== peg$FAILED) {
        s1.push(s2);
        s2 = input.charAt(peg$currPos);
        if (peg$r4.test(s2)) {
          peg$currPos++;
        } else {
          s2 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e39);
          }
        }
      }
    } else {
      s1 = peg$FAILED;
    }
    if (s1 !== peg$FAILED) {
      s0 = input.substring(s0, peg$currPos);
    } else {
      s0 = s1;
    }
    return s0;
  }
  function peg$parseNL() {
    var s0, s1, s2;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 13) {
      s1 = peg$c42;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e49);
      }
    }
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s2 = peg$c43;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e50);
      }
    }
    if (s2 !== peg$FAILED) {
      s1 = [s1, s2];
      s0 = s1;
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseBlankLine() {
    var s0, s1, s2, s3;
    s0 = peg$currPos;
    s1 = peg$parse_();
    if (input.charCodeAt(peg$currPos) === 13) {
      s2 = peg$c42;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e49);
      }
    }
    if (s2 === peg$FAILED) {
      s2 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s3 = peg$c43;
      peg$currPos++;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e50);
      }
    }
    if (s3 !== peg$FAILED) {
      peg$savedPos = s0;
      s0 = peg$f34();
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseLineEnd() {
    var s0, s1, s2;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 13) {
      s1 = peg$c42;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e49);
      }
    }
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s2 = peg$c43;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e50);
      }
    }
    if (s2 !== peg$FAILED) {
      s1 = [s1, s2];
      s0 = s1;
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      peg$silentFails++;
      if (input.length > peg$currPos) {
        s1 = input.charAt(peg$currPos);
        peg$currPos++;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e51);
        }
      }
      peg$silentFails--;
      if (s1 === peg$FAILED) {
        s0 = void 0;
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    }
    return s0;
  }
  function peg$parse_() {
    var s0, s1;
    s0 = [];
    s1 = input.charAt(peg$currPos);
    if (peg$r1.test(s1)) {
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e3);
      }
    }
    while (s1 !== peg$FAILED) {
      s0.push(s1);
      s1 = input.charAt(peg$currPos);
      if (peg$r1.test(s1)) {
        peg$currPos++;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e3);
        }
      }
    }
    return s0;
  }
  function peg$parse__() {
    var s0, s1;
    s0 = [];
    s1 = input.charAt(peg$currPos);
    if (peg$r1.test(s1)) {
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e3);
      }
    }
    if (s1 !== peg$FAILED) {
      while (s1 !== peg$FAILED) {
        s0.push(s1);
        s1 = input.charAt(peg$currPos);
        if (peg$r1.test(s1)) {
          peg$currPos++;
        } else {
          s1 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e3);
          }
        }
      }
    } else {
      s0 = peg$FAILED;
    }
    return s0;
  }
  peg$result = peg$startRuleFunction();
  if (options.peg$library) {
    return (
      /** @type {any} */
      {
        peg$result,
        peg$currPos,
        peg$FAILED,
        peg$maxFailExpected,
        peg$maxFailPos
      }
    );
  }
  if (peg$result !== peg$FAILED && peg$currPos === input.length) {
    return peg$result;
  } else {
    if (peg$result !== peg$FAILED && peg$currPos < input.length) {
      peg$fail(peg$endExpectation());
    }
    throw peg$buildStructuredError(
      peg$maxFailExpected,
      peg$maxFailPos < input.length ? input.charAt(peg$maxFailPos) : null,
      peg$maxFailPos < input.length ? peg$computeLocation(peg$maxFailPos, peg$maxFailPos + 1) : peg$computeLocation(peg$maxFailPos, peg$maxFailPos)
    );
  }
}
var architecture = {
  parseMermaid(input) {
    return peg$parse21(input);
  },
  parseYaml(input) {
    return JSON.parse(input);
  },
  layout(ir, theme, options) {
    return layoutArchitecture(ir, theme, options);
  }
};
var BITS_PER_ROW = 32;
function layoutPacket(ir, theme) {
  const { palette, typography, spacing } = theme;
  const p = pen(theme);
  const margin = spacing.diagramMargin;
  const fields = [...ir.fields].sort((a, b) => a.start - b.start);
  const maxBit = Math.max(0, ...fields.map((f2) => f2.end));
  const numRows = Math.floor(maxBit / BITS_PER_ROW) + 1;
  const bitW = 26;
  const rowH = 46;
  const rowGap = 26;
  const bitFont = typography.smallFontSize;
  const labelFont = typography.smallFontSize;
  const title = ir.metadata.title;
  const titleH = title ? typography.titleFontSize + 16 : 0;
  const gridLeft = margin;
  const gridTop = margin + titleH + rowGap;
  const elements = [];
  if (title)
    elements.push(
      p.text(
        title,
        margin,
        margin + typography.titleFontSize,
        typography.titleFontSize,
        palette.text,
        { weight: "bold" }
      )
    );
  const fieldTokens = [
    palette.primary,
    palette.secondary,
    palette.success,
    palette.warning,
    palette.error
  ];
  const rowTop = (r) => gridTop + r * (rowH + rowGap);
  for (let r = 0; r < numRows; r++) {
    for (let c = 0; c <= BITS_PER_ROW; c += 8) {
      const bit = r * BITS_PER_ROW + c;
      const x = gridLeft + c * bitW;
      if (c < BITS_PER_ROW)
        elements.push(
          p.text(String(bit), rhu(x + 2), rhu(rowTop(r) - 6), bitFont, palette.textMuted)
        );
    }
  }
  fields.forEach((f2, i) => {
    const accent = fieldTokens[i % fieldTokens.length];
    const fill = mixHex(accent, palette.background, 0.8);
    const labelColor = readableText(fill, theme);
    for (let r = Math.floor(f2.start / BITS_PER_ROW); r <= Math.floor(f2.end / BITS_PER_ROW); r++) {
      const rowStartBit = r * BITS_PER_ROW;
      const cStart = Math.max(f2.start, rowStartBit) - rowStartBit;
      const cEnd = Math.min(f2.end, rowStartBit + BITS_PER_ROW - 1) - rowStartBit;
      const x = gridLeft + cStart * bitW;
      const w = (cEnd - cStart + 1) * bitW;
      const y = rowTop(r);
      elements.push(
        p.rect({ x: rhu(x), y: rhu(y), width: rhu(w), height: rowH }, fill, accent, 1.4, { rx: 3 })
      );
      placeFieldLabel(
        p,
        elements,
        x,
        y,
        w,
        rowH,
        f2.label,
        labelFont,
        labelColor,
        bitFont + 3,
        bitW * 2
      );
      elements.push(
        p.text(String(f2.start), rhu(x + 3), rhu(y + bitFont + 1), bitFont, palette.textMuted)
      );
    }
  });
  const totalW = rhuInt(gridLeft + BITS_PER_ROW * bitW + margin);
  const totalH = rhuInt(rowTop(numRows - 1) + rowH + margin);
  const scene = applyOverlays(
    {
      viewBox: { x: 0, y: 0, width: totalW, height: totalH },
      background: palette.background,
      elements
    },
    ir.overlays,
    theme
  );
  return { scene, anchors: {} };
}
var MIN_LABEL_FONT = 7;
function placeFieldLabel(p, elements, x, y, w, cellH, text, baseFont, color, topReserve, narrowW) {
  const cx = x + w / 2;
  const padX = 6;
  if (measureText(text, baseFont).width <= w - padX) {
    elements.push(
      p.text(text, rhuInt(cx), rhu(y + cellH / 2 + baseFont * 0.35), baseFont, color, {
        weight: "bold",
        anchor: "middle"
      })
    );
    return;
  }
  if (w <= narrowW) {
    const availV = cellH - topReserve - 4;
    const cy = y + topReserve + availV / 2;
    let f3 = baseFont;
    while (f3 > MIN_LABEL_FONT && measureText(text, f3).width > availV) f3 -= 0.5;
    const label2 = measureText(text, f3).width > availV ? truncateText(text, f3, availV) : text;
    const px = rhuInt(cx), py = rhu(cy);
    elements.push(
      p.group(
        [p.text(label2, px, rhu(cy + f3 * 0.35), f3, color, { weight: "bold", anchor: "middle" })],
        { transform: `rotate(-90 ${px} ${py})` }
      )
    );
    return;
  }
  let f2 = baseFont;
  while (f2 > MIN_LABEL_FONT && measureText(text, f2).width > w - padX) f2 -= 0.5;
  const label = measureText(text, f2).width > w - padX ? truncateText(text, f2, w - padX) : text;
  elements.push(
    p.text(label, rhuInt(cx), rhu(y + cellH / 2 + f2 * 0.35), f2, color, {
      weight: "bold",
      anchor: "middle"
    })
  );
}
function parseHex2(c) {
  const s = c.trim();
  const m3 = s.match(/^#([0-9a-f])([0-9a-f])([0-9a-f])$/i);
  if (m3)
    return [
      parseInt(m3[1] + m3[1], 16),
      parseInt(m3[2] + m3[2], 16),
      parseInt(m3[3] + m3[3], 16)
    ];
  const m6 = s.match(/^#([0-9a-f]{2})([0-9a-f]{2})([0-9a-f]{2})$/i);
  if (m6) return [parseInt(m6[1], 16), parseInt(m6[2], 16), parseInt(m6[3], 16)];
  return null;
}
function toHex(n) {
  return Math.max(0, Math.min(255, Math.round(n))).toString(16).padStart(2, "0");
}
function mixHex(a, b, t) {
  const ca = parseHex2(a), cb = parseHex2(b);
  if (!ca || !cb) return a;
  return `#${toHex(ca[0] + (cb[0] - ca[0]) * t)}${toHex(ca[1] + (cb[1] - ca[1]) * t)}${toHex(ca[2] + (cb[2] - ca[2]) * t)}`;
}
function peg$subclass22(child, parent) {
  function C() {
    this.constructor = child;
  }
  C.prototype = parent.prototype;
  child.prototype = new C();
}
function peg$SyntaxError22(message, expected, found, location) {
  var self = Error.call(this, message);
  if (Object.setPrototypeOf) {
    Object.setPrototypeOf(self, peg$SyntaxError22.prototype);
  }
  self.expected = expected;
  self.found = found;
  self.location = location;
  self.name = "SyntaxError";
  return self;
}
peg$subclass22(peg$SyntaxError22, Error);
function peg$padEnd22(str, targetLength, padString) {
  padString = padString || " ";
  if (str.length > targetLength) {
    return str;
  }
  targetLength -= str.length;
  padString += padString.repeat(targetLength);
  return str + padString.slice(0, targetLength);
}
peg$SyntaxError22.prototype.format = function(sources) {
  var str = "Error: " + this.message;
  if (this.location) {
    var src = null;
    var k;
    for (k = 0; k < sources.length; k++) {
      if (sources[k].source === this.location.source) {
        src = sources[k].text.split(/\r\n|\n|\r/g);
        break;
      }
    }
    var s = this.location.start;
    var offset_s = this.location.source && typeof this.location.source.offset === "function" ? this.location.source.offset(s) : s;
    var loc = this.location.source + ":" + offset_s.line + ":" + offset_s.column;
    if (src) {
      var e = this.location.end;
      var filler = peg$padEnd22("", offset_s.line.toString().length, " ");
      var line = src[s.line - 1];
      var last = s.line === e.line ? e.column : line.length + 1;
      var hatLen = last - s.column || 1;
      str += "\n --> " + loc + "\n" + filler + " |\n" + offset_s.line + " | " + line + "\n" + filler + " | " + peg$padEnd22("", s.column - 1, " ") + peg$padEnd22("", hatLen, "^");
    } else {
      str += "\n at " + loc;
    }
  }
  return str;
};
peg$SyntaxError22.buildMessage = function(expected, found) {
  var DESCRIBE_EXPECTATION_FNS = {
    literal: function(expectation) {
      return '"' + literalEscape(expectation.text) + '"';
    },
    class: function(expectation) {
      var escapedParts = expectation.parts.map(function(part) {
        return Array.isArray(part) ? classEscape(part[0]) + "-" + classEscape(part[1]) : classEscape(part);
      });
      return "[" + (expectation.inverted ? "^" : "") + escapedParts.join("") + "]";
    },
    any: function() {
      return "any character";
    },
    end: function() {
      return "end of input";
    },
    other: function(expectation) {
      return expectation.description;
    }
  };
  function hex(ch) {
    return ch.charCodeAt(0).toString(16).toUpperCase();
  }
  function literalEscape(s) {
    return s.replace(/\\/g, "\\\\").replace(/"/g, '\\"').replace(/\0/g, "\\0").replace(/\t/g, "\\t").replace(/\n/g, "\\n").replace(/\r/g, "\\r").replace(/[\x00-\x0F]/g, function(ch) {
      return "\\x0" + hex(ch);
    }).replace(/[\x10-\x1F\x7F-\x9F]/g, function(ch) {
      return "\\x" + hex(ch);
    });
  }
  function classEscape(s) {
    return s.replace(/\\/g, "\\\\").replace(/\]/g, "\\]").replace(/\^/g, "\\^").replace(/-/g, "\\-").replace(/\0/g, "\\0").replace(/\t/g, "\\t").replace(/\n/g, "\\n").replace(/\r/g, "\\r").replace(/[\x00-\x0F]/g, function(ch) {
      return "\\x0" + hex(ch);
    }).replace(/[\x10-\x1F\x7F-\x9F]/g, function(ch) {
      return "\\x" + hex(ch);
    });
  }
  function describeExpectation(expectation) {
    return DESCRIBE_EXPECTATION_FNS[expectation.type](expectation);
  }
  function describeExpected(expected2) {
    var descriptions = expected2.map(describeExpectation);
    var i, j;
    descriptions.sort();
    if (descriptions.length > 0) {
      for (i = 1, j = 1; i < descriptions.length; i++) {
        if (descriptions[i - 1] !== descriptions[i]) {
          descriptions[j] = descriptions[i];
          j++;
        }
      }
      descriptions.length = j;
    }
    switch (descriptions.length) {
      case 1:
        return descriptions[0];
      case 2:
        return descriptions[0] + " or " + descriptions[1];
      default:
        return descriptions.slice(0, -1).join(", ") + ", or " + descriptions[descriptions.length - 1];
    }
  }
  function describeFound(found2) {
    return found2 ? '"' + literalEscape(found2) + '"' : "end of input";
  }
  return "Expected " + describeExpected(expected) + " but " + describeFound(found) + " found.";
};
function peg$parse22(input, options) {
  options = options !== void 0 ? options : {};
  var peg$FAILED = {};
  var peg$source = options.grammarSource;
  var peg$startRuleFunctions = { Document: peg$parseDocument };
  var peg$startRuleFunction = peg$parseDocument;
  var peg$c0 = "---";
  var peg$c1 = "packet-beta";
  var peg$c2 = "packet";
  var peg$c3 = "title";
  var peg$c4 = ":";
  var peg$c5 = "-";
  var peg$c6 = '"';
  var peg$c7 = "'";
  var peg$c8 = "%%";
  var peg$c9 = "\r\n";
  var peg$r0 = /^[^\n]/;
  var peg$r1 = /^[^"]/;
  var peg$r2 = /^[^']/;
  var peg$r3 = /^[^\r\n]/;
  var peg$r4 = /^[0-9]/;
  var peg$r5 = /^[\n\r]/;
  var peg$r6 = /^[ \t]/;
  var peg$e0 = peg$literalExpectation("---", false);
  var peg$e1 = peg$classExpectation(["\n"], true, false);
  var peg$e2 = peg$literalExpectation("packet-beta", true);
  var peg$e3 = peg$literalExpectation("packet", true);
  var peg$e4 = peg$literalExpectation("title", true);
  var peg$e5 = peg$literalExpectation(":", false);
  var peg$e6 = peg$literalExpectation("-", false);
  var peg$e7 = peg$literalExpectation('"', false);
  var peg$e8 = peg$classExpectation(['"'], true, false);
  var peg$e9 = peg$literalExpectation("'", false);
  var peg$e10 = peg$classExpectation(["'"], true, false);
  var peg$e11 = peg$classExpectation(["\r", "\n"], true, false);
  var peg$e12 = peg$classExpectation([["0", "9"]], false, false);
  var peg$e13 = peg$literalExpectation("%%", false);
  var peg$e14 = peg$literalExpectation("\r\n", false);
  var peg$e15 = peg$classExpectation(["\n", "\r"], false, false);
  var peg$e16 = peg$anyExpectation();
  var peg$e17 = peg$classExpectation([" ", "	"], false, false);
  var peg$f0 = function(fm, stmts) {
    let title;
    const fields = [];
    for (const s of stmts.filter(Boolean)) {
      if (s.t === "title") title = s.v;
      else if (s.t === "field") {
        const sMin = Math.min(s.start, s.end);
        const sMax = Math.max(s.start, s.end);
        fields.push({ start: sMin, end: sMax, label: s.label });
      }
    }
    return { version: "1.0", metadata: { ...fm || {}, ...title ? { title } : {} }, fields };
  };
  var peg$f1 = function(line) {
    return line;
  };
  var peg$f2 = function(lines2) {
    const meta = {};
    for (const line of lines2) {
      const m = line.match(/^\s*(\w+)\s*:\s*(.+?)\s*$/);
      if (m) meta[m[1]] = m[2];
    }
    return meta;
  };
  var peg$f3 = function(v) {
    return { t: "title", v: v.trim() };
  };
  var peg$f4 = function(start, e) {
    return e;
  };
  var peg$f5 = function(start, end, label) {
    return { t: "field", start, end: end != null ? end : start, label };
  };
  var peg$f6 = function(s) {
    return s.trim();
  };
  var peg$f7 = function(s) {
    return s.trim();
  };
  var peg$f8 = function(s) {
    return s.trim();
  };
  var peg$f9 = function(n) {
    return parseInt(n, 10);
  };
  var peg$f10 = function() {
    return null;
  };
  var peg$currPos = options.peg$currPos | 0;
  var peg$savedPos = peg$currPos;
  var peg$posDetailsCache = [{ line: 1, column: 1 }];
  var peg$maxFailPos = peg$currPos;
  var peg$maxFailExpected = options.peg$maxFailExpected || [];
  var peg$silentFails = options.peg$silentFails | 0;
  var peg$result;
  if (options.startRule) {
    if (!(options.startRule in peg$startRuleFunctions)) {
      throw new Error(`Can't start parsing from rule "` + options.startRule + '".');
    }
    peg$startRuleFunction = peg$startRuleFunctions[options.startRule];
  }
  function text() {
    return input.substring(peg$savedPos, peg$currPos);
  }
  function offset() {
    return peg$savedPos;
  }
  function range() {
    return {
      source: peg$source,
      start: peg$savedPos,
      end: peg$currPos
    };
  }
  function location() {
    return peg$computeLocation(peg$savedPos, peg$currPos);
  }
  function expected(description, location2) {
    location2 = location2 !== void 0 ? location2 : peg$computeLocation(peg$savedPos, peg$currPos);
    throw peg$buildStructuredError(
      [peg$otherExpectation(description)],
      input.substring(peg$savedPos, peg$currPos),
      location2
    );
  }
  function error(message, location2) {
    location2 = location2 !== void 0 ? location2 : peg$computeLocation(peg$savedPos, peg$currPos);
    throw peg$buildSimpleError(message, location2);
  }
  function peg$literalExpectation(text2, ignoreCase) {
    return { type: "literal", text: text2, ignoreCase };
  }
  function peg$classExpectation(parts, inverted, ignoreCase) {
    return { type: "class", parts, inverted, ignoreCase };
  }
  function peg$anyExpectation() {
    return { type: "any" };
  }
  function peg$endExpectation() {
    return { type: "end" };
  }
  function peg$otherExpectation(description) {
    return { type: "other", description };
  }
  function peg$computePosDetails(pos) {
    var details = peg$posDetailsCache[pos];
    var p;
    if (details) {
      return details;
    } else {
      if (pos >= peg$posDetailsCache.length) {
        p = peg$posDetailsCache.length - 1;
      } else {
        p = pos;
        while (!peg$posDetailsCache[--p]) {
        }
      }
      details = peg$posDetailsCache[p];
      details = {
        line: details.line,
        column: details.column
      };
      while (p < pos) {
        if (input.charCodeAt(p) === 10) {
          details.line++;
          details.column = 1;
        } else {
          details.column++;
        }
        p++;
      }
      peg$posDetailsCache[pos] = details;
      return details;
    }
  }
  function peg$computeLocation(startPos, endPos, offset2) {
    var startPosDetails = peg$computePosDetails(startPos);
    var endPosDetails = peg$computePosDetails(endPos);
    var res = {
      source: peg$source,
      start: {
        offset: startPos,
        line: startPosDetails.line,
        column: startPosDetails.column
      },
      end: {
        offset: endPos,
        line: endPosDetails.line,
        column: endPosDetails.column
      }
    };
    if (offset2 && peg$source && typeof peg$source.offset === "function") {
      res.start = peg$source.offset(res.start);
      res.end = peg$source.offset(res.end);
    }
    return res;
  }
  function peg$fail(expected2) {
    if (peg$currPos < peg$maxFailPos) {
      return;
    }
    if (peg$currPos > peg$maxFailPos) {
      peg$maxFailPos = peg$currPos;
      peg$maxFailExpected = [];
    }
    peg$maxFailExpected.push(expected2);
  }
  function peg$buildSimpleError(message, location2) {
    return new peg$SyntaxError22(message, null, null, location2);
  }
  function peg$buildStructuredError(expected2, found, location2) {
    return new peg$SyntaxError22(
      peg$SyntaxError22.buildMessage(expected2, found),
      expected2,
      found,
      location2
    );
  }
  function peg$parseDocument() {
    var s0, s1, s2, s3, s4, s5, s6, s7;
    s0 = peg$currPos;
    s1 = peg$parseFrontmatter();
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    s2 = [];
    s3 = peg$parseBlankLine();
    while (s3 !== peg$FAILED) {
      s2.push(s3);
      s3 = peg$parseBlankLine();
    }
    s3 = peg$parse_();
    s4 = peg$parsePacketHeader();
    if (s4 !== peg$FAILED) {
      s5 = peg$parseLineEnd();
      if (s5 !== peg$FAILED) {
        s6 = [];
        s7 = peg$parseStatement();
        while (s7 !== peg$FAILED) {
          s6.push(s7);
          s7 = peg$parseStatement();
        }
        s7 = peg$parse_();
        peg$savedPos = s0;
        s0 = peg$f0(s1, s6);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseFrontmatter() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 3) === peg$c0) {
      s1 = peg$c0;
      peg$currPos += 3;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e0);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$parseNL();
      if (s2 !== peg$FAILED) {
        s3 = [];
        s4 = peg$currPos;
        s5 = peg$currPos;
        peg$silentFails++;
        s6 = peg$currPos;
        if (input.substr(peg$currPos, 3) === peg$c0) {
          s7 = peg$c0;
          peg$currPos += 3;
        } else {
          s7 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e0);
          }
        }
        if (s7 !== peg$FAILED) {
          s8 = peg$parseNL();
          if (s8 !== peg$FAILED) {
            s7 = [s7, s8];
            s6 = s7;
          } else {
            peg$currPos = s6;
            s6 = peg$FAILED;
          }
        } else {
          peg$currPos = s6;
          s6 = peg$FAILED;
        }
        peg$silentFails--;
        if (s6 === peg$FAILED) {
          s5 = void 0;
        } else {
          peg$currPos = s5;
          s5 = peg$FAILED;
        }
        if (s5 !== peg$FAILED) {
          s6 = peg$currPos;
          s7 = [];
          s8 = input.charAt(peg$currPos);
          if (peg$r0.test(s8)) {
            peg$currPos++;
          } else {
            s8 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e1);
            }
          }
          while (s8 !== peg$FAILED) {
            s7.push(s8);
            s8 = input.charAt(peg$currPos);
            if (peg$r0.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e1);
              }
            }
          }
          s6 = input.substring(s6, peg$currPos);
          s7 = peg$parseNL();
          if (s7 !== peg$FAILED) {
            peg$savedPos = s4;
            s4 = peg$f1(s6);
          } else {
            peg$currPos = s4;
            s4 = peg$FAILED;
          }
        } else {
          peg$currPos = s4;
          s4 = peg$FAILED;
        }
        while (s4 !== peg$FAILED) {
          s3.push(s4);
          s4 = peg$currPos;
          s5 = peg$currPos;
          peg$silentFails++;
          s6 = peg$currPos;
          if (input.substr(peg$currPos, 3) === peg$c0) {
            s7 = peg$c0;
            peg$currPos += 3;
          } else {
            s7 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e0);
            }
          }
          if (s7 !== peg$FAILED) {
            s8 = peg$parseNL();
            if (s8 !== peg$FAILED) {
              s7 = [s7, s8];
              s6 = s7;
            } else {
              peg$currPos = s6;
              s6 = peg$FAILED;
            }
          } else {
            peg$currPos = s6;
            s6 = peg$FAILED;
          }
          peg$silentFails--;
          if (s6 === peg$FAILED) {
            s5 = void 0;
          } else {
            peg$currPos = s5;
            s5 = peg$FAILED;
          }
          if (s5 !== peg$FAILED) {
            s6 = peg$currPos;
            s7 = [];
            s8 = input.charAt(peg$currPos);
            if (peg$r0.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e1);
              }
            }
            while (s8 !== peg$FAILED) {
              s7.push(s8);
              s8 = input.charAt(peg$currPos);
              if (peg$r0.test(s8)) {
                peg$currPos++;
              } else {
                s8 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e1);
                }
              }
            }
            s6 = input.substring(s6, peg$currPos);
            s7 = peg$parseNL();
            if (s7 !== peg$FAILED) {
              peg$savedPos = s4;
              s4 = peg$f1(s6);
            } else {
              peg$currPos = s4;
              s4 = peg$FAILED;
            }
          } else {
            peg$currPos = s4;
            s4 = peg$FAILED;
          }
        }
        if (input.substr(peg$currPos, 3) === peg$c0) {
          s4 = peg$c0;
          peg$currPos += 3;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e0);
          }
        }
        if (s4 !== peg$FAILED) {
          s5 = peg$parseNL();
          if (s5 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f2(s3);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parsePacketHeader() {
    var s0;
    s0 = input.substr(peg$currPos, 11);
    if (s0.toLowerCase() === peg$c1) {
      peg$currPos += 11;
    } else {
      s0 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e2);
      }
    }
    if (s0 === peg$FAILED) {
      s0 = input.substr(peg$currPos, 6);
      if (s0.toLowerCase() === peg$c2) {
        peg$currPos += 6;
      } else {
        s0 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e3);
        }
      }
    }
    return s0;
  }
  function peg$parseStatement() {
    var s0;
    s0 = peg$parseTitleLine();
    if (s0 === peg$FAILED) {
      s0 = peg$parseFieldLine();
      if (s0 === peg$FAILED) {
        s0 = peg$parseBlankLine();
      }
    }
    return s0;
  }
  function peg$parseTitleLine() {
    var s0, s1, s2, s3, s4, s5, s6;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = input.substr(peg$currPos, 5);
    if (s2.toLowerCase() === peg$c3) {
      peg$currPos += 5;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e4);
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$parse_();
      s4 = peg$currPos;
      if (input.charCodeAt(peg$currPos) === 58) {
        s5 = peg$c4;
        peg$currPos++;
      } else {
        s5 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e5);
        }
      }
      if (s5 !== peg$FAILED) {
        s6 = peg$parse_();
        s5 = [s5, s6];
        s4 = s5;
      } else {
        peg$currPos = s4;
        s4 = peg$FAILED;
      }
      if (s4 === peg$FAILED) {
        s4 = null;
      }
      s5 = peg$parseRestOfLine();
      if (s5 !== peg$FAILED) {
        s6 = peg$parseLineEnd();
        if (s6 !== peg$FAILED) {
          peg$savedPos = s0;
          s0 = peg$f3(s5);
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseFieldLine() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8, s9;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = peg$parseInt();
    if (s2 !== peg$FAILED) {
      s3 = peg$currPos;
      s4 = peg$parse_();
      if (input.charCodeAt(peg$currPos) === 45) {
        s5 = peg$c5;
        peg$currPos++;
      } else {
        s5 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e6);
        }
      }
      if (s5 !== peg$FAILED) {
        s6 = peg$parse_();
        s7 = peg$parseInt();
        if (s7 !== peg$FAILED) {
          peg$savedPos = s3;
          s3 = peg$f4(s2, s7);
        } else {
          peg$currPos = s3;
          s3 = peg$FAILED;
        }
      } else {
        peg$currPos = s3;
        s3 = peg$FAILED;
      }
      if (s3 === peg$FAILED) {
        s3 = null;
      }
      s4 = peg$parse_();
      if (input.charCodeAt(peg$currPos) === 58) {
        s5 = peg$c4;
        peg$currPos++;
      } else {
        s5 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e5);
        }
      }
      if (s5 !== peg$FAILED) {
        s6 = peg$parse_();
        s7 = peg$parseQuoted();
        if (s7 !== peg$FAILED) {
          s8 = peg$parse_();
          s9 = peg$parseLineEnd();
          if (s9 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f5(s2, s3, s7);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseQuoted() {
    var s0, s1, s2, s3, s4;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 34) {
      s1 = peg$c6;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e7);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$currPos;
      s3 = [];
      s4 = input.charAt(peg$currPos);
      if (peg$r1.test(s4)) {
        peg$currPos++;
      } else {
        s4 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e8);
        }
      }
      while (s4 !== peg$FAILED) {
        s3.push(s4);
        s4 = input.charAt(peg$currPos);
        if (peg$r1.test(s4)) {
          peg$currPos++;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e8);
          }
        }
      }
      s2 = input.substring(s2, peg$currPos);
      if (input.charCodeAt(peg$currPos) === 34) {
        s3 = peg$c6;
        peg$currPos++;
      } else {
        s3 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e7);
        }
      }
      if (s3 !== peg$FAILED) {
        peg$savedPos = s0;
        s0 = peg$f6(s2);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      if (input.charCodeAt(peg$currPos) === 39) {
        s1 = peg$c7;
        peg$currPos++;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e9);
        }
      }
      if (s1 !== peg$FAILED) {
        s2 = peg$currPos;
        s3 = [];
        s4 = input.charAt(peg$currPos);
        if (peg$r2.test(s4)) {
          peg$currPos++;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e10);
          }
        }
        while (s4 !== peg$FAILED) {
          s3.push(s4);
          s4 = input.charAt(peg$currPos);
          if (peg$r2.test(s4)) {
            peg$currPos++;
          } else {
            s4 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e10);
            }
          }
        }
        s2 = input.substring(s2, peg$currPos);
        if (input.charCodeAt(peg$currPos) === 39) {
          s3 = peg$c7;
          peg$currPos++;
        } else {
          s3 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e9);
          }
        }
        if (s3 !== peg$FAILED) {
          peg$savedPos = s0;
          s0 = peg$f7(s2);
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
      if (s0 === peg$FAILED) {
        s0 = peg$currPos;
        s1 = peg$currPos;
        s2 = [];
        s3 = input.charAt(peg$currPos);
        if (peg$r3.test(s3)) {
          peg$currPos++;
        } else {
          s3 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e11);
          }
        }
        if (s3 !== peg$FAILED) {
          while (s3 !== peg$FAILED) {
            s2.push(s3);
            s3 = input.charAt(peg$currPos);
            if (peg$r3.test(s3)) {
              peg$currPos++;
            } else {
              s3 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e11);
              }
            }
          }
        } else {
          s2 = peg$FAILED;
        }
        if (s2 !== peg$FAILED) {
          s1 = input.substring(s1, peg$currPos);
        } else {
          s1 = s2;
        }
        if (s1 !== peg$FAILED) {
          peg$savedPos = s0;
          s1 = peg$f8(s1);
        }
        s0 = s1;
      }
    }
    return s0;
  }
  function peg$parseInt() {
    var s0, s1, s2, s3;
    s0 = peg$currPos;
    s1 = peg$currPos;
    s2 = [];
    s3 = input.charAt(peg$currPos);
    if (peg$r4.test(s3)) {
      peg$currPos++;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e12);
      }
    }
    if (s3 !== peg$FAILED) {
      while (s3 !== peg$FAILED) {
        s2.push(s3);
        s3 = input.charAt(peg$currPos);
        if (peg$r4.test(s3)) {
          peg$currPos++;
        } else {
          s3 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e12);
          }
        }
      }
    } else {
      s2 = peg$FAILED;
    }
    if (s2 !== peg$FAILED) {
      s1 = input.substring(s1, peg$currPos);
    } else {
      s1 = s2;
    }
    if (s1 !== peg$FAILED) {
      peg$savedPos = s0;
      s1 = peg$f9(s1);
    }
    s0 = s1;
    return s0;
  }
  function peg$parseRestOfLine() {
    var s0, s1, s2;
    s0 = peg$currPos;
    s1 = [];
    s2 = input.charAt(peg$currPos);
    if (peg$r3.test(s2)) {
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e11);
      }
    }
    if (s2 !== peg$FAILED) {
      while (s2 !== peg$FAILED) {
        s1.push(s2);
        s2 = input.charAt(peg$currPos);
        if (peg$r3.test(s2)) {
          peg$currPos++;
        } else {
          s2 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e11);
          }
        }
      }
    } else {
      s1 = peg$FAILED;
    }
    if (s1 !== peg$FAILED) {
      s0 = input.substring(s0, peg$currPos);
    } else {
      s0 = s1;
    }
    return s0;
  }
  function peg$parseBlankLine() {
    var s0, s1, s2, s3;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = peg$parseComment();
    if (s2 === peg$FAILED) {
      s2 = null;
    }
    s3 = peg$parseNL();
    if (s3 !== peg$FAILED) {
      peg$savedPos = s0;
      s0 = peg$f10();
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseComment() {
    var s0, s1, s2, s3;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 2) === peg$c8) {
      s1 = peg$c8;
      peg$currPos += 2;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e13);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = [];
      s3 = input.charAt(peg$currPos);
      if (peg$r3.test(s3)) {
        peg$currPos++;
      } else {
        s3 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e11);
        }
      }
      while (s3 !== peg$FAILED) {
        s2.push(s3);
        s3 = input.charAt(peg$currPos);
        if (peg$r3.test(s3)) {
          peg$currPos++;
        } else {
          s3 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e11);
          }
        }
      }
      s1 = [s1, s2];
      s0 = s1;
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseNL() {
    var s0;
    if (input.substr(peg$currPos, 2) === peg$c9) {
      s0 = peg$c9;
      peg$currPos += 2;
    } else {
      s0 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e14);
      }
    }
    if (s0 === peg$FAILED) {
      s0 = input.charAt(peg$currPos);
      if (peg$r5.test(s0)) {
        peg$currPos++;
      } else {
        s0 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e15);
        }
      }
    }
    return s0;
  }
  function peg$parseLineEnd() {
    var s0, s1, s2, s3, s4;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = peg$parseComment();
    if (s2 === peg$FAILED) {
      s2 = null;
    }
    s3 = peg$parseNL();
    if (s3 === peg$FAILED) {
      s3 = peg$currPos;
      peg$silentFails++;
      if (input.length > peg$currPos) {
        s4 = input.charAt(peg$currPos);
        peg$currPos++;
      } else {
        s4 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e16);
        }
      }
      peg$silentFails--;
      if (s4 === peg$FAILED) {
        s3 = void 0;
      } else {
        peg$currPos = s3;
        s3 = peg$FAILED;
      }
    }
    if (s3 !== peg$FAILED) {
      s1 = [s1, s2, s3];
      s0 = s1;
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parse_() {
    var s0, s1;
    s0 = [];
    s1 = input.charAt(peg$currPos);
    if (peg$r6.test(s1)) {
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e17);
      }
    }
    while (s1 !== peg$FAILED) {
      s0.push(s1);
      s1 = input.charAt(peg$currPos);
      if (peg$r6.test(s1)) {
        peg$currPos++;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e17);
        }
      }
    }
    return s0;
  }
  function peg$parse__() {
    var s0, s1;
    s0 = [];
    s1 = input.charAt(peg$currPos);
    if (peg$r6.test(s1)) {
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e17);
      }
    }
    if (s1 !== peg$FAILED) {
      while (s1 !== peg$FAILED) {
        s0.push(s1);
        s1 = input.charAt(peg$currPos);
        if (peg$r6.test(s1)) {
          peg$currPos++;
        } else {
          s1 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e17);
          }
        }
      }
    } else {
      s0 = peg$FAILED;
    }
    return s0;
  }
  peg$result = peg$startRuleFunction();
  if (options.peg$library) {
    return (
      /** @type {any} */
      {
        peg$result,
        peg$currPos,
        peg$FAILED,
        peg$maxFailExpected,
        peg$maxFailPos
      }
    );
  }
  if (peg$result !== peg$FAILED && peg$currPos === input.length) {
    return peg$result;
  } else {
    if (peg$result !== peg$FAILED && peg$currPos < input.length) {
      peg$fail(peg$endExpectation());
    }
    throw peg$buildStructuredError(
      peg$maxFailExpected,
      peg$maxFailPos < input.length ? input.charAt(peg$maxFailPos) : null,
      peg$maxFailPos < input.length ? peg$computeLocation(peg$maxFailPos, peg$maxFailPos + 1) : peg$computeLocation(peg$maxFailPos, peg$maxFailPos)
    );
  }
}
var packet = {
  parseMermaid(input) {
    return peg$parse22(input);
  },
  parseYaml(input) {
    return JSON.parse(input);
  },
  layout(ir, theme) {
    return layoutPacket(ir, theme);
  }
};
function treeLayout(nodes, options = {}) {
  const direction = options.direction ?? "TB";
  const levelGap = options.levelGap ?? 56;
  const siblingGap = options.siblingGap ?? 28;
  const margin = options.margin ?? 32;
  const horizontal = direction === "LR";
  if (nodes.length === 0) return { boxes: /* @__PURE__ */ new Map(), width: margin * 2, height: margin * 2 };
  const byId = new Map(nodes.map((n) => [n.id, n]));
  const crossOf = (n) => horizontal ? n.height : n.width;
  const alongOf = (n) => horizontal ? n.width : n.height;
  const childIds = /* @__PURE__ */ new Set();
  for (const n of nodes) for (const c of n.children) childIds.add(c);
  const roots = nodes.filter((n) => !childIds.has(n.id));
  const depth = /* @__PURE__ */ new Map();
  const queue2 = roots.map((r) => {
    depth.set(r.id, 0);
    return r.id;
  });
  for (let i = 0; i < queue2.length; i++) {
    const id = queue2[i];
    const d = depth.get(id);
    for (const c of byId.get(id).children) {
      if (!depth.has(c)) {
        depth.set(c, d + 1);
        queue2.push(c);
      }
    }
  }
  const maxDepth2 = Math.max(...depth.values());
  const levelAlong = Array.from({ length: maxDepth2 + 1 }, () => 0);
  for (const n of nodes) {
    const d = depth.get(n.id);
    levelAlong[d] = Math.max(levelAlong[d], alongOf(n));
  }
  const levelStart = [];
  let acc = margin;
  for (let d = 0; d <= maxDepth2; d++) {
    levelStart[d] = acc;
    acc += levelAlong[d] + levelGap;
  }
  const alongTotal = acc - levelGap + margin;
  const subCross = /* @__PURE__ */ new Map();
  const computeCross = (id) => {
    const cached = subCross.get(id);
    if (cached !== void 0) return cached;
    const node = byId.get(id);
    let own = crossOf(node);
    if (node.children.length > 0) {
      const childrenCross = node.children.reduce(
        (s, c, i) => s + computeCross(c) + (i > 0 ? siblingGap : 0),
        0
      );
      own = Math.max(own, childrenCross);
    }
    subCross.set(id, own);
    return own;
  };
  const crossPos = /* @__PURE__ */ new Map();
  const assign = (id, u0) => {
    const node = byId.get(id);
    const sc = computeCross(id);
    const center = u0 + sc / 2;
    crossPos.set(id, center - crossOf(node) / 2);
    if (node.children.length === 0) return;
    const childrenCross = node.children.reduce(
      (s, c, i) => s + computeCross(c) + (i > 0 ? siblingGap : 0),
      0
    );
    let cursor2 = u0 + (sc - childrenCross) / 2;
    for (const c of node.children) {
      assign(c, cursor2);
      cursor2 += computeCross(c) + siblingGap;
    }
  };
  let cursor = margin;
  for (const r of roots) {
    assign(r.id, cursor);
    cursor += computeCross(r.id) + siblingGap;
  }
  const crossTotal = cursor - siblingGap + margin;
  const boxes = /* @__PURE__ */ new Map();
  for (const n of nodes) {
    const d = depth.get(n.id);
    const u = crossPos.get(n.id);
    const v = levelStart[d] + (levelAlong[d] - alongOf(n)) / 2;
    const x = horizontal ? v : u;
    const y = horizontal ? u : v;
    boxes.set(n.id, { id: n.id, x, y, width: n.width, height: n.height });
  }
  return {
    boxes,
    width: horizontal ? alongTotal : crossTotal,
    height: horizontal ? crossTotal : alongTotal
  };
}
var RB_RED = "#d64545";
function stripCells(label, font) {
  return label.split("|").map((s) => s.trim()).map((key) => ({
    key,
    width: Math.max(measureText(key, font).width + 18, 34)
  }));
}
function parseHex3(c) {
  const s = c.trim();
  const m3 = s.match(/^#([0-9a-f])([0-9a-f])([0-9a-f])$/i);
  if (m3)
    return [
      parseInt(m3[1] + m3[1], 16),
      parseInt(m3[2] + m3[2], 16),
      parseInt(m3[3] + m3[3], 16)
    ];
  const m6 = s.match(/^#([0-9a-f]{2})([0-9a-f]{2})([0-9a-f]{2})$/i);
  if (m6) return [parseInt(m6[1], 16), parseInt(m6[2], 16), parseInt(m6[3], 16)];
  return null;
}
function toHex2(n) {
  return Math.max(0, Math.min(255, Math.round(n))).toString(16).padStart(2, "0");
}
function mixHex2(a, b, t) {
  const ca = parseHex3(a), cb = parseHex3(b);
  if (!ca || !cb) return a;
  const r = ca[0] + (cb[0] - ca[0]) * t;
  const g = ca[1] + (cb[1] - ca[1]) * t;
  const bl = ca[2] + (cb[2] - ca[2]) * t;
  return `#${toHex2(r)}${toHex2(g)}${toHex2(bl)}`;
}
function canvasColor(theme) {
  return parseHex3(theme.palette.background) ? theme.palette.background : theme.palette.surface;
}
function isDarkTheme2(theme) {
  const lum = relativeLuminance(theme.palette.background) ?? relativeLuminance(theme.palette.surface);
  return lum !== void 0 ? lum < 0.5 : false;
}
function readableText2(fill, theme) {
  const themed = bestContrast(
    fill,
    [theme.palette.text, theme.palette.background],
    theme.palette.text
  );
  if (contrastRatio(themed, fill) >= 4.5) return themed;
  return bestContrast(fill, ["#ffffff", "#000000"], themed);
}
function outlineStroke(fill, theme) {
  const canvas = canvasColor(theme);
  const candidates = [
    theme.palette.text,
    theme.palette.border,
    theme.palette.textMuted,
    theme.palette.background
  ];
  let best = theme.palette.text, score = -Infinity;
  for (const c of candidates) {
    if (!parseHex3(c)) continue;
    const next = Math.min(contrastRatio(c, fill), contrastRatio(c, canvas)) + contrastRatio(c, canvas) * 0.15;
    if (next > score) {
      best = c;
      score = next;
    }
  }
  return best;
}
function isRedHue(c) {
  const rgb = parseHex3(c);
  return rgb !== null && rgb[0] > rgb[1] * 1.35 && rgb[0] > rgb[2] * 1.35;
}
function redNodeFill(theme) {
  const { palette } = theme;
  if (isRedHue(palette.error)) return mixHex2(RB_RED, palette.error, isDarkTheme2(theme) ? 0.7 : 0.6);
  return mixHex2(RB_RED, palette.text, isDarkTheme2(theme) ? 0.12 : 0.08);
}
function blackNodeFill(theme) {
  const { palette } = theme;
  if (!isDarkTheme2(theme)) return mixHex2(palette.text, palette.background, 0.08);
  const canvas = canvasColor(theme);
  for (const source of [palette.text, palette.surface, palette.border, "#ffffff"]) {
    for (const t of [0.22, 0.26, 0.3, 0.34]) {
      const fill = mixHex2(canvas, source, t);
      const lum = relativeLuminance(fill);
      if (lum !== void 0 && lum < 0.32 && contrastRatio(fill, canvas) >= 1.7) return fill;
    }
  }
  return mixHex2(canvas, "#ffffff", 0.28);
}
function nodeStyle(kinds, theme) {
  const { palette } = theme;
  const has = (k) => kinds.includes(k);
  const shape = has("strip") ? "strip" : has("leaf") || has("pill") ? "pill" : has("circle") || has("red") || has("black") || has("dot") ? "circle" : "rect";
  if (has("red")) {
    const fill2 = redNodeFill(theme);
    return { shape, fill: fill2, stroke: outlineStroke(fill2, theme), text: readableText2(fill2, theme) };
  }
  if (has("black")) {
    const fill2 = blackNodeFill(theme);
    return { shape, fill: fill2, stroke: outlineStroke(fill2, theme), text: readableText2(fill2, theme) };
  }
  if (has("active") || has("primary") || has("focal"))
    return {
      shape,
      fill: palette.primary,
      stroke: palette.primary,
      text: readableText2(palette.primary, theme)
    };
  if (has("scan"))
    return { shape, fill: palette.surface, stroke: palette.primary, text: palette.text };
  if (has("join"))
    return { shape, fill: palette.surface, stroke: palette.secondary, text: palette.text };
  if (has("build") || has("muted"))
    return { shape, fill: palette.surface, stroke: palette.textMuted, text: palette.text };
  const fill = palette.surface;
  return { shape, fill, stroke: palette.border, text: palette.text };
}
function infoLine(node) {
  return node.info;
}
function badgeColor(badge, theme) {
  const n = Number(badge);
  if (!Number.isNaN(n)) {
    if (n === 0) return theme.palette.success;
    if (Math.abs(n) === 1) return theme.palette.primary;
    return theme.palette.error;
  }
  return theme.palette.primary;
}
function layoutTree(ir, theme) {
  const { palette, typography, spacing } = theme;
  const p = pen(theme);
  const margin = spacing.diagramMargin;
  const font = typography.baseFontSize;
  const smallFont = typography.smallFontSize;
  if (ir.nodes.length === 0) {
    const scene2 = {
      viewBox: { x: 0, y: 0, width: 120, height: 80 },
      background: palette.background,
      elements: []
    };
    return { scene: scene2, anchors: {} };
  }
  const style = /* @__PURE__ */ new Map();
  const sizes = /* @__PURE__ */ new Map();
  for (const node of ir.nodes) {
    const st = nodeStyle(node.kinds, theme);
    style.set(node.id, st);
    const info = infoLine(node);
    const labelW = measureText(node.label, font).width;
    const infoW = info ? measureText(info, smallFont).width : 0;
    const contentW = Math.max(labelW, infoW);
    if (st.shape === "strip") {
      const width = stripCells(node.label, font).reduce((s, c) => s + c.width, 0);
      sizes.set(node.id, { width, height: font + 20 });
    } else if (st.shape === "circle") {
      const side = info ? Math.max(contentW + 24, font + smallFont + 32, 48) : Math.max(contentW + 18, font + 24, 42);
      sizes.set(node.id, { width: side, height: side });
    } else {
      const width = contentW + 28;
      const height = info ? font + smallFont + 24 : font + 22;
      sizes.set(node.id, { width, height });
    }
  }
  const isCompact = ir.metadata["compact"] === true || ir.metadata["layout"] === "compact" || String(ir.metadata["compact"]) === "true";
  const inputs = ir.nodes.map((n) => ({
    id: n.id,
    width: sizes.get(n.id).width,
    height: sizes.get(n.id).height,
    children: n.children
  }));
  const placed = treeLayout(inputs, {
    direction: ir.direction,
    levelGap: isCompact ? 38 : 52,
    siblingGap: isCompact ? 12 : 28,
    margin
  });
  const titleText = ir.metadata["title"] ? String(ir.metadata["title"]) : void 0;
  const titleW = titleText ? measureText(titleText, typography.titleFontSize).width : 0;
  const titleH = titleText ? typography.titleFontSize + 14 : 0;
  const box = (id) => {
    const b = placed.boxes.get(id);
    return { x: b.x, y: b.y + titleH, width: b.width, height: b.height };
  };
  const elements = [];
  if (titleText) {
    elements.push(
      p.text(
        titleText,
        margin,
        margin + typography.titleFontSize,
        typography.titleFontSize,
        palette.text,
        { weight: "bold" }
      )
    );
  }
  const byId = new Map(ir.nodes.map((n) => [n.id, n]));
  const activeEdgeSet = new Set((ir.activePaths ?? []).map(([a, b]) => `${a}:${b}`));
  const parentMap = /* @__PURE__ */ new Map();
  for (const node of ir.nodes) {
    node.children.forEach((cid, idx) => {
      parentMap.set(cid, { parentId: node.id, index: idx, total: node.children.length });
    });
  }
  function circleBorder(center, radius, dir, sign) {
    return { x: rhu(center.x + sign * radius * dir.x), y: rhu(center.y + sign * radius * dir.y) };
  }
  for (const node of ir.nodes) {
    const pb = box(node.id);
    const parentShape = style.get(node.id).shape;
    for (const cid of node.children) {
      const cb = box(cid);
      const childShape = style.get(cid).shape;
      const pc = { x: pb.x + pb.width / 2, y: pb.y + pb.height / 2 };
      const cc = { x: cb.x + cb.width / 2, y: cb.y + cb.height / 2 };
      const dx = cc.x - pc.x, dy = cc.y - pc.y;
      const len = Math.hypot(dx, dy);
      const dir = len > 0 ? { x: dx / len, y: dy / len } : { x: 0, y: 1 };
      const { start: boxStart, end: boxEnd } = connectSlots(pb, cb);
      const start = parentShape === "circle" ? circleBorder(pc, pb.width / 2, dir, 1) : boxStart;
      const end = childShape === "circle" ? circleBorder(cc, cb.width / 2, dir, -1) : boxEnd;
      const isActive = activeEdgeSet.has(`${node.id}:${cid}`);
      const edgeColor = isActive ? palette.primary : palette.textMuted;
      const edgeWidth = isActive ? 2.5 : 1.5;
      elements.push(
        p.path(
          `M ${rhu(start.x)} ${rhu(start.y)} L ${rhu(end.x)} ${rhu(end.y)}`,
          edgeColor,
          edgeWidth
        )
      );
      const child = byId.get(cid);
      if (child.edgeLabel) {
        const mx = (start.x + end.x) / 2, my = (start.y + end.y) / 2;
        const edgeFont = theme.edges?.labelFontSize ?? 12;
        const w = measureText(child.edgeLabel, edgeFont).width + 8;
        elements.push(
          p.rect(
            { x: mx - w / 2, y: my - 9, width: w, height: 16 },
            palette.background,
            palette.background,
            0,
            { rx: 3 }
          )
        );
        elements.push(
          p.text(
            child.edgeLabel,
            mx,
            my + 3,
            edgeFont,
            isActive ? palette.primary : palette.textMuted,
            { anchor: "middle" }
          )
        );
      }
    }
  }
  for (const node of ir.nodes) {
    const b = box(node.id);
    const st = style.get(node.id);
    const info = infoLine(node);
    const cx = b.x + b.width / 2, cy = b.y + b.height / 2;
    const isLeaf = node.kinds.includes("leaf") || node.kinds.includes("pill");
    const sw = isLeaf ? theme.nodes?.leaf.borderWidth ?? 1.2 : theme.nodes?.standard.borderWidth ?? 1.5;
    if (st.shape === "strip") {
      elements.push(
        p.rect(b, st.fill, st.stroke, sw, {
          rx: theme.nodes?.standard.cornerRadius ?? 4
        })
      );
      let sx = b.x;
      stripCells(node.label, font).forEach((cell, idx) => {
        if (idx > 0)
          elements.push(
            p.path(`M ${rhu(sx)} ${rhu(b.y)} L ${rhu(sx)} ${rhu(b.y + b.height)}`, st.stroke, 1)
          );
        elements.push(
          p.text(
            cell.key,
            rhu(sx + cell.width / 2),
            rhu(b.y + b.height / 2 + font * 0.35),
            font,
            st.text,
            { anchor: "middle", weight: "bold" }
          )
        );
        sx += cell.width;
      });
    } else if (st.shape === "circle") {
      elements.push(p.circle({ x: cx, y: cy }, b.width / 2, st.fill, st.stroke, sw));
    } else {
      const rx = st.shape === "pill" ? theme.nodes?.leaf.cornerRadius ?? b.height / 2 : theme.nodes?.standard.cornerRadius ?? 6;
      elements.push(p.rect(b, st.fill, st.stroke, sw, { rx }));
    }
    const textWeight = isLeaf ? "normal" : "bold";
    if (st.shape !== "strip") {
      if (info) {
        const topPad = 10;
        elements.push(
          p.text(node.label, cx, rhu(b.y + topPad + font * 0.75), font, st.text, {
            anchor: "middle",
            weight: textWeight
          })
        );
        elements.push(
          p.text(
            info,
            cx,
            rhu(b.y + topPad + font + 5 + smallFont * 0.75),
            smallFont,
            st.fill === palette.surface ? palette.textMuted : st.text,
            { anchor: "middle" }
          )
        );
      } else {
        elements.push(
          p.text(node.label, cx, rhu(cy + font * 0.35), font, st.text, {
            anchor: "middle",
            weight: textWeight
          })
        );
      }
    }
    if (node.badge !== void 0) {
      const bc = badgeColor(node.badge, theme);
      const parentInfo = parentMap.get(node.id);
      const pb = parentInfo ? box(parentInfo.parentId) : void 0;
      const isLeftChild = pb ? b.x + b.width / 2 < pb.x + pb.width / 2 : false;
      const badgeX = isLeftChild ? b.x + 3 : b.x + b.width - 3;
      const badgeY = b.y + 3;
      elements.push(p.circle({ x: badgeX, y: badgeY }, 9, palette.surface, bc, 1.5));
      elements.push(
        p.text(node.badge, badgeX, rhu(badgeY + smallFont * 0.35), smallFont, bc, {
          anchor: "middle",
          weight: "bold"
        })
      );
    }
  }
  const anchors = {};
  for (const node of ir.nodes) anchors[node.id] = { bounds: box(node.id) };
  const viewWidth = Math.max(placed.width, titleW + margin * 2);
  const scene = applyOverlays(
    {
      viewBox: { x: 0, y: 0, width: viewWidth, height: placed.height + titleH },
      background: palette.background,
      elements
    },
    ir.overlays,
    theme
  );
  return { scene, anchors };
}
function toObj(pairs) {
  const o = {};
  for (const [k, v] of pairs) o[k] = v;
  return o;
}
function peg$subclass23(child, parent) {
  function C() {
    this.constructor = child;
  }
  C.prototype = parent.prototype;
  child.prototype = new C();
}
function peg$SyntaxError23(message, expected, found, location) {
  var self = Error.call(this, message);
  if (Object.setPrototypeOf) {
    Object.setPrototypeOf(self, peg$SyntaxError23.prototype);
  }
  self.expected = expected;
  self.found = found;
  self.location = location;
  self.name = "SyntaxError";
  return self;
}
peg$subclass23(peg$SyntaxError23, Error);
function peg$padEnd23(str, targetLength, padString) {
  padString = padString || " ";
  if (str.length > targetLength) {
    return str;
  }
  targetLength -= str.length;
  padString += padString.repeat(targetLength);
  return str + padString.slice(0, targetLength);
}
peg$SyntaxError23.prototype.format = function(sources) {
  var str = "Error: " + this.message;
  if (this.location) {
    var src = null;
    var k;
    for (k = 0; k < sources.length; k++) {
      if (sources[k].source === this.location.source) {
        src = sources[k].text.split(/\r\n|\n|\r/g);
        break;
      }
    }
    var s = this.location.start;
    var offset_s = this.location.source && typeof this.location.source.offset === "function" ? this.location.source.offset(s) : s;
    var loc = this.location.source + ":" + offset_s.line + ":" + offset_s.column;
    if (src) {
      var e = this.location.end;
      var filler = peg$padEnd23("", offset_s.line.toString().length, " ");
      var line = src[s.line - 1];
      var last = s.line === e.line ? e.column : line.length + 1;
      var hatLen = last - s.column || 1;
      str += "\n --> " + loc + "\n" + filler + " |\n" + offset_s.line + " | " + line + "\n" + filler + " | " + peg$padEnd23("", s.column - 1, " ") + peg$padEnd23("", hatLen, "^");
    } else {
      str += "\n at " + loc;
    }
  }
  return str;
};
peg$SyntaxError23.buildMessage = function(expected, found) {
  var DESCRIBE_EXPECTATION_FNS = {
    literal: function(expectation) {
      return '"' + literalEscape(expectation.text) + '"';
    },
    class: function(expectation) {
      var escapedParts = expectation.parts.map(function(part) {
        return Array.isArray(part) ? classEscape(part[0]) + "-" + classEscape(part[1]) : classEscape(part);
      });
      return "[" + (expectation.inverted ? "^" : "") + escapedParts.join("") + "]";
    },
    any: function() {
      return "any character";
    },
    end: function() {
      return "end of input";
    },
    other: function(expectation) {
      return expectation.description;
    }
  };
  function hex(ch) {
    return ch.charCodeAt(0).toString(16).toUpperCase();
  }
  function literalEscape(s) {
    return s.replace(/\\/g, "\\\\").replace(/"/g, '\\"').replace(/\0/g, "\\0").replace(/\t/g, "\\t").replace(/\n/g, "\\n").replace(/\r/g, "\\r").replace(/[\x00-\x0F]/g, function(ch) {
      return "\\x0" + hex(ch);
    }).replace(/[\x10-\x1F\x7F-\x9F]/g, function(ch) {
      return "\\x" + hex(ch);
    });
  }
  function classEscape(s) {
    return s.replace(/\\/g, "\\\\").replace(/\]/g, "\\]").replace(/\^/g, "\\^").replace(/-/g, "\\-").replace(/\0/g, "\\0").replace(/\t/g, "\\t").replace(/\n/g, "\\n").replace(/\r/g, "\\r").replace(/[\x00-\x0F]/g, function(ch) {
      return "\\x0" + hex(ch);
    }).replace(/[\x10-\x1F\x7F-\x9F]/g, function(ch) {
      return "\\x" + hex(ch);
    });
  }
  function describeExpectation(expectation) {
    return DESCRIBE_EXPECTATION_FNS[expectation.type](expectation);
  }
  function describeExpected(expected2) {
    var descriptions = expected2.map(describeExpectation);
    var i, j;
    descriptions.sort();
    if (descriptions.length > 0) {
      for (i = 1, j = 1; i < descriptions.length; i++) {
        if (descriptions[i - 1] !== descriptions[i]) {
          descriptions[j] = descriptions[i];
          j++;
        }
      }
      descriptions.length = j;
    }
    switch (descriptions.length) {
      case 1:
        return descriptions[0];
      case 2:
        return descriptions[0] + " or " + descriptions[1];
      default:
        return descriptions.slice(0, -1).join(", ") + ", or " + descriptions[descriptions.length - 1];
    }
  }
  function describeFound(found2) {
    return found2 ? '"' + literalEscape(found2) + '"' : "end of input";
  }
  return "Expected " + describeExpected(expected) + " but " + describeFound(found) + " found.";
};
function peg$parse23(input, options) {
  options = options !== void 0 ? options : {};
  var peg$FAILED = {};
  var peg$source = options.grammarSource;
  var peg$startRuleFunctions = { Start: peg$parseStart };
  var peg$startRuleFunction = peg$parseStart;
  var peg$c0 = "tree";
  var peg$c1 = "plan";
  var peg$c2 = "---";
  var peg$c3 = "\r";
  var peg$c4 = "\n";
  var peg$c5 = "TD";
  var peg$c6 = "TB";
  var peg$c7 = "LR";
  var peg$c8 = "title";
  var peg$c9 = "|";
  var peg$c10 = ":";
  var peg$c11 = "{";
  var peg$c12 = "}";
  var peg$c13 = ",";
  var peg$c14 = '"';
  var peg$c15 = ".";
  var peg$r0 = /^[^\n]/;
  var peg$r1 = /^[^\r\n]/;
  var peg$r2 = /^[ \t]/;
  var peg$r3 = /^[^|\r\n]/;
  var peg$r4 = /^[^ \t:{|\r\n]/;
  var peg$r5 = /^[^:{|\r\n]/;
  var peg$r6 = /^[A-Za-z]/;
  var peg$r7 = /^[A-Za-z0-9_\-]/;
  var peg$r8 = /^[A-Za-z_]/;
  var peg$r9 = /^[^,}\r\n]/;
  var peg$r10 = /^[^"]/;
  var peg$r11 = /^[0-9]/;
  var peg$e0 = peg$literalExpectation("tree", false);
  var peg$e1 = peg$literalExpectation("plan", false);
  var peg$e2 = peg$literalExpectation("---", false);
  var peg$e3 = peg$classExpectation(["\n"], true, false);
  var peg$e4 = peg$literalExpectation("\r", false);
  var peg$e5 = peg$literalExpectation("\n", false);
  var peg$e6 = peg$literalExpectation("TD", false);
  var peg$e7 = peg$literalExpectation("TB", false);
  var peg$e8 = peg$literalExpectation("LR", false);
  var peg$e9 = peg$literalExpectation("title", false);
  var peg$e10 = peg$classExpectation(["\r", "\n"], true, false);
  var peg$e11 = peg$classExpectation([" ", "	"], false, false);
  var peg$e12 = peg$literalExpectation("|", false);
  var peg$e13 = peg$classExpectation(["|", "\r", "\n"], true, false);
  var peg$e14 = peg$classExpectation([" ", "	", ":", "{", "|", "\r", "\n"], true, false);
  var peg$e15 = peg$classExpectation([":", "{", "|", "\r", "\n"], true, false);
  var peg$e16 = peg$literalExpectation(":", false);
  var peg$e17 = peg$classExpectation([["A", "Z"], ["a", "z"]], false, false);
  var peg$e18 = peg$classExpectation([["A", "Z"], ["a", "z"], ["0", "9"], "_", "-"], false, false);
  var peg$e19 = peg$literalExpectation("{", false);
  var peg$e20 = peg$literalExpectation("}", false);
  var peg$e21 = peg$literalExpectation(",", false);
  var peg$e22 = peg$classExpectation([["A", "Z"], ["a", "z"], "_"], false, false);
  var peg$e23 = peg$classExpectation([",", "}", "\r", "\n"], true, false);
  var peg$e24 = peg$literalExpectation('"', false);
  var peg$e25 = peg$classExpectation(['"'], true, false);
  var peg$e26 = peg$classExpectation([["0", "9"]], false, false);
  var peg$e27 = peg$literalExpectation(".", false);
  var peg$f0 = function(fm, d) {
    return d;
  };
  var peg$f1 = function(fm, dir, title, lines2) {
    return {
      version: "1.0",
      metadata: { ...fm || {}, ...title ? { title } : {} },
      direction: dir || "TB",
      lines: lines2.filter((l) => l !== null)
    };
  };
  var peg$f2 = function(line) {
    return line;
  };
  var peg$f3 = function(lines2) {
    const meta = {};
    for (const line of lines2) {
      const m = line.match(/^\s*(\w+)\s*:\s*(.+?)\s*$/);
      if (m) meta[m[1]] = m[2];
    }
    return meta;
  };
  var peg$f4 = function() {
    return "TB";
  };
  var peg$f5 = function() {
    return "TB";
  };
  var peg$f6 = function() {
    return "LR";
  };
  var peg$f7 = function(t) {
    return t.trim();
  };
  var peg$f8 = function() {
    return null;
  };
  var peg$f9 = function(indent, content) {
    return { indent: indent.length, ...content };
  };
  var peg$f10 = function(e) {
    return e;
  };
  var peg$f11 = function(edge, label, kinds, a) {
    return a;
  };
  var peg$f12 = function(edge, label, kinds, attrs) {
    return {
      edgeLabel: edge || void 0,
      label: label.trim(),
      kinds,
      attrs: attrs || {}
    };
  };
  var peg$f13 = function(t) {
    return t.trim();
  };
  var peg$f14 = function(name) {
    return name;
  };
  var peg$f15 = function(pairs) {
    return pairs ? toObj(pairs) : {};
  };
  var peg$f16 = function(first, p) {
    return p;
  };
  var peg$f17 = function(first, rest) {
    return [first, ...rest];
  };
  var peg$f18 = function(k, val) {
    return val;
  };
  var peg$f19 = function(k, v) {
    return [k, v === null ? true : v];
  };
  var peg$f20 = function(n) {
    return n;
  };
  var peg$f21 = function(r) {
    return r.trim();
  };
  var peg$f22 = function(s) {
    return s;
  };
  var peg$f23 = function(n) {
    return parseFloat(n);
  };
  var peg$currPos = options.peg$currPos | 0;
  var peg$savedPos = peg$currPos;
  var peg$posDetailsCache = [{ line: 1, column: 1 }];
  var peg$maxFailPos = peg$currPos;
  var peg$maxFailExpected = options.peg$maxFailExpected || [];
  var peg$silentFails = options.peg$silentFails | 0;
  var peg$result;
  if (options.startRule) {
    if (!(options.startRule in peg$startRuleFunctions)) {
      throw new Error(`Can't start parsing from rule "` + options.startRule + '".');
    }
    peg$startRuleFunction = peg$startRuleFunctions[options.startRule];
  }
  function text() {
    return input.substring(peg$savedPos, peg$currPos);
  }
  function offset() {
    return peg$savedPos;
  }
  function range() {
    return {
      source: peg$source,
      start: peg$savedPos,
      end: peg$currPos
    };
  }
  function location() {
    return peg$computeLocation(peg$savedPos, peg$currPos);
  }
  function expected(description, location2) {
    location2 = location2 !== void 0 ? location2 : peg$computeLocation(peg$savedPos, peg$currPos);
    throw peg$buildStructuredError(
      [peg$otherExpectation(description)],
      input.substring(peg$savedPos, peg$currPos),
      location2
    );
  }
  function error(message, location2) {
    location2 = location2 !== void 0 ? location2 : peg$computeLocation(peg$savedPos, peg$currPos);
    throw peg$buildSimpleError(message, location2);
  }
  function peg$literalExpectation(text2, ignoreCase) {
    return { type: "literal", text: text2, ignoreCase };
  }
  function peg$classExpectation(parts, inverted, ignoreCase) {
    return { type: "class", parts, inverted, ignoreCase };
  }
  function peg$anyExpectation() {
    return { type: "any" };
  }
  function peg$endExpectation() {
    return { type: "end" };
  }
  function peg$otherExpectation(description) {
    return { type: "other", description };
  }
  function peg$computePosDetails(pos) {
    var details = peg$posDetailsCache[pos];
    var p;
    if (details) {
      return details;
    } else {
      if (pos >= peg$posDetailsCache.length) {
        p = peg$posDetailsCache.length - 1;
      } else {
        p = pos;
        while (!peg$posDetailsCache[--p]) {
        }
      }
      details = peg$posDetailsCache[p];
      details = {
        line: details.line,
        column: details.column
      };
      while (p < pos) {
        if (input.charCodeAt(p) === 10) {
          details.line++;
          details.column = 1;
        } else {
          details.column++;
        }
        p++;
      }
      peg$posDetailsCache[pos] = details;
      return details;
    }
  }
  function peg$computeLocation(startPos, endPos, offset2) {
    var startPosDetails = peg$computePosDetails(startPos);
    var endPosDetails = peg$computePosDetails(endPos);
    var res = {
      source: peg$source,
      start: {
        offset: startPos,
        line: startPosDetails.line,
        column: startPosDetails.column
      },
      end: {
        offset: endPos,
        line: endPosDetails.line,
        column: endPosDetails.column
      }
    };
    if (offset2 && peg$source && typeof peg$source.offset === "function") {
      res.start = peg$source.offset(res.start);
      res.end = peg$source.offset(res.end);
    }
    return res;
  }
  function peg$fail(expected2) {
    if (peg$currPos < peg$maxFailPos) {
      return;
    }
    if (peg$currPos > peg$maxFailPos) {
      peg$maxFailPos = peg$currPos;
      peg$maxFailExpected = [];
    }
    peg$maxFailExpected.push(expected2);
  }
  function peg$buildSimpleError(message, location2) {
    return new peg$SyntaxError23(message, null, null, location2);
  }
  function peg$buildStructuredError(expected2, found, location2) {
    return new peg$SyntaxError23(
      peg$SyntaxError23.buildMessage(expected2, found),
      expected2,
      found,
      location2
    );
  }
  function peg$parseStart() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8;
    s0 = peg$currPos;
    s1 = peg$parseFrontmatter();
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    s2 = peg$parse_();
    if (input.substr(peg$currPos, 4) === peg$c0) {
      s3 = peg$c0;
      peg$currPos += 4;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e0);
      }
    }
    if (s3 === peg$FAILED) {
      if (input.substr(peg$currPos, 4) === peg$c1) {
        s3 = peg$c1;
        peg$currPos += 4;
      } else {
        s3 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e1);
        }
      }
    }
    if (s3 !== peg$FAILED) {
      s4 = peg$currPos;
      s5 = peg$parse__();
      if (s5 !== peg$FAILED) {
        s6 = peg$parseDirection();
        if (s6 !== peg$FAILED) {
          peg$savedPos = s4;
          s4 = peg$f0(s1, s6);
        } else {
          peg$currPos = s4;
          s4 = peg$FAILED;
        }
      } else {
        peg$currPos = s4;
        s4 = peg$FAILED;
      }
      if (s4 === peg$FAILED) {
        s4 = null;
      }
      s5 = peg$parseLineEnd();
      s6 = peg$parseTitleLine();
      if (s6 === peg$FAILED) {
        s6 = null;
      }
      s7 = [];
      s8 = peg$parseNodeLine();
      if (s8 === peg$FAILED) {
        s8 = peg$parseBlankLine();
      }
      while (s8 !== peg$FAILED) {
        s7.push(s8);
        s8 = peg$parseNodeLine();
        if (s8 === peg$FAILED) {
          s8 = peg$parseBlankLine();
        }
      }
      peg$savedPos = s0;
      s0 = peg$f1(s1, s4, s6, s7);
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseFrontmatter() {
    var s0, s1, s2, s3, s4, s5, s6, s7, s8;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 3) === peg$c2) {
      s1 = peg$c2;
      peg$currPos += 3;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e2);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$parseNL();
      if (s2 !== peg$FAILED) {
        s3 = [];
        s4 = peg$currPos;
        s5 = peg$currPos;
        peg$silentFails++;
        s6 = peg$currPos;
        if (input.substr(peg$currPos, 3) === peg$c2) {
          s7 = peg$c2;
          peg$currPos += 3;
        } else {
          s7 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e2);
          }
        }
        if (s7 !== peg$FAILED) {
          s8 = peg$parseNL();
          if (s8 !== peg$FAILED) {
            s7 = [s7, s8];
            s6 = s7;
          } else {
            peg$currPos = s6;
            s6 = peg$FAILED;
          }
        } else {
          peg$currPos = s6;
          s6 = peg$FAILED;
        }
        peg$silentFails--;
        if (s6 === peg$FAILED) {
          s5 = void 0;
        } else {
          peg$currPos = s5;
          s5 = peg$FAILED;
        }
        if (s5 !== peg$FAILED) {
          s6 = peg$currPos;
          s7 = [];
          s8 = input.charAt(peg$currPos);
          if (peg$r0.test(s8)) {
            peg$currPos++;
          } else {
            s8 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e3);
            }
          }
          while (s8 !== peg$FAILED) {
            s7.push(s8);
            s8 = input.charAt(peg$currPos);
            if (peg$r0.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e3);
              }
            }
          }
          s6 = input.substring(s6, peg$currPos);
          s7 = peg$parseNL();
          if (s7 !== peg$FAILED) {
            peg$savedPos = s4;
            s4 = peg$f2(s6);
          } else {
            peg$currPos = s4;
            s4 = peg$FAILED;
          }
        } else {
          peg$currPos = s4;
          s4 = peg$FAILED;
        }
        while (s4 !== peg$FAILED) {
          s3.push(s4);
          s4 = peg$currPos;
          s5 = peg$currPos;
          peg$silentFails++;
          s6 = peg$currPos;
          if (input.substr(peg$currPos, 3) === peg$c2) {
            s7 = peg$c2;
            peg$currPos += 3;
          } else {
            s7 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e2);
            }
          }
          if (s7 !== peg$FAILED) {
            s8 = peg$parseNL();
            if (s8 !== peg$FAILED) {
              s7 = [s7, s8];
              s6 = s7;
            } else {
              peg$currPos = s6;
              s6 = peg$FAILED;
            }
          } else {
            peg$currPos = s6;
            s6 = peg$FAILED;
          }
          peg$silentFails--;
          if (s6 === peg$FAILED) {
            s5 = void 0;
          } else {
            peg$currPos = s5;
            s5 = peg$FAILED;
          }
          if (s5 !== peg$FAILED) {
            s6 = peg$currPos;
            s7 = [];
            s8 = input.charAt(peg$currPos);
            if (peg$r0.test(s8)) {
              peg$currPos++;
            } else {
              s8 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e3);
              }
            }
            while (s8 !== peg$FAILED) {
              s7.push(s8);
              s8 = input.charAt(peg$currPos);
              if (peg$r0.test(s8)) {
                peg$currPos++;
              } else {
                s8 = peg$FAILED;
                if (peg$silentFails === 0) {
                  peg$fail(peg$e3);
                }
              }
            }
            s6 = input.substring(s6, peg$currPos);
            s7 = peg$parseNL();
            if (s7 !== peg$FAILED) {
              peg$savedPos = s4;
              s4 = peg$f2(s6);
            } else {
              peg$currPos = s4;
              s4 = peg$FAILED;
            }
          } else {
            peg$currPos = s4;
            s4 = peg$FAILED;
          }
        }
        if (input.substr(peg$currPos, 3) === peg$c2) {
          s4 = peg$c2;
          peg$currPos += 3;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e2);
          }
        }
        if (s4 !== peg$FAILED) {
          s5 = peg$parseNL();
          if (s5 !== peg$FAILED) {
            peg$savedPos = s0;
            s0 = peg$f3(s3);
          } else {
            peg$currPos = s0;
            s0 = peg$FAILED;
          }
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseNL() {
    var s0, s1, s2;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 13) {
      s1 = peg$c3;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e4);
      }
    }
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s2 = peg$c4;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e5);
      }
    }
    if (s2 !== peg$FAILED) {
      s1 = [s1, s2];
      s0 = s1;
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseDirection() {
    var s0, s1;
    s0 = peg$currPos;
    if (input.substr(peg$currPos, 2) === peg$c5) {
      s1 = peg$c5;
      peg$currPos += 2;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e6);
      }
    }
    if (s1 !== peg$FAILED) {
      peg$savedPos = s0;
      s1 = peg$f4();
    }
    s0 = s1;
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      if (input.substr(peg$currPos, 2) === peg$c6) {
        s1 = peg$c6;
        peg$currPos += 2;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e7);
        }
      }
      if (s1 !== peg$FAILED) {
        peg$savedPos = s0;
        s1 = peg$f5();
      }
      s0 = s1;
      if (s0 === peg$FAILED) {
        s0 = peg$currPos;
        if (input.substr(peg$currPos, 2) === peg$c7) {
          s1 = peg$c7;
          peg$currPos += 2;
        } else {
          s1 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e8);
          }
        }
        if (s1 !== peg$FAILED) {
          peg$savedPos = s0;
          s1 = peg$f6();
        }
        s0 = s1;
      }
    }
    return s0;
  }
  function peg$parseTitleLine() {
    var s0, s1, s2, s3, s4, s5, s6;
    s0 = peg$currPos;
    s1 = peg$parse_();
    if (input.substr(peg$currPos, 5) === peg$c8) {
      s2 = peg$c8;
      peg$currPos += 5;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e9);
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = peg$parse__();
      if (s3 !== peg$FAILED) {
        s4 = peg$currPos;
        s5 = [];
        s6 = input.charAt(peg$currPos);
        if (peg$r1.test(s6)) {
          peg$currPos++;
        } else {
          s6 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e10);
          }
        }
        if (s6 !== peg$FAILED) {
          while (s6 !== peg$FAILED) {
            s5.push(s6);
            s6 = input.charAt(peg$currPos);
            if (peg$r1.test(s6)) {
              peg$currPos++;
            } else {
              s6 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e10);
              }
            }
          }
        } else {
          s5 = peg$FAILED;
        }
        if (s5 !== peg$FAILED) {
          s4 = input.substring(s4, peg$currPos);
        } else {
          s4 = s5;
        }
        if (s4 !== peg$FAILED) {
          s5 = peg$parseLineEnd();
          peg$savedPos = s0;
          s0 = peg$f7(s4);
        } else {
          peg$currPos = s0;
          s0 = peg$FAILED;
        }
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseBlankLine() {
    var s0, s1, s2, s3;
    s0 = peg$currPos;
    s1 = peg$parse_();
    if (input.charCodeAt(peg$currPos) === 13) {
      s2 = peg$c3;
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e4);
      }
    }
    if (s2 === peg$FAILED) {
      s2 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s3 = peg$c4;
      peg$currPos++;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e5);
      }
    }
    if (s3 !== peg$FAILED) {
      peg$savedPos = s0;
      s0 = peg$f8();
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseNodeLine() {
    var s0, s1, s2, s3;
    s0 = peg$currPos;
    s1 = peg$currPos;
    s2 = [];
    s3 = input.charAt(peg$currPos);
    if (peg$r2.test(s3)) {
      peg$currPos++;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e11);
      }
    }
    while (s3 !== peg$FAILED) {
      s2.push(s3);
      s3 = input.charAt(peg$currPos);
      if (peg$r2.test(s3)) {
        peg$currPos++;
      } else {
        s3 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e11);
        }
      }
    }
    s1 = input.substring(s1, peg$currPos);
    s2 = peg$parseNodeContent();
    if (s2 !== peg$FAILED) {
      s3 = peg$parseLineEnd();
      peg$savedPos = s0;
      s0 = peg$f9(s1, s2);
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseNodeContent() {
    var s0, s1, s2, s3, s4, s5, s6;
    s0 = peg$currPos;
    s1 = peg$currPos;
    s2 = peg$parseEdgeLabel();
    if (s2 !== peg$FAILED) {
      s3 = peg$parse_();
      peg$savedPos = s1;
      s1 = peg$f10(s2);
    } else {
      peg$currPos = s1;
      s1 = peg$FAILED;
    }
    if (s1 === peg$FAILED) {
      s1 = null;
    }
    s2 = peg$parseLabel();
    if (s2 !== peg$FAILED) {
      s3 = [];
      s4 = peg$parseKind();
      while (s4 !== peg$FAILED) {
        s3.push(s4);
        s4 = peg$parseKind();
      }
      s4 = peg$currPos;
      s5 = peg$parse_();
      s6 = peg$parseAttrBlock();
      if (s6 !== peg$FAILED) {
        peg$savedPos = s4;
        s4 = peg$f11(s1, s2, s3, s6);
      } else {
        peg$currPos = s4;
        s4 = peg$FAILED;
      }
      if (s4 === peg$FAILED) {
        s4 = null;
      }
      peg$savedPos = s0;
      s0 = peg$f12(s1, s2, s3, s4);
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseEdgeLabel() {
    var s0, s1, s2, s3, s4;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 124) {
      s1 = peg$c9;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e12);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$currPos;
      s3 = [];
      s4 = input.charAt(peg$currPos);
      if (peg$r3.test(s4)) {
        peg$currPos++;
      } else {
        s4 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e13);
        }
      }
      while (s4 !== peg$FAILED) {
        s3.push(s4);
        s4 = input.charAt(peg$currPos);
        if (peg$r3.test(s4)) {
          peg$currPos++;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e13);
          }
        }
      }
      s2 = input.substring(s2, peg$currPos);
      if (input.charCodeAt(peg$currPos) === 124) {
        s3 = peg$c9;
        peg$currPos++;
      } else {
        s3 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e12);
        }
      }
      if (s3 !== peg$FAILED) {
        peg$savedPos = s0;
        s0 = peg$f13(s2);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseLabel() {
    var s0, s1, s2, s3, s4;
    s0 = peg$currPos;
    s1 = peg$currPos;
    s2 = input.charAt(peg$currPos);
    if (peg$r4.test(s2)) {
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e14);
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = [];
      s4 = input.charAt(peg$currPos);
      if (peg$r5.test(s4)) {
        peg$currPos++;
      } else {
        s4 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e15);
        }
      }
      while (s4 !== peg$FAILED) {
        s3.push(s4);
        s4 = input.charAt(peg$currPos);
        if (peg$r5.test(s4)) {
          peg$currPos++;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e15);
          }
        }
      }
      s2 = [s2, s3];
      s1 = s2;
    } else {
      peg$currPos = s1;
      s1 = peg$FAILED;
    }
    if (s1 !== peg$FAILED) {
      s0 = input.substring(s0, peg$currPos);
    } else {
      s0 = s1;
    }
    return s0;
  }
  function peg$parseKind() {
    var s0, s1, s2, s3, s4, s5, s6;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 58) {
      s1 = peg$c10;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e16);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$currPos;
      s3 = peg$currPos;
      s4 = input.charAt(peg$currPos);
      if (peg$r6.test(s4)) {
        peg$currPos++;
      } else {
        s4 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e17);
        }
      }
      if (s4 !== peg$FAILED) {
        s5 = [];
        s6 = input.charAt(peg$currPos);
        if (peg$r7.test(s6)) {
          peg$currPos++;
        } else {
          s6 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e18);
          }
        }
        while (s6 !== peg$FAILED) {
          s5.push(s6);
          s6 = input.charAt(peg$currPos);
          if (peg$r7.test(s6)) {
            peg$currPos++;
          } else {
            s6 = peg$FAILED;
            if (peg$silentFails === 0) {
              peg$fail(peg$e18);
            }
          }
        }
        s4 = [s4, s5];
        s3 = s4;
      } else {
        peg$currPos = s3;
        s3 = peg$FAILED;
      }
      if (s3 !== peg$FAILED) {
        s2 = input.substring(s2, peg$currPos);
      } else {
        s2 = s3;
      }
      if (s2 !== peg$FAILED) {
        peg$savedPos = s0;
        s0 = peg$f14(s2);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseAttrBlock() {
    var s0, s1, s2, s3, s4, s5;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 123) {
      s1 = peg$c11;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e19);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$parse_();
      s3 = peg$parseAttrPairs();
      if (s3 === peg$FAILED) {
        s3 = null;
      }
      s4 = peg$parse_();
      if (input.charCodeAt(peg$currPos) === 125) {
        s5 = peg$c12;
        peg$currPos++;
      } else {
        s5 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e20);
        }
      }
      if (s5 !== peg$FAILED) {
        peg$savedPos = s0;
        s0 = peg$f15(s3);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseAttrPairs() {
    var s0, s1, s2, s3, s4, s5, s6, s7;
    s0 = peg$currPos;
    s1 = peg$parseAttrPair();
    if (s1 !== peg$FAILED) {
      s2 = [];
      s3 = peg$currPos;
      s4 = peg$parse_();
      if (input.charCodeAt(peg$currPos) === 44) {
        s5 = peg$c13;
        peg$currPos++;
      } else {
        s5 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e21);
        }
      }
      if (s5 !== peg$FAILED) {
        s6 = peg$parse_();
        s7 = peg$parseAttrPair();
        if (s7 !== peg$FAILED) {
          peg$savedPos = s3;
          s3 = peg$f16(s1, s7);
        } else {
          peg$currPos = s3;
          s3 = peg$FAILED;
        }
      } else {
        peg$currPos = s3;
        s3 = peg$FAILED;
      }
      while (s3 !== peg$FAILED) {
        s2.push(s3);
        s3 = peg$currPos;
        s4 = peg$parse_();
        if (input.charCodeAt(peg$currPos) === 44) {
          s5 = peg$c13;
          peg$currPos++;
        } else {
          s5 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e21);
          }
        }
        if (s5 !== peg$FAILED) {
          s6 = peg$parse_();
          s7 = peg$parseAttrPair();
          if (s7 !== peg$FAILED) {
            peg$savedPos = s3;
            s3 = peg$f16(s1, s7);
          } else {
            peg$currPos = s3;
            s3 = peg$FAILED;
          }
        } else {
          peg$currPos = s3;
          s3 = peg$FAILED;
        }
      }
      peg$savedPos = s0;
      s0 = peg$f17(s1, s2);
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseAttrPair() {
    var s0, s1, s2, s3, s4, s5, s6;
    s0 = peg$currPos;
    s1 = peg$parseKey();
    if (s1 !== peg$FAILED) {
      s2 = peg$currPos;
      s3 = peg$parse_();
      if (input.charCodeAt(peg$currPos) === 58) {
        s4 = peg$c10;
        peg$currPos++;
      } else {
        s4 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e16);
        }
      }
      if (s4 !== peg$FAILED) {
        s5 = peg$parse_();
        s6 = peg$parseVal();
        if (s6 !== peg$FAILED) {
          peg$savedPos = s2;
          s2 = peg$f18(s1, s6);
        } else {
          peg$currPos = s2;
          s2 = peg$FAILED;
        }
      } else {
        peg$currPos = s2;
        s2 = peg$FAILED;
      }
      if (s2 === peg$FAILED) {
        s2 = null;
      }
      peg$savedPos = s0;
      s0 = peg$f19(s1, s2);
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseKey() {
    var s0, s1, s2, s3, s4;
    s0 = peg$currPos;
    s1 = peg$currPos;
    s2 = input.charAt(peg$currPos);
    if (peg$r8.test(s2)) {
      peg$currPos++;
    } else {
      s2 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e22);
      }
    }
    if (s2 !== peg$FAILED) {
      s3 = [];
      s4 = input.charAt(peg$currPos);
      if (peg$r7.test(s4)) {
        peg$currPos++;
      } else {
        s4 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e18);
        }
      }
      while (s4 !== peg$FAILED) {
        s3.push(s4);
        s4 = input.charAt(peg$currPos);
        if (peg$r7.test(s4)) {
          peg$currPos++;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e18);
          }
        }
      }
      s2 = [s2, s3];
      s1 = s2;
    } else {
      peg$currPos = s1;
      s1 = peg$FAILED;
    }
    if (s1 !== peg$FAILED) {
      s0 = input.substring(s0, peg$currPos);
    } else {
      s0 = s1;
    }
    return s0;
  }
  function peg$parseVal() {
    var s0, s1, s2, s3;
    s0 = peg$parseQuotedString();
    if (s0 === peg$FAILED) {
      s0 = peg$currPos;
      s1 = peg$parseNumber();
      if (s1 !== peg$FAILED) {
        peg$savedPos = s0;
        s1 = peg$f20(s1);
      }
      s0 = s1;
      if (s0 === peg$FAILED) {
        s0 = peg$currPos;
        s1 = peg$currPos;
        s2 = [];
        s3 = input.charAt(peg$currPos);
        if (peg$r9.test(s3)) {
          peg$currPos++;
        } else {
          s3 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e23);
          }
        }
        if (s3 !== peg$FAILED) {
          while (s3 !== peg$FAILED) {
            s2.push(s3);
            s3 = input.charAt(peg$currPos);
            if (peg$r9.test(s3)) {
              peg$currPos++;
            } else {
              s3 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e23);
              }
            }
          }
        } else {
          s2 = peg$FAILED;
        }
        if (s2 !== peg$FAILED) {
          s1 = input.substring(s1, peg$currPos);
        } else {
          s1 = s2;
        }
        if (s1 !== peg$FAILED) {
          peg$savedPos = s0;
          s1 = peg$f21(s1);
        }
        s0 = s1;
      }
    }
    return s0;
  }
  function peg$parseQuotedString() {
    var s0, s1, s2, s3, s4;
    s0 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 34) {
      s1 = peg$c14;
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e24);
      }
    }
    if (s1 !== peg$FAILED) {
      s2 = peg$currPos;
      s3 = [];
      s4 = input.charAt(peg$currPos);
      if (peg$r10.test(s4)) {
        peg$currPos++;
      } else {
        s4 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e25);
        }
      }
      while (s4 !== peg$FAILED) {
        s3.push(s4);
        s4 = input.charAt(peg$currPos);
        if (peg$r10.test(s4)) {
          peg$currPos++;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e25);
          }
        }
      }
      s2 = input.substring(s2, peg$currPos);
      if (input.charCodeAt(peg$currPos) === 34) {
        s3 = peg$c14;
        peg$currPos++;
      } else {
        s3 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e24);
        }
      }
      if (s3 !== peg$FAILED) {
        peg$savedPos = s0;
        s0 = peg$f22(s2);
      } else {
        peg$currPos = s0;
        s0 = peg$FAILED;
      }
    } else {
      peg$currPos = s0;
      s0 = peg$FAILED;
    }
    return s0;
  }
  function peg$parseNumber() {
    var s0, s1, s2, s3, s4, s5, s6, s7;
    s0 = peg$currPos;
    s1 = peg$currPos;
    s2 = peg$currPos;
    s3 = [];
    s4 = input.charAt(peg$currPos);
    if (peg$r11.test(s4)) {
      peg$currPos++;
    } else {
      s4 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e26);
      }
    }
    if (s4 !== peg$FAILED) {
      while (s4 !== peg$FAILED) {
        s3.push(s4);
        s4 = input.charAt(peg$currPos);
        if (peg$r11.test(s4)) {
          peg$currPos++;
        } else {
          s4 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e26);
          }
        }
      }
    } else {
      s3 = peg$FAILED;
    }
    if (s3 !== peg$FAILED) {
      s4 = peg$currPos;
      if (input.charCodeAt(peg$currPos) === 46) {
        s5 = peg$c15;
        peg$currPos++;
      } else {
        s5 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e27);
        }
      }
      if (s5 !== peg$FAILED) {
        s6 = [];
        s7 = input.charAt(peg$currPos);
        if (peg$r11.test(s7)) {
          peg$currPos++;
        } else {
          s7 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e26);
          }
        }
        if (s7 !== peg$FAILED) {
          while (s7 !== peg$FAILED) {
            s6.push(s7);
            s7 = input.charAt(peg$currPos);
            if (peg$r11.test(s7)) {
              peg$currPos++;
            } else {
              s7 = peg$FAILED;
              if (peg$silentFails === 0) {
                peg$fail(peg$e26);
              }
            }
          }
        } else {
          s6 = peg$FAILED;
        }
        if (s6 !== peg$FAILED) {
          s5 = [s5, s6];
          s4 = s5;
        } else {
          peg$currPos = s4;
          s4 = peg$FAILED;
        }
      } else {
        peg$currPos = s4;
        s4 = peg$FAILED;
      }
      if (s4 === peg$FAILED) {
        s4 = null;
      }
      s3 = [s3, s4];
      s2 = s3;
    } else {
      peg$currPos = s2;
      s2 = peg$FAILED;
    }
    if (s2 !== peg$FAILED) {
      s1 = input.substring(s1, peg$currPos);
    } else {
      s1 = s2;
    }
    if (s1 !== peg$FAILED) {
      peg$savedPos = s0;
      s1 = peg$f23(s1);
    }
    s0 = s1;
    return s0;
  }
  function peg$parseLineEnd() {
    var s0, s1, s2, s3, s4;
    s0 = peg$currPos;
    s1 = peg$parse_();
    s2 = peg$currPos;
    if (input.charCodeAt(peg$currPos) === 13) {
      s3 = peg$c3;
      peg$currPos++;
    } else {
      s3 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e4);
      }
    }
    if (s3 === peg$FAILED) {
      s3 = null;
    }
    if (input.charCodeAt(peg$currPos) === 10) {
      s4 = peg$c4;
      peg$currPos++;
    } else {
      s4 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e5);
      }
    }
    if (s4 !== peg$FAILED) {
      s3 = [s3, s4];
      s2 = s3;
    } else {
      peg$currPos = s2;
      s2 = peg$FAILED;
    }
    if (s2 === peg$FAILED) {
      s2 = null;
    }
    s1 = [s1, s2];
    s0 = s1;
    return s0;
  }
  function peg$parse_() {
    var s0, s1;
    s0 = [];
    s1 = input.charAt(peg$currPos);
    if (peg$r2.test(s1)) {
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e11);
      }
    }
    while (s1 !== peg$FAILED) {
      s0.push(s1);
      s1 = input.charAt(peg$currPos);
      if (peg$r2.test(s1)) {
        peg$currPos++;
      } else {
        s1 = peg$FAILED;
        if (peg$silentFails === 0) {
          peg$fail(peg$e11);
        }
      }
    }
    return s0;
  }
  function peg$parse__() {
    var s0, s1;
    s0 = [];
    s1 = input.charAt(peg$currPos);
    if (peg$r2.test(s1)) {
      peg$currPos++;
    } else {
      s1 = peg$FAILED;
      if (peg$silentFails === 0) {
        peg$fail(peg$e11);
      }
    }
    if (s1 !== peg$FAILED) {
      while (s1 !== peg$FAILED) {
        s0.push(s1);
        s1 = input.charAt(peg$currPos);
        if (peg$r2.test(s1)) {
          peg$currPos++;
        } else {
          s1 = peg$FAILED;
          if (peg$silentFails === 0) {
            peg$fail(peg$e11);
          }
        }
      }
    } else {
      s0 = peg$FAILED;
    }
    return s0;
  }
  peg$result = peg$startRuleFunction();
  if (options.peg$library) {
    return (
      /** @type {any} */
      {
        peg$result,
        peg$currPos,
        peg$FAILED,
        peg$maxFailExpected,
        peg$maxFailPos
      }
    );
  }
  if (peg$result !== peg$FAILED && peg$currPos === input.length) {
    return peg$result;
  } else {
    if (peg$result !== peg$FAILED && peg$currPos < input.length) {
      peg$fail(peg$endExpectation());
    }
    throw peg$buildStructuredError(
      peg$maxFailExpected,
      peg$maxFailPos < input.length ? input.charAt(peg$maxFailPos) : null,
      peg$maxFailPos < input.length ? peg$computeLocation(peg$maxFailPos, peg$maxFailPos + 1) : peg$computeLocation(peg$maxFailPos, peg$maxFailPos)
    );
  }
}
function parseLines(input) {
  return peg$parse23(input);
}
function buildNodes(lines2) {
  const nodes = [];
  const byId = /* @__PURE__ */ new Map();
  const stack2 = [];
  lines2.forEach((ln, i) => {
    const id = `n${i}`;
    while (stack2.length > 0 && stack2[stack2.length - 1].indent >= ln.indent) stack2.pop();
    const parent = stack2.length > 0 ? stack2[stack2.length - 1] : void 0;
    const kinds = [...ln.kinds];
    const infoParts = [];
    let badge;
    for (const [k, v] of Object.entries(ln.attrs ?? {})) {
      if (v === true) {
        kinds.push(k);
        continue;
      }
      if (k === "badge") {
        badge = String(v);
        continue;
      }
      infoParts.push(k === "sub" || k === "info" ? String(v) : `${k}: ${v}`);
    }
    const info = infoParts.length > 0 ? infoParts.join(" \xB7 ") : void 0;
    const node = {
      id,
      label: ln.label,
      kinds,
      children: [],
      ...info !== void 0 ? { info } : {},
      ...badge !== void 0 ? { badge } : {},
      ...ln.edgeLabel !== void 0 ? { edgeLabel: ln.edgeLabel } : {}
    };
    nodes.push(node);
    byId.set(id, node);
    if (parent) byId.get(parent.id).children.push(id);
    stack2.push({ indent: ln.indent, id });
  });
  return nodes;
}
function insertBPlus(node, key, order) {
  if (node.isLeaf) {
    let i2 = node.keys.length - 1;
    while (i2 >= 0 && key < node.keys[i2]) i2--;
    if (i2 >= 0 && node.keys[i2] === key) return null;
    node.keys.splice(i2 + 1, 0, key);
    if (node.keys.length <= order - 1) return null;
    const mid2 = Math.floor(node.keys.length / 2);
    const upKey2 = node.keys[mid2];
    const rightLeaf = {
      isLeaf: true,
      keys: node.keys.slice(mid2),
      // includes upKey
      next: node.next,
      prev: node
    };
    if (node.next) node.next.prev = rightLeaf;
    node.next = rightLeaf;
    node.keys = node.keys.slice(0, mid2);
    return { upKey: upKey2, rightChild: rightLeaf };
  }
  let i = 0;
  while (i < node.keys.length && key >= node.keys[i]) i++;
  const split = insertBPlus(node.children[i], key, order);
  if (!split) return null;
  node.keys.splice(i, 0, split.upKey);
  node.children.splice(i + 1, 0, split.rightChild);
  if (node.keys.length <= order - 1) return null;
  const mid = Math.floor(node.keys.length / 2);
  const upKey = node.keys[mid];
  const rightInternal = {
    isLeaf: false,
    keys: node.keys.slice(mid + 1),
    // excludes upKey
    children: node.children.slice(mid + 1)
  };
  node.keys = node.keys.slice(0, mid);
  node.children = node.children.slice(0, mid + 1);
  return { upKey, rightChild: rightInternal };
}
function buildBPlusTree(input) {
  const lines2 = input.split(/\r?\n/).map((l) => l.trim()).filter(Boolean);
  let title;
  let compact = false;
  let maxWidth;
  let direction;
  const filteredLines = [];
  const notes = [];
  let bidirectional = false;
  for (const line of lines2) {
    if (/^(b\+?tree|bplustree)$/i.test(line)) {
      continue;
    }
    const headerWithArgs = line.match(/^(b\+?tree|bplustree)\s+(.+)$/i);
    if (headerWithArgs) {
      filteredLines.push(headerWithArgs[2].trim());
      continue;
    }
    const titleMatch = line.match(/^title\s+(.+)$/i);
    if (titleMatch) {
      title = titleMatch[1].trim().replace(/^["']|["']$/g, "");
      continue;
    }
    const compactMatch = line.match(/^(layout\s+compact|compact\s*(true)?|compact)\b/i);
    if (compactMatch) {
      compact = true;
      continue;
    }
    const maxWidthMatch = line.match(/^max-?width\s+(\d+)/i);
    if (maxWidthMatch) {
      maxWidth = Number(maxWidthMatch[1]);
      compact = true;
      continue;
    }
    const dirMatch = line.match(/^direction\s+(TD|TB|LR)/i);
    if (dirMatch) {
      direction = dirMatch[1] === "LR" ? "LR" : "TB";
      continue;
    }
    if (/^(bidirectional|doubly-linked|doublyLinked)\b/i.test(line)) {
      bidirectional = true;
      continue;
    }
    const noteMatch = line.match(/^note\s+(\w+)\s+["']?([^"']+)["']?$/i);
    if (noteMatch) {
      notes.push({ target: noteMatch[1].toLowerCase(), text: noteMatch[2].trim() });
      continue;
    }
    filteredLines.push(line);
  }
  const extraMeta = {
    ...compact ? { compact: true } : {},
    ...maxWidth !== void 0 ? { maxWidth } : {},
    ...direction ? { direction } : {}
  };
  const cleanInput = filteredLines.join("\n");
  const isExplicit = filteredLines.some((l) => l.includes("->") || /^leaf\b/i.test(l) || /^page\b/i.test(l)) || input.includes("\n  ") && !/\binsert\b/i.test(input) && !/\border\b/i.test(input);
  if (isExplicit) {
    return parseExplicitBPlusTree(cleanInput, title, bidirectional, notes, extraMeta);
  }
  return buildAlgorithmicBPlusTree(cleanInput, title, bidirectional, notes, extraMeta);
}
function buildAlgorithmicBPlusTree(input, title, bidirectional, notes, extraMeta) {
  const orderMatch = input.match(/order\s+(\d+)/i);
  const order = Math.max(3, orderMatch ? Number(orderMatch[1]) : 3);
  const afterOrder = input.replace(/order\s+\d+/i, "").replace(/insert/gi, "");
  const keys = (afterOrder.match(/-?\d+/g) ?? []).map(Number);
  if (keys.length === 0) {
    return {
      version: "1.0",
      metadata: title ? { title } : {},
      direction: "TB",
      nodes: [],
      ...bidirectional ? { bidirectionalLeaves: true } : {},
      ...notes && notes.length > 0 ? { notes } : {}
    };
  }
  let root = {
    isLeaf: true,
    keys: [],
    next: null,
    prev: null
  };
  for (const k of keys) {
    const split = insertBPlus(root, k, order);
    if (split) {
      root = {
        isLeaf: false,
        keys: [split.upKey],
        children: [root, split.rightChild]
      };
    }
  }
  let internalCount = 1;
  let leafCount = 1;
  const nodes = [];
  const emit = (n) => {
    if (n.isLeaf) {
      const pageId2 = `L${leafCount++}`;
      const id2 = `leaf_${pageId2}`;
      nodes.push({
        id: id2,
        label: n.keys.join(" | "),
        kinds: ["strip", "leaf"],
        pageId: pageId2,
        isLeaf: true,
        children: []
      });
      return id2;
    }
    const pageId = `P${internalCount++}`;
    const id = `page_${pageId}`;
    const childIds = [];
    for (const c of n.children) {
      childIds.push(emit(c));
    }
    nodes.push({
      id,
      label: n.keys.join(" | "),
      kinds: ["strip", "internal"],
      pageId,
      isLeaf: false,
      children: childIds
    });
    return id;
  };
  emit(root);
  return {
    version: "1.0",
    metadata: title ? { title } : {},
    direction: "TB",
    nodes,
    ...bidirectional ? { bidirectionalLeaves: true } : {},
    ...notes && notes.length > 0 ? { notes } : {}
  };
}
function parseExplicitBPlusTree(input, title, bidirectional, notes, extraMeta) {
  const nodes = [];
  const lines2 = input.split(/\r?\n/).filter((l) => l.trim().length > 0);
  const isStatementSyntax = lines2.some((l) => l.includes("->"));
  if (isStatementSyntax) {
    const rawNodes = /* @__PURE__ */ new Map();
    for (const line of lines2) {
      const trimmed = line.trim();
      if (!trimmed) continue;
      const isLeafDecl = /^leaf\b/i.test(trimmed);
      const isPageDecl = /^page\b/i.test(trimmed);
      const cleanLine = trimmed.replace(/^(page|leaf|node)\s+/i, "");
      const arrowIdx = cleanLine.indexOf("->");
      const declPart = arrowIdx >= 0 ? cleanLine.slice(0, arrowIdx).trim() : cleanLine;
      const targetsPart = arrowIdx >= 0 ? cleanLine.slice(arrowIdx + 2).trim() : "";
      let label = "";
      let id = "";
      let pageId;
      const bracketMatch = declPart.match(/\[(.*?)\]/);
      const quoteMatch = declPart.match(/"(.*?)"/);
      if (bracketMatch) {
        label = bracketMatch[1].replace(/\s*\|\s*/g, " | ").trim();
        id = declPart.slice(0, bracketMatch.index).trim();
      } else if (quoteMatch) {
        label = quoteMatch[1].replace(/\s*\|\s*/g, " | ").trim();
        id = declPart.slice(0, quoteMatch.index).trim();
      } else {
        const parts = declPart.split(/\s+/);
        id = parts[0];
        label = parts.slice(1).join(" | ");
      }
      if (!id) id = `n${rawNodes.size + 1}`;
      pageId = id;
      const children = targetsPart ? targetsPart.split(/[,;\s]+/).map((s) => s.trim()).filter(Boolean) : [];
      const isLeaf = isLeafDecl || children.length === 0 && !isPageDecl;
      rawNodes.set(id, {
        id,
        label: label || id,
        pageId,
        isLeaf,
        children
      });
    }
    for (const raw of rawNodes.values()) {
      nodes.push({
        id: raw.id,
        label: raw.label,
        kinds: raw.isLeaf ? ["strip", "leaf"] : ["strip", "internal"],
        ...raw.pageId ? { pageId: raw.pageId } : {},
        isLeaf: raw.isLeaf,
        children: raw.children
      });
    }
  } else {
    const rawLines = lines2.map((l) => {
      const indent = l.search(/\S|$/);
      const content = l.trim();
      return { indent, content };
    });
    const stack2 = [];
    rawLines.forEach((ln, idx) => {
      while (stack2.length > 0 && stack2[stack2.length - 1].indent >= ln.indent) stack2.pop();
      const parent = stack2.length > 0 ? stack2[stack2.length - 1] : void 0;
      let content = ln.content;
      let isLeaf = false;
      let pageId;
      const attrMatch = content.match(/\{(.*?)\}/);
      if (attrMatch) {
        const attrStr = attrMatch[1];
        if (/\bleaf\s*:\s*true\b/i.test(attrStr) || /\bleaf\b/i.test(attrStr)) isLeaf = true;
        const pageMatch = attrStr.match(/page\s*:\s*["']?([^"',}]+)["']?/i);
        const idMatch = attrStr.match(/id\s*:\s*["']?([^"',}]+)["']?/i);
        if (pageMatch) pageId = pageMatch[1].trim();
        else if (idMatch) pageId = idMatch[1].trim();
        content = content.replace(/\{.*?\}/, "").trim();
      }
      const bracketMatch = content.match(/^(\w+)?\s*\[(.*?)\]/);
      let label = content;
      if (bracketMatch) {
        label = bracketMatch[2];
        if (bracketMatch[1] && !pageId) pageId = bracketMatch[1];
      }
      const id = pageId || `bn_${idx + 1}`;
      if (!parent && rawLines.length === 1) isLeaf = true;
      nodes.push({
        id,
        label: label.replace(/\s*\|\s*/g, " | "),
        kinds: isLeaf ? ["strip", "leaf"] : ["strip", "internal"],
        ...pageId ? { pageId } : {},
        isLeaf,
        children: []
      });
      if (parent) {
        const pNode = nodes.find((n) => n.id === parent.id);
        if (pNode) {
          pNode.children.push(id);
          pNode.isLeaf = false;
        }
      }
      stack2.push({ indent: ln.indent, id });
    });
    for (const node of nodes) {
      if (node.children.length === 0) {
        node.isLeaf = true;
        if (!node.kinds.includes("leaf")) {
          node.kinds = ["strip", "leaf"];
        }
      }
    }
  }
  return {
    version: "1.0",
    metadata: { ...title ? { title } : {}, ...extraMeta ?? {} },
    direction: "TB",
    nodes,
    ...bidirectional ? { bidirectionalLeaves: true } : {},
    ...notes && notes.length > 0 ? { notes } : {}
  };
}
var ARROW_ID7 = "bplus-arrow";
var ARROW_START_ID3 = "bplus-arrow-start";
var TREE_ARROW_ID = "bplus-tree-arrow";
var NOTE_ARROW_ID = "bplus-note-arrow";
function stripCells2(label, font, isCompact = false) {
  return label.split("|").map((s) => s.trim()).map((key) => ({
    key,
    width: Math.max(measureText(key, isCompact ? font - 2 : font).width + (isCompact ? 14 : 24), isCompact ? 28 : 40)
  }));
}
function layoutBPlusTree(ir, theme) {
  const { palette, typography, spacing, edges: edges2 } = theme;
  const p = pen(theme);
  const margin = spacing.diagramMargin;
  const font = typography.baseFontSize;
  const smallFont = typography.smallFontSize;
  if (ir.nodes.length === 0) {
    const scene2 = {
      viewBox: { x: 0, y: 0, width: 120, height: 80 },
      background: palette.background,
      elements: []
    };
    return { scene: scene2, anchors: {} };
  }
  const isCompact = ir.metadata["compact"] === true || ir.metadata["layout"] === "compact" || String(ir.metadata["compact"]) === "true";
  const sizes = /* @__PURE__ */ new Map();
  const isLeafNode = /* @__PURE__ */ new Map();
  for (const node of ir.nodes) {
    const isLeaf = Boolean(node.isLeaf || node.children.length === 0 || node.kinds.includes("leaf"));
    isLeafNode.set(node.id, isLeaf);
    const cells2 = stripCells2(node.label, font, isCompact);
    const width = cells2.reduce((sum, c) => sum + c.width, 0);
    const height = (isCompact ? font - 2 : font) + (isCompact ? 16 : 22);
    sizes.set(node.id, { width, height });
  }
  const inputs = ir.nodes.map((n) => ({
    id: n.id,
    width: sizes.get(n.id).width,
    height: sizes.get(n.id).height,
    children: n.children
  }));
  const placed = treeLayout(inputs, {
    direction: ir.metadata["direction"] ?? ir.direction ?? "TB",
    levelGap: isCompact ? 42 : 68,
    siblingGap: isCompact ? 16 : 38,
    margin
  });
  const titleH = ir.metadata["title"] ? typography.titleFontSize + 24 : 0;
  const leafIds = ir.nodes.filter((n) => isLeafNode.get(n.id)).map((n) => n.id);
  const leafBoxes = leafIds.map((id) => placed.boxes.get(id)).filter(Boolean);
  const maxLeafY = leafBoxes.length > 0 ? Math.max(...leafBoxes.map((b) => b.y)) : 0;
  const adjustedBoxes = /* @__PURE__ */ new Map();
  for (const node of ir.nodes) {
    const b = placed.boxes.get(node.id);
    const isLeaf = isLeafNode.get(node.id);
    const y = (isLeaf && maxLeafY > 0 ? maxLeafY : b.y) + titleH;
    adjustedBoxes.set(node.id, { x: b.x, y, width: b.width, height: b.height });
  }
  const box = (id) => adjustedBoxes.get(id);
  const elements = [];
  if (titleH > 0) {
    elements.push(
      p.text(
        String(ir.metadata["title"]),
        placed.width / 2,
        margin + typography.titleFontSize,
        typography.titleFontSize + 2,
        palette.text,
        { anchor: "middle", weight: "bold" }
      )
    );
  }
  for (const node of ir.nodes) {
    const pb = box(node.id);
    const cells2 = stripCells2(node.label, font);
    const childCount = node.children.length;
    node.children.forEach((cid, cIdx) => {
      const cb = box(cid);
      let startX;
      if (childCount === cells2.length + 1) {
        if (cIdx === 0) {
          startX = pb.x;
        } else if (cIdx === childCount - 1) {
          startX = pb.x + pb.width;
        } else {
          let curX = pb.x;
          for (let j = 0; j < cIdx; j++) {
            curX += cells2[j].width;
          }
          startX = curX;
        }
      } else {
        const { start } = connectSlots(pb, cb);
        startX = start.x;
      }
      const startY = pb.y + pb.height;
      const endX = cb.x + cb.width / 2;
      const endY = cb.y;
      elements.push(
        p.path(
          `M ${rhu(startX)} ${rhu(startY)} L ${rhu(endX)} ${rhu(endY)}`,
          palette.textMuted,
          1.5,
          {
            markerEnd: TREE_ARROW_ID
          }
        )
      );
    });
  }
  const sortedLeaves = [...leafIds].sort((a, b) => box(a).x - box(b).x);
  for (let i = 0; i < sortedLeaves.length - 1; i++) {
    const curId = sortedLeaves[i];
    const nextId = sortedLeaves[i + 1];
    const curBox = box(curId);
    const nextBox = box(nextId);
    const startX = curBox.x + curBox.width;
    const startY = curBox.y + curBox.height / 2;
    const endX = nextBox.x;
    const endY = nextBox.y + nextBox.height / 2;
    if (endX > startX + 2) {
      elements.push(
        p.path(
          `M ${rhu(startX)} ${rhu(startY)} L ${rhu(endX)} ${rhu(endY)}`,
          palette.primary,
          1.5,
          {
            markerEnd: ARROW_ID7,
            ...ir.bidirectionalLeaves ? { markerStart: ARROW_START_ID3 } : {}
          }
        )
      );
    }
  }
  for (const node of ir.nodes) {
    const b = box(node.id);
    const isLeaf = isLeafNode.get(node.id);
    const fill = palette.surface;
    const stroke = isLeaf ? palette.primary : palette.border;
    const strokeWidth = isLeaf ? 1.8 : 1.5;
    elements.push(p.rect(b, fill, stroke, strokeWidth, { rx: 4 }));
    let sx = b.x;
    const cells2 = stripCells2(node.label, font);
    cells2.forEach((cell, idx) => {
      if (idx > 0) {
        elements.push(
          p.path(`M ${rhu(sx)} ${rhu(b.y)} L ${rhu(sx)} ${rhu(b.y + b.height)}`, stroke, 1)
        );
      }
      elements.push(
        p.text(
          cell.key,
          rhu(sx + cell.width / 2),
          rhu(b.y + b.height / 2 + font * 0.35),
          font,
          palette.text,
          { anchor: "middle", weight: "bold" }
        )
      );
      sx += cell.width;
    });
    const headerLabel = node.badge ?? (node.pageId && !node.pageId.startsWith("n") && !node.pageId.startsWith("l") && !node.pageId.startsWith("p") ? node.pageId : isLeaf ? "leaf" : "internal node");
    if (headerLabel) {
      elements.push(
        p.text(
          headerLabel,
          rhu(b.x + b.width / 2),
          rhu(b.y - 7),
          smallFont,
          isLeaf ? palette.primary : palette.textMuted,
          { anchor: "middle", weight: "normal" }
        )
      );
    }
  }
  const rootNode = ir.nodes.find((n) => !isLeafNode.get(n.id)) ?? ir.nodes[0];
  const rootBox = rootNode ? box(rootNode.id) : void 0;
  let extraBottomH = 0;
  let extraRightW = 0;
  if (ir.notes && ir.notes.length > 0) {
    for (const note of ir.notes) {
      if ((note.target === "internal" || note.target === "root") && rootBox) {
        const noteX = rootBox.x + rootBox.width + 48;
        const noteY = rootBox.y - 2;
        extraRightW = Math.max(extraRightW, measureText(note.text, smallFont).width + 60);
        elements.push(
          p.text(note.text, rhu(noteX), rhu(noteY), smallFont, palette.textMuted, {
            anchor: "start"
          })
        );
        elements.push(
          p.path(
            `M ${rhu(noteX - 6)} ${rhu(noteY - 4)} L ${rhu(rootBox.x + rootBox.width + 4)} ${rhu(rootBox.y + rootBox.height / 2)}`,
            palette.textMuted,
            1.2,
            { markerEnd: NOTE_ARROW_ID }
          )
        );
      } else if (note.target === "leaves" || note.target === "leaf" || note.target === "data") {
        const leafH = sizes.get(leafIds[0] ?? "")?.height ?? 34;
        const noteY = maxLeafY + leafH + titleH + 34;
        const noteX = placed.width / 2;
        extraBottomH = Math.max(extraBottomH, 48);
        elements.push(
          p.text(note.text, rhu(noteX), rhu(noteY), smallFont, palette.textMuted, {
            anchor: "middle"
          })
        );
        const arcStart = noteX - 80;
        const arcEnd = noteX + 80;
        const arcMidY = noteY - 14;
        elements.push(
          p.path(
            `M ${rhu(arcStart)} ${rhu(arcMidY)} Q ${rhu(noteX)} ${rhu(arcMidY + 10)} ${rhu(arcEnd)} ${rhu(arcMidY)}`,
            palette.textMuted,
            1.2,
            { markerEnd: NOTE_ARROW_ID }
          )
        );
      }
    }
  }
  const anchors = {};
  for (const node of ir.nodes) {
    const b = box(node.id);
    anchors[node.id] = { bounds: b };
    if (node.pageId) {
      anchors[node.pageId] = { bounds: b };
      anchors[`page_${node.pageId}`] = { bounds: b };
      anchors[`leaf_${node.pageId}`] = { bounds: b };
    }
  }
  const s = edges2?.arrowSize ?? 8;
  const sH = rhu(s * 0.7);
  const sMidY = rhu(s * 0.35);
  const sRefX = rhu(s - 1);
  const defs = [
    `<marker id="${ARROW_ID7}" markerWidth="${s}" markerHeight="${sH}" refX="${sRefX}" refY="${sMidY}" orient="auto" markerUnits="userSpaceOnUse"><polygon points="0 0, ${s} ${sMidY}, 0 ${sH}" fill="${palette.primary}" /></marker>`,
    `<marker id="${ARROW_START_ID3}" markerWidth="${s}" markerHeight="${sH}" refX="1" refY="${sMidY}" orient="auto-start-reverse" markerUnits="userSpaceOnUse"><polygon points="0 0, ${s} ${sMidY}, 0 ${sH}" fill="${palette.primary}" /></marker>`,
    `<marker id="${TREE_ARROW_ID}" markerWidth="${s}" markerHeight="${sH}" refX="${sRefX}" refY="${sMidY}" orient="auto" markerUnits="userSpaceOnUse"><polygon points="0 0, ${s} ${sMidY}, 0 ${sH}" fill="${palette.textMuted}" /></marker>`,
    `<marker id="${NOTE_ARROW_ID}" markerWidth="${s}" markerHeight="${sH}" refX="${sRefX}" refY="${sMidY}" orient="auto" markerUnits="userSpaceOnUse"><polygon points="0 0, ${s} ${sMidY}, 0 ${sH}" fill="${palette.textMuted}" /></marker>`
  ];
  const totalH = rhuInt(maxLeafY + (sizes.get(leafIds[0] ?? "")?.height ?? 34) + margin * 2 + titleH + extraBottomH);
  const totalW = rhuInt(placed.width + extraRightW);
  const scene = applyOverlays(
    {
      viewBox: { x: 0, y: 0, width: totalW, height: totalH },
      background: palette.background,
      elements,
      defs
    },
    ir.overlays,
    theme
  );
  return { scene, anchors };
}
var bplustree = {
  parseMermaid: buildBPlusTree,
  parseYaml: (input) => JSON.parse(input),
  layout: (ir, theme) => layoutBPlusTree(ir, theme)
};
function mockHash(input) {
  let h2 = 2166136261;
  for (let i = 0; i < input.length; i++) {
    h2 ^= input.charCodeAt(i);
    h2 = Math.imul(h2, 16777619);
  }
  return (h2 >>> 0).toString(16).padStart(8, "0").slice(0, 8);
}
function buildMerkleTree(input) {
  const lines2 = input.split(/\r?\n/).map((l) => l.trim()).filter(Boolean);
  let title;
  let proofTarget;
  let compact = false;
  let maxWidth;
  let direction;
  const dataItems = [];
  const manualLines = [];
  for (const line of lines2) {
    if (/^merkletree\b/i.test(line)) continue;
    const titleMatch = line.match(/^title\s+(.+)$/i);
    if (titleMatch) {
      title = titleMatch[1].trim().replace(/^["']|["']$/g, "");
      continue;
    }
    const proofMatch = line.match(/^(proof|verify)\s+(.+)$/i);
    if (proofMatch) {
      proofTarget = proofMatch[2].trim().replace(/^["']|["']$/g, "");
      continue;
    }
    const compactMatch = line.match(/^(layout\s+compact|compact\s*(true)?|compact)\b/i);
    if (compactMatch) {
      compact = true;
      continue;
    }
    const maxWidthMatch = line.match(/^max-?width\s+(\d+)/i);
    if (maxWidthMatch) {
      maxWidth = Number(maxWidthMatch[1]);
      compact = true;
      continue;
    }
    const dirMatch = line.match(/^direction\s+(TD|TB|LR)/i);
    if (dirMatch) {
      direction = dirMatch[1] === "LR" ? "LR" : "TB";
      continue;
    }
    const dataMatch = line.match(/^data\s+(.+)$/i);
    if (dataMatch) {
      const items = dataMatch[1].match(/"([^"]+)"|'([^']+)'|(\S+)/g) ?? [];
      for (const item of items) {
        dataItems.push(item.replace(/^["']|["']$/g, ""));
      }
      continue;
    }
    manualLines.push(line);
  }
  const extraMeta = {
    ...compact ? { compact: true } : {},
    ...maxWidth !== void 0 ? { maxWidth } : {},
    ...direction ? { direction } : {}
  };
  if (dataItems.length > 0) {
    return buildAlgorithmicMerkleTree(dataItems, title, proofTarget, extraMeta);
  }
  return parseExplicitMerkleTree(manualLines.join("\n"), title, proofTarget, extraMeta);
}
function buildAlgorithmicMerkleTree(dataItems, title, proofTarget, extraMeta) {
  const leaves = [...dataItems];
  while ((leaves.length & leaves.length - 1) !== 0 || leaves.length < 2) {
    leaves.push(leaves[leaves.length - 1]);
  }
  let currentLevel = leaves.map((data, idx) => ({
    id: `leaf_${idx}`,
    hash: mockHash(`data:${data}`),
    data,
    isLeaf: true,
    children: []
  }));
  const allNodes = [...currentLevel];
  let levelIdx = 1;
  while (currentLevel.length > 1) {
    const nextLevel = [];
    for (let i = 0; i < currentLevel.length; i += 2) {
      const left = currentLevel[i];
      const right = currentLevel[i + 1] ?? left;
      const combinedHash = mockHash(`${left.hash}+${right.hash}`);
      const parentNode = {
        id: `node_${levelIdx}_${i / 2}`,
        hash: combinedHash,
        isLeaf: false,
        children: [left, right]
      };
      nextLevel.push(parentNode);
      allNodes.push(parentNode);
    }
    currentLevel = nextLevel;
    levelIdx++;
  }
  const root = currentLevel[0];
  const proofNodes = /* @__PURE__ */ new Set();
  const proofSiblings = /* @__PURE__ */ new Set();
  if (proofTarget) {
    const targetLeaf = allNodes.find(
      (n) => n.isLeaf && (n.data === proofTarget || n.id === proofTarget || n.hash.startsWith(proofTarget))
    );
    if (targetLeaf) {
      let cur = targetLeaf;
      proofNodes.add(cur.id);
      while (cur && cur.id !== root.id) {
        const targetId = cur.id;
        const parent = allNodes.find(
          (p) => p.children.some((c) => c.id === targetId)
        );
        if (parent) {
          proofNodes.add(parent.id);
          const sibling = parent.children.find((c) => c.id !== targetId);
          if (sibling) proofSiblings.add(sibling.id);
          cur = parent;
        } else {
          break;
        }
      }
    }
  }
  const nodes = allNodes.map((n) => ({
    id: n.id,
    hash: n.hash,
    ...n.data ? { data: n.data } : {},
    isLeaf: n.isLeaf,
    ...n.id === root.id ? { isRoot: true } : {},
    ...proofNodes.has(n.id) ? { isProof: true } : {},
    ...proofSiblings.has(n.id) ? { isProofSibling: true } : {},
    children: n.children.map((c) => c.id)
  }));
  return {
    version: "1.0",
    metadata: { ...title ? { title } : {}, ...extraMeta ?? {} },
    ...title ? { title } : {},
    ...proofTarget ? { proofTarget } : {},
    nodes
  };
}
function parseExplicitMerkleTree(input, title, proofTarget, extraMeta) {
  const lines2 = input.split(/\r?\n/).filter((l) => l.trim().length > 0);
  const nodes = [];
  for (const line of lines2) {
    const isLeaf = /^leaf\b/i.test(line);
    const isRoot = /^root\b/i.test(line);
    const clean = line.replace(/^(root|leaf|node)\s+/i, "");
    const arrowIdx = clean.indexOf("->");
    const decl = arrowIdx >= 0 ? clean.slice(0, arrowIdx).trim() : clean;
    const targets = arrowIdx >= 0 ? clean.slice(arrowIdx + 2).trim().split(/[,;\s]+/).filter(Boolean) : [];
    const bracketMatch = decl.match(/\[(.*?)\]/);
    let id = "";
    let hash = "";
    let data;
    if (bracketMatch) {
      id = decl.slice(0, bracketMatch.index).trim();
      const content = bracketMatch[1].trim();
      if (content.includes(":")) {
        const parts = content.split(":");
        hash = parts[0].trim();
        data = parts[1].trim().replace(/^["']|["']$/g, "");
      } else {
        hash = content;
      }
    } else {
      const parts = decl.split(/\s+/);
      id = parts[0];
      hash = parts[1] ?? mockHash(id);
    }
    if (!id) id = `m_${nodes.length + 1}`;
    nodes.push({
      id,
      hash,
      ...data ? { data } : {},
      isLeaf: isLeaf || targets.length === 0,
      ...isRoot ? { isRoot: true } : {},
      children: targets
    });
  }
  return {
    version: "1.0",
    metadata: { ...title ? { title } : {}, ...extraMeta ?? {} },
    ...title ? { title } : {},
    ...proofTarget ? { proofTarget } : {},
    nodes
  };
}
var ARROW_ID8 = "merkle-arrow";
var PROOF_ARROW_ID = "merkle-proof-arrow";
function layoutMerkleTree(ir, theme) {
  const { palette, typography, spacing, edges: edges2 } = theme;
  const p = pen(theme);
  const margin = spacing.diagramMargin;
  const font = typography.baseFontSize;
  const smallFont = typography.smallFontSize;
  if (ir.nodes.length === 0) {
    const scene2 = {
      viewBox: { x: 0, y: 0, width: 140, height: 90 },
      background: palette.background,
      elements: []
    };
    return { scene: scene2, anchors: {} };
  }
  const isCompact = ir.metadata["compact"] === true || ir.metadata["layout"] === "compact" || String(ir.metadata["compact"]) === "true";
  const sizes = /* @__PURE__ */ new Map();
  for (const node of ir.nodes) {
    const hashLabel = isCompact ? node.hash.slice(0, 6) : `hash: ${node.hash.slice(0, 8)}`;
    const dataLabel = node.data ? isCompact ? node.data : `data: ${node.data}` : void 0;
    const w = isCompact ? Math.max(
      measureText(hashLabel, smallFont - 1).width + 16,
      dataLabel ? measureText(dataLabel, smallFont - 1).width + 16 : 0,
      56
    ) : Math.max(
      measureText(hashLabel, smallFont).width + 24,
      dataLabel ? measureText(dataLabel, smallFont).width + 24 : 0,
      100
    );
    const h2 = node.data ? isCompact ? 40 : 48 : isCompact ? 28 : 36;
    sizes.set(node.id, { width: w, height: h2 });
  }
  const inputs = ir.nodes.map((n) => ({
    id: n.id,
    width: sizes.get(n.id).width,
    height: sizes.get(n.id).height,
    children: n.children
  }));
  const placed = treeLayout(inputs, {
    direction: ir.metadata["direction"] ?? "TB",
    levelGap: isCompact ? 36 : 52,
    siblingGap: isCompact ? 10 : 24,
    margin
  });
  const titleH = ir.metadata["title"] ? typography.titleFontSize + 24 : 0;
  const box = (id) => {
    const b = placed.boxes.get(id);
    return { x: b.x, y: b.y + titleH, width: b.width, height: b.height };
  };
  const elements = [];
  if (titleH > 0) {
    elements.push(
      p.text(
        String(ir.metadata["title"]),
        placed.width / 2,
        margin + typography.titleFontSize,
        typography.titleFontSize + 2,
        palette.text,
        { anchor: "middle", weight: "bold" }
      )
    );
  }
  for (const node of ir.nodes) {
    const pb = box(node.id);
    for (const cid of node.children) {
      const cb = box(cid);
      const childNode = ir.nodes.find((n) => n.id === cid);
      const isProofEdge = node.isProof && childNode?.isProof;
      const stroke = isProofEdge ? palette.primary : palette.textMuted;
      const strokeWidth = isProofEdge ? 2.2 : 1.5;
      const marker = isProofEdge ? PROOF_ARROW_ID : ARROW_ID8;
      elements.push(
        p.path(
          `M ${rhu(pb.x + pb.width / 2)} ${rhu(pb.y + pb.height)} L ${rhu(cb.x + cb.width / 2)} ${rhu(cb.y)}`,
          stroke,
          strokeWidth,
          { markerEnd: marker }
        )
      );
    }
  }
  for (const node of ir.nodes) {
    const b = box(node.id);
    let fill = palette.surface;
    let stroke = palette.border;
    let strokeWidth = 1.5;
    if (node.isRoot) {
      stroke = palette.primary;
      strokeWidth = 2;
    }
    if (node.isProof) {
      fill = palette.surface;
      stroke = palette.primary;
      strokeWidth = 2.4;
    } else if (node.isProofSibling) {
      stroke = palette.secondary ?? palette.primary;
      strokeWidth = 1.8;
    }
    elements.push(p.rect(b, fill, stroke, strokeWidth, { rx: isCompact ? 4 : 6 }));
    const hashText = isCompact ? node.hash.slice(0, 6) : node.hash.slice(0, 8);
    const hashY = node.data ? isCompact ? b.y + 14 : b.y + 18 : b.y + b.height / 2 + (isCompact ? smallFont - 1 : smallFont) * 0.35;
    elements.push(
      p.text(
        hashText,
        rhu(b.x + b.width / 2),
        rhu(hashY),
        isCompact ? smallFont - 1 : smallFont,
        palette.text,
        { anchor: "middle", weight: node.isRoot || node.isProof ? "bold" : "normal" }
      )
    );
    if (node.data) {
      const dataText = isCompact ? node.data : `data: ${node.data}`;
      const divY = isCompact ? b.y + 22 : b.y + 28;
      elements.push(
        p.path(`M ${rhu(b.x)} ${rhu(divY)} L ${rhu(b.x + b.width)} ${rhu(divY)}`, stroke, 1)
      );
      elements.push(
        p.text(
          dataText,
          rhu(b.x + b.width / 2),
          rhu(divY + (isCompact ? 11 : 14)),
          isCompact ? smallFont - 2 : smallFont - 1,
          palette.textMuted,
          { anchor: "middle" }
        )
      );
    }
    let badgeText;
    let badgeColor2 = palette.primary;
    if (node.isRoot) {
      badgeText = "ROOT";
      badgeColor2 = palette.primary;
    } else if (node.isProof) {
      badgeText = "VERIFY";
      badgeColor2 = palette.primary;
    } else if (node.isProofSibling) {
      badgeText = "SIBLING HASH";
      badgeColor2 = palette.secondary ?? palette.textMuted;
    }
    if (badgeText) {
      const bw = measureText(badgeText, smallFont - 2).width + 8;
      const bh = 13;
      const bx = b.x + b.width - bw - 4;
      const by = b.y - bh / 2;
      elements.push(
        p.rect(
          { x: rhu(bx), y: rhu(by), width: rhu(bw), height: bh },
          palette.background,
          badgeColor2,
          1,
          { rx: 3 }
        )
      );
      elements.push(
        p.text(
          badgeText,
          rhu(bx + bw / 2),
          rhu(by + bh / 2 + (smallFont - 2) * 0.35),
          smallFont - 2,
          badgeColor2,
          { anchor: "middle", weight: "bold" }
        )
      );
    }
  }
  const s = edges2?.arrowSize ?? 8;
  const sH = rhu(s * 0.7);
  const sMidY = rhu(s * 0.35);
  const sRefX = rhu(s - 1);
  const defs = [
    `<marker id="${ARROW_ID8}" markerWidth="${s}" markerHeight="${sH}" refX="${sRefX}" refY="${sMidY}" orient="auto" markerUnits="userSpaceOnUse"><polygon points="0 0, ${s} ${sMidY}, 0 ${sH}" fill="${palette.textMuted}" /></marker>`,
    `<marker id="${PROOF_ARROW_ID}" markerWidth="${s}" markerHeight="${sH}" refX="${sRefX}" refY="${sMidY}" orient="auto" markerUnits="userSpaceOnUse"><polygon points="0 0, ${s} ${sMidY}, 0 ${sH}" fill="${palette.primary}" /></marker>`
  ];
  const anchors = {};
  for (const node of ir.nodes) {
    const b = box(node.id);
    anchors[node.id] = { bounds: b };
    if (node.data) anchors[`data_${node.data}`] = { bounds: b };
  }
  const scene = applyOverlays(
    {
      viewBox: { x: 0, y: 0, width: rhuInt(placed.width), height: rhuInt(placed.height + titleH) },
      background: palette.background,
      elements,
      defs
    },
    ir.overlays,
    theme
  );
  return { scene, anchors };
}
var merkletree = {
  parseMermaid: buildMerkleTree,
  parseYaml: (input) => JSON.parse(input),
  layout: (ir, theme) => layoutMerkleTree(ir, theme)
};
function buildLsmTree(input) {
  const lines2 = input.split(/\r?\n/).map((l) => l.trim()).filter(Boolean);
  let title;
  let wal;
  const memtables = [];
  const levels = [];
  const operations = [];
  let currentLevel = null;
  for (const line of lines2) {
    if (/^lsmtree\b/i.test(line)) continue;
    const titleMatch = line.match(/^title\s+(.+)$/i);
    if (titleMatch) {
      title = titleMatch[1].trim().replace(/^["']|["']$/g, "");
      continue;
    }
    const walMatch = line.match(/^wal\s*(.*)$/i);
    if (walMatch) {
      wal = walMatch[1] ? walMatch[1].trim().replace(/^["']|["']$/g, "") : "Write-Ahead Log (WAL)";
      continue;
    }
    const memMatch = line.match(/^memtable\s+(\w+)\s*(?:"([^"]+)")?\s*(?:\[(.*?)\])?/i);
    if (memMatch) {
      const id = memMatch[1];
      const name = memMatch[2] ?? (id === "immutable" ? "Immutable MemTable" : "Active MemTable");
      const rawKeys = memMatch[3] ? memMatch[3].split("|").map((s) => s.trim()).filter(Boolean) : [];
      memtables.push({
        id,
        name,
        keys: rawKeys.length > 0 ? rawKeys : ["k1", "k2", "k3"],
        ...id.toLowerCase().includes("imm") ? { isImmutable: true } : {}
      });
      continue;
    }
    const levelStart = line.match(/^level\s+(\w+)\s*(?:"([^"]+)")?/i);
    if (levelStart) {
      if (currentLevel) levels.push(currentLevel);
      currentLevel = {
        id: levelStart[1],
        title: levelStart[2] ?? `Level ${levelStart[1]}`,
        ssts: []
      };
      continue;
    }
    if (/^end\b/i.test(line) && currentLevel) {
      levels.push(currentLevel);
      currentLevel = null;
      continue;
    }
    const sstMatch = line.match(/^sst\s+(\w+)\s*(?:\[(.*?)\])?\s*(?:"([^"]+)")?/i);
    if (sstMatch && currentLevel) {
      currentLevel.ssts.push({
        id: sstMatch[1],
        range: sstMatch[2] ?? "0..100",
        ...sstMatch[3] ? { size: sstMatch[3] } : {}
      });
      continue;
    }
    const flushMatch = line.match(/^flush\s+(\w+)\s*->\s*(\w+)(?:\s+"([^"]+)")?/i);
    if (flushMatch) {
      operations.push({
        type: "flush",
        from: flushMatch[1],
        to: flushMatch[2],
        ...flushMatch[3] ? { label: flushMatch[3] } : {}
      });
      continue;
    }
    const compactMatch = line.match(/^compact\s+([\w.]+)\s*->\s*([\w.]+)(?:\s+"([^"]+)")?/i);
    if (compactMatch) {
      operations.push({
        type: "compact",
        from: compactMatch[1],
        to: compactMatch[2],
        ...compactMatch[3] ? { label: compactMatch[3] } : {}
      });
      continue;
    }
  }
  if (currentLevel) levels.push(currentLevel);
  if (memtables.length === 0) {
    memtables.push(
      { id: "active", name: "Active MemTable", keys: ["10", "25", "40"] },
      { id: "immutable", name: "Immutable MemTable", keys: ["5", "18", "32"], isImmutable: true }
    );
  }
  if (levels.length === 0) {
    levels.push(
      { id: "L0", title: "Level 0 (Overlapping)", ssts: [{ id: "s0_1", range: "1..50" }, { id: "s0_2", range: "25..75" }] },
      { id: "L1", title: "Level 1 (Partitioned)", ssts: [{ id: "s1_1", range: "1..30" }, { id: "s1_2", range: "31..60" }, { id: "s1_3", range: "61..90" }] }
    );
  }
  return {
    version: "1.0",
    metadata: title ? { title } : {},
    ...title ? { title } : {},
    ...wal ? { wal } : {},
    memtables,
    levels,
    operations
  };
}
function layoutLsmTree(ir, theme) {
  const { palette, typography, spacing, edges: edges2 } = theme;
  const p = pen(theme);
  const margin = spacing.diagramMargin;
  const font = typography.baseFontSize;
  const smallFont = typography.smallFontSize;
  const elements = [];
  const anchors = {};
  const titleH = ir.title ? typography.titleFontSize + 24 : 0;
  let curY = margin + titleH;
  const totalWidth = 680;
  const contentWidth = totalWidth - margin * 2;
  if (ir.title) {
    elements.push(
      p.text(
        ir.title,
        totalWidth / 2,
        margin + typography.titleFontSize,
        typography.titleFontSize + 2,
        palette.text,
        { anchor: "middle", weight: "bold" }
      )
    );
  }
  const memY = curY;
  const memH = 100;
  elements.push(
    p.rect(
      { x: margin, y: memY, width: contentWidth, height: memH },
      palette.surface,
      palette.primary,
      1.5,
      { rx: 8 }
    )
  );
  elements.push(
    p.text(
      "MEMORY BUFFER (DRAM)",
      margin + 16,
      memY + 18,
      smallFont - 1,
      palette.primary,
      { weight: "bold" }
    )
  );
  let memCardsX = margin + 16;
  if (ir.wal) {
    const walW = 120;
    const walH = 54;
    const walBox = { x: memCardsX, y: memY + 30, width: walW, height: walH };
    elements.push(p.rect(walBox, palette.background, palette.textMuted, 1.2, { rx: 4 }));
    elements.push(
      p.text("WAL Log", walBox.x + walW / 2, walBox.y + 22, smallFont, palette.text, {
        anchor: "middle",
        weight: "bold"
      })
    );
    elements.push(
      p.text("append-only", walBox.x + walW / 2, walBox.y + 40, smallFont - 2, palette.textMuted, {
        anchor: "middle"
      })
    );
    anchors["wal"] = { bounds: walBox };
    memCardsX += walW + 16;
  }
  const memTableW = Math.min(220, (contentWidth - memCardsX + margin - 20) / ir.memtables.length);
  const memBoxes = /* @__PURE__ */ new Map();
  for (const mt of ir.memtables) {
    const mtBox = { x: memCardsX, y: memY + 30, width: memTableW, height: 54 };
    memBoxes.set(mt.id, mtBox);
    anchors[mt.id] = { bounds: mtBox };
    const stroke = mt.isImmutable ? palette.textMuted : palette.primary;
    elements.push(p.rect(mtBox, palette.background, stroke, 1.5, { rx: 4 }));
    elements.push(
      p.text(mt.name, mtBox.x + 8, mtBox.y + 16, smallFont - 1, stroke, { weight: "bold" })
    );
    const cellW = (mtBox.width - 16) / mt.keys.length;
    mt.keys.forEach((k, idx) => {
      const cx = mtBox.x + 8 + idx * cellW;
      const cy = mtBox.y + 24;
      elements.push(
        p.rect({ x: cx, y: cy, width: cellW, height: 22 }, palette.surface, palette.border, 1, {
          rx: 2
        })
      );
      elements.push(
        p.text(k, cx + cellW / 2, cy + 15, smallFont - 1, palette.text, {
          anchor: "middle",
          weight: "bold"
        })
      );
    });
    memCardsX += memTableW + 16;
  }
  curY += memH + 28;
  const levelBoxes = /* @__PURE__ */ new Map();
  const sstBoxes = /* @__PURE__ */ new Map();
  for (const lvl of ir.levels) {
    const lvlY = curY;
    const lvlH = 68;
    const lvlBox = { x: margin, y: lvlY, width: contentWidth, height: lvlH };
    levelBoxes.set(lvl.id, lvlBox);
    anchors[lvl.id] = { bounds: lvlBox };
    elements.push(p.rect(lvlBox, palette.surface, palette.border, 1.2, { rx: 6 }));
    const tagW = measureText(lvl.title, smallFont).width + 16;
    elements.push(
      p.rect(
        { x: margin + 12, y: lvlY + 10, width: tagW, height: 18 },
        palette.background,
        palette.textMuted,
        1,
        { rx: 3 }
      )
    );
    elements.push(
      p.text(lvl.title, margin + 12 + tagW / 2, lvlY + 23, smallFont - 1, palette.textMuted, {
        anchor: "middle",
        weight: "bold"
      })
    );
    const sstStartX = margin + tagW + 28;
    const availableW = contentWidth - (sstStartX - margin) - 16;
    const sstW = Math.min(130, availableW / Math.max(1, lvl.ssts.length) - 12);
    lvl.ssts.forEach((sst, idx) => {
      const sx = sstStartX + idx * (sstW + 12);
      const sy = lvlY + 12;
      const sbox = { x: sx, y: sy, width: sstW, height: 44 };
      sstBoxes.set(sst.id, sbox);
      sstBoxes.set(`${lvl.id}.${sst.id}`, sbox);
      anchors[sst.id] = { bounds: sbox };
      anchors[`${lvl.id}.${sst.id}`] = { bounds: sbox };
      elements.push(p.rect(sbox, palette.background, palette.primary, 1.5, { rx: 4 }));
      elements.push(
        p.text(`SST [${sst.range}]`, sx + sstW / 2, sy + 20, smallFont, palette.text, {
          anchor: "middle",
          weight: "bold"
        })
      );
      elements.push(
        p.text(
          sst.size ?? "Immutable block",
          sx + sstW / 2,
          sy + 34,
          smallFont - 2,
          palette.textMuted,
          { anchor: "middle" }
        )
      );
    });
    curY += lvlH + 20;
  }
  const FLUSH_ARROW_ID = "lsm-flush-arrow";
  const COMPACT_ARROW_ID = "lsm-compact-arrow";
  for (const op of ir.operations) {
    if (op.type === "flush") {
      const fromBox = memBoxes.get(op.from) ?? memBoxes.get("immutable") ?? memBoxes.get("active");
      const toBox = levelBoxes.get(op.to) ?? levelBoxes.get("L0");
      if (fromBox && toBox) {
        const startX = fromBox.x + fromBox.width / 2;
        const startY = fromBox.y + fromBox.height;
        const endX = toBox.x + 60;
        const endY = toBox.y;
        elements.push(
          p.path(
            `M ${rhu(startX)} ${rhu(startY)} L ${rhu(endX)} ${rhu(endY)}`,
            palette.primary,
            1.8,
            { markerEnd: FLUSH_ARROW_ID, dash: "3 3" }
          )
        );
      }
    } else if (op.type === "compact") {
      const fromBox = sstBoxes.get(op.from) ?? levelBoxes.get(op.from);
      const toBox = sstBoxes.get(op.to) ?? levelBoxes.get(op.to);
      if (fromBox && toBox) {
        const startX = fromBox.x + fromBox.width / 2;
        const startY = fromBox.y + fromBox.height;
        const endX = toBox.x + toBox.width / 2;
        const endY = toBox.y;
        elements.push(
          p.path(
            `M ${rhu(startX)} ${rhu(startY)} L ${rhu(endX)} ${rhu(endY)}`,
            palette.secondary ?? palette.primary,
            1.8,
            { markerEnd: COMPACT_ARROW_ID }
          )
        );
      }
    }
  }
  const s = edges2?.arrowSize ?? 8;
  const sH = rhu(s * 0.7);
  const sMidY = rhu(s * 0.35);
  const sRefX = rhu(s - 1);
  const defs = [
    `<marker id="${FLUSH_ARROW_ID}" markerWidth="${s}" markerHeight="${sH}" refX="${sRefX}" refY="${sMidY}" orient="auto" markerUnits="userSpaceOnUse"><polygon points="0 0, ${s} ${sMidY}, 0 ${sH}" fill="${palette.primary}" /></marker>`,
    `<marker id="${COMPACT_ARROW_ID}" markerWidth="${s}" markerHeight="${sH}" refX="${sRefX}" refY="${sMidY}" orient="auto" markerUnits="userSpaceOnUse"><polygon points="0 0, ${s} ${sMidY}, 0 ${sH}" fill="${palette.secondary ?? palette.primary}" /></marker>`
  ];
  const scene = applyOverlays(
    {
      viewBox: { x: 0, y: 0, width: totalWidth, height: rhuInt(curY + margin) },
      background: palette.background,
      elements,
      defs
    },
    ir.overlays,
    theme
  );
  return { scene, anchors };
}
var lsmtree = {
  parseMermaid: buildLsmTree,
  parseYaml: (input) => JSON.parse(input),
  layout: (ir, theme) => layoutLsmTree(ir, theme)
};
function buildBehaviorTree(input) {
  const lines2 = input.split(/\r?\n/).filter((l) => l.trim().length > 0);
  let title;
  let compact = false;
  let maxWidth;
  let direction;
  const filteredLines = [];
  for (const line of lines2) {
    const trimmed = line.trim();
    if (/^behaviortree\b/i.test(trimmed)) continue;
    const titleMatch = trimmed.match(/^title\s+(.+)$/i);
    if (titleMatch) {
      title = titleMatch[1].trim().replace(/^["']|["']$/g, "");
      continue;
    }
    const compactMatch = trimmed.match(/^(layout\s+compact|compact\s*(true)?|compact)\b/i);
    if (compactMatch) {
      compact = true;
      continue;
    }
    const maxWidthMatch = trimmed.match(/^max-?width\s+(\d+)/i);
    if (maxWidthMatch) {
      maxWidth = Number(maxWidthMatch[1]);
      compact = true;
      continue;
    }
    const dirMatch = trimmed.match(/^direction\s+(TD|TB|LR)/i);
    if (dirMatch) {
      direction = dirMatch[1] === "LR" ? "LR" : "TB";
      continue;
    }
    const indent = line.search(/\S|$/);
    filteredLines.push({ indent, text: trimmed });
  }
  const nodes = [];
  const stack2 = [];
  filteredLines.forEach((item, idx) => {
    const id = `bn_${idx + 1}`;
    while (stack2.length > 0 && stack2[stack2.length - 1].indent >= item.indent) {
      stack2.pop();
    }
    const parent = stack2.length > 0 ? stack2[stack2.length - 1] : void 0;
    let text = item.text;
    let status;
    let kind = "action";
    let decoratorType;
    const attrMatch = text.match(/\{(.*?)\}/);
    if (attrMatch) {
      const attrStr = attrMatch[1];
      const statMatch = attrStr.match(/status\s*:\s*["']?(\w+)["']?/i);
      if (statMatch) {
        status = statMatch[1].toLowerCase();
      }
      text = text.replace(/\{.*?\}/, "").trim();
    }
    if (/^(\?|fallback\b|selector\b)/i.test(text)) {
      kind = "selector";
      text = text.replace(/^(\?|fallback|selector)\s*/i, "");
    } else if (/^(->|→|sequence\b)/i.test(text)) {
      kind = "sequence";
      text = text.replace(/^(->|→|sequence)\s*/i, "");
    } else if (/^(\|\||⇶|parallel\b)/i.test(text)) {
      kind = "parallel";
      text = text.replace(/^(\|\||⇶|parallel)\s*/i, "");
    } else if (/^(decorator|invert|repeat|retry|timeout)\b/i.test(text)) {
      kind = "decorator";
      const decM = text.match(/^(\w+)/);
      decoratorType = decM ? decM[1] : "decorator";
      text = text.replace(/^(\w+)\s*/i, "");
    } else if (text.startsWith("(") && text.endsWith(")")) {
      kind = "condition";
      text = text.slice(1, -1).trim();
    } else if (text.startsWith("[") && text.endsWith("]")) {
      kind = "action";
      text = text.slice(1, -1).trim();
    } else if (/^condition\b/i.test(text)) {
      kind = "condition";
      text = text.replace(/^condition\s+/i, "");
    } else if (/^action\b/i.test(text)) {
      kind = "action";
      text = text.replace(/^action\s+/i, "");
    }
    const node = {
      id,
      label: text.replace(/^["']+|["']+$/g, "").trim() || (kind === "selector" ? "?" : kind === "sequence" ? "\u2192" : "Action"),
      kind,
      ...status ? { status } : {},
      ...decoratorType ? { decoratorType } : {},
      children: []
    };
    nodes.push(node);
    if (parent) {
      const pNode = nodes.find((n) => n.id === parent.id);
      if (pNode) {
        pNode.children.push(id);
      }
    }
    stack2.push({ indent: item.indent, id });
  });
  return {
    version: "1.0",
    metadata: {
      ...title ? { title } : {},
      ...compact ? { compact: true } : {},
      ...maxWidth !== void 0 ? { maxWidth } : {},
      ...direction ? { direction } : {}
    },
    ...title ? { title } : {},
    nodes
  };
}
var ARROW_ID9 = "bt-arrow";
function layoutBehaviorTree(ir, theme) {
  const { palette, typography, spacing, edges: edges2 } = theme;
  const p = pen(theme);
  const margin = spacing.diagramMargin;
  const font = typography.baseFontSize;
  const smallFont = typography.smallFontSize;
  if (ir.nodes.length === 0) {
    const scene2 = {
      viewBox: { x: 0, y: 0, width: 140, height: 90 },
      background: palette.background,
      elements: []
    };
    return { scene: scene2, anchors: {} };
  }
  const isCompact = ir.metadata["compact"] === true || ir.metadata["layout"] === "compact" || String(ir.metadata["compact"]) === "true";
  const sizes = /* @__PURE__ */ new Map();
  for (const node of ir.nodes) {
    let w = measureText(node.label, isCompact ? smallFont : font).width + (isCompact ? 24 : 36);
    let h2 = (isCompact ? smallFont : font) + (isCompact ? 16 : 24);
    if (node.kind === "selector" || node.kind === "sequence" || node.kind === "parallel") {
      w = Math.max(w, isCompact ? 44 : 54);
      h2 = isCompact ? 36 : 44;
    } else if (node.kind === "condition") {
      w = Math.max(w, isCompact ? 64 : 80);
      h2 = isCompact ? 30 : 38;
    } else {
      w = Math.max(w, isCompact ? 70 : 90);
      h2 = isCompact ? 32 : 42;
    }
    if (node.status) w += isCompact ? 14 : 20;
    sizes.set(node.id, { width: w, height: h2 });
  }
  const inputs = ir.nodes.map((n) => ({
    id: n.id,
    width: sizes.get(n.id).width,
    height: sizes.get(n.id).height,
    children: n.children
  }));
  const placed = treeLayout(inputs, {
    direction: ir.metadata["direction"] ?? "TB",
    levelGap: isCompact ? 38 : 56,
    siblingGap: isCompact ? 12 : 24,
    margin
  });
  const titleH = ir.title ? typography.titleFontSize + 24 : 0;
  const box = (id) => {
    const b = placed.boxes.get(id);
    return { x: b.x, y: b.y + titleH, width: b.width, height: b.height };
  };
  const elements = [];
  if (ir.title) {
    elements.push(
      p.text(
        ir.title,
        placed.width / 2,
        margin + typography.titleFontSize,
        typography.titleFontSize + 2,
        palette.text,
        { anchor: "middle", weight: "bold" }
      )
    );
  }
  for (const node of ir.nodes) {
    const pb = box(node.id);
    for (const cid of node.children) {
      const cb = box(cid);
      elements.push(
        p.path(
          `M ${rhu(pb.x + pb.width / 2)} ${rhu(pb.y + pb.height)} L ${rhu(cb.x + cb.width / 2)} ${rhu(cb.y)}`,
          palette.textMuted,
          1.5,
          { markerEnd: ARROW_ID9 }
        )
      );
    }
  }
  for (const node of ir.nodes) {
    const b = box(node.id);
    let fill = palette.surface;
    let stroke = palette.border;
    let rx = 4;
    if (node.kind === "selector") {
      stroke = palette.primary;
      rx = 6;
    } else if (node.kind === "sequence") {
      stroke = palette.secondary ?? palette.primary;
      rx = 6;
    } else if (node.kind === "parallel") {
      stroke = palette.primary;
      rx = 6;
    } else if (node.kind === "condition") {
      rx = b.height / 2;
      stroke = palette.textMuted;
    } else if (node.kind === "decorator") {
      rx = 4;
      stroke = palette.secondary ?? palette.primary;
    }
    elements.push(p.rect(b, fill, stroke, 1.5, { rx }));
    let prefix = "";
    if (node.kind === "selector") prefix = "? ";
    else if (node.kind === "sequence") prefix = "\u2192 ";
    else if (node.kind === "parallel") prefix = "\u21F6 ";
    const displayText = `${prefix}${node.label}`;
    const textX = node.status ? b.x + (b.width - 24) / 2 : b.x + b.width / 2;
    const textY = b.y + b.height / 2 + font * 0.35;
    elements.push(
      p.text(
        displayText,
        rhu(textX),
        rhu(textY),
        font,
        palette.text,
        { anchor: "middle", weight: node.kind === "action" || node.kind === "condition" ? "normal" : "bold" }
      )
    );
    if (node.status) {
      let icon = "";
      let statColor = palette.textMuted;
      if (node.status === "success") {
        icon = "\u2713";
        statColor = "#10B981";
      } else if (node.status === "running") {
        icon = "\u27F3";
        statColor = "#F59E0B";
      } else if (node.status === "failure") {
        icon = "\u2717";
        statColor = "#EF4444";
      } else {
        icon = "\u2022";
        statColor = palette.textMuted;
      }
      const badgeX = b.x + b.width - 16;
      const badgeY = b.y + b.height / 2;
      elements.push(
        p.circle({ x: badgeX, y: badgeY }, 9, palette.background, statColor, 1.2)
      );
      elements.push(
        p.text(icon, badgeX, badgeY + 4, smallFont, statColor, { anchor: "middle", weight: "bold" })
      );
    }
  }
  const s = edges2?.arrowSize ?? 8;
  const sH = rhu(s * 0.7);
  const sMidY = rhu(s * 0.35);
  const sRefX = rhu(s - 1);
  const defs = [
    `<marker id="${ARROW_ID9}" markerWidth="${s}" markerHeight="${sH}" refX="${sRefX}" refY="${sMidY}" orient="auto" markerUnits="userSpaceOnUse"><polygon points="0 0, ${s} ${sMidY}, 0 ${sH}" fill="${palette.textMuted}" /></marker>`
  ];
  const anchors = {};
  for (const node of ir.nodes) {
    anchors[node.id] = { bounds: box(node.id) };
  }
  const scene = applyOverlays(
    {
      viewBox: { x: 0, y: 0, width: rhuInt(placed.width), height: rhuInt(placed.height + titleH) },
      background: palette.background,
      elements,
      defs
    },
    ir.overlays,
    theme
  );
  return { scene, anchors };
}
var behaviortree = {
  parseMermaid: buildBehaviorTree,
  parseYaml: (input) => JSON.parse(input),
  layout: (ir, theme) => layoutBehaviorTree(ir, theme)
};
function buildQuadTree(input) {
  const lines2 = input.split(/\r?\n/).map((l) => l.trim()).filter(Boolean);
  let title;
  let capacity = 1;
  let bounds = { x: 0, y: 0, w: 100, h: 100 };
  let compact = false;
  let maxWidth;
  let direction;
  const rawPoints = [];
  const manualLines = [];
  for (const line of lines2) {
    if (/^quadtree\b/i.test(line)) continue;
    const titleMatch = line.match(/^title\s+(.+)$/i);
    if (titleMatch) {
      title = titleMatch[1].trim().replace(/^["']|["']$/g, "");
      continue;
    }
    const compactMatch = line.match(/^(layout\s+compact|compact\s*(true)?|compact)\b/i);
    if (compactMatch) {
      compact = true;
      continue;
    }
    const maxWidthMatch = line.match(/^max-?width\s+(\d+)/i);
    if (maxWidthMatch) {
      maxWidth = Number(maxWidthMatch[1]);
      compact = true;
      continue;
    }
    const dirMatch = line.match(/^direction\s+(TD|TB|LR)/i);
    if (dirMatch) {
      direction = dirMatch[1] === "LR" ? "LR" : "TB";
      continue;
    }
    const capMatch = line.match(/^capacity\s+(\d+)/i);
    if (capMatch) {
      capacity = Math.max(1, Number(capMatch[1]));
      continue;
    }
    const boundsMatch = line.match(/^bounds\s+(-?\d+)\s+(-?\d+)\s+(-?\d+)\s+(-?\d+)/i);
    if (boundsMatch) {
      bounds = {
        x: Number(boundsMatch[1]),
        y: Number(boundsMatch[2]),
        w: Number(boundsMatch[3]),
        h: Number(boundsMatch[4])
      };
      continue;
    }
    const insertMatch = line.match(/^insert\s*\(\s*(-?\d+(?:\.\d+)?)\s*,\s*(-?\d+(?:\.\d+)?)\s*\)(?:\s+["']?([^"']+)["']?)?/i);
    if (insertMatch) {
      rawPoints.push({
        x: Number(insertMatch[1]),
        y: Number(insertMatch[2]),
        ...insertMatch[3] ? { label: insertMatch[3] } : {}
      });
      continue;
    }
    manualLines.push(line);
  }
  const extraMeta = {
    ...compact ? { compact: true } : {},
    ...maxWidth !== void 0 ? { maxWidth } : {},
    ...direction ? { direction } : {}
  };
  if (rawPoints.length > 0) {
    return buildAlgorithmicQuadTree(rawPoints, bounds, capacity, title, extraMeta);
  }
  return parseExplicitQuadTree(manualLines.join("\n"), bounds, title, extraMeta);
}
function buildAlgorithmicQuadTree(points, bounds, capacity, title, extraMeta) {
  let idCounter = 1;
  function subdivide(box, pts, label, depth) {
    const id = `q_${idCounter++}`;
    if (pts.length <= capacity || depth >= 4) {
      return { id, label, b: box, pts, children: [] };
    }
    const hw = box.w / 2;
    const hh = box.h / 2;
    const nwBox = { x: box.x, y: box.y, w: hw, h: hh };
    const neBox = { x: box.x + hw, y: box.y, w: hw, h: hh };
    const swBox = { x: box.x, y: box.y + hh, w: hw, h: hh };
    const seBox = { x: box.x + hw, y: box.y + hh, w: hw, h: hh };
    const inBox = (p, b) => p.x >= b.x && p.x < b.x + b.w && p.y >= b.y && p.y < b.y + b.h;
    const nwPts = pts.filter((p) => inBox(p, nwBox));
    const nePts = pts.filter((p) => inBox(p, neBox));
    const swPts = pts.filter((p) => inBox(p, swBox));
    const sePts = pts.filter((p) => inBox(p, seBox));
    return {
      id,
      label,
      b: box,
      pts: [],
      children: [
        subdivide(nwBox, nwPts, `${label}-NW`, depth + 1),
        subdivide(neBox, nePts, `${label}-NE`, depth + 1),
        subdivide(swBox, swPts, `${label}-SW`, depth + 1),
        subdivide(seBox, sePts, `${label}-SE`, depth + 1)
      ]
    };
  }
  const root = subdivide(bounds, points, "Root", 0);
  const flatNodes = [];
  function collect2(node) {
    flatNodes.push({
      id: node.id,
      label: node.label,
      bounds: node.b,
      points: node.pts,
      children: node.children.map((c) => c.id)
    });
    for (const c of node.children) collect2(c);
  }
  collect2(root);
  return {
    version: "1.0",
    metadata: { ...title ? { title } : {}, ...extraMeta ?? {} },
    ...title ? { title } : {},
    bounds,
    capacity,
    nodes: flatNodes
  };
}
function parseExplicitQuadTree(input, bounds, title, extraMeta) {
  const lines2 = input.split(/\r?\n/).filter((l) => l.trim().length > 0);
  const nodes = [];
  for (const line of lines2) {
    const clean = line.replace(/^(root|leaf|node)\s+/i, "");
    const arrowIdx = clean.indexOf("->");
    const decl = arrowIdx >= 0 ? clean.slice(0, arrowIdx).trim() : clean;
    const targets = arrowIdx >= 0 ? clean.slice(arrowIdx + 2).trim().split(/[,;\s]+/).filter(Boolean) : [];
    const bracketMatch = decl.match(/\[(.*?)\]/);
    let id = "";
    let label = "";
    if (bracketMatch) {
      id = decl.slice(0, bracketMatch.index).trim();
      label = bracketMatch[1].trim();
    } else {
      const parts = decl.split(/\s+/);
      id = parts[0];
      label = parts.slice(1).join(" ") || id;
    }
    if (!id) id = `qn_${nodes.length + 1}`;
    nodes.push({
      id,
      label,
      bounds,
      points: [],
      children: targets
    });
  }
  return {
    version: "1.0",
    metadata: { ...title ? { title } : {}, ...extraMeta ?? {} },
    ...title ? { title } : {},
    bounds,
    capacity: 1,
    nodes
  };
}
var ARROW_ID10 = "quad-arrow";
function layoutQuadTree(ir, theme) {
  const { palette, typography, spacing, edges: edges2 } = theme;
  const p = pen(theme);
  const margin = spacing.diagramMargin;
  const font = typography.baseFontSize;
  const smallFont = typography.smallFontSize;
  if (ir.nodes.length === 0) {
    const scene2 = {
      viewBox: { x: 0, y: 0, width: 140, height: 90 },
      background: palette.background,
      elements: []
    };
    return { scene: scene2, anchors: {} };
  }
  const isCompact = ir.metadata["compact"] === true || ir.metadata["layout"] === "compact" || String(ir.metadata["compact"]) === "true";
  const sizes = /* @__PURE__ */ new Map();
  for (const node of ir.nodes) {
    const ptText = node.points.length > 0 ? node.points.map((pt) => pt.label ?? `(${pt.x},${pt.y})`).join(", ") : "";
    const mainW = measureText(node.label, smallFont).width + (isCompact ? 16 : 24);
    const ptW = ptText ? measureText(ptText, smallFont - 1).width + (isCompact ? 16 : 24) : 0;
    const w = Math.max(mainW, ptW, isCompact ? 56 : 80);
    const h2 = ptText ? isCompact ? 36 : 44 : isCompact ? 26 : 32;
    sizes.set(node.id, { width: w, height: h2 });
  }
  const inputs = ir.nodes.map((n) => ({
    id: n.id,
    width: sizes.get(n.id).width,
    height: sizes.get(n.id).height,
    children: n.children
  }));
  const treePlaced = treeLayout(inputs, {
    direction: ir.metadata["direction"] ?? "TB",
    levelGap: isCompact ? 36 : 52,
    siblingGap: isCompact ? 10 : 20,
    margin
  });
  const titleH = ir.title ? typography.titleFontSize + 24 : 0;
  const gridSize = Math.max(220, Math.min(280, treePlaced.height - 40));
  const gridX = margin;
  const gridY = margin + titleH + 10;
  const treeOffsetX = gridX + gridSize + 40;
  const box = (id) => {
    const b = treePlaced.boxes.get(id);
    return { x: b.x + treeOffsetX, y: b.y + titleH, width: b.width, height: b.height };
  };
  const elements = [];
  if (ir.title) {
    elements.push(
      p.text(
        ir.title,
        (treeOffsetX + treePlaced.width) / 2,
        margin + typography.titleFontSize,
        typography.titleFontSize + 2,
        palette.text,
        { anchor: "middle", weight: "bold" }
      )
    );
  }
  elements.push(
    p.text("SPATIAL 2D PARTITION", gridX + gridSize / 2, gridY - 8, smallFont, palette.textMuted, {
      anchor: "middle",
      weight: "bold"
    })
  );
  elements.push(
    p.rect(
      { x: gridX, y: gridY, width: gridSize, height: gridSize },
      palette.surface,
      palette.primary,
      1.8,
      { rx: 6 }
    )
  );
  const scaleX = gridSize / ir.bounds.w;
  const scaleY = gridSize / ir.bounds.h;
  for (const node of ir.nodes) {
    if (node.children.length > 0) {
      const midX = gridX + (node.bounds.x + node.bounds.w / 2 - ir.bounds.x) * scaleX;
      const midY = gridY + (node.bounds.y + node.bounds.h / 2 - ir.bounds.y) * scaleY;
      const minX = gridX + (node.bounds.x - ir.bounds.x) * scaleX;
      const maxX = gridX + (node.bounds.x + node.bounds.w - ir.bounds.x) * scaleX;
      const minY = gridY + (node.bounds.y - ir.bounds.y) * scaleY;
      const maxY = gridY + (node.bounds.y + node.bounds.h - ir.bounds.y) * scaleY;
      elements.push(p.path(`M ${rhu(midX)} ${rhu(minY)} L ${rhu(midX)} ${rhu(maxY)}`, palette.border, 1.2));
      elements.push(p.path(`M ${rhu(minX)} ${rhu(midY)} L ${rhu(maxX)} ${rhu(midY)}`, palette.border, 1.2));
    }
    for (const pt of node.points) {
      const px = gridX + (pt.x - ir.bounds.x) * scaleX;
      const py = gridY + (pt.y - ir.bounds.y) * scaleY;
      elements.push(p.circle({ x: px, y: py }, 4.5, palette.primary, palette.background, 1.5));
      const ptLabel = pt.label ? `${pt.label} (${pt.x},${pt.y})` : `(${pt.x},${pt.y})`;
      elements.push(
        p.text(ptLabel, px + 8, py + 3, smallFont - 2, palette.text, { weight: "bold" })
      );
    }
  }
  for (const node of ir.nodes) {
    const pb = box(node.id);
    for (const cid of node.children) {
      const cb = box(cid);
      elements.push(
        p.path(
          `M ${rhu(pb.x + pb.width / 2)} ${rhu(pb.y + pb.height)} L ${rhu(cb.x + cb.width / 2)} ${rhu(cb.y)}`,
          palette.textMuted,
          1.5,
          { markerEnd: ARROW_ID10 }
        )
      );
    }
  }
  for (const node of ir.nodes) {
    const b = box(node.id);
    const isLeaf = node.children.length === 0;
    const stroke = isLeaf ? palette.primary : palette.border;
    elements.push(p.rect(b, palette.surface, stroke, isLeaf ? 1.8 : 1.5, { rx: 4 }));
    const textY = node.points.length > 0 ? b.y + 16 : b.y + b.height / 2 + smallFont * 0.35;
    elements.push(
      p.text(node.label, rhu(b.x + b.width / 2), rhu(textY), smallFont, palette.text, {
        anchor: "middle",
        weight: "bold"
      })
    );
    if (node.points.length > 0) {
      const ptText = node.points.map((pt) => pt.label ?? `(${pt.x},${pt.y})`).join(", ");
      elements.push(
        p.text(ptText, rhu(b.x + b.width / 2), rhu(b.y + 34), smallFont - 1, palette.primary, {
          anchor: "middle"
        })
      );
    }
  }
  const s = edges2?.arrowSize ?? 8;
  const sH = rhu(s * 0.7);
  const sMidY = rhu(s * 0.35);
  const sRefX = rhu(s - 1);
  const defs = [
    `<marker id="${ARROW_ID10}" markerWidth="${s}" markerHeight="${sH}" refX="${sRefX}" refY="${sMidY}" orient="auto" markerUnits="userSpaceOnUse"><polygon points="0 0, ${s} ${sMidY}, 0 ${sH}" fill="${palette.textMuted}" /></marker>`
  ];
  const totalW = rhuInt(treeOffsetX + treePlaced.width);
  const totalH = rhuInt(Math.max(gridY + gridSize + margin, treePlaced.height + titleH + margin));
  const anchors = {};
  for (const node of ir.nodes) {
    anchors[node.id] = { bounds: box(node.id) };
  }
  const scene = applyOverlays(
    {
      viewBox: { x: 0, y: 0, width: totalW, height: totalH },
      background: palette.background,
      elements,
      defs
    },
    ir.overlays,
    theme
  );
  return { scene, anchors };
}
var quadtree = {
  parseMermaid: buildQuadTree,
  parseYaml: (input) => JSON.parse(input),
  layout: (ir, theme) => layoutQuadTree(ir, theme)
};
function rotateRight(y) {
  const x = y.left;
  y.left = x.right;
  x.right = y;
  return x;
}
function rotateLeft(x) {
  const y = x.right;
  x.right = y.left;
  y.left = x;
  return y;
}
function insertTreap(node, key, priority) {
  if (!node) return { key, priority, left: null, right: null };
  if (key < node.key) {
    node.left = insertTreap(node.left, key, priority);
    if (node.left.priority > node.priority) {
      node = rotateRight(node);
    }
  } else if (key > node.key) {
    node.right = insertTreap(node.right, key, priority);
    if (node.right.priority > node.priority) {
      node = rotateLeft(node);
    }
  }
  return node;
}
function buildTreap(input) {
  const lines2 = input.split(/\r?\n/).map((l) => l.trim()).filter(Boolean);
  let title;
  const pairs = [];
  const manualLines = [];
  for (const line of lines2) {
    if (/^treap\b/i.test(line)) continue;
    const titleMatch = line.match(/^title\s+(.+)$/i);
    if (titleMatch) {
      title = titleMatch[1].trim().replace(/^["']|["']$/g, "");
      continue;
    }
    const pairMatches = line.matchAll(/\(?\s*(?:k\s*:\s*)?(-?\d+|[a-zA-Z]+)\s*[,:]\s*(?:p\s*:\s*)?(-?\d+)\s*\)?/gi);
    let matchedAny = false;
    for (const m of pairMatches) {
      matchedAny = true;
      const rawK = m[1];
      const key = /^-?\d+$/.test(rawK) ? Number(rawK) : rawK;
      const priority = Number(m[2]);
      pairs.push({ key, priority });
    }
    if (!matchedAny) manualLines.push(line);
  }
  if (pairs.length > 0) {
    let root = null;
    for (const p of pairs) {
      root = insertTreap(root, p.key, p.priority);
    }
    const nodes2 = [];
    let idCount = 1;
    const emit = (n) => {
      const id = `t_${idCount++}`;
      const children = [];
      if (n.left) children.push(emit(n.left));
      if (n.right) children.push(emit(n.right));
      nodes2.push({
        id,
        key: String(n.key),
        priority: n.priority,
        children
      });
      return id;
    };
    if (root) emit(root);
    return {
      version: "1.0",
      metadata: title ? { title } : {},
      ...title ? { title } : {},
      nodes: nodes2
    };
  }
  const nodes = [];
  for (const line of manualLines) {
    const arrowIdx = line.indexOf("->");
    const decl = arrowIdx >= 0 ? line.slice(0, arrowIdx).trim() : line.trim();
    const targets = arrowIdx >= 0 ? line.slice(arrowIdx + 2).trim().split(/[,;\s]+/).filter(Boolean) : [];
    const bracketMatch = decl.match(/\[(.*?)\]/);
    let id = "";
    let key = "";
    let priority = 0;
    if (bracketMatch) {
      id = decl.slice(0, bracketMatch.index).trim();
      const content = bracketMatch[1].trim();
      const m = content.match(/^(.*?)(?:,\s*p\s*:\s*(\d+))?$/i);
      key = m ? m[1].trim() : content;
      priority = m && m[2] ? Number(m[2]) : 50;
    } else {
      const parts = decl.split(/\s+/);
      id = parts[0];
      key = parts[1] ?? id;
      priority = parts[2] ? Number(parts[2]) : 50;
    }
    if (!id) id = `t_${nodes.length + 1}`;
    nodes.push({
      id,
      key,
      priority,
      children: targets
    });
  }
  return {
    version: "1.0",
    metadata: title ? { title } : {},
    ...title ? { title } : {},
    nodes
  };
}
var ARROW_ID11 = "treap-arrow";
function layoutTreap(ir, theme) {
  const { palette, typography, spacing, edges: edges2 } = theme;
  const p = pen(theme);
  const margin = spacing.diagramMargin;
  const font = typography.baseFontSize;
  const smallFont = typography.smallFontSize;
  if (ir.nodes.length === 0) {
    const scene2 = {
      viewBox: { x: 0, y: 0, width: 140, height: 90 },
      background: palette.background,
      elements: []
    };
    return { scene: scene2, anchors: {} };
  }
  const sizes = /* @__PURE__ */ new Map();
  for (const node of ir.nodes) {
    const keyW = measureText(node.key, font).width;
    const prioW = measureText(String(node.priority), smallFont - 1).width + 12;
    const w = Math.max(keyW + prioW + 28, 68);
    const h2 = 38;
    sizes.set(node.id, { width: w, height: h2 });
  }
  const inputs = ir.nodes.map((n) => ({
    id: n.id,
    width: sizes.get(n.id).width,
    height: sizes.get(n.id).height,
    children: n.children
  }));
  const placed = treeLayout(inputs, {
    direction: "TB",
    levelGap: 52,
    siblingGap: 24,
    margin
  });
  const titleH = ir.title ? typography.titleFontSize + 24 : 0;
  const box = (id) => {
    const b = placed.boxes.get(id);
    return { x: b.x, y: b.y + titleH, width: b.width, height: b.height };
  };
  const elements = [];
  if (ir.title) {
    elements.push(
      p.text(
        ir.title,
        placed.width / 2,
        margin + typography.titleFontSize,
        typography.titleFontSize + 2,
        palette.text,
        { anchor: "middle", weight: "bold" }
      )
    );
  }
  for (const node of ir.nodes) {
    const pb = box(node.id);
    for (const cid of node.children) {
      const cb = box(cid);
      elements.push(
        p.path(
          `M ${rhu(pb.x + pb.width / 2)} ${rhu(pb.y + pb.height)} L ${rhu(cb.x + cb.width / 2)} ${rhu(cb.y)}`,
          palette.textMuted,
          1.5,
          { markerEnd: ARROW_ID11 }
        )
      );
    }
  }
  for (const node of ir.nodes) {
    const b = box(node.id);
    elements.push(p.rect(b, palette.surface, palette.primary, 1.6, { rx: b.height / 2 }));
    const keyX = b.x + (b.width - 24) / 2;
    elements.push(
      p.text(
        node.key,
        rhu(keyX),
        rhu(b.y + b.height / 2 + font * 0.35),
        font,
        palette.text,
        { anchor: "middle", weight: "bold" }
      )
    );
    const prioText = String(node.priority);
    const prioW = measureText(prioText, smallFont - 1).width + 10;
    const prioH = 16;
    const prioX = b.x + b.width - prioW - 4;
    const prioY = b.y + (b.height - prioH) / 2;
    elements.push(
      p.rect(
        { x: rhu(prioX), y: rhu(prioY), width: rhu(prioW), height: prioH },
        palette.primary,
        palette.primary,
        1,
        { rx: prioH / 2 }
      )
    );
    elements.push(
      p.text(
        prioText,
        rhu(prioX + prioW / 2),
        rhu(prioY + prioH / 2 + (smallFont - 1) * 0.35),
        smallFont - 1,
        "#FFFFFF",
        { anchor: "middle", weight: "bold" }
      )
    );
  }
  const s = edges2?.arrowSize ?? 8;
  const sH = rhu(s * 0.7);
  const sMidY = rhu(s * 0.35);
  const sRefX = rhu(s - 1);
  const defs = [
    `<marker id="${ARROW_ID11}" markerWidth="${s}" markerHeight="${sH}" refX="${sRefX}" refY="${sMidY}" orient="auto" markerUnits="userSpaceOnUse"><polygon points="0 0, ${s} ${sMidY}, 0 ${sH}" fill="${palette.textMuted}" /></marker>`
  ];
  const anchors = {};
  for (const node of ir.nodes) {
    anchors[node.id] = { bounds: box(node.id) };
  }
  const scene = applyOverlays(
    {
      viewBox: { x: 0, y: 0, width: rhuInt(placed.width), height: rhuInt(placed.height + titleH) },
      background: palette.background,
      elements,
      defs
    },
    ir.overlays,
    theme
  );
  return { scene, anchors };
}
var treap = {
  parseMermaid: buildTreap,
  parseYaml: (input) => JSON.parse(input),
  layout: (ir, theme) => layoutTreap(ir, theme)
};
function insert234(root, key) {
  if (root.keys.length === 3) {
    const newRoot = { keys: [], children: [root] };
    splitChild(newRoot, 0);
    insertNonFull(newRoot, key);
    return newRoot;
  }
  insertNonFull(root, key);
  return root;
}
function splitChild(parent, index) {
  const full = parent.children[index];
  const right = {
    keys: [full.keys[2]],
    children: full.children.length > 0 ? full.children.slice(2) : []
  };
  const upKey = full.keys[1];
  full.keys = [full.keys[0]];
  if (full.children.length > 0) {
    full.children = full.children.slice(0, 2);
  }
  parent.children.splice(index + 1, 0, right);
  parent.keys.splice(index, 0, upKey);
}
function insertNonFull(node, key) {
  let i = node.keys.length - 1;
  if (node.children.length === 0) {
    while (i >= 0 && key < node.keys[i]) i--;
    if (i >= 0 && node.keys[i] === key) return;
    node.keys.splice(i + 1, 0, key);
    return;
  }
  while (i >= 0 && key < node.keys[i]) i--;
  i++;
  if (node.children[i].keys.length === 3) {
    splitChild(node, i);
    if (key > node.keys[i]) {
      i++;
    } else if (key === node.keys[i]) {
      return;
    }
  }
  insertNonFull(node.children[i], key);
}
function buildTree234(input) {
  const lines2 = input.split(/\r?\n/).map((l) => l.trim()).filter(Boolean);
  let title;
  const filteredLines = [];
  for (const line of lines2) {
    if (/^(2-3-4tree|234tree)\b/i.test(line)) continue;
    const titleMatch = line.match(/^title\s+(.+)$/i);
    if (titleMatch) {
      title = titleMatch[1].trim().replace(/^["']|["']$/g, "");
      continue;
    }
    filteredLines.push(line);
  }
  const clean = filteredLines.join("\n");
  const numbers = (clean.match(/-?\d+/g) ?? []).map(Number);
  if (numbers.length === 0) {
    return {
      version: "1.0",
      metadata: title ? { title } : {},
      ...title ? { title } : {},
      nodes: []
    };
  }
  let root = { keys: [], children: [] };
  for (const n of numbers) {
    if (root.keys.length === 0 && root.children.length === 0) {
      root.keys.push(n);
    } else {
      root = insert234(root, n);
    }
  }
  const nodes = [];
  let idCount = 1;
  const emit = (n) => {
    const id = `n234_${idCount++}`;
    const childIds = [];
    for (const c of n.children) {
      childIds.push(emit(c));
    }
    nodes.push({
      id,
      keys: n.keys,
      children: childIds
    });
    return id;
  };
  emit(root);
  return {
    version: "1.0",
    metadata: title ? { title } : {},
    ...title ? { title } : {},
    nodes
  };
}
var ARROW_ID12 = "tree234-arrow";
function layoutTree234(ir, theme) {
  const { palette, typography, spacing, edges: edges2 } = theme;
  const p = pen(theme);
  const margin = spacing.diagramMargin;
  const font = typography.baseFontSize;
  const smallFont = typography.smallFontSize;
  if (ir.nodes.length === 0) {
    const scene2 = {
      viewBox: { x: 0, y: 0, width: 140, height: 90 },
      background: palette.background,
      elements: []
    };
    return { scene: scene2, anchors: {} };
  }
  const sizes = /* @__PURE__ */ new Map();
  for (const node of ir.nodes) {
    const cellWidths = node.keys.map((k) => Math.max(measureText(String(k), font).width + 24, 38));
    const width = cellWidths.reduce((sum, w) => sum + w, 0);
    const height = font + 20;
    sizes.set(node.id, { width, height, cellWidths });
  }
  const inputs = ir.nodes.map((n) => ({
    id: n.id,
    width: sizes.get(n.id).width,
    height: sizes.get(n.id).height,
    children: n.children
  }));
  const parentMap = /* @__PURE__ */ new Map();
  for (const node of ir.nodes) {
    for (const cid of node.children) {
      parentMap.set(cid, node.id);
    }
  }
  const placed = treeLayout(inputs, {
    direction: "TB",
    levelGap: 66,
    siblingGap: 24,
    margin
  });
  const titleH = ir.title ? typography.titleFontSize + 24 : 0;
  const box = (id) => {
    const b = placed.boxes.get(id);
    return { x: b.x, y: b.y + titleH, width: b.width, height: b.height };
  };
  const elements = [];
  if (ir.title) {
    elements.push(
      p.text(
        ir.title,
        placed.width / 2,
        margin + typography.titleFontSize,
        typography.titleFontSize + 2,
        palette.text,
        { anchor: "middle", weight: "bold" }
      )
    );
  }
  for (const node of ir.nodes) {
    const pb = box(node.id);
    const cellWidths = sizes.get(node.id).cellWidths;
    const childCount = node.children.length;
    node.children.forEach((cid, cIdx) => {
      const cb = box(cid);
      let startX;
      if (childCount === cellWidths.length + 1) {
        if (cIdx === 0) {
          startX = pb.x;
        } else if (cIdx === childCount - 1) {
          startX = pb.x + pb.width;
        } else {
          let curX = pb.x;
          for (let j = 0; j < cIdx; j++) curX += cellWidths[j];
          startX = curX;
        }
      } else {
        const { start } = connectSlots(pb, cb);
        startX = start.x;
      }
      elements.push(
        p.path(
          `M ${rhu(startX)} ${rhu(pb.y + pb.height)} L ${rhu(cb.x + cb.width / 2)} ${rhu(cb.y)}`,
          palette.textMuted,
          1.5,
          { markerEnd: ARROW_ID12 }
        )
      );
    });
  }
  for (const node of ir.nodes) {
    const b = box(node.id);
    const cellWidths = sizes.get(node.id).cellWidths;
    const nodeType = node.keys.length === 1 ? "2-node" : node.keys.length === 2 ? "3-node" : "4-node";
    elements.push(p.rect(b, palette.surface, palette.primary, 1.6, { rx: 4 }));
    let sx = b.x;
    node.keys.forEach((k, idx) => {
      const cw = cellWidths[idx];
      if (idx > 0) {
        elements.push(
          p.path(`M ${rhu(sx)} ${rhu(b.y)} L ${rhu(sx)} ${rhu(b.y + b.height)}`, palette.primary, 1)
        );
      }
      elements.push(
        p.text(
          String(k),
          rhu(sx + cw / 2),
          rhu(b.y + b.height / 2 + font * 0.35),
          font,
          palette.text,
          { anchor: "middle", weight: "bold" }
        )
      );
      sx += cw;
    });
    const tagY = !parentMap.has(node.id) ? b.y - 8 : b.y + b.height + 12;
    elements.push(
      p.text(nodeType, rhu(b.x + b.width / 2), rhu(tagY), smallFont - 2, palette.textMuted, {
        anchor: "middle"
      })
    );
  }
  const s = edges2?.arrowSize ?? 8;
  const sH = rhu(s * 0.7);
  const sMidY = rhu(s * 0.35);
  const sRefX = rhu(s - 1);
  const defs = [
    `<marker id="${ARROW_ID12}" markerWidth="${s}" markerHeight="${sH}" refX="${sRefX}" refY="${sMidY}" orient="auto" markerUnits="userSpaceOnUse"><polygon points="0 0, ${s} ${sMidY}, 0 ${sH}" fill="${palette.textMuted}" /></marker>`
  ];
  const anchors = {};
  for (const node of ir.nodes) {
    anchors[node.id] = { bounds: box(node.id) };
  }
  const scene = applyOverlays(
    {
      viewBox: { x: 0, y: 0, width: rhuInt(placed.width), height: rhuInt(placed.height + titleH) },
      background: palette.background,
      elements,
      defs
    },
    ir.overlays,
    theme
  );
  return { scene, anchors };
}
var tree234 = {
  parseMermaid: buildTree234,
  parseYaml: (input) => JSON.parse(input),
  layout: (ir, theme) => layoutTree234(ir, theme)
};
function extractPathDirectives(input) {
  const cleanLines = [];
  const paths = [];
  const PATH_RE = /^[ \t]*path[ \t]+(.+)$/;
  for (const line of input.split("\n")) {
    const m = line.match(PATH_RE);
    if (m && m[1].includes("->")) {
      const nodes = m[1].split("->").map((s) => s.trim()).filter(Boolean);
      if (nodes.length >= 2) {
        paths.push(nodes);
        continue;
      }
    }
    cleanLines.push(line);
  }
  return { clean: cleanLines.join("\n"), paths };
}
var tree = {
  parseMermaid(input) {
    const { clean, paths } = extractPathDirectives(input);
    const raw = parseLines(clean);
    const nodes = buildNodes(raw.lines);
    const labelToId = /* @__PURE__ */ new Map();
    for (const node of nodes) {
      if (!labelToId.has(node.label)) labelToId.set(node.label, node.id);
    }
    const activePairs = [];
    for (const seg of paths) {
      for (let i = 0; i < seg.length - 1; i++) {
        const fromId = labelToId.get(seg[i]);
        const toId = labelToId.get(seg[i + 1]);
        if (fromId && toId) activePairs.push([fromId, toId]);
      }
    }
    return {
      version: raw.version,
      metadata: raw.metadata ?? {},
      direction: raw.direction,
      nodes,
      ...activePairs.length > 0 ? { activePaths: activePairs } : {}
    };
  },
  parseYaml(input) {
    return JSON.parse(input);
  },
  layout(ir, theme) {
    return layoutTree(ir, theme);
  }
};
var COLOUR_KINDS = ["scan", "join", "build", "red", "black", "active", "primary", "muted"];
function inferKind(label) {
  const l = label.toLowerCase();
  if (l.includes("join")) return "join";
  if (l.includes("scan")) return "scan";
  if (l.includes("hash") || l.includes("sort") || l.includes("aggregate") || l.includes("group") || l.includes("materialize") || l.includes("gather"))
    return "build";
  return void 0;
}
var plan = {
  parseMermaid(input) {
    const raw = parseLines(input);
    const nodes = buildNodes(raw.lines);
    for (const n of nodes) {
      if (n.kinds.some((k2) => COLOUR_KINDS.includes(k2))) continue;
      const k = inferKind(n.label);
      if (k) n.kinds.push(k);
    }
    return {
      version: raw.version,
      metadata: raw.metadata ?? {},
      direction: raw.direction,
      nodes
    };
  },
  parseYaml(input) {
    return JSON.parse(input);
  },
  layout(ir, theme) {
    return layoutTree(ir, theme);
  }
};
var h = (n) => n ? n.height : 0;
var upd = (n) => {
  n.height = 1 + Math.max(h(n.left), h(n.right));
};
var bf = (n) => h(n.left) - h(n.right);
function rotateRight2(y) {
  const x = y.left;
  y.left = x.right;
  x.right = y;
  upd(y);
  upd(x);
  return x;
}
function rotateLeft2(x) {
  const y = x.right;
  x.right = y.left;
  y.left = x;
  upd(x);
  upd(y);
  return y;
}
function insert(node, key) {
  if (!node) return { key, left: null, right: null, height: 1 };
  if (key < node.key) node.left = insert(node.left, key);
  else if (key > node.key) node.right = insert(node.right, key);
  else return node;
  upd(node);
  const balance = bf(node);
  if (balance > 1 && key < node.left.key) return rotateRight2(node);
  if (balance < -1 && key > node.right.key) return rotateLeft2(node);
  if (balance > 1 && key > node.left.key) {
    node.left = rotateLeft2(node.left);
    return rotateRight2(node);
  }
  if (balance < -1 && key < node.right.key) {
    node.right = rotateRight2(node.right);
    return rotateLeft2(node);
  }
  return node;
}
function buildAvl(input) {
  const keys = (input.match(/-?\d+/g) ?? []).map(Number);
  let root = null;
  for (const k of keys) root = insert(root, k);
  const nodes = [];
  let i = 0;
  const emit = (n) => {
    const id = `n${i++}`;
    const children = [];
    nodes.push({ id, label: String(n.key), kinds: ["circle"], badge: String(bf(n)), children });
    if (n.left) children.push(emit(n.left));
    if (n.right) children.push(emit(n.right));
    return id;
  };
  if (root) emit(root);
  return { version: "1.0", metadata: {}, direction: "TB", nodes };
}
var avl = {
  parseMermaid(input) {
    return buildAvl(input);
  },
  parseYaml(input) {
    return JSON.parse(input);
  },
  layout(ir, theme) {
    return layoutTree(ir, theme);
  }
};
var RED = true;
var BLACK = false;
var isRed = (n) => n !== null && n.color === RED;
function rotateLeft3(h2) {
  const x = h2.right;
  h2.right = x.left;
  x.left = h2;
  x.color = h2.color;
  h2.color = RED;
  return x;
}
function rotateRight3(h2) {
  const x = h2.left;
  h2.left = x.right;
  x.right = h2;
  x.color = h2.color;
  h2.color = RED;
  return x;
}
function flip(h2) {
  h2.color = !h2.color;
  if (h2.left) h2.left.color = !h2.left.color;
  if (h2.right) h2.right.color = !h2.right.color;
}
function insert2(h2, key) {
  if (!h2) return { key, left: null, right: null, color: RED };
  if (key < h2.key) h2.left = insert2(h2.left, key);
  else if (key > h2.key) h2.right = insert2(h2.right, key);
  if (isRed(h2.right) && !isRed(h2.left)) h2 = rotateLeft3(h2);
  if (isRed(h2.left) && isRed(h2.left.left)) h2 = rotateRight3(h2);
  if (isRed(h2.left) && isRed(h2.right)) flip(h2);
  return h2;
}
function buildRbtree(input) {
  const keys = (input.match(/-?\d+/g) ?? []).map(Number);
  let root = null;
  for (const k of keys) {
    root = insert2(root, k);
    root.color = BLACK;
  }
  const nodes = [];
  let i = 0;
  const emit = (n) => {
    const id = `n${i++}`;
    const children = [];
    nodes.push({ id, label: String(n.key), kinds: [n.color === RED ? "red" : "black"], children });
    if (n.left) children.push(emit(n.left));
    if (n.right) children.push(emit(n.right));
    return id;
  };
  if (root) emit(root);
  return { version: "1.0", metadata: {}, direction: "TB", nodes };
}
var rbtree = {
  parseMermaid: buildRbtree,
  parseYaml: (input) => JSON.parse(input),
  layout: (ir, theme) => layoutTree(ir, theme)
};
function insertRec(node, key, order) {
  if (node.leaf) {
    let i = node.keys.length - 1;
    while (i >= 0 && key < node.keys[i]) i--;
    if (i >= 0 && node.keys[i] === key) return null;
    node.keys.splice(i + 1, 0, key);
  } else {
    let i = node.keys.length - 1;
    while (i >= 0 && key < node.keys[i]) i--;
    if (i >= 0 && node.keys[i] === key) return null;
    i++;
    const split = insertRec(node.children[i], key, order);
    if (split) {
      node.keys.splice(i, 0, split.up);
      node.children.splice(i + 1, 0, split.right);
    }
  }
  if (node.keys.length <= order - 1) return null;
  const mid = node.keys.length >> 1;
  const up = node.keys[mid];
  const right = {
    keys: node.keys.slice(mid + 1),
    children: node.leaf ? [] : node.children.slice(mid + 1),
    leaf: node.leaf
  };
  node.keys = node.keys.slice(0, mid);
  node.children = node.leaf ? [] : node.children.slice(0, mid + 1);
  return { up, right };
}
function buildBtree(input) {
  const orderMatch = input.match(/order\s+(\d+)/i);
  const order = Math.max(3, orderMatch ? Number(orderMatch[1]) : 3);
  const afterOrder = input.replace(/order\s+\d+/i, "");
  const keys = (afterOrder.match(/-?\d+/g) ?? []).map(Number);
  let root = { keys: [], children: [], leaf: true };
  for (const k of keys) {
    const split = insertRec(root, k, order);
    if (split) root = { keys: [split.up], children: [root, split.right], leaf: false };
  }
  const nodes = [];
  let i = 0;
  const emit = (n) => {
    const id = `n${i++}`;
    const children = [];
    nodes.push({ id, label: n.keys.join(" | "), kinds: ["strip"], children });
    for (const c of n.children) children.push(emit(c));
    return id;
  };
  if (root.keys.length > 0 || root.children.length > 0) emit(root);
  return { version: "1.0", metadata: {}, direction: "TB", nodes };
}
var btree = {
  parseMermaid: buildBtree,
  parseYaml: (input) => JSON.parse(input),
  layout: (ir, theme) => layoutTree(ir, theme)
};
var newNode = () => ({ edges: /* @__PURE__ */ new Map(), end: false });
function commonPrefix(a, b) {
  let i = 0;
  while (i < a.length && i < b.length && a[i] === b[i]) i++;
  return i;
}
function insert3(root, word) {
  let node = root;
  let rest = word;
  while (rest.length > 0) {
    let matched;
    for (const label of node.edges.keys()) {
      if (label[0] === rest[0]) {
        matched = label;
        break;
      }
    }
    if (matched === void 0) {
      const child = newNode();
      child.end = true;
      node.edges.set(rest, child);
      return;
    }
    const cp = commonPrefix(matched, rest);
    if (cp === matched.length) {
      node = node.edges.get(matched);
      rest = rest.slice(cp);
      if (rest.length === 0) node.end = true;
    } else {
      const existingChild = node.edges.get(matched);
      node.edges.delete(matched);
      const mid = newNode();
      node.edges.set(matched.slice(0, cp), mid);
      mid.edges.set(matched.slice(cp), existingChild);
      if (cp === rest.length) {
        mid.end = true;
      } else {
        const leaf = newNode();
        leaf.end = true;
        mid.edges.set(rest.slice(cp), leaf);
      }
      return;
    }
  }
  node.end = true;
}
function buildRadix(input) {
  const tokens = input.split(/\s+/).filter(Boolean);
  const words = tokens.filter((t) => /^[A-Za-z]+$/.test(t) && t !== "radix" && t !== "insert");
  const root = newNode();
  for (const w of words) insert3(root, w);
  const nodes = [];
  let i = 0;
  const emit = (n, prefix, edgeLabel) => {
    const id = `n${i++}`;
    const children = [];
    const node = n.end ? {
      id,
      label: prefix,
      kinds: ["leaf", "active"],
      children,
      ...edgeLabel !== void 0 ? { edgeLabel } : {}
    } : {
      id,
      label: "",
      kinds: ["dot"],
      children,
      ...edgeLabel !== void 0 ? { edgeLabel } : {}
    };
    nodes.push(node);
    for (const [label, child] of n.edges) children.push(emit(child, prefix + label, label));
    return id;
  };
  emit(root, "");
  return { version: "1.0", metadata: {}, direction: "TB", nodes };
}
var radix = {
  parseMermaid: buildRadix,
  parseYaml: (input) => JSON.parse(input),
  layout: (ir, theme) => layoutTree(ir, theme)
};
function pickReducer(input) {
  const l = input.toLowerCase();
  if (l.includes("min")) return Math.min;
  if (l.includes("max")) return Math.max;
  return (a, b) => a + b;
}
function buildSegtree(input) {
  const arr = (input.match(/-?\d+/g) ?? []).map(Number);
  const reduce = pickReducer(input);
  const nodes = [];
  let i = 0;
  const build = (lo, hi) => {
    const id = `n${i++}`;
    const children = [];
    const node = { id, label: "", kinds: ["box"], info: `[${lo},${hi}]`, children };
    nodes.push(node);
    let value;
    if (lo === hi) {
      value = arr[lo];
    } else {
      const mid = lo + hi >> 1;
      const left = build(lo, mid);
      const right = build(mid + 1, hi);
      children.push(left.id, right.id);
      value = reduce(left.value, right.value);
    }
    node.label = String(value);
    return { id, value };
  };
  if (arr.length > 0) build(0, arr.length - 1);
  return { version: "1.0", metadata: {}, direction: "TB", nodes };
}
var segtree = {
  parseMermaid: buildSegtree,
  parseYaml: (input) => JSON.parse(input),
  layout: (ir, theme) => layoutTree(ir, theme)
};
function buildHeap(input) {
  const isMin = /\bmin\b/i.test(input);
  const keys = (input.match(/-?\d+/g) ?? []).map(Number);
  const higher = (a, b) => isMin ? a < b : a > b;
  const heap2 = [];
  for (const k of keys) {
    heap2.push(k);
    let i = heap2.length - 1;
    while (i > 0) {
      const parent = i - 1 >> 1;
      if (higher(heap2[i], heap2[parent])) {
        [heap2[i], heap2[parent]] = [heap2[parent], heap2[i]];
        i = parent;
      } else break;
    }
  }
  const nodes = [];
  const emit = (idx) => {
    const id = `n${idx}`;
    const children = [];
    nodes.push({ id, label: String(heap2[idx]), kinds: ["circle"], children });
    const l = 2 * idx + 1, r = 2 * idx + 2;
    if (l < heap2.length) children.push(emit(l));
    if (r < heap2.length) children.push(emit(r));
    return id;
  };
  if (heap2.length > 0) emit(0);
  return { version: "1.0", metadata: {}, direction: "TB", nodes };
}
var heap = {
  parseMermaid: buildHeap,
  parseYaml: (input) => JSON.parse(input),
  layout: (ir, theme) => layoutTree(ir, theme)
};
function parse2(input) {
  let title;
  const values = [];
  for (const line of lines(input)) {
    const t = tokenizeDirective(line);
    if (t[0] === "linkedlist") {
      values.push(...t.slice(1));
      continue;
    }
    if (t[0] === "title") {
      title = t.slice(1).join(" ");
      continue;
    }
    if (t[0] === "values" || t[0] === "nodes") {
      values.push(...t.slice(1));
      continue;
    }
    values.push(...t);
  }
  return { ...title !== void 0 ? { title } : {}, values };
}
function layoutList(doc, theme) {
  const { palette, typography, spacing } = theme;
  const p = pen(theme);
  const margin = spacing.diagramMargin;
  const font = typography.baseFontSize;
  const nh = 40, nw = 26, gap = 46;
  const titleH = doc.title ? typography.titleFontSize + 14 : 0;
  const y = margin + titleH;
  const headW = measureText("head", font).width;
  const startX = margin + headW + 38;
  const nodes = [];
  let cursor = startX;
  for (const v of doc.values) {
    const vw = Math.max(measureText(v, font).width + 18, 30);
    nodes.push({ x: cursor, vw, value: v });
    cursor += vw + nw + gap;
  }
  const elements = [];
  if (doc.title) {
    elements.push(
      p.text(
        doc.title,
        margin,
        margin + typography.titleFontSize,
        typography.titleFontSize,
        palette.text,
        { weight: "bold" }
      )
    );
  }
  elements.push(
    p.text("head", margin, y + nh / 2 + font * 0.35, font, palette.textMuted, { weight: "bold" })
  );
  if (nodes.length > 0) {
    elements.push(
      p.path(
        `M ${rhu(margin + headW + 6)} ${rhu(y + nh / 2)} L ${rhu(nodes[0].x - 4)} ${rhu(y + nh / 2)}`,
        palette.primary,
        1.5,
        { markerEnd: ARROW_ID2 }
      )
    );
  }
  nodes.forEach((node, i) => {
    const nextX = node.x + node.vw;
    elements.push(
      p.rect({ x: node.x, y, width: node.vw, height: nh }, palette.surface, palette.border, 1.5, {
        rx: 4
      })
    );
    elements.push(
      p.rect({ x: nextX, y, width: nw, height: nh }, "#f1f3f5", palette.border, 1.5, { rx: 4 })
    );
    elements.push(
      p.path(`M ${rhu(nextX)} ${rhu(y)} L ${rhu(nextX)} ${rhu(y + nh)}`, palette.border, 1.5)
    );
    elements.push(
      p.text(
        node.value,
        rhu(node.x + node.vw / 2),
        rhu(y + nh / 2 + font * 0.35),
        font,
        palette.text,
        { anchor: "middle", weight: "bold" }
      )
    );
    const linkCx = nextX + nw / 2, linkCy = y + nh / 2;
    if (i < nodes.length - 1) {
      elements.push(p.circle({ x: linkCx, y: linkCy }, 3, palette.primary, palette.primary, 1));
      elements.push(
        p.path(
          `M ${rhu(linkCx)} ${rhu(linkCy)} L ${rhu(nodes[i + 1].x - 4)} ${rhu(linkCy)}`,
          palette.primary,
          1.5,
          { markerEnd: ARROW_ID2 }
        )
      );
    } else {
      elements.push(
        p.path(
          `M ${rhu(nextX)} ${rhu(y + nh)} L ${rhu(nextX + nw)} ${rhu(y)}`,
          palette.textMuted,
          1.5
        )
      );
    }
  });
  const anchors = {};
  nodes.forEach((node, i) => {
    anchors[`n${i}`] = { bounds: { x: node.x, y, width: node.vw + nw, height: nh } };
  });
  const width = (nodes.length > 0 ? cursor - gap : startX) + margin;
  const scene = {
    viewBox: { x: 0, y: 0, width, height: y + nh + margin },
    background: palette.background,
    elements,
    defs: [arrowDef(palette.primary, theme.edges?.arrowSize)]
  };
  return { scene, anchors };
}
var linkedlist = {
  parseMermaid(input) {
    return { version: "1.0", metadata: {}, ...parse2(input) };
  },
  parseYaml(input) {
    return JSON.parse(input);
  },
  layout(ir, theme) {
    return layoutList(ir, theme);
  }
};
function parse3(input) {
  let title;
  const regions = [];
  let cur;
  for (const line of lines(input)) {
    const t = tokenizeDirective(line);
    if (t[0] === "memory") continue;
    if (t[0] === "title") {
      title = t.slice(1).join(" ");
      continue;
    }
    if (t[0] === "region") {
      cur = { name: decodeLabel(line.slice(6)), items: [] };
      regions.push(cur);
      continue;
    }
    if (!cur) continue;
    if (t[0] === "var") {
      const parsed = parseVarDirective(t);
      if (parsed)
        cur.items.push({
          kind: "var",
          name: parsed.name,
          ...parsed.target ? { target: parsed.target } : {}
        });
      continue;
    }
    if (t[0] === "object") {
      const parts = line.slice(6).split(":").map((s) => s.trim());
      const id = parts[0] ?? "obj";
      const titleO = parts[1] ? decodeLabel(parts[1]) : id;
      const fields = (parts[2] ?? "").split(",").map((s) => s.trim()).filter(Boolean).map((pair) => {
        const [k, v] = pair.split("=").map((x) => x.trim());
        return { k: k ?? "", v: v ?? "" };
      });
      cur.items.push({ kind: "object", id, title: titleO, fields });
    }
  }
  return { ...title !== void 0 ? { title } : {}, regions };
}
function decodeLabel(raw) {
  return tokenizeDirective(raw.trim()).join(" ");
}
function parseVarDirective(tokens) {
  const nameToken = tokens[1];
  if (nameToken === void 0) return void 0;
  const inlineArrow = nameToken.indexOf("->");
  if (inlineArrow >= 0) {
    const name = nameToken.slice(0, inlineArrow);
    const target = nameToken.slice(inlineArrow + 2);
    return { name, ...target ? { target } : {} };
  }
  if (tokens[2] === "->") {
    return { name: nameToken, ...tokens[3] ? { target: tokens[3] } : {} };
  }
  if (tokens[2]?.startsWith("->")) {
    const target = tokens[2].slice(2) || tokens[3];
    return { name: nameToken, ...target ? { target } : {} };
  }
  return { name: nameToken };
}
function layoutMemory(doc, theme) {
  const { palette, typography, spacing } = theme;
  const p = pen(theme);
  const margin = spacing.diagramMargin;
  const font = typography.baseFontSize;
  const small = typography.smallFontSize;
  const titleH = doc.title ? typography.titleFontSize + 14 : 0;
  const HEADER = 26, PAD = 14, GAP = 14, REGION_GAP = 90;
  const REGION_FILL_OPACITY = 0.26;
  const VAR_FILL_OPACITY = 0.56;
  const OBJECT_FILL_OPACITY = 0.62;
  const itemSize = (item) => {
    if (item.kind === "var") {
      return { w: Math.max(130, measureText(item.name, font).width + 56), h: 34 };
    }
    const titleW = measureText(item.title, font).width;
    const fieldW = Math.max(
      0,
      ...item.fields.map((f2) => measureText(`${f2.k}: ${f2.v}`, small).width)
    );
    return { w: Math.max(120, titleW + 24, fieldW + 24), h: 24 + item.fields.length * 20 + 8 };
  };
  const elements = [];
  if (doc.title)
    elements.push(
      p.text(
        doc.title,
        margin,
        margin + typography.titleFontSize,
        typography.titleFontSize,
        palette.text,
        { weight: "bold" }
      )
    );
  const idBox = /* @__PURE__ */ new Map();
  const anchors = {};
  const pending = [];
  let regionX = margin;
  let maxBottom2 = margin + titleH;
  const regionTop = margin + titleH;
  for (const region of doc.regions) {
    const sizes = region.items.map(itemSize);
    const innerW = Math.max(80, ...sizes.map((s) => s.w));
    const regionW = Math.max(innerW + PAD * 2, measureText(region.name, small).width + 24);
    const contentH = sizes.reduce((s, sz) => s + sz.h + GAP, 0) - (sizes.length ? GAP : 0);
    const regionH = HEADER + PAD + contentH + PAD;
    elements.push(
      p.rect(
        { x: regionX, y: regionTop, width: regionW, height: regionH },
        palette.surface,
        palette.border,
        1.5,
        { rx: 8, fillOpacity: REGION_FILL_OPACITY }
      )
    );
    elements.push(
      p.text(region.name, regionX + 12, regionTop + 17, small, palette.textMuted, {
        weight: "bold"
      })
    );
    let iy = regionTop + HEADER + PAD;
    region.items.forEach((item, i) => {
      const { h: h2 } = sizes[i];
      const box = { x: regionX + PAD, y: iy, width: innerW, height: h2 };
      if (item.kind === "var") {
        elements.push(
          p.rect(box, palette.surface, palette.border, 1.5, {
            rx: 4,
            fillOpacity: VAR_FILL_OPACITY
          })
        );
        elements.push(
          p.text(item.name, box.x + 12, box.y + h2 / 2 + font * 0.35, font, palette.text, {
            weight: "bold"
          })
        );
        idBox.set(item.name, box);
        anchors[item.name] = { bounds: box };
        if (item.target) {
          elements.push(
            p.circle(
              { x: box.x + box.width - 12, y: box.y + h2 / 2 },
              3,
              palette.primary,
              palette.primary,
              1
            )
          );
          pending.push({ from: box, target: item.target });
        }
      } else {
        elements.push(
          p.rect(box, palette.surface, palette.primary, 2, {
            rx: 6,
            fillOpacity: OBJECT_FILL_OPACITY
          })
        );
        elements.push(
          p.text(item.title, box.x + 12, box.y + 18, font, palette.primary, { weight: "bold" })
        );
        item.fields.forEach((f2, fi) => {
          elements.push(
            p.text(`${f2.k}: ${f2.v}`, box.x + 12, box.y + 24 + (fi + 1) * 18, small, palette.text)
          );
        });
        idBox.set(item.id, box);
        anchors[item.id] = { bounds: box };
      }
      iy += h2 + GAP;
    });
    maxBottom2 = Math.max(maxBottom2, regionTop + regionH);
    regionX += regionW + REGION_GAP;
  }
  for (const { from, target } of pending) {
    const key = target.includes(".") ? target.slice(target.indexOf(".") + 1) : target;
    const to = idBox.get(key);
    if (!to) continue;
    const startX = from.x + from.width - 12;
    const startY = from.y + from.height / 2;
    const start = { x: startX, y: startY };
    const endX = startX <= to.x ? to.x : to.x + to.width;
    const endY = to.y + Math.min(to.height / 2, 18);
    const end = { x: endX, y: endY };
    elements.push(
      p.path(
        `M ${rhu(start.x)} ${rhu(start.y)} L ${rhu(end.x)} ${rhu(end.y)}`,
        palette.primary,
        1.5,
        { markerEnd: ARROW_ID2 }
      )
    );
  }
  const scene = {
    viewBox: { x: 0, y: 0, width: regionX - REGION_GAP + margin, height: maxBottom2 + margin },
    background: palette.background,
    elements,
    defs: [arrowDef(palette.primary, theme.edges?.arrowSize)]
  };
  return { scene, anchors };
}
var memory = {
  parseMermaid(input) {
    return { version: "1.0", metadata: {}, ...parse3(input) };
  },
  parseYaml(input) {
    return JSON.parse(input);
  },
  layout(ir, theme) {
    return layoutMemory(ir, theme);
  }
};
function parse4(input) {
  let title;
  let slots;
  const tuples = [];
  for (const line of lines(input)) {
    const t = tokenizeDirective(line);
    if (t[0] === "page") continue;
    if (t[0] === "title") {
      title = t.slice(1).join(" ");
      continue;
    }
    if (t[0] === "slots") {
      slots = Number(t[1]);
      continue;
    }
    if (t[0] === "tuples") {
      tuples.push(...t.slice(1));
      continue;
    }
  }
  return {
    ...title !== void 0 ? { title } : {},
    ...slots !== void 0 ? { slots } : {},
    tuples
  };
}
function layoutPage(doc, theme) {
  const { palette, typography, spacing } = theme;
  const p = pen(theme);
  const margin = spacing.diagramMargin;
  const font = typography.baseFontSize;
  const small = typography.smallFontSize;
  const titleH = doc.title ? typography.titleFontSize + 14 : 0;
  const slotCount = doc.slots ?? doc.tuples.length;
  const PAD = 14, HEADER = 26, SLOT_W = 54, SLOT_H = 22, TUPLE_H = 28, TGAP = 18;
  const SLOT_TOP_GAP = 12, FREE_BAND = 48, MIN_INNER_W = 240, SLOT_FILL_OPACITY = 0.14;
  const tupleWidths = doc.tuples.map((t) => Math.max(110, measureText(t, font).width + 24));
  const columnCount = Math.max(slotCount, doc.tuples.length, 1);
  const columnWidths = Array.from(
    { length: columnCount },
    (_, i) => Math.max(SLOT_W, tupleWidths[i] ?? 0)
  );
  const contentW = columnWidths.reduce((sum, width) => sum + width, 0) + (columnCount - 1) * TGAP;
  const innerW = Math.max(contentW, MIN_INNER_W);
  const pageW = innerW + PAD * 2;
  const pageH = HEADER + SLOT_TOP_GAP + SLOT_H + FREE_BAND + TUPLE_H + PAD;
  const contentX = pxForCenteredContent(margin + PAD, innerW, contentW);
  const px = margin, py = margin + titleH;
  const elements = [];
  if (doc.title)
    elements.push(
      p.text(
        doc.title,
        margin,
        margin + typography.titleFontSize,
        typography.titleFontSize,
        palette.text,
        { weight: "bold" }
      )
    );
  elements.push(
    p.rect({ x: px, y: py, width: pageW, height: pageH }, palette.surface, palette.border, 1.5, {
      rx: 6
    })
  );
  elements.push(
    p.rect({ x: px, y: py, width: pageW, height: HEADER }, palette.primary, palette.primary, 0, {
      rx: 0
    })
  );
  elements.push(
    p.text("PageHeader    freeStart \u2192        \u2190 freeEnd", px + 12, py + 17, small, "#ffffff", {
      weight: "bold"
    })
  );
  const slotY = py + HEADER + SLOT_TOP_GAP;
  const slotCx = [];
  const tupleCx = [];
  const anchors = {};
  const columnX = [];
  let x = contentX;
  for (const width of columnWidths) {
    columnX.push(x);
    x += width + TGAP;
  }
  for (let i = 0; i < slotCount; i++) {
    const sx = (columnX[i] ?? contentX) + ((columnWidths[i] ?? SLOT_W) - SLOT_W) / 2;
    const box = { x: sx, y: slotY, width: SLOT_W, height: SLOT_H };
    elements.push(
      p.rect(box, palette.primary, palette.primary, 1.5, { rx: 3, fillOpacity: SLOT_FILL_OPACITY })
    );
    elements.push(
      p.text(`slot${i}`, sx + SLOT_W / 2, slotY + 15, small, palette.primary, {
        anchor: "middle",
        weight: "bold"
      })
    );
    slotCx.push(sx + SLOT_W / 2);
    anchors[`slot${i}`] = { bounds: box };
  }
  const tupleY = slotY + SLOT_H + FREE_BAND;
  const freeLabel = "free space";
  const freeLabelY = slotY + SLOT_H + FREE_BAND / 2 + font * 0.35;
  const freeLabelX = px + pageW / 2;
  const freeLabelWidth = measureText(freeLabel, font).width;
  const freeLabelBounds = {
    x1: freeLabelX - freeLabelWidth / 2 - 6,
    x2: freeLabelX + freeLabelWidth / 2 + 6,
    y1: freeLabelY - font - 3,
    y2: freeLabelY + 5
  };
  elements.push(
    p.text(freeLabel, freeLabelX, freeLabelY, font, palette.textMuted, { anchor: "middle" })
  );
  doc.tuples.forEach((tuple, i) => {
    const tupleW = tupleWidths[i];
    const tx = (columnX[i] ?? contentX) + ((columnWidths[i] ?? tupleW) - tupleW) / 2;
    const box = { x: tx, y: tupleY, width: tupleW, height: TUPLE_H };
    const cx = tx + tupleW / 2;
    tupleCx.push(cx);
    elements.push(p.rect(box, palette.surface, palette.border, 1.5, { rx: 3 }));
    elements.push(
      p.text(tuple, cx, tupleY + TUPLE_H / 2 + font * 0.35, font, palette.text, {
        anchor: "middle",
        weight: "bold"
      })
    );
    anchors[`tuple${i}`] = { bounds: box };
  });
  for (let i = 0; i < Math.min(slotCx.length, tupleCx.length); i++) {
    elements.push(
      p.path(
        verticalArrow(slotCx[i], slotY + SLOT_H, tupleY, freeLabelBounds),
        palette.textMuted,
        1.5,
        { markerEnd: ARROW_ID2 }
      )
    );
  }
  const scene = {
    viewBox: { x: 0, y: 0, width: pageW + margin * 2, height: py + pageH + margin },
    background: palette.background,
    elements,
    defs: [arrowDef(palette.textMuted, theme.edges?.arrowSize)]
  };
  const chromeRects = [{ x: px, y: py, width: pageW, height: HEADER }];
  return { scene, anchors, chromeRects };
}
function pxForCenteredContent(innerLeft, innerW, contentW) {
  return innerLeft + Math.max(0, (innerW - contentW) / 2);
}
function verticalArrow(x, y1, y2, avoid) {
  const rx = rhu(x);
  if (avoid && x >= avoid.x1 && x <= avoid.x2 && y1 < avoid.y2 && y2 > avoid.y1) {
    return `M ${rx} ${rhu(y1)} L ${rx} ${rhu(Math.max(y1, avoid.y1))} M ${rx} ${rhu(Math.min(y2, avoid.y2))} L ${rx} ${rhu(y2)}`;
  }
  return `M ${rx} ${rhu(y1)} L ${rx} ${rhu(y2)}`;
}
var page = {
  parseMermaid(input) {
    return { version: "1.0", metadata: {}, ...parse4(input) };
  },
  parseYaml(input) {
    return JSON.parse(input);
  },
  layout(ir, theme) {
    return layoutPage(ir, theme);
  }
};
function buildStrip(pen2, theme, cells2, options) {
  const { origin, cellWidth, cellHeight } = options;
  const orientation = options.orientation ?? "horizontal";
  const gap = options.gap ?? 0;
  const horizontal = orientation === "horizontal";
  const palette = theme.palette;
  const elements = [];
  const slots = [];
  cells2.forEach((cell, i) => {
    const step = (horizontal ? cellWidth : cellHeight) + gap;
    const x = origin.x + (horizontal ? i * step : 0);
    const y = origin.y + (horizontal ? 0 : i * step);
    const slot = { x, y, width: cellWidth, height: cellHeight };
    slots.push(slot);
    elements.push(
      pen2.rect(slot, cell.fill ?? palette.surface, cell.stroke ?? palette.border, 1.5, {
        rx: 3,
        ...cell.fillOpacity !== void 0 ? { fillOpacity: cell.fillOpacity } : {}
      })
    );
    if (cell.label !== void 0) {
      elements.push(
        pen2.text(
          cell.label,
          x + cellWidth / 2,
          y + cellHeight / 2 + 5,
          theme.typography.baseFontSize,
          palette.text,
          { anchor: "middle", weight: "bold" }
        )
      );
    }
    if (cell.index !== void 0) {
      const ix = horizontal ? x + cellWidth / 2 : x - 6;
      const iy = horizontal ? y - 6 : y + cellHeight / 2 + 4;
      elements.push(
        pen2.text(cell.index, ix, iy, theme.typography.smallFontSize, palette.textMuted, {
          anchor: horizontal ? "middle" : "end"
        })
      );
    }
  });
  const n = cells2.length;
  const totalAlong = n === 0 ? 0 : n * (horizontal ? cellWidth : cellHeight) + (n - 1) * gap;
  const bounds = {
    x: origin.x,
    y: origin.y,
    width: horizontal ? totalAlong : cellWidth,
    height: horizontal ? cellHeight : totalAlong
  };
  return { elements, slots, bounds };
}
var ARROW_FWD = "queue-arrow-fwd";
var ARROW_REV = "queue-arrow-rev";
function parseAxisToken(token, fallback) {
  return token === "horizontal" || token === "vertical" ? token : fallback;
}
function arrowDefs(color, size = 8) {
  const s = size;
  const sH = s;
  const sMidY = rhu(s / 2);
  const sRefXFwd = rhu(s - 1);
  const sRefXRev = rhu(1);
  return `<marker id="${ARROW_FWD}" markerWidth="${s + 1}" markerHeight="${sH}" refX="${sRefXFwd}" refY="${sMidY}" orient="auto"><path d="M0 0 L${s} ${sMidY} L0 ${sH} z" fill="${color}" /></marker><marker id="${ARROW_REV}" markerWidth="${s + 1}" markerHeight="${sH}" refX="${sRefXRev}" refY="${sMidY}" orient="auto"><path d="M${s} 0 L0 ${sMidY} L${s} ${sH} z" fill="${color}" /></marker>`;
}
function pointerBelow(p, theme, slot, name, color, stack2 = 0) {
  const pointer = pointerToSlot(p, theme, slot, name, color, "bottom", stack2);
  return { elements: pointer.elements, bottom: pointer.outerEdge };
}
function pointerToSlot(p, theme, slot, name, color, side, stack2 = 0) {
  const font = theme.typography.baseFontSize;
  const laneGap = font + 6;
  const arrowLen = 30;
  const labelGap = 6;
  const cx = slot.x + slot.width / 2;
  const cy = slot.y + slot.height / 2;
  if (side === "bottom" || side === "top") {
    const s2 = side === "bottom" ? 1 : -1;
    const laneX = cx;
    const edgeY = side === "bottom" ? slot.y + slot.height + 4 : slot.y - 4;
    const tailY = edgeY + s2 * (arrowLen + stack2 * laneGap);
    const labelY = tailY + s2 * (labelGap + (s2 > 0 ? font : 0));
    return {
      elements: [
        p.path(`M ${rhu(laneX)} ${rhu(tailY)} L ${rhu(laneX)} ${rhu(edgeY)}`, color, 1.5, {
          markerEnd: ARROW_FWD
        }),
        p.text(name, laneX, labelY, font, color, { anchor: "middle", weight: "bold" })
      ],
      outerEdge: s2 > 0 ? labelY + 4 : labelY - font - 4
    };
  }
  const s = side === "right" ? 1 : -1;
  const laneY = cy + stack2 * laneGap;
  const edgeX = side === "right" ? slot.x + slot.width + 4 : slot.x - 4;
  const tailX = edgeX + s * arrowLen;
  const labelX = tailX + s * labelGap;
  const anchor = s > 0 ? "start" : "end";
  return {
    elements: [
      p.path(`M ${rhu(tailX)} ${rhu(laneY)} L ${rhu(edgeX)} ${rhu(laneY)}`, color, 1.5, {
        markerEnd: ARROW_FWD
      }),
      p.text(name, labelX, laneY + 4, font, color, { anchor, weight: "bold" })
    ],
    outerEdge: labelX
  };
}
function finalizeStripScene(elements, anchors, theme, defs) {
  const margin = theme.spacing.diagramMargin;
  const contentBounds = boundsForElements2(elements);
  const dx = margin - contentBounds.x;
  const dy = margin - contentBounds.y;
  const shiftedElements = translateElements2(elements, dx, dy);
  const shiftedAnchors = translateAnchors2(anchors, dx, dy);
  return {
    scene: {
      viewBox: {
        x: 0,
        y: 0,
        width: rhu(contentBounds.width + margin * 2),
        height: rhu(contentBounds.height + margin * 2)
      },
      background: theme.palette.background,
      elements: shiftedElements,
      ...defs !== void 0 ? { defs } : {}
    },
    anchors: shiftedAnchors
  };
}
function boundsForElements2(elements) {
  const bounds = elements.map(boundsForElement2).filter((b) => b !== void 0);
  if (bounds.length === 0) return { x: 0, y: 0, width: 0, height: 0 };
  const minX = Math.min(...bounds.map((b) => b.x));
  const minY = Math.min(...bounds.map((b) => b.y));
  const maxX = Math.max(...bounds.map((b) => b.x + b.width));
  const maxY = Math.max(...bounds.map((b) => b.y + b.height));
  return { x: minX, y: minY, width: maxX - minX, height: maxY - minY };
}
function boundsForElement2(element) {
  switch (element.type) {
    case "rect":
      return element.bounds;
    case "circle":
      return {
        x: element.center.x - element.radius,
        y: element.center.y - element.radius,
        width: element.radius * 2,
        height: element.radius * 2
      };
    case "path":
      return boundsForPath2(element.d, element.strokeWidth);
    case "text":
      return boundsForText2(element);
    case "group":
      return boundsForElements2(element.children);
  }
}
function boundsForText2(text) {
  const measured = measureText(text.content, text.fontSize);
  const x = text.anchor === "middle" ? text.position.x - measured.width / 2 : text.anchor === "end" ? text.position.x - measured.width : text.position.x;
  return {
    x,
    y: text.position.y - text.fontSize,
    width: measured.width,
    height: text.fontSize * 1.25
  };
}
function boundsForPath2(d, strokeWidth) {
  const nums = [...d.matchAll(/-?\d+(?:\.\d+)?/g)].map((match) => Number(match[0]));
  if (nums.length < 2) return { x: 0, y: 0, width: 0, height: 0 };
  const xs = nums.filter((_, i) => i % 2 === 0);
  const ys = nums.filter((_, i) => i % 2 === 1);
  const pad = strokeWidth / 2;
  const minX = Math.min(...xs) - pad;
  const minY = Math.min(...ys) - pad;
  const maxX = Math.max(...xs) + pad;
  const maxY = Math.max(...ys) + pad;
  return { x: minX, y: minY, width: maxX - minX, height: maxY - minY };
}
function translateElements2(elements, dx, dy) {
  return elements.map((element) => translateElement2(element, dx, dy));
}
function translateElement2(element, dx, dy) {
  switch (element.type) {
    case "rect":
      return { ...element, bounds: translateRect2(element.bounds, dx, dy) };
    case "circle":
      return {
        ...element,
        center: { x: rhu(element.center.x + dx), y: rhu(element.center.y + dy) }
      };
    case "path":
      return { ...element, d: translatePathD2(element.d, dx, dy) };
    case "text":
      return {
        ...element,
        position: { x: rhu(element.position.x + dx), y: rhu(element.position.y + dy) }
      };
    case "group":
      return { ...element, children: translateElements2(element.children, dx, dy) };
    case "icon":
      return { ...element, x: rhu(element.x + dx), y: rhu(element.y + dy) };
  }
}
function translateAnchors2(anchors, dx, dy) {
  return Object.fromEntries(
    Object.entries(anchors).map(([key, anchor]) => [
      key,
      { bounds: translateRect2(anchor.bounds, dx, dy) }
    ])
  );
}
function translateRect2(rect, dx, dy) {
  return { x: rhu(rect.x + dx), y: rhu(rect.y + dy), width: rect.width, height: rect.height };
}
function translatePathD2(d, dx, dy) {
  let i = 0;
  return d.replace(/-?\d+(?:\.\d+)?/g, (value) => {
    const delta = i++ % 2 === 0 ? dx : dy;
    return String(rhu(Number(value) + delta));
  });
}
function parse5(input) {
  let title;
  let cells2 = [];
  let capacity;
  let axis = "horizontal";
  for (const line of lines(input)) {
    const t = line.split(/\s+/);
    if (t[0] === "queue") {
      if (t.length > 1) cells2 = t.slice(1);
      continue;
    }
    if (t[0] === "title") {
      title = line.slice(5).trim();
      continue;
    }
    if (t[0] === "cells" || t[0] === "items") {
      cells2 = t.slice(1);
      continue;
    }
    if (t[0] === "capacity") {
      const n = Number(t[1]);
      if (Number.isFinite(n)) capacity = n;
      continue;
    }
    if (t[0] === "axis") {
      axis = parseAxisToken(t[1], axis);
      continue;
    }
  }
  return {
    ...title !== void 0 ? { title } : {},
    cells: cells2,
    ...capacity !== void 0 ? { capacity } : {},
    axis
  };
}
function layoutQueue(doc, theme) {
  const { palette, typography, spacing } = theme;
  const p = pen(theme);
  const margin = spacing.diagramMargin;
  const font = typography.baseFontSize;
  const cellH = 40;
  const filled = doc.cells.length;
  const total = Math.max(filled, doc.capacity ?? filled);
  const cellW = Math.max(46, ...doc.cells.map((c) => measureText(c, font).width + 24));
  const axis = doc.axis === "vertical" ? "vertical" : "horizontal";
  const horizontal = axis === "horizontal";
  const titleH = doc.title ? typography.titleFontSize + 14 : 0;
  const topH = font + 14;
  const sideGap = 50;
  const origin = horizontal ? { x: margin + sideGap, y: margin + titleH + topH } : { x: margin, y: margin + titleH + sideGap };
  const cellInputs = Array.from(
    { length: total },
    (_, i) => i < filled ? { label: doc.cells[i] } : { fill: palette.background }
  );
  const strip = buildStrip(p, theme, cellInputs, {
    origin,
    cellWidth: cellW,
    cellHeight: cellH,
    orientation: axis
  });
  const elements = [...strip.elements];
  if (doc.title) {
    elements.push(
      p.text(
        doc.title,
        margin,
        margin + typography.titleFontSize,
        typography.titleFontSize,
        palette.text,
        { weight: "bold" }
      )
    );
  }
  if (horizontal) {
    const midY = origin.y + cellH / 2;
    const leftEdge = origin.x;
    const rightEdge = origin.x + total * cellW;
    elements.push(
      p.path(
        `M ${rhu(leftEdge)} ${rhu(midY)} L ${rhu(margin)} ${rhu(midY)}`,
        palette.primary,
        1.5,
        { markerEnd: ARROW_FWD }
      )
    );
    elements.push(
      p.text("dequeue", leftEdge - sideGap / 2, origin.y - 8, font, palette.primary, {
        anchor: "middle",
        weight: "bold"
      })
    );
    elements.push(
      p.path(
        `M ${rhu(rightEdge + sideGap)} ${rhu(midY)} L ${rhu(rightEdge)} ${rhu(midY)}`,
        palette.primary,
        1.5,
        { markerEnd: ARROW_FWD }
      )
    );
    elements.push(
      p.text("enqueue", rightEdge + sideGap / 2, origin.y - 8, font, palette.primary, {
        anchor: "middle",
        weight: "bold"
      })
    );
  } else {
    const midX = origin.x + cellW / 2;
    const topEdge = origin.y;
    const bottomEdge = origin.y + total * cellH;
    elements.push(
      p.path(
        `M ${rhu(midX)} ${rhu(topEdge)} L ${rhu(midX)} ${rhu(topEdge - sideGap)}`,
        palette.primary,
        1.5,
        { markerEnd: ARROW_FWD }
      )
    );
    elements.push(
      p.text("dequeue", midX, topEdge - sideGap / 2 - 6, font, palette.primary, {
        anchor: "middle",
        weight: "bold"
      })
    );
    elements.push(
      p.path(
        `M ${rhu(midX)} ${rhu(bottomEdge + sideGap)} L ${rhu(midX)} ${rhu(bottomEdge)}`,
        palette.primary,
        1.5,
        { markerEnd: ARROW_FWD }
      )
    );
    elements.push(
      p.text("enqueue", midX, bottomEdge + sideGap / 2 + font, font, palette.primary, {
        anchor: "middle",
        weight: "bold"
      })
    );
  }
  if (filled > 0) {
    const frontSlot = strip.slots[0];
    const rearSlot = strip.slots[filled - 1];
    const same = filled === 1;
    const front = horizontal ? pointerBelow(p, theme, frontSlot, "front", palette.secondary, 0) : pointerToSlot(p, theme, frontSlot, "front", palette.secondary, "right", 0);
    const rear = horizontal ? pointerBelow(p, theme, rearSlot, "rear", palette.primary, same ? 1 : 0) : pointerToSlot(p, theme, rearSlot, "rear", palette.primary, "right", same ? 1 : 0);
    elements.push(...front.elements, ...rear.elements);
  }
  const anchors = {};
  strip.slots.forEach((slot, i) => {
    anchors[`c${i}`] = { bounds: slot };
  });
  const finalized = finalizeStripScene(elements, anchors, theme, [
    arrowDefs(palette.primary, theme.edges?.arrowSize)
  ]);
  return { scene: finalized.scene, anchors: finalized.anchors };
}
var queue = {
  parseMermaid(input) {
    return { version: "1.0", metadata: {}, ...parse5(input) };
  },
  parseYaml(input) {
    return JSON.parse(input);
  },
  layout(ir, theme) {
    return layoutQueue(ir, theme);
  }
};
var EMPTY = /* @__PURE__ */ new Set(["_", ".", "-"]);
function parse6(input) {
  let title;
  let cells2 = [];
  let capacity;
  let front;
  let rear;
  let axis = "horizontal";
  for (const line of lines(input)) {
    const t = line.split(/\s+/);
    if (t[0] === "cqueue") {
      continue;
    }
    if (t[0] === "title") {
      title = line.slice(5).trim();
      continue;
    }
    if (t[0] === "capacity") {
      const n = Number(t[1]);
      if (Number.isFinite(n)) capacity = n;
      continue;
    }
    if (t[0] === "cells" || t[0] === "items") {
      cells2 = t.slice(1).map((v) => EMPTY.has(v) ? null : v);
      continue;
    }
    if (t[0] === "front") {
      const n = Number(t[1]);
      if (Number.isFinite(n)) front = n;
      continue;
    }
    if (t[0] === "rear") {
      const n = Number(t[1]);
      if (Number.isFinite(n)) rear = n;
      continue;
    }
    if (t[0] === "axis") {
      axis = parseAxisToken(t[1], axis);
      continue;
    }
  }
  const cap = capacity ?? cells2.length;
  const padded = Array.from({ length: cap }, (_, i) => cells2[i] ?? null);
  const firstFilled = padded.findIndex((c) => c !== null);
  let lastFilled = -1;
  for (let i = 0; i < padded.length; i++) if (padded[i] !== null) lastFilled = i;
  const f2 = front ?? (firstFilled >= 0 ? firstFilled : 0);
  const r = rear ?? (lastFilled >= 0 ? lastFilled : 0);
  return {
    ...title !== void 0 ? { title } : {},
    capacity: cap,
    cells: padded,
    front: f2,
    rear: r,
    axis
  };
}
function layoutCQueue(doc, theme) {
  const { palette, typography, spacing } = theme;
  const p = pen(theme);
  const margin = spacing.diagramMargin;
  const font = typography.baseFontSize;
  const cellH = 40;
  const total = doc.capacity;
  const labelW = doc.cells.map((c) => c ? measureText(c, font).width : 0);
  const cellW = Math.max(46, ...labelW.map((w) => w + 24));
  const axis = doc.axis === "vertical" ? "vertical" : "horizontal";
  const horizontal = axis === "horizontal";
  const titleH = doc.title ? typography.titleFontSize + 14 : 0;
  const arcH = 44;
  const origin = horizontal ? { x: margin, y: margin + titleH + arcH } : { x: margin + arcH, y: margin + titleH };
  const cellInputs = doc.cells.map(
    (v, i) => v !== null ? { label: v, index: String(i) } : { fill: palette.background, index: String(i) }
  );
  const strip = buildStrip(p, theme, cellInputs, {
    origin,
    cellWidth: cellW,
    cellHeight: cellH,
    orientation: axis
  });
  const elements = [...strip.elements];
  if (doc.title) {
    elements.push(
      p.text(
        doc.title,
        margin,
        margin + typography.titleFontSize,
        typography.titleFontSize,
        palette.text,
        { weight: "bold" }
      )
    );
  }
  const rearSlot = strip.slots[doc.rear ?? 0];
  const frontSlot = strip.slots[doc.front ?? 0];
  const anchors = {};
  strip.slots.forEach((slot, i) => {
    anchors[`c${i}`] = { bounds: slot };
  });
  if (frontSlot) anchors.front = { bounds: frontSlot };
  if (rearSlot) anchors.rear = { bounds: rearSlot };
  if (rearSlot && frontSlot) {
    const selfLoop = (doc.front ?? 0) === (doc.rear ?? 0);
    const connectorAnchors = selfLoop ? selfLoopWrapAnchors(frontSlot, horizontal) : strip.slots.reduce((acc, slot, i) => {
      acc[`c${i}`] = { bounds: slot };
      return acc;
    }, {});
    const fromKey = selfLoop ? "__rearWrap" : `c${doc.rear ?? 0}`;
    const toKey = selfLoop ? "__frontWrap" : `c${doc.front ?? 0}`;
    const connectorResult = routeConnectors({
      anchors: connectorAnchors,
      connectors: [
        {
          fromKey,
          toKey,
          direction: "directed",
          style: "solid",
          label: `mod ${total}`,
          routing: "orthogonal",
          exitWall: horizontal ? "N" : "W",
          entryWall: horizontal ? "N" : "W",
          animation: "none",
          props: {
            color: palette.primary,
            routePadding: arcH - 8,
            labelPlacement: "path-midpoint"
          }
        }
      ],
      theme
    });
    const connectorElements = connectorResult.elements.map(
      (element) => element.type === "path" && element.markerEnd?.startsWith("triton-crosslink-arrow-") ? { ...element, markerEnd: ARROW_FWD, strokeWidth: 1.5 } : element
    );
    const linkPaths = connectorElements.filter((e) => e.type !== "text");
    const linkLabels = connectorElements.filter((e) => e.type === "text");
    elements.push(...linkPaths, ...linkLabels);
  }
  if (frontSlot && rearSlot) {
    const same = (doc.front ?? 0) === (doc.rear ?? 0);
    const front = horizontal ? pointerBelow(p, theme, frontSlot, "front", palette.secondary, 0) : pointerToSlot(p, theme, frontSlot, "front", palette.secondary, "right", 0);
    const rear = horizontal ? pointerBelow(p, theme, rearSlot, "rear", palette.primary, same ? 1 : 0) : pointerToSlot(p, theme, rearSlot, "rear", palette.primary, "right", same ? 1 : 0);
    elements.push(...front.elements, ...rear.elements);
  }
  const finalized = finalizeStripScene(elements, anchors, theme, [
    arrowDefs(palette.primary, theme.edges?.arrowSize)
  ]);
  return { scene: finalized.scene, anchors: finalized.anchors };
}
function selfLoopWrapAnchors(slot, horizontal) {
  if (horizontal) {
    return {
      __rearWrap: {
        bounds: {
          x: slot.x + slot.width / 2,
          y: slot.y,
          width: slot.width / 2,
          height: slot.height
        }
      },
      __frontWrap: { bounds: { x: slot.x, y: slot.y, width: slot.width / 2, height: slot.height } }
    };
  }
  return {
    __rearWrap: {
      bounds: {
        x: slot.x,
        y: slot.y + slot.height / 2,
        width: slot.width,
        height: slot.height / 2
      }
    },
    __frontWrap: { bounds: { x: slot.x, y: slot.y, width: slot.width, height: slot.height / 2 } }
  };
}
var cqueue = {
  parseMermaid(input) {
    return { version: "1.0", metadata: {}, ...parse6(input) };
  },
  parseYaml(input) {
    return JSON.parse(input);
  },
  layout(ir, theme) {
    return layoutCQueue(ir, theme);
  }
};
function parse7(input) {
  let title;
  let cells2 = [];
  let axis = "horizontal";
  for (const line of lines(input)) {
    const t = line.split(/\s+/);
    if (t[0] === "deque") {
      if (t.length > 1) cells2 = t.slice(1);
      continue;
    }
    if (t[0] === "title") {
      title = line.slice(5).trim();
      continue;
    }
    if (t[0] === "cells" || t[0] === "items") {
      cells2 = t.slice(1);
      continue;
    }
    if (t[0] === "axis") {
      axis = parseAxisToken(t[1], axis);
      continue;
    }
  }
  return { ...title !== void 0 ? { title } : {}, cells: cells2, axis };
}
function layoutDeque(doc, theme) {
  const { palette, typography, spacing } = theme;
  const p = pen(theme);
  const margin = spacing.diagramMargin;
  const font = typography.baseFontSize;
  const cellH = 40;
  const n = doc.cells.length;
  const cellW = Math.max(46, ...doc.cells.map((c) => measureText(c, font).width + 24));
  const axis = doc.axis === "vertical" ? "vertical" : "horizontal";
  const horizontal = axis === "horizontal";
  const titleH = doc.title ? typography.titleFontSize + 14 : 0;
  const topH = font + 14;
  const sideGap = 56;
  const origin = horizontal ? { x: margin + sideGap, y: margin + titleH + topH } : { x: margin, y: margin + titleH + sideGap };
  const cellInputs = doc.cells.map((v) => ({ label: v }));
  const strip = buildStrip(p, theme, cellInputs, {
    origin,
    cellWidth: cellW,
    cellHeight: cellH,
    orientation: axis
  });
  const elements = [...strip.elements];
  if (doc.title) {
    elements.push(
      p.text(
        doc.title,
        margin,
        margin + typography.titleFontSize,
        typography.titleFontSize,
        palette.text,
        { weight: "bold" }
      )
    );
  }
  if (horizontal) {
    const midY = origin.y + cellH / 2;
    const leftEdge = origin.x;
    const rightEdge = origin.x + n * cellW;
    elements.push(
      p.path(
        `M ${rhu(leftEdge)} ${rhu(midY)} L ${rhu(margin)} ${rhu(midY)}`,
        palette.primary,
        1.5,
        { markerStart: ARROW_REV, markerEnd: ARROW_FWD }
      )
    );
    elements.push(
      p.text("push / pop", leftEdge - sideGap / 2, origin.y - 8, font, palette.primary, {
        anchor: "middle",
        weight: "bold"
      })
    );
    elements.push(
      p.path(
        `M ${rhu(rightEdge)} ${rhu(midY)} L ${rhu(rightEdge + sideGap)} ${rhu(midY)}`,
        palette.primary,
        1.5,
        { markerStart: ARROW_REV, markerEnd: ARROW_FWD }
      )
    );
    elements.push(
      p.text("push / pop", rightEdge + sideGap / 2, origin.y - 8, font, palette.primary, {
        anchor: "middle",
        weight: "bold"
      })
    );
  } else {
    const midX = origin.x + cellW / 2;
    const topEdge = origin.y;
    const bottomEdge = origin.y + n * cellH;
    elements.push(
      p.path(
        `M ${rhu(midX)} ${rhu(topEdge)} L ${rhu(midX)} ${rhu(topEdge - sideGap)}`,
        palette.primary,
        1.5,
        { markerStart: ARROW_REV, markerEnd: ARROW_FWD }
      )
    );
    elements.push(
      p.text("push / pop", midX, topEdge - sideGap / 2 - 6, font, palette.primary, {
        anchor: "middle",
        weight: "bold"
      })
    );
    elements.push(
      p.path(
        `M ${rhu(midX)} ${rhu(bottomEdge)} L ${rhu(midX)} ${rhu(bottomEdge + sideGap)}`,
        palette.primary,
        1.5,
        { markerStart: ARROW_REV, markerEnd: ARROW_FWD }
      )
    );
    elements.push(
      p.text("push / pop", midX, bottomEdge + sideGap / 2 + font, font, palette.primary, {
        anchor: "middle",
        weight: "bold"
      })
    );
  }
  if (n > 0) {
    const frontSlot = strip.slots[0];
    const rearSlot = strip.slots[n - 1];
    const same = n === 1;
    const front = horizontal ? pointerBelow(p, theme, frontSlot, "front", palette.secondary, 0) : pointerToSlot(p, theme, frontSlot, "front", palette.secondary, "right", 0);
    const rear = horizontal ? pointerBelow(p, theme, rearSlot, "rear", palette.primary, same ? 1 : 0) : pointerToSlot(p, theme, rearSlot, "rear", palette.primary, "right", same ? 1 : 0);
    elements.push(...front.elements, ...rear.elements);
  }
  const anchors = {};
  strip.slots.forEach((slot, i) => {
    anchors[`c${i}`] = { bounds: slot };
  });
  const finalized = finalizeStripScene(elements, anchors, theme, [
    arrowDefs(palette.primary, theme.edges?.arrowSize)
  ]);
  return { scene: finalized.scene, anchors: finalized.anchors };
}
var deque = {
  parseMermaid(input) {
    return { version: "1.0", metadata: {}, ...parse7(input) };
  },
  parseYaml(input) {
    return JSON.parse(input);
  },
  layout(ir, theme) {
    return layoutDeque(ir, theme);
  }
};
function parse8(input) {
  let title;
  const items = [];
  let axis = "horizontal";
  for (const line of lines(input)) {
    const t = line.split(/\s+/);
    if (t[0] === "pqueue") {
      continue;
    }
    if (t[0] === "title") {
      title = line.slice(5).trim();
      continue;
    }
    if (t[0] === "axis") {
      axis = parseAxisToken(t[1], axis);
      continue;
    }
    if (t[0] === "item") {
      const rest = line.slice(4).trim();
      const m = rest.match(/^(.*?)\s+(-?\d+(?:\.\d+)?)\s*$/);
      if (m) items.push({ label: m[1].trim(), priority: Number(m[2]) });
      else if (rest) items.push({ label: rest, priority: 0 });
    }
  }
  return { ...title !== void 0 ? { title } : {}, items, axis };
}
function parseHex4(c) {
  const s = c.trim();
  const m3 = s.match(/^#([0-9a-f])([0-9a-f])([0-9a-f])$/i);
  if (m3)
    return [
      parseInt(m3[1] + m3[1], 16),
      parseInt(m3[2] + m3[2], 16),
      parseInt(m3[3] + m3[3], 16)
    ];
  const m6 = s.match(/^#([0-9a-f]{2})([0-9a-f]{2})([0-9a-f]{2})$/i);
  if (m6) return [parseInt(m6[1], 16), parseInt(m6[2], 16), parseInt(m6[3], 16)];
  return null;
}
function toHex3(n) {
  return Math.max(0, Math.min(255, Math.round(n))).toString(16).padStart(2, "0");
}
function mixHex3(a, b, t) {
  const ca = parseHex4(a), cb = parseHex4(b);
  if (!ca || !cb) return a;
  const r = ca[0] + (cb[0] - ca[0]) * t;
  const g = ca[1] + (cb[1] - ca[1]) * t;
  const bl = ca[2] + (cb[2] - ca[2]) * t;
  return `#${toHex3(r)}${toHex3(g)}${toHex3(bl)}`;
}
function layoutPQueue(doc, theme) {
  const { palette, typography, spacing } = theme;
  const p = pen(theme);
  const margin = spacing.diagramMargin;
  const font = typography.baseFontSize;
  const cellH = 38;
  const axis = doc.axis === "vertical" ? "vertical" : "horizontal";
  const items = doc.items.map((it, i) => ({ ...it, i })).sort((a, b) => b.priority - a.priority || a.i - b.i);
  const priorities = items.map((it) => it.priority);
  const max = priorities.length ? Math.max(...priorities) : 0;
  const min = priorities.length ? Math.min(...priorities) : 0;
  const span = max - min;
  const labelW = Math.max(40, ...items.map((it) => measureText(it.label, font).width));
  const prioW = Math.max(24, ...items.map((it) => measureText(String(it.priority), font).width));
  const padX = 14;
  const cellW = labelW + prioW + padX * 3;
  const titleH = doc.title ? typography.titleFontSize + 14 : 0;
  const origin = { x: margin, y: margin + titleH };
  const cellInputs = items.map((it) => {
    const norm = span === 0 ? 1 : (it.priority - min) / span;
    const t = 0.15 + (1 - norm) * 0.65;
    return { fill: mixHex3(palette.primary, palette.surface, t) };
  });
  const strip = buildStrip(p, theme, cellInputs, {
    origin,
    cellWidth: cellW,
    cellHeight: cellH,
    orientation: axis
  });
  const elements = [...strip.elements];
  if (doc.title) {
    elements.push(
      p.text(
        doc.title,
        margin,
        margin + typography.titleFontSize,
        typography.titleFontSize,
        palette.text,
        { weight: "bold" }
      )
    );
  }
  items.forEach((it, i) => {
    const slot = strip.slots[i];
    const cy = slot.y + slot.height / 2 + font * 0.35;
    elements.push(p.text(it.label, slot.x + padX, cy, font, palette.text, { weight: "bold" }));
    elements.push(
      p.text(String(it.priority), slot.x + slot.width - padX, cy, font, palette.text, {
        anchor: "end",
        weight: "bold"
      })
    );
  });
  const anchors = {};
  strip.slots.forEach((slot, i) => {
    anchors[`c${i}`] = { bounds: slot };
  });
  const finalized = finalizeStripScene(elements, anchors, theme);
  return { scene: finalized.scene, anchors: finalized.anchors };
}
var pqueue = {
  parseMermaid(input) {
    return { version: "1.0", metadata: {}, ...parse8(input) };
  },
  parseYaml(input) {
    return JSON.parse(input);
  },
  layout(ir, theme) {
    return layoutPQueue(ir, theme);
  }
};
function parse9(input) {
  let title;
  let cells2 = [];
  let capacity;
  let axis = "vertical";
  for (const line of lines(input)) {
    const t = line.split(/\s+/);
    if (t[0] === "stack") {
      if (t.length > 1) cells2 = t.slice(1);
      continue;
    }
    if (t[0] === "title") {
      title = line.slice(5).trim();
      continue;
    }
    if (t[0] === "cells" || t[0] === "items") {
      cells2 = t.slice(1);
      continue;
    }
    if (t[0] === "capacity") {
      const n = Number(t[1]);
      if (Number.isFinite(n)) capacity = n;
      continue;
    }
    if (t[0] === "axis") {
      axis = parseAxisToken(t[1], axis);
      continue;
    }
  }
  return {
    ...title !== void 0 ? { title } : {},
    cells: cells2,
    ...capacity !== void 0 ? { capacity } : {},
    axis
  };
}
function layoutStack(doc, theme) {
  const { palette, typography, spacing } = theme;
  const p = pen(theme);
  const margin = spacing.diagramMargin;
  const font = typography.baseFontSize;
  const cellH = 38;
  const filled = doc.cells.length;
  const total = Math.max(filled, doc.capacity ?? filled, 1);
  const empty = total - filled;
  const cellW = Math.max(64, ...doc.cells.map((c) => measureText(c, font).width + 28));
  const axis = doc.axis === "horizontal" ? "horizontal" : "vertical";
  const horizontal = axis === "horizontal";
  const titleH = doc.title ? typography.titleFontSize + 14 : 0;
  const topH = font + 18;
  const sideGap = 64;
  const origin = horizontal ? { x: margin, y: margin + titleH + topH } : { x: margin + sideGap, y: margin + titleH + topH };
  const display = [
    ...Array.from({ length: empty }, () => null),
    ...[...doc.cells].reverse()
  ];
  const cellInputs = display.map(
    (v) => v !== null ? { label: v } : { fill: palette.background }
  );
  const strip = buildStrip(p, theme, cellInputs, {
    origin,
    cellWidth: cellW,
    cellHeight: cellH,
    orientation: axis
  });
  const elements = [...strip.elements];
  if (doc.title) {
    elements.push(
      p.text(
        doc.title,
        margin,
        margin + typography.titleFontSize,
        typography.titleFontSize,
        palette.text,
        { weight: "bold" }
      )
    );
  }
  const topSlot = filled > 0 ? strip.slots[empty] : strip.slots[0];
  const actionX = topSlot ? topSlot.x + topSlot.width / 2 : origin.x + cellW / 2;
  elements.push(
    p.text("push / pop", actionX, origin.y - 9, font, palette.primary, {
      anchor: "middle",
      weight: "bold"
    })
  );
  elements.push(
    p.path(
      `M ${rhu(actionX)} ${rhu(origin.y - topH + 8)} L ${rhu(actionX)} ${rhu(origin.y)}`,
      palette.primary,
      1.5,
      { markerEnd: ARROW_ID2 }
    )
  );
  if (filled > 0) {
    if (horizontal) {
      const top = pointerToSlot(p, theme, topSlot, "top", palette.secondary, "bottom", 0);
      elements.push(...top.elements);
    } else {
      const cyTop = topSlot.y + topSlot.height / 2;
      elements.push(
        p.path(
          `M ${rhu(origin.x - sideGap + 8)} ${rhu(cyTop)} L ${rhu(origin.x)} ${rhu(cyTop)}`,
          palette.secondary,
          1.5,
          { markerEnd: ARROW_ID2 }
        )
      );
      elements.push(
        p.text("top", origin.x - sideGap + 6, cyTop - 6, font, palette.secondary, {
          anchor: "start",
          weight: "bold"
        })
      );
    }
  }
  const anchors = {};
  strip.slots.forEach((slot, i) => {
    anchors[`c${i}`] = { bounds: slot };
  });
  const finalized = finalizeStripScene(elements, anchors, theme, [
    arrowDef(palette.primary, theme.edges?.arrowSize)
  ]);
  return { scene: finalized.scene, anchors: finalized.anchors };
}
var stack = {
  parseMermaid(input) {
    return { version: "1.0", metadata: {}, ...parse9(input) };
  },
  parseYaml(input) {
    return JSON.parse(input);
  },
  layout(ir, theme) {
    return layoutStack(ir, theme);
  }
};
function parseEntries(spec) {
  return spec.split(",").map((s) => s.trim()).filter(Boolean).map((pair) => {
    const m = pair.match(/^(.*?)\s*(?:->|=>|:|=)\s*(.*)$/);
    if (m) return { key: m[1].trim(), value: m[2].trim() };
    return { key: pair, value: "" };
  });
}
function parseBucketLabels(spec) {
  const labels = [];
  let current = "";
  let inQuotes = false;
  let escaping = false;
  for (const ch of spec) {
    if (escaping) {
      current += ch;
      escaping = false;
      continue;
    }
    if (inQuotes && ch === "\\") {
      escaping = true;
      continue;
    }
    if (ch === '"') {
      inQuotes = !inQuotes;
      continue;
    }
    if (!inQuotes && ch === ",") {
      const label = current.trim();
      if (label) labels.push(label);
      current = "";
      continue;
    }
    current += ch;
  }
  if (inQuotes) throw new Error(`Unterminated bucket label: ${spec}`);
  const tail = current.trim();
  if (tail) labels.push(tail);
  return labels;
}
function splitBucketDirective(spec) {
  let inQuotes = false;
  let escaping = false;
  for (let i = 0; i < spec.length; i++) {
    const ch = spec[i];
    if (escaping) {
      escaping = false;
      continue;
    }
    if (inQuotes && ch === "\\") {
      escaping = true;
      continue;
    }
    if (ch === '"') {
      inQuotes = !inQuotes;
      continue;
    }
    if (!inQuotes && ch === ":") {
      return { id: spec.slice(0, i).trim(), entries: spec.slice(i + 1).trim() };
    }
  }
  return null;
}
function parseBucketId(spec) {
  const [label = ""] = parseBucketLabels(spec);
  return /^\d+$/.test(label) ? Number(label) : label;
}
function normalizeDoc(doc) {
  const chains = doc.chains ?? [];
  const bucketLabels = [...doc.bucketLabels ?? []];
  for (const chain of chains) {
    if (typeof chain.index === "string" && !bucketLabels.includes(chain.index))
      bucketLabels.push(chain.index);
  }
  const maxIdx = chains.reduce(
    (mx, c) => typeof c.index === "number" ? Math.max(mx, c.index) : mx,
    -1
  );
  const numericBucketCount = Math.max(doc.buckets ?? 0, maxIdx + 1, 1);
  if (bucketLabels.length === 0) {
    for (let i = 0; i < numericBucketCount; i++) bucketLabels.push(String(i));
  } else {
    while (bucketLabels.length < numericBucketCount) bucketLabels.push(String(bucketLabels.length));
  }
  return {
    ...doc.title !== void 0 ? { title: doc.title } : {},
    buckets: bucketLabels.length,
    bucketLabels,
    chains
  };
}
function parse10(input) {
  let title;
  let bucketCount;
  let explicitBucketLabels;
  const chains = [];
  for (const line of lines(input)) {
    const t = line.split(/\s+/);
    if (t[0] === "hashmap") {
      continue;
    }
    if (t[0] === "title") {
      title = line.slice(5).trim();
      continue;
    }
    if (t[0] === "buckets") {
      const spec = line.slice(7).trim();
      if (!spec) continue;
      if (/^\d+$/.test(spec)) {
        bucketCount = Number(spec);
        explicitBucketLabels = void 0;
      } else {
        explicitBucketLabels = parseBucketLabels(spec);
      }
      continue;
    }
    if (t[0] === "bucket") {
      const parsed = splitBucketDirective(line.slice(6).trim());
      if (parsed)
        chains.push({ index: parseBucketId(parsed.id), entries: parseEntries(parsed.entries) });
    }
  }
  return normalizeDoc({ title, buckets: bucketCount, bucketLabels: explicitBucketLabels, chains });
}
function bucketKey(id) {
  return typeof id === "number" ? `n:${id}` : `s:${id}`;
}
function layoutHashmap(doc, theme) {
  const { palette, typography, spacing } = theme;
  const p = pen(theme);
  const margin = spacing.diagramMargin;
  const font = typography.baseFontSize;
  const cellH = 38;
  const idxW = Math.max(
    46,
    ...doc.bucketLabels.map((label) => measureText(label, font).width + 24)
  );
  const entryH = 30;
  const entryGap = 30;
  const chainGap = 36;
  const titleH = doc.title ? typography.titleFontSize + 14 : 0;
  const origin = { x: margin, y: margin + titleH };
  const elements = [];
  if (doc.title) {
    elements.push(
      p.text(
        doc.title,
        margin,
        margin + typography.titleFontSize,
        typography.titleFontSize,
        palette.text,
        { weight: "bold" }
      )
    );
  }
  const slotById = /* @__PURE__ */ new Map();
  const anchors = {};
  for (let i = 0; i < doc.buckets; i++) {
    const slot = { x: origin.x, y: origin.y + i * cellH, width: idxW, height: cellH };
    const label = doc.bucketLabels[i] ?? String(i);
    slotById.set(bucketKey(i), { row: i, bounds: slot });
    if (!slotById.has(bucketKey(label))) slotById.set(bucketKey(label), { row: i, bounds: slot });
    anchors[`b${i}`] = { bounds: slot };
    elements.push(p.rect(slot, palette.surface, palette.border, 1.5, { rx: 3 }));
    elements.push(
      p.text(
        label,
        slot.x + slot.width / 2,
        slot.y + slot.height / 2 + font * 0.35,
        font,
        palette.textMuted,
        { anchor: "middle", weight: "bold" }
      )
    );
  }
  const chainStartX = origin.x + idxW + chainGap;
  let maxRight2 = chainStartX;
  for (const chain of doc.chains) {
    const slotRef = slotById.get(bucketKey(chain.index));
    if (!slotRef || chain.entries.length === 0) continue;
    const { bounds: slot, row } = slotRef;
    const cy = slot.y + slot.height / 2;
    let x = chainStartX;
    let fromX = slot.x + slot.width;
    chain.entries.forEach((e, j) => {
      const label = e.value ? `${e.key} : ${e.value}` : e.key;
      const w = measureText(label, font).width + 22;
      const box = { x, y: cy - entryH / 2, width: w, height: entryH };
      elements.push(
        p.path(`M ${rhu(fromX)} ${rhu(cy)} L ${rhu(x - 3)} ${rhu(cy)}`, palette.primary, 1.5, {
          markerEnd: ARROW_ID2
        })
      );
      elements.push(p.rect(box, palette.surface, palette.border, 1.5, { rx: 4 }));
      elements.push(
        p.text(label, x + w / 2, cy + font * 0.35, font, palette.text, {
          anchor: "middle",
          weight: "bold"
        })
      );
      anchors[`b${row}e${j}`] = { bounds: box };
      fromX = x + w;
      x = fromX + entryGap;
      maxRight2 = Math.max(maxRight2, fromX);
    });
  }
  const width = maxRight2 + margin;
  const height = origin.y + doc.buckets * cellH + margin;
  const scene = {
    viewBox: { x: 0, y: 0, width, height },
    background: palette.background,
    elements,
    defs: [arrowDef(palette.primary, theme.edges?.arrowSize)]
  };
  return { scene, anchors };
}
var hashmap = {
  parseMermaid(input) {
    return { version: "1.0", metadata: {}, ...parse10(input) };
  },
  parseYaml(input) {
    return {
      version: "1.0",
      metadata: {},
      ...normalizeDoc(JSON.parse(input))
    };
  },
  layout(ir, theme) {
    return layoutHashmap(ir, theme);
  }
};
function parse11(input) {
  let title;
  const rows = [];
  let showIndex = true;
  const highlights = [];
  for (const line of input.split(/\r?\n/).map((l) => l.trimEnd())) {
    const trimmed = line.trim();
    if (!trimmed) continue;
    const t = trimmed.split(/\s+/);
    if (t[0] === "matrix") {
      const dim = t[1]?.match(/^(\d+)x(\d+)$/i);
      if (dim) {
        const r = Number(dim[1]), c = Number(dim[2]);
        for (let i = 0; i < r; i++) rows.push(Array.from({ length: c }, () => ""));
      }
      continue;
    }
    if (t[0] === "title") {
      title = trimmed.slice(5).trim();
      continue;
    }
    if (t[0] === "noindex") {
      showIndex = false;
      continue;
    }
    if (t[0] === "row") {
      rows.push(t.slice(1));
      continue;
    }
    if (t[0] === "highlight") {
      for (const tok of t.slice(1)) {
        const m = tok.match(/^(\d+),(\d+)$/);
        if (m) highlights.push([Number(m[1]), Number(m[2])]);
      }
      continue;
    }
  }
  return {
    ...title !== void 0 ? { title } : {},
    rows,
    showIndex,
    ...highlights.length > 0 ? { highlights } : {}
  };
}
function layoutMatrix(doc, theme) {
  const { palette, typography, spacing } = theme;
  const p = pen(theme);
  const margin = spacing.diagramMargin;
  const font = typography.baseFontSize;
  const cellH = 38;
  const nRows = doc.rows.length;
  const nCols = doc.rows.reduce((m, r) => Math.max(m, r.length), 0);
  const grid = doc.rows.map((r) => Array.from({ length: nCols }, (_, j) => r[j] ?? ""));
  const cellW = Math.max(40, ...grid.flat().map((v) => measureText(v, font).width + 20));
  const titleH = doc.title ? typography.titleFontSize + 14 : 0;
  const colIdxH = doc.showIndex ? typography.smallFontSize + 8 : 0;
  const rowIdxW = doc.showIndex ? 22 : 0;
  const origin = { x: margin + rowIdxW, y: margin + titleH + colIdxH };
  const elements = [];
  if (doc.title) {
    elements.push(
      p.text(
        doc.title,
        margin,
        margin + typography.titleFontSize,
        typography.titleFontSize,
        palette.text,
        { weight: "bold" }
      )
    );
  }
  const anchors = {};
  grid.forEach((row, r) => {
    const rowOrigin = { x: origin.x, y: origin.y + r * cellH };
    const cellInputs = row.map((v, c) => {
      const isHighlit = doc.highlights?.some(([hr, hc]) => hr === r && hc === c) ?? false;
      return isHighlit ? { label: v, fill: palette.primary, fillOpacity: 0.22, stroke: palette.primary } : { label: v };
    });
    const strip = buildStrip(p, theme, cellInputs, {
      origin: rowOrigin,
      cellWidth: cellW,
      cellHeight: cellH
    });
    elements.push(...strip.elements);
    strip.slots.forEach((slot, c) => {
      anchors[`r${r}c${c}`] = { bounds: slot };
    });
    if (doc.showIndex) {
      elements.push(
        p.text(
          String(r),
          origin.x - 7,
          rowOrigin.y + cellH / 2 + font * 0.35,
          typography.smallFontSize,
          palette.textMuted,
          { anchor: "end" }
        )
      );
    }
  });
  if (doc.showIndex) {
    for (let c = 0; c < nCols; c++) {
      const cx = origin.x + c * cellW + cellW / 2;
      elements.push(
        p.text(String(c), cx, origin.y - 6, typography.smallFontSize, palette.textMuted, {
          anchor: "middle"
        })
      );
    }
  }
  const width = origin.x + nCols * cellW + margin;
  const height = origin.y + nRows * cellH + margin;
  const scene = {
    viewBox: { x: 0, y: 0, width, height },
    background: palette.background,
    elements
  };
  return { scene, anchors };
}
var matrix = {
  parseMermaid(input) {
    return { version: "1.0", metadata: {}, ...parse11(input) };
  },
  parseYaml(input) {
    return JSON.parse(input);
  },
  layout(ir, theme) {
    return layoutMatrix(ir, theme);
  }
};
var newNode2 = () => ({ edges: /* @__PURE__ */ new Map(), end: false });
function insert4(root, word) {
  let node = root;
  for (const ch of word) {
    let child = node.edges.get(ch);
    if (child === void 0) {
      child = newNode2();
      node.edges.set(ch, child);
    }
    node = child;
  }
  node.end = true;
}
function buildTrie(input) {
  const tokens = input.split(/\s+/).filter(Boolean);
  const words = tokens.filter((t) => t !== "trie" && t !== "insert" && /^[A-Za-z0-9]+$/.test(t));
  const root = newNode2();
  for (const w of words) insert4(root, w);
  const nodes = [];
  let i = 0;
  const emit = (n, prefix, edgeLabel) => {
    const id = `n${i++}`;
    const children = [];
    const node = n.end ? {
      id,
      label: prefix,
      kinds: ["leaf", "active"],
      children,
      ...edgeLabel !== void 0 ? { edgeLabel } : {}
    } : {
      id,
      label: "",
      kinds: ["dot"],
      children,
      ...edgeLabel !== void 0 ? { edgeLabel } : {}
    };
    nodes.push(node);
    for (const [ch, child] of n.edges) children.push(emit(child, prefix + ch, ch));
    return id;
  };
  emit(root, "");
  return { version: "1.0", metadata: {}, direction: "TB", nodes };
}
var trie = {
  parseMermaid: buildTrie,
  parseYaml: (input) => JSON.parse(input),
  layout: (ir, theme) => layoutTree(ir, theme)
};
var ARROW_ID13 = "dsgraph-arrow";
var ARROW_ACTIVE_ID = "dsgraph-arrow-active";
function graphArrowDef(color, id, size = 8) {
  const s = size;
  const sH = rhu(s * 0.7);
  const sMidY = rhu(s * 0.35);
  const sRefX = rhu(s - 1);
  return `<marker id="${id}" markerWidth="${s}" markerHeight="${sH}" refX="${sRefX}" refY="${sMidY}" orient="auto" markerUnits="userSpaceOnUse"><polygon points="0 0, ${s} ${sMidY}, 0 ${sH}" fill="${color}" /></marker>`;
}
var EDGE_RE = /^(\S+)\s*(<->|->|--)\s*(\S+)(?:\s*:\s*(.+))?$/;
function parse12(input) {
  let title;
  let directed = false;
  const order = [];
  const labels = /* @__PURE__ */ new Map();
  const nodeKinds = /* @__PURE__ */ new Map();
  const edges2 = [];
  const ensure = (id) => {
    if (!labels.has(id)) {
      labels.set(id, id);
      order.push(id);
    }
  };
  for (const raw of input.split(/\r?\n/)) {
    const line = raw.trim();
    if (!line) continue;
    const t = line.split(/\s+/);
    if (t[0] === "nodegraph" || t[0] === "dsgraph") continue;
    if (t[0] === "directed") {
      directed = true;
      continue;
    }
    if (t[0] === "undirected") {
      directed = false;
      continue;
    }
    if (t[0] === "title") {
      title = line.slice(5).trim();
      continue;
    }
    if (t[0] === "node") {
      const parts = line.slice(4).split(":").map((s) => s.trim());
      const id = parts[0] ?? "";
      if (id) {
        ensure(id);
        if (parts.length === 2) {
          labels.set(id, parts[1] || id);
        } else if (parts.length >= 3) {
          labels.set(id, parts[1] || id);
          if (parts[2] === "active") {
            nodeKinds.set(id, "active");
          }
        }
      }
      continue;
    }
    const m = line.match(EDGE_RE);
    if (m) {
      const from = m[1], to = m[3];
      const rawLabel = m[4]?.trim();
      let label;
      let kind;
      if (rawLabel === "active") kind = "active";
      else if (rawLabel === "dashed") kind = "dashed";
      else label = rawLabel;
      ensure(from);
      ensure(to);
      edges2.push({ from, to, ...label ? { label } : {}, ...kind ? { kind } : {} });
    }
  }
  const nodes = order.map((id) => ({
    id,
    label: labels.get(id),
    ...nodeKinds.has(id) ? { kind: nodeKinds.get(id) } : {}
  }));
  return { ...title !== void 0 ? { title } : {}, directed, nodes, edges: edges2 };
}
function layoutGraph(doc, theme) {
  const { palette, typography, spacing } = theme;
  const p = pen(theme);
  const margin = spacing.diagramMargin;
  const font = typography.baseFontSize;
  const small = typography.smallFontSize;
  const titleH = doc.title ? typography.titleFontSize + 14 : 0;
  const nodeH = 40;
  const sizeOf = (n) => Math.max(64, measureText(n.label, font).width + 28);
  const gNodes = doc.nodes.map((n) => ({ id: n.id, width: sizeOf(n), height: nodeH }));
  const gEdges = doc.edges.map((e) => ({ from: e.from, to: e.to }));
  const placed = layeredLayout(gNodes, gEdges, {
    direction: "TB",
    layerGap: 64,
    nodeGap: 44,
    margin
  });
  const box = (id) => {
    const b = placed.boxes.get(id);
    return { x: b.x, y: b.y + titleH, width: b.width, height: b.height };
  };
  const pathData = (points) => points.map((pt, i) => `${i === 0 ? "M" : "L"} ${rhu(pt.x)} ${rhu(pt.y)}`).join(" ");
  const pathMidpoint = (points) => {
    if (points.length === 0) return { x: 0, y: 0 };
    let total = 0;
    for (let i = 1; i < points.length; i++) {
      total += Math.hypot(points[i].x - points[i - 1].x, points[i].y - points[i - 1].y);
    }
    if (total === 0) return points[0];
    let walked = 0;
    const target = total / 2;
    for (let i = 1; i < points.length; i++) {
      const from = points[i - 1];
      const to = points[i];
      const segment = Math.hypot(to.x - from.x, to.y - from.y);
      if (walked + segment >= target && segment > 0) {
        const t = (target - walked) / segment;
        return { x: from.x + (to.x - from.x) * t, y: from.y + (to.y - from.y) * t };
      }
      walked += segment;
    }
    return points[points.length - 1];
  };
  const dirForWall = (wall) => {
    switch (wall) {
      case "top":
        return "N";
      case "bottom":
        return "S";
      case "left":
        return "W";
      case "right":
        return "E";
    }
  };
  const targetWall = (from, to) => {
    if (from.y + from.height <= to.y) return "top";
    if (to.y + to.height <= from.y) return "bottom";
    const dx = to.x + to.width / 2 - (from.x + from.width / 2);
    const dy = to.y + to.height / 2 - (from.y + from.height / 2);
    if (Math.abs(dy) >= Math.abs(dx)) return dy >= 0 ? "top" : "bottom";
    return dx >= 0 ? "left" : "right";
  };
  const sourceWall = (from, to) => targetWall(to, from);
  const wallPoint2 = (r, wall, axis) => {
    switch (wall) {
      case "top":
        return { x: axis, y: r.y };
      case "bottom":
        return { x: axis, y: r.y + r.height };
      case "left":
        return { x: r.x, y: axis };
      case "right":
        return { x: r.x + r.width, y: axis };
    }
  };
  const MIN_PORT_GAP2 = 28;
  const WALL_MARGIN2 = 16;
  const SKIP_LANE_CLEARANCE = 40;
  const SKIP_LANE_GAP = 28;
  const SKIP_STUB = 16;
  const cascadePorts2 = (ideals, lo, hi) => {
    const n = ideals.length;
    if (n === 0) return [];
    if (hi <= lo) return Array.from({ length: n }, () => (lo + hi) / 2);
    if (n === 1) return [Math.max(lo, Math.min(hi, ideals[0]))];
    if ((n - 1) * MIN_PORT_GAP2 > hi - lo) {
      const step = (hi - lo) / (n + 1);
      return Array.from({ length: n }, (_, i) => lo + step * (i + 1));
    }
    const pos = ideals.map((v) => Math.max(lo, Math.min(hi, v)));
    for (let iter = 0; iter < 5; iter++) {
      let changed = false;
      for (let i = 1; i < n; i++) {
        const minI = pos[i - 1] + MIN_PORT_GAP2;
        if (pos[i] < minI) {
          pos[i] = minI;
          changed = true;
        }
      }
      for (let i = n - 1; i >= 0; i--) {
        const maxI = i === n - 1 ? hi : pos[i + 1] - MIN_PORT_GAP2;
        if (pos[i] > maxI) {
          pos[i] = maxI;
          changed = true;
        }
      }
      if (pos[0] < lo) {
        pos[0] = lo;
        changed = true;
      }
      if (!changed) break;
    }
    return pos;
  };
  const assignPorts = (rect, wall, group) => {
    const result = /* @__PURE__ */ new Map();
    if (group.length === 0) return result;
    const sorted = [...group].sort((a, b) => a.ideal - b.ideal || a.edgeIndex - b.edgeIndex);
    const horizontal = wall === "top" || wall === "bottom";
    const base = horizontal ? rect.x : rect.y;
    const len = horizontal ? rect.width : rect.height;
    const lo = base + Math.min(WALL_MARGIN2, len / 3);
    const hi = base + len - Math.min(WALL_MARGIN2, len / 3);
    const positions = cascadePorts2(
      sorted.map((e) => e.ideal),
      lo,
      hi
    );
    for (let i = 0; i < sorted.length; i++) {
      result.set(sorted[i].edgeIndex, wallPoint2(rect, wall, positions[i]));
    }
    return result;
  };
  const simplifyPoints = (raw) => {
    const points = [];
    for (const pt of raw) {
      const prev = points[points.length - 1];
      if (!prev || Math.abs(prev.x - pt.x) > 1e-6 || Math.abs(prev.y - pt.y) > 1e-6) {
        points.push(pt);
      }
    }
    for (let i = 1; i < points.length - 1; ) {
      const a = points[i - 1], b = points[i], c = points[i + 1];
      const collinearX = Math.abs(a.x - b.x) < 1e-6 && Math.abs(b.x - c.x) < 1e-6;
      const collinearY = Math.abs(a.y - b.y) < 1e-6 && Math.abs(b.y - c.y) < 1e-6;
      if (collinearX || collinearY) points.splice(i, 1);
      else i++;
    }
    return points;
  };
  const realBoxes = [...placed.boxes.values()];
  const skipLaneX = /* @__PURE__ */ new Map();
  let skipLaneOrdinal = 0;
  for (const [i, e] of doc.edges.entries()) {
    const bends = placed.edgeBends.get(i);
    if (!bends || bends.length === 0) continue;
    const a = placed.boxes.get(e.from);
    const b = placed.boxes.get(e.to);
    if (!a || !b) continue;
    const minCy = Math.min(a.y + a.height / 2, b.y + b.height / 2);
    const maxCy = Math.max(a.y + a.height / 2, b.y + b.height / 2);
    const spanBoxes = realBoxes.filter((ob) => {
      const cy = ob.y + ob.height / 2;
      return cy >= minCy && cy <= maxCy;
    });
    const right = Math.max(...spanBoxes.map((ob) => ob.x + ob.width));
    skipLaneX.set(i, right + SKIP_LANE_CLEARANCE + skipLaneOrdinal * SKIP_LANE_GAP);
    skipLaneOrdinal++;
  }
  const fromGroups = /* @__PURE__ */ new Map();
  const toGroups = /* @__PURE__ */ new Map();
  const fromWallByEdge = /* @__PURE__ */ new Map();
  const toWallByEdge = /* @__PURE__ */ new Map();
  const groupKey = (id, wall) => `${id}\0${wall}`;
  const axisIdeal = (wall, other, edgeIndex) => {
    const lane = skipLaneX.get(edgeIndex);
    if (lane !== void 0 && (wall === "top" || wall === "bottom")) return lane;
    return wall === "top" || wall === "bottom" ? other.x + other.width / 2 : other.y + other.height / 2;
  };
  for (const [i, e] of doc.edges.entries()) {
    const a = placed.boxes.has(e.from) ? box(e.from) : void 0;
    const b = placed.boxes.has(e.to) ? box(e.to) : void 0;
    if (!a || !b) continue;
    const lane = skipLaneX.get(i);
    const fw = lane !== void 0 ? lane > a.x + a.width / 2 ? "right" : "left" : sourceWall(a, b);
    const tw = lane !== void 0 ? lane > b.x + b.width / 2 ? "right" : "left" : targetWall(a, b);
    fromWallByEdge.set(i, fw);
    toWallByEdge.set(i, tw);
    const fk = groupKey(e.from, fw);
    const tk = groupKey(e.to, tw);
    if (!fromGroups.has(fk)) fromGroups.set(fk, []);
    if (!toGroups.has(tk)) toGroups.set(tk, []);
    fromGroups.get(fk).push({ edgeIndex: i, ideal: axisIdeal(fw, b, i) });
    toGroups.get(tk).push({ edgeIndex: i, ideal: axisIdeal(tw, a, i) });
  }
  const fromPorts = /* @__PURE__ */ new Map();
  const toPorts = /* @__PURE__ */ new Map();
  for (const [key, group] of fromGroups) {
    const [id, wall] = key.split("\0");
    fromPorts.set(key, assignPorts(box(id), wall, group));
  }
  for (const [key, group] of toGroups) {
    const [id, wall] = key.split("\0");
    toPorts.set(key, assignPorts(box(id), wall, group));
  }
  const elements = [];
  const labelElements = [];
  let maxRouteX = placed.width - margin;
  if (doc.title) {
    elements.push(
      p.text(
        doc.title,
        margin,
        margin + typography.titleFontSize,
        typography.titleFontSize,
        palette.text,
        { weight: "bold" }
      )
    );
  }
  for (const [i, e] of doc.edges.entries()) {
    const a = placed.boxes.has(e.from) ? box(e.from) : void 0;
    const b = placed.boxes.has(e.to) ? box(e.to) : void 0;
    if (!a || !b) continue;
    const bends = placed.edgeBends.get(i);
    const fw = fromWallByEdge.get(i) ?? sourceWall(a, b);
    const tw = toWallByEdge.get(i) ?? targetWall(a, b);
    const fromPt = fromPorts.get(groupKey(e.from, fw))?.get(i) ?? borderPoint(a, b.x + b.width / 2, b.y + b.height / 2);
    const toPt = toPorts.get(groupKey(e.to, tw))?.get(i) ?? borderPoint(b, a.x + a.width / 2, a.y + a.height / 2);
    const hasBends = bends && bends.length > 0;
    const points = hasBends && skipLaneX.has(i) ? (() => {
      const laneX = skipLaneX.get(i);
      return simplifyPoints([
        fromPt,
        { x: laneX, y: fromPt.y },
        { x: laneX, y: toPt.y },
        toPt
      ]);
    })() : hasBends ? (() => {
      const obstacles = [...placed.boxes.values()].filter((ob) => ob.id !== e.from && ob.id !== e.to).map((ob) => ({ x: ob.x, y: ob.y + titleH, width: ob.width, height: ob.height }));
      return orthogonalRouter.route({
        from: fromPt,
        to: toPt,
        style: "orthogonal",
        obstacles,
        padding: 10,
        fromDir: dirForWall(fw),
        toDir: dirForWall(tw)
      }).points;
    })() : (() => {
      const { start, end } = connectSlots(a, b);
      return [start, end];
    })();
    maxRouteX = Math.max(maxRouteX, ...points.map((pt) => pt.x));
    const isActive = e.kind === "active";
    const edgeColor = isActive ? palette.primary : palette.textMuted;
    const edgeWidth = isActive ? 2.5 : 1.5;
    const pathOpts = {
      ...doc.directed ? { markerEnd: isActive ? ARROW_ACTIVE_ID : ARROW_ID13 } : {},
      ...e.kind === "dashed" ? { dash: "6 3" } : {}
    };
    elements.push(p.path(pathData(points), edgeColor, edgeWidth, pathOpts));
    if (e.label) {
      const { x: mx, y: my } = pathMidpoint(points);
      const edgeFont = theme.edges?.labelFontSize ?? 12;
      const w = measureText(e.label, edgeFont).width + 8;
      maxRouteX = Math.max(maxRouteX, mx + w / 2);
      labelElements.push(
        p.rect(
          { x: mx - w / 2, y: my - 9, width: w, height: 16 },
          palette.background,
          palette.background,
          0,
          { rx: 3 }
        )
      );
      labelElements.push(
        p.text(e.label, mx, my + 3, edgeFont, isActive ? palette.primary : palette.textMuted, {
          anchor: "middle"
        })
      );
    }
  }
  elements.push(...labelElements);
  const anchors = {};
  for (const n of doc.nodes) {
    const b = box(n.id);
    const isActive = n.kind === "active";
    const fill = isActive ? palette.primary : palette.surface;
    const stroke = isActive ? palette.primary : palette.border;
    const sw = isActive ? 2 : theme.nodes?.standard.borderWidth ?? 1.5;
    const rx = theme.nodes?.standard.cornerRadius ?? 6;
    const textColor = isActive ? readableText(fill, theme) : palette.text;
    elements.push(p.rect(b, fill, stroke, sw, { rx }));
    elements.push(
      p.text(n.label, b.x + b.width / 2, b.y + b.height / 2 + font * 0.35, font, textColor, {
        anchor: "middle",
        weight: "bold"
      })
    );
    anchors[n.id] = { bounds: b };
  }
  const titleWidth = doc.title ? measureText(doc.title, typography.titleFontSize).width : 0;
  const viewWidth = Math.max(
    placed.width + margin,
    maxRouteX + margin,
    doc.title ? margin + titleWidth + margin : 0
  );
  const scene = {
    viewBox: { x: 0, y: 0, width: viewWidth, height: placed.height + titleH + margin },
    background: palette.background,
    elements,
    ...doc.directed ? {
      defs: [
        graphArrowDef(palette.textMuted, ARROW_ID13, theme.edges?.arrowSize ?? 8),
        graphArrowDef(palette.primary, ARROW_ACTIVE_ID, theme.edges?.arrowSize ?? 8)
      ]
    } : {}
  };
  return { scene, anchors };
}
var graph = {
  parseMermaid(input) {
    return { version: "1.0", metadata: {}, ...parse12(input) };
  },
  parseYaml(input) {
    return JSON.parse(input);
  },
  layout(ir, theme) {
    return layoutGraph(ir, theme);
  }
};
function find(parent, x) {
  let r = x;
  for (let guard = 0; guard < parent.length && parent[r] !== r; guard++) r = parent[r];
  return r;
}
function buildUnionFind(input) {
  const linesArr = input.split(/\r?\n/).map((l) => l.trim()).filter(Boolean);
  let count = 0;
  let parent = [];
  let title;
  const unions = [];
  for (const line of linesArr) {
    const t = line.split(/\s+/);
    if (t[0] === "unionfind" || t[0] === "dsu") {
      const n = Number(t[1]);
      if (Number.isFinite(n) && n > 0) count = Math.floor(n);
      continue;
    }
    if (t[0] === "title") {
      title = line.slice(5).trim();
      continue;
    }
    if (t[0] === "parent") {
      const vals = t.slice(1).map(Number).filter(Number.isFinite);
      parent = vals;
      count = Math.max(count, vals.length);
      continue;
    }
    if (t[0] === "union") {
      const a = Number(t[1]);
      const b = Number(t[2]);
      if (Number.isFinite(a) && Number.isFinite(b)) {
        unions.push([a, b]);
        count = Math.max(count, a + 1, b + 1);
      }
      continue;
    }
  }
  const par = Array.from(
    { length: count },
    (_, i) => parent[i] !== void 0 ? parent[i] : i
  );
  for (const [a, b] of unions) {
    const ra = find(par, a);
    const rb = find(par, b);
    if (ra !== rb) par[ra] = rb;
  }
  const roots = [];
  for (let i = 0; i < count; i++) if (par[i] === i) roots.push(i);
  const nodes = [];
  for (let i = 0; i < count; i++) {
    const children = [];
    for (let j = 0; j < count; j++) if (j !== i && par[j] === i) children.push(`e${j}`);
    const isRoot = par[i] === i;
    nodes.push({
      id: `e${i}`,
      label: String(i),
      kinds: isRoot ? ["circle", "active"] : ["circle"],
      children
    });
  }
  return {
    version: "1.0",
    metadata: { ...title !== void 0 ? { title } : {} },
    direction: "TB",
    nodes,
    parent: par,
    roots,
    count
  };
}
function layoutUnionFind(ir, theme) {
  return layoutTree(ir, theme);
}
var unionfind = {
  parseMermaid: buildUnionFind,
  parseYaml: (input) => JSON.parse(input),
  layout: layoutUnionFind
};
function classifyCost(scale, weight) {
  for (const t of scale.tiers) if (weight <= t.maxWeight) return t;
  return scale.tiers[scale.tiers.length - 1];
}
function buildLegend(pen2, theme, scale, origin) {
  const palette = theme.palette;
  const pad = 14, rowH = 22, swatch = 16;
  const rows = scale.tiers.length + 1;
  const width = 220;
  const height = pad + rows * rowH;
  const elements = [];
  elements.push(
    pen2.rect({ x: origin.x, y: origin.y, width, height }, palette.background, palette.border, 1.5, {
      rx: 8
    })
  );
  const title = scale.unit ? `Cost (${scale.unit})` : "Cost";
  elements.push(
    pen2.text(
      title,
      origin.x + pad,
      origin.y + pad + 8,
      theme.typography.baseFontSize,
      palette.text,
      { weight: "bold" }
    )
  );
  scale.tiers.forEach((t, i) => {
    const y = origin.y + pad + (i + 1) * rowH;
    elements.push(
      pen2.rect(
        { x: origin.x + pad, y: y - swatch + 4, width: swatch, height: swatch - 4 },
        t.color,
        t.color,
        1,
        { rx: 2 }
      )
    );
    elements.push(
      pen2.text(
        t.name,
        origin.x + pad + swatch + 8,
        y,
        theme.typography.smallFontSize,
        palette.text
      )
    );
    elements.push(
      pen2.text(
        `\u2264 ${t.maxWeight}`,
        origin.x + width - pad,
        y,
        theme.typography.smallFontSize,
        palette.textMuted,
        { anchor: "end", weight: "bold" }
      )
    );
  });
  return { elements, bounds: { x: origin.x, y: origin.y, width, height } };
}
function parse13(input) {
  let title;
  let unit;
  let pattern;
  const tiers = [];
  const groups = [];
  const nodes = [];
  const edges2 = [];
  let curGroup;
  const lines2 = input.split(/\r?\n/);
  for (let rawLine of lines2) {
    const line = rawLine.trim();
    if (!line || line.startsWith("%%")) continue;
    const headerMatch = line.match(/^topology(?:\s*::\s*|\s+title\s+|\s+)(.+)$/i);
    if (headerMatch && !title && !line.startsWith("topology\n") && line !== "topology") {
      const rest = headerMatch[1].trim();
      if (!/^(costs|pattern|tier|group|subnet|zone|vlan|rack|node|router|switch|server|db|cloud|firewall)\b/i.test(rest)) {
        title = rest.replace(/^["']|["']$/g, "");
        continue;
      }
    }
    if (/^topology$/i.test(line)) continue;
    const titleMatch = line.match(/^title(?:\s*::\s*|\s+)(.+)$/i);
    if (titleMatch) {
      title = titleMatch[1].trim().replace(/^["']|["']$/g, "");
      continue;
    }
    const patternMatch = line.match(/^pattern\s+([\w-]+)/i);
    if (patternMatch) {
      pattern = patternMatch[1].toLowerCase();
      continue;
    }
    if (line.startsWith("costs")) {
      const t = line.split(/\s+/);
      unit = t[1];
      continue;
    }
    if (line.startsWith("tier")) {
      const t = line.split(/\s+/);
      const tier = {
        name: t[1] ?? "",
        maxWeight: Number(t[2]),
        color: t[3] ?? "#888",
        ...t[4] ? { dash: t.slice(4).join(" ") } : {}
      };
      tiers.push(tier);
      continue;
    }
    const groupMatch = line.match(/^(group|subnet|zone|vlan|rack)\s+(.+)$/i);
    if (groupMatch) {
      const gType = groupMatch[1].toLowerCase();
      const rest = groupMatch[2].trim();
      let id = "";
      let label = "";
      let cidr;
      if (gType === "group" && rest.includes(":") && !rest.includes('"')) {
        const parts = rest.split(":").map((s) => s.trim());
        id = parts[0];
        label = parts[1] || id;
      } else {
        const quoted = rest.matchAll(/"([^"]+)"|'([^']+)'|(\S+)/g);
        const tokens = [];
        for (const m of quoted) {
          tokens.push(m[1] ?? m[2] ?? m[3]);
        }
        if (tokens.length >= 2) {
          if (tokens[0].includes("/") || /^\d+\.\d+/.test(tokens[0])) {
            cidr = tokens[0];
            label = tokens[1];
            id = `sub_${groups.length + 1}`;
          } else {
            id = tokens[0];
            label = tokens[1];
          }
        } else if (tokens.length === 1) {
          label = tokens[0];
          id = `grp_${groups.length + 1}`;
        }
      }
      if (!id) id = `g_${groups.length + 1}`;
      if (!label) label = id;
      groups.push({
        id,
        label,
        type: gType,
        ...cidr ? { cidr } : {}
      });
      curGroup = id;
      continue;
    }
    if (/^end\b/i.test(line)) {
      curGroup = void 0;
      continue;
    }
    const deviceMatch = line.match(
      /^(node|router|gateway|switch|spine|leaf|tor|firewall|waf|loadbalancer|lb|server|host|compute|database|db|storage|cloud|internet|wan|client|device|cluster|pod)\s+(.+)$/i
    );
    if (deviceMatch) {
      const role = deviceMatch[1].toLowerCase();
      const rest = deviceMatch[2].trim();
      let id = "";
      let label = "";
      let sub;
      let ip;
      if (role === "node" && rest.includes(":") && !rest.includes('"')) {
        const parts = rest.split(":").map((s) => s.trim());
        id = parts[0];
        label = parts[1] || id;
        if (parts[2]) sub = parts[2];
      } else {
        const idM = rest.match(/^([\w-]+)/);
        if (idM) {
          id = idM[1];
          let remainder = rest.slice(id.length).trim();
          const bracketM = remainder.match(/\[(.*?)\]/);
          if (bracketM) {
            sub = bracketM[1].trim();
            remainder = remainder.replace(/\[.*?\]/, "").trim();
          }
          const labelM = remainder.match(/"([^"]+)"|'([^']+)'/);
          if (labelM) {
            label = labelM[1] ?? labelM[2];
          } else if (remainder) {
            label = remainder;
          } else {
            label = id;
          }
        }
      }
      if (!id) id = `n_${nodes.length + 1}`;
      if (!label) label = id;
      if (sub && (/^\d+\.\d+\.\d+\.\d+/.test(sub) || sub.includes("/"))) {
        ip = sub;
      }
      nodes.push({
        id,
        label,
        role: role === "node" ? void 0 : role,
        ...sub ? { sub } : {},
        ...ip ? { ip } : {},
        ...curGroup ? { group: curGroup } : {}
      });
      continue;
    }
    const edgeMatch = line.match(/^(\S+)\s*(-->|<-->|<--|--|\.\.|\.\.\.)\s*(.+)$/);
    if (edgeMatch) {
      const fromRaw = edgeMatch[1];
      const op = edgeMatch[2];
      let rest = edgeMatch[3].trim();
      const directed = op === "-->" || op === "->";
      const bidirectional = op === "<-->";
      let edgeLabel;
      const labelMatch = rest.match(/"([^"]+)"|'([^']+)'/);
      if (labelMatch) {
        edgeLabel = labelMatch[1] ?? labelMatch[2];
        rest = rest.replace(labelMatch[0], "").trim();
      }
      let cost;
      const costMatch = rest.match(/:\s*(-?\d+(?:\.\d+)?)/);
      if (costMatch) {
        cost = Number(costMatch[1]);
        rest = rest.replace(costMatch[0], "").trim();
      }
      let routeStyle;
      const routerMatch = rest.match(/@(orthogonal|straight|bezier|polyline)\b/i);
      if (routerMatch) {
        routeStyle = routerMatch[1].toLowerCase();
        rest = rest.replace(routerMatch[0], "").trim();
      }
      const toRawList = rest.split(/[,;]+/).map((s) => s.trim()).filter(Boolean);
      const fromParts = fromRaw.split(":");
      const fromId = fromParts[0];
      const fromPort = fromParts[1];
      for (const toItem of toRawList) {
        const toParts = toItem.split(":");
        const toId = toParts[0];
        const toPort = toParts[1];
        edges2.push({
          from: fromId,
          to: toId,
          ...fromPort ? { fromPort } : {},
          ...toPort ? { toPort } : {},
          ...edgeLabel ? { label: edgeLabel } : {},
          ...routeStyle ? { routeStyle } : {},
          ...directed ? { directed: true } : {},
          ...bidirectional ? { bidirectional: true } : {},
          ...cost !== void 0 ? { cost } : {}
        });
      }
    }
  }
  return {
    version: "1.0",
    metadata: title ? { title } : {},
    ...title ? { title } : {},
    ...pattern ? { pattern } : {},
    scale: { ...unit !== void 0 ? { unit } : {}, tiers },
    groups,
    nodes,
    edges: edges2
  };
}
var ARROW_ID14 = "topo-arrow";
var ARROW_START_ID4 = "topo-arrow-start";
function intersects(a, b, pad = 2) {
  return !(a.x + a.width < b.x - pad || a.x > b.x + b.width + pad || a.y + a.height < b.y - pad || a.y > b.y + b.height + pad);
}
function layoutTopology(doc, theme) {
  const { palette, typography, spacing, edges: edges2 } = theme;
  const p = pen(theme);
  const margin = spacing.diagramMargin;
  const font = typography.baseFontSize;
  const small = typography.smallFontSize;
  const titleH = doc.title ? typography.titleFontSize + 24 : 0;
  const nodeWidth = (n) => Math.max(
    130,
    Math.max(measureText(n.label, font).width, measureText(n.sub ?? "", small).width) + 40
  );
  const nodeH = doc.nodes.some((n) => n.sub || n.role) ? 58 : 42;
  const elements = [];
  const box = /* @__PURE__ */ new Map();
  const groupBox = /* @__PURE__ */ new Map();
  if (doc.title) {
    elements.push(
      p.text(
        doc.title,
        margin,
        margin + typography.titleFontSize,
        typography.titleFontSize + 2,
        palette.text,
        { weight: "bold" }
      )
    );
  }
  const nodeGroupMap = /* @__PURE__ */ new Map();
  doc.nodes.forEach((n) => {
    if (n.group) nodeGroupMap.set(n.id, n.group);
  });
  let maxInterGroupLabelW = 0;
  let maxIntraGroupLabelW = 0;
  let maxIntraGroupLabelH = 0;
  let hasIntraGroupLabels = false;
  let maxTierLabelH = 0;
  let maxSpokeLabelW = 0;
  for (const e of doc.edges) {
    const labelText = e.label ?? (e.cost !== void 0 ? doc.scale.unit ? `${e.cost} ${doc.scale.unit}` : String(e.cost) : void 0);
    if (!labelText) continue;
    const lw = measureText(labelText, small - 1).width + 16;
    const lh = 18;
    const gFrom = nodeGroupMap.get(e.from);
    const gTo = nodeGroupMap.get(e.to);
    if (gFrom && gTo && gFrom !== gTo) {
      maxInterGroupLabelW = Math.max(maxInterGroupLabelW, lw);
    } else if (gFrom && gTo && gFrom === gTo) {
      hasIntraGroupLabels = true;
      maxIntraGroupLabelW = Math.max(maxIntraGroupLabelW, lw);
      maxIntraGroupLabelH = Math.max(maxIntraGroupLabelH, lh);
    } else if (!gFrom && !gTo) {
      maxTierLabelH = Math.max(maxTierLabelH, lh);
      maxSpokeLabelW = Math.max(maxSpokeLabelW, lw);
      maxInterGroupLabelW = Math.max(maxInterGroupLabelW, lw);
    } else {
      maxInterGroupLabelW = Math.max(maxInterGroupLabelW, lw);
    }
  }
  if (doc.pattern === "spine-leaf") {
    const isSpine = (n) => n.role === "spine" || n.id.toLowerCase().startsWith("spine");
    const isLeaf = (n) => !isSpine(n) && (n.role === "leaf" || n.role === "tor" || n.id.toLowerCase().startsWith("leaf") || n.role === "switch");
    const isCompute = (n) => !isSpine(n) && !isLeaf(n);
    const spines = doc.nodes.filter(isSpine);
    const leaves = doc.nodes.filter(isLeaf);
    const compute = doc.nodes.filter(isCompute);
    const tiers = [spines, leaves, compute].filter((t) => t.length > 0);
    const startY = margin + titleH + 20;
    const tierGap = Math.max(130, maxTierLabelH + 110);
    const maxCount = Math.max(...tiers.map((t) => t.length), 1);
    const colSpacing = Math.max(180, maxInterGroupLabelW + 20);
    const totalContentW = maxCount * colSpacing;
    tiers.forEach((tierNodes, tIdx) => {
      const ty = startY + tIdx * (nodeH + tierGap);
      const tierW = tierNodes.length * colSpacing;
      const startX = margin + (totalContentW - tierW) / 2;
      tierNodes.forEach((n, i) => {
        const w = nodeWidth(n);
        const bx = startX + i * colSpacing + (colSpacing - w) / 2;
        box.set(n.id, { x: bx, y: ty, width: w, height: nodeH });
      });
    });
  } else if (doc.pattern === "hub-spoke" || doc.pattern === "star") {
    const hub = doc.nodes[0];
    const spokes = doc.nodes.slice(1);
    const hubW = nodeWidth(hub);
    const maxSpokeW = Math.max(...spokes.map(nodeWidth), 130);
    const radius = Math.max(240, hubW / 2 + maxSpokeW / 2 + maxSpokeLabelW + 60);
    const centerX = margin + radius + maxSpokeW / 2 + 20;
    const centerY = margin + titleH + radius + nodeH / 2 + 20;
    box.set(hub.id, { x: centerX - hubW / 2, y: centerY - nodeH / 2, width: hubW, height: nodeH });
    spokes.forEach((spoke, idx) => {
      const angle = idx / spokes.length * 2 * Math.PI - Math.PI / 2;
      const sx = centerX + radius * Math.cos(angle);
      const sy = centerY + radius * Math.sin(angle);
      const sw = nodeWidth(spoke);
      box.set(spoke.id, { x: sx - sw / 2, y: sy - nodeH / 2, width: sw, height: nodeH });
    });
  } else if (doc.pattern === "ring") {
    const count = doc.nodes.length;
    const maxNodeW = Math.max(...doc.nodes.map(nodeWidth), 130);
    const radius = Math.max(220, count * 40);
    const centerX = margin + radius + maxNodeW / 2 + 20;
    const centerY = margin + titleH + radius + nodeH / 2 + 20;
    doc.nodes.forEach((n, idx) => {
      const angle = idx / count * 2 * Math.PI - Math.PI / 2;
      const nx = centerX + radius * Math.cos(angle);
      const ny = centerY + radius * Math.sin(angle);
      const nw = nodeWidth(n);
      box.set(n.id, { x: nx - nw / 2, y: ny - nodeH / 2, width: nw, height: nodeH });
    });
  } else if (doc.groups.length > 0) {
    let gx = margin;
    const gy = margin + titleH + 10;
    let maxBottom2 = gy;
    const GHEADER = 36, GPAD = 24, CGAP_X = Math.max(28, maxIntraGroupLabelW > 0 ? 36 : 28), CGAP_Y = hasIntraGroupLabels ? 56 : 32, GROUP_GAP = Math.max(70, maxInterGroupLabelW + 60);
    for (const g of doc.groups) {
      const kids = doc.nodes.filter((n) => n.group === g.id);
      const childW = Math.max(140, ...kids.length > 0 ? kids.map(nodeWidth) : [140]);
      const cols = kids.length <= 4 ? 1 : 2;
      const rows = Math.max(1, Math.ceil(kids.length / cols));
      const innerW = cols * childW + (cols - 1) * CGAP_X;
      const innerH = rows * nodeH + (rows - 1) * CGAP_Y;
      const gw = Math.max(innerW + GPAD * 2, 230);
      const gh = GHEADER + GPAD + innerH + GPAD;
      groupBox.set(g.id, { x: gx, y: gy, width: gw, height: gh });
      kids.forEach((n, i) => {
        const col = i % cols;
        const row = Math.floor(i / cols);
        box.set(n.id, {
          x: gx + GPAD + col * (childW + CGAP_X),
          y: gy + GHEADER + GPAD + row * (nodeH + CGAP_Y),
          width: childW,
          height: nodeH
        });
      });
      maxBottom2 = Math.max(maxBottom2, gy + gh);
      gx += gw + GROUP_GAP;
    }
    let ux = margin;
    const uy = maxBottom2 + 40;
    for (const n of doc.nodes.filter((n2) => !n2.group)) {
      const w = nodeWidth(n);
      box.set(n.id, { x: ux, y: uy, width: w, height: nodeH });
      ux += w + 40;
    }
  } else {
    const nodeW = Math.max(140, ...doc.nodes.map(nodeWidth));
    const cols = Math.max(1, Math.ceil(Math.sqrt(doc.nodes.length)));
    const colGap = Math.max(100, maxInterGroupLabelW + 50);
    const rowGap = hasIntraGroupLabels ? 90 : 80;
    doc.nodes.forEach((n, i) => {
      const col = i % cols;
      const row = Math.floor(i / cols);
      box.set(n.id, {
        x: margin + col * (nodeW + colGap),
        y: margin + titleH + row * (nodeH + rowGap) + 10,
        width: nodeW,
        height: nodeH
      });
    });
  }
  const groupHeaderBoxes = [];
  for (const g of doc.groups) {
    const gb = groupBox.get(g.id);
    const isSubnet = g.type === "subnet" || Boolean(g.cidr);
    elements.push(
      p.rect(gb, palette.surface, isSubnet ? palette.primary : palette.border, 1.6, {
        rx: 10
      })
    );
    const tagText = g.cidr ? `${g.label} [${g.cidr}]` : g.label;
    elements.push(
      p.text(tagText, gb.x + 16, gb.y + 22, small, palette.primary, { weight: "bold" })
    );
    groupHeaderBoxes.push({ x: gb.x, y: gb.y, width: gb.width, height: 38 });
  }
  const idBox = new Map([...box, ...groupBox]);
  const placedLabels = [];
  const obstacles = [...box.values(), ...groupHeaderBoxes];
  doc.edges.forEach((e, edgeIdx) => {
    const a = idBox.get(e.from);
    const b = idBox.get(e.to);
    if (!a || !b) return;
    const tier = e.cost !== void 0 && doc.scale.tiers.length > 0 ? classifyCost(doc.scale, e.cost) : void 0;
    const color = tier?.color ?? palette.primary;
    const strokeWidth = 1.8;
    const acx = a.x + a.width / 2;
    const acy = a.y + a.height / 2;
    const bcx = b.x + b.width / 2;
    const bcy = b.y + b.height / 2;
    const dx = bcx - acx;
    const dy = bcy - acy;
    let start;
    let end;
    const isTopToBottom = dy > 30 && Math.abs(dy) > Math.abs(dx) * 0.4;
    const isBottomToTop = dy < -30 && Math.abs(dy) > Math.abs(dx) * 0.4;
    const isLeftToRight = dx > 30 && Math.abs(dx) >= Math.abs(dy) * 0.8;
    const isRightToLeft = dx < -30 && Math.abs(dx) >= Math.abs(dy) * 0.8;
    if (isTopToBottom) {
      start = { x: acx, y: a.y + a.height };
      end = { x: bcx, y: b.y };
    } else if (isBottomToTop) {
      start = { x: acx, y: a.y };
      end = { x: bcx, y: b.y + b.height };
    } else if (isLeftToRight) {
      start = { x: a.x + a.width, y: acy };
      end = { x: b.x, y: bcy };
    } else if (isRightToLeft) {
      start = { x: a.x, y: acy };
      end = { x: b.x + b.width, y: bcy };
    } else {
      start = borderPoint(a, bcx, bcy);
      end = borderPoint(b, acx, acy);
    }
    let pathD;
    let labelPos;
    if (e.routeStyle === "orthogonal") {
      if (isTopToBottom || isBottomToTop) {
        if (Math.abs(start.x - end.x) < 4) {
          pathD = `M ${rhu(start.x)} ${rhu(start.y)} L ${rhu(end.x)} ${rhu(end.y)}`;
          labelPos = { x: start.x, y: (start.y + end.y) / 2 };
        } else {
          const corridorFraction = 0.32 + edgeIdx % 4 * 0.12;
          const yMid = start.y + (end.y - start.y) * corridorFraction;
          pathD = `M ${rhu(start.x)} ${rhu(start.y)} L ${rhu(start.x)} ${rhu(yMid)} L ${rhu(end.x)} ${rhu(yMid)} L ${rhu(end.x)} ${rhu(end.y)}`;
          labelPos = { x: (start.x + end.x) / 2, y: yMid - 6 };
        }
      } else if (isLeftToRight || isRightToLeft) {
        if (Math.abs(start.y - end.y) < 4) {
          pathD = `M ${rhu(start.x)} ${rhu(start.y)} L ${rhu(end.x)} ${rhu(end.y)}`;
          labelPos = { x: (start.x + end.x) / 2, y: start.y - 6 };
        } else {
          const corridorFraction = 0.32 + edgeIdx % 4 * 0.12;
          const xMid = start.x + (end.x - start.x) * corridorFraction;
          pathD = `M ${rhu(start.x)} ${rhu(start.y)} L ${rhu(xMid)} ${rhu(start.y)} L ${rhu(xMid)} ${rhu(end.y)} L ${rhu(end.x)} ${rhu(end.y)}`;
          labelPos = { x: xMid, y: (start.y + end.y) / 2 };
        }
      } else {
        pathD = `M ${rhu(start.x)} ${rhu(start.y)} L ${rhu(end.x)} ${rhu(end.y)}`;
        labelPos = { x: (start.x + end.x) / 2, y: (start.y + end.y) / 2 - 6 };
      }
    } else if (e.routeStyle === "bezier") {
      if (Math.abs(dx) > Math.abs(dy)) {
        const cx1 = start.x + dx * 0.45;
        const cx2 = end.x - dx * 0.45;
        pathD = `M ${rhu(start.x)} ${rhu(start.y)} C ${rhu(cx1)} ${rhu(start.y)}, ${rhu(cx2)} ${rhu(end.y)}, ${rhu(end.x)} ${rhu(end.y)}`;
      } else {
        const cy1 = start.y + dy * 0.45;
        const cy2 = end.y - dy * 0.45;
        pathD = `M ${rhu(start.x)} ${rhu(start.y)} C ${rhu(start.x)} ${rhu(cy1)}, ${rhu(end.x)} ${rhu(cy2)}, ${rhu(end.x)} ${rhu(end.y)}`;
      }
      labelPos = { x: (start.x + end.x) / 2, y: (start.y + end.y) / 2 - 8 };
    } else {
      pathD = `M ${rhu(start.x)} ${rhu(start.y)} L ${rhu(end.x)} ${rhu(end.y)}`;
      if (Math.abs(start.x - end.x) < 4) {
        labelPos = { x: start.x, y: (start.y + end.y) / 2 };
      } else if (Math.abs(start.y - end.y) < 4) {
        labelPos = { x: (start.x + end.x) / 2, y: start.y - 8 };
      } else {
        const t = edgeIdx % 2 === 0 ? 0.3 : 0.7;
        const lx = start.x + (end.x - start.x) * t;
        const ly = start.y + (end.y - start.y) * t;
        const len = Math.hypot(dx, dy) || 1;
        const nx = -dy / len;
        const ny = dx / len;
        labelPos = { x: lx + nx * 10, y: ly + ny * 10 };
      }
    }
    elements.push(
      p.path(pathD, color, strokeWidth, {
        ...tier?.dash ? { dash: tier.dash } : {},
        ...e.directed ? { markerEnd: ARROW_ID14 } : {},
        ...e.bidirectional ? { markerStart: ARROW_START_ID4, markerEnd: ARROW_ID14 } : {}
      })
    );
    if (e.fromPort) {
      const offsetX = start.x <= a.x + 2 ? -6 : start.x >= a.x + a.width - 2 ? 6 : 0;
      const offsetY = start.y <= a.y + 2 ? -6 : start.y >= a.y + a.height - 2 ? 14 : -6;
      elements.push(
        p.text(e.fromPort, start.x + offsetX, start.y + offsetY, small - 2, palette.textMuted, {
          anchor: offsetX < 0 ? "end" : offsetX > 0 ? "start" : "middle"
        })
      );
    }
    if (e.toPort) {
      const offsetX = end.x <= b.x + 2 ? -6 : end.x >= b.x + b.width - 2 ? 6 : 0;
      const offsetY = end.y <= b.y + 2 ? -6 : end.y >= b.y + b.height - 2 ? 14 : -6;
      elements.push(
        p.text(e.toPort, end.x + offsetX, end.y + offsetY, small - 2, palette.textMuted, {
          anchor: offsetX < 0 ? "end" : offsetX > 0 ? "start" : "middle"
        })
      );
    }
    const labelText = e.label ?? (e.cost !== void 0 ? doc.scale.unit ? `${e.cost} ${doc.scale.unit}` : String(e.cost) : void 0);
    if (labelText) {
      const textDim = measureText(labelText, small - 1);
      const lw = textDim.width + 10;
      const lh = 16;
      let bestX = labelPos.x - lw / 2;
      let bestY = labelPos.y - lh / 2;
      const allObstacles = [...obstacles, ...placedLabels];
      let hasCollision = allObstacles.some(
        (obs) => intersects({ x: bestX, y: bestY, width: lw, height: lh }, obs, 2)
      );
      if (hasCollision) {
        const sampleT = [0.5, 0.4, 0.6, 0.3, 0.7, 0.2, 0.8];
        let found = false;
        for (const t of sampleT) {
          const cx = start.x + (end.x - start.x) * t;
          const cy = start.y + (end.y - start.y) * t;
          const testX = cx - lw / 2;
          const testY = cy - lh / 2;
          if (!allObstacles.some((obs) => intersects({ x: testX, y: testY, width: lw, height: lh }, obs, 2))) {
            bestX = testX;
            bestY = testY;
            found = true;
            break;
          }
        }
        if (!found) {
          for (let step = 1; step <= 8; step++) {
            const offsetY = (step % 2 === 0 ? 1 : -1) * Math.ceil(step / 2) * (lh + 4);
            const testY = bestY + offsetY;
            if (!allObstacles.some((obs) => intersects({ x: bestX, y: testY, width: lw, height: lh }, obs, 2))) {
              bestY = testY;
              found = true;
              break;
            }
          }
        }
      }
      const finalRect = { x: bestX, y: bestY, width: lw, height: lh };
      placedLabels.push(finalRect);
      elements.push(
        p.rect(
          { x: rhu(bestX), y: rhu(bestY), width: rhu(lw), height: lh },
          palette.background,
          palette.border,
          0.8,
          { rx: 3 }
        )
      );
      elements.push(
        p.text(
          labelText,
          rhu(bestX + lw / 2),
          rhu(bestY + lh / 2 + (small - 1) * 0.35),
          small - 1,
          color,
          {
            anchor: "middle",
            weight: "bold"
          }
        )
      );
    }
  });
  const anchors = {};
  for (const n of doc.nodes) {
    const b = box.get(n.id);
    elements.push(p.rect(b, palette.surface, palette.primary, 1.8, { rx: 6 }));
    if (n.role) {
      const roleText = n.role.toUpperCase();
      const rw = measureText(roleText, small - 2).width + 8;
      const rh = 13;
      elements.push(
        p.rect(
          { x: b.x + 6, y: b.y - rh / 2, width: rw, height: rh },
          palette.background,
          palette.primary,
          1,
          { rx: 3 }
        )
      );
      elements.push(
        p.text(roleText, b.x + 6 + rw / 2, b.y + (small - 2) * 0.35, small - 2, palette.primary, {
          anchor: "middle",
          weight: "bold"
        })
      );
    }
    const textY = n.sub ? b.y + 24 : b.y + b.height / 2 + font * 0.35;
    elements.push(
      p.text(n.label, rhu(b.x + b.width / 2), rhu(textY), font, palette.text, {
        anchor: "middle",
        weight: "bold"
      })
    );
    if (n.sub) {
      elements.push(
        p.text(n.sub, rhu(b.x + b.width / 2), rhu(b.y + 44), small - 1, palette.textMuted, {
          anchor: "middle"
        })
      );
    }
    anchors[n.id] = { bounds: b };
    anchors[`node.${n.id}`] = { bounds: b };
    if (n.role) anchors[`${n.role}.${n.id}`] = { bounds: b };
    if (n.group) anchors[`${n.group}.${n.id}`] = { bounds: b };
  }
  for (const g of doc.groups) {
    const gb = groupBox.get(g.id);
    anchors[g.id] = { bounds: gb };
    anchors[`zone_${g.id}`] = { bounds: gb };
    anchors[`subnet_${g.id}`] = { bounds: gb };
  }
  const allBoxes = [...box.values(), ...groupBox.values(), ...placedLabels];
  const boxesRight = Math.max(margin, ...allBoxes.map((b) => b.x + b.width));
  const contentBottom = Math.max(margin + titleH, ...allBoxes.map((b) => b.y + b.height));
  let contentRight = boxesRight;
  if (doc.scale.tiers.length > 0) {
    const legend = buildLegend(p, theme, doc.scale, { x: boxesRight + 36, y: margin + titleH });
    elements.push(...legend.elements);
    contentRight = legend.bounds.x + legend.bounds.width;
  }
  const s = edges2?.arrowSize ?? 8;
  const sH = rhu(s * 0.7);
  const sMidY = rhu(s * 0.35);
  const sRefX = rhu(s - 1);
  const defs = [
    `<marker id="${ARROW_ID14}" markerWidth="${s}" markerHeight="${sH}" refX="${sRefX}" refY="${sMidY}" orient="auto" markerUnits="userSpaceOnUse"><polygon points="0 0, ${s} ${sMidY}, 0 ${sH}" fill="${palette.primary}" /></marker>`,
    `<marker id="${ARROW_START_ID4}" markerWidth="${s}" markerHeight="${sH}" refX="1" refY="${sMidY}" orient="auto-start-reverse" markerUnits="userSpaceOnUse"><polygon points="0 0, ${s} ${sMidY}, 0 ${sH}" fill="${palette.primary}" /></marker>`
  ];
  const scene = applyOverlays(
    {
      viewBox: { x: 0, y: 0, width: rhuInt(contentRight + margin), height: rhuInt(contentBottom + margin) },
      background: palette.background,
      elements,
      defs
    },
    doc.metadata?.["overlays"],
    theme
  );
  return { scene, anchors };
}
var topology = {
  parseMermaid(input) {
    return parse13(input);
  },
  parseYaml(input) {
    return JSON.parse(input);
  },
  layout(ir, theme) {
    return layoutTopology(ir, theme);
  }
};
var REVEAL_EFFECTS = ["fade", "slide", "grow", "draw"];
var LIST_STYLES = [
  "bullets",
  "numbered",
  "block",
  "box",
  "tree",
  "chevron",
  "process",
  "timeline",
  "pyramid",
  "columns",
  "cycle",
  "matrix",
  "funnel",
  "stepup",
  "venn"
];
var REVEAL_MODES = ["sequence", "subtree", "layer", "none"];
var PROCESS_FLOWS = ["ltr", "ttb", "snake", "snake-v"];
function asFlow(token) {
  const t = token.toLowerCase();
  return PROCESS_FLOWS.includes(t) ? t : void 0;
}
var TURN_STYLES = ["corridor", "direct"];
function asTurn(token) {
  const t = token.toLowerCase();
  return TURN_STYLES.includes(t) ? t : void 0;
}
function cellForIndex(i, flow, wrap) {
  switch (flow) {
    case "ltr":
      return { row: 0, col: i };
    case "ttb":
      return { row: i, col: 0 };
    case "snake": {
      const row = Math.floor(i / wrap);
      const pos = i % wrap;
      const col = row % 2 === 0 ? pos : wrap - 1 - pos;
      return { row, col };
    }
    case "snake-v": {
      const col = Math.floor(i / wrap);
      const pos = i % wrap;
      const row = col % 2 === 0 ? pos : wrap - 1 - pos;
      return { row, col };
    }
  }
}
function asEffect(token) {
  const t = token.toLowerCase();
  return REVEAL_EFFECTS.includes(t) ? t : void 0;
}
function asStyle(token) {
  const t = token.toLowerCase();
  return LIST_STYLES.includes(t) ? t : void 0;
}
function asMode(token) {
  const t = token.toLowerCase();
  if (t === "none" || t === "all" || t === "off" || t === "static") return "none";
  return REVEAL_MODES.includes(t) ? t : void 0;
}
function indentWidth(line) {
  const lead = line.match(/^[ \t]*/)?.[0] ?? "";
  return lead.replace(/\t/g, "  ").length;
}
function sourceLines(input) {
  const body = input.replace(/^\s*---\r?\n[\s\S]*?\r?\n---\r?\n?/, "");
  const lines2 = body.split(/\r?\n/);
  const out = [];
  for (let i = 0; i < lines2.length; i++) {
    let line = lines2[i];
    if (line.trim().length === 0) continue;
    while ((line.trimEnd().endsWith("\\") || /(?:<br\s*\/?>)\s*$/i.test(line)) && i + 1 < lines2.length) {
      if (line.trimEnd().endsWith("\\")) {
        line = line.trimEnd().slice(0, -1).trimEnd() + "\n" + lines2[i + 1].trim();
      } else {
        line = line + "\n" + lines2[i + 1].trim();
      }
      i++;
    }
    while (i + 1 < lines2.length && /^\s*\|\s*/.test(lines2[i + 1])) {
      const nextClean = lines2[i + 1].replace(/^\s*\|\s*/, "");
      line = line + "\n" + nextClean;
      i++;
    }
    if (/^\s*"[^"]*$/.test(line)) {
      while (i + 1 < lines2.length) {
        i++;
        line = line + "\n" + lines2[i];
        if (lines2[i].includes('"')) break;
      }
      line = line.replace(/^\s*"|"\s*$/g, "");
    }
    out.push(line);
  }
  return out;
}
function frontmatterMeta(input) {
  const m = input.match(/^\s*---\r?\n([\s\S]*?)\r?\n---\r?\n?/);
  if (!m) return {};
  const meta = {};
  for (const line of (m[1] ?? "").split(/\r?\n/)) {
    const idx = line.indexOf(":");
    if (idx === -1) continue;
    const key = line.slice(0, idx).trim();
    const value = line.slice(idx + 1).trim().replace(/^["']|["']$/g, "");
    if (key) meta[key] = value;
  }
  return meta;
}
function parseList(input) {
  let title;
  let style = "bullets";
  let reveal = "sequence";
  let effect;
  let group;
  let flow = "ltr";
  let wrap;
  let turn = "corridor";
  const raw = [];
  for (const line of sourceLines(input)) {
    const trimmed = line.trim();
    const lower = trimmed.toLowerCase();
    if (lower === "list") continue;
    if (lower.startsWith("title ") && !trimmed.includes("::") && raw.length === 0) {
      title = trimmed.slice(6).trim();
      continue;
    }
    if (lower.startsWith("style ") && !trimmed.includes("::") && raw.length === 0) {
      style = asStyle(trimmed.slice(6).trim()) ?? style;
      continue;
    }
    if (lower.startsWith("reveal ")) {
      reveal = asMode(trimmed.slice(7).trim()) ?? reveal;
      continue;
    }
    if (lower.startsWith("effect ")) {
      effect = asEffect(trimmed.slice(7).trim()) ?? effect;
      continue;
    }
    if (lower.startsWith("group ")) {
      const num = parseInt(trimmed.slice(6).trim(), 10);
      if (Number.isFinite(num) && num >= 1) group = num;
      continue;
    }
    if (lower.startsWith("flow ")) {
      flow = asFlow(trimmed.slice(5).trim()) ?? flow;
      continue;
    }
    if (lower.startsWith("wrap ")) {
      const num = parseInt(trimmed.slice(5).trim(), 10);
      if (Number.isFinite(num) && num >= 1) wrap = num;
      continue;
    }
    if (lower.startsWith("turn ")) {
      turn = asTurn(trimmed.slice(5).trim()) ?? turn;
      continue;
    }
    const indent = indentWidth(line);
    let text = trimmed;
    const join = /^\+\s+/.test(text);
    if (join) text = text.replace(/^\+\s+/, "");
    text = text.replace(/^[-*]\s+/, "");
    let itemEffect;
    const m = text.match(/\s+@(\w+)\s*$/);
    if (m) {
      const e = asEffect(m[1] ?? "");
      if (e) {
        itemEffect = e;
        text = text.slice(0, m.index ?? 0).trimEnd();
      }
    }
    raw.push({ indent, text, join, ...itemEffect ? { effect: itemEffect } : {} });
  }
  const items = assignTree(raw);
  return {
    ...title !== void 0 ? { title } : {},
    style,
    reveal,
    ...effect !== void 0 ? { effect } : {},
    ...group !== void 0 ? { group } : {},
    ...flow !== "ltr" ? { flow } : {},
    ...wrap !== void 0 ? { wrap } : {},
    ...turn !== "corridor" ? { turn } : {},
    items,
    version: "1.0",
    metadata: frontmatterMeta(input)
  };
}
function assignTree(raw) {
  const stack2 = [];
  const path = [];
  const items = [];
  for (const r of raw) {
    while (stack2.length > 0 && r.indent < stack2[stack2.length - 1]) stack2.pop();
    if (stack2.length === 0 || r.indent > stack2[stack2.length - 1]) {
      stack2.push(r.indent);
    }
    const depth = stack2.length - 1;
    if (path.length === depth) {
      path.push(0);
    } else {
      path.length = depth + 1;
      path[depth] = (path[depth] ?? -1) + 1;
    }
    items.push({
      text: r.text,
      depth,
      id: `item-${path.join("-")}`,
      numberLabel: path.map((num) => num + 1).join("."),
      join: r.join,
      ...r.effect ? { effect: r.effect } : {}
    });
  }
  return items;
}
function buildSteps(doc) {
  const out = [];
  if (doc.reveal === "subtree") {
    let cur = null;
    for (const it of doc.items) {
      if (it.depth === 0 || cur === null) {
        cur = { enter: [it.id], effect: it.effect ?? doc.effect ?? "fade", label: it.text };
        out.push(cur);
      } else {
        cur.enter.push(it.id);
      }
    }
  } else if (doc.reveal === "layer") {
    const byDepth = /* @__PURE__ */ new Map();
    for (const it of doc.items) {
      const arr = byDepth.get(it.depth) ?? [];
      arr.push(it.id);
      byDepth.set(it.depth, arr);
    }
    for (const depth of [...byDepth.keys()].sort((a, b) => a - b)) {
      out.push({
        enter: byDepth.get(depth),
        effect: doc.effect ?? "fade",
        label: `Level ${depth + 1}`
      });
    }
  } else {
    const chunk = doc.group && doc.group >= 1 ? doc.group : 1;
    let inStep = 0;
    for (const it of doc.items) {
      const startNew = out.length === 0 ? true : it.join ? false : inStep >= chunk;
      if (startNew) {
        out.push({ enter: [it.id], effect: it.effect ?? doc.effect ?? "fade", label: it.text });
        inStep = 1;
      } else {
        out[out.length - 1].enter.push(it.id);
        inStep++;
      }
    }
  }
  return out.map((s, i) => ({ index: i + 1, enter: s.enter, effect: s.effect, label: s.label }));
}
function markerColor(depth, palette) {
  return depth === 0 ? palette.primary : depth === 1 ? palette.secondary : palette.textMuted;
}
function markerRadius(depth, font) {
  return depth === 0 ? Math.max(3, rhu(font / 6)) : depth === 1 ? Math.max(2.5, rhu(font / 7)) : Math.max(2, rhu(font / 8));
}
function arrowTriangle(tx, ty, dir, al, aw = Math.max(4, rhu(al * 0.55))) {
  switch (dir) {
    case "right":
      return `M ${rhu(tx - al)} ${rhu(ty - aw)} L ${tx} ${ty} L ${rhu(tx - al)} ${rhu(ty + aw)} Z`;
    case "left":
      return `M ${rhu(tx + al)} ${rhu(ty - aw)} L ${tx} ${ty} L ${rhu(tx + al)} ${rhu(ty + aw)} Z`;
    case "down":
      return `M ${rhu(tx - aw)} ${rhu(ty - al)} L ${tx} ${ty} L ${rhu(tx + aw)} ${rhu(ty - al)} Z`;
    case "up":
      return `M ${rhu(tx - aw)} ${rhu(ty + al)} L ${tx} ${ty} L ${rhu(tx + aw)} ${rhu(ty + al)} Z`;
  }
}
function getItemLineCount(info) {
  if (info.titleLines !== void 0) {
    return info.titleLines.length + (info.subtitleLines?.length ?? 0);
  }
  return info.lines.length;
}
function measureItemLines(rawText, font, smallFont, maxAutoWrapWidth = 260) {
  const sepIdx = rawText.indexOf("::");
  if (sepIdx !== -1) {
    const rawTitle = rawText.slice(0, sepIdx).trim();
    const rawSubtitle = rawText.slice(sepIdx + 2).trim();
    const titleNorm = rawTitle.replace(/<br\s*\/?>/gi, "\n").replace(/\\n/g, "\n");
    const subNorm = rawSubtitle.replace(/<br\s*\/?>/gi, "\n").replace(/\\n/g, "\n");
    const titleLines = titleNorm.split(/\r?\n/).map((l) => l.trim()).filter(Boolean);
    const subtitleLines = subNorm.split(/\r?\n/).map((l) => l.trim()).filter(Boolean);
    let titleMaxW = 0;
    for (const t of titleLines) {
      titleMaxW = Math.max(titleMaxW, measureText(t, font).width * 1.05);
    }
    let subMaxW = 0;
    for (const s of subtitleLines) {
      subMaxW = Math.max(subMaxW, measureText(s, smallFont).width);
    }
    const firstTitle = titleLines[0];
    const firstSub = subtitleLines.length > 0 ? subtitleLines.join(" ") : void 0;
    return {
      title: firstTitle,
      subtitle: firstSub,
      titleLines: titleLines.length > 0 ? titleLines : void 0,
      subtitleLines: subtitleLines.length > 0 ? subtitleLines : void 0,
      lines: [...titleLines, ...subtitleLines],
      maxLineWidth: Math.max(titleMaxW, subMaxW)
    };
  }
  const normalized = rawText.replace(/<br\s*\/?>/gi, "\n").replace(/\\n/g, "\n");
  const rawLines = normalized.split(/\r?\n/).map((l) => l.trim()).filter(Boolean);
  const lines2 = [];
  for (const r of rawLines) {
    if (measureText(r, font).width > maxAutoWrapWidth) {
      const wrapped = wrapText2(r, font, maxAutoWrapWidth, 6);
      lines2.push(...wrapped.lines);
    } else {
      lines2.push(r);
    }
  }
  const finalLines = lines2.length > 0 ? lines2 : [rawText];
  const maxLineWidth = finalLines.reduce((m, l) => Math.max(m, measureText(l, font).width), 0);
  return {
    lines: finalLines,
    maxLineWidth
  };
}
function renderItemText(p, itemInfo, x, y, w, h2, font, smallFont, textColor, mutedColor, align = "start", paddingLeft = 0, defaultBold = false) {
  const elements = [];
  if (itemInfo.titleLines !== void 0) {
    const titleLines = itemInfo.titleLines;
    const subLines = itemInfo.subtitleLines ?? [];
    const titleLH = font * 1.25;
    const subLH = smallFont * 1.25;
    const totalH = titleLines.length * titleLH + subLines.length * subLH;
    const startY2 = y + (h2 - totalH) / 2 + font * 0.85;
    const titleX = align === "middle" ? rhu(x + w / 2) : rhu(x + paddingLeft);
    const subX = titleX;
    let curY = startY2;
    for (let ti = 0; ti < titleLines.length; ti++) {
      elements.push(
        p.text(titleLines[ti], titleX, rhu(curY), font, textColor, {
          weight: "bold",
          anchor: align
        })
      );
      curY += titleLH;
    }
    for (let si = 0; si < subLines.length; si++) {
      elements.push(
        p.text(subLines[si], subX, rhu(curY), smallFont, mutedColor, { anchor: align })
      );
      curY += subLH;
    }
    return elements;
  }
  const lines2 = itemInfo.lines;
  const count = lines2.length;
  const lineH = font * 1.25;
  if (count === 1) {
    const textY = rhu(y + h2 / 2 + font * 0.34);
    const textX2 = align === "middle" ? rhu(x + w / 2) : rhu(x + paddingLeft);
    elements.push(
      p.text(lines2[0], textX2, textY, font, textColor, {
        ...defaultBold ? { weight: "bold" } : {},
        anchor: align
      })
    );
    return elements;
  }
  const startY = y + (h2 - (count - 1) * lineH) / 2 + font * 0.34;
  const textX = align === "middle" ? rhu(x + w / 2) : rhu(x + paddingLeft);
  for (let li = 0; li < count; li++) {
    const lineY = rhu(startY + li * lineH);
    elements.push(
      p.text(lines2[li], textX, lineY, font, textColor, {
        ...defaultBold ? { weight: "bold" } : {},
        anchor: align
      })
    );
  }
  return elements;
}
function layoutList2(doc, theme) {
  const { palette, typography, spacing } = theme;
  const p = pen(theme);
  const margin = spacing.diagramMargin;
  const font = typography.baseFontSize;
  const smallFont = typography.smallFontSize;
  const indentPx = rhu(font * 1.6);
  const titleH = doc.title ? typography.titleFontSize + 16 : 0;
  const top = margin + titleH;
  const n = Math.max(doc.items.length, 1);
  const elements = [];
  const anchors = {};
  let contentRight = 0;
  let height;
  if (doc.style === "block" || doc.style === "box") {
    const pad = rhu(font * 0.8);
    const gap = rhu(font * 0.6);
    const barW = doc.style === "box" ? Math.max(3, rhu(font * 0.35)) : 0;
    const itemInfos = doc.items.map((it) => measureItemLines(it.text, font, smallFont));
    const itemHeights = itemInfos.map((info) => {
      const lineCount = getItemLineCount(info);
      return lineCount > 1 ? rhu(lineCount * (font * 1.25) + 2 * pad) : rhu(font * 2.2);
    });
    let maxRight2 = 0;
    doc.items.forEach((it, i) => {
      const x = margin + it.depth * indentPx;
      const tw = itemInfos[i].maxLineWidth;
      maxRight2 = Math.max(maxRight2, x + barW + pad + tw + pad);
    });
    contentRight = maxRight2;
    let curY = top;
    doc.items.forEach((it, i) => {
      const y = curY;
      const itemH = itemHeights[i];
      const x = margin + it.depth * indentPx;
      const w = rhu(contentRight - x);
      const info = itemInfos[i];
      const children = [
        p.rect({ x, y, width: w, height: itemH }, palette.surface, palette.border, 1, {
          rx: doc.style === "block" ? 6 : 4
        })
      ];
      if (doc.style === "box") {
        const c = markerColor(it.depth, palette);
        children.push(p.rect({ x, y, width: barW, height: itemH }, c, c, 0, { rx: 2 }));
      }
      children.push(
        ...renderItemText(
          p,
          info,
          x,
          y,
          w,
          itemH,
          font,
          smallFont,
          palette.text,
          palette.textMuted,
          "start",
          barW + pad
        )
      );
      elements.push(p.group(children, { id: it.id }));
      anchors[it.id] = { bounds: { x, y, width: w, height: itemH } };
      curY += itemH + gap;
    });
    height = rhu(curY - gap + margin);
  } else if (doc.style === "tree") {
    const pad = rhu(font * 0.8);
    const hGap = rhu(font * 1.2);
    const vGap = rhu(font * 1.8);
    const itemInfos = doc.items.map((it) => measureItemLines(it.text, font, smallFont));
    let maxTextW = 0;
    let maxLines = 1;
    itemInfos.forEach((info) => {
      maxTextW = Math.max(maxTextW, info.maxLineWidth);
      const lc = getItemLineCount(info);
      maxLines = Math.max(maxLines, lc);
    });
    const nodeH = maxLines > 1 ? rhu(maxLines * (font * 1.25) + 2 * pad) : rhu(font * 2.2);
    const nodeW = rhu(maxTextW + 2 * pad);
    const colStep = nodeW + hGap;
    const parentOf = new Array(doc.items.length).fill(-1);
    const childrenOf = /* @__PURE__ */ new Map();
    const ancestor = [];
    doc.items.forEach((it, i) => {
      ancestor.length = it.depth;
      const parent = it.depth === 0 ? -1 : ancestor[it.depth - 1];
      parentOf[i] = parent;
      const arr = childrenOf.get(parent) ?? [];
      arr.push(i);
      childrenOf.set(parent, arr);
      ancestor[it.depth] = i;
    });
    const col = new Array(doc.items.length).fill(0);
    let nextCol = 0;
    const assign = (i) => {
      const kids = childrenOf.get(i) ?? [];
      if (kids.length === 0) {
        col[i] = nextCol;
        nextCol += 1;
        return;
      }
      kids.forEach(assign);
      col[i] = (col[kids[0]] + col[kids[kids.length - 1]]) / 2;
    };
    (childrenOf.get(-1) ?? []).forEach(assign);
    const xOf = (i) => rhu(margin + col[i] * colStep);
    const yOf = (i) => rhu(top + doc.items[i].depth * (nodeH + vGap));
    let maxDepth2 = 0;
    doc.items.forEach((it, i) => {
      const x = xOf(i);
      const y = yOf(i);
      const cx = rhu(x + nodeW / 2);
      contentRight = Math.max(contentRight, x + nodeW);
      maxDepth2 = Math.max(maxDepth2, it.depth);
      const children = [];
      if (parentOf[i] >= 0) {
        const pi = parentOf[i];
        const px = rhu(xOf(pi) + nodeW / 2);
        const py = rhu(yOf(pi) + nodeH);
        const midY = rhu((py + y) / 2);
        const d = `M ${px} ${py} L ${px} ${midY} L ${cx} ${midY} L ${cx} ${y}`;
        children.push(p.path(d, palette.border, 1.5));
      }
      const stroke = it.depth === 0 ? palette.primary : palette.border;
      children.push(
        p.rect(
          { x, y, width: nodeW, height: nodeH },
          palette.surface,
          stroke,
          it.depth === 0 ? 2 : 1,
          { rx: 6 }
        )
      );
      children.push(
        ...renderItemText(
          p,
          itemInfos[i],
          x,
          y,
          nodeW,
          nodeH,
          font,
          smallFont,
          palette.text,
          palette.textMuted,
          "middle"
        )
      );
      elements.push(p.group(children, { id: it.id }));
      anchors[it.id] = { bounds: { x, y, width: nodeW, height: nodeH } };
    });
    height = rhu(top + maxDepth2 * (nodeH + vGap) + nodeH + margin);
  } else if (doc.style === "chevron") {
    const padX = rhu(font * 1);
    const itemInfos = doc.items.map((it) => measureItemLines(it.text, font, smallFont));
    let maxTextW = 0;
    let maxLines = 1;
    itemInfos.forEach((info) => {
      maxTextW = Math.max(maxTextW, info.maxLineWidth);
      const lc = getItemLineCount(info);
      maxLines = Math.max(maxLines, lc);
    });
    const boxH = maxLines > 1 ? rhu(maxLines * (font * 1.3) + font * 1.4) : rhu(font * 2.8);
    const notch = rhu(boxH * 0.3);
    const boxW = rhu(maxTextW + 2 * padX + 2 * notch);
    const stepX = rhu(boxW - notch);
    const y = top;
    const cy = rhu(y + boxH / 2);
    const yb = rhu(y + boxH);
    doc.items.forEach((it, i) => {
      const x = rhu(margin + i * stepX);
      const fill = i % 2 === 0 ? palette.primary : palette.secondary;
      const k = notch;
      const tipR = rhu(x + boxW);
      const innerR = rhu(x + boxW - k);
      const d = i === 0 ? `M ${x} ${y} L ${innerR} ${y} L ${tipR} ${cy} L ${innerR} ${yb} L ${x} ${yb} Z` : `M ${x} ${y} L ${innerR} ${y} L ${tipR} ${cy} L ${innerR} ${yb} L ${x} ${yb} L ${rhu(x + k)} ${cy} Z`;
      const txtColor = readableText(fill, theme);
      const mutedTxtColor = txtColor === "#ffffff" ? "rgba(255, 255, 255, 0.8)" : palette.textMuted;
      const children = [
        p.path(d, fill, 0, { fill }),
        ...renderItemText(
          p,
          itemInfos[i],
          rhu(x + k / 2),
          y,
          rhu(boxW - k),
          boxH,
          font,
          smallFont,
          txtColor,
          mutedTxtColor,
          "middle"
        )
      ];
      elements.push(p.group(children, { id: it.id }));
      anchors[it.id] = { bounds: { x, y, width: boxW, height: boxH } };
      contentRight = Math.max(contentRight, x + boxW);
    });
    height = rhu(top + boxH + margin);
  } else if (doc.style === "process") {
    const flow = doc.flow ?? "ltr";
    const padX = rhu(font * 1);
    const arrowGap = rhu(font * 1.8);
    const al = Math.max(8, rhu(font * 0.65));
    const aw = Math.max(4.5, rhu(font * 0.35));
    const itemInfos = doc.items.map((it) => measureItemLines(it.text, font, smallFont));
    let maxTextW = 0;
    let maxLines = 1;
    itemInfos.forEach((info) => {
      maxTextW = Math.max(maxTextW, info.maxLineWidth);
      const lc = getItemLineCount(info);
      maxLines = Math.max(maxLines, lc);
    });
    const boxH = maxLines > 1 ? rhu(maxLines * (font * 1.3) + font * 1.4) : rhu(font * 2.8);
    const boxW = rhu(maxTextW + 2 * padX);
    if (flow === "ltr") {
      const stepX = rhu(boxW + arrowGap);
      const y = top;
      const cy = rhu(y + boxH / 2);
      doc.items.forEach((it, i) => {
        const x = rhu(margin + i * stepX);
        const children = [];
        if (i > 0) {
          const ax1 = rhu(x);
          const ax0 = rhu(x - arrowGap);
          children.push(p.path(`M ${ax0} ${cy} L ${rhu(ax1 - al)} ${cy}`, palette.textMuted, 2));
          children.push(
            p.path(arrowTriangle(ax1, cy, "right", al, aw), palette.textMuted, 0, {
              fill: palette.textMuted
            })
          );
        }
        children.push(
          p.rect({ x, y, width: boxW, height: boxH }, palette.surface, palette.primary, 1.5, {
            rx: 6
          })
        );
        children.push(
          ...renderItemText(
            p,
            itemInfos[i],
            x,
            y,
            boxW,
            boxH,
            font,
            smallFont,
            palette.text,
            palette.textMuted,
            "middle"
          )
        );
        elements.push(p.group(children, { id: it.id }));
        anchors[it.id] = { bounds: { x, y, width: boxW, height: boxH } };
        contentRight = Math.max(contentRight, x + boxW);
      });
      height = rhu(top + boxH + margin);
    } else {
      const wrap = doc.wrap ?? Math.ceil(Math.sqrt(n));
      const turn = doc.turn ?? "corridor";
      const elbow = rhu(arrowGap * 0.75);
      const hasTopTurn = flow === "snake-v" && turn === "corridor" && n > 2 * wrap;
      const hasBottomTurn = flow === "snake-v" && turn === "corridor" && n > wrap;
      const topExtra = hasTopTurn ? rhu(elbow + font * 0.3) : 0;
      const processTop = top + topExtra;
      const cellX = (col) => rhu(margin + col * (boxW + arrowGap));
      const cellY = (row) => rhu(processTop + row * (boxH + arrowGap));
      let maxRow = 0;
      doc.items.forEach((it, i) => {
        const { row, col } = cellForIndex(i, flow, wrap);
        maxRow = Math.max(maxRow, row);
        const x = cellX(col);
        const y = cellY(row);
        const cx = rhu(x + boxW / 2);
        const cy = rhu(y + boxH / 2);
        const children = [];
        if (i > 0) {
          const prev = cellForIndex(i - 1, flow, wrap);
          const prevX = cellX(prev.col);
          const prevY = cellY(prev.row);
          const prevCX = rhu(prevX + boxW / 2);
          const prevCY = rhu(prevY + boxH / 2);
          const isTurn = flow === "snake" ? prev.row !== row : prev.col !== col;
          if (!isTurn) {
            if (flow === "ttb") {
              const ty = y;
              children.push(
                p.path(
                  `M ${cx} ${rhu(prevY + boxH)} L ${cx} ${rhu(ty - al)}`,
                  palette.textMuted,
                  2
                )
              );
              children.push(
                p.path(arrowTriangle(cx, ty, "down", al, aw), palette.textMuted, 0, {
                  fill: palette.textMuted
                })
              );
            } else if (flow === "snake") {
              if (row % 2 === 0) {
                children.push(
                  p.path(
                    `M ${rhu(prevX + boxW)} ${cy} L ${rhu(x - al)} ${cy}`,
                    palette.textMuted,
                    2
                  )
                );
                children.push(
                  p.path(arrowTriangle(x, cy, "right", al, aw), palette.textMuted, 0, {
                    fill: palette.textMuted
                  })
                );
              } else {
                const tipX = rhu(x + boxW);
                children.push(
                  p.path(`M ${prevX} ${cy} L ${rhu(tipX + al)} ${cy}`, palette.textMuted, 2)
                );
                children.push(
                  p.path(arrowTriangle(tipX, cy, "left", al, aw), palette.textMuted, 0, {
                    fill: palette.textMuted
                  })
                );
              }
            } else {
              if (col % 2 === 0) {
                const ty = y;
                children.push(
                  p.path(
                    `M ${cx} ${rhu(prevY + boxH)} L ${cx} ${rhu(ty - al)}`,
                    palette.textMuted,
                    2
                  )
                );
                children.push(
                  p.path(arrowTriangle(cx, ty, "down", al, aw), palette.textMuted, 0, {
                    fill: palette.textMuted
                  })
                );
              } else {
                const tipY = rhu(y + boxH);
                children.push(
                  p.path(`M ${cx} ${prevY} L ${cx} ${rhu(tipY + al)}`, palette.textMuted, 2)
                );
                children.push(
                  p.path(arrowTriangle(cx, tipY, "up", al, aw), palette.textMuted, 0, {
                    fill: palette.textMuted
                  })
                );
              }
            }
          } else if (flow === "snake") {
            if (turn === "direct") {
              children.push(
                p.path(
                  `M ${prevCX} ${rhu(prevY + boxH)} L ${prevCX} ${rhu(y - al)}`,
                  palette.textMuted,
                  2
                )
              );
              children.push(
                p.path(arrowTriangle(prevCX, y, "down", al, aw), palette.textMuted, 0, {
                  fill: palette.textMuted
                })
              );
            } else if (prev.row % 2 === 0) {
              const rx = rhu(prevX + boxW);
              children.push(
                p.path(
                  `M ${rx} ${prevCY} L ${rhu(rx + elbow)} ${prevCY} L ${rhu(rx + elbow)} ${cy} L ${rhu(rx + al)} ${cy}`,
                  palette.textMuted,
                  2
                )
              );
              children.push(
                p.path(arrowTriangle(rx, cy, "left", al, aw), palette.textMuted, 0, {
                  fill: palette.textMuted
                })
              );
              contentRight = Math.max(contentRight, rhu(rx + elbow + margin));
            } else {
              const lx = prevX;
              children.push(
                p.path(
                  `M ${lx} ${prevCY} L ${rhu(lx - elbow)} ${prevCY} L ${rhu(lx - elbow)} ${cy} L ${rhu(lx - al)} ${cy}`,
                  palette.textMuted,
                  2
                )
              );
              children.push(
                p.path(arrowTriangle(lx, cy, "right", al, aw), palette.textMuted, 0, {
                  fill: palette.textMuted
                })
              );
            }
          } else {
            if (turn === "direct") {
              children.push(
                p.path(`M ${rhu(prevX + boxW)} ${cy} L ${rhu(x - al)} ${cy}`, palette.textMuted, 2)
              );
              children.push(
                p.path(arrowTriangle(x, cy, "right", al, aw), palette.textMuted, 0, {
                  fill: palette.textMuted
                })
              );
            } else if (prev.col % 2 === 0) {
              const by = rhu(prevY + boxH);
              children.push(
                p.path(
                  `M ${prevCX} ${by} L ${prevCX} ${rhu(by + elbow)} L ${cx} ${rhu(by + elbow)} L ${cx} ${rhu(by + al)}`,
                  palette.textMuted,
                  2
                )
              );
              children.push(
                p.path(arrowTriangle(cx, by, "up", al, aw), palette.textMuted, 0, {
                  fill: palette.textMuted
                })
              );
            } else {
              const ty = prevY;
              children.push(
                p.path(
                  `M ${prevCX} ${ty} L ${prevCX} ${rhu(ty - elbow)} L ${cx} ${rhu(ty - elbow)} L ${cx} ${rhu(ty - al)}`,
                  palette.textMuted,
                  2
                )
              );
              children.push(
                p.path(arrowTriangle(cx, ty, "down", al, aw), palette.textMuted, 0, {
                  fill: palette.textMuted
                })
              );
            }
          }
        }
        children.push(
          p.rect({ x, y, width: boxW, height: boxH }, palette.surface, palette.primary, 1.5, {
            rx: 6
          })
        );
        children.push(
          ...renderItemText(
            p,
            itemInfos[i],
            x,
            y,
            boxW,
            boxH,
            font,
            smallFont,
            palette.text,
            palette.textMuted,
            "middle"
          )
        );
        elements.push(p.group(children, { id: it.id }));
        anchors[it.id] = { bounds: { x, y, width: boxW, height: boxH } };
        contentRight = Math.max(contentRight, x + boxW);
      });
      const bottomExtra = hasBottomTurn ? elbow : 0;
      height = rhu(processTop + (maxRow + 1) * (boxH + arrowGap) - arrowGap + bottomExtra + margin);
    }
  } else if (doc.style === "timeline") {
    const dotR = Math.max(5, rhu(font * 0.5));
    const itemInfos = doc.items.map((it) => measureItemLines(it.text, font, smallFont));
    let maxTextW = 0;
    let maxLines = 1;
    itemInfos.forEach((info) => {
      maxTextW = Math.max(maxTextW, info.maxLineWidth);
      const lc = getItemLineCount(info);
      maxLines = Math.max(maxLines, lc);
    });
    const lineH = font * 1.25;
    const labelH = maxLines > 1 ? rhu(maxLines * lineH + font * 0.6) : rhu(font * 1.8);
    const stepX = rhu(maxTextW + font * 2.4);
    const axisY = rhu(top + labelH);
    doc.items.forEach((it, i) => {
      const cx = rhu(margin + stepX / 2 + i * stepX);
      const children = [];
      if (i > 0) {
        const px = rhu(margin + stepX / 2 + (i - 1) * stepX);
        children.push(p.path(`M ${px} ${axisY} L ${cx} ${axisY}`, palette.border, 2));
      }
      children.push(p.circle({ x: cx, y: axisY }, dotR, palette.primary, palette.background, 2));
      const above = i % 2 === 0;
      const ly = above ? rhu(axisY - dotR - 8 - (maxLines > 1 ? (maxLines - 1) * lineH : 0)) : rhu(axisY + dotR + font + 4);
      const info = itemInfos[i];
      if (info.titleLines !== void 0) {
        let curY = ly;
        for (const tl of info.titleLines) {
          children.push(
            p.text(tl, cx, curY, font, palette.text, { weight: "bold", anchor: "middle" })
          );
          curY = rhu(curY + lineH);
        }
        if (info.subtitleLines) {
          for (const sl of info.subtitleLines) {
            children.push(
              p.text(sl, cx, curY, smallFont, palette.textMuted, {
                anchor: "middle"
              })
            );
            curY = rhu(curY + smallFont * 1.25);
          }
        }
      } else if (info.lines.length > 1) {
        info.lines.forEach((l, li) => {
          children.push(
            p.text(l, cx, rhu(ly + li * lineH), font, palette.text, { anchor: "middle" })
          );
        });
      } else {
        children.push(
          p.text(info.lines[0] ?? it.text, cx, ly, font, palette.text, { anchor: "middle" })
        );
      }
      elements.push(p.group(children, { id: it.id }));
      anchors[it.id] = {
        bounds: {
          x: rhu(cx - stepX / 2),
          y: top,
          width: stepX,
          height: rhu(labelH * 2 + dotR * 2)
        }
      };
      contentRight = Math.max(contentRight, cx + stepX / 2);
    });
    height = rhu(axisY + dotR + font + labelH + margin);
  } else if (doc.style === "pyramid") {
    const itemInfos = doc.items.map((it) => measureItemLines(it.text, font, smallFont));
    let maxTextW = 0;
    let maxLines = 1;
    itemInfos.forEach((info) => {
      maxTextW = Math.max(maxTextW, info.maxLineWidth);
      const lc = getItemLineCount(info);
      maxLines = Math.max(maxLines, lc);
    });
    const bandH = maxLines > 1 ? rhu(maxLines * (font * 1.3) + font * 1.4) : rhu(font * 2.8);
    const vGap = rhu(font * 0.4);
    const baseW = rhu(Math.max(maxTextW * 1.5, font * 14));
    const apexW = rhu(baseW * 0.28);
    const cxCenter = rhu(margin + baseW / 2);
    const widthAt = (frac) => apexW + (baseW - apexW) * frac;
    doc.items.forEach((it, i) => {
      const y = top + i * (bandH + vGap);
      const yTop = rhu(y);
      const yBot = rhu(y + bandH);
      const wTop = widthAt(n === 1 ? 1 : i / n);
      const wBot = widthAt(n === 1 ? 1 : (i + 1) / n);
      const tl = rhu(cxCenter - wTop / 2);
      const tr = rhu(cxCenter + wTop / 2);
      const bl = rhu(cxCenter - wBot / 2);
      const br = rhu(cxCenter + wBot / 2);
      const fill = i % 2 === 0 ? palette.primary : palette.secondary;
      const d = `M ${tl} ${yTop} L ${tr} ${yTop} L ${br} ${yBot} L ${bl} ${yBot} Z`;
      const txtColor = readableText(fill, theme);
      const mutedTxtColor = txtColor === "#ffffff" ? "rgba(255, 255, 255, 0.8)" : palette.textMuted;
      const children = [
        p.path(d, fill, 0, { fill }),
        ...renderItemText(
          p,
          itemInfos[i],
          bl,
          yTop,
          rhu(br - bl),
          bandH,
          font,
          smallFont,
          txtColor,
          mutedTxtColor,
          "middle"
        )
      ];
      elements.push(p.group(children, { id: it.id }));
      anchors[it.id] = { bounds: { x: bl, y: yTop, width: rhu(br - bl), height: bandH } };
      contentRight = Math.max(contentRight, cxCenter + baseW / 2);
    });
    height = rhu(top + n * (bandH + vGap) - vGap + margin);
  } else if (doc.style === "columns") {
    const cols = [];
    for (const it of doc.items) {
      if (it.depth === 0 || cols.length === 0) cols.push({ header: it, cells: [] });
      else cols[cols.length - 1].cells.push(it);
    }
    const itemInfos = /* @__PURE__ */ new Map();
    doc.items.forEach((it) => {
      itemInfos.set(it.id, measureItemLines(it.text, font, smallFont));
    });
    let maxHeaderLines = 1;
    let maxCellLines = 1;
    cols.forEach((c) => {
      const hInfo = itemInfos.get(c.header.id);
      const hCount = getItemLineCount(hInfo);
      maxHeaderLines = Math.max(maxHeaderLines, hCount);
      c.cells.forEach((ce) => {
        const cInfo = itemInfos.get(ce.id);
        const cCount = getItemLineCount(cInfo);
        maxCellLines = Math.max(maxCellLines, cCount);
      });
    });
    const headerH = maxHeaderLines > 1 ? rhu(maxHeaderLines * (font * 1.3) + font * 1) : rhu(font * 2.4);
    const cellH = maxCellLines > 1 ? rhu(maxCellLines * (font * 1.25) + font * 0.7) : rhu(font * 2);
    const gap = rhu(font * 0.5);
    const colGap = rhu(font * 1.2);
    const padX = rhu(font * 0.9);
    let x = margin;
    let maxCells = 0;
    const colX = [];
    const colW = [];
    cols.forEach((c) => {
      let w = itemInfos.get(c.header.id).maxLineWidth;
      c.cells.forEach((ce) => {
        w = Math.max(w, itemInfos.get(ce.id).maxLineWidth);
      });
      const cw = rhu(w + 2 * padX);
      colX.push(rhu(x));
      colW.push(cw);
      x += cw + colGap;
      maxCells = Math.max(maxCells, c.cells.length);
    });
    contentRight = rhu(x - colGap);
    cols.forEach((c, ci) => {
      const cx0 = colX[ci];
      const cw = colW[ci];
      const hInfo = itemInfos.get(c.header.id);
      const hchildren = [
        p.rect(
          { x: cx0, y: top, width: cw, height: headerH },
          palette.primary,
          palette.primary,
          0,
          { rx: 6 }
        ),
        ...renderItemText(
          p,
          hInfo,
          cx0,
          top,
          cw,
          headerH,
          font,
          smallFont,
          readableText(palette.primary, theme),
          "rgba(255, 255, 255, 0.8)",
          "middle",
          0,
          true
        )
      ];
      elements.push(p.group(hchildren, { id: c.header.id }));
      anchors[c.header.id] = { bounds: { x: cx0, y: top, width: cw, height: headerH } };
      c.cells.forEach((ce, ri) => {
        const cy0 = rhu(top + headerH + gap + ri * (cellH + gap));
        const cInfo = itemInfos.get(ce.id);
        const cch = [
          p.rect({ x: cx0, y: cy0, width: cw, height: cellH }, palette.surface, palette.border, 1, {
            rx: 4
          }),
          ...renderItemText(
            p,
            cInfo,
            cx0,
            cy0,
            cw,
            cellH,
            font,
            smallFont,
            palette.text,
            palette.textMuted,
            "middle"
          )
        ];
        elements.push(p.group(cch, { id: ce.id }));
        anchors[ce.id] = { bounds: { x: cx0, y: cy0, width: cw, height: cellH } };
      });
    });
    height = rhu(top + headerH + gap + maxCells * (cellH + gap) + margin);
  } else if (doc.style === "cycle") {
    const pad = rhu(font * 0.9);
    const itemInfos = doc.items.map((it) => measureItemLines(it.text, font, smallFont));
    let maxTextW = 0;
    let maxLines = 1;
    itemInfos.forEach((info) => {
      maxTextW = Math.max(maxTextW, info.maxLineWidth);
      const lc = getItemLineCount(info);
      maxLines = Math.max(maxLines, lc);
    });
    const nodeH = maxLines > 1 ? rhu(maxLines * (font * 1.25) + 2 * pad) : rhu(font * 2.2);
    const nodeW = rhu(maxTextW + 2 * pad);
    const chord = nodeW + rhu(font * 2.4);
    const R = n > 1 ? rhu(Math.max(nodeH * 2.4, chord / (2 * Math.sin(Math.PI / n)))) : 0;
    const cxC = rhu(margin + R + nodeW / 2);
    const cyC = rhu(top + R + nodeH / 2);
    const step = 2 * Math.PI / Math.max(n, 1);
    const gapAng = R > 0 ? Math.min(step * 0.3, (nodeW / 2 + pad) / R) : 0;
    doc.items.forEach((it, i) => {
      const a = -Math.PI / 2 + i * step;
      const ncx = rhu(cxC + R * Math.cos(a));
      const ncy = rhu(cyC + R * Math.sin(a));
      const x = rhu(ncx - nodeW / 2);
      const y = rhu(ncy - nodeH / 2);
      const children = [];
      if (n > 1) {
        const a0 = a + gapAng;
        const a1 = a + step - gapAng;
        if (a1 > a0) {
          const sx = rhu(cxC + R * Math.cos(a0));
          const sy = rhu(cyC + R * Math.sin(a0));
          const ex = rhu(cxC + R * Math.cos(a1));
          const ey = rhu(cyC + R * Math.sin(a1));
          children.push(p.path(`M ${sx} ${sy} A ${R} ${R} 0 0 1 ${ex} ${ey}`, palette.primary, 2));
          const pa = a1 - 0.12;
          const px = cxC + R * Math.cos(pa);
          const py = cyC + R * Math.sin(pa);
          const dx = ex - px, dy = ey - py;
          const len = Math.hypot(dx, dy) || 1;
          const ux = dx / len, uy = dy / len;
          const ah = Math.max(6, rhu(font * 0.6));
          const bx = ex - ux * ah, by = ey - uy * ah;
          const tri = `M ${rhu(bx - uy * ah * 0.6)} ${rhu(by + ux * ah * 0.6)} L ${ex} ${ey} L ${rhu(bx + uy * ah * 0.6)} ${rhu(by - ux * ah * 0.6)} Z`;
          children.push(p.path(tri, palette.primary, 0, { fill: palette.primary }));
        }
      }
      children.push(
        p.rect({ x, y, width: nodeW, height: nodeH }, palette.surface, palette.primary, 1.5, {
          rx: 8
        })
      );
      children.push(
        ...renderItemText(
          p,
          itemInfos[i],
          x,
          y,
          nodeW,
          nodeH,
          font,
          smallFont,
          palette.text,
          palette.textMuted,
          "middle"
        )
      );
      elements.push(p.group(children, { id: it.id }));
      anchors[it.id] = { bounds: { x, y, width: nodeW, height: nodeH } };
      contentRight = Math.max(contentRight, x + nodeW);
    });
    height = rhu(top + 2 * R + nodeH + margin);
  } else if (doc.style === "matrix") {
    const quad = [palette.primary, palette.secondary, palette.success, palette.warning];
    const cols = 2;
    const rows = Math.ceil(n / cols);
    const pad = rhu(font * 0.9);
    const gap = rhu(font * 0.5);
    const itemInfos = doc.items.map((it) => measureItemLines(it.text, font, smallFont));
    let maxTextW = 0;
    let maxLines = 1;
    itemInfos.forEach((info) => {
      maxTextW = Math.max(maxTextW, info.maxLineWidth);
      const lc = getItemLineCount(info);
      maxLines = Math.max(maxLines, lc);
    });
    const tileW = rhu(maxTextW + 2 * pad);
    const tileH = maxLines > 1 ? rhu(maxLines * (font * 1.3) + font * 1.8) : rhu(font * 3.2);
    doc.items.forEach((it, i) => {
      const r = Math.floor(i / cols);
      const c = i % cols;
      const x = rhu(margin + c * (tileW + gap));
      const y = rhu(top + r * (tileH + gap));
      const fill = quad[i % quad.length];
      const txtColor = readableText(fill, theme);
      const mutedTxtColor = txtColor === "#ffffff" ? "rgba(255, 255, 255, 0.8)" : palette.textMuted;
      const children = [
        p.rect({ x, y, width: tileW, height: tileH }, fill, fill, 0, { rx: 6 }),
        ...renderItemText(
          p,
          itemInfos[i],
          x,
          y,
          tileW,
          tileH,
          font,
          smallFont,
          txtColor,
          mutedTxtColor,
          "middle",
          0,
          true
        )
      ];
      elements.push(p.group(children, { id: it.id }));
      anchors[it.id] = { bounds: { x, y, width: tileW, height: tileH } };
      contentRight = Math.max(contentRight, x + tileW);
    });
    height = rhu(top + rows * (tileH + gap) - gap + margin);
  } else if (doc.style === "funnel") {
    const itemInfos = doc.items.map((it) => measureItemLines(it.text, font, smallFont));
    let maxTextW = 0;
    let maxLines = 1;
    itemInfos.forEach((info) => {
      maxTextW = Math.max(maxTextW, info.maxLineWidth);
      const lc = getItemLineCount(info);
      maxLines = Math.max(maxLines, lc);
    });
    const bandH = maxLines > 1 ? rhu(maxLines * (font * 1.3) + font * 1.4) : rhu(font * 2.8);
    const vGap = rhu(font * 0.4);
    const baseW = rhu(Math.max(maxTextW * 1.5, font * 14));
    const apexW = rhu(baseW * 0.28);
    const cxCenter = rhu(margin + baseW / 2);
    const widthAt = (frac) => apexW + (baseW - apexW) * frac;
    doc.items.forEach((it, i) => {
      const y = top + i * (bandH + vGap);
      const yTop = rhu(y);
      const yBot = rhu(y + bandH);
      const wTop = widthAt(n === 1 ? 1 : (n - i) / n);
      const wBot = widthAt(n === 1 ? 1 : (n - i - 1) / n);
      const tl = rhu(cxCenter - wTop / 2);
      const tr = rhu(cxCenter + wTop / 2);
      const bl = rhu(cxCenter - wBot / 2);
      const br = rhu(cxCenter + wBot / 2);
      const fill = i % 2 === 0 ? palette.primary : palette.secondary;
      const d = `M ${tl} ${yTop} L ${tr} ${yTop} L ${br} ${yBot} L ${bl} ${yBot} Z`;
      const txtColor = readableText(fill, theme);
      const mutedTxtColor = txtColor === "#ffffff" ? "rgba(255, 255, 255, 0.8)" : palette.textMuted;
      const children = [
        p.path(d, fill, 0, { fill }),
        ...renderItemText(
          p,
          itemInfos[i],
          tl,
          yTop,
          rhu(tr - tl),
          bandH,
          font,
          smallFont,
          txtColor,
          mutedTxtColor,
          "middle"
        )
      ];
      elements.push(p.group(children, { id: it.id }));
      anchors[it.id] = { bounds: { x: tl, y: yTop, width: rhu(tr - tl), height: bandH } };
      contentRight = Math.max(contentRight, cxCenter + baseW / 2);
    });
    height = rhu(top + n * (bandH + vGap) - vGap + margin);
  } else if (doc.style === "stepup") {
    const itemInfos = doc.items.map((it) => measureItemLines(it.text, font, smallFont));
    let maxTextW = 0;
    let maxLines = 1;
    itemInfos.forEach((info) => {
      maxTextW = Math.max(maxTextW, info.maxLineWidth);
      const lc = getItemLineCount(info);
      maxLines = Math.max(maxLines, lc);
    });
    const boxH = maxLines > 1 ? rhu(maxLines * (font * 1.3) + font * 1.2) : rhu(font * 2.4);
    const gapX = rhu(font * 0.8);
    const stepUp = rhu(boxH * 0.7);
    const boxW = rhu(maxTextW + 2 * rhu(font * 0.9));
    const topmost = top;
    const yOf = (i) => rhu(topmost + (n - 1 - i) * stepUp);
    const xOf = (i) => rhu(margin + i * (boxW + gapX));
    doc.items.forEach((it, i) => {
      const x = xOf(i);
      const y = yOf(i);
      const children = [];
      if (i > 0) {
        const px = rhu(xOf(i - 1) + boxW);
        const py = rhu(yOf(i - 1) + boxH / 2);
        const cy = rhu(y + boxH / 2);
        children.push(
          p.path(
            `M ${px} ${py} L ${rhu((px + x) / 2)} ${py} L ${rhu((px + x) / 2)} ${cy} L ${x} ${cy}`,
            palette.border,
            1.5
          )
        );
      }
      children.push(
        p.rect({ x, y, width: boxW, height: boxH }, palette.surface, palette.primary, 1.5, {
          rx: 6
        })
      );
      children.push(
        ...renderItemText(
          p,
          itemInfos[i],
          x,
          y,
          boxW,
          boxH,
          font,
          smallFont,
          palette.text,
          palette.textMuted,
          "middle"
        )
      );
      elements.push(p.group(children, { id: it.id }));
      anchors[it.id] = { bounds: { x, y, width: boxW, height: boxH } };
      contentRight = Math.max(contentRight, x + boxW);
    });
    height = rhu(top + (n - 1) * stepUp + boxH + margin);
  } else if (doc.style === "venn") {
    const tint = [
      palette.primary,
      palette.secondary,
      palette.success,
      palette.warning,
      palette.error
    ];
    const itemInfos = doc.items.map((it) => measureItemLines(it.text, font, smallFont));
    let maxTextW = 0;
    itemInfos.forEach((it) => {
      maxTextW = Math.max(maxTextW, it.maxLineWidth);
    });
    const r = rhu(Math.max(font * 3.4, maxTextW / 2 + font));
    const d = n > 1 ? rhu(r * 0.72) : 0;
    const start = n === 2 ? Math.PI : -Math.PI / 2;
    const cxC = rhu(margin + d + r);
    const cyC = rhu(top + d + r);
    const labelR = d + rhu(r * 0.42);
    doc.items.forEach((it, i) => {
      const ang = start + i * 2 * Math.PI / n;
      const cx = rhu(cxC + d * Math.cos(ang));
      const cy = rhu(cyC + d * Math.sin(ang));
      const lx = n === 1 ? cxC : rhu(cxC + labelR * Math.cos(ang));
      const ly = n === 1 ? cyC : rhu(cyC + labelR * Math.sin(ang));
      const fill = tint[i % tint.length];
      const info = itemInfos[i];
      const children = [
        p.circle({ x: cx, y: cy }, r, fill, fill, 0, { opacity: 0.5 })
      ];
      if (info.titleLines !== void 0) {
        const titleLH = font * 1.25;
        const subLH = smallFont * 1.25;
        const totalLines = info.titleLines.length + (info.subtitleLines?.length ?? 0);
        let curY = rhu(ly + font * 0.34 - (totalLines - 1) * titleLH / 2);
        for (const tl of info.titleLines) {
          children.push(
            p.text(tl, lx, curY, font, palette.text, {
              weight: "bold",
              anchor: "middle"
            })
          );
          curY = rhu(curY + titleLH);
        }
        if (info.subtitleLines) {
          for (const sl of info.subtitleLines) {
            children.push(
              p.text(sl, lx, curY, smallFont, palette.textMuted, {
                anchor: "middle"
              })
            );
            curY = rhu(curY + subLH);
          }
        }
      } else if (info.lines.length > 1) {
        const lineH = font * 1.2;
        const startY = rhu(ly + font * 0.34 - (info.lines.length - 1) * lineH / 2);
        info.lines.forEach((l, li) => {
          children.push(
            p.text(l, lx, rhu(startY + li * lineH), font, palette.text, {
              weight: "bold",
              anchor: "middle"
            })
          );
        });
      } else {
        children.push(
          p.text(info.lines[0] ?? it.text, lx, rhu(ly + font * 0.34), font, palette.text, {
            weight: "bold",
            anchor: "middle"
          })
        );
      }
      elements.push(p.group(children, { id: it.id }));
      anchors[it.id] = {
        bounds: { x: rhu(cx - r), y: rhu(cy - r), width: rhu(2 * r), height: rhu(2 * r) }
      };
      contentRight = Math.max(contentRight, cx + r);
    });
    height = rhu(top + 2 * (d + r) + margin);
  } else {
    let curY = top;
    doc.items.forEach((it, i) => {
      const info = measureItemLines(it.text, font, smallFont);
      const lineCount = getItemLineCount(info);
      const rowH = lineCount > 1 ? rhu(lineCount * (font * 1.25) + font * 0.4) : rhu(font * 1.9);
      const rowY = curY;
      const cy = lineCount > 1 ? rowY + font * 0.85 : rowY + rowH / 2;
      const x = margin + it.depth * indentPx;
      const children = [];
      let textX;
      if (doc.style === "numbered") {
        const label = `${it.numberLabel}.`;
        const numBaseline = lineCount > 1 ? rhu(rowY + font * 0.85) : rhu(cy + font * 0.34);
        children.push(
          p.text(label, x, numBaseline, font, palette.primary, { weight: "bold", anchor: "start" })
        );
        textX = x + measureText(label, font).width + 10;
      } else {
        const r = markerRadius(it.depth, font);
        const c = markerColor(it.depth, palette);
        children.push(p.circle({ x: x + r, y: rhu(cy) }, r, c, c, 0));
        textX = x + r * 2 + 12;
      }
      if (info.titleLines !== void 0) {
        const titleLH = font * 1.25;
        const subLH = smallFont * 1.25;
        let curTextY = rhu(rowY + font * 0.85);
        for (let ti = 0; ti < info.titleLines.length; ti++) {
          children.push(
            p.text(info.titleLines[ti], textX, curTextY, font, palette.text, {
              weight: "bold",
              anchor: "start"
            })
          );
          curTextY += titleLH;
        }
        if (info.subtitleLines) {
          for (let si = 0; si < info.subtitleLines.length; si++) {
            children.push(
              p.text(info.subtitleLines[si], textX, curTextY, smallFont, palette.textMuted, {
                anchor: "start"
              })
            );
            curTextY += subLH;
          }
        }
      } else if (info.lines.length > 1) {
        const lineH = font * 1.25;
        info.lines.forEach((line, li) => {
          const ly = rhu(rowY + font * 0.85 + li * lineH);
          children.push(p.text(line, textX, ly, font, palette.text, { anchor: "start" }));
        });
      } else {
        const textBaseline = rhu(rowY + rowH / 2 + font * 0.34);
        children.push(
          p.text(info.lines[0] ?? it.text, textX, textBaseline, font, palette.text, {
            anchor: "start"
          })
        );
      }
      const tw = info.maxLineWidth;
      contentRight = Math.max(contentRight, textX + tw);
      elements.push(p.group(children, { id: it.id }));
      anchors[it.id] = { bounds: { x, y: rowY, width: rhu(textX - x + tw), height: rowH } };
      curY += rowH;
    });
    height = rhu(curY + margin);
  }
  if (doc.title) {
    elements.unshift(
      p.text(
        doc.title,
        margin,
        rhu(margin + typography.titleFontSize),
        typography.titleFontSize,
        palette.text,
        { weight: "bold" }
      )
    );
  }
  const width = rhu(contentRight + margin);
  const scene = {
    viewBox: { x: 0, y: 0, width, height },
    background: palette.background,
    elements
  };
  const base = { scene, anchors };
  return doc.reveal === "none" ? base : { ...base, reveal: { steps: buildSteps(doc) } };
}
var list = {
  parseMermaid(input) {
    return parseList(input);
  },
  parseYaml(input) {
    return JSON.parse(input);
  },
  layout(ir, theme) {
    return layoutList2(ir, theme);
  }
};
function parseFishbone(input) {
  const { metadata, body } = extractFrontmatter(input);
  const lines2 = body.split(/\r?\n/);
  let effect = "Effect";
  let title = typeof metadata.title === "string" ? metadata.title : "";
  const categories = [];
  let currentCategory = null;
  for (let rawLine of lines2) {
    const trimmed = rawLine.trim();
    if (!trimmed || trimmed.startsWith("%%") || trimmed.startsWith("#")) {
      continue;
    }
    if (/^fishbone\b/i.test(trimmed)) {
      continue;
    }
    const titleMatch = trimmed.match(/^title(?:\s+|:\s*)(?:"([^"]+)"|'([^']+)'|(.+))$/i);
    if (titleMatch) {
      title = (titleMatch[1] ?? titleMatch[2] ?? titleMatch[3] ?? "").trim();
      continue;
    }
    const effectMatch = trimmed.match(/^effect(?:\s+|:\s*)(?:"([^"]+)"|'([^']+)'|(.+))$/i);
    if (effectMatch) {
      effect = (effectMatch[1] ?? effectMatch[2] ?? effectMatch[3] ?? "").trim();
      continue;
    }
    const catMatch = trimmed.match(/^category(?:\s+|:\s*)(?:"([^"]+)"|'([^']+)'|(.+))$/i);
    if (catMatch) {
      const name = (catMatch[1] ?? catMatch[2] ?? catMatch[3] ?? "").trim();
      currentCategory = { name, causes: [] };
      categories.push(currentCategory);
      continue;
    }
    if (currentCategory) {
      const causeText = trimmed.replace(/^-\s*/, "").replace(/^"([^"]+)"$/, "$1").replace(/^'([^']+)'$/, "$1").trim();
      if (causeText) {
        currentCategory.causes.push({ label: causeText });
      }
    }
  }
  return {
    version: "1",
    metadata: { ...metadata, title },
    effect,
    categories
  };
}
function layoutFishbone(doc, theme) {
  const p = pen(theme);
  const { palette, typography } = theme;
  const elements = [];
  const categories = doc.categories;
  const numCats = categories.length;
  const topCats = categories.filter((_, i) => i % 2 === 0);
  const botCats = categories.filter((_, i) => i % 2 === 1);
  const numPairs = Math.max(topCats.length, botCats.length, 1);
  const colWidth = 240;
  const marginX = 60;
  const titleH = doc.metadata.title ? 60 : 20;
  const spineStartX = marginX;
  const spineEndX = marginX + numPairs * colWidth + 40;
  const effectBoxW = 180;
  const effectBoxH = 64;
  const effectBoxX = spineEndX + 10;
  const spineY = titleH + 260;
  const canvasW = effectBoxX + effectBoxW + marginX;
  const canvasH = spineY + 280;
  if (doc.metadata.title) {
    elements.push(
      p.text(String(doc.metadata.title), marginX, 40, typography.titleFontSize, palette.text, {
        weight: "bold"
      })
    );
  }
  elements.push(p.path(`M ${spineStartX} ${spineY} L ${spineEndX} ${spineY}`, palette.primary, 3));
  elements.push(
    p.path(
      `M ${spineEndX - 10} ${spineY - 8} L ${spineEndX + 6} ${spineY} L ${spineEndX - 10} ${spineY + 8} Z`,
      palette.primary,
      1,
      { fill: palette.primary }
    )
  );
  elements.push(
    p.rect(
      { x: effectBoxX, y: spineY - effectBoxH / 2, width: effectBoxW, height: effectBoxH },
      palette.surface,
      palette.primary,
      2,
      { rx: 6 }
    )
  );
  const effectMeasured = measureText(doc.effect, typography.baseFontSize);
  elements.push(
    p.text(
      doc.effect,
      effectBoxX + effectBoxW / 2,
      spineY + typography.baseFontSize * 0.35,
      typography.baseFontSize,
      palette.text,
      { weight: "bold", anchor: "middle" }
    )
  );
  for (let pairIdx = 0; pairIdx < numPairs; pairIdx++) {
    const spineAttachX = spineStartX + 120 + pairIdx * colWidth;
    if (pairIdx < topCats.length) {
      const cat = topCats[pairIdx];
      const ribStartX = spineAttachX - 100;
      const ribStartY = spineY - 200;
      elements.push(
        p.path(`M ${ribStartX} ${ribStartY} L ${spineAttachX} ${spineY}`, palette.border, 2)
      );
      const catTextW = Math.max(90, measureText(cat.name, typography.smallFontSize).width + 20);
      const catBoxH = 26;
      elements.push(
        p.rect(
          { x: ribStartX - catTextW / 2, y: ribStartY - catBoxH, width: catTextW, height: catBoxH },
          palette.surface,
          palette.border,
          1.5,
          { rx: 4 }
        )
      );
      elements.push(
        p.text(
          cat.name,
          ribStartX,
          ribStartY - catBoxH / 2 + typography.smallFontSize * 0.35,
          typography.smallFontSize,
          palette.text,
          { weight: "bold", anchor: "middle" }
        )
      );
      const numCauses = cat.causes.length;
      for (let cIdx = 0; cIdx < numCauses; cIdx++) {
        const t = (cIdx + 1) / (numCauses + 1);
        const ribPtX = ribStartX + (spineAttachX - ribStartX) * t;
        const ribPtY = ribStartY + (spineY - ribStartY) * t;
        const branchW = 100;
        const branchEndX = ribPtX - branchW;
        elements.push(
          p.path(`M ${branchEndX} ${ribPtY} L ${ribPtX} ${ribPtY}`, palette.border, 1.2)
        );
        elements.push(
          p.text(
            cat.causes[cIdx].label,
            branchEndX + 4,
            ribPtY - 4,
            typography.smallFontSize - 1,
            palette.textMuted
          )
        );
      }
    }
    if (pairIdx < botCats.length) {
      const cat = botCats[pairIdx];
      const ribStartX = spineAttachX - 100;
      const ribStartY = spineY + 200;
      elements.push(
        p.path(`M ${ribStartX} ${ribStartY} L ${spineAttachX} ${spineY}`, palette.border, 2)
      );
      const catTextW = Math.max(90, measureText(cat.name, typography.smallFontSize).width + 20);
      const catBoxH = 26;
      elements.push(
        p.rect(
          { x: ribStartX - catTextW / 2, y: ribStartY, width: catTextW, height: catBoxH },
          palette.surface,
          palette.border,
          1.5,
          { rx: 4 }
        )
      );
      elements.push(
        p.text(
          cat.name,
          ribStartX,
          ribStartY + catBoxH / 2 + typography.smallFontSize * 0.35,
          typography.smallFontSize,
          palette.text,
          { weight: "bold", anchor: "middle" }
        )
      );
      const numCauses = cat.causes.length;
      for (let cIdx = 0; cIdx < numCauses; cIdx++) {
        const t = (cIdx + 1) / (numCauses + 1);
        const ribPtX = ribStartX + (spineAttachX - ribStartX) * t;
        const ribPtY = ribStartY + (spineY - ribStartY) * t;
        const branchW = 100;
        const branchEndX = ribPtX - branchW;
        elements.push(
          p.path(`M ${branchEndX} ${ribPtY} L ${ribPtX} ${ribPtY}`, palette.border, 1.2)
        );
        elements.push(
          p.text(
            cat.causes[cIdx].label,
            branchEndX + 4,
            ribPtY - 4,
            typography.smallFontSize - 1,
            palette.textMuted
          )
        );
      }
    }
  }
  const rawScene = {
    viewBox: { x: 0, y: 0, width: canvasW, height: canvasH },
    background: palette.background,
    elements
  };
  const scene = applyOverlays(rawScene, doc.overlays, theme);
  const anchors = {};
  return { scene, anchors };
}
var fishbone = {
  parseMermaid: parseFishbone,
  parseYaml(input) {
    return JSON.parse(input);
  },
  layout: layoutFishbone
};
function parsePyramid(input) {
  const { metadata, body } = extractFrontmatter(input);
  const lines2 = body.split(/\r?\n/);
  let direction = "pyramid";
  let title = typeof metadata.title === "string" ? metadata.title : "";
  const tiers = [];
  for (const rawLine of lines2) {
    const trimmed = rawLine.trim();
    if (!trimmed || trimmed.startsWith("%%") || trimmed.startsWith("#")) {
      continue;
    }
    if (/^funnel\b/i.test(trimmed)) {
      direction = "funnel";
      continue;
    }
    if (/^pyramid\b/i.test(trimmed)) {
      direction = "pyramid";
      continue;
    }
    const titleMatch = trimmed.match(/^title(?:\s+|:\s*)(?:"([^"]+)"|'([^']+)'|(.+))$/i);
    if (titleMatch) {
      title = (titleMatch[1] ?? titleMatch[2] ?? titleMatch[3] ?? "").trim();
      continue;
    }
    let lineContent = trimmed.replace(/^tier\s+/i, "").replace(/^-\s*/, "");
    const isFocal = /(:{1,3}focal\b|\bfocal\b)/i.test(lineContent);
    lineContent = lineContent.replace(/(:{1,3}focal\b|\bfocal\b)/gi, "").trim();
    const colonIdx = lineContent.lastIndexOf(":");
    let label = lineContent;
    let value;
    if (colonIdx > 0 && !lineContent.startsWith("http")) {
      label = lineContent.slice(0, colonIdx).trim();
      value = lineContent.slice(colonIdx + 1).trim();
    }
    label = label.replace(/^["']|["']$/g, "").trim();
    if (value) {
      value = value.replace(/^["']|["']$/g, "").trim();
    }
    if (label) {
      tiers.push({
        label,
        ...value !== void 0 ? { value } : {},
        ...isFocal ? { isFocal: true } : {}
      });
    }
  }
  return {
    version: "1",
    metadata: { ...metadata, title },
    direction,
    tiers
  };
}
function layoutPyramid(doc, theme) {
  const p = pen(theme);
  const { palette, typography } = theme;
  const elements = [];
  const tiers = doc.tiers;
  const N = Math.max(tiers.length, 1);
  const isFunnel = doc.direction === "funnel";
  const marginX = 80;
  const titleH = doc.metadata.title ? 60 : 30;
  const tierH = 54;
  const tierGap = 6;
  const totalTierH = N * tierH + (N - 1) * tierGap;
  const minW = 200;
  const maxW = 560;
  const canvasW = 860;
  const centerX = 380;
  const canvasH = titleH + totalTierH + 60;
  if (doc.metadata.title) {
    elements.push(
      p.text(String(doc.metadata.title), marginX, 40, typography.titleFontSize, palette.text, {
        weight: "bold"
      })
    );
  }
  for (let i = 0; i < N; i++) {
    const tier = tiers[i];
    const curY = titleH + i * (tierH + tierGap);
    let topFrac;
    let botFrac;
    if (!isFunnel) {
      topFrac = i / N;
      botFrac = (i + 1) / N;
    } else {
      topFrac = (N - i) / N;
      botFrac = (N - i - 1) / N;
    }
    const wTop = minW + (maxW - minW) * topFrac;
    const wBot = minW + (maxW - minW) * botFrac;
    const x1 = centerX - wTop / 2;
    const x2 = centerX + wTop / 2;
    const x3 = centerX + wBot / 2;
    const x4 = centerX - wBot / 2;
    const y1 = curY;
    const y2 = curY + tierH;
    const isFocal = tier.isFocal;
    const fill = isFocal ? palette.primary : palette.surface;
    const stroke = isFocal ? palette.primary : palette.border;
    const textFill = isFocal ? theme.name.includes("dark") ? "#1E293B" : "#FFFFFF" : palette.text;
    const pathD = `M ${x1} ${y1} L ${x2} ${y1} L ${x3} ${y2} L ${x4} ${y2} Z`;
    elements.push(p.path(pathD, stroke, 1.5, { fill }));
    elements.push(
      p.text(
        tier.label,
        centerX,
        curY + tierH / 2 + typography.baseFontSize * 0.35,
        typography.baseFontSize,
        textFill,
        { weight: isFocal ? "bold" : "normal", anchor: "middle" }
      )
    );
    if (tier.value !== void 0) {
      const rightX = Math.max(x2, x3) + 24;
      const valText = String(tier.value);
      elements.push(
        p.path(
          `M ${Math.max(x2, x3) + 4} ${curY + tierH / 2} L ${rightX - 6} ${curY + tierH / 2}`,
          palette.border,
          1,
          { dash: "3 2" }
        )
      );
      elements.push(
        p.text(
          valText,
          rightX,
          curY + tierH / 2 + typography.smallFontSize * 0.35,
          typography.smallFontSize,
          palette.primary,
          { weight: "bold" }
        )
      );
    }
  }
  const rawScene = {
    viewBox: { x: 0, y: 0, width: canvasW, height: canvasH },
    background: palette.background,
    elements
  };
  const scene = applyOverlays(rawScene, doc.overlays, theme);
  const anchors = {};
  return { scene, anchors };
}
var pyramid = {
  parseMermaid: parsePyramid,
  parseYaml(input) {
    return JSON.parse(input);
  },
  layout: layoutPyramid
};
function parseLoop(input) {
  const { metadata, body } = extractFrontmatter(input);
  const lines2 = body.split(/\r?\n/);
  let title = typeof metadata.title === "string" ? metadata.title : "";
  let hub;
  const steps = [];
  for (const rawLine of lines2) {
    const trimmed = rawLine.trim();
    if (!trimmed || trimmed.startsWith("%%") || trimmed.startsWith("#")) {
      continue;
    }
    if (/^(loop|flywheel)\b/i.test(trimmed)) {
      continue;
    }
    const titleMatch = trimmed.match(/^title(?:\s+|:\s*)(?:"([^"]+)"|'([^']+)'|(.+))$/i);
    if (titleMatch) {
      title = (titleMatch[1] ?? titleMatch[2] ?? titleMatch[3] ?? "").trim();
      continue;
    }
    const hubMatch = trimmed.match(/^hub(?:\s+|:\s*)(?:"([^"]+)"|'([^']+)'|(.+))$/i);
    if (hubMatch) {
      hub = (hubMatch[1] ?? hubMatch[2] ?? hubMatch[3] ?? "").trim();
      continue;
    }
    let stepLine = trimmed.replace(/^step\s+/i, "").replace(/^-\s*/, "");
    const isFocal = /(:{1,3}focal\b|\bfocal\b)/i.test(stepLine);
    stepLine = stepLine.replace(/(:{1,3}focal\b|\bfocal\b)/gi, "").trim();
    let label = stepLine;
    let desc;
    const colonIdx = stepLine.indexOf(":");
    if (colonIdx > 0 && !stepLine.startsWith("http")) {
      label = stepLine.slice(0, colonIdx).trim();
      desc = stepLine.slice(colonIdx + 1).trim();
    }
    label = label.replace(/^["']|["']$/g, "").trim();
    if (desc) {
      desc = desc.replace(/^["']|["']$/g, "").trim();
    }
    if (label) {
      steps.push({
        label,
        ...desc ? { desc } : {},
        ...isFocal ? { isFocal: true } : {}
      });
    }
  }
  return {
    version: "1",
    metadata: { ...metadata, title },
    ...hub ? { hub } : {},
    steps
  };
}
function layoutLoop(doc, theme) {
  const p = pen(theme);
  const { palette, typography } = theme;
  const elements = [];
  const steps = doc.steps;
  const N = Math.max(steps.length, 1);
  const canvasW = 860;
  const canvasH = 720;
  const titleH = doc.metadata.title ? 60 : 30;
  const cx = canvasW / 2;
  const cy = titleH + 310;
  const radius = 200;
  if (doc.metadata.title) {
    elements.push(
      p.text(String(doc.metadata.title), 60, 40, typography.titleFontSize, palette.text, {
        weight: "bold"
      })
    );
  }
  if (doc.hub) {
    for (let i = 0; i < N; i++) {
      const angle = -Math.PI / 2 + 2 * Math.PI * i / N;
      const sx = cx + radius * Math.cos(angle);
      const sy = cy + radius * Math.sin(angle);
      elements.push(p.path(`M ${cx} ${cy} L ${sx} ${sy}`, palette.border, 1, { dash: "4 4" }));
    }
  }
  if (N > 1) {
    for (let i = 0; i < N; i++) {
      const nextIdx = (i + 1) % N;
      const a1 = -Math.PI / 2 + 2 * Math.PI * i / N;
      const a2 = -Math.PI / 2 + 2 * Math.PI * nextIdx / N;
      const midAngle = a1 + (a2 > a1 ? a2 - a1 : a2 + 2 * Math.PI - a1) / 2;
      const arcR = radius + 25;
      const p1x = cx + radius * Math.cos(a1 + 0.35);
      const p1y = cy + radius * Math.sin(a1 + 0.35);
      const p2x = cx + radius * Math.cos(a2 - 0.35);
      const p2y = cy + radius * Math.sin(a2 - 0.35);
      const cpx = cx + arcR * Math.cos(midAngle);
      const cpy = cy + arcR * Math.sin(midAngle);
      elements.push(p.path(`M ${p1x} ${p1y} Q ${cpx} ${cpy} ${p2x} ${p2y}`, palette.primary, 1.8));
      const tangentX = p2x - cpx;
      const tangentY = p2y - cpy;
      const len = Math.hypot(tangentX, tangentY) || 1;
      const ux = tangentX / len;
      const uy = tangentY / len;
      const arrowLen = theme.edges?.arrowSize ?? 8;
      const arrowW = arrowLen * 0.6;
      const ax1 = p2x - ux * arrowLen + uy * arrowW;
      const ay1 = p2y - uy * arrowLen - ux * arrowW;
      const ax2 = p2x - ux * arrowLen - uy * arrowW;
      const ay2 = p2y - uy * arrowLen + ux * arrowW;
      elements.push(
        p.path(`M ${ax1} ${ay1} L ${p2x} ${p2y} L ${ax2} ${ay2} Z`, palette.primary, 1, {
          fill: palette.primary
        })
      );
    }
  }
  if (doc.hub) {
    const hubW = 140;
    const hubH = 64;
    elements.push(
      p.rect(
        { x: cx - hubW / 2, y: cy - hubH / 2, width: hubW, height: hubH },
        palette.surface,
        palette.primary,
        2,
        { rx: 32 }
      )
    );
    elements.push(
      p.text(
        doc.hub,
        cx,
        cy + typography.smallFontSize * 0.35,
        typography.smallFontSize,
        palette.text,
        { weight: "bold", anchor: "middle" }
      )
    );
  }
  const nodeW = 150;
  const nodeH = 52;
  for (let i = 0; i < N; i++) {
    const step = steps[i];
    const angle = -Math.PI / 2 + 2 * Math.PI * i / N;
    const nx = cx + radius * Math.cos(angle);
    const ny = cy + radius * Math.sin(angle);
    const isFocal = step.isFocal;
    const fill = isFocal ? palette.primary : palette.surface;
    const stroke = isFocal ? palette.primary : palette.border;
    const textFill = isFocal ? theme.name.includes("dark") ? "#1E293B" : "#FFFFFF" : palette.text;
    const descFill = isFocal ? theme.name.includes("dark") ? "#334155" : "rgba(255,255,255,0.85)" : palette.textMuted;
    elements.push(
      p.rect(
        { x: nx - nodeW / 2, y: ny - nodeH / 2, width: nodeW, height: nodeH },
        fill,
        stroke,
        isFocal ? 2 : 1.5,
        { rx: 6 }
      )
    );
    const hasDesc = Boolean(step.desc);
    const titleY = hasDesc ? ny - 4 : ny + typography.smallFontSize * 0.35;
    elements.push(
      p.text(step.label, nx, titleY, typography.smallFontSize, textFill, {
        weight: "bold",
        anchor: "middle"
      })
    );
    if (hasDesc && step.desc) {
      elements.push(
        p.text(step.desc, nx, ny + 12, typography.smallFontSize - 2, descFill, {
          anchor: "middle"
        })
      );
    }
  }
  const rawScene = {
    viewBox: { x: 0, y: 0, width: canvasW, height: canvasH },
    background: palette.background,
    elements
  };
  const scene = applyOverlays(rawScene, doc.overlays, theme);
  const anchors = {};
  return { scene, anchors };
}
var loop = {
  parseMermaid: parseLoop,
  parseYaml(input) {
    return JSON.parse(input);
  },
  layout: layoutLoop
};
var ANIMATION_PERIOD_SECONDS = {
  march: 0.8,
  particle: 1.6,
  draw: 2.4,
  pulse: 1.6,
  glow: 1.6,
  comet: 1.6,
  stream: 2.4,
  flow: 1.6,
  colorcycle: 2.4
};
var COLOR_CYCLE_STROKES = ["#4A90D9", "#9b51e0", "#e54444", "#2ecc71", "#4A90D9"];
var FLOW_STOP_OFFSETS = [
  [0, 60, 100],
  [10, 70, 100],
  [20, 80, 100]
];
function animationPeriodSeconds(animation) {
  return ANIMATION_PERIOD_SECONDS[animation];
}
function animationDuration(animation) {
  return `${formatNum(animationPeriodSeconds(animation))}s`;
}
function marchDashoffsetValues(dasharray) {
  return { from: 0, to: -parseDasharrayPeriod(dasharray) };
}
function drawDashoffsetValues(pathLength) {
  return [0, pathLength, 0];
}
function pulseStrokeWidthValues(strokeWidth) {
  return [strokeWidth, Number(formatNum(strokeWidth * 2)), strokeWidth];
}
function glowStrokeOpacityValues() {
  return [1, 0.3, 1];
}
function colorCycleStrokeValues() {
  return COLOR_CYCLE_STROKES;
}
function flowStopOffsetValues(stopIndex) {
  return FLOW_STOP_OFFSETS[stopIndex];
}
function motionParticleSpecs(animation) {
  if (animation === "particle") return [{ radius: 4, phase: 0 }];
  if (animation === "comet") {
    return [
      { radius: 4.2, opacity: 0.95, phase: 0 },
      { radius: 3.1, opacity: 0.45, phase: 0.1 },
      { radius: 2.2, opacity: 0.22, phase: 0.2 }
    ];
  }
  return [
    { radius: 3.2, opacity: 0.9, phase: 0 },
    { radius: 3.2, opacity: 0.9, phase: 0.25 },
    { radius: 3.2, opacity: 0.9, phase: 0.5 },
    { radius: 3.2, opacity: 0.9, phase: 0.75 }
  ];
}
function motionBeginSeconds(animation, phase) {
  return -phase * ANIMATION_PERIOD_SECONDS[animation];
}
function pathLengthApprox(path) {
  const pts = pathPoints(path);
  if (pts.length < 2) return 1e3;
  let length = 0;
  for (let i = 1; i < pts.length; i++) {
    length += Math.hypot(pts[i].x - pts[i - 1].x, pts[i].y - pts[i - 1].y);
  }
  return Math.max(1, Number(formatNum(length)));
}
function pathPoints(path) {
  const nums = [...path.matchAll(/-?\d+(?:\.\d+)?(?:e[-+]?\d+)?/gi)].map((m) => Number(m[0]));
  const pts = [];
  for (let i = 0; i + 1 < nums.length; i += 2) {
    const x = nums[i];
    const y = nums[i + 1];
    if (Number.isFinite(x) && Number.isFinite(y)) pts.push({ x, y });
  }
  return pts;
}
function parseDasharrayPeriod(dasharray) {
  return dasharray.trim().split(/[\s,]+/).map(Number).filter((n) => !isNaN(n) && n > 0).reduce((sum, n) => sum + n, 0);
}
function formatNum(value) {
  return Number.isFinite(value) ? String(Math.round(value * 1e3) / 1e3) : "0";
}
function renderSVG(scene) {
  const { viewBox, background, elements, defs } = scene;
  const vb = `${viewBox.x} ${viewBox.y} ${viewBox.width} ${viewBox.height}`;
  const markerMetrics = markerMetricsById(defs ?? []);
  const lines2 = [
    `<svg xmlns="http://www.w3.org/2000/svg" viewBox="${vb}" width="${viewBox.width}" height="${viewBox.height}">`
  ];
  if (defs && defs.length > 0) {
    lines2.push("  <defs>");
    for (const def of defs) lines2.push(`    ${def}`);
    lines2.push("  </defs>");
  }
  if (background) {
    lines2.push(
      `  <rect x="${viewBox.x}" y="${viewBox.y}" width="${viewBox.width}" height="${viewBox.height}" fill="${background}" />`
    );
  }
  for (const el of elements) {
    lines2.push(renderElement(el, 1, markerMetrics));
  }
  lines2.push("</svg>");
  return lines2.join("\n");
}
function renderElement(el, depth, markerMetrics) {
  const pad = "  ".repeat(depth);
  switch (el.type) {
    case "rect": {
      const rx = el.rx != null ? ` rx="${el.rx}"` : "";
      const fillOpacity = el.fillOpacity != null ? ` fill-opacity="${el.fillOpacity}"` : "";
      const opacity = el.opacity != null ? ` opacity="${el.opacity}"` : "";
      return `${pad}<rect x="${el.bounds.x}" y="${el.bounds.y}" width="${el.bounds.width}" height="${el.bounds.height}" fill="${fillVal(el.fill)}"${fillOpacity} stroke="${el.stroke}" stroke-width="${el.strokeWidth}"${rx}${opacity} />`;
    }
    case "circle": {
      const opacity = el.opacity != null ? ` opacity="${el.opacity}"` : "";
      return `${pad}<circle cx="${el.center.x}" cy="${el.center.y}" r="${el.radius}" fill="${fillVal(el.fill)}" stroke="${el.stroke}" stroke-width="${el.strokeWidth}"${opacity} />`;
    }
    case "text": {
      const anchor = el.anchor != null ? ` text-anchor="${el.anchor}"` : "";
      const weight = el.fontWeight != null ? ` font-weight="${el.fontWeight}"` : "";
      const opacity = el.opacity != null ? ` opacity="${el.opacity}"` : "";
      return `${pad}<text x="${el.position.x}" y="${el.position.y}" font-size="${el.fontSize}" font-family="${escapeAttr(el.fontFamily)}" fill="${fillVal(el.fill)}"${anchor}${weight}${opacity}>${escapeXml(el.content)}</text>`;
    }
    case "path": {
      const drawLength = el.animated === "draw" ? pathLengthApprox(el.d) : void 0;
      const dashValue = drawLength != null ? `${drawLength} ${drawLength}` : el.strokeDasharray;
      const dash = dashValue != null ? ` stroke-dasharray="${escapeAttr(dashValue)}"` : "";
      const mEnd = el.markerEnd != null ? ` marker-end="url(#${escapeAttr(el.markerEnd)})"` : "";
      const mStart = el.markerStart != null ? ` marker-start="url(#${escapeAttr(el.markerStart)})"` : "";
      const fill = ` fill="${fillVal(el.fill)}"`;
      const opacity = el.opacity != null ? ` opacity="${el.opacity}"` : "";
      const stroke = el.animated === "flow" ? `url(#${flowGradientId(el.d, el.stroke)})` : el.stroke;
      const attrs = `${pad}<path d="${escapeAttr(el.d)}" stroke="${escapeAttr(stroke)}" stroke-width="${el.strokeWidth}"${fill}${dash}${mEnd}${mStart}${opacity}`;
      if (el.animated === "march" && el.strokeDasharray) {
        const values = marchDashoffsetValues(el.strokeDasharray);
        const animate = `<animate attributeName="stroke-dashoffset" from="${values.from}" to="${values.to}" dur="${animationDuration("march")}" repeatCount="indefinite"/>`;
        return `${attrs}>
${pad}  ${animate}
${pad}</path>`;
      }
      if (el.animated === "draw") {
        const values = drawDashoffsetValues(drawLength ?? pathLengthApprox(el.d));
        const animate = `<animate attributeName="stroke-dashoffset" values="${values.join(";")}" dur="${animationDuration("draw")}" repeatCount="indefinite"/>`;
        return `${attrs}>
${pad}  ${animate}
${pad}</path>`;
      }
      if (el.animated === "pulse") {
        const values = pulseStrokeWidthValues(el.strokeWidth).map(formatNum);
        const animate = `<animate attributeName="stroke-width" values="${values.join(";")}" dur="${animationDuration("pulse")}" repeatCount="indefinite"/>`;
        return `${attrs}>
${pad}  ${animate}
${pad}</path>`;
      }
      if (el.animated === "glow") {
        const animate = `<animate attributeName="stroke-opacity" values="${glowStrokeOpacityValues().join(";")}" dur="${animationDuration("glow")}" repeatCount="indefinite"/>`;
        return `${attrs}>
${pad}  ${animate}
${pad}</path>`;
      }
      if (el.animated === "colorcycle") {
        const animate = `<animate attributeName="stroke" values="${colorCycleStrokeValues().join(";")}" dur="${animationDuration("colorcycle")}" repeatCount="indefinite"/>`;
        return `${attrs}>
${pad}  ${animate}
${pad}</path>`;
      }
      if (el.animated === "flow") {
        const gradient = renderFlowGradient(el, pad);
        return `${gradient}
${attrs} />`;
      }
      if (el.animated === "particle") {
        const spec = motionParticleSpecs("particle")[0];
        const motionPath = trimMotionPathForArrowhead(
          el.d,
          motionArrowheadClearance(el, spec.radius, markerMetrics)
        );
        const circle = renderMotionCircle(motionPath, el.stroke, spec, "particle", pad);
        return `${attrs} />
${circle}`;
      }
      if (el.animated === "comet") {
        const specs = motionParticleSpecs("comet");
        const motionPath = trimMotionPathForArrowhead(
          el.d,
          motionArrowheadClearance(el, specs[0].radius, markerMetrics)
        );
        const circles = specs.map((spec) => renderMotionCircle(motionPath, el.stroke, spec, "comet", pad)).join("\n");
        return `${attrs} />
${circles}`;
      }
      if (el.animated === "stream") {
        const specs = motionParticleSpecs("stream");
        const motionPath = trimMotionPathForArrowhead(
          el.d,
          motionArrowheadClearance(el, specs[0].radius, markerMetrics)
        );
        const circles = specs.map((spec) => renderMotionCircle(motionPath, el.stroke, spec, "stream", pad)).join("\n");
        return `${attrs} />
${circles}`;
      }
      return `${attrs} />`;
    }
    case "icon": {
      return renderIcon(el, depth);
    }
    case "group": {
      const id = el.id != null ? ` id="${escapeAttr(el.id)}"` : "";
      const transform = el.transform != null ? ` transform="${escapeAttr(el.transform)}"` : "";
      const opacity = el.opacity != null ? ` opacity="${el.opacity}"` : "";
      if (el.children.length === 0) {
        return `${pad}<g${id}${transform}${opacity} />`;
      }
      const inner = el.children.map((c) => renderElement(c, depth + 1, markerMetrics)).join("\n");
      return `${pad}<g${id}${transform}${opacity}>
${inner}
${pad}</g>`;
    }
  }
}
var iconEmitCounter = 0;
function renderIcon(el, depth) {
  const pad = "  ".repeat(depth);
  const { icon, x, y, size } = el;
  const { viewBox, transforms, colorMode } = icon;
  const vbW = viewBox.width;
  const vbH = viewBox.height;
  const vbL = viewBox.left;
  const vbT = viewBox.top;
  const scale = Math.min(size / vbW, size / vbH);
  const scaledW = formatNum(vbW * scale);
  const scaledH = formatNum(vbH * scale);
  const offX = formatNum(x + (size - vbW * scale) / 2);
  const offY = formatNum(y + (size - vbH * scale) / 2);
  const styleAttr = colorMode === "monochrome" && el.color ? ` style="color:${escapeAttr(el.color)}"` : "";
  const opacityAttr = el.opacity != null ? ` opacity="${el.opacity}"` : "";
  const body = colorMode === "brand" ? namespaceIconIds(icon.body, `icn${iconEmitCounter++}`) : icon.body;
  const cx = vbL + vbW / 2;
  const cy = vbT + vbH / 2;
  const innerTransform = buildIconTransform(transforms, cx, cy);
  const vbAttr = `${vbL} ${vbT} ${formatNum(vbW)} ${formatNum(vbH)}`;
  const inner = innerTransform ? `${pad}  <g transform="${innerTransform}">${body}</g>` : `${pad}  ${body}`;
  return [
    `${pad}<svg x="${offX}" y="${offY}" width="${scaledW}" height="${scaledH}" viewBox="${vbAttr}"${styleAttr}${opacityAttr}>`,
    inner,
    `${pad}</svg>`
  ].join("\n");
}
function buildIconTransform(t, cx, cy) {
  if (t.rotate === 0 && !t.hFlip && !t.vFlip) return null;
  const deg = t.rotate * 90;
  const sf = t.hFlip ? -1 : 1;
  const vf = t.vFlip ? -1 : 1;
  const cxs = formatNum(cx);
  const cys = formatNum(cy);
  const parts = [];
  parts.push(`translate(${cxs} ${cys})`);
  if (t.hFlip || t.vFlip) parts.push(`scale(${sf} ${vf})`);
  if (t.rotate !== 0) parts.push(`rotate(${deg})`);
  parts.push(`translate(${formatNum(-cx)} ${formatNum(-cy)})`);
  return parts.join(" ");
}
function namespaceIconIds(body, prefix) {
  const idRe = /\bid="([^"]+)"/g;
  const ids = /* @__PURE__ */ new Set();
  let m;
  while ((m = idRe.exec(body)) !== null) {
    ids.add(m[1]);
  }
  if (ids.size === 0) return body;
  let result = body;
  for (const id of ids) {
    const newId = `${prefix}-${id}`;
    result = result.split(`id="${id}"`).join(`id="${newId}"`);
    result = result.split(`url(#${id})`).join(`url(#${newId})`);
    result = result.split(`href="#${id}"`).join(`href="#${newId}"`);
    result = result.split(`href='#${id}'`).join(`href='#${newId}'`);
  }
  return result;
}
function renderMotionCircle(path, fill, spec, animation, pad) {
  const opacityAttr = spec.opacity != null ? ` opacity="${spec.opacity}"` : "";
  const beginSeconds = motionBeginSeconds(animation, spec.phase);
  const begin = beginSeconds === 0 ? "0s" : `${formatNum(beginSeconds)}s`;
  return `${pad}<circle r="${spec.radius}" fill="${escapeAttr(fill)}"${opacityAttr}>
${pad}  <animateMotion dur="${animationDuration(animation)}" begin="${begin}" repeatCount="indefinite" rotate="auto" path="${escapeAttr(path)}"/>
${pad}</circle>`;
}
var DEFAULT_MARKER_REF_X = 7;
var MIN_MOTION_SEGMENT_LENGTH = 1;
function trimMotionPathForArrowhead(path, clearance) {
  if (clearance <= 0) return path;
  const pts = pathPoints(path);
  if (pts.length < 2) return path;
  const end = pts[pts.length - 1];
  for (let i = pts.length - 2; i >= 0; i--) {
    const a = pts[i];
    const b = pts[i + 1];
    const distA = Math.hypot(end.x - a.x, end.y - a.y);
    const distB = Math.hypot(end.x - b.x, end.y - b.y);
    if (distA >= clearance && distB <= clearance) {
      const dx = b.x - a.x;
      const dy = b.y - a.y;
      const fx = a.x - end.x;
      const fy = a.y - end.y;
      const qa = dx * dx + dy * dy;
      const qb = 2 * (fx * dx + fy * dy);
      const qc = fx * fx + fy * fy - clearance * clearance;
      const discriminant = qb * qb - 4 * qa * qc;
      if (qa > 0 && discriminant >= 0) {
        const roots = [
          (-qb - Math.sqrt(discriminant)) / (2 * qa),
          (-qb + Math.sqrt(discriminant)) / (2 * qa)
        ].filter((t2) => t2 >= 0 && t2 <= 1);
        const t = roots.sort((left, right) => right - left)[0];
        if (t != null) {
          return [...pts.slice(0, i + 1), { x: a.x + dx * t, y: a.y + dy * t }].map((pt, j) => `${j === 0 ? "M" : "L"} ${formatNum(pt.x)} ${formatNum(pt.y)}`).join(" ");
        }
      }
    }
  }
  let totalLength = 0;
  const segmentLengths = [];
  for (let i = 1; i < pts.length; i++) {
    const a = pts[i - 1];
    const b = pts[i];
    const segmentLength = Math.hypot(b.x - a.x, b.y - a.y);
    segmentLengths.push(segmentLength);
    totalLength += segmentLength;
  }
  if (totalLength === 0) return path;
  const targetLength = totalLength > MIN_MOTION_SEGMENT_LENGTH ? Math.max(MIN_MOTION_SEGMENT_LENGTH, totalLength - clearance) : totalLength / 2;
  let walked = 0;
  const motionPts = [pts[0]];
  for (let i = 1; i < pts.length; i++) {
    const segmentLength = segmentLengths[i - 1];
    if (segmentLength === 0) continue;
    const a = pts[i - 1];
    const b = pts[i];
    if (walked + segmentLength >= targetLength) {
      const t = (targetLength - walked) / segmentLength;
      motionPts.push({
        x: a.x + (b.x - a.x) * t,
        y: a.y + (b.y - a.y) * t
      });
      break;
    }
    motionPts.push(b);
    walked += segmentLength;
  }
  return motionPts.map((pt, i) => `${i === 0 ? "M" : "L"} ${formatNum(pt.x)} ${formatNum(pt.y)}`).join(" ");
}
function motionArrowheadClearance(el, dotRadius, markerMetrics) {
  if (el.markerEnd == null) return 0;
  const marker = markerMetrics.get(el.markerEnd) ?? {
    refX: DEFAULT_MARKER_REF_X,
    markerUnits: "strokeWidth"
  };
  const markerBackOffset = marker.markerUnits === "strokeWidth" ? marker.refX * el.strokeWidth : marker.refX;
  return markerBackOffset + dotRadius;
}
function markerMetricsById(defs) {
  const markers = /* @__PURE__ */ new Map();
  for (const def of defs) {
    for (const match of def.matchAll(/<marker\b[^>]*>/g)) {
      const marker = match[0];
      const id = attrValue(marker, "id");
      const refX = attrNumber(marker, "refX");
      if (id == null || refX == null) continue;
      const markerUnits = attrValue(marker, "markerUnits") === "userSpaceOnUse" ? "userSpaceOnUse" : "strokeWidth";
      markers.set(id, { refX, markerUnits });
    }
  }
  return markers;
}
function attrValue(text, name) {
  return text.match(new RegExp(`\\b${name}\\s*=\\s*"([^"]+)"`))?.[1] ?? text.match(new RegExp(`\\b${name}\\s*=\\s*'([^']+)'`))?.[1];
}
function attrNumber(text, name) {
  const value = attrValue(text, name);
  if (value == null) return void 0;
  const n = Number.parseFloat(value);
  return Number.isFinite(n) ? n : void 0;
}
function renderFlowGradient(el, pad) {
  const id = flowGradientId(el.d, el.stroke);
  const { start, end } = pathEndpoints(el.d);
  const base = escapeAttr(el.stroke);
  return [
    `${pad}<defs>`,
    `${pad}  <linearGradient id="${id}" gradientUnits="userSpaceOnUse" x1="${start.x}" y1="${start.y}" x2="${end.x}" y2="${end.y}">`,
    `${pad}    <stop offset="0%" stop-color="${base}" stop-opacity="0.75"/>`,
    `${pad}    <stop offset="20%" stop-color="${base}" stop-opacity="0.85">`,
    `${pad}      <animate attributeName="offset" values="${flowStopOffsetValues(0).map((v) => `${v}%`).join(";")}" dur="${animationDuration("flow")}" repeatCount="indefinite"/>`,
    `${pad}    </stop>`,
    `${pad}    <stop offset="35%" stop-color="#FFFFFF" stop-opacity="1">`,
    `${pad}      <animate attributeName="offset" values="${flowStopOffsetValues(1).map((v) => `${v}%`).join(";")}" dur="${animationDuration("flow")}" repeatCount="indefinite"/>`,
    `${pad}    </stop>`,
    `${pad}    <stop offset="50%" stop-color="${base}" stop-opacity="0.85">`,
    `${pad}      <animate attributeName="offset" values="${flowStopOffsetValues(2).map((v) => `${v}%`).join(";")}" dur="${animationDuration("flow")}" repeatCount="indefinite"/>`,
    `${pad}    </stop>`,
    `${pad}    <stop offset="100%" stop-color="${base}" stop-opacity="0.75"/>`,
    `${pad}  </linearGradient>`,
    `${pad}</defs>`
  ].join("\n");
}
function flowGradientId(path, stroke) {
  return `triton-flow-${hashString(`${path}|${stroke}`)}`;
}
function pathEndpoints(path) {
  const pts = pathPoints(path);
  const start = pts[0] ?? { x: 0, y: 0 };
  const end = pts[pts.length - 1] ?? { x: 1, y: 0 };
  if (start.x === end.x && start.y === end.y) {
    return { start, end: { x: end.x + 1, y: end.y } };
  }
  return { start, end };
}
function hashString(value) {
  let hash = 2166136261;
  for (let i = 0; i < value.length; i++) {
    hash ^= value.charCodeAt(i);
    hash = Math.imul(hash, 16777619);
  }
  return (hash >>> 0).toString(36);
}
function fillVal(fill) {
  return fill != null && fill.trim() !== "" ? fill : "none";
}
function escapeXml(text) {
  return text.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;");
}
function escapeAttr(text) {
  return text.replace(/&/g, "&amp;").replace(/"/g, "&quot;").replace(/</g, "&lt;").replace(/>/g, "&gt;");
}
function embedRevealManifest(svg, reveal) {
  const json = JSON.stringify(reveal).replace(/<\//g, "<\\/");
  const block2 = `<script type="application/json" id="triton-reveal">${json}</script>`;
  return svg.replace("</svg>", `${block2}
</svg>`);
}
var svgRenderer = {
  name: "svg",
  render: renderSVG
};
registerDiagram("flowchart", flowchart);
registerDiagram("timeline", timeline);
registerDiagram("poster", poster);
registerDiagram("pie", pie);
registerDiagram("xychart", xychart);
registerDiagram("quadrant", quadrant);
registerDiagram("radar", radar);
registerDiagram("gantt", gantt);
registerDiagram("journey", journey);
registerDiagram("kanban", kanban);
registerDiagram("sequence", sequence);
registerDiagram("class", classDiagram);
registerDiagram("state", state);
registerDiagram("er", er);
registerDiagram("block", block);
registerDiagram("requirement", requirement);
registerDiagram("sankey", sankey);
registerDiagram("mindmap", mindmap);
registerDiagram("gitgraph", gitgraph);
registerDiagram("c4", c4);
registerDiagram("architecture", architecture);
registerDiagram("packet", packet);
registerDiagram("tree", tree);
registerDiagram("plan", plan);
registerDiagram("avl", avl);
registerDiagram("rbtree", rbtree);
registerDiagram("btree", btree);
registerDiagram("bplustree", bplustree);
registerDiagram("b+tree", bplustree);
registerDiagram("radix", radix);
registerDiagram("segtree", segtree);
registerDiagram("heap", heap);
registerDiagram("array", array);
registerDiagram("linkedlist", linkedlist);
registerDiagram("memory", memory);
registerDiagram("page", page);
registerDiagram("queue", queue);
registerDiagram("cqueue", cqueue);
registerDiagram("deque", deque);
registerDiagram("pqueue", pqueue);
registerDiagram("stack", stack);
registerDiagram("hashmap", hashmap);
registerDiagram("matrix", matrix);
registerDiagram("trie", trie);
registerDiagram("nodegraph", graph);
registerDiagram("unionfind", unionfind);
registerDiagram("topology", topology);
registerDiagram("list", list);
registerDiagram("fishbone", fishbone);
registerDiagram("pyramid", pyramid);
registerDiagram("loop", loop);
registerDiagram("merkletree", merkletree);
registerDiagram("lsmtree", lsmtree);
registerDiagram("behaviortree", behaviortree);
registerDiagram("quadtree", quadtree);
registerDiagram("treap", treap);
registerDiagram("234tree", tree234);
registerDiagram("2-3-4tree", tree234);
registerRenderer(svgRenderer);
registerRouter("straight", straightRouter);
registerRouter("orthogonal", orthogonalRouter);
registerRouter("bezier", bezierRouter);
registerRouter("polyline", polylineRouter);
function compileSync(input, themeInput, forcedThemeName, icons) {
  const compiled = compileSyncWithTheme(input, themeInput, forcedThemeName, icons);
  return compiled.ok ? ok(compiled.value.layout) : compiled;
}
function compileSyncWithTheme(input, themeInput, forcedThemeName, icons) {
  const cleaned = stripComments(input);
  const { metadata: fmMeta, body: fmBody } = extractFrontmatter(cleaned);
  const { format, diagramType } = detect(cleaned);
  const module = getModule(diagramType);
  if (!module) {
    return err("UNKNOWN_DIAGRAM", `No module registered for diagram type: ${diagramType}`);
  }
  try {
    let ir;
    if (format === "yaml") {
      ir = module.parseYaml(cleaned);
    } else {
      try {
        ir = module.parseMermaid(cleaned);
      } catch (firstErr) {
        if (Object.keys(fmMeta).length > 0) {
          ir = module.parseMermaid(fmBody);
        } else {
          throw firstErr;
        }
      }
    }
    const mergedMetadata = { ...fmMeta, ...ir.metadata ?? {} };
    ir = { ...ir, metadata: mergedMetadata };
    const themeName = typeof mergedMetadata.theme === "string" ? mergedMetadata.theme : void 0;
    const base = resolveTheme(themeInput ?? {}, getThemePreset(forcedThemeName ?? themeName));
    const withModuleDefaults = module.defaultThemeOverride ? resolveTheme(module.defaultThemeOverride, base) : base;
    const finalTheme = ir.themeOverride ? resolveTheme(ir.themeOverride, withModuleDefaults) : withModuleDefaults;
    const layoutOptions = icons !== void 0 ? { icons } : void 0;
    const result = module.layout(ir, finalTheme, layoutOptions);
    return ok({ layout: result, theme: finalTheme });
  } catch (cause) {
    const message = cause instanceof Error ? cause.message : String(cause);
    return err("PARSE_ERROR", message, cause);
  }
}
function renderSync(input, themeInput, rendererName = "svg", forcedThemeName, icons) {
  const compileResult = compileSync(input, themeInput, forcedThemeName, icons);
  if (!compileResult.ok) return compileResult;
  const renderer = getRenderer(rendererName);
  if (!renderer) {
    return err("UNKNOWN_RENDERER", `No renderer registered: ${rendererName}`);
  }
  try {
    return ok(renderer.render(compileResult.value.scene));
  } catch (cause) {
    const message = cause instanceof Error ? cause.message : String(cause);
    return err("LAYOUT_ERROR", message, cause);
  }
}
function compileAndRenderSync(input, themeInput, rendererName = "svg", forcedThemeName, icons) {
  const compileResult = compileSync(input, themeInput, forcedThemeName, icons);
  if (!compileResult.ok) return compileResult;
  const renderer = getRenderer(rendererName);
  if (!renderer) {
    return err("UNKNOWN_RENDERER", `No renderer registered: ${rendererName}`);
  }
  try {
    const { scene, anchors, reveal } = compileResult.value;
    const svg = renderer.render(scene);
    return ok(reveal ? { svg, anchors, reveal } : { svg, anchors });
  } catch (cause) {
    const message = cause instanceof Error ? cause.message : String(cause);
    return err("LAYOUT_ERROR", message, cause);
  }
}

// vendor-triton-entry.mjs
var THEME_ALIASES = {
  dark: "midnight",
  light: "default",
  contrast: "showcase",
  auto: "midnight",
  midnight: "showcase",
  blueprint: "consulting",
  editorial: "minimal"
};
function resolveThemeName(theme) {
  if (!theme) {
    return void 0;
  }
  return THEME_ALIASES[theme] ?? theme;
}
function injectTheme(text, theme) {
  const resolvedTheme = resolveThemeName(theme);
  if (!resolvedTheme) {
    return text;
  }
  return text.startsWith("---\n") ? text.replace(/^---\n([\s\S]*?)\n---\n?/, (_match, frontmatter) => {
    const lines2 = frontmatter.split(/\r?\n/);
    let replaced = false;
    const next = lines2.map((line) => {
      if (/^\s*theme\s*:/.test(line)) {
        replaced = true;
        return "theme: " + resolvedTheme;
      }
      return line;
    });
    if (!replaced) {
      next.push("theme: " + resolvedTheme);
    }
    return "---\n" + next.join("\n") + "\n---\n";
  }) : "---\ntheme: " + resolvedTheme + "\n---\n" + text;
}
function detectDiagramType(text) {
  const result = compileSync(text);
  return result.ok ? "known" : "unknown";
}
function renderMermaid(text, options = {}) {
  const themedText = injectTheme(text, options.theme ?? "midnight");
  const result = compileAndRenderSync(themedText, void 0, options.format ?? "svg");
  if (!result.ok) {
    return { svg: void 0, warnings: [result.error.message], kind: "unknown" };
  }
  let svg = result.value.svg;
  const reveal = result.value.reveal;
  if (reveal) {
    svg = embedRevealManifest(svg, reveal);
  }
  return { svg, warnings: [], kind: "known", reveal };
}
export {
  compileAndRenderSync,
  compileSync,
  detectDiagramType,
  renderMermaid,
  renderSync
};
